# TawanFarm App — ประวัติการแก้บัค/ฟีเจอร์ฉบับเต็ม (Archive)

ย้ายมาจาก CLAUDE.md เพื่อลดขนาดไฟล์ที่โหลดเข้า context ทุกครั้ง — เปิดไฟล์นี้เมื่อต้องการอ้างอิงเหตุผล/รายละเอียดของการแก้ไขในอดีต (entry #1–210)

### 210. เลขรอบซ้ำในสรุปรอบเลี้ยง + ล้างวันลงกุ้งของบ่อที่ยกเลิกไม่ได้ (แก้แล้ว)

พี่แจ้ง 2 จุด

**1. บ่อที่มีมากกว่า 1 รอบ แสดง "รอบที่ 1" ทั้ง 2 รอบ**

ข้อมูลจริงมี `cropNum` เป็น **1 ซ้ำกันทั้ง 2 รายการ** (เกิดได้จากรอบที่กรอกย้อนหลังเองผ่าน "เพิ่มข้อมูลรอบเลี้ยงเก่า" หรือรอบที่เปลี่ยนวันลงกุ้งโดยไม่ผ่านการปิดรอบ ทำให้ `savePond()` ไม่ได้คำนวณเลขรอบใหม่ — `wasIdle` เป็น false)

ซ้ำร้าย `renderCrops()` **เรียงด้วย `cropNum`** ด้วย พอค่าซ้ำกันลำดับก็มั่วตามไปด้วย

แก้ 2 ชั้น:
- **เรียงตามวันที่** (`startDate` → `closeDate`) แทน `cropNum` — ลำดับถูกเสมอไม่ว่าข้อมูลจะเพี้ยนแค่ไหน
- **ป้าย "รอบ N" นับจากลำดับจริงในปีนั้น** (`i+1`) ไม่ใช่ `c.cropNum` · ถ้าค่าที่บันทึกไว้ไม่ตรงกับลำดับ ต่อท้ายด้วย `(บันทึกไว้ว่ารอบ 1)` สีเหลือง + tooltip บอกให้ไปแก้ที่ปุ่ม ✏️ (ที่เพิ่มให้ตอน entry #206) — **ไม่ไปแก้ข้อมูลใน Firebase เอง** ให้พี่ตัดสินใจ

**2. บ่อ A.16 / A.17 ยกเลิกการลงกุ้งแล้ว แต่ล้างวันที่ออกไม่ได้**

`savePond()` มี `if(!name||!stock||!size||!startDate){alert('กรุณากรอกข้อมูลให้ครบ');return}` — **บังคับ `startDate` และ `stock` เสมอ** บ่อที่ยกเลิกการลงกุ้งจึงค้างวันลงกุ้งเก่าไว้ตลอด แล้วโผล่เป็น "กำลังเลี้ยง" ทั้งที่ไม่มีกุ้ง

แก้:
- บังคับแค่ **ชื่อบ่อ + ขนาดบ่อ** · `startDate`/`stock` เว้นว่างได้
- ใส่วันที่แล้วต้องมียอดสั่งซื้อ (กันกรอกครึ่งๆ) พร้อมบอกทางออกในข้อความเตือนว่า "ถ้าจะยกเลิกให้ล้างช่องวันที่"
- **เตือนถ้าบ่อยังมีข้อมูลรอบปัจจุบันค้างอยู่** (ค่าน้ำ/อาหาร/สุ่ม/ฯลฯ) ว่าล้างวันที่แล้วข้อมูลจะกำพร้า ไม่ถูกนับเข้ารอบไหน — ตอบยกเลิกได้ ข้อมูลไม่เปลี่ยน · ล้างข้อมูลกำพร้าต่อได้ด้วยปุ่ม 🧹 ที่มีอยู่แล้วในหน้าจัดการบ่อ
- `wasIdle&&startDate` — ล้างวันที่ทิ้งไม่ใช่การเริ่มรอบใหม่ ไม่ให้ `computeNextCrop()` วิ่ง · บ่อสร้างใหม่แบบไม่ใส่วันที่ก็ไม่คำนวณเลขรอบเช่นกัน
- `stock:stock||0` กัน `NaN` ตอนเว้นว่าง (`parseInt('')` = NaN)
- เพิ่ม hint ใต้ช่องทั้งสอง: "เว้นว่างได้ถ้ายังไม่ลงกุ้ง / ยกเลิกการลง" และ "ล้างช่องนี้ = ยกเลิกการลงกุ้ง (บ่อจะกลายเป็นบ่อว่าง)"

**ผลพลอยได้**: หน้าจัดการบ่อเดิมโชว์ยอดลูกกุ้ง + ความหนาแน่นของบ่อว่างเป็นตัวเลขเหมือนมีกุ้งอยู่ (`563 ตัว/ตรม.`) เปลี่ยนเป็น `-` เมื่อไม่มีวันลงกุ้ง

ทดสอบผ่าน browser (stub `save()`/`fbSave()`, ลบข้อมูลทดสอบครบ): บ่อที่มี `cropNum` = 1 ทั้ง 2 รายการ แสดง **"รอบ 1" / "รอบ 2"** ถูกต้องตามลำดับวันที่ + ป้ายเตือนค่าที่บันทึกผิด · ล้างวันที่ของบ่อที่ไม่มีข้อมูลค้างได้ทันที ไม่มี alert · บ่อที่มีข้อมูลค้าง 3 รายการขึ้นเตือน ตอบยกเลิกแล้วข้อมูลไม่เปลี่ยน ตอบตกลงแล้วล้างได้ · ใส่วันที่แต่ไม่ใส่ยอดสั่งซื้อยังเตือนถูก · หน้าจัดการบ่อโชว์บ่อว่างเป็น `-` และบ่อที่มีกุ้งยังโชว์ครบเหมือนเดิม · ไม่มี console error

### 209. ตรวจ flow ปิดบ่อก่อน A.1 ปิดจริง — เจอรายได้/อัตรารอด นับพาเชียลซ้ำ (แก้แล้ว)

พี่สั่งตรวจ flow ระหว่างการ์ด "รอบเลี้ยง" กับ "จับกุ้ง" ก่อน A.1 ปิดบ่ออาทิตย์หน้า + ไม่ให้ยึด 2 ครอป/ปี + เรียงบ่อ A.1–A.27

**การเชื่อมต่อ 2 หน้า — ปกติดีอยู่แล้ว**: หน้าจับกุ้ง เลือก "ปิดรอบ" → ปุ่มเปิด `openCropModalFromHarvest()` → `openCropModal()` ฟอร์มเดียวกับปุ่ม "ปิดรอบ" ในหน้ารอบเลี้ยง · พาเชียลที่บันทึกไว้ไหลเข้าฟอร์มปิดบ่อผ่าน `partialTotals()` ครบ · ตอนปิด `confirmCloseCrop()` archive ทุก log รวม `partialHarvests` เข้า `cropHistory` ครบ

**🔴 บั๊กร้ายแรง: นับพาเชียลซ้ำ 2 รอบ (รายได้ + จำนวนตัว)**

`calcCropEcon()` คำนวณช่อง "รายได้" และ "ตัวที่จับได้" เป็น **ยอดรวมทั้งรอบ (ปิดบ่อ + เกรดพิเศษ + พาเชียล)** มาให้แล้ว และปุ่ม "ใช้ตัวเลขที่คำนวณ" ก็เติมค่านั้น (แถมป้ายบอก "✓ ตรงกับที่คำนวณ" เชียร์ให้ใช้) — แต่ `cropHarvestResult()` **บวกพาเชียลเข้าไปอีกรอบตอนแสดงผล**

ทดสอบจำลอง A.1 (ปิดบ่อ 8,000 กก. × ฿170 + พาเชียล 2 ครั้ง 10,000 กก. ฿1,550,000):

| | ในฟอร์ม (ถูก) | ตารางประวัติ (ก่อนแก้) | เกินไป |
|---|---|---|---|
| รายได้ | ฿2,910,000 | **฿4,460,000** | +฿1,550,000 |
| กำไรสุทธิ | ฿2,760,000 | **฿4,310,000** | +฿1,550,000 |
| ตัวที่จับได้ | 615,000 | **990,000** | +375,000 |
| อัตรารอด | 61.5% | **99%** | +37.5% |

**แก้แล้ว** — ติดธง `revenueIncludesPartial:true` / `caughtIncludesPartial:true` ตอนบันทึก แล้ว `cropHarvestResult()` บวกพาเชียล**เฉพาะรอบที่ยังไม่ได้รวมมา**
- รอบเก่าที่ไม่มีธง: ตรวจซ้ำด้วยว่ายอดตรงกับ "ปิดบ่อ+เกรด+พาเชียล" พอดีไหม (= ค่าที่ปุ่มคำนวณเติมให้) ถ้าตรงแปลว่ารวมมาแล้ว ห้ามบวกซ้ำ · เช็คเฉพาะตอนมีราคา/ไซส์ ไม่งั้น closeOnly เป็น 0 แล้วเดาผิด
- **ทดสอบรอบเก่าแบบเดิม (revenue = ปิดบ่ออย่างเดียว ไม่มีธง) ยังบวกพาเชียลให้ถูกต้องเหมือนเดิม** (1,360,000 → 2,110,000 · caught 240,000 → 440,000)

⚠️ FCR ไม่ได้รับผลกระทบ เพราะ `hr.totalWeight` เก็บเฉพาะกุ้งปกติวันปิดบ่อ (ไม่รวมพาเชียล) `cropHarvestResult` จึงบวก `pt.kg` ได้ถูกต้องอยู่แล้ว — **นิยามของ 3 ฟิลด์นี้ไม่ตรงกัน** (`totalWeight` = ปิดบ่ออย่างเดียว แต่ `revenue`/`caught` = รวมทั้งรอบ) ต้นเหตุที่ทำให้พลาดตั้งแต่แรก

**หน้าต่าง "ปิดรอบสำเร็จ" โชว์ผลผลิตน้อยกว่าจริง**: ใช้ `hr.totalWeight` (8,000 กก.) ทั้งที่จริง 18,000 กก. — เปลี่ยนเป็นอ่านผ่าน `cropHarvestResult()` แล้วโชว์ `18,000 กก. (รวมพาเชียล)`

**ไม่ยึด 2 ครอป/ปี อีกต่อไป**

- ตารางเดิมมีแค่ 2 ช่อง (`colspan=3` × 2) = โชว์ได้สูงสุด 2 รอบ/บ่อ/ปี **บ่อที่เลี้ยง 3 ครอปจะมีรอบหายไปจากตารางเงียบๆ** → เปลี่ยนเป็นลิสต์ทุกรอบในช่องเดียว รองรับกี่รอบก็ได้ (รอบที่กำลังเลี้ยง/เตรียมบ่อ ต่อท้ายเสมอ) · ลบ `cropCells()` ที่ไม่ได้ใช้แล้วทิ้ง
- KPI เดิม "บ่อทันเป้า / บ่อล่าช้า / เป้ารวม = บ่อ×2" → เปลี่ยนเป็น **รอบที่ปิดแล้ว · กำลังเลี้ยง · เตรียมบ่อ/บ่อว่าง · เฉลี่ยรอบ/บ่อ (สูงสุด n)** รายงานความจริงแทนการตัดสินว่าทันเป้าไหม
- ช่อง "ปิดแล้ว" เดิม `n/2` → เหลือ `n` เฉยๆ · สถานะเดิม "✅ ครบ / ⏳ 1/2 / ⚠️ ยังไม่มี" → เปลี่ยนเป็น 🦐 กำลังเลี้ยง / 🧹 เตรียมบ่อ / ✅ ปิดแล้ว n รอบ / ⚪ บ่อว่าง
- ข้อความหัวหน้า "เป้าหมาย: 2 รอบ/ปี/บ่อ" → "แสดงทุกรอบที่เลี้ยงในปีนั้น (บางบ่อทำได้ 3 รอบ/ปี)"

**เรียงบ่อ A.1 → A.27** — `renderCrops()` และ `renderManage()` เดิมใช้ลำดับดิบใน `D.ponds` (ตามลำดับที่ถูกสร้าง) เปลี่ยนเป็น `sortedPonds()` ที่แยกตัวอักษร/ตัวเลข → A.1 → A.2 → A.10 → A.27 (ไม่ใช่ A.1, A.10, A.2, A.27 แบบเรียงสตริง) · **ไม่แตะหน้าภาพรวม** ที่ตั้งใจเรียงตามอายุกุ้งให้ตรงกับหน้ากรอกน้ำ/อาหาร

ทดสอบผ่าน browser (stub `save()`/`fbSave()`, ปิดบ่อจริงผ่าน `confirmCloseCrop()` แล้วลบข้อมูลทดสอบครบ): รายได้/กำไร/ตัวที่จับ/อัตรารอด ในตารางประวัติ **ตรงกับที่กรอกในฟอร์มเป๊ะทุกตัว** · ผลผลิตรวม 18,000 กก. · FCR 1.56 · รอบเก่าไม่มีธงยังบวกพาเชียลถูก · บ่อ 3 ครอปแสดงครบทั้ง 3 รอบ · ลำดับบ่อ A.1 → A.2 → A.10 → A.27 · ไม่มี console error

### 208. ตะวันปาล์ม ภาพรวม — การ์ดสรุปธุรกิจสำหรับเจ้าของ + แก้บั๊กรายจ่ายส่วนกลางหาย (แก้แล้ว)

พี่ให้วิเคราะห์ว่าแท็บภาพรวมปาล์ม (เจ้าของ/ผู้จัดการดูเป็นหลัก staff เข้าไม่ได้) ควรมีข้อมูลอะไรอีก แล้วสั่งทำทั้ง 5 ข้อที่เสนอ + แก้บั๊กที่เจอระหว่างวิเคราะห์

**🔴 บั๊กที่เจอจาก entry #207 (การ์ดสรุปรายแปลงที่เพิ่งทำ)**

ตอนบันทึกรายจ่ายมีตัวเลือก **"ไม่ระบุ / รวมทั้งสวน"** (`plotId` ว่าง เช่นค่าน้ำมันรถตัดที่ใช้ร่วมกัน) — การ์ดรายแปลงกรองด้วย `plotId` รายจ่ายพวกนี้จึงหายไปหมด → **แถบรวมบอกกำไร ฿55,000 แต่ KPI ด้านบนหน้าเดียวกันบอก ฿48,000** ต่างกันเท่ากับรายจ่ายส่วนกลางพอดี (ทดสอบยืนยันแล้ว)

แก้โดยให้แถบรวมอ่านจาก `palmYearStats()` (ยอดจริงทั้งสวน) แทนการบวกจากรายแปลงเอง + เพิ่มบรรทัดอธิบายว่ามีรายจ่ายส่วนกลางเท่าไร ที่ไม่ได้ถูกหักในการ์ดรายแปลง (ไม่งั้นเจ้าของบวกกำไรรายแปลงเองแล้วไม่ตรง) · เปลี่ยนป้ายรายแปลงเป็น "รายจ่าย/กำไร **ของแปลงนี้**" ให้ชัดว่าเป็นเฉพาะที่ผูกกับแปลง

**เพิ่ม single source of truth 3 ตัว** (ห้ามคำนวณคู่ขนาน):
- `palmYearStats(year)` — kg/รายรับ/รายจ่าย/รายจ่ายส่วนกลาง/กำไร/ราคาเฉลี่ย/ต้นทุนต่อกก./กำไรต่อกก. ของปีนั้น
- `palmFundSummary()` — ฐานะกองทุนปาล์ม (สูตรเดียวกับ `renderFundPane()` ยกมาโชว์ ไม่คำนวณใหม่)
- `palmLatestSale()` — การขายครั้งล่าสุดที่มีราคา/กก.

**การ์ดใหม่ "💼 สรุปธุรกิจปีนี้ (สำหรับเจ้าของ/ผู้จัดการ)"** — `renderPalmOvBiz()`

1. **เทียบปีต่อปี** 4 กล่อง (ผลผลิต/ราคาเฉลี่ย/รายรับ/กำไร) พร้อม ▲▼% และยอดปีที่แล้วใต้ตัวเลข
   - ⚠️ ลูกศรโผล่เฉพาะเมื่อปีที่แล้ว**มีข้อมูลจริง** (`prev.hasData` + `before>0`) ไม่งั้นจะได้ +∞ หรือเทียบกับศูนย์ · ปีแรกขึ้นข้อความ "ปี xxxx ยังไม่มีข้อมูลให้เทียบ" แทน
   - ราคาเฉลี่ยลดลงถือว่าแย่ (ไม่ invert) ตรงกับสัญชาตญาณเจ้าของ
2. **กำไรต่อกิโล** — `เงินเข้าจริง ฿5.33 − ต้นทุน ฿1.61 = ฿3.72/กก. (อัตรากำไร 70%)`
   - ⚠️ **"เงินเข้าจริง/กก." ไม่เท่ากับ "ราคาเฉลี่ย" ในกล่องด้านบน** (5.33 vs 5.55) เพราะ `amount` เป็นรายรับสุทธิหลังหักค่าจ้างทีมตัด ส่วน `pricePerKg` เป็นราคาก่อนหัก — **ต้องเขียนอธิบายไว้ในการ์ด** ไม่งั้นดูเหมือนตัวเลขขัดกันเองในการ์ดเดียว (เจอตอนรีวิว screenshot รอบแรก)
   - ซ่อนทั้งบล็อกถ้าปีนี้ยังไม่ได้ขาย (`kg>0`) ไม่งั้นหารศูนย์
3. **ราคาล่าสุด vs ค่าเฉลี่ยปีนี้** — `฿5.20 (20 ส.ค. · แปลง 2) ▼ ต่ำกว่าค่าเฉลี่ยปีนี้ ฿0.35` ใช้ตัดสินใจว่าจะรีบตัดหรือรอราคา
4. **เงินในกองทุนปาล์ม** — `฿224,000 (NAV 1 ส.ค.) +฿24,000 (+12.0%)` เดิมเงินที่ย้ายไปกองทุนหายจากภาพรวม เจ้าของเห็นแค่ "กำไร" ไม่เห็นทรัพย์สินรวม · ซ่อนถ้ายังไม่เคยซื้อ · เตือนถ้ายังไม่อัปเดต NAV

**การ์ดรายแปลง เพิ่ม "⏱️ ตัดล่าสุด กี่วันที่แล้ว"** — ปาล์มปกติตัดทุก ~15–20 วัน ทิ้งช่วงนาน = ผลผลิตค้างต้น/ลืมตัด · 🟢 ≤20 วัน · 🟡 21–30 · 🔴 >30 เป็นตัวเลขที่ต้องลงมือทำ ไม่ใช่แค่ดู

ทดสอบผ่าน browser (stub `save()`/`fbSave()`, ข้อมูลจำลอง 2 ปี 2 แปลง + กองทุน แล้วลบทิ้ง): เลขตรงทุกตัวกับที่คำนวณมือ (ปีนี้ 23,000 กก. · ราคาเฉลี่ย ฿5.55 · ต้นทุน ฿1.61 · กำไร ฿3.72/กก. · ปีที่แล้ว 18,000 กก. ฿5.90 → ▲+28% / ▼−6% / ▲+20% / ▲+4% · กองทุน 20,000 หน่วย × NAV 11.2 = ฿224,000 กำไร +12%) · **แถบรวมตรงกับ KPI ด้านบนแล้ว (฿85,500 ทั้งคู่)** และรายแปลงบวกกัน 92,500 − ส่วนกลาง 7,000 = 85,500 ตรงตามที่บรรทัดอธิบายบอก · **เคสขอบ**: ปีแรกไม่มีลูกศร/ไม่มีบรรทัดปีที่แล้ว · ไม่มีกองทุนซ่อนแถบ · ปีนี้ยังไม่ขายซ่อนบล็อกกำไรต่อกิโลและไม่พัง · **staff เข้า `sec-palm` ไม่ได้ (goTab + palmTab กันไว้ 2 ชั้น ไม่กระทบ)** · มือถือ 375px กล่องเรียง 2 คอลัมน์อ่านได้ · ไม่มี console error

### 207. ตะวันปาล์ม หน้าภาพรวม — เพิ่มราคาขายรายเดือน + สรุปรายแปลงปีนี้ (เพิ่มใหม่)

พี่สั่ง 2 อย่าง: (1) กราฟผลผลิตรายเดือนให้โชว์ราคาขายด้วย จะได้รู้ว่าเดือนไหนราคาดีที่สุด (2) เพิ่มสรุปเบื้องต้นของทั้ง 2 แปลงในหน้าภาพรวม — ปีนี้ตัดไปกี่กิโล ได้เงินเท่าไร ใส่ปุ๋ยไปเท่าไร

**1. กราฟ "แนวโน้มผลผลิต (12 เดือนล่าสุด)" → กราฟผสม 2 แกน**

- แท่งเขียว = น้ำหนักที่ขายได้ (กก.) แกนซ้าย (ของเดิม เปลี่ยนจากเส้นเป็นแท่ง)
- เส้นส้ม = **ราคาเฉลี่ย ฿/กก.** แกนขวา (ใหม่) · จุดเดือนที่ราคาดีที่สุดเป็นสีเขียวใหญ่กว่าเพื่อน
- ป้ายสรุปเหนือกราฟ: `⭐ ราคาดีที่สุดใน 12 เดือนนี้: มิ.ย. 69 — ฿6.72/กก.`
- `spanGaps:true` เดือนที่ไม่ได้ตัดเส้นจะลากข้ามไป ไม่ขาดเป็นท่อนๆ

⚠️ **ราคาเฉลี่ยถัวแบบถ่วงน้ำหนักด้วย กก. ของแต่ละครั้ง** (ครั้งที่ตัดเยอะมีผลต่อค่าเฉลี่ยมากกว่า) และ **ใช้ `pricePerKg` ที่กรอกไว้ตอนบันทึกโดยตรง ไม่คำนวณจาก `amount/weightKg`** เพราะ `amount` คือรายรับ **สุทธิหลังหักค่าจ้างทีมตัดแล้ว** (แต่ละเจ้าคิดไม่เท่ากัน) — หลักเดียวกับ `renderPalmHarvestChart()` ในแท็บประวัติตัดปาล์ม ต่างกันแค่ที่นั่นรวมทุกปีตามเดือนปฏิทิน ที่นี่ไล่ 12 เดือนจริงย้อนหลัง ตอบโจทย์ "เดือนไหนในปีนั้นๆ" ที่พี่ถาม

**2. การ์ดใหม่ "🌴 สรุปรายแปลง (ปีนี้)"** — `renderPalmOvPlots(year)` เรียกจาก `renderPalmOverview()`

แต่ละแปลงโชว์ 8 บรรทัด: ตัดไปแล้ว (กก.) · รายรับสุทธิ · **ราคาเฉลี่ย ฿/กก.** · ใส่ปุ๋ยไปแล้ว (กก. + กี่ครั้ง) · รายจ่ายปีนี้ · กำไรสุทธิปีนี้ · ผลผลิต/ไร่ · จำนวนครั้งที่ตัด + วันที่ตัดล่าสุด

ปิดท้ายด้วยแถบรวมทุกแปลง (ตัดไปแล้ว / รายรับสุทธิ / ปุ๋ยที่ใส่ / กำไรสุทธิ) โผล่เมื่อมีมากกว่า 1 แปลง — ตรวจแล้วตรงกับ KPI ด้านบนของหน้าเป๊ะ (฿122,000 / ฿92,000)

- นับเฉพาะปีปัจจุบัน (`date.startsWith(year)`) ต่างจากการ์ดในแท็บ "แปลงปาล์ม" ที่เป็นยอดสะสมตั้งแต่เริ่ม — จึงไม่ซ้ำซ้อนกัน
- อ่านจาก `D.palmMoney` / `D.palmFertilizer` ชุดเดียวกับแท็บอื่น ไม่คำนวณคู่ขนาน
- ⚠️ **ค่าปุ๋ยไม่ถูกนับซ้ำ**: `savePalmFert()` สร้าง expense ใน `palmMoney` ให้อัตโนมัติอยู่แล้ว การ์ดนี้จึงอ่าน กก. จาก `palmFertilizer` แต่อ่านเงินจาก `palmMoney` อย่างเดียว

ทดสอบผ่าน browser (stub `save()`/`fbSave()`, สร้างข้อมูลปาล์มจำลอง 2 แปลงแล้วลบทิ้ง): ราคาเฉลี่ยถ่วงน้ำหนักถูกต้อง (มิ.ย. = (6.80×6,000 + 6.60×4,000)/10,000 = ฿6.72) · แปลง 1 ตัด 14,000 กก. ราคาเฉลี่ย ฿5.49 กำไร ฿54,500 ตรงตามที่คำนวณมือ · ยอดรวมตรงกับ KPI ด้านบน · **เคสขอบ**: แปลงที่ยังไม่ตัด/ไม่ใส่ปุ๋ยขึ้น "— ยังไม่ได้ตัด/ใส่" ไม่ใช่ 0 เปล่าๆ · บันทึกที่ไม่ได้กรอกราคา/กก. เส้นราคาไม่พัง ขึ้นข้อความบอกให้กรอก · ไม่มีแปลงเลยขึ้นข้อความแนะนำ · **staff ยังเปิดแท็บปาล์มไม่ได้ (role gate เดิม entry #182 ไม่กระทบ)** · มือถือ 375px การ์ดเรียงซ้อนอ่านได้ · ไม่มี console error

### 206. แก้ "รอบที่/ปี" ของรอบเลี้ยงที่ปิดแล้วไม่ได้ — เพิ่มช่องแก้ในฟอร์มแก้ไข (แก้แล้ว)

พี่แจ้งว่าประวัติการเลี้ยงบ่อ A.2 รอบที่ 1/2536 ใส่ผิด ที่ถูกคือ 1/2568

**สาเหตุ**: `renderPdCropHistCard()` โชว์ปีจาก `c.cropYear` ตรงๆ ผ่าน `yearBE()` ซึ่งแปลงปี ค.ศ. เป็น พ.ศ. โดยบวก 543 — ถ้า `cropYear` ที่บันทึกไว้ผิดเป็น ค.ศ. 1993 (แทนที่จะเป็น 2025) จะโชว์ 1993+543 = **2536** แทนที่จะเป็น 2568

**ปัญหาที่แท้จริง**: ฟอร์ม "✏️ แก้ไขข้อมูลปิดบ่อ" (`editCropModal`) **ไม่มีช่องแก้ "รอบที่/ปี" เลยสำหรับรอบที่มีอยู่แล้ว** — ช่องนี้มีอยู่แต่ถูกซ่อนไว้เฉพาะโหมด "➕ เพิ่มข้อมูลรอบเก่าที่ตกหล่น" (`openAddCropModal`, idx===-1) เท่านั้น พอเป็นรอบที่มีอยู่แล้ว (`openEditCropModal`, idx>=0) `saveEditCrop()` ไม่แตะ `cropNum`/`cropYear` เลย พี่จึงพิมพ์ผิดแล้วแก้เองไม่ได้ต้องมาแจ้งให้ช่วย

**แก้แล้ว** — แยกช่อง "รอบที่ / ปี" ออกจากบล็อก "เลือกบ่อ" (ที่ยังซ่อนไว้เฉพาะโหมดเพิ่มรอบเก่า เพราะรอบที่แก้อยู่แล้วรู้บ่ออยู่แล้วไม่ต้องเลือกซ้ำ) ให้ **โชว์เสมอทั้ง 2 โหมด**:

- `openEditCropModal()` เติมค่าเดิม `c.cropNum` / `yearBE(c.cropYear||c.year)` ลงช่องให้แก้ได้
- `saveEditCrop()` โหมดแก้รอบที่มีอยู่ (idx>=0) เพิ่ม validate รอบที่/ปีห้ามว่าง + เช็คซ้ำกับรอบอื่นของบ่อเดียวกัน (ตัด self-match ด้วย `x!==c`) เหมือนโหมดเพิ่มรอบเก่า แล้วเขียน `c.cropNum`/`c.cropYear`(=ค.ศ.)/`c.year` ให้ตรงกัน

ทดสอบผ่าน browser (stub `save()`/`fbSave()`, จำลอง `cropYear:1993` แบบพี่เจอ): เปิดฟอร์มแก้ไข ช่องปีโชว์ 2536 ตามที่พี่เห็น → แก้เป็น 2568 → บันทึก → `cropYear` กลายเป็น 2025 (ค.ศ.) → โชว์กลับมา 2568 ถูกต้อง · ฟิลด์อื่น (โรงเพาะฟัก/ผลผลิต/วันที่) ไม่ถูกแตะ · ทดสอบชนกับรอบอื่นของบ่อเดียวกัน ขึ้นเตือนยืนยันก่อนบันทึกซ้ำ ตอบยกเลิกแล้วข้อมูลไม่เปลี่ยน · โหมด "เพิ่มรอบเก่าที่ตกหล่น" เดิมยังทำงานปกติ ไม่กระทบ · ไม่มี console error

### 205. หน้าสุ่มตัวอย่าง — Biomass ไม่หักพาเชียล ขัดกับหน้ารายละเอียดบ่อ (แก้แล้ว)

พี่ทักว่า "บันทึกการสุ่ม Biomass รวม ยังไม่แก้ไขตามน้ำหนักกุ้งหลังพาเชียล ยังเป็น biomass รวมของน้ำหนักทั้งบ่อ ข้อมูลนี้จะย้อนแย้งไหม" — **ย้อนแย้งจริง** เป็นบั๊กชุดเดียวกับ entry #200 ที่ตอนนั้นตกหน้าสุ่มไป

**อาการ** (บ่อปล่อย 1,000,000 · อัตรารอด 80% · ABW 25g · พาเชียลไปแล้ว 2 ครั้ง 10,000 กก. / 375,000 ตัว):

| หน้า | ป้าย | ตัวเลข |
|---|---|---|
| สุ่มตัวอย่าง | "Biomass รวม (น้ำหนักกุ้งทั้งบ่อ)" | **20,000 กก.** |
| รายละเอียดบ่อ | "น้ำหนักรวม" | **10,625 กก.** |

`calcBiomass()` คิด `pRelease × surv% × abw` เฉยๆ ไม่หักพาเชียล → ได้เลขที่ **ไม่ใช่ทั้งกุ้งในบ่อ (10,625) และไม่ใช่ผลผลิตรวมทั้งรอบ (20,625)** เพราะกุ้งที่จับไปตอนนั้นไซส์ 40/35 ไม่ใช่ 25g ตามที่สูตรคิด — เป็นเลขสมมติว่า "ถ้ากุ้งที่รอดทั้ง 800,000 ตัวยังอยู่ครบและหนัก 25g หมด"

**ผลกระทบจำกัด**: ตรวจแล้ว `sample.biomass` ถูกใช้แค่ **แสดงผล 4 จุด** (ช่องกรอก · ชิป · การ์ดสรุป · ตารางประวัติ+PDF) **ไม่มีสูตรไหนอ่านไปคำนวณต่อ** — FCR/อัตรารอด/ต้นทุนต่อกก./กก.ต่อไร่/คาดการณ์ อ่านจาก `pondShrimpCount()` ซึ่งถูกอยู่แล้ว จึงเป็นปัญหาการแสดงผลล้วน ไม่ได้ทำให้ตัวเลขตัดสินใจเพี้ยน

**แก้ตามที่พี่เลือก (ทางเลือก ก — แยกเป็นหลายบรรทัด/หลายการ์ด):**

- `calcBiomass()` หักพาเชียลแล้ว → ช่องหลักกลายเป็น **กุ้งในบ่อตอนนี้** ตรงกับ `getLatestBiomass()` · ป้ายเปลี่ยนจาก "น้ำหนักกุ้งทั้งบ่อ" เป็น "กุ้งที่อยู่ในบ่อตอนนี้"
- เพิ่มบรรทัดแจกแจงใต้ช่อง (`#sp-bio-breakdown`) โผล่เฉพาะบ่อที่พาเชียลแล้ว:
  `รอดสะสม 800,000 ตัว − จับพาเชียลไปแล้ว 375,000 ตัว = 425,000 ตัว ในบ่อ` / `🎣 จับไปแล้ว 10,000 กก. · 📦 ผลผลิตรวมทั้งรอบ 20,625 กก.`
- การ์ดสรุปด้านขวา: เปลี่ยนการ์ด Biomass เป็น "กุ้งในบ่อตอนนี้" + **เพิ่ม 2 การ์ดใหม่** (🎣 จับพาเชียลไปแล้ว / 📦 ผลผลิตรวมทั้งรอบ) เฉพาะบ่อที่มีพาเชียล และติดป้าย "(สะสม)" ที่ Survival Rate
- เพิ่ม `sampleInPondBiomass(p,r)` — คำนวณ biomass ในบ่อ **ณ วันที่สุ่มแต่ละแถว** (หักเฉพาะพาเชียลที่เกิดก่อน/เท่ากับวันนั้น) ใช้ในตารางประวัติหน้าสุ่ม + ตารางใน PDF ปิดบ่อ
  - ⚠️ **อ่านค่าใหม่ตอนแสดงผล ไม่แก้ข้อมูลเก่าใน Firebase** (หลักเดียวกับ `cropHarvestResult()` entry #176) บันทึกเก่าที่เก็บเลขไม่หักพาเชียลจึงแสดงถูกทันทีโดยไม่ต้อง migrate
  - ⚠️ ต้องคิดตามวันที่ ไม่ใช่หักยอดพาเชียลทั้งหมดกับทุกแถว ไม่งั้นแถวเก่าก่อนพาเชียลจะถูกหักผิด
  - ตารางใน PDF (`renderChSampleTable(c)`) สโคปไม่มีตัวแปร `p` — ส่ง `c` (ระเบียนรอบที่ปิดแล้ว) เข้าไปแทน ใช้ได้เพราะ archive เก็บ `stock`/`stockBonus`/`partialHarvests` ครบ

ทดสอบผ่าน browser (stub `save()`/`fbSave()`, ลบบ่อทดสอบครบ): บ่อที่พาเชียล 2 ครั้ง ช่อง Biomass = 10,625 **ตรงกับ `getLatestBiomass()` เป๊ะ** · บรรทัดแจกแจง + 2 การ์ดใหม่ขึ้นถูก · **ตารางประวัติคิดตามวันที่ถูก** (แถว 10 ส.ค. ก่อนพาเชียลใดๆ = 14,400 เท่าเดิม · แถว 24 ส.ค. หักเฉพาะพาเชียลรอบแรก = 15,000 ไม่หักรอบที่เกิดทีหลัง) · **บ่อที่ไม่มีพาเชียลได้ 20,000 เท่าสูตรเดิมเป๊ะ** ไม่มีบรรทัด/การ์ดส่วนเกินโผล่ · ไม่มี console error

### 204. ปรับ workflow กรอกข้อมูลรายวันของพนักงาน — ลำดับบ่อตรงกันทุกหน้า + จำบ่อข้ามหน้า (แก้แล้ว)

พี่ให้ประเมิน workflow ในมุมพนักงานที่ต้องกรอก ค่าน้ำ / อาหาร / กุ้งตาย / น้ำเข้า / ผลแล็บ / ตัดสต๊อก ทุกวัน — เดินดูจริงด้วย role staff บนจอ 375px

**สิ่งที่พบ: แอปมี 3 รูปแบบการกรอกปนกัน**
- **อาหาร** — ตารางทุกบ่อ กรอกรวดเดียว กด "บันทึกทั้งหมด" ครั้งเดียว (ดีที่สุด)
- **คุณภาพน้ำ** — ชิปเลือกบ่อ + ตัวนับ "0/8 กรอกแล้ว" + `epNextUnfilled()` เด้งไปบ่อถัดไปอัตโนมัติหลังบันทึก (ดีมาก)
- **กุ้งตาย / จัดการน้ำ / ผลแล็บ** — dropdown ทีละบ่อ ไม่มีตัวนับ ไม่เด้งบ่อถัดไป (แบบเดิม)

**ยืนยันกับพี่แล้วว่า กุ้งตาย/น้ำเข้า บันทึก "เฉพาะบ่อที่มีเหตุ" ไม่ใช่ทุกบ่อทุกวัน** → จึง**ไม่ทำ**เป็นตารางแบบหน้าอาหาร (จะเปลืองพื้นที่เปล่า) แก้แค่ 2 เรื่องที่เป็นปัญหาจริง

**1. 🔴 ลำดับบ่อใน dropdown ไม่ตรงกับหน้าอื่น — เสี่ยงกรอกผิดบ่อ**

`populateAll()` ใช้ `sortedPonds()` (เรียงตามชื่อ ไม่มี DOC) สำหรับ `mt-pond`/`wl-pond`/`lb-pond`/`sp-pond`/`hv-pond`/`ch-pond` แต่หน้าคุณภาพน้ำ/อาหาร/คาดการณ์ใช้ `sortPondsByAge()` + ป้าย DOC

→ หน้าค่าน้ำบ่อบนสุด = **A.14 (DOC 156)** แต่หน้ากุ้งตายบ่อบนสุด = **A.1** พนักงานที่ชินกับตำแหน่งจากหน้าหนึ่งมาอีกหน้าแล้วเลือกผิดบ่อได้ (พี่สั่งเรียงตามอายุไว้ตั้งแต่ entry #193 แต่ตอนนั้นแก้แค่หน้าสต็อก ตกไป 6 dropdown)

แก้: เพิ่ม `pondOptionsHtml()` (เรียงตามอายุ + ป้าย `— DOC n`) ใช้กับ dropdown ของงานกรอกรายวันทั้งหมด
- ⚠️ **ไม่กรองบ่อออก** — บ่อเตรียม/ปิดแล้วยังต้องบันทึกค่าน้ำ/ผลแล็บ/จัดการน้ำ ได้ (`pondCurAge()=0` จะไปอยู่ท้ายลิสต์เอง ไม่ต้องกรอง)
- ยกเว้น `ex-pond`/`cash-pond` (หน้ารายงาน ไม่ใช่หน้ากรอก) และ `prep-pond` (บ่อเตรียมอายุ 0 หมด เรียงตามอายุไม่มีความหมาย)

**2. ต้องเลือกบ่อใหม่ทุกหน้า — ไม่มี state ร่วม**

ทดสอบแล้ว: เลือก A.1 ที่หน้าค่าน้ำ → ไปหน้ากุ้งตาย/จัดการน้ำ/ผลแล็บ/สุ่มตัวอย่าง/จับกุ้ง **ว่างหมดทุกหน้า** เดินตรวจบ่อเดียวแล้วบันทึก 3 อย่าง = เลือกบ่อเดิมซ้ำ 3 รอบ

แก้: เพิ่ม `_curPondId` + `setCurPond()` / `applyCurPond()` / `initCurPondSync()`
- ผูก listener `change` ครั้งเดียวตอน init (ไม่ต้องแก้ `onMtPond`/`onWlPond`/… ทีละตัว) และเรียก `setCurPond()` ใน `selectEPond()` ด้วย
- `applyCurPond()` เรียกท้าย `goTab()` — เติม**เฉพาะหน้าที่ยังไม่ได้เลือกบ่อ** ไม่เขียนทับสิ่งที่ผู้ใช้เลือกเอง
- ยิง `dispatchEvent(new Event('change'))` เพื่อให้ `onXxPond()` เดิมทำงานตามปกติ (โหลดตาราง/สรุปของบ่อนั้นให้ด้วย)
- กันบ่อที่จำไว้ถูกลบ (`D.ponds.some(...)`) ไม่งั้น applyCurPond จะเซ็ตค่าที่ไม่มีใน options

ทดสอบผ่าน browser (role staff, 375px, stub `save()`/`fbSave()`): dropdown กุ้งตาย/จัดการน้ำ เรียง A.14 DOC 156 → A.15 → … ตรงกับหน้าค่าน้ำแล้ว · เลือก A.1 ที่หน้ากุ้งตาย แล้วไป จัดการน้ำ/ผลแล็บ/สุ่ม/จับกุ้ง เลือกตามครบทุกหน้า · **เลือก A.15 เองที่หน้าจัดการน้ำแล้วออก-เข้าใหม่ ไม่ถูกเขียนทับ** และหน้ากุ้งตายก็ยังคง A.1 ของตัวเอง · บ่อที่จำไว้ถูกลบแล้วไม่ crash · หน้ากุ้งตายเปิดมาพร้อมบ่อ+อายุ+สรุป แทนที่จะว่างเปล่า · ไม่มี console error

**ยังไม่ทำ (เสนอไว้ให้พี่ตัดสินใจ)**: ผลแล็บอยู่อันดับ 14 ในเมนู ☰ ต้องเลื่อนหา ขณะที่แถบล่างมี "สต็อก" ซึ่งไม่ได้ใช้ทุกวัน — สลับตำแหน่งได้ถ้าพี่ต้องการ

### 203. ตรวจบัคก่อนพาเชียลรอบ 2 ของ A.1/A.2 — เจอ 4 จุด แก้ครบ (แก้แล้ว)

พี่สั่งตรวจซ้ำก่อน A.1/A.2 พาเชียล **รอบที่ 2** — ต่างจากรอบแรก (entry #191) ตรงที่คราวนี้บ่อมีพาเชียลค้างอยู่แล้ว เป็นเส้นทาง "พาเชียลสะสมหลายครั้ง" ที่ยังไม่เคยถูกใช้จริง และโค้ดเพิ่งเปลี่ยนเยอะจาก #198–#202

**ผลตรวจส่วนที่ถูกต้องอยู่แล้ว** (จำลองบ่อ ปล่อย 1,000,000 · อัตรารอด 80% · ABW 25 · พาเชียล 5,000 กก. 2 ครั้ง):
- `partialTotals` สะสม 2 ครั้งถูก (10,000 กก. / 375,000 ตัว / 1,550,000 บ.)
- `pondShrimpCount` — อัตรารอดสะสมคง 80% · คงเหลือ 600,000 → 425,000 ตัว · biomass 15,000 → 10,625 กก. · กก./ไร่ 3,000 → 2,125
- `pondFeedBaseOnDate` กรองตามวันที่ถูก — อาหารวันที่ 30 ส.ค. ยังใช้ฐาน 800,000 (พาเชียลรอบ 2 วันที่ 31 ยังไม่เกิด) แล้วค่อยลดเป็น 625,000 ตั้งแต่วันที่ 31 · กก./แสนตัว 50 → 48 ไม่มีหน้าผา
- ฟอร์มปิดบ่อรวมพาเชียลครบ: caught 615,000 · อัตรารอด 61.5% / ยอดสั่งซื้อ 76.9% · รายได้รวม 2,910,000 · กล่องสรุปขึ้น "2 ครั้ง"

**บั๊กที่เจอและแก้แล้ว 4 จุด:**

**1. ฟอร์มปิดบ่อดึงชื่อแพผิดตัว (เจอเฉพาะตอนมีพาเชียล ≥2 ครั้ง)**
`const lastPartial=(p.partialHarvests||[]).slice(-1)[0]` — คอมเมนต์เขียนว่า "พาเชียลล่าสุด" แต่ `savePartialHarvest()` sort เป็น **ใหม่→เก่า** (`b.date.localeCompare(a.date)`) ตัวล่าสุดจึงเป็น `[0]` ไม่ใช่ `slice(-1)` → ปิดบ่อแล้วช่องแพเด้งชื่อแพของการจับ **ครั้งแรกสุด** · แก้เป็น sort ใหม่แล้วหยิบ `[0]` (ไม่พึ่งลำดับใน array ที่อาจเปลี่ยน)

**2. 🔴 จับเกินจำนวนกุ้งที่ประเมินว่ามีในบ่อ — เงียบสนิท (ร้ายแรงสุด)**
ถ้าพาเชียลสะสม > `aliveAll` (เช่นสุ่มได้ 30% = 300,000 ตัว แต่จับไปแล้ว 375,000) `pondShrimpCount` จะ clamp `inPond` เป็น 0 → **Biomass / กก.ไร่ / กุ้งคงเหลือ ขึ้น 0 ทั้งที่ฟาร์มยังให้อาหารอยู่ 300 กก./วัน** และการ์ดพาเชียล (`partialHarvestInfo`) คืน null หายไปทั้งใบ — ไม่มีคำเตือนใดๆ ทั้งที่ข้อมูลขัดแย้งกันชัดเจน (KPI ขึ้น "จากรอด 300,000 − จับ 375,000")

สาเหตุจริงคือ **อัตรารอดที่กรอกไว้ต่ำกว่าความจริง** ไม่ใช่การจับผิด · แก้ 2 ชั้น:
- **ตอนบันทึก**: เตือนถ้ายอดจับสะสมจะเกิน `aliveAll` บอกตัวเลขทั้งสองฝั่ง + ส่วนต่าง และแนะให้สุ่มใหม่ — **ไม่บล็อก** เพราะการจับเกิดขึ้นจริงแล้ว ตัวเลขที่ผิดคืออัตรารอด
- **ตอนแสดงผล**: แถบแดงถาวรในการ์ดอัตรารอด อธิบายว่าทำไมตัวเลขขึ้น 0 และต้องแก้ตรงไหน

**3. วันที่พิมพ์ผิดเป็นอนาคต — หักกุ้งออกทันที**
`partialTotals` ไม่กรองวันที่ · พิมพ์ปี 2027 แทน 2026 → กุ้ง 5,000 กก. ถูกหักออกจากบ่อทันทีทั้งที่ยังไม่ได้จับ (คงเหลือ 425,000 → 250,000) · เพิ่มเตือนเมื่อ `date>today()` บอกผลกระทบชัดๆ ก่อนบันทึก

**4. บันทึกซ้ำ (กดปุ่ม 2 ที / ไม่แน่ใจว่าบันทึกไปยัง) — ไม่มีการเตือน**
`id:'PH'+Date.now()` ต่างกันเสมอ จึงเข้าเป็น 2 แถว → ผลผลิต+รายได้เบิ้ล และกุ้งถูกหักออกจากบ่อ 2 เท่า (10,000 → 20,000 กก.) · เสี่ยงจริงเพราะพรุ่งนี้กรอก 2 บ่อติดกันบนมือถือ · เพิ่มตรวจซ้ำจาก วันที่+น้ำหนัก+แพ แล้วถามยืนยันว่าเป็น "อีกเที่ยวหนึ่ง" จริงไหม

ทดสอบผ่าน browser (stub `save()`/`fbSave()`, ยิง `savePartialHarvest()` จริงโดยดัก `window.confirm` แล้วตอบยกเลิก จะได้ไม่เขียนข้อมูล, ลบบ่อทดสอบครบ): คำเตือนทั้ง 3 แบบขึ้นถูกเคส · **เคสปกติไม่มีคำเตือนและบันทึกได้ตามปกติ** · ชื่อแพเด้ง "แพ B" (ล่าสุด) ถูกต้อง · แถบแดงขึ้นเมื่อจับเกินและหายเมื่อแก้อัตรารอดกลับ · บ่อที่ไม่เคยพาเชียลไม่มีป้าย/คำเตือนใดโผล่ ตัวเลขเหมือนเดิมทุกอย่าง · ไม่มี console error

### 202. เพิ่มกล่อง KPI "กุ้งคงเหลือในบ่อ" + "กก./ไร่" หน้ารายละเอียดบ่อ (เพิ่มใหม่)

พี่สั่ง: "ต้องการเพิ่มกล่องข้อมูลจำนวนตัวกุ้งที่เหลืออยู่ในบ่อ หากมีการพาเชียล จะได้รู้ว่าเหลือกุ้งทั้งหมดกี่ตัว"

เดิมแถบ KPI มี 7 กล่อง (ไซส์ · น้ำหนักรวม · อาหารวันนี้ · กก./แสนตัว · อาหารรวม · FCR ประเมิน · อัตรารอด) — บอกได้แต่ % และน้ำหนัก ไม่มี "จำนวนตัว" ให้ดูตรงๆ

เพิ่มกล่องที่ 8 **"กุ้งคงเหลือในบ่อ"** วางต่อจาก "อัตรารอด" ให้เป็นคู่กันในแถวเดียว (grid 2 คอลัมน์ลงตัวพอดี 4 แถว) ใช้ `pdSC.inPond` จาก `pondShrimpCount()` ที่มีอยู่แล้ว — ไม่คำนวณคู่ขนาน

- **บ่อที่ยังไม่พาเชียล**: โชว์ `930,000` + บรรทัดล่าง "ตัว (ประเมิน)"
- **บ่อที่พาเชียลแล้ว**: โชว์ `795,000` + บรรทัดล่าง "ตัว · จากรอด 930,000 − จับ 135,000" ให้เห็นที่มาของตัวเลขในบรรทัดเดียว ไม่ต้องเปิดการ์ดอัตรารอด
- tooltip อธิบายว่าเป็นค่าประเมินจาก ยอดปล่อย × อัตรารอด (หักพาเชียลถ้ามี) ไม่ใช่การนับจริง
- ไม่มีข้อมูลสุ่มตัวอย่าง (`pondShrimpCount()` คืน null) → โชว์ `—`

ตัวเลขสอดคล้องกันทั้งแถบ: อัตรารอด (สะสม) 93% · ในบ่อ 79.5% · จับไป 135,000 ตัว / กุ้งคงเหลือ 795,000 ตัว (= 930,000 − 135,000 = 79.5% ของยอดปล่อย 1,000,000) / กก./แสนตัว ฐาน 865,000 ตัว (= ยอดปล่อย − 135,000 ตามฐานที่ตกลงไว้ entry #201)

**ตามมาอีกคำสั่ง: กล่อง "กก./ไร่" ต่อจาก "น้ำหนักรวม"** (รวมเป็น 9 กล่อง)

ใช้สูตรเดียวกับ `partialHarvestInfo()` คือ `biomass ในบ่อ ÷ p.size` — อ่านจาก `pdSC.biomass` ที่ `pondShrimpCount()` คำนวณไว้แล้ว ไม่เรียก `getLatestBiomass()` ซ้ำ → ตัวเลขตรงกับการ์ดพาเชียลเสมอ (หักกุ้งที่จับไปแล้วออก)

- เทียบกับเกณฑ์พาเชียล `_phTarget` (ค่าเริ่มต้น 4,000 กก./ไร่) — ถึงเกณฑ์แล้วเปลี่ยนเป็นสีเขียว + ติด ✓ ไม่ถึงโชว์เกณฑ์เฉยๆ
- ไม่มีขนาดบ่อ (ไร่) หรือยังไม่เคยสุ่มตัวอย่าง → โชว์ `—` ไม่ crash
- สีน้ำเงินตามกลุ่ม ไซส์/น้ำหนัก (เป็นค่าเกี่ยวกับตัวกุ้ง ไม่ใช่อาหาร/ประสิทธิภาพ)

ตรวจแล้วว่าค่าตรงกับ `partialHarvestInfo().yr` เป๊ะ (3,720 ทั้งคู่) · หลังพาเชียล 5,000 กก. ลดจาก 4,650 → 3,650 ถูกต้อง (23,250 − 5,000 = 18,250 ÷ 5 ไร่)

หมายเหตุเลย์เอาต์: 9 กล่องใน grid 2 คอลัมน์ → แถวสุดท้ายเหลือกล่องเดียวชิดซ้าย ดูแล้วรับได้ทั้งเดสก์ท็อปและมือถือ ไม่ได้บังคับให้เต็มแถว

ทดสอบผ่าน browser (stub `save()`/`fbSave()`, สร้างบ่อทดสอบในหน่วยความจำแล้วลบทิ้ง): ทั้ง 9 กล่องเรียงถูกตำแหน่งบนเดสก์ท็อป, มือถือ 375px บรรทัดล่างตัดขึ้นบรรทัดใหม่ได้ปกติไม่ล้นกล่อง, เคสไม่มีพาเชียลและมีพาเชียลถูกต้องทั้งคู่, ไม่มี console error

### 201. กก./แสนตัว หักตัวที่พาเชียลออก + แก้ crash บ่อที่ยังไม่มีค่าน้ำ (แก้แล้ว)

พี่สั่ง 2 ข้อต่อจาก entry #200 · ข้อ 1 มีการปรับคำสั่งระหว่างทาง (บันทึกไว้กันสับสนภายหลัง)

**1. กก./แสนตัว — ยังใช้ยอดปล่อยเหมือนเดิม แต่หลังพาเชียลให้หักตัวที่จับออกไป**

รอบแรกพี่สั่งว่า "หารด้วยยอดจริงที่เหลืออยู่" ผมทำเป็น *ยอดปล่อย × อัตรารอด − พาเชียล* แล้วรายงานว่าตัวเลขจะสูงขึ้น ~1.85 เท่าในทุกบ่อ · พี่แก้คำสั่งเป็น **"ใช้ตัวเลขยอดปล่อย หลังจากพาเชียลให้ใช้ตัวเลขยอดกุ้งที่เหลืออยู่"** จึงตัดอัตรารอดออกจากตัวหาร

เหตุผลที่ฐานนี้ดีกว่า: **อัตรารอดเป็นค่าประเมิน** ถ้าเอามาหารด้วย ตัวเลขจะขยับทุกครั้งที่สุ่มตัวอย่างได้ค่าใหม่ เกณฑ์ที่ฟาร์มจำไว้จะใช้ไม่ได้ · ส่วน **จำนวนตัวที่จับพาเชียลออกไปเป็นตัวเลขจริงที่นับได้** หักได้อย่างมั่นใจ

สร้าง `pondFeedBaseOnDate(p,d)` = `ยอดปล่อย − พาเชียลที่เกิดก่อน/เท่ากับวันที่ d`

⚠️ **ต้องคิดตามวันที่ ห้ามใช้ยอดวันนี้ย้อนหลังทั้งกราฟ** ไม่งั้นจุดเก่าก่อนพาเชียลจะถูกหารด้วยยอดหลังพาเชียล ตัวเลขเด้งขึ้นผิดทั้งเส้น

แก้ครบทั้ง 7 จุดที่เรียก `feedPer100k()`: การ์ดรายละเอียดบ่อ (KPI + แถวในการ์ดอาหาร) · กราฟประวัติ 2 จุด (`feedPts` + metric `per100k`) · หน้าคำนวณ & คาดการณ์ prefill 2 จุด + `fcSnap()` 3 จุด (`fd100k` / `factor` / แปลงกลับเป็น `fdDay` — ทั้งหมดใช้ `fdBase = rel − _fcPhCount` ตัวเดียวกัน ไม่งั้นค่าที่ผู้ใช้กรอกกับที่สูตรคำนวณอยู่คนละฐาน แล้วอาหาร/วัน เพี้ยน)

ป้ายใต้ตัวเลข ("ฐาน 750,000 ตัว (หักพาเชียล)") **โผล่เฉพาะบ่อที่พาเชียลไปแล้ว** — บ่อปกติหน้าตาเหมือนเดิมทุกประการ

ทดสอบด้วยบ่อจำลอง (ปล่อย 1,000,000 · สุ่ม 60% → 54% · พาเชียล 5,000 กก. ไซส์ 50 = 250,000 ตัว วันที่ 26 ส.ค.):

| วันที่ | อาหาร | ฐาน | เดิม | ใหม่ |
|---|---|---|---|---|
| 1 ก.ค. | 100 | 1,000,000 | 10.0 | 10.0 |
| 15 ก.ค. | 150 | 1,000,000 | 15.0 | 15.0 |
| 25 ส.ค. (ก่อนพาเชียล) | 200 | 1,000,000 | 20.0 | 20.0 |
| 28 ส.ค. (หลังพาเชียล) | 110 | 750,000 | **11.0** | **14.7** |

**ทุกจุดก่อนพาเชียลเท่าเดิมเป๊ะ** — ข้อมูลย้อนหลังและเกณฑ์เดิมของฟาร์มยังใช้ได้ · เปลี่ยนเฉพาะหลังพาเชียล

**หมายเหตุ — ยังเหลือรอยบุ๋มเล็กน้อย ไม่ใช่บั๊ก**: 20.0 → 14.7 ยังลงอยู่บ้าง เพราะฐานหักด้วย "จำนวนตัวที่จับ" (250,000 = 25% ของยอดปล่อย) แต่กุ้งที่หายไปจริงคิดเป็น 46% ของตัวที่ยังมีชีวิต (250,000 จาก 540,000) — ตัวเลขสองอันนี้ไม่เท่ากันเพราะฐานเป็นยอดปล่อย ไม่ใช่ตัวที่รอด · ถ้าจะให้เรียบสนิทต้องใช้อัตรารอดมาหารด้วย ซึ่งพี่ตัดสินใจไม่เอาแล้ว (เหตุผลด้านบน) · เดิมก่อนแก้ตกจาก 20.0 → 11.0 (45%) ตอนนี้เหลือ 27%

**2. แก้ crash: บ่อที่ลงกุ้งแล้วแต่ยังไม่เคยบันทึกค่าน้ำ**

`renderFarmCard()` ส่ง `last=null` มาโดยตั้งใจเมื่อ `p.records` ว่าง และ `pondRiskDO()`/`pondRiskStability()` เช็คไว้แล้ว แต่ `pondRiskDisease()` ไม่ได้เช็ค → อ่าน `last.sal` แล้วโยน `TypeError: Cannot read properties of null (reading 'sal')` ทะลุขึ้นไปพังทั้งหน้ารายละเอียดบ่อ

แก้โดยเติม guard 2 จุดใน `pondRiskDisease()` (บล็อกไนไตรท์-ความเค็มต่ำ และบล็อก Bottom DO) — **ไม่ใช้ early-return** แบบ `pondRiskStability()` เพราะสัญญาณจากผลแล็บ / กุ้งตาย / แนวโน้มอาหาร ยังใช้ได้แม้ไม่มีค่าน้ำ · ตรวจ `pondRiskFeed()` ด้วยแล้ว — ไม่ได้ dereference `last` เลย ปลอดภัยอยู่แล้ว

ทดสอบ: บ่อ `records:[]` เดิม throw ตอนนี้ `pondRiskDisease` คืน level `ok` และหน้ารายละเอียดบ่อ render ครบทั้ง 5 การ์ดความเสี่ยง

ทดสอบผ่าน browser (stub `save()`/`fbSave()`, สร้างบ่อทดสอบในหน่วยความจำแล้วลบทิ้งครบ): บ่อที่ไม่มีพาเชียลตัวเลข + ป้ายเหมือนเดิมทุกอย่าง, หน้าคาดการณ์ prefill 14.7 ตรงกับการ์ดรายบ่อ และ `fd100k ↔ fdDay` แปลงกลับได้ตรง (14.7 × 750,000/100,000 = 110.25 กก. ≈ อาหารจริง 110 กก.), ไม่มี console error

### 200. อัตรารอด/Biomass ไม่หักกุ้งที่พาเชียลออกไป (แก้แล้ว)

พี่แจ้งว่า "อัตรารอดของทางฟาร์ม หลังจากพาเชียลแล้ว ยังไม่มีการปรับตัวเลข จำนวนกุ้งที่ออกไป และน้ำหนักที่ยังค้างอยู่เป็นน้ำหนักเก่าก่อนพาเชียล"

**Root cause**: `getBioCompareCalc()` คำนวณ `farmBiomass = abw × release × surv% / 1000` และ `farmCount = release × surv%` ตรงๆ **ไม่หักตัวที่จับพาเชียลออกไป** ขณะที่ `getLatestBiomass()` หักอยู่แล้วตั้งแต่ entry #176 → มี 2 ตัวเลข biomass ขัดกันเองในหน้าเดียว (การ์ด "อัตรารอด" โชว์ 10,800 กก. แต่ KPI "น้ำหนักรวม" โชว์ 5,800 กก.)

**แก้แล้ว** — สร้าง single source of truth ใหม่ `pondShrimpCount(p)` ที่แยก 2 นิยามให้ชัด แล้วให้ทุกที่เรียกผ่านตัวนี้:

- `survAll` / `aliveAll` = **อัตรารอดสะสม** — นับกุ้งที่จับพาเชียลไปแล้วเป็น "รอด" ด้วย (ไม่ได้ตาย แค่ขายออกไป) — ตัวนี้ไม่ควรตกเวลาพาเชียล
- `inPond` / `biomass` = **ที่ยังอยู่ในบ่อจริง** = รอดทั้งหมด − ที่จับออกไป — ต้องลดทันทีที่พาเชียล

จุดที่แก้:

- `getLatestBiomass()` รีแดกเตอร์ให้เรียก `pondShrimpCount()` แทน (เพิ่ม guard `if(!release)return null` กัน `inPondPct` เป็น NaN — เดิมบ่อที่ไม่มียอดปล่อยคืน 0 ตอนนี้คืน null)
- `getBioCompareCalc()` ใช้ `pondShrimpCount()` แทนการคำนวณเอง → `farmBiomass`/`farmCount` หักพาเชียลแล้ว ทำให้ `farmImpliedRate` และ `delta` ถูกตามไปด้วย
- `docImpliedSurvival` บวกตัวที่จับไปแล้วกลับเข้าไป — ฝั่งเอกสารคำนวณย้อนจากอาหารวันนี้จึงได้ "ในบ่อ" โดยธรรมชาติ ต้องบวกกลับจึงเทียบกับ "อัตรารอดสะสม" ได้ตรงนิยาม
- **การ์ด "อัตรารอด"**: เพิ่ม 3 แถวเมื่อมีพาเชียล — รอดสะสมทั้งรอบ / จับพาเชียลไปแล้ว (ตัว + กก.) / คงเหลือในบ่อตอนนี้ (ตัว + % + กก.) และเปลี่ยนป้ายวงแหวนเป็น "อัตรารอดสะสม (นับกุ้งที่จับไปแล้วด้วย)"
- **KPI กล่อง "อัตรารอด"**: เติม "(สะสม)" ท้ายหัวข้อ + บรรทัดล่างโชว์ "ในบ่อ X% · จับไป Y ตัว"
- **การ์ด "ไซส์ล่าสุด"**: เพิ่มแถว "คงเหลือในบ่อ" + ติดป้าย (สะสม)/ในบ่อ ให้ชัด
- **หน้าคำนวณ & คาดการณ์**: เพิ่ม hint ใต้ช่องอัตรารอด บอกว่าระบบหักตัวที่พาเชียลไปแล้วให้อัตโนมัติ ไม่ต้องหักเอง (คำนวณจาก `p` ตรงๆ ห้ามอ่าน `_fcPhCount` เพราะถูกเซ็ตใน `fcCalc()` ซึ่งเรียกทีหลัง จะได้ค่าของบ่อก่อนหน้า)
- `fcLoadPond()` fallback ตอนยังไม่มีบันทึกอาหาร เปลี่ยนมาใช้ `getLatestBiomass()` แทนการคำนวณเองที่ไม่หักพาเชียล

**สิ่งที่จงใจไม่แก้**: "กก./แสนตัว" ยังหารด้วย `pRelease(p)` (ยอดปล่อย ไม่ใช่ตัวที่รอด) ตามมติเดิมที่ตกลงไว้ — ถ้าเปลี่ยนจะกระทบกราฟประวัติย้อนหลังทั้งหมด รอพี่ตัดสินใจก่อน

**เจอบั๊กแยกระหว่างทาง (ยังไม่แก้ แจ้งพี่แล้ว)**: `pondRiskDisease()` ไม่มี null guard สำหรับ `last` ขณะที่ `pondRiskDO()`/`pondRiskStability()` มี — บ่อที่ลงกุ้งแล้วแต่ยังไม่เคยบันทึกค่าน้ำเลยจะ crash ทั้งหน้ารายละเอียดบ่อ (`TypeError: Cannot read properties of null (reading 'sal')`)

ทดสอบผ่าน browser (stub `save()`/`fbSave()`, สร้างบ่อทดสอบในหน่วยความจำแล้วลบทิ้ง): ยอดปล่อย 1,000,000 ตัว อัตรารอด 54% ABW 20 กรัม → ก่อนพาเชียล biomass 10,800 กก. / หลังพาเชียล 5,000 กก. (ไซส์ 50) → อัตรารอดสะสมคง 54% แต่คงเหลือในบ่อ 290,000 ตัว (29.0%) biomass 5,800 กก. — `getLatestBiomass()` กับ `getBioCompareCalc().farmBiomass` ตรงกันแล้ว (ก่อนแก้เป็น 5,800 vs 10,800), `docImpliedSurvival` 62.5% บวกตัวที่จับไปแล้วกลับเข้าถูกต้อง, บ่อที่ไม่มีพาเชียลแสดงเหมือนเดิมทุกอย่าง (ไม่มีแถวพาเชียลโผล่), hint หน้าคาดการณ์ขึ้นจำนวนตัวของบ่อที่เลือกถูก, มือถือ 375px อ่านได้ครบ, ไม่มี console error

### 199. Protocol เวชภัณฑ์ — กล่องพิมพ์ข้อความขยายเต็มจอบนมือถือ (แก้แล้ว)

พี่แจ้งว่ากล่องพิมพ์ข้อความในหน้า "💊 Protocol เวชภัณฑ์ล่าสุด" (`#mp-ta`) บนมือถือเล็กและอ่านลำบาก ต้องเลื่อนดูตลอด — ต้องการให้กดกล่องแล้วขยายเป็นหน้าเต็มจอ

**แก้แล้ว**: เปลี่ยน `#mp-ta` เป็น `readonly` + `onclick="mpExpand()"` (ไม่พิมพ์ในกล่องเล็กอีกต่อไป แค่เป็นตัวเปิดหน้าใหญ่) เพิ่ม modal ใหม่ `#mpExpandModal` ที่เต็มจอบนมือถือ (`.mp-exp-modal`, ใช้ `.modal-bg`/`.modal` เดิมของแอปที่มี breakpoint มือถือเต็มจอ/เต็มความสูงอยู่แล้ว) มีหัวข้อ + ปุ่มปิด + textarea ใหญ่ (`font-size:16.5px`, เต็มความสูงที่เหลือด้วย flex) + ปุ่มบันทึก

- ฟังก์ชันใหม่: `mpExpand()` (เปิด, sync ค่า/สถานะ read-only ตาม role, focus อัตโนมัติถ้าแก้ได้), `mpExpandClose()` (เตือนก่อนปิดถ้ายังไม่บันทึก — ใช้ `_mpDirty` เดิม), `mpExpandSave()` (แทนที่ `mpSave()` เดิมที่ลบทิ้งเพราะกล่องเล็กพิมพ์ไม่ได้แล้ว)
- staff (`mpCanEdit()===false`) ยังกดเปิดดูได้ แต่ textarea เป็น read-only + ป้าย "อ่านอย่างเดียว — ให้ Admin เป็นคนแก้ไข" แทนปุ่มบันทึก (`.admin-only` เดิม)
- เพิ่ม listener กดพื้นหลังนอก modal เพื่อปิด เหมือน `pondModal` เดิม

ทดสอบผ่าน browser (stub `save()`/`fbSave()`, จำลอง role admin/staff โดยไม่ผ่าน login จริงเพราะเชื่อม Firebase production): มือถือ 375px กล่องเล็กมีคำแนะนำ "แตะเพื่อขยาย", `mpExpand()` เปิด modal เต็มจอพร้อม textarea ใหญ่, พิมพ์ + กดบันทึกอัปเดตข้อความ/ตัวนับ/timestamp ในหน้าเดิมถูกต้อง, สลับเป็น role staff แล้วเปิดได้แต่แก้ไม่ได้พร้อมป้ายบอกเหตุผล, เดสก์ท็อปแสดง modal กึ่งกลางจอปกติ ไม่มี console error

### 198. ต้นทุน/กก. พุ่งหลังพาเชียล + คาดการณ์ไม่หักกุ้งที่จับไปแล้ว (แก้แล้ว)

พี่แจ้งว่าหลังพาเชียลแล้ว "ต้นทุนสะสมเพิ่มสูงขึ้น" ให้เข้าไปเช็ค

**ตรวจแล้ว — ต้นทุน*รวม*ไม่ได้เพิ่ม (ถูกต้อง) แต่ "ต้นทุน/กก." พุ่ง** จำลองบ่อ ต้นทุนสะสม 500,000 · biomass 6,776 กก. · พาเชียล 5,000 กก.:

| | ก่อนพาเชียล | หลังพาเชียล |
|---|---|---|
| ต้นทุนรวม | 500,000 | 500,000 (ไม่เปลี่ยน ✅) |
| Biomass | 6,776 | 1,776 |
| **ต้นทุน/กก.** | **73.8** | **281.5** ❌ (พุ่ง 4 เท่า) |

**Root cause**: `pdCostPerKg = pdCostTotal / biomass` — ตั้งแต่ entry #176 `getLatestBiomass()` หักกุ้งที่พาเชียลไปแล้วออกจากตัวหาร **แต่ต้นทุนที่ใช้เลี้ยงกุ้งชุดนั้นยังอยู่ครบในตัวตั้ง** พอจับออก 5 ตัน ตัวหารหายไป 5 ตัน ต้นทุน/กก. เลยพุ่งทันทีทั้งที่ยังไม่ได้จ่ายเงินเพิ่มแม้แต่บาทเดียว — เป็นบั๊กคลาสเดียวกับที่แก้ให้ FCR ไปแล้วใน entry #176 (`getFCR` ใช้ `produced = biomass + partialKg`) แต่ตอนนั้นตกจุดต้นทุน/กก. ไป

**แก้แล้ว**: `pdCostPerKg = pdCostTotal ÷ (biomass ในบ่อ + พาเชียลที่จับไปแล้ว)` — ใช้หลักเดียวกับ `getFCR()` · เพิ่มคำอธิบายใต้ตัวเลขให้แจกแจงตัวหารด้วย เช่น `= ต้นทุนสะสม ÷ 6,776 กก. (Biomass ในบ่อ 1,776 + พาเชียลไปแล้ว 5,000)`

**เจอบั๊กเดียวกันอีกจุดตอนไล่ตรวจ — หน้า "🔮 คำนวณ & คาดการณ์"**: `fcSnap()` คำนวณ biomass จาก `rel × surv% × abw` ตรงๆ **ไม่หักกุ้งที่พาเชียลไปแล้ว** — บ่อทดสอบที่พาเชียลไป 5,500 กก. หน้าคำนวณคิด biomass 6,776 กก. ทั้งที่ในบ่อเหลือจริง 1,276 กก. (**เกินไป 5,500 กก. พอดี**) แปลว่าคาดการณ์ผลผลิต/รายได้/กำไรทั้งตารางสูงเกินจริงมาก เหมือนกุ้งที่ขายไปแล้วยังอยู่ในบ่อและโตต่อ — **แก้แล้ว** เพิ่ม `_fcPhCount` (จำนวนตัวที่พาเชียลไปแล้ว) หักออกจาก `aliveCount` ใน `fcSnap()` เซ็ตค่าคู่กับ `_fcDOC0` ใน `fcCalc()`

**จุดที่ตรวจแล้วถูกต้องอยู่แล้ว ไม่ต้องแก้** (ใช้ biomass ในบ่อโดยตั้งใจ): Feed Rate (%) · กก./ไร่ ในปฏิทินพาเชียล · Biomass ในตารางภาพรวม · อัตราการให้อาหารแนะนำ — ทั้งหมดควรอิงกุ้งที่เหลือในบ่อจริง

ทดสอบผ่าน browser (stub `save()`/`fbSave()`): หลังแก้ ต้นทุน/กก. **73.8 เท่ากันทั้งก่อนและหลังพาเชียล** ✅ · พาเชียลครั้งที่ 2 ก็ยัง 73.8 ✅ · จ่ายค่าลูกกุ้งเพิ่มจริง 100,000 → ขึ้นเป็น 88.5 ✅ (ยังตอบสนองต้นทุนที่เพิ่มจริง) · กุ้งโตขึ้น ABW 20→24 → ลดเป็น 70.1 ✅ · หน้าคำนวณ biomass ตรงกับ `getLatestBiomass()` ทั้งเคสมีและไม่มีพาเชียล ✅ ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบและ `save()` จริงหลังทดสอบเสร็จ

### 197. หน้ารายละเอียดบ่อ — จัดระเบียบการ์ด 6 จุดตามที่พี่สั่ง (แก้แล้ว)

**1. ลบการ์ด "💧 คุณภาพน้ำล่าสุด"** — ข้อมูลชุดเดียวกับการ์ด "ประวัติค่าน้ำย้อนหลัง" ที่กดดูได้อยู่แล้ว · ลบบล็อก `waterHtml` (~1,890 ตัวอักษร) ที่กลายเป็น dead code ออกด้วย

**2. "📖 ประวัติค่าน้ำย้อนหลัง" แสดงครบทุกพารามิเตอร์** — เดิม `const show=PARAMS.slice(0,8)` ตัดไป 6 ตัวคือ **Ca / Mg / Hardness / NH4+ / NO2 / NO3** ทั้งที่กรอกไว้ในหน้าคุณภาพน้ำ (NH4/NO2 เป็นค่าสำคัญด้วย) → เปลี่ยนเป็น `PARAMS` ทั้งชุด ตารางจาก 8 เป็น 14 พารามิเตอร์ (18 คอลัมน์รวมวันที่/อายุ/หมายเหตุ/ปุ่ม) — กระทบทั้งตาราง desktop และการ์ดมือถือเพราะใช้ตัวแปร `show` ร่วมกัน

**3. รวมการ์ด "🍖 อาหารล่าสุด" เข้ากับ "📖 สมุดประวัติการให้อาหาร"** — เหลือการ์ดเดียวชื่อ "🍖 อาหาร · ล่าสุด {วันที่}" โดยยกสรุปล่าสุด (อาหารวันนี้/กก.ต่อแสนตัว/Feed Rate/จำนวนมื้อ) ขึ้นเป็นกล่องสีเทาด้านบน แล้วตามด้วยตารางประวัติเดิม

**4. "📚 ประวัติรอบเลี้ยงบ่อนี้" เพิ่มคอลัมน์ "แหล่งลูกกุ้ง"** — แสดง `c.hatchery` พร้อม `c.plType` เป็นบรรทัดเล็กใต้ชื่อ เพื่อวิเคราะห์ว่าแหล่งลูกกุ้งไหนให้อัตรารอดดีกว่ากัน (ข้อมูลมีอยู่แล้วใน cropHistory ตั้งแต่ตอน archive)

**5. ลบการ์ด "📊 เทียบกับรอบก่อน"** — ⚠️ **ก่อนลบได้ทักพี่ก่อน**ว่าการ์ดนี้มี 2 โหมดที่ไม่ซ้ำกับตารางประวัติรอบ: (ก) เทียบกับ**บ่อพี่น้อง**ที่กำลังเลี้ยงอยู่ตอนนี้ (ขนาดเดียวกัน/อายุใกล้กัน/แหล่งลูกกุ้งเดียวกัน) และ (ข) เทียบ ABW/FCR/อัตรารอด **ณ อายุกุ้งเท่ากัน** (ตารางประวัติเทียบผลสุดท้ายตอนปิดบ่อ คนละคำถามกัน) — **พี่ยืนยันให้ลบทั้งการ์ด** จึงลบพร้อมฟังก์ชันช่วย `bmBuildSeries`/`bmFindSibling`/`bmGetRef`/`bmSetMode`/`bmKpiHtml`/`_bmMode` และ `renderCropBenchmark` ทั้งหมด (~8,100 ตัวอักษร) — ตรวจแล้วว่าไม่มีที่อื่นเรียกใช้ (grep เหลือ 0)

**6. กราฟ "⚖️ อาหาร vs การเติบโต รายสัปดาห์" มาร์กจุดพาเชียล** — เดิมย้อนดูแล้วงงว่าทำไมอาหารลดฮวบบางสัปดาห์ · เพิ่ม `phKg`/`phCount` เข้า `pdWeekBuckets()` แล้ววาด dataset ที่ 3 เป็น**สามเหลี่ยมสีส้มลอยเหนือแท่งอาหาร**ของสัปดาห์นั้น (สูงกว่าแท่ง 8% ของค่าสูงสุด) พร้อม tooltip บอก "🦐 พาเชียล N ครั้ง · X กก." — สัปดาห์ที่ไม่มีพาเชียลเป็น `null` ไม่วาดจุด และถ้าทั้งรอบไม่มีพาเชียลเลยจะ**ไม่เพิ่ม dataset** (legend ไม่มีรายการค้าง)

ทดสอบผ่าน browser (stub `save()`/`fbSave()`): รายการการ์ดเหลือ 15 ใบ ไม่มี "คุณภาพน้ำล่าสุด"/"เทียบกับรอบก่อน" ✅ · ตารางค่าน้ำมี NH4+/NO2/NO3/Ca/Mg/Hardness ครบ 18 คอลัมน์ ✅ · การ์ดอาหารมีทั้งสรุปล่าสุด (250 กก./56.8 กก.ต่อแสนตัว/Feed Rate 5.3%/4 มื้อ) และตารางประวัติในใบเดียว ✅ · ประวัติรอบโชว์ "ซีพี บางปะกง" + "PL12" ✅ · กราฟมี dataset "🦐 พาเชียล" 1 จุดที่สัปดาห์ 8 (ตรงกับพาเชียล 800 กก. ที่ยิงเข้าไป) และเคสไม่มีพาเชียลไม่มี legend ค้าง ✅ ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบและ `save()` จริงหลังทดสอบเสร็จ

### 196. หน้ารายละเอียดบ่อ — เพิ่มการ์ด ผลแล็บ / กุ้งตาย / ประวัติรอบเลี้ยง (เพิ่มใหม่)

พี่ถามว่าหน้ารายละเอียดบ่อยังขาดอะไรอีกไหม — ตรวจโค้ดแล้วพบว่ามีข้อมูลรายบ่อ **3 ชุดที่แอปเก็บไว้แล้วแต่ไม่เคยแสดงในหน้านี้เลย** (`p.labResults` / `p.mortalityLog` / `p.cropHistory`) ต้องออกไปดูหน้าอื่นทุกครั้ง — พี่สั่งให้เพิ่มทั้ง 3 อัน

*(หมายเหตุ: `p.chemLog` ก็ไม่ได้แสดงเหมือนกัน แต่เป็นฟิลด์ legacy ที่เลิกใช้แล้ว ถูกแทนที่ด้วยระบบสต็อก จึงไม่เพิ่ม)*

**1. 🔬 ผลแล็บ** — เดิม `labResults` ถูกอ่านเฉพาะใน `pondRiskDisease()` เพื่อแปลงเป็นป้ายเตือนบรรทัดเดียวในการ์ด "ความเสี่ยง" (เช่น "EHP ผิดปกติจากผลแล็บล่าสุด") **แต่ดูค่าจริงไม่ได้** — การ์ดใหม่โชว์ผลล่าสุดครบ: เชื้อเขียว/เหลือง (น้ำ+ตับ) · TVC · EHP · EHP ตัวอ่อน · ลำไส้อักเสบ · เม็ดไขมันตับ · สภาพตับอ่อน · หมายเหตุ พร้อมสี/ป้ายตามเกณฑ์ — **ใช้เกณฑ์ชุดเดียวกับตารางหน้า "ผลแล็บ"** (`classifyOv`/`EHP_LBL`/`HEPATO_LBL`) ไม่ตั้งเกณฑ์ใหม่

**2. 🦐 บันทึกกุ้งตาย** — เดิมการ์ดความเสี่ยงเตือนเฉพาะตอน "ตายวันนี้เกิน 2 เท่าของค่าเฉลี่ย 7 วัน" ดูตัวเลขจริงไม่ได้ — การ์ดใหม่โชว์ ตายวันนี้ / สะสมรอบนี้ + % จากยอดปล่อย / เฉลี่ย 7 วัน + **กราฟแท่ง 7 วันล่าสุด** (แท่งแดง = วันที่สูงกว่าเฉลี่ยเกิน 2 เท่า) — ใช้ `cropMortalityLog()`/`mtLast7()`/`mtAvg7()` ชุดเดียวกับหน้า "กุ้งตาย"

**3. 📚 ประวัติรอบเลี้ยงบ่อนี้** — เดิมการ์ด "เทียบกับรอบก่อน" เอา `cropHistory` ไปทำกราฟเทียบ แต่ไม่มีตารางบอกว่าบ่อนี้เคยปิดไปกี่รอบ ได้อะไรบ้าง — การ์ดใหม่แสดงตาราง รอบ/วันปิด/จำนวนวันเลี้ยง/ผลผลิต/ไซส์/FCR/อัตรารอด/กำไร · **อ่านผ่าน `cropHarvestResult()` เสมอ** ตัวเลขจึงตรงกับหน้า P&L และ PDF ปิดบ่อ (นับพาเชียล + ฐานยอดปล่อยจริง) · คอลัมน์กำไรเป็น **admin เท่านั้น**

**ทางลัด**: ปุ่มในแต่ละการ์ดเด้งไปหน้าเต็มพร้อมเลือกบ่อนี้ให้ — `jumpLab()` / `jumpMort()` (แพทเทิร์นเดียวกับ `jumpFeed()`/`jumpHarvest()` ที่มีอยู่แล้ว) ส่วนการ์ดประวัติรอบใช้ `goCropHistoryFor()` ที่มีอยู่แล้ว

**บั๊กระหว่างทำ (แก้แล้ว)**: เขียน `onclick="jumpLab(''+p.id+'')"` ตอนแรก ทำให้ได้ `jumpLab(B1)` (ไม่มี quote) — แก้ด้วยการใช้ `const Q=String.fromCharCode(39)` แล้วต่อสตริงเป็น `'+Q+p.id+Q+'` แบบเดียวกับที่ `mpRender()` ใช้อยู่ (ปัญหา quote ซ้อนชั้นเดิมที่เจอมาหลายรอบ)

ทดสอบผ่าน browser (stub `save()`/`fbSave()`): ยิงข้อมูลจำลอง — ผลแล็บ 2 ครั้ง (ครั้งล่าสุด EHP 8% ตับซีด) · กุ้งตาย 3 วัน (วันนี้ 400 ตัว) · ประวัติ 1 รอบที่มีพาเชียล →
- ผลแล็บ: โชว์ EHP 8% ป้ายแดง · ตับ "ซีด" · หมายเหตุครบ ✅
- กุ้งตาย: ตายวันนี้ 400 ⚠️ สูงผิดปกติ · สะสม 530 ตัว (**0.120%** = 530/440,000 ถูกต้อง) · เฉลี่ย 18.6 ตัว/วัน · กราฟ 7 วันแสดงถูก ✅
- ประวัติรอบ: ผลผลิต **4,400 กก.** (3,800 + พาเชียล 600) พร้อมป้าย "รวมพาเชียล" · FCR **1.14** · อัตรารอด **52.7%** · กำไร **+240,000** (150,000 + พาเชียล 90,000) — ตรงกับที่ `cropHarvestResult()` คำนวณใหม่ ✅
- staff: ไม่เห็นคอลัมน์กำไร แต่ยังเห็น FCR/อัตรารอด และการ์ดผลแล็บ/กุ้งตายครบ ✅
- ปุ่มทางลัด `jumpLab`/`jumpMort` เปิดหน้าถูก + เลือกบ่อถูก ✅ · เคสว่างเปล่าทั้ง 3 การ์ดแสดงข้อความ ไม่ crash ✅
ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบและ `save()` จริงหลังทดสอบเสร็จ

### 195. หน้ารายละเอียดบ่อ — เพิ่มการ์ด "🦐 การจับกุ้ง / พาเชียล" (เพิ่มใหม่)

พี่ขอให้ข้อมูลการจับกุ้ง/พาเชียลของแต่ละบ่อ เข้าไปแสดงในการ์ดข้อมูลรายบ่อ (หน้ารายละเอียดบ่อ) ด้วย ไม่ต้องออกไปหน้า "จับกุ้ง" แยก

**เพิ่มการ์ดใหม่** `pdCard('harvest', '🦐 การจับกุ้ง / พาเชียล')` วางไว้ต่อจากการ์ด "อัตรารอด" — หัวการ์ดโชว์จำนวนครั้งพาเชียลของรอบนี้ (เช่น "· 2 ครั้ง") เนื้อหาข้างใน:
- **สรุปผลผลิตสะสม** (กก./ไร่ เทียบเกณฑ์พาเชียล + คาดว่าจะถึงเกณฑ์อีกกี่วัน) — ใช้ `partialHarvestInfo()` ตัวเดียวกับที่หน้าภาพรวมใช้ ไม่คำนวณคู่ขนาน
- **ตารางประวัติพาเชียลของรอบนี้** — วันที่/แพ/น้ำหนักรวม (พร้อมบรรทัดย่อยแจกแจงเกรดพิเศษ)/เงิน — ใช้ `harvestRowKg()`/`harvestRowBaht()` ตัวเดียวกับทุกจุดในระบบ (entry #192) แถวที่ข้อมูลไม่ครบไฮไลต์เหลืองเหมือนหน้าจับกุ้ง

**ทางลัด 2 ปุ่ม**:
- ปุ่ม "🦐 กรอกจับกุ้ง" ในแถบปุ่มล่างการ์ด (`jumpHarvest()`) — เด้งไปหน้าจับกุ้ง เลือกบ่อนี้ + สลับเป็นโหมด "พาเชียล" ให้อัตโนมัติ (ตามแพทเทิร์นเดียวกับ `jumpFeed()`/`jumpEntry()` ที่มีอยู่แล้ว)
- ปุ่ม ✏️ ที่แต่ละแถวในตาราง (`jumpHarvestEdit()`) — เด้งไปหน้าจับกุ้งแล้วเรียก `editPartialHarvest()` เปิดโหมดแก้ไขรายการนั้นให้ทันที ไม่ต้องไปหาแถวเองอีกที

ทดสอบผ่าน browser (stub `save()`/`fbSave()` กันเขียนข้อมูลจริง): บ่อจำลองมีพาเชียล 2 ครั้ง (ครั้งแรกครบทุกช่อง+เกรดพิเศษ, ครั้งที่สองไม่มีราคา/ยอดเงิน) → การ์ดโชว์สรุปผลผลิต กก./ไร่ ✅ · ตารางแสดงทั้ง 2 แถวพร้อมเกรดย่อย ✅ · แถวที่ 2 (ข้อมูลไม่ครบ) ไฮไลต์เหลืองถูกต้อง ✅ · หัวการ์ดนับ "2 ครั้ง" ถูกต้อง ✅ · `jumpHarvest()` เปิดหน้าจับกุ้ง เลือกบ่อ + โหมดพาเชียลถูกต้อง ✅ · `jumpHarvestEdit()` โหลดข้อมูลเดิม (น้ำหนัก/ยอดเงิน/เกรด 2 รายการ) ขึ้นฟอร์มแก้ไขถูกต้อง ปุ่มเปลี่ยนเป็น "อัปเดตรายการ" ✅ · เคสไม่มีพาเชียลเลยแสดงข้อความว่างไม่ crash ✅ ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบและ `save()` จริงหลังทดสอบเสร็จ

### 194. ปุ่มโหมดหน้าสต็อก — ตัวหนังสือขึ้นบรรทัดใหม่กลางคำ (แก้แล้ว)

พี่ส่งภาพมาว่าปุ่ม "เบิกออก"/"รับเข้า"/"แชร์ทุกบ่อ" ตัวหนังสือแตกเป็น 2 บรรทัดกลางคำ (เช่น "เบิก" ขึ้นบรรทัดแล้ว "ออก" ตกลงมาอีกบรรทัด)

**สาเหตุ**: `.stock-mode-btn` ไม่มี `white-space:nowrap` — พอ `flex:1` บีบความกว้างปุ่มลงตามพื้นที่ที่เหลือ ข้อความเลยตัดขึ้นบรรทัดใหม่กลางคำแทนที่จะรักษาความกว้างปุ่มให้พอดีกับข้อความ

**แก้แล้ว**: เพิ่ม `white-space:nowrap` ใน `.stock-mode-btn` — ใช้ร่วมกันทั้งแถบ "บันทึกรับ-เบิก" (4 ปุ่ม) และแถบ "ประวัติ" (3 ปุ่ม) เพราะเป็น class เดียวกัน

ทดสอบผ่าน browser: desktop ทุกปุ่มสูง ~32px บรรทัดเดียว `scrollWidth===clientWidth` (ไม่มีตัดคำ) ✅ · มือถือ (375px) ปุ่มยังพอดีในจอ ไม่ล้น แถบ 4 ปุ่มห่อลงบรรทัดใหม่ทั้งปุ่ม (ไม่ใช่ตัดคำกลางปุ่ม) ✅ ไม่มี console error

### 193. หน้าสต็อก แถบประวัติ: ย่อชื่อปุ่ม + เรียง dropdown เลือกบ่อตามอายุ (แก้แล้ว)

พี่ขอแก้ 2 จุดในการ์ด "ประวัติ" หน้าสต็อก:
1. ปุ่ม "📤 เบิกออก รายบ่อ" → **"📤 เบิกออก"**
2. ปุ่ม "📥 รับเข้าส่วนกลาง" → **"📥 รับเข้า"**
3. Dropdown เลือกบ่อ (ตัวกรองแถบเบิกออก) ให้เรียง **บ่ออายุเยอะสุดอยู่บนสุด**

**แก้แล้ว**: `populateStockOutFilters()` เดิมเรียงตามชื่อบ่อ (`a.name.localeCompare(b.name,'th')`) — เปลี่ยนเป็นคำนวณอายุแต่ละบ่อด้วย `pondCurAge()` (ฟังก์ชันกลางเดียวกับที่ใช้เรียงบ่อทั้งแอป) แล้วเรียงมาก→น้อย บ่อที่หา pond ไม่เจอ/ไม่มี `startDate` จะได้อายุ 0 ตกท้ายลิสต์อัตโนมัติ

ทดสอบผ่าน browser (stub `save()`): จำลอง 3 บ่ออายุ 91/61/31 วัน ที่มีประวัติเบิกออก → dropdown เรียงได้ A.1(91) → A.12(61) → A.2(31) ถูกต้อง ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบหลังทดสอบเสร็จ

### 192. จับกุ้ง: กรอกยอดเงินเองได้ + แยกกุ้งเกรดพิเศษ (เพิ่มใหม่)

พี่แจ้งว่าการจับแต่ละครั้ง **เอาน้ำหนัก × ราคา ทั้งบ่อไม่ได้** เพราะมีหลายราคาปนกัน (กุ้งนิ่ม/กุ้งผอม/กุ้งเสีย) ต้องการกรอกยอดเงินสรุปสุดท้ายเอง + เพิ่มช่องแยกน้ำหนักกุ้งเกรดพิเศษ

**ถามก่อนทำ 2 ข้อ** — พี่ตอบ: เกรดพิเศษ **แยกต่างหาก บวกเพิ่ม** จากกุ้งปกติ · ยอดเงิน **กรอกเองเสมอ มีตัวช่วยบวกให้ดู** · และหลังดู mockup พี่เลือกวิธีคิดจำนวนตัวแบบ **(ค) ใช้ไซส์กุ้งปกติคูณน้ำหนักรวมทุกเกรด** (ไม่ต้องกรอกไซส์แยกรายเกรด)

**โครงสร้างข้อมูลใหม่** (ใช้ทั้งพาเชียลและปิดบ่อ):
- `grades:[{n,kg,pr}]` — เกรดพิเศษแต่ละแถว (ชนิด/น้ำหนัก/ราคา)
- `amount` (พาเชียล) / `revenue` (ปิดบ่อ) — **ยอดเงินที่กรอกเอง เป็นตัวจริงเสมอ**

**ฟังก์ชันกลางใหม่** — ทุกจุดที่คิดน้ำหนัก/เงินของการจับ 1 ครั้ง ต้องผ่าน 3 ตัวนี้ ห้ามคูณเอง:
- `gradeKg(r)` / `gradeBaht(r)` — รวมเกรดพิเศษ
- `harvestRowKg(r)` = กุ้งปกติ + เกรดพิเศษ
- `harvestRowBaht(r)` = ใช้ `amount` ถ้ามี · **ถ้าไม่มี fallback ไปสูตรเดิม** (กุ้งปกติ×ราคา + เกรดพิเศษ) เพื่อไม่ให้ประวัติเก่าที่บันทึกก่อนมีช่องนี้กลายเป็น 0

**UI ทั้ง 2 ฟอร์ม**: กล่อง "🏷️ กุ้งเกรดพิเศษ" เพิ่ม/ลบแถวได้ (มี datalist กุ้งนิ่ม/เสีย/ผอม/9 ซม./แตกไซส์ พิมพ์เองได้) · กล่องยอดเงินตัวใหญ่กรอกเอง พร้อม **ตัวช่วยบวก** แจกแจง (ปิดบ่อ + เกรดพิเศษ + พาเชียล) และปุ่ม "↙ ใช้ค่าที่คำนวณได้" · **ป้ายเทียบ** เขียว "✓ ตรงกับที่คำนวณ" / เหลือง "ต่างจากที่คำนวณ ±X บ." — **ไม่บล็อก** เพราะราคาจริงจากแพมักไม่ตรงสูตรอยู่แล้ว

**จุดปลายทางที่แก้ตาม**: `partialTotals()` (kg/count/baht) · `hsBuyersFromApp()` (ยอดขายรายแพ) · `getCloseSummary()` (PDF+สถิติ) · `cropHarvestResult()` (บวก `gradeKg(hr)` เข้า closeKg) · ตารางประวัติพาเชียล 2 จุด (หน้าจับกุ้ง + หน้าประวัติรอบเลี้ยง) แสดงน้ำหนักรวมพร้อมบรรทัดย่อยแจกแจงเกรด · เกณฑ์ไฮไลต์เหลือง "ข้อมูลไม่ครบ" เปลี่ยนจาก "ไม่มีราคา" เป็น "ไม่มียอดเงิน"

**บั๊กระหว่างทำ (แก้แล้ว)**: สคริปต์ generate โค้ดทำ backslash escape หายอีกครั้ง (`'n'` → `'n'`) ทำให้ inline `onclick` พัง — แก้ถาวรโดย**เปลี่ยน `hvSetGrade`/`cmSetGrade` ให้รับ field เป็นตัวเลข (0/1/2) แทนชื่อ string** จะได้ไม่ต้องใส่ quote ซ้อนในสตริงเลย

ทดสอบผ่าน browser (stub `save()`/`fbSave()`): **พาเชียล** — กุ้งปกติ 4,700 กก. ไซส์ 50 + นิ่ม 200 + เสีย 100 → น้ำหนักรวม **5,000 กก.** · จำนวนตัว **250,000** (5,000×50) · ตัวช่วยบวกได้ 704,500 แต่กรอกยอดจริง 700,000 → ระบบใช้ **700,000** ✅ · biomass ลด 5,000 พอดี · FCR 1.33 ไม่เพี้ยน · ยอดขายรายแพ 5,000 กก./700,000 บ. · ตารางโชว์เกรดย่อย · กดแก้ไขโหลดเกรด+ยอดเงินกลับมาครบ ✅
**ปิดบ่อ** — ปิดบ่อ 1,600 + เกรดนิ่ม 150 = 1,750 กก. ไซส์ 45 → ตัวที่จับทั้งรอบ **328,750** (1,750×45 + พาเชียล 250,000) · FCR 1.33 · ตัวช่วยบวก 256,000+15,000+700,000 = 971,000 · กรอกจริง 965,000 → กำไรใช้ 965,000 ✅ · อัตรารอด 74.7%/82.2% ✅
ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบและ `save()` จริงหลังทดสอบเสร็จ

### 191. บันทึกพาเชียล — บังคับกรอกไซส์/น้ำหนัก + เพิ่มปุ่มแก้ไข (แก้แล้ว)

พี่แจ้งว่าพรุ่งนี้บ่อ A.1 และ A.2 จะพาเชียลบ่อละ 5 ตัน ให้ตรวจก่อนว่าใส่ข้อมูลแล้วเชื่อมกันครบทุกค่าไหม

**ผลตรวจ — ถ้ากรอกครบ ทุกอย่างถูกต้อง** (จำลองบ่อ DOC 100 · ABW 20g · รอด 77% · ปล่อย 440,000 · อาหาร 9,000 กก. · พาเชียล 5,000 กก. ไซส์ 50 ราคา 145): กุ้งที่จับ 250,000 ตัว · biomass 6,776→1,776 กก. (ลด 5,000 พอดี) · FCR 1.33 ไม่เพี้ยน · ยอดขายรายแพ 5,000 กก./725,000 บ. · ตอนปิดบ่อ ตัวที่จับทั้งรอบ 338,800 · อัตรารอด 77.0%/84.7% · รายได้ 982,520 (รวมพาเชียล) ✅

**🔴 แต่เจอกับดักร้ายแรง**: ช่อง **ไซส์/น้ำหนัก/ราคา ไม่ได้บังคับกรอก** กดบันทึกผ่านได้เลยโดยไม่เตือน — `savePartialHarvest()` ใช้ `?parseFloat():null` ทั้ง 3 ช่อง

ทดสอบเว้นไซส์ว่าง ผลคือ:
| จุด | ควรเป็น | ถ้าไม่กรอกไซส์ |
|---|---|---|
| Biomass ในบ่อ | 1,776 | **6,776 (ไม่ลดเลย)** |
| FCR ประเมิน | 1.33 | **0.76** |
| อัตรารอดตอนปิดบ่อ | 77.0% | **20.2%** |
| รายได้ (ถ้าไม่กรอกราคา) | 982,520 | ขาด **725,000** |

**สาเหตุ**: `partialTotals()` แปลง กก.→จำนวนตัว ด้วย `weight×size` ถ้าไม่มีไซส์ `count=0` → `getLatestBiomass()` หักกุ้งออก 0 ตัว กุ้งที่จับไปแล้วยังถูกนับว่าอยู่ในบ่อ แล้ว `getFCR()` ก็เอา `biomass + partialKg` ไปหาร = **นับกุ้ง 5 ตันซ้ำ 2 รอบ** FCR เลยต่ำผิดปกติ

**และแก้ย้อนหลังไม่ได้** — รายการพาเชียลมีแค่ปุ่มลบ ไม่มีปุ่มแก้ไข

**แก้แล้ว**:
1. **บังคับกรอกไซส์ + น้ำหนัก** — บล็อกการบันทึกพร้อมข้อความอธิบายว่าถ้าไม่กรอกอะไรจะเพี้ยน · ติดดาวแดง `*` ที่ label + hint ใต้ช่อง
2. **ไม่กรอกราคา → ถามยืนยัน** (ไม่บล็อก เพราะบางครั้งอาจยังไม่รู้ราคา) พร้อมบอกว่ารายได้/ยอดขายรายแพจะขาดไป
3. **เพิ่มปุ่ม ✏️ แก้ไข** ในตารางรายการ — โหลดค่าเดิมขึ้นฟอร์ม จำ id ไว้ใน `_phEditId` ให้เขียนทับแถวเดิมแทนการเพิ่มใหม่ + ปุ่ม "✕ ยกเลิกแก้ไข"
4. **ไฮไลต์เหลืองแถวที่ข้อมูลไม่ครบ** — รายการเก่าที่บันทึกไว้ก่อนมีการบังคับกรอก จะเห็นทันทีว่าต้องกดแก้
5. `savePartialHarvest()` เรียก `renderHvRoundStrip()` + `renderOv()` เพิ่ม — เดิมอัปเดตแค่ตารางรายการ ชิปสรุปรอบกับหน้าภาพรวมยังค้างค่าเก่าจนกว่าจะสลับหน้า

ทดสอบผ่าน browser (stub `save()`/`fbSave()` กันเขียนข้อมูลจริง): ไม่กรอกน้ำหนัก→บล็อก ✅ · ไม่กรอกไซส์→บล็อก ✅ · ไม่กรอกราคา→ถามยืนยัน ตอบไม่=ไม่บันทึก ตอบใช่=บันทึกได้ ✅ · แถวไม่ครบไฮไลต์เหลือง ✅ · กดแก้ไขโหลดค่าเดิมครบ ปุ่มเปลี่ยนเป็น "อัปเดตรายการ" เติมราคาแล้วเงิน 0→725,000 **ไม่เพิ่มแถวใหม่** ไฮไลต์เหลืองหายไป ✅ · จำลองสถานการณ์จริงทั้ง 2 บ่อ (5 ตัน×2) ได้ยอดขายรายแพรวม 10,000 กก./1,450,000 บ. ✅ ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบและ `save()` จริงหลังทดสอบเสร็จ

**หมายเหตุ**: เครื่องทดสอบโหลดข้อมูลจริงจาก Firebase ไม่ได้ (ต้อง login) จึงทดสอบด้วยบ่อจำลองที่ตั้งค่าให้ใกล้ของจริง — เดินผ่านโค้ดเส้นเดียวกับที่ผู้ใช้กดจริงทุกขั้นตอน

### 190. Protocol เวชภัณฑ์ — แบ่งเป็น 2 ชั้น (กลุ่มหลัก → หัวข้อย่อย)

ต่อจาก entry #189 พี่ขอให้แยกเป็น **2 หัวข้อหลัก (ผสมกิน / ลงน้ำ)** กดเข้าไปแล้วเจอหัวข้อย่อยให้เลือกอ่านอีกชั้น

**โครงสร้าง**: เพิ่ม `D.medProtoGroups=[{key,title}]` (2 กลุ่ม: `feed`/`water`) และเพิ่ม field `grp` ในแต่ละ topic
- `key` คงที่ห้ามเปลี่ยน (topic อ้างถึง) แต่ `title` เก็บใน data เพื่อให้เปลี่ยนชื่อกลุ่มได้ทีหลังโดยไม่ต้องแก้โค้ด
- ชื่อหัวข้อย่อยตัดคำซ้ำออก (เดิม "🍚 โปรแกรมผสมอาหาร (กุ้งปกติ)" → "🍚 กุ้งปกติ" เพราะกลุ่มบอกอยู่แล้วว่าเป็นผสมกิน)
- **migrate 2 ทาง**: ข้อมูลเดิม 2 ช่อง → 5 หัวข้อแบ่ง 3/2 · ข้อมูลที่สร้างไว้ตอน entry #189 (ยังไม่มี `grp`) → เดากลุ่มจากชื่อหัวข้อด้วย regex `/ลงน้ำ|ตัดเชื้อ|น้ำเข้า/` ถ้าเดาไม่ได้ลงกลุ่มแรก

**UI**: แถวกลุ่มหลักด้านบน (พร้อมตัวนับ "กรอกแล้ว/ทั้งหมด" ของกลุ่มนั้น) → แถวหัวข้อย่อยเฉพาะกลุ่มที่เลือก → textarea · เพิ่มปุ่ม **🔀 ย้ายกลุ่ม** ย้าย topic ข้ามกลุ่มได้ · สรุปหน้าภาพรวมเติมชื่อกลุ่มนำหน้า ("🍚 ผสมกิน › 🍚 กุ้งปกติ")

**จุดที่ต้องคิดเพิ่มระหว่างทำ**:
- `_mpGrpSel` แยกจาก `_mpCur` — ถ้าอนุมานกลุ่มจาก `list[_mpCur].grp` อย่างเดียว พอกดกลุ่มที่ยังไม่มีหัวข้อจะเด้งกลับกลุ่มเดิมทันที กดเข้าไม่ได้เลย
- `mpMove()` เลื่อนลำดับ**ภายในกลุ่มเดียวกันเท่านั้น** — เดิมสลับกับ index ข้างเคียงในลิสต์รวม ซึ่งอาจเป็นหัวข้อของอีกกลุ่ม ทำให้หัวข้อกระโดดข้ามกลุ่มโดยไม่ได้ตั้งใจ
- `mpAdd()` เพิ่มเข้ากลุ่มที่กำลังเปิดอยู่ ไม่ใช่กลุ่มแรกเสมอ

**บั๊ก 2 ชั้นที่เจอตอนทดสอบ role (แก้แล้ว)** — แถบปุ่มจัดการหัวข้อโผล่ให้ staff เห็น:
1. เดิมเขียน `<span class="admin-only" style="display:flex;...">` — **inline style ชนะ class เสมอ** `.admin-only{display:none}` เลยไม่มีผล
2. พอย้ายเป็น class `.mp-actions` แล้วยังใส่ `.admin-only` ไว้ด้วย → ชนกับ `body.role-admin .admin-only{display:revert!important}` ที่ specificity เท่ากัน (0,0,2,1) แต่ประกาศทีหลัง ทำให้ admin ได้ `display:block` แทน `flex` ปุ่มเรียงเพี้ยน
→ แก้โดยใช้ `.mp-actions` ตัวเดียว ไม่ใส่ `.admin-only` ซ้ำ พร้อมคอมเมนต์เตือนไว้ใน CSS

ทดสอบผ่าน browser (stub `save()`): กลุ่มหลัก 2 กลุ่ม + ตัวนับ 1/3, 1/2 ✅ · สลับกลุ่มแล้วหัวข้อย่อยเปลี่ยนถูก textarea โหลดถูก ✅ · เพิ่มหัวข้อเข้ากลุ่มที่เปิดอยู่ ✅ · ย้ายกลุ่มได้ ✅ · เลื่อนลำดับไม่ข้ามกลุ่ม ✅ · กลุ่มว่างขึ้นข้อความแทน ไม่เด้งกลับ ✅ · migrate ข้อมูลรุ่น entry #189 เดากลุ่มถูกทั้ง 3 ✅ · สรุปหน้าภาพรวมโชว์ "กลุ่ม › หัวข้อ" ✅ · staff ซ่อนปุ่มจัดการ/เพิ่ม/บันทึกครบ แต่ยังสลับกลุ่มและอ่านได้ ✅ · admin เห็นปุ่มเป็น flex ถูกต้อง ✅ ไม่มี console error — ลบข้อมูลทดสอบและคืนค่า `save()` จริงหลังทดสอบเสร็จ

### 189. ย้าย Protocol เวชภัณฑ์ ออกเป็นเมนูของตัวเอง + แบ่งเป็นหัวข้อที่จัดการเองได้ (เพิ่มใหม่)

พี่ขอย้ายการ์ด "💊 Protocol เวชภัณฑ์ล่าสุด" ออกจากหน้าภาพรวม มาเป็นเมนูใหม่ต่อจาก "🔮 คำนวณ & คาดการณ์" และแบ่งเนื้อหาเป็นหัวข้อกดเลือกอ่านได้ (ยกตัวอย่างมา 5 หัวข้อ)

**ถามก่อนทำ 3 ข้อ** (พี่เขียนว่า "เช่น" เลยไม่ชัดว่าตายตัวหรือเปลี่ยนได้) — พี่ตอบ: เมนูใหม่ในแถบซ้าย · เพิ่ม/แก้/ลบหัวข้อเองได้ · ย้ายข้อมูลเดิมเข้าหัวข้อที่ตรงกัน · และหลังดู mockup พี่สั่งเพิ่มว่า**ให้คงสรุปไว้ในหน้าภาพรวมด้วย**

**โครงสร้างข้อมูลใหม่**: `D.medProtocols=[{id,title,text,updatedAt,updatedBy,pinned}]` แทนของเดิม `D.medProtocolOverview={feedNote,waterNote,...}` ที่เป็น 2 ช่องตายตัว
- `migrateMedProtocols(d)` สร้าง 5 หัวข้อตั้งต้น แล้วย้าย `feedNote`→หัวข้อ 1 (กุ้งปกติ) · `waterNote`→หัวข้อ 4 (โปรแกรมลงน้ำ) พร้อมวันเวลา/ผู้แก้เดิม
- **ไม่ลบ `D.medProtocolOverview` ทิ้ง** เก็บเป็น backup เผื่อ migrate พลาด (ไม่มีที่ไหนอ่านแล้ว)
- เรียก migrate ใน `loadData()` และใน `mpList()` อีกชั้น กันกรณีข้อมูลจากเครื่องอื่น sync เข้ามาโดยยังไม่มี field นี้

**หน้าใหม่ `sec-medproto`**: แท็บหัวข้อ (จุดเขียว=กรอกแล้ว / จุดจาง=ยังว่าง) · textarea · ปุ่มเปลี่ยนชื่อ/ปักหมุด/เลื่อนขึ้น-ลง/ลบ/เพิ่มหัวข้อ/บันทึก — ปุ่มจัดการทั้งหมดเป็น `admin-only`, staff อ่านได้อย่างเดียว (textarea readonly, ไม่มีปุ่มบันทึก)

**สรุปในหน้าภาพรวม**: การ์ดเดิมเปลี่ยนเป็นโชว์**หัวข้อที่ปักหมุด** (📌 มีได้ทีละ 1) + ตัวนับ "2/5 หัวข้อ" + ลิงก์ "ดูทั้งหมด ›" ไปหน้าเต็ม — ถ้าหัวข้อที่ปักหมุดยังว่าง จะ fallback ไปหัวข้อแรกที่มีข้อความแทน (ไม่โชว์กล่องว่าง)

**จุดที่ระวังไว้**: ลบหัวข้อที่ปักหมุดอยู่ → ย้ายหมุดไปหัวข้อแรกอัตโนมัติ (ไม่งั้นหน้าภาพรวมว่าง) · ต้องเหลืออย่างน้อย 1 หัวข้อ · เตือนก่อนสลับแท็บถ้ายังไม่บันทึก (textarea ถูกเขียนทับตอน render ข้อความจะหายเงียบๆ) · ลบหัวข้อที่มีข้อความจะเตือนว่าจะหายถาวร · เพิ่ม `mpEsc()` escape HTML เพราะชื่อ/ข้อความผู้ใช้พิมพ์เองแล้วยัดเข้า `innerHTML`

**บั๊กระหว่างทำ (แก้แล้ว)**: ตอน generate โค้ดผ่านสคริปต์ ตัว `
` ในสตริง JS กลายเป็น**ขึ้นบรรทัดจริง**กลางสตริง ทำให้ syntax error (`Invalid or unexpected token`) — แก้โดยดึงข้อความออกมาเป็นตัวแปร `warnTxt` แยกบรรทัด แทนการฝัง escape ในสตริงยาว

ทดสอบผ่าน browser (stub `save()` กันเขียนข้อมูลจริง): migrate จากข้อมูลเดิม 2 ช่อง → ได้ 5 หัวข้อ ข้อมูลเข้าหัวข้อ 1 กับ 4 ถูกต้อง อีก 3 หัวข้อว่าง ข้อมูลเดิมยังอยู่ ✅ · แท็บ/จุดสถานะ/สลับแท็บ/บันทึก/เปลี่ยนชื่อ/ปักหมุด (มีได้ทีละ 1)/เลื่อนลำดับ/เพิ่ม/ลบ ✅ · ลบหัวข้อที่ปักหมุด → หมุดย้ายอัตโนมัติ ✅ · ลบจนเหลือ 1 แล้วลบอีกไม่ได้ ✅ · สรุปหน้าภาพรวมโชว์หัวข้อที่ปักหมุด + นับ "2/5 หัวข้อ" + ลิงก์ทำงาน ✅ · staff เข้าอ่านได้ แก้ไม่ได้ ไม่เห็นปุ่มบันทึก ✅ ไม่มี console error — ลบข้อมูลทดสอบและคืนค่า `save()` จริงหลังทดสอบเสร็จ

### 188. หน้าคำนวณ — "อาหาร กก./แสนตัว" เปลี่ยนจากผลลัพธ์ เป็นตัวแปรที่ขับการคำนวณได้ (เพิ่มใหม่)

ต่อจาก entry #187 ที่เพิ่ม "อาหาร กก./แสนตัว" เป็นผลลัพธ์ — พี่ขอให้ใช้ตัวเลขนี้เป็น**ตัวแปรป้อนเข้า**ได้ด้วย (ฟาร์มรู้อัตราการให้อาหารจริงของตัวเองอยู่แล้ว แม่นกว่าสูตรตำรา)

**เพิ่มช่อง "อาหาร กก./แสนตัว (ที่ให้จริง)"**:
- **ดึงอัตโนมัติจากบันทึกอาหารล่าสุด** (`feedPer100k(totalFeed ล่าสุด, ยอดปล่อย)`) — ถ้ายังไม่เคยกรอกอาหารเลย ใช้ค่าที่สูตร Precision Feeding แนะนำแทน พร้อมบอกที่มาใต้ช่อง
- แก้ทับได้ + ปุ่ม ↺ คืนค่าอัตโนมัติ · **เว้นว่าง = กลับไปใช้สูตรคำนวณให้** (พฤติกรรมเดิม ไม่ regress)

**เช็คบ็อกซ์ "ให้เพิ่มขึ้นตามขนาดกุ้ง" (ติ๊กไว้เป็นค่าเริ่มต้น)** — จุดนี้สำคัญ:
- **ติ๊ก**: เอาค่าที่กรอกเทียบกับค่าที่สูตรแนะนำ ณ วันนี้ ได้เป็น**ตัวคูณ** แล้วคูณกับเส้นโค้งของสูตรไปตลอดช่วงคาดการณ์ — กุ้งโตขึ้นย่อมกินมากขึ้น ถ้าใช้ค่าคงที่ตลอดจะประเมินอาหาร (และต้นทุน) **ต่ำเกินจริง** เมื่อทอดยาวหลายสิบวัน
- **ไม่ติ๊ก**: ใช้ค่าที่กรอกคงที่ตลอด

**อาหารสะสม/ต้นทุนอาหาร เปลี่ยนวิธีคำนวณตามแหล่งข้อมูล**:
- กรอกอัตราการให้จริง → **รวมอาหารรายวันจริงๆ** (`Σ fdDay(1..d)`) ตรงกว่า เพราะรู้อัตราแล้ว
- เว้นว่าง → ประมาณจาก FCR แบบเดิม (เนื้อที่เพิ่ม × FCR)

**เพิ่ม "FCR ที่จะได้จริง"** ในการ์ดวันที่เลือก (โชว์เฉพาะตอนกรอกอัตราเอง) = อาหารสะสม ÷ เนื้อที่เพิ่ม — **ขึ้นสีเหลืองเตือนเมื่อต่างจากช่อง "FCR ที่คาด" เกิน 0.25** เป็นตัวไขว้ตรวจว่าอัตราการให้อาหารที่กรอกสมเหตุสมผลกับ FCR ที่ตั้งไว้หรือไม่ (เช่น ให้ 40 กก./แสน จะได้ FCR 2.00 ทั้งที่ตั้ง FCR ไว้ 1.5 → เตือน)

ทดสอบผ่าน browser (ABW 16.2, PDG 0.28, รอด 77%, ปล่อย 440,000):
- auto-fill จากบันทึกอาหาร 132 กก. ÷ 492,000 × 100,000 = **26.8** ✅ พร้อม hint บอกที่มา
- เว้นว่าง (สูตร): วันนี้ 27.4 → +14 วัน 31.0 ✅
- กรอก 40 + ติ๊กโตตาม: วันนี้ 40.0 → +14 วัน **45.2** = สูตร 31.0 × ตัวคูณ 1.458 เป๊ะ ✅
- กรอก 40 + ไม่ติ๊ก: 40.0 คงที่ทั้ง 2 วัน ✅
- อาหารสะสม 14 วัน = **2,652 กก.** ตรงกับรวมรายวันด้วยมือ ✅ · FCR ที่จะได้จริง **2.00** ตรงกับ 2652÷1328 ✅ · ขึ้นเตือนสีเหลืองเมื่อ FCR ที่คาด = 1.5 และหายเมื่อตั้งเป็น 2.0 ✅
- เว้นว่าง → กลับไปใช้ FCR (1,328 × 1.5 = 1,992 กก.) และซ่อนบรรทัด "FCR ที่จะได้จริง" ✅
ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบหลังทดสอบเสร็จ

### 187. หน้าคำนวณ&คาดการณ์ — เพิ่ม "อาหาร/วัน" และ "อาหาร กก./แสนตัว" (เพิ่มใหม่)

พี่ขอเพิ่มข้อมูล "อาหาร กิโลกรัม/แสน" เข้าไปในหน้าคำนวณ — ทำได้โดยไม่ต้องสร้างสูตรใหม่ เพราะชิ้นส่วนที่ต้องใช้มีอยู่แล้วทั้งคู่

**สูตรที่ใช้ (ประกอบจากของเดิม ไม่สร้างคู่ขนาน)**:
- `bcFeedRateFormula(DOC, ABW)` — feeding rate (% ของน้ำหนักตัว/วัน) ตามสูตร Precision Feeding (ดร.พุทธ ส่องแสงจินดา) ตัวเดียวกับที่การ์ด "เปรียบเทียบ Biomass 2 วิธี" ใช้
- อาหาร/วัน (กก.) = `biomass × rate ÷ 100`
- อาหาร กก./แสนตัว = `feedPer100k(อาหาร/วัน, ยอดปล่อย)` — ฟังก์ชันเดิมของแอป

⚠️ **หารด้วย "ยอดปล่อย" ไม่ใช่ "ตัวที่รอด"** โดยตั้งใจ — เพื่อให้ตัวเลขเทียบกันได้กับ "กก./แสนตัว" ที่หน้าอาหารและหน้ารายละเอียดบ่อแสดงอยู่แล้ว (ทั้ง 2 จุดนั้นใช้ `feedPer100k(totalFeed, pRelease(p))`) ถ้าใช้ฐานต่างกันตัวเลขจะเทียบกันไม่ได้

**เพิ่มที่**:
- `fcSnap()` คืนค่า `rate` / `fdDay` / `fd100k` เพิ่ม · `fcEcon()` ส่งต่อทั้ง 3 ค่า
- เพิ่ม state `_fcDOC0` (อายุกุ้งวันนี้ของบ่อที่เลือก) เพราะ feeding rate ขึ้นกับอายุ ต้องรู้ DOC ณ วันที่คาดการณ์ — เซ็ตใน `fcCalc()` ก่อนเรียก `fcSnap`/`fcEcon` เสมอ
- การ์ด "วันที่เลือก": เพิ่ม 2 บรรทัด "อาหาร/วัน (กก.)" และ "อาหาร กก./แสนตัว"
- ตารางเทียบวันจับ: เพิ่ม 2 คอลัมน์เดียวกัน — **ไม่ใช่ข้อมูลเงิน staff จึงเห็นด้วย**

ทดสอบผ่าน browser (บ่อ DOC 94, ABW 16.2 ก., อัตรารอด 77%, ยอดปล่อย 440,000):
- feeding rate = **2.2%** ตรงกับ `bcFeedRateFormula(94, 16.2)` ที่คำนวณมือ ✅
- biomass 5,489 กก. → อาหาร/วัน **120.7 กก.** = `5489 × 2.2 ÷ 100` ✅
- **27.4 กก./แสนตัว** = `120.7 ÷ 440,000 × 100,000` ✅
- การ์ดและตารางแสดงครบทั้ง 2 ค่า ✅ · admin เห็น 13 คอลัมน์ / staff เห็น 8 คอลัมน์ (มีอาหาร ไม่มีเงิน) ✅
ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบหลังทดสอบเสร็จ

### 186. หน้าคำนวณ&คาดการณ์ — แก้อาการหน่วง/พิมพ์ไม่ติด (แก้แล้ว)

พี่แจ้งว่าหน้านี้ "หน่วงๆ ตอนใส่ค่าต่างๆ"

**ไม่ใช่ปัญหาความเร็ว** — วัดแล้ว `fcCalc()` ใช้เวลาแค่ **3.4 ms/ครั้ง** เร็วมาก

**Root cause**: ตอนสร้างหน้านี้ (entry #184) ผมวางช่องกรอก 2 ช่อง — **ปฏิทินเลือกวัน (`fc-pickd`)** และ **ไซส์เป้าหมาย (`fc-tgtsize`)** — ไว้ **ข้างใน `#fc-res` ซึ่ง `fcCalc()` ล้าง `innerHTML` ทิ้งแล้วสร้างใหม่ทั้งบล็อกทุกครั้ง** พอผู้ใช้พิมพ์ 1 ตัวอักษร → `oninput` → `fcCalc()` → ตัว `<input>` ที่กำลังพิมพ์อยู่ถูกทำลายและสร้างใหม่ → **โฟกัส/เคอร์เซอร์หลุดไปที่ body** ต้องคลิกใหม่ทุกตัวอักษร

ทดสอบยืนยันก่อนแก้: ช่องใน `#fc-res` → focus หลุดทุกครั้ง ❌ · ช่องในการ์ดตั้งค่า (ไม่ถูก re-render) → พิมพ์ต่อได้ปกติ ✅ — ตรงกับอาการที่พี่เจอ

**แก้แล้ว**: ย้าย 2 ช่องนี้ออกมาไว้ใน **การ์ด "ตัวแปรที่ใช้คำนวณ"** (บล็อกที่ไม่เคยถูก re-render เลย) แล้วให้ `#fc-res` แสดงผลอย่างเดียว — หัวการ์ดเปลี่ยนเป็นข้อความสรุปแทน (`📅 วันที่เลือก · 4 ก.ย. 69` / `🎯 ถึงไซส์ 40 ตัว/กก. ใช้กี่วัน`) พร้อมย้ายปุ่มลัด +7/+14/+30 วันไปด้วย และเพิ่มการตั้งค่าเริ่มต้นของช่องปฏิทินใน `initForecast()` (เดิมค่าเริ่มต้นมากับ template ที่ถูกสร้างใหม่ทุกครั้ง)

ใส่คอมเมนต์เตือนไว้ในโค้ดตรงจุดนั้นแล้วว่า **ห้ามย้าย input กลับเข้าไปใน `#fc-res`**

ทดสอบผ่าน browser: ทั้ง 4 ช่อง (`fc-tgtsize`/`fc-abw`/`fc-pdg`/`fc-surv`) พิมพ์ต่อเนื่องได้ element ไม่ถูกสร้างใหม่ ✅ · ปุ่ม +30 วัน อัปเดตปฏิทินและผลลัพธ์ถูกต้อง ✅ · เปลี่ยนไซส์เป้าหมาย 40→30 หัวการ์ดอัปเดตตาม ✅ · ตารางยัง render ครบ 6 แถว ✅ · staff ยังไม่เห็นคอลัมน์เงินและใช้ 2 ช่องนี้ได้ปกติ ✅ ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบหลังทดสอบเสร็จ

**บทเรียน**: อย่าวาง `<input>` ไว้ในบล็อกที่ถูก `innerHTML` เขียนทับจาก event ของ input นั้นเอง — เป็นวงจรทำลายตัวเอง ที่ผู้ใช้จะรู้สึกเป็น "แอปหน่วง" ทั้งที่โค้ดเร็ว

### 185. หน้า "คำนวณ & คาดการณ์" — เปิดให้ staff เข้าได้ แต่ซ่อนทุกอย่างที่เป็นเงิน (แก้แล้ว)

ต่อจาก entry #184 ที่ตั้งหน้านี้เป็น admin เท่านั้น — พี่สั่งใหม่ว่า **ให้ staff เข้าได้ แต่เห็นได้แค่ส่วนการคำนวณ ไม่เห็นตัวเงิน**

**เปิดสิทธิ์**: เอา `'forecast'` ออกจาก `ADMIN_ONLY_TABS` · เอา `admin-only` ออกจากปุ่มเมนูทั้ง 3 จุด (แถบข้าง/แท็บมือถือ/เมนู sheet) · เอา `if(APP_ROLE!=='admin')return;` ออกจาก `initForecast()`

**ซ่อนเฉพาะส่วนเงิน**:
- ช่องตั้งค่า 3 ช่อง → `admin-only`: ราคาอาหาร · ต้นทุนคงที่/วัน · ต้นทุนสะสมถึงวันนี้
- การ์ด **ตารางราคาขายตามไซส์** ทั้งใบ → `admin-only`
- **ตารางเทียบวันจับ**: staff เห็นแค่ 6 คอลัมน์ (จับเมื่อ/DOC/ไซส์/อัตรารอด/น้ำหนัก/กก.ต่อไร่) — admin เห็นครบ 11 คอลัมน์
- ป้าย "กำไรสูงสุด" และแถบเตือนเรื่องช่วงกำไร ±5% → เฉพาะ admin
- แถบเตือน "ดึงค่าอัตโนมัติไม่ได้" ตัดรายการเรื่องเงิน (ราคาอาหาร/ต้นทุนคงที่) ออกสำหรับ staff เพราะไม่เกี่ยวกับสิ่งที่เขาเห็น
- หัวการ์ด/คำอธิบายเขียนคนละเวอร์ชัน: admin = "📊 เทียบผลตอบแทนตามวันจับ" · staff = "📊 การเติบโตตามวันจับ"

**⚠️ จุดสำคัญ — คอลัมน์เงินของ staff ไม่ถูก render ออกมาเลย ไม่ใช่แค่ซ่อนด้วย CSS** (`const fcMoney=(APP_ROLE==='admin')` คุมตอนสร้าง HTML) เพราะถ้าซ่อนด้วย CSS อย่างเดียว เปิด DevTools ดู DOM ก็ยังเห็นตัวเลขได้ ส่วนช่องตั้งค่า/การ์ดราคาที่ใช้ `admin-only` เป็น input ที่ค่าไม่ได้มาจากข้อมูลลับ (เป็นค่า config) จึงยอมรับได้

**เพิ่ม CSS ใหม่**: `.staff-only` (คู่ตรงข้ามของ `.admin-only`) — `body:not(.role-admin) .staff-only{display:revert}` ใช้เขียนคำอธิบายเวอร์ชันที่ไม่พูดถึงเงิน

ทดสอบผ่าน browser ทั้ง 2 role (stub `save()` กันเขียนข้อมูลจริง):
- **staff**: เปิดหน้าได้ ✅ · ตารางเหลือ 6 คอลัมน์ ไม่มีคอลัมน์เงิน ✅ · ช่องราคาอาหาร/ต้นทุนคงที่/ต้นทุนสะสม ซ่อน ✅ (เช็คด้วย `offsetParent===null` ไม่ใช่ `getComputedStyle` เพราะ element ลูกของ parent ที่ `display:none` ยังรายงาน display ของตัวเองอยู่) · การ์ดตารางราคา ซ่อน ✅ · **ทั้งหน้าไม่มีคำว่า "กำไร"/"ต้นทุน"/"รายได้" เลย** ✅ · ช่อง ABW/PDG/เกณฑ์ กก.ต่อไร่ ยังเห็นและใช้งานได้ ✅
- **admin**: ครบ 11 คอลัมน์เหมือนเดิม ✅ · การ์ดตารางราคาเห็น ✅ · หัวการ์ดเป็นเวอร์ชันเต็ม ✅ · ตัวเลขแถวแรกตรงกับก่อนแก้ (702,536 / 108.4 / +107,536) ไม่ regress ✅
ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบและ `save()` จริงหลังทดสอบเสร็จ

### 184. หน้าใหม่ "🔮 คำนวณ & คาดการณ์" (เพิ่มใหม่ — admin เท่านั้น)

พี่ขอหน้าใหม่สำหรับคำนวณตัวเลขคาดการณ์ล่วงหน้า (ไซส์อนาคต / กี่วันถึงไซส์เป้าหมาย / น้ำหนักในบ่ออนาคต) และให้แนะนำเพิ่มถ้ามีอะไรจำเป็น — ทำ mockup ให้รีวิว 3 รอบก่อนลงมือจริง (`Mockup และ Prototype/mockup_คำนวณคาดการณ์.html`)

**ถามก่อนทำ** พี่ตอบ: วางเป็น **เมนูแยกต่อจากกลุ่ม "ตลาด/ต้นทุน"** · PDG ใช้แบบ **ดึงอัตโนมัติ + แก้เองได้** · ราคากุ้ง **กรอกเอง** · ต้นทุนคงที่/วัน **รวมก้อนเดียวไม่ต้องแยก** (เป็นหน้าคำนวณคร่าวๆ รายละเอียดจริงดูรายบ่อ) · ระหว่างทางพี่สั่งเพิ่ม **เลือกวันจากปฏิทิน** (แทนที่จะล็อก 2 อาทิตย์) · **กก./ไร่ ณ วันจับ** · **ช่วงกำไรถ้าอัตรารอดพลาด ±5%**

**สิทธิ์**: `admin` เท่านั้น (เพิ่มเข้า `ADMIN_ONLY_TABS` + `initForecast()` เช็ค role + เมนูทั้ง 3 จุดเป็น `admin-only`) เพราะหน้านี้แสดงต้นทุน/รายได้/กำไร ซึ่งตามนโยบายเดิม staff ไม่เห็น

**⚠️ หน้านี้อ่านอย่างเดียว ไม่เขียนข้อมูลบ่อใดๆ** ค่าที่แก้ในฟอร์มเป็นการลองสมมติชั่วคราว เปลี่ยนบ่อ/เปิดใหม่แล้วกลับเป็นค่าอัตโนมัติ — ยกเว้น `D.forecastPrices` (ตารางราคาตามไซส์) ที่บันทึกถาวรเพราะใช้ร่วมทุกบ่อ

**ค่าที่ดึงอัตโนมัติจากข้อมูลจริง** (แก้ทับได้ทุกช่อง): ABW + อัตรารอด (สุ่มล่าสุด) · PDG (`getPDG`) · ยอดปล่อย (`pRelease`) · ขนาดบ่อ · FCR (`getFCR`) · ราคาอาหาร (เฉลี่ยจากสินค้าหมวด feed ÷ 25 กก./กระสอบ) · ต้นทุนคงที่/วัน ((ค่าแรง+ค่าไฟ สะสม) ÷ วันที่เลี้ยง) · ต้นทุนสะสม (`pondRunningCost`) — ทุกตัวใช้ helper เดิมที่มีอยู่แล้ว ไม่สร้างสูตรคู่ขนาน

**ส่วนประกอบ**:
1. การ์ด **เลือกวันจากปฏิทิน** — โชว์ไซส์/ABW/น้ำหนักในบ่อ/กก.ต่อไร่ ณ วันนั้น + ปุ่มลัด +7/+14/+30 วัน
2. การ์ด **ถึงไซส์เป้าหมายกี่วัน** — กรอกไซส์เป้าหมายเอง บอกวันที่และ DOC
3. การ์ด **น้ำหนักในบ่อ ณ วันนั้น** — เทียบกับตอนนี้ + กก.ต่อไร่
4. **ตารางราคาตามไซส์** — กรอกเอง เพิ่ม/ลบช่วงได้ ไซส์ระหว่างช่วงเทียบสัดส่วนเชิงเส้น (ไม่ทำนายเกินช่วงที่กรอกไว้) + ปุ่ม **"ดึงจากการขายล่าสุด"** อ่านจากพาเชียล/ปิดรอบที่มีทั้งไซส์และราคา
5. **ตารางเทียบผลตอบแทนตามวันจับ** — วันนี้/+7/วันที่เลือก/วันถึงเป้า/+14 หลังเป้า พร้อมไซส์ · อัตรารอด · น้ำหนัก · กก.ต่อไร่ (✓ เขียวเมื่อถึงเกณฑ์) · ราคา · รายได้ · ต้นทุนรวม · ต้นทุนต่อ กก. · กำไร + **ช่วงกำไรถ้าอัตรารอดพลาด ±5%** (คำนวณใหม่ทั้งชุด ไม่ใช่แค่คูณกำไร เพราะอัตรารอดต่างทำให้น้ำหนักต่าง → รายได้และค่าอาหารที่ต้องใช้ต่างด้วย)

**กันตัวเลขหลอกตา**: มีช่อง "อัตรารอดลดลง %/10 วัน" (default 1%) เพราะถ้าสมมติอัตรารอดคงที่ ตารางจะบอกว่า "รอนานยิ่งกำไร" เสมอซึ่งไม่จริง · และถ้าดึงค่าอัตโนมัติไม่ได้ (ABW/อัตรารอด/PDG/ราคาอาหาร/ต้นทุนคงที่) จะขึ้นแถบเตือนบอกชัดว่าขาดอะไรและต้องทำอะไร แทนที่จะโชว์ 0 เงียบๆ — โดยเฉพาะต้นทุนคงที่/วัน ที่ถ้าเป็น 0 จะทำให้กำไรดูสูงเกินจริง

ทดสอบผ่าน browser (stub `save()` กันเขียนข้อมูลจริง) บ่อทดสอบ สั่ง 400,000 + แถม 40,000, 3.4 ไร่, ABW 16.2, PDG 0.28, อัตรารอด 77%, อาหาร 5,000 กก.:
- auto-fill ครบทุกช่องจากข้อมูลจริง ✅ · แถบเตือนขึ้นถูกต้องเมื่อไม่มีราคาอาหาร/ต้นทุนคงที่ ✅
- เลือกวัน +14 → 50 ตัว/กก., 6,693 กก., 1,968 กก.ต่อไร่ **ตรงกับที่คำนวณด้วยมือทุกตัว** ✅
- ตารางเทียบ 5 แถว ครบ 11 คอลัมน์ · ✓ เขียวเมื่อ กก.ต่อไร่ ถึงเกณฑ์ · ป้าย ตอนนี้/วันที่เลือก/กำไรสูงสุด ✅ · ตรวจมือ: 5488.56×128 = 702,536 ตรง ✅
- ตารางราคา: แก้/เพิ่ม/ลบ/กันเหลือน้อยกว่า 2 ช่วง ✅ · ราคาที่ 45 ตัว/กก. เทียบสัดส่วนได้ 155 ✅
- ดึงจากการขายล่าสุด: ไม่มีประวัติ → เตือนถูกต้อง · มี 2 ไซส์ → ดึงมา 55=152, 42=171 ✅
- **สิทธิ์**: staff เปิดผ่าน `goTab`/`goTabByName` ไม่ได้ · ปุ่มเมนูซ่อนหมด · `initForecast()` ถูกบล็อก ✅ (admin ใช้งานได้ปกติ)
ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบและ `save()` จริงหลังทดสอบเสร็จ

**ยังไม่ได้ทำ (เสนอไว้ใน mockup พี่ยังไม่ได้เลือก)**: จุดคุ้มทุน (break-even) · อาหารที่ต้องเตรียมแยกออกมาเป็นการ์ด · ไฮไลต์วันจับที่ตรงน้ำเกิด/วันพระ

### 183. หน้า "เปรียบเทียบ" โชว์เฉพาะบ่อที่มีกุ้งอยู่ตอนนี้ (แก้แล้ว)

พี่แจ้งว่าหน้าวิเคราะห์/เปรียบเทียบมีบ่อให้กดเลือกเยอะเกินไป และบางบ่อยังไม่มีข้อมูลด้วยซ้ำ

**สาเหตุ**: `initCompare()` สร้างชิปเลือกบ่อจาก `sortedPonds()` = **ทุกบ่อใน `D.ponds`** รวมบ่อที่กำลังเตรียม บ่อว่าง และบ่อที่ปิดรอบไปแล้ว ซึ่งไม่มีข้อมูลให้เทียบเลย — ต่างจากตาราง heatmap คุณภาพน้ำในหน้าเดียวกันที่กรอง `p.startDate&&!p.closed` ไว้อยู่แล้ว ทำให้ 2 ส่วนในหน้าเดียวกันใช้เกณฑ์คนละแบบ

**แก้แล้ว 3 จุด**:
- `initCompare()` เปลี่ยนมาใช้ `sortPondsByAge(wqfeedActivePonds())` — helper เดิมที่ใช้ร่วมกับหน้าอาหาร/สุ่มตัวอย่าง (ตัดสถานะ prep/empty/closed ออก) เรียงตามอายุกุ้งเหมือนหน้าอื่น
- `getSelPonds()` เดิม fallback เป็น `D.ponds` ทั้งหมดตอนไม่ได้ติ๊กอะไรเลย → เปลี่ยนเป็น `wqfeedActivePonds()` และกรองตัวที่ติ๊กด้วยลิสต์เดียวกัน
- **ล้าง `cmpP` ที่ค้าง**: บ่อที่เคยติ๊กไว้แล้วตอนนี้ปิดรอบ/ไม่มีกุ้งแล้ว จะถูกลบออกจาก selection อัตโนมัติตอน `initCompare()` — กันกราฟค้างข้อมูลบ่อที่หายไปจากลิสต์
- เพิ่มข้อความ "ยังไม่มีบ่อที่กำลังเลี้ยงอยู่" กรณีไม่มีบ่อเลย (เดิมจะเป็นแถบว่างเปล่า)

ทดสอบผ่าน browser (stub `save()` กันเขียนข้อมูลจริง) จำลอง 8 บ่อ = มีกุ้ง 3 / เตรียม 2 / ว่าง 3:
- ชิปโชว์ 3 บ่อที่มีกุ้งเท่านั้น ✅ (เดิมโชว์ 8)
- ไม่ติ๊กอะไรเลย → `getSelPonds()` คืน 3 บ่อที่มีกุ้ง ✅ · ติ๊ก 1 บ่อ → คืน 1 ✅
- ติ๊กบ่อที่ไม่มีกุ้งค้างไว้ → ถูกล้างออกจริง ✅
- เคสไม่มีบ่อเลี้ยงเลย → ขึ้นข้อความแทน ไม่ crash ✅
ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบและ `save()` จริงหลังทดสอบเสร็จ

### 182. อุดช่องโหว่ role: staff เข้าหน้า "ตะวันปาล์ม" และ "จัดการผู้ใช้" ได้ (แก้แล้ว)

พี่แจ้งว่าไม่ต้องการให้ role staff เห็นจำนวนเงินในตะวันปาล์ม — ตรวจสอบด้วย role staff จริงแล้ว **เจอช่องโหว่จริง 2 ชั้น**

**ชั้นที่ 1 — CSS specificity ชนกัน (บั๊กแบบเดียวกับ entry #21)**
ปุ่มเมนูแถบข้าง "🌴 ตะวันปาล์ม" และ "👤 จัดการผู้ใช้" ติด class `.admin-only` ไว้แล้ว แต่ยังโผล่ให้ staff เห็น เพราะ `.side-nav .sn-top` / `.side-nav .sn-link` (class+class = **0,0,2,0**) มี specificity สูงกว่า `.admin-only{display:none}` (**0,0,1,0**) — กฎซ่อนเลยแพ้ ทั้งที่เขียนไว้ถูกแล้ว (ปุ่มเวอร์ชันมือถือ `#tab-palm` ซ่อนได้ปกติ เพราะไม่ได้อยู่ใต้ `.side-nav`)

**ชั้นที่ 2 — ไม่มี gate ในโค้ดเลย**
`initPalm()` มี `if(APP_ROLE!=='admin')return;` อยู่ แต่**ถูกข้ามได้** เพราะ `goTab()` เปิด section ให้ก่อนแล้วค่อยเรียก `initPalm()` — พอกดปุ่มที่รั่วมาจากชั้นที่ 1 หน้าตะวันปาล์มก็เปิดทั้งหน้า และ `palmTab()` / `renderPalmMoney()` ไม่มี gate เลย กดแท็บ "💰 รายรับ-รายจ่าย" / "📈 กองทุน" ต่อได้

**ถามพี่ก่อนแก้** ว่าจะให้ staff เข้าหน้าปาล์มได้ไหม (เข้าได้แต่ซ่อนเงิน vs ปิดทั้งหน้า) — พี่เลือก **ปิดทั้งหน้า admin เท่านั้น**

**แก้แล้ว 3 จุด (defense in depth)**:
1. **CSS**: เพิ่ม `.side-nav .sn-top.admin-only,.side-nav .sn-link.admin-only{display:none!important}` เป็นค่าตั้งต้น แล้วเปิดคืนเฉพาะ `body.role-admin` แยกตามชนิดปุ่ม (`flex` สำหรับ `.sn-top`, `block` สำหรับ `.sn-link` — ต้องแยกเพราะ `display:revert` จะได้ค่า default ของ `<button>` ไม่ใช่ค่าที่เลย์เอาต์ต้องการ บทเรียนเดียวกับ entry #21)
2. **`goTab()` + `goTabByName()`**: เพิ่มลิสต์ `ADMIN_ONLY_TABS=['palm','users']` เช็ค role ก่อนเปิด section — กันทั้งการกดปุ่มที่หลุดมา และการเรียกผ่านทางลัดข้ามหน้า/console
3. **`palmTab()`**: เพิ่ม `if(APP_ROLE!=='admin')return;` กันเปิดแท็บย่อยที่มีจำนวนเงินตรงๆ

ทดสอบผ่าน browser ทั้ง 2 role:
- **staff**: ปุ่มเมนูข้างไม่รั่วแล้ว ✅ · เปิดหน้าปาล์มไม่ได้ ✅ · เปิดแท็บเงินไม่ได้ ✅ · เข้าผ่าน `goTabByName()` ไม่ได้ ✅ · เปิดหน้าจัดการผู้ใช้ไม่ได้ ✅
- **admin**: ปุ่มเมนูเห็นครบ (display `flex`/`block` ถูกต้องตามชนิดปุ่ม) ✅ · เปิดหน้าปาล์ม/แท็บเงิน/จัดการผู้ใช้ได้ปกติ ✅ ไม่ over-block
ไม่มี console error

**บทเรียนซ้ำรอบที่ 3**: การซ่อนด้วย `.admin-only` เฉยๆ ไม่พอเมื่อ element อยู่ใต้ selector ที่ specificity สูงกว่า — และการซ่อน UI อย่างเดียวไม่ใช่การกันสิทธิ์ ต้องมี gate ในโค้ดคู่กันเสมอ

### 181. อัตรารอดตอนปิดบ่อ แสดง 2 ชุด (ยอดปล่อยจริง / ยอดสั่งซื้อ) ไว้เทียบความแม่นยำ (เพิ่มใหม่)

ต่อจาก entry #179-180 — พี่จะเอาตัวเลขในแอปไปเทียบกับสมุดจดมือของฟาร์มที่ใช้ **ยอดสั่งซื้ออย่างเดียว** เป็นฐาน เพื่อดูว่าฐานไหนใกล้ความจริงกว่าตอนปิดบ่อ จึงขอให้แอปเก็บ/แสดง **อัตรารอด 2 ชุดคู่กัน**

**ชุดที่ 1 (หลัก)** `survivalRate` = จับได้ทั้งรอบ ÷ **ยอดปล่อยจริง (สั่งซื้อ+แถม)** — ฐานที่แอปใช้คำนวณทุกอย่างต่อ (entry #179)
**ชุดที่ 2 (เทียบ)** `survivalRateOrder` = จับได้ทั้งรอบ ÷ **ยอดสั่งซื้ออย่างเดียว** — ฐานเดียวกับสมุดจดมือ ใช้เพื่อวิเคราะห์เท่านั้น ไม่ป้อนเข้าสูตรอื่นใดทั้งสิ้น

**เพิ่ม/แก้**:
- ฟอร์มปิดรอบ: เพิ่มช่อง `cm-survival-order` (readonly, คำนวณอัตโนมัติ) คู่กับช่องเดิม พร้อมเปลี่ยน label ให้ชัด — "อัตรารอด — ยอดปล่อยจริง (%) (สั่งซื้อ+แถม)" กับ "อัตรารอด — ยอดสั่งซื้อ (%) (ไม่รวมแถม · ไว้เทียบกับสมุดจด)"
- `calcCropEcon()` คำนวณทั้ง 2 ค่า · `confirmCloseCrop()` บันทึก `survivalRateOrder` ลง `harvestResult` ด้วย · `openCropModal()` ล้างช่องใหม่ตอนเปิดฟอร์ม
- `cropHarvestResult()` คำนวณทั้ง 2 ชุดให้รอบเก่าที่ปิดไปแล้วด้วย (ยังไม่แตะข้อมูลใน Firebase เหมือนเดิม) — รอบเก่าจึงมีตัวเลขเทียบครบทันทีโดยไม่ต้องกรอกใหม่
- `getCloseSummary()` ส่งต่อ `survivalRateOrder` → แสดงใน **PDF ปิดบ่อ** และ **หน้าสรุปปิดบ่อ** เป็น 2 บรรทัดแยกกัน
- การ์ด metric หน้าสรุปปิดบ่อ: โชว์ชุดหลักตัวใหญ่ + ชุดเทียบตัวเล็กใต้ป้าย
- **CSV export หน้า P&L**: เพิ่มคอลัมน์ "อัตรารอด-ยอดสั่งซื้อ(%)" — ⚠️ ต้องเพิ่มทั้งใน `hdr` และแถวข้อมูลพร้อมกัน (รอบแรกเพิ่มแต่แถวลืม header ทำให้คอลัมน์เหลื่อม จับได้ตอนนับคอลัมน์ก่อน/หลังแล้วแก้ทัน — ตรวจสอบแล้วตรงกัน 26 คอลัมน์ทั้งคู่)

ทดสอบผ่าน browser (stub `save()` กันเขียนข้อมูลจริง) บ่อสั่ง 400,000 + แถม 40,000 จับได้ 220,000 ตัว:
- ฟอร์มปิดรอบ: ชุด 1 = **50.0%** ✅ · ชุด 2 = **55.0%** ✅ (ทั้ง 2 ช่องมีค่า)
- รอบเก่าที่ปิดแล้ว: `cropHarvestResult()` คืน 50.0 / 55.0 ✅ · `getCloseSummary()` ส่งต่อครบทั้ง 2 ค่า ✅ · ข้อมูลต้นฉบับใน Firebase ไม่ถูกแตะ ✅
- ตรวจ source ของ `closeSummaryHtml()` มีทั้งบรรทัด "ยอดปล่อยจริง" และ "ยอดสั่งซื้อ" ✅ · CSV header/row = 26 คอลัมน์เท่ากัน ✅
ไม่มี console error — ลบข้อมูลทดสอบและคืนค่า `save()` จริงหลังทดสอบเสร็จ

### 180. อุดช่องโหว่ entry #179: รอบเก่าที่ "ไม่มีพาเชียล" ยังค้างอยู่ฐานยอดสั่งซื้อ (แก้แล้ว)

พี่ถามว่า "อัตรารอดมีข้อมูลส่วนไหนที่ยังคำนวณใช้ยอดสั่งซื้อ ไม่รวมแถมอยู่อีกไหม" — ตรวจแล้ว **เจอเหลืออยู่ 1 จุด และเป็นจุดสำคัญ**

**Root cause**: ตอนทำ entry #176 เขียน `cropHarvestResult()` ให้ออกจากฟังก์ชันทันทีถ้ารอบนั้นไม่มีพาเชียล (`if(!(pt.kg>0))return hr;`) พร้อมคอมเมนต์ว่า "ไม่มีพาเชียล = ค่าเดิมถูกอยู่แล้ว" — **จริงตอนนั้น แต่ไม่จริงอีกต่อไปหลังเปลี่ยนฐานคำนวณใน entry #179** เพราะรอบที่ปิดในช่วง entry #174→#179 ถูกบันทึก `survivalRate`/`fcr` ด้วยฐาน "ยอดสั่งซื้ออย่างเดียว" แล้วไม่เคยถูกคำนวณใหม่เลย

ทดสอบยืนยัน (บ่อสั่ง 400,000 + แถม 40,000 จับได้ 220,000 ตัว): รอบที่**ไม่มี**พาเชียลแสดง **55.0%** (ค่าเดิมที่บันทึกไว้ ฐานยอดซื้อ) ทั้งที่ควรเป็น **50.0%** — ส่วนรอบที่**มี**พาเชียลแสดง 52.7% ถูกต้องอยู่แล้ว ทำให้ประวัติ 2 แบบอยู่คนละฐานปนกันในหน้าเดียว

**แก้แล้ว**: ปรับ `cropHarvestResult()` ให้**คำนวณ `fcr` และ `survivalRate` ใหม่เสมอ** ไม่ว่ารอบนั้นจะมีพาเชียลหรือไม่ (เอา early-return ออก) — ธง `_phAdj`/`_phKg`/`totalWeightAll` ยังติดเฉพาะรอบที่มีพาเชียลจริงเหมือนเดิม (ป้าย "รวมพาเชียล" ในหน้า P&L จะไม่ขึ้นมั่ว) และยัง**ไม่แตะข้อมูลใน Firebase** เหมือนเดิม คำนวณสดตอนแสดงผลอย่างเดียว

ทดสอบผ่าน browser (stub `save()`) 3 เคส + regression:
- รอบ**ไม่มี**พาเชียล (สั่ง 400,000 + แถม 40,000, จับ 220,000 ตัว, อาหาร 5,000) → อัตรารอด **50.0%** ✅ · FCR **1.14** ✅ · ไม่ติดธง `_phAdj` ✅
- รอบ**มี**พาเชียล → อัตรารอด 52.7% ✅ · รายได้ 698,000 ✅ · ติดธง `_phAdj` ✅
- บ่อที่**ไม่มีแถม** (stockBonus=0) → ค่าคงเดิม 50.0% ✅ ไม่กระทบ
- **ไม่นับซ้ำ**: `getCloseSummary().totalWeight` = 4,400 ✅ · ยอดขายรายแพ 4,400 กก. ✅ · ข้อมูลต้นฉบับใน `c.harvestResult` ไม่ถูกเขียนทับ ✅
ไม่มี console error — ลบข้อมูลทดสอบและคืนค่า `save()` จริงหลังทดสอบเสร็จ

**บทเรียน**: คอมเมนต์ที่เขียนว่า "ค่าเดิมถูกอยู่แล้ว" อ้างอิงสมมติฐาน ณ เวลานั้น — พอสมมติฐาน (ฐานคำนวณ) เปลี่ยน early-return แบบนี้จะกลายเป็นช่องโหว่เงียบๆ ที่ไม่มีอะไรเตือน

### 179. กลับไปใช้ "ยอดสั่งซื้อ + ยอดแถม" เป็นฐานคำนวณทุกจุดที่เกี่ยวกับจำนวนลูกกุ้ง (ย้อน entry #174)

พี่สั่งเปลี่ยนกลับ: **ทุกสูตรที่เกี่ยวกับจำนวนลูกกุ้ง — อัตรารอด, FCR ประเมิน, อาหาร กก./แสนตัว, น้ำหนักรวม (biomass) — ให้คำนวณจากยอดสั่งซื้อ + ยอดแถม** (`pRelease(p)`) ทั้งหมด

**หมายเหตุประวัติ**: นี่คือการ**ย้อน entry #174** ที่เคยเปลี่ยนให้ใช้ยอดสั่งซื้ออย่างเดียว (ไม่รวมแถม) — กลับมาเป็นแนวเดียวกับ entry #62 เดิม แต่รอบนี้ครอบคลุมกว่า เพราะรวมจุดที่เพิ่มเข้ามาทีหลัง (การ์ดอัตรารอด entry #172, พาเชียล entry #176, % กุ้งตาย entry #175) ด้วย

**แก้ 12 จุด** (`p.stock` / `c.stock` → `pRelease(p)` / `c.stock+c.stockBonus`):
- `getLatestBiomass()` — biomass ประเมิน (หน้ารายละเอียดบ่อ/ภาพรวม/FCR)
- `getFCR()` (ผ่าน biomass) และ `fcPeriodFCR()` — FCR ประเมิน + FCR เฉพาะช่วง 2 ครั้งสุ่ม
- `getBioCompareCalc()` — การ์ด "เปรียบเทียบ Biomass 2 วิธี" + การ์ด "🦐 อัตรารอด"
- `bmBuildSeries()` ทั้ง 3 จุด — การ์ด "เทียบกับรอบก่อน" (รอบที่ปิดแล้ว/บ่อพี่น้อง/บ่อปัจจุบัน)
- `calcBiomass()` — พรีวิว biomass สดตอนกรอกฟอร์ม "สุ่มตัวอย่าง"
- `trialFcrInRange()` — FCR ในช่วงทดลอง (หน้าทดลองเวชภัณฑ์)
- `calcCropEcon()` — ช่อง "อัตรารอด" ตอนปิดรอบ
- `cropHarvestResult()` — อัตรารอดของรอบที่ปิดแล้ว (คำนวณใหม่ตอนแสดงผล)
- `renderMtSummary()` — "% กุ้งตายสะสม จากจำนวนปล่อย"

**เพิ่มเติม**: คอลัมน์ "ยอดปล่อย" ในตาราง "จัดการบ่อ" เดิมแสดงแค่ยอดสั่งซื้อ ทั้งที่หัวคอลัมน์เขียนว่า "ยอดปล่อย" — แก้ให้แสดง `pRelease(p)` (ยอดปล่อยจริง) พร้อมบรรทัดเล็กแจกแจง "สั่ง X + แถม Y" ใต้ตัวเลขเมื่อบ่อนั้นมียอดแถม

**จุดที่ไม่ต้องแก้** (ใช้ `pRelease` อยู่แล้วตั้งแต่ entry #62): ความหนาแน่นตัว/ตร.ม. (`dens`), อาหาร กก./แสนตัว (`feedPer100k`), ต้นทุนลูกกุ้งต่อตัวในการ์ดต้นทุนสะสม

ทดสอบผ่าน browser (stub `save()` กันเขียนข้อมูลจริง) — บ่อทดสอบ สั่งซื้อ 400,000 + แถม 40,000 = **440,000**:
- `pRelease` = 440,000 ✅ · biomass (ABW 20g, อัตรารอด 50%) = 4,400 กก. ✅ · FCR (อาหาร 5,000) = 1.14 ✅ · `getBioCompareCalc().farmCount` = 220,000 ตัว ✅
- อัตรารอดตอนปิดรอบ (จับ 4,400 กก. ไซส์ 50) = **50.0%** ✅ (ถ้าใช้ยอดซื้ออย่างเดียวจะได้ 55.0% — ยืนยันว่าเปลี่ยนฐานจริง)
- % กุ้งตายสะสม (ตาย 4,400 ตัว) = **1.000%** ✅ · อาหาร กก./แสนตัว = 1,136 ✅
- รอบที่ปิดแล้ว + มีพาเชียล: caught รวม 232,000 ตัว → อัตรารอด **52.7%** ✅ (ยอดซื้ออย่างเดียวจะได้ 58.0%)
ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบและ `save()` จริงหลังทดสอบเสร็จ

### 178. แก้ entry #177: บ่อเตรียมกรอกน้ำได้เฉพาะที่ใกล้ลงกุ้ง ไม่ใช่ทุกบ่อ (แก้แล้ว)

พี่ทักว่า entry #177 ทำผิดไป — เดิมให้บ่อสถานะ "เตรียม" **ทุกบ่อ** ขึ้นในหน้าบันทึกคุณภาพน้ำ แต่ที่ต้องการจริงคือ **เฉพาะบ่อที่ใกล้ถึงวันลงกุ้ง (ภายใน 7 วันก่อนลงกุ้ง)** เท่านั้น ไม่ใช่ทุกบ่อที่อยู่ระหว่างเตรียม (บางบ่ออาจเพิ่งเริ่มเตรียม อีกเป็นเดือนกว่าจะลงกุ้งจริง ไม่จำเป็นต้องตรวจน้ำตั้งแต่ตอนนั้น)

**แก้แล้ว**: `wqEntryPonds()` เปลี่ยนเงื่อนไขสำหรับบ่อสถานะ `prep` จาก "รวมทุกบ่อ" เป็น **ต้องมีการตั้งวันลงกุ้งไว้แล้ว** (`p.prepChecklist.stockDate`) **และเหลือเวลาไม่เกิน 7 วันก่อนถึงวันนั้น** (`dDiff(today(),stockDate)<=7` — ครอบคลุมกรณีเลยกำหนดแล้วยังไม่ได้ลงจริงด้วย ไม่ตัดออกทันทีที่ถึงวัน) บ่อที่ยังไม่ตั้งวันลงกุ้ง หรือวันลงกุ้งยังอีกไกลเกิน 7 วัน จะไม่ขึ้นในหน้านี้

ทดสอบผ่าน browser (สร้างบ่อทดสอบสังเคราะห์แยกจากข้อมูลจริง กันปนกับ record เก่าที่มีอยู่แล้วของบ่อจริงซึ่งทำให้ผลทดสอบเพี้ยน): บ่อเตรียมที่ตั้งวันลงกุ้งอีก 20 วัน → **ไม่ขึ้น** ✅, บ่อเตรียมที่ตั้งวันลงกุ้งอีก 5 วัน → **ขึ้น** ✅, บ่อเตรียมที่ยังไม่ตั้งวันลงกุ้งเลย → **ไม่ขึ้น** ✅ ไม่มี console error

### 177. บ่อที่ "กำลังเตรียม" (prep) กรอกคุณภาพน้ำได้แล้ว — ตรวจน้ำก่อนลงกุ้ง (เพิ่มใหม่)

พี่ต้องการให้บ่อที่กำลังเตรียมอยู่ (ยังไม่ลงกุ้ง ไม่มี `startDate`) สามารถบันทึกคุณภาพน้ำได้ด้วย เพราะมีการตรวจน้ำก่อนลงกุ้งจริง — เดิมหน้า "บันทึกคุณภาพน้ำ" ใช้ `wqfeedActivePonds()` ซึ่งกรองบ่อสถานะ `prep` ออกไปโดยตั้งใจ (ฟังก์ชันนี้ใช้ร่วมกับหน้าอื่นๆ อีกหลายหน้าที่ต้อง "มีกุ้งในบ่อจริง" เช่น อาหาร/สุ่มตัวอย่าง/โรค/สต็อก ซึ่งไม่ควรรวมบ่อเตรียมด้วย)

**แก้แล้ว**: เพิ่มฟังก์ชันใหม่ `wqEntryPonds()` = เหมือน `wqfeedActivePonds()` แต่**รวมสถานะ `prep` ด้วย** ใช้เฉพาะหน้า "บันทึกคุณภาพน้ำ" จุดเดียว (4 จุด: `initEntry`, `restoreLastEntry`, `epNextUnfilled`, `renderEpChips`) — หน้าอื่นๆ ทั้งหมดยังใช้ `wqfeedActivePonds()` เดิม ไม่กระทบ

บ่อเตรียมในชิปเลือกบ่อจะมีไอคอน 🧹 ต่อท้ายชื่อบ่อให้แยกจากบ่อที่กำลังเลี้ยงจริง

**บั๊กที่เจอระหว่างทำ (แก้แล้ว)**: บ่อเตรียมไม่มี `startDate` ทำให้จุดที่คำนวณ "อายุ" จาก `dDiff(p.startDate,...)` ได้ `NaN` แทนที่จะเป็นค่าว่าง — แก้ 4 จุด: ช่องอายุในฟอร์ม (`calcAge`) แสดงว่างแทน NaN, `saveWQ()` บันทึก `age:null` แทน NaN ลงฐานข้อมูล, ตารางประวัติค่าน้ำ (ทั้งหน้าบันทึกคุณภาพน้ำและหน้ารายละเอียดบ่อ) แสดง "-" แทน "null", และป้าย "⏱ อายุ X วัน" บนหน้ารายละเอียดบ่อ (3 จุดที่มีสูตรเดียวกัน) แสดง 0 แทน null เมื่อบันทึกล่าสุดไม่มี age

ทดสอบผ่าน browser (stub `save()` กันเขียนข้อมูลจริง): ตั้งบ่อทดสอบเป็นสถานะ `prep` (ลบ `startDate`) ยืนยันว่าปรากฏในรายการกรอกน้ำ (ไม่ปรากฏในรายการอาหาร) ชิปมีไอคอน 🧹 ถูกต้อง กรอก pH แล้วบันทึกสำเร็จโดยไม่มี NaN ในช่องอายุหรือในข้อมูลที่บันทึก ตารางประวัติแสดง "-" ในคอลัมน์อายุถูกต้อง และหน้ารายละเอียดบ่อแสดง "อายุ 0 วัน" แทน "อายุ null วัน" หลังแก้ ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบและ `save()` จริงหลังทดสอบเสร็จ

### 176. พาเชียลถูกนับเข้า FCR / อัตรารอด / รายได้ แล้ว (ข้อ 1 จากผล audit — แก้แล้ว)

ต่อจาก entry #175 (ทำข้อ 2/3/4 ไปแล้ว) — ข้อ 1 คือปัญหาที่กระทบตัวเลขการเงินหนักสุด: **กุ้งที่จับพาเชียลไปแล้ว "กินอาหารไปแล้ว" และ "ขายได้เงินแล้ว" แต่ทุกสูตรคิดจากน้ำหนักตอนปิดบ่ออย่างเดียว** ทดสอบพบ FCR 2.00 (ควร 1.50) · อัตรารอด 37.5% (ควร 54.0%) · รายได้ขาดไป 150,000 บ.

**ก่อนแก้ ไล่ผลกระทบทั้งระบบให้พี่ดูก่อน แล้วเจอกับดักสำคัญ 1 อย่าง** ที่ทำให้ต้องปรับแบบจาก mockup เดิม: ถ้าเปลี่ยน `harvestResult.totalWeight` ให้รวมพาเชียลตรงๆ ตามที่ออกแบบไว้แรก **จะเกิดการนับซ้ำ 2 เท่า** ที่ `getCloseSummary()` (PDF ปิดบ่อ + สถิติ) และ `hsBuyersFromApp()` (ยอดขายรายแพ/ห้องเย็น) เพราะ 2 ฟังก์ชันนี้บวกพาเชียลแยกอยู่แล้ว

**แนวทางที่ใช้จริง** — ปลอดภัยกว่าและย้อนกลับได้:
1. **ไม่แตะโครงสร้างข้อมูลเดิมเลย** `harvestResult.totalWeight` ยังเก็บ "น้ำหนักปิดบ่อ" เหมือนเดิม
2. แก้เฉพาะ **3 ค่าที่คำนวณผิด** (fcr / survivalRate / revenue+netProfit)
3. **รอบเก่าใช้วิธีคำนวณใหม่ตอนแสดงผล ไม่เขียนทับข้อมูลใน Firebase** — ถ้าไม่ถูกใจแค่เลิกเรียกฟังก์ชันก็กลับเป็นเหมือนเดิมทันที ไม่มีอะไรเสียหาย

**ฟังก์ชันกลางใหม่ 2 ตัว**:
- `partialTotals(list)` → `{kg, count, baht}` รวมยอดพาเชียล (จำนวนตัว = กก.×ไซส์ของครั้งนั้น, เงิน = กก.×ราคาของครั้งนั้น — คิดรายครั้งเพราะไซส์/ราคาแต่ละครั้งไม่เท่ากัน)
- `cropHarvestResult(c)` → คืน harvestResult ฉบับคำนวณใหม่ให้นับพาเชียลแล้ว **โดยไม่แตะข้อมูลต้นฉบับ** — รอบที่ไม่มีพาเชียลคืนค่าเดิมทุกตัว (ไม่กระทบอะไรเลย) รอบที่มีจะติดธง `_phAdj` ให้ UI รู้ + เพิ่ม `totalWeightAll`

**จุดที่แก้**:
- `calcCropEcon()` (ฟอร์มปิดรอบ) — FCR/อัตรารอด/ตัวที่จับได้/รายได้ นับพาเชียลรวมแล้ว + เพิ่มกล่องสรุป `#cm-partial-box` โชว์ "พาเชียล N ครั้ง · X กก. · Y บ. → ผลผลิตรวมทั้งรอบ Z กก." ดึงอัตโนมัติจากหน้าจับกุ้ง
- เปลี่ยนชื่อช่อง "ผลผลิตรวม (กก.)" → **"น้ำหนักที่จับตอนปิดบ่อ (กก.) (เฉพาะครั้งนี้ ไม่ต้องรวมพาเชียล)"** และ "ตัวที่จับได้" → "ตัวที่จับได้ทั้งรอบ (รวมพาเชียล)" กันกรอกสับสน
- จุดอ่านประวัติ 12 จุดเปลี่ยนไปเรียก `cropHarvestResult(c)`: วิเคราะห์โรงเพาะฟัก, `getCloseSummary` (PDF+สถิติ), P&L (rows + CSV export), ตารางประวัติรอบเลี้ยง, หน้าสรุปปิดบ่อ, กราฟ FCR/อัตรารอด/รายได้/กำไร (2 ชุด), กราฟเปรียบเทียบรอบ
- P&L: คอลัมน์ผลผลิตและ "ต้นทุน/กก." ใช้ `totalWeightAll` + โชว์บรรทัดเล็ก "รวมพาเชียล X" ใต้ตัวเลขให้รู้ว่าแถวไหนถูกคำนวณใหม่
- **ตัวเลขระหว่างเลี้ยง** (คำนวณสด ไม่ได้บันทึก จึงถูกทันทีทุกบ่อ ไม่ต้อง migrate): `getLatestBiomass()` หักจำนวนตัวที่พาเชียลไปแล้วออกจาก biomass ในบ่อ · `getFCR()` หารด้วย (biomass ในบ่อ + พาเชียลที่จับไปแล้ว) · `fcPeriodFCR()` บวกน้ำหนักพาเชียลที่คั่นระหว่าง 2 ครั้งสุ่มกลับเข้า gain (ไม่งั้น gain ติดลบแล้ว FCR ช่วงนั้นเพี้ยน)

ทดสอบผ่าน browser (stub `save()` กันเขียนข้อมูลจริง) ด้วยเคส: ปล่อย 400,000 · อาหาร 6,000 กก. · พาเชียล 600กก.@ไซส์70 + 400กก.@ไซส์60 ขาย 150 · ปิดบ่อ 3,000 กก. ไซส์ 50 ขาย 160
- **ฟอร์มปิดรอบ**: FCR 1.50 ✅ · อัตรารอด 54.0% ✅ · รายได้ 630,000 ✅ · ตัวที่จับได้ 216,000 ✅ · กล่องสรุปพาเชียลแสดงถูกต้อง
- **รอบเก่าคำนวณใหม่**: 2.00→1.50 · 37.5%→54.0% · 480,000→630,000 · กำไร 100,000→250,000 · **ยืนยันข้อมูลต้นฉบับใน object ไม่ถูกแตะ** ✅
- **กันการนับซ้ำ**: `getCloseSummary().totalWeight` = 4,000 (ไม่ใช่ 5,000) ✅ · ยอดขายรายแพ = 4,000 กก. / 630,000 บ. ✅
- **ไม่ regress**: รอบที่ไม่มีพาเชียลได้ค่าเดิมทุกตัว ไม่มีธง `_phAdj` ✅
- **ระหว่างเลี้ยง**: biomass ในบ่อ 3,000 กก. (หัก 66,000 ตัวที่จับไปแล้ว) ✅ · FCR ประเมิน 1.50 ✅
ไม่มี console error — ลบข้อมูลทดสอบและคืนค่า `save()` จริงหลังทดสอบเสร็จ

**หมายเหตุ**: ยังไม่ทราบว่าประวัติจริงของฟาร์มมีกี่รอบที่มีพาเชียล (แท็บทดสอบโหลดข้อมูล Firebase จริงไม่ได้ ใช้ชุด seed 8 บ่อที่ไม่มีประวัติ) — รอบที่ไม่มีพาเชียลตัวเลขจะไม่ขยับเลย ส่วนรอบที่มีจะเห็นตัวเลขเปลี่ยนพร้อมป้าย "รวมพาเชียล" ในหน้า P&L

### 175. Audit ทั้งระบบ — แก้ 3 จุดที่ตัวเลขไม่สมเหตุสมผล (ข้อ 2/3/4 จากผลตรวจ)

พี่ให้ตรวจทั้งระบบว่ามีอะไร "ไม่ make sense" บ้าง — ตรวจแล้วเจอ 4 กลุ่มปัญหา ทำ mockup เสนอทางแก้ทั้งหมด (`Mockup และ Prototype/mockup_แก้4จุดที่ตัวเลขไม่ตรง.html`) แล้วพี่สั่งให้ทำข้อ 2/3/4 ก่อน (ข้อ 1 = พาเชียลไม่ถูกนับใน FCR/อัตรารอด/รายได้ ยกไปทำรอบถัดไป เพราะกระทบตัวเลขการเงินและประวัติเก่า)

**ข้อ 2 — "ต้นทุนสะสม" มี 3 สูตร ไม่ตรงกัน (แก้แล้ว)**

มี 3 จุดในแอปที่โชว์ "ต้นทุนสะสมของบ่อ" แต่คำนวณคนละสูตร — การ์ดหน้ารายละเอียดบ่อรวม "อุปกรณ์นอก/น้ำมัน" (โหมดแชร์ทุกบ่อ) ส่วน `pondRunningCost()` (ชิปในตารางรอบเลี้ยง + แถบหน้าจับกุ้ง) และ `ceoTotalCost()` (KPI หน้าภาพรวม) **ลืมบวกทั้งคู่** — ทดสอบพบต่างกัน 1,400 บ./บ่อ (101,400 vs 100,000)

⚠️ **จุดนี้เป็นความพลาดของผมเองตอนแก้ entry #169** — ตอนนั้นรวมจุดตัดหมวดสินค้าให้เป็น single source of truth จริง แต่ไม่ได้ไล่ขึ้นไปถึงฟังก์ชันรวมยอดชั้นบนอีก 2 ตัว จึงยังเหลืออาการเดิมที่พี่แจ้งไว้

**แก้แล้ว**: ทำ `pondRunningCost(p)` เป็น single source of truth ตัวเดียวของ "ต้นทุนสะสมของบ่อ ณ วันนี้" — เพิ่ม `shared`/`fuel` เข้าไปในผลลัพธ์, คืนค่าดิบไม่ปัดเศษ (ให้ผู้เรียกปัดตอนแสดงผล กันยอดรวมกับยอดรายหมวดเพี้ยนจากการปัดซ้อน), แล้วให้ `ceoTotalCost()` เหลือแค่ `return pondRunningCost(p).total` และการ์ดหน้ารายละเอียดบ่อดึงยอดรวมจากฟังก์ชันนี้แทนการบวกเอง (ตัวเลขรายหมวดยังคำนวณเองเพื่อแจกแจงรายการย่อย แต่ยอดรวมมาจากจุดเดียว) + เพิ่มอุปกรณ์นอก/น้ำมันเข้า tooltip แจกแจงในตารางรอบเลี้ยง

**ข้อ 3 — บันทึกกุ้งตายไม่ถูกล้าง/เก็บ ตอนปิดรอบ (แก้แล้ว)**

`confirmCloseCrop()` เก็บเข้าประวัติ + ล้าง + tombstone ให้ทุก log (feedLog/samples/waterLog/chemLog/labResults/partialHarvests) **ยกเว้น `mortalityLog`** ที่ตกสำรวจทั้ง 3 จุด — ผลคือข้อมูลกุ้งตายรอบเก่าค้างข้ามไปรอบใหม่ตลอดไป ทดสอบพบ "สะสมรอบนี้" แสดง 5,200 ตัว ทั้งที่รอบปัจจุบันตายจริงแค่ 200 (อีก 5,000 เป็นของรอบก่อน) และประวัติกุ้งตายของรอบที่ปิดไปแล้วหายถาวรเพราะไม่เคยถูก archive

**แก้แล้ว** 4 จุด:
- เพิ่ม `mortalityLog` เข้า object ที่ archive ลง `cropHistory` — กรองเก็บเฉพาะรายการตั้งแต่วันลงกุ้งรอบนั้น (รายการที่เก่ากว่าคือของค้างจากรอบก่อนที่ไม่เคยถูกล้าง ปล่อยให้ตกไปกับการล้าง)
- เพิ่มเข้าชุด tombstone กันข้อมูลรอบเก่าจากเครื่องอื่นถูก sync ดันกลับเข้ารอบใหม่
- ล้าง `p.mortalityLog=[]` หลังปิดรอบ เหมือน log ตัวอื่น
- เพิ่มฟังก์ชันกลาง `cropMortalityLog(p)` กรองเฉพาะรอบปัจจุบันด้วยวันลงกุ้ง แล้วเปลี่ยนทุกจุดที่แสดงผล (สรุป 3 ชิป, กราฟแท่ง 7 วัน, ตารางประวัติ, การ์ดความเสี่ยงหน้ารายละเอียดบ่อ) มาใช้ตัวนี้แทนการอ่าน `p.mortalityLog` ตรงๆ — **จำเป็นเพราะบ่อที่ปิดรอบไปก่อนมีการแก้นี้ยังมีข้อมูลเก่าค้างอยู่** จะได้ไม่บวมจนกว่าจะถูกล้างตอนปิดรอบครั้งถัดไป

**ข้อ 4.1 — "% กุ้งตายสะสม" อยู่คนละฐานกับอัตรารอด (แก้แล้ว)**

หลัง entry #174 อัตรารอดเปลี่ยนไปหารด้วยยอดสั่งซื้อ (`p.stock`) แล้ว แต่ "% จากจำนวนปล่อย" ในหน้าบันทึกกุ้งตายยังหารด้วยยอดปล่อยรวมแถม (`pRelease`) — 2 ตัวเลขนี้อยู่คนละฐาน เอามาบวก/ลบกันไม่ได้ **แก้แล้ว**: เปลี่ยนมาหารด้วย `p.stock` ให้อยู่ฐานเดียวกันตามธรรมเนียมฟาร์ม

**ข้อ 4.2 — เตือนหน่วยอาหารผิด (เพิ่มใหม่)**

`pondFeedCost()` คิดต้นทุนอาหารโดยสมมติ "1 กระสอบ = 25 กก." เสมอ (`totalFeed/25 × unitPrice`) — ถ้ามีใครเพิ่มสินค้าหมวด "อาหารกุ้ง" แล้วตั้งหน่วยเป็น กก./ลิตร/ขวด ต้นทุนจะเพี้ยน 25 เท่าโดยไม่มีอะไรเตือน **เพิ่มแล้ว**: `saveStockItem()` เช็คว่าหมวด `feed` + หน่วยไม่เข้าเงื่อนไข `กระสอบ|ถุง|sack|bag` → ขึ้น confirm อธิบายผลกระทบ แต่**ไม่บล็อกการบันทึก** (เผื่อฟาร์มมีเคสพิเศษจริง)

ทดสอบผ่าน browser (stub `save()` กันเขียนข้อมูลจริง): ข้อ 2 — ยิงต้นทุนแชร์ทดสอบเข้าบ่อ ยืนยันทั้ง 3 จุดได้ 101,400 เท่ากันเป๊ะ (เดิม 2 จุดได้ 100,000); ข้อ 3 — ยิง mortalityLog คร่อม 2 รอบ ยืนยัน `cropMortalityLog()` กรองเหลือ 200 (จาก 5,200) และจำลอง logic ปิดรอบยืนยันว่า archive เฉพาะรายการรอบปัจจุบัน + tombstone ครบ + ล้างเกลี้ยง; ข้อ 4.1 — ยืนยัน % เปลี่ยนฐานถูกต้อง; ข้อ 4.2 — ทดสอบ regex กับ 6 หน่วย (กระสอบ/ถุง/Bag ผ่าน · กก./ลิตร/ขวด เตือน) ไม่มี console error — ลบข้อมูลทดสอบและคืนค่า `save()` จริงหลังทดสอบเสร็จ

**⚠️ ยังค้าง: ข้อ 1 (พาเชียลไม่ถูกนับ)** — FCR/อัตรารอด/รายได้ ตอนปิดบ่อคิดจากน้ำหนักปิดบ่ออย่างเดียว ไม่รวมพาเชียล ทดสอบพบ FCR 2.00 (ควร 1.50), อัตรารอด 37.5% (ควร 54.0%), รายได้ขาดไป 150,000 บ. — พี่ยืนยันแล้วว่าให้ "ผลผลิตรวม" หมายถึงรวมพาเชียล และเลือกทำรอบถัดไป (ดูแบบที่ตกลงกันไว้ในไฟล์ mockup ข้างต้น)

### 174. อัตรารอด/biomass ที่คำนวณจากอัตรารอด — เปลี่ยนฐานจาก "ยอดปล่อยจริง (รวมแถม)" เป็น "ยอดสั่งซื้อ" ทั้งระบบ (แก้แล้ว)

พี่แจ้งว่าวิธีคำนวณอัตรารอดที่ฟาร์มใช้จริง **อ้างอิงกับยอดสั่งซื้อกุ้ง (p.stock) เท่านั้น ไม่รวมยอดแถม (p.stockBonus)** — ต่างจากที่แอปใช้อยู่เดิม (`pRelease(p)=p.stock+p.stockBonus`, ตั้งใจแก้ไว้ตั้งแต่ entry #62 ให้ใช้ยอดปล่อยจริงทั้งหมดในทุกจุดคำนวณ) ตรวจสอบแล้วพบว่าสูตร "biomass/จำนวนกุ้งที่คำนวณจากอัตรารอด" ใช้ `pRelease(p)` เป็นฐานอยู่ทั่วทั้งระบบ ไม่ใช่แค่การ์ด "อัตรารอด" ที่เพิ่งทำ (entry #172/#173) — ถามพี่แล้วว่าจะแก้แค่จุดใหม่หรือทั้งระบบ พี่เลือก **ทั้งระบบ**

**หลักการแยกจุดที่แก้/ไม่แก้**: จุดที่คำนวณ **biomass หรือจำนวนกุ้งจาก `survivalRate` (%)** → เปลี่ยนฐานเป็น `p.stock` (ยอดสั่งซื้อ) ทั้งหมด เพราะตัวเลข % อัตรารอดที่ฟาร์มกรอก/อ้างอิงกันมาตลอด คิดเทียบกับยอดสั่งซื้อ ไม่ใช่ยอดปล่อยจริง — ส่วนจุดที่ใช้ยอดปล่อยจริงเพื่อเหตุผลอื่น (ความหนาแน่นตัว/ตร.ม., ต้นทุนต่อตัว, อาหารต่อแสนตัว, % กุ้งตายสะสมจาก mortality log ที่นับจากเหตุการณ์จริงไม่ใช่ประมาณจาก survivalRate) **ยังคงใช้ `pRelease(p)` เหมือนเดิม** เพราะเป็นคนละแนวคิดกัน ไม่เกี่ยวกับอัตรารอด

**แก้จุดต่อไปนี้ (`pRelease(p)`/`(x.stock+x.stockBonus)` → `p.stock`/`x.stock` เฉพาะจุดที่คูณกับ `survivalRate`)**:
- `getLatestBiomass()` — biomass ประเมินหน้ารายละเอียดบ่อ/ภาพรวม/FCR ที่อ้างอิงจากฟังก์ชันนี้
- `fcPeriodFCR()` — FCR เฉพาะช่วง 2 ครั้งสุ่มล่าสุด (การ์ด "🚦 ความเสี่ยงหลักของบ่อ")
- `getBioCompareCalc()` — การ์ด "เปรียบเทียบ Biomass 2 วิธี" + การ์ด "🦐 อัตรารอด" (entry #172)
- `bmBuildSeries()` ทั้ง 3 จุดที่เรียก — ประวัติรอบที่ปิดแล้ว, บ่อพี่น้อง, บ่อปัจจุบัน (การ์ด "เทียบกับรอบก่อน")
- `calcBiomass()` — พรีวิว biomass สดตอนกรอกฟอร์ม "สุ่มตัวอย่าง"
- `trialFcrInRange()`/`trialBiomassAt()` — FCR ในช่วงทดลอง (หน้า "ทดลองเวชภัณฑ์")
- `calcCropEcon()` — ช่อง "อัตรารอด" (`cm-survival`) auto-calc ตอนปิดรอบ (วิธี ③ อัตรารอดจริง = จับได้ ÷ ยอดสั่งซื้อ)

ทดสอบผ่าน browser (stub `save()` กันเขียนข้อมูลจริง): ตั้งบ่อทดสอบ stock=400,000 + stockBonus=40,000 (แถม 10%) survivalRate=77% → `getLatestBiomass()` และ `getBioCompareCalc().farmCount` ได้ 308,000 ตัว (=400,000×77%) ตรงตามสูตรใหม่ ไม่ใช่ 338,800 ตัว (แบบเดิมที่รวมแถม) และช่อง `cm-survival` ตอนปิดรอบคำนวณจากยอดสั่งซื้อ 400,000 ถูกต้อง ไม่มี console error — คืนค่าข้อมูลบ่อทดสอบและ `save()` จริงหลังทดสอบเสร็จ

### 173. เพิ่มกล่องข้อมูล "อัตรารอด" ต่อจากกล่อง "FCR ประเมิน" ในแถบ KPI หน้ารายละเอียดบ่อ (เพิ่มใหม่)

พี่ขอเพิ่มกล่องโชว์ตัวเลขอัตรารอดต่อจากกล่อง "FCR ประเมิน" ในแถบ KPI hero (6 กล่อง: ไซส์/น้ำหนักรวม/อาหารวันนี้/กก.แสนตัว/อาหารรวม/FCR ประเมิน) หน้ารายละเอียดบ่อ — เพิ่มเติมจากการ์ด "🦐 อัตรารอด" (entry #172) ที่ทำไปก่อนหน้า เพื่อให้เห็นตัวเลขไวๆ ตรงแถบสรุปด้านบนโดยไม่ต้องกางการ์ดย่อย

เพิ่มกล่องที่ 7 ต่อท้าย "FCR ประเมิน" โชว์ `lat.survivalRate` (ค่าล่าสุดจากสุ่มตัวอย่าง — ตัวเดียวกับที่วงแหวนในการ์ด "อัตรารอด" ใช้ ไม่สร้างค่าคู่ขนาน) สีข้อความไล่เขียว/เหลือง/แดงตามเกณฑ์เดียวกับวงแหวน (ใช้ฟังก์ชัน `survRingColor()` ร่วมกัน) พร้อมวันที่สุ่มตัวอย่างล่าสุดกำกับใต้ตัวเลข

ทดสอบผ่าน browser: ยิงข้อมูลสุ่มตัวอย่างทดสอบ (77%) เข้าบ่อทดสอบ ยืนยันกล่องโชว์ "77%" สีเขียวถูกต้อง (ตรงกับสีในวงแหวนการ์ดอัตรารอด) ไม่มี console error — ลบข้อมูลทดสอบและคืนค่า `save()` จริงหลังทดสอบเสร็จ

### 172. หน้ารายละเอียดบ่อ — เพิ่มการ์ด "🦐 อัตรารอด" (เพิ่มใหม่)

พี่ต้องการเพิ่มข้อมูลอัตรารอดของกุ้งในหน้ารายละเอียดบ่อ — คุยกันแล้วสรุปวิธีคำนวณที่ใช้ได้จริงมี 3 แบบ: ① กรอกเองจากประสบการณ์/สุ่มตัวอย่าง (มีอยู่แล้ว) ② คำนวณย้อนกลับจากอาหารที่ให้จริง (มี logic อยู่แล้วในการ์ด "เปรียบเทียบ Biomass 2 วิธี" แค่ไม่เคยโชว์เป็น "อัตรารอด" ตรงๆ) ③ คำนวณแม่นจริงตอนปิดบ่อ (มีอยู่แล้วในฟอร์มปิดรอบ) — รีวิว mockup กับพี่ก่อนแล้วค่อยทำจริง (`Mockup และ Prototype/mockup_อัตรารอดหน้ารายละเอียดบ่อ.html`)

**เพิ่มการ์ดใหม่ "🦐 อัตรารอด"** ในหน้ารายละเอียดบ่อ (ระหว่างการ์ด "ไซส์ล่าสุด" กับ "เปรียบเทียบ Biomass 2 วิธี") ประกอบด้วย:
- วงแหวนวงกลม (SVG progress ring) โชว์อัตรารอดที่กรอกเองล่าสุดจากสุ่มตัวอย่าง พร้อมสีเขียว/เหลือง/แดงตามเกณฑ์ (≥70%/50-69%/<50%)
- แถบเทียบกับครั้งก่อนหน้า (▲/▼ เพิ่ม/ลดกี่ %)
- แถวเทียบกับ "อัตรารอดโดยประมาณ (จากอาหารจริง)" — ดึงมาจากสูตร Precision Feeding เดิม พร้อมป้ายเตือนถ้าตัวเลข 2 แบบต่างกันเกิน 10%/25% (สูตรเดียวกับ banner ในการ์ดเปรียบเทียบ Biomass)
- แถวแสดง Biomass ล่าสุดประมาณการ
- กราฟแท่งย้อนหลังอัตรารอดทุกครั้งที่สุ่มตัวอย่างในรอบเลี้ยงปัจจุบัน (สูงสุด 6 ครั้งล่าสุด) พร้อมกำกับอายุกุ้ง (DOC)

**Refactor (single source of truth)**: ดึงตรรกะคำนวณของการ์ด "เปรียบเทียบ Biomass 2 วิธี" (เดิมอยู่ในตัว `renderBioCompare()` ทั้งก้อน) ออกมาเป็นฟังก์ชันกลาง `getBioCompareCalc(p)` แยกจากส่วน render — ทั้งการ์ดอัตรารอดใหม่และการ์ด "เปรียบเทียบ Biomass 2 วิธี" เดิม เรียกใช้ฟังก์ชันนี้ร่วมกัน กันคำนวณคู่ขนานแยกกันเหมือนที่เคยแก้บั๊กมาแล้วหลายรอบในเซสชันนี้ (elec/labor/stock cost)

ทดสอบผ่าน browser (stub `save()` กันเขียนข้อมูลจริง): ยิงข้อมูลสุ่มตัวอย่างจำลอง 5 ครั้ง (survivalRate 95→77%) + อาหารล่าสุด เข้าบ่อทดสอบ ยืนยันว่าวงแหวนโชว์ 77% สีถูกต้อง, ป้ายเทรนด์ขึ้น "▼ ลดลง 6% จากครั้งก่อน (83%)" ตรง, ตัวเลขเทียบกับอาหารจริงและ delta ตรงกับที่คำนวณด้วยมือ (53.8%, ต่าง 30.2% → ขึ้น banner แดง), การ์ด "เปรียบเทียบ Biomass 2 วิธี" เดิมยังทำงานถูกต้องหลัง refactor (ตัวเลขตรงกับการ์ดใหม่เป๊ะ ยืนยันว่าใช้สูตรเดียวกันจริง) ไม่มี console error — ลบข้อมูลทดสอบและคืนค่า `save()` จริงหลังทดสอบเสร็จ

### 171. ซ่อนการ์ดเครื่องมือ migration/seed ข้อมูลย้อนหลัง ออกจากหน้า "จัดการบ่อ" (แก้แล้ว)

พี่แจ้งว่าการ์ด "นำเข้าประวัติการเลี้ยงย้อนหลัง" (Admin เท่านั้น) ในหน้า "จัดการบ่อ" อยากให้ลบออกจากหน้าจอ กันกดโดนโดยไม่ตั้งใจแล้วข้อมูลเปลี่ยน — ตรวจดูแล้วการ์ดนี้มีทั้งหมด 7 ปุ่ม (ไม่ใช่แค่ 4 ปุ่มที่เห็นในภาพหน้าจอ) เป็นเครื่องมือ migration/seed ข้อมูลย้อนหลังที่ใช้ครั้งเดียวตอน migrate ข้อมูลเข้าระบบ งานเสร็จหมดแล้วไม่มีประโยชน์ต่อเนื่อง โดย 3 ปุ่มด้านล่างที่พี่ไม่เห็นในภาพยังเสี่ยงกว่าที่เห็นอีก:
- ⚡ Force-inject รอบ 1 ปี 2569 — เขียนข้อมูลตรงลง Firebase ข้าม ID check/tombstone ทั้งหมด
- 🔴 ลบประวัติรอบเลี้ยงเก่ากว่าปี 2568 ทิ้งถาวร — ลบถาวรกู้คืนไม่ได้ (มี auto-backup ก่อนลบ)
- ➕ เพิ่มรอบที่หาย A.1 รอบ 3 ปี 2568

ถามพี่แล้วว่าจะลบแค่ 4 ปุ่มที่เห็นหรือทั้งการ์ด — พี่เลือก **ลบทั้งการ์ด**

**แก้แล้ว**: comment-out (`<!-- ... -->`) การ์ดทั้งใบออกจากหน้า `sec-manage` (จัดการบ่อ) ไม่ลบ HTML ทิ้งถาวร เผื่อพี่ต้องการเปิดกลับมาใช้อีกในอนาคต — **ฟังก์ชัน JS เบื้องหลังทั้ง 8 ตัว** (`runSeedHistoricalCrops`, `runSeedHarvestHistory`, `migratePondNamesB2A`, `fixYear2569Round1`, `forceInjectYear2569Round1`, `fixA1Round3_2568`, `previewCropHistoryCleanup`, `cleanupOldCropHistory`) **ยังอยู่ในระบบเหมือนเดิม ไม่ได้ลบ** — ถ้าจำเป็นต้องใช้อีกในอนาคต เรียกผ่าน browser console (F12) ได้โดยตรง

ทดสอบผ่าน browser: ตรวจสอบ DOM จริง (ไม่ใช่แค่ตรวจ innerHTML string ที่ยังมีข้อความอยู่ในคอมเมนต์) ยืนยันว่าปุ่ม `runSeedHistoricalCrops` ไม่มีอยู่ใน DOM แล้ว หน้า "จัดการบ่อ" เหลือแค่ 2 การ์ด (ตารางบ่อ + logout มือถือ) ตรวจ HTML comment ปิดถูกจุดไม่หลุดก่อนกำหนด (จุดเสี่ยงเพราะเนื้อหาในคอมเมนต์มี `var(--fs11)` ที่มี `--` ซ้อนอยู่) ไม่มี console error

### 170. เพิ่มหมวดสต็อก "น้ำมัน" — คิดต้นทุนแบบแชร์ทุกบ่อเหมือน "อุปกรณ์นอก" (เพิ่มใหม่)

พี่ขอเพิ่มหมวดหมู่ต้นทุน "น้ำมัน" โดยให้เป็นข้อมูลแชร์ทุกบ่อเหมือนกับ "อุปกรณ์นอก" (ที่แก้ไว้ก่อนหน้านี้) — เดิมช่อง "ต้นทุนน้ำมัน" ในฟอร์มปิดรอบเป็นช่องกรอกเองล้วนๆ ไม่เชื่อมกับระบบสต็อกเลย

**ออกแบบ**: ใช้กลไกเดียวกับโหมด "แชร์ทุกบ่อ" ของอุปกรณ์นอกทุกอย่าง (บันทึกผ่านฟอร์มพิมพ์ชื่อ/ราคาเอง ไม่ผูกกับสต็อกคงคลัง ไม่มี "รับเข้า/เบิกออก" รายบ่อ) เพียงแต่แท็กหมวด `'fuel'` แยกจาก `'equip'`/หมวดอื่นๆ เพื่อให้แสดงเป็นคนละบรรทัดในการ์ดต้นทุนและฟอร์มปิดรอบ — **ไม่เปิดให้สร้างเป็นรายการสต็อกถาวร** (ไม่มีในฟอร์ม "เพิ่มรายการพัสดุใหม่"/ตัวกรองรายการสต็อกทั้งหมด) เพราะน้ำมันเป็นของที่ซื้อมาใช้ครั้งเดียวแบบเดียวกับอุปกรณ์นอก ไม่ใช่ของที่มียอดคงคลังให้ติดตาม

**แก้ไข**:
- เพิ่ม `fuel` เข้า `STOCK_CATEGORIES`/`STOCK_CAT_LABEL`/`STOCK_CAT_CSS`/`CEO_CAT_DOT` + CSS `.stock-cat-fuel`
- เพิ่มตัวเลือก "⛽ น้ำมัน" ในดรอปดาวน์หมวดตอนพิมพ์รายการเองในโหมด "แชร์ทุกบ่อ" (`sk-manual-cat`) และตัวกรองประวัติแชร์ (`skl-shared-cat`)
- `getSharedItemsInRange()`/`getSharedCostForPond()` เพิ่มพารามิเตอร์ `category` (ไม่บังคับ) — ระบุ `'fuel'` เพื่อดึงเฉพาะรายการน้ำมัน ไม่ระบุ = ดึงทุกหมวดเหมือนเดิม (backward compatible)
- การ์ด "ต้นทุนสะสม" (หน้ารายละเอียดบ่อ): แยกรายการแชร์ที่แท็ก `fuel` ออกจากที่เหลือ ตอนนี้มี 2 แถว "อุปกรณ์นอก (แชร์)" กับ "น้ำมัน (แชร์)" คนละยอด แต่รวมกันแล้วเท่าเดิม (`pdCostTotal` บวก `pdFuelCost` เพิ่มเข้าไป)
- ฟอร์มปิดรอบ (`openCropModal`): auto-fill ช่อง "ต้นทุนน้ำมัน" จาก `getSharedCostForPond(pid,from,to,'fuel')` และช่อง "อุปกรณ์นอก" ใช้ยอดรวมทั้งหมดลบน้ำมันออก (`getSharedCostForPond(...)−autoFuelCost`) กันนับซ้ำ — ยังแก้ไขเองได้ถ้าจำเป็นเหมือนเดิม

ทดสอบผ่าน browser (stub `save()` กันเขียนข้อมูลจริง): ยิงรายการแชร์ทดสอบเข้า `D.inventoryLog` แยก 2 หมวด (น้ำมัน 100 ลิตร×32 บาท, อุปกรณ์ 1 ชิ้น×1,000 บาท) หารด้วยบ่อ active 8 บ่อ → การ์ดต้นทุนสะสมแสดง "น้ำมัน (แชร์) ฿400" และ "อุปกรณ์นอก (แชร์) ฿125" ถูกต้อง, เปิดฟอร์มปิดรอบ auto-fill ตรงกันเป๊ะ (cm-cost-fuel=400, cm-cost-equip-out=125) ไม่มี console error — ลบข้อมูลทดสอบและคืนค่า `save()` จริงหลังทดสอบเสร็จ

### 169. "รวมต้นทุนสะสมของบ่อ" (หน้ารายละเอียดบ่อ) กับตอน "ปิดบ่อ" ตัวเลขบางช่องไม่ตรงกัน (แก้แล้ว)

พี่แจ้งว่าข้อมูล "รวมต้นทุนสะสมของบ่อ" กับข้อมูลที่ auto-fill ตอนกด "ปิดบ่อ" บางช่องไม่ตรงกัน

**Root cause**: `getStockItemsForPond()` (ฟังก์ชันกลางที่ทั้งการ์ดต้นทุนสะสมหน้ารายละเอียดบ่อ, KPI ต้นทุนสะสมหน้าภาพรวม, และฟอร์มปิดรอบเรียกใช้ร่วมกัน) เดิมตัดออกแค่หมวด `feed` เพียงหมวดเดียว ส่วนหมวดเก่า `seed` (ลูกกุ้งเดิม) ถูกตัดแยกต่างหากอีกทีเฉพาะฝั่งฟอร์มปิดรอบเท่านั้น (ใน `getStockCostGroupsForPond()`) เพื่อกันนับซ้ำกับ `p.costFry` — ทำให้ถ้ามีรายการสต็อกที่ยังติดหมวดเก่า `seed` อยู่ การ์ด "ต้นทุนสะสม" (นับรวมทุกหมวดที่ไม่ใช่ feed) จะสูงกว่าตัวเลขที่ auto-fill เข้าฟอร์มปิดรอบ (ที่ตัด seed ออกไปแล้ว)

พบอีกจุดซ้อนกัน: `getStockCostGroupsForPond()` ใช้ if/else-if ไล่แมปหมวด (`lime`→ปูน, `equip`→อุปกรณ์, `chemOral/chemWater/probioOral/probioWater`→เวชภัณฑ์) แบบไม่มี `else` ปิดท้าย — หมวดไหนไม่ตรงเงื่อนไข (เช่นหมวดเก่า `chem`, หรือหมวดแปลกๆ ในอนาคต) จะหายไปจากผลรวมฟอร์มปิดรอบเงียบๆ ทั้งที่การ์ดต้นทุนสะสมนับรวมไว้แล้ว เป็นสาเหตุเดียวกันของอาการ "บางช่องไม่ตรงกัน"

**แก้แล้ว** (ยึดหลัก single source of truth เดียวกับ entry #166/#167 — คุมจุดตัดหมวดจุดเดียว ทุกจุดที่เรียกใช้จะตรงกันเสมอ):
1. ย้ายการตัดหมวด `seed` ขึ้นไปอยู่ใน `getStockItemsForPond()` เอง (`.filter(i=>i.category!=='feed'&&i.category!=='seed')`) — เป็นจุดตัดหมวดจุดเดียวที่ทั้งการ์ดต้นทุนสะสม/KPI ภาพรวม/ฟอร์มปิดรอบใช้ร่วมกันอัตโนมัติ
2. เปลี่ยน `getStockCostGroupsForPond()` จาก if/else-if ไม่มี else ท้าย เป็น catch-all: หมวดที่ไม่ใช่ `lime`/`equip` ทั้งหมด (รวมหมวดเก่า `chem` และหมวดไม่รู้จักใดๆ) ไหลเข้ากลุ่ม "เวชภัณฑ์" (`med`) แทน — รับประกันว่า `med+lime+equip` เท่ากับผลรวมสต็อกทั้งหมดที่การ์ดต้นทุนสะสมนับไว้เป๊ะๆ เสมอ

**หมายเหตุ (ไม่ใช่บั๊ก ไม่ได้แก้)**: ฟอร์มปิดรอบมีช่อง "ค่าน้ำมัน"/"ค่าจับกุ้ง"/"% ค่าดูแล"/"% ผู้จัดการ" ที่กรอกมือเท่านั้น ไม่มี auto-fill และไม่โชว์ในการ์ดต้นทุนสะสมหน้ารายละเอียดบ่อเลย — เป็นความต่างที่ตั้งใจ (ต้นทุนตอนปิดรอบจริงมีรายการเพิ่มจากต้นทุนสะสมระหว่างเลี้ยง) ไม่ใช่ส่วนหนึ่งของปัญหาที่พี่แจ้ง

ทดสอบผ่าน browser (stub `save()` กันเขียนข้อมูลจริง): ยิงรายการสต็อกทดสอบเข้า `D.inventoryItems`/`D.inventoryLog` ภายใต้หมวดเก่า `seed` และ `chem` ผูกกับบ่อ B1 → ยืนยันว่า `seed` ถูกตัดออกจาก `getStockItemsForPond()` ตั้งแต่ต้นทาง (ไม่นับซ้ำกับ `p.costFry`), หมวด `chem` ไหลเข้ากลุ่ม `med` ผ่าน catch-all ถูกต้อง, ผลรวม `getStockCostGroupsForPond()` (med+lime+equip) เท่ากับผลรวม `getStockItemsForPond()` เป๊ะ (250=250) ไม่มี console error — ลบข้อมูลทดสอบและคืนค่า `save()` จริงหลังทดสอบเสร็จ

### 168. หน้าค่าแรง: ลบแท็บย่อย "เงินเบิกล่วงหน้า" ทิ้ง — ไม่ได้ใช้งานจริง

พี่แจ้งว่าแท็บ "💳 เงินเบิกล่วงหน้า" ในหน้าค่าแรงไม่ได้ใช้งานจริง ให้ลบออก

**ตรวจก่อนลบ**: ระบบนี้ (บัญชีลูกหนี้ `D.staffAdvances`/`D.staffAdvanceRepay`) ถูกออกแบบให้แยกอิสระจากค่าแรงจริงตั้งแต่แรก (มีคอมเมนต์กำกับไว้ชัดเจนตอนสร้าง: "ไม่แตะ dw-amount/getLaborCostForPond เลย") — ไม่ได้ป้อนเข้าสูตรคำนวณค่าแรง/กก., ไม่เชื่อมกับฟอร์ม "ปิดรอบ", ไม่มีหน้าไหนอื่นอ้างอิงถึงเลย ลบทิ้งได้โดยไม่กระทบการคำนวณต้นทุนแรงงานใดๆ ทั้งสิ้น

**ลบ**:
- ปุ่มแท็บย่อย "💳 เงินเบิกล่วงหน้า" ออกจากแถบ `.labor-sub` (เหลือ 3 แท็บ: เงินเดือนประจำ / พนักงานรายวัน / ประวัติ-สรุป)
- HTML sub-pane `#lp-adv` ทั้งหมด (ฟอร์มบันทึกเบิก, ฟอร์มบันทึกหักคืน, ตารางรายการเบิก)
- เงื่อนไข `if(paneId==='lp-adv'){...}` ใน `laborTab()`
- ฟังก์ชัน JS ทั้งหมด: `staffAdvances()`, `staffAdvanceRepays()`, `advanceRepaidTotal()`, `advanceRemaining()`, `saveStaffAdvance()`, `deleteStaffAdvance()`, `populateAdvanceRepaySelect()`, `advRepayPreview()`, `saveStaffAdvanceRepay()`, `deleteStaffAdvanceRepay()`, `toggleAdvanceHistory()`, `renderStaffAdvancePane()`, `renderStaffAdvanceList()`

**ไม่แตะ**: `D.staffAdvances`/`D.staffAdvanceRepay` ที่อาจมีข้อมูลเก่าอยู่ใน Firebase จริง — ไม่ได้ลบข้อมูล แค่เอา UI ที่เข้าถึงออก (ถ้าพี่อยากให้ลบข้อมูลจริงด้วยแจ้งได้ แต่เนื่องจากไม่ได้ใช้งานจริงตามที่แจ้ง ประเมินว่าไม่มีข้อมูลค้างอยู่)

ทดสอบผ่าน local server: แท็บย่อยเหลือ 3 ปุ่มถูกต้อง ✓ `#lp-adv` ไม่มีใน DOM แล้ว ✓ สลับแท็บที่เหลือ (พนักงานรายวัน/ประวัติ-สรุป) ทำงานปกติ ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

### 167. หน้าค่าแรง: "ค่าแรง/กก. เฉลี่ย (รอบที่ปิด)" คิดแยกรายปีแทนถัวรวมทุกปี (แพทเทิร์นเดียวกับ entry #166)

พี่ถามว่าหน้าค่าแรงมีอะไรยังไม่ได้แก้ — ตอบว่าหน้าค่าแรงมีปัญหาแบบเดียวกับค่าไฟก่อนแก้เป๊ะ (สร้างตามแพทเทิร์นเดียวกันมาตั้งแต่แรก) พี่ยืนยันให้ทำเลย

**แก้ (เหมือน entry #166 ทุกจุด แค่สลับเป็นค่าแรง):**
- `renderLaborKPI()`: การ์ด "ค่าแรง/กก. เฉลี่ย (รอบที่ปิด)" → เพิ่มกรอง `yearBE(closeDate)===yearBE(ปีปัจจุบัน)` เอาเฉพาะรอบที่ปิดปีนี้ เปลี่ยนหัวข้อเป็น "(รอบที่ปิดปีนี้)" sub-text โชว่ "ปี {ปีปัจจุบัน}"
- `renderLaborCropSummary()`: เพิ่ม dropdown เลือกปี (`lbCropYearSel` ตามแพทเทิร์น `elCropYearSel`) — ดึงทุกปีมาก่อน (ไม่ทิ้งข้อมูลเก่า) นับจำนวนรอบต่อปีเป็น option default ปีปัจจุบัน ค่าเฉลี่ย+ป้ายเทียบเฉลี่ย+ตารางรายรอบ กรองเหลือเฉพาะปีที่เลือก สลับดูปีอื่นได้จาก dropdown

ทดสอบผ่าน local server: inject cropHistory ทดสอบ 2 รอบคนละปี (2568/2569, ไม่บันทึกจริง — stub `save()`) ยืนยัน KPI นับแค่ 1 รอบของปี 2569 ✓ ตาราง "ค่าแรง/กก. เทียบรอบที่ปิด" มี dropdown ปี 2569/2568 สลับดูได้ทั้งคู่ ข้อมูลปีเก่ายังอยู่ครบ ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

### 166. หน้าค่าไฟฟ้า: "ค่าไฟ/กก. เฉลี่ย (รอบที่ปิด)" คิดแยกรายปีแทนถัวรวมทุกปี

พี่แจ้งว่า KPI "ค่าไฟ/กก. เฉลี่ย (รอบที่ปิด)" ไม่ควรดึงข้อมูลรอบที่ปิดในปีเก่าๆ มาถัวเฉลี่ยรวมกับปีปัจจุบัน อยากให้คิดแยกเป็นรายปี (ปีไหนปีนั้น) แต่ข้อมูลรอบเก่ายังต้องเก็บไว้ดูย้อนหลังเพื่อวิเคราะห์ได้ ไม่ใช่ลบทิ้ง

**แก้**:
- `renderElecKPI()`: การ์ด "ค่าไฟ/กก. เฉลี่ย (รอบที่ปิด)" — เปลี่ยนจากนับรอบที่ปิดทั้งหมดตั้งแต่ `elecDataStartDate()` (ถัวรวมทุกปี) เป็นกรองเพิ่มด้วย `yearBE(closeDate) === yearBE(ปีปัจจุบัน)` เอาเฉพาะรอบที่ปิดในปีนี้เท่านั้น เปลี่ยนหัวข้อเป็น "(รอบที่ปิดปีนี้)" และ sub-text โชว์ "ปี {ปีปัจจุบัน}" แทน "ตั้งแต่ {วันที่}"
- `renderElecCropSummary()`: เพิ่ม dropdown เลือกปี (`elCropYearSel` ตามแพทเทิร์นเดียวกับ `elHistYearSel` ที่มีอยู่แล้วในตารางประวัติบิล) — ดึง `allRows` ทุกปีมาก่อน (ไม่ทิ้งข้อมูลเก่า) นับจำนวนรอบต่อปีทำเป็น option ให้เลือก default เป็นปีปัจจุบัน (หรือปีล่าสุดที่มีข้อมูลถ้าไม่มีรอบปิดปีนี้) ตัวค่าเฉลี่ย+ป้าย "สูงกว่าเฉลี่ย/สูงผิดปกติ"+ตารางรายรอบ กรองเหลือเฉพาะปีที่เลือกทั้งหมด — สลับปีดูย้อนหลังได้จาก dropdown โดยไม่ต้องลบข้อมูลใดๆ

ทดสอบผ่าน local server: inject cropHistory ทดสอบ 2 รอบคนละปี (2568 กับ 2569, ไม่บันทึกจริง — stub `save()`) ยืนยัน KPI นับแค่ 1 รอบของปี 2569 (ไม่รวมรอบ 2568) ✓ ตาราง "ค่าไฟ/กก. เทียบรอบที่ปิด" มี dropdown ให้เลือก "ปี 2569 (1 รอบ)" / "ปี 2568 (1 รอบ)" สลับดูได้ทั้งคู่ ข้อมูลปี 2568 ยังอยู่ครบไม่หาย ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

**หมายเหตุ**: หน้า "ค่าแรง" มีโค้ดรูปแบบเดียวกันทุกประการ (`renderLaborKPI`/`renderLaborCropSummary` ถัวเฉลี่ยรวมทุกปีเหมือนกัน) — พี่ไม่ได้ขอให้แก้ตรงนั้นด้วยรอบนี้ เลยไม่แตะ ถ้าอยากให้ทำแบบเดียวกันแจ้งได้

### 165. ลบ role CEO ออกจากแอปทั้งหมด (ทุกคนเปลี่ยนมาใช้ admin แล้ว)

พี่แจ้งว่า role "CEO" ไม่มีใครใช้งานแล้ว ทุกคนเปลี่ยนมาใช้ admin กันหมดแล้ว ให้ลบออกจากแอปได้เลย

**ก่อนลบ ตรวจโค้ดพบว่าโค้ดที่มี "ceo" ในชื่อ แบ่งเป็น 2 กลุ่มปนกันอยู่ — ต้องแยกให้ถูกก่อนลบ ไม่งั้นจะพังฟีเจอร์ของ admin ที่ไม่เกี่ยวกับ role CEO เลย:**
1. **ของเฉพาะหน้า CEO Dashboard จริงๆ** (เข้าได้เฉพาะ role=ceo, ซ่อนเมนูทั้งหมดโชว์เต็มจอ) — ลบทิ้งทั้งหมด
2. **โค้ดคำนวณ/แสดงต้นทุนสะสมที่ตั้งชื่อขึ้นต้นด้วย "ceo" ตอนสร้างหน้า CEO แต่ถูกดึงไปใช้ซ้ำที่อื่นในแอปด้วย** (เช่น การ์ด "💰 ต้นทุนสะสมรวมทั้งฟาร์ม" บน KPI bar หน้าภาพรวมสำหรับ admin, การ์ดต้นทุนสะสมแบบแยกหมวด accordion ในหน้ารายละเอียดบ่อของ admin, จุดสีหมวดหมู่ในหน้าสต็อก) — **เก็บไว้ทั้งหมด** เพราะไม่เกี่ยวกับ role เลย เป็นแค่ตั้งชื่อตัวแปร/ฟังก์ชันตามหน้าที่สร้างขึ้นก่อน

**ลบ (เฉพาะ role CEO):**
- HTML: ทั้ง section `#sec-ceo` (การ์ด KPI, รายการบ่อ, sheet รายละเอียดบ่อเมื่อกด, ปุ่มลิงก์ไปตะวันปาล์ม) และปุ่ม "← กลับแดชบอร์ด CEO" ในหน้าตะวันปาล์ม
- CSS: กฎ `body.role-ceo` ที่ซ่อนเมนู/บังคับ full-screen, คลาส `.ceo-hdr/.ceo-kpi*/.ceo-sec-*/.ceo-pond-list/.ceo-pcard*/.ceo-status-*/.ceo-mini-*/.ceo-scrim/.ceo-sheet*/.ceo-block-title/.ceo-info-list/.ceo-plain-row*/.ceo-stat*/.ceo-wq-*/.ceo-growth-note/.ceo-acc*/.ceo-hist-tbl*/.ceo-empty` และ `.role-ceo` badge
- JS: `showCeoOnly()`, `renderCeo()`, `ceoOpenSheet()`, `ceoCloseSheet()`, `ceoToggleAcc()`, `ceoToggleCatItems()`, `ceoStatus()`, `ceoFeedDisplay()`, `ceoFeedPer100k()`, `ceoRelDays()`, `ceoLeaveIfActive()`, `openPalmFromCeo()`
- ตัวเลือก role "ceo" ในฟอร์มเพิ่มผู้ใช้ + dropdown เปลี่ยนสิทธิ์ในตารางจัดการผู้ใช้ + ป้าย role badge "CEO (ผู้บริหาร)"
- `applyRole()`: ตัดสาขา `role==='ceo'` ทั้งหมด — เหลือแค่ `admin` กับ `else` (staff) **role อื่นที่ไม่ใช่ admin ทั้งหมด (รวมถึง 'ceo' เก่าที่อาจหลงเหลือใน Firebase ของ user ที่ยังไม่ได้เปลี่ยน) จะตกไปเป็น staff อัตโนมัติ** — กันเผื่อไม่ได้ error หรือได้สิทธิ์เกินตัว
- `initPalm()`: gate จาก `admin||ceo` เหลือแค่ `admin`

**เก็บไว้ (ใช้ร่วมกับฟีเจอร์อื่นที่ไม่เกี่ยวกับ role CEO เลย ลบแล้วจะพัง):** `ceoMoney()`, `CEO_CAT_DOT`, `ceoTotalCost()`, `pondCostStartDate()`, `getStockItemsForPond()`, `getSharedItemsInRange()`, `getSharedCostForPond()`, และคลาส CSS กลุ่ม `.ceo-item-row/.ceo-cat-dot/.ceo-item-main/.ceo-item-name/.ceo-item-val/.ceo-cat-row/.ceo-cat-caret/.ceo-cat-items/.ceo-cat-item-row/.ceo-cost-total-row` (การ์ดต้นทุนแยกหมวดของ admin ในหน้ารายละเอียดบ่อ + หน้าสต็อก ยังใช้ชื่อคลาสกลุ่มนี้อยู่จริง) — **ไม่ได้ rename ให้ เพราะเป็นแค่ชื่อตัวแปร/คลาสภายใน ไม่กระทบผู้ใช้งาน และลดความเสี่ยงจากการ refactor ชื่อข้ามหลายจุด** ถ้าพี่อยากให้เปลี่ยนชื่อให้ดูสะอาดขึ้นทีหลังแจ้งได้

ทดสอบผ่าน local server: `applyRole('admin')` → ไม่มี `#sec-ceo` ในหน้าเว็บแล้ว ✓ `applyRole('ceo')` (จำลอง role เก่าที่อาจหลงเหลือ) → ตกไปเป็น staff อัตโนมัติ ไม่ error ไม่ได้สิทธิ์เกิน ✓ หน้าภาพรวม KPI "ต้นทุนสะสมรวมทั้งฟาร์ม" (admin) ยังคำนวณ/แสดงถูกต้อง ✓ การ์ดต้นทุนสะสมแยกหมวดหน้ารายละเอียดบ่อยังมี `.ceo-cat-dot` ทำงานปกติ ✓ หน้าตะวันปาล์มเข้าได้ปกติไม่มีปุ่ม CEO ค้าง ✓ ฟอร์มเพิ่มผู้ใช้/ตารางจัดการผู้ใช้เหลือแค่ staff/admin ✓ role staff ไม่เห็น KPI ต้นทุนเหมือนเดิม ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error ทุกหน้าที่ทดสอบ — sync ไฟล์แล้ว

**หมายเหตุ**: ข้อมูล role="ceo" เดิมที่อาจยังค้างอยู่ใน Firebase ของบางบัญชี (ถ้ายังไม่ได้เปลี่ยนเป็น admin ด้วยตัวเอง) จะไม่ error แต่จะถูกปฏิบัติเหมือน staff โดยอัตโนมัติ — ถ้าพี่อยากให้ตรวจสอบว่ามีบัญชีไหนยังเป็น ceo ค้างอยู่บ้าง เข้าไปดูได้ที่หน้า "จัดการผู้ใช้"

### 164. "Protocol เวชภัณฑ์ล่าสุด": ขยายตัวหนังสือ/กล่องให้อ่านง่ายขึ้นบนมือถือ

พี่แจ้งว่าหลัง entry #159 ที่ลดขนาดการ์ดลง ตอนนี้เล็กเกินไป อ่านบนโทรศัพท์ลำบาก ขอให้ขยายใหญ่ขึ้น

**แก้**: `textarea` ทั้ง 2 ช่อง (`ov-mprot-feed`/`ov-mprot-water`) เพิ่ม `rows` จาก 3 เป็น 5 และเปลี่ยน `font-size` จาก `var(--fs11)` (12px) เป็น `15px` ตรงๆ (มากกว่าดีฟอลต์ของ `.fi` ที่ 13px ด้วย เพราะพี่ระบุว่าอยากให้ใหญ่ชัดเจน ไม่ใช่แค่กลับไปเท่าเดิม) เพิ่ม `line-height` เป็น 1.6 ให้อ่านสบายตาขึ้น และขยายป้ายหัวข้อย่อย (🍚/💧) จาก `var(--fs11)` เป็น `14px` ให้สมส่วนกับกล่องที่ใหญ่ขึ้น

ทดสอบผ่าน local server จำลองหน้าจอมือถือ (375×812): ยืนยัน `font-size` จริงที่ render ออกมา = 15px, `rows`=5 ✓ สกรีนช็อตยืนยันอ่านง่ายขึ้นชัดเจน กล่องไม่ล้นจอ ✓ ไม่มี console error — sync ไฟล์แล้ว

### 163. หน้า "ประวัติ/สรุป" (รอบเลี้ยงที่ปิดแล้ว): เพิ่มปุ่มดูค่าคุณภาพน้ำย้อนหลัง

พี่ถามว่าตอนปิดบ่อ ข้อมูลคุณภาพน้ำกับสมุดจดอาหารย้อนหลังยังเก็บไว้ให้กลับมาดูเทียบกับบ่อปัจจุบันได้ไหม — ตรวจโค้ดพบว่า `confirmCloseCrop()` archive ข้อมูลทุกอย่างรวมถึง `p.records` (ค่าคุณภาพน้ำรายวัน: pH/DO/แอมโมเนีย/ไนไตรท์/ความเค็ม/อุณหภูมิ) เข้า `p.cropHistory[]` อยู่แล้วจริง แต่หน้า "ประวัติ/สรุป" ที่ใช้ดูย้อนหลัง มีปุ่มให้กดดูแค่ อาหาร/สุ่มกุ้ง/จัดการน้ำ(waterLog)/สารเคมี/แล็บ — ไม่มีปุ่มดู `records` (ค่าคุณภาพน้ำ) เลย ทั้งที่ข้อมูลมีอยู่จริงในระบบหลังบ้าน พี่ยืนยันให้เพิ่ม

**แก้**: เพิ่มปุ่ม "💧 คุณภาพน้ำ" (คนละอันกับปุ่ม "จัดการน้ำ" เดิม ที่เปลี่ยนไอคอนเป็น 💦 เพื่อไม่ให้สับสนกัน) ในส่วน "📂 ข้อมูลดิบของรอบนี้" ของ `renderClosedDetail()` เพิ่ม `<div id="ch-sec-wq">` คู่กับปุ่ม เพิ่ม case `'wq'` ใน `toggleChSec()` และเขียนฟังก์ชันใหม่ `renderChWqTable(c)` อ่านจาก `c.records` แสดงตารางเลื่อนซ้าย-ขวาได้ (วันที่ + อายุ + ค่า 8 พารามิเตอร์แรกจาก `PARAMS` + หมายเหตุ) ตามแพทเทิร์นเดียวกับตารางประวัติค่าน้ำในหน้ารายละเอียดบ่อ (`renderPdWqHistory`) — เป็นตารางอ่านอย่างเดียว ไม่มีปุ่มแก้ไข/ลบ เพราะเป็นข้อมูล archive ของรอบที่ปิดแล้ว

ทดสอบผ่าน local server: inject cropHistory ทดสอบ 1 รายการพร้อมค่าคุณภาพน้ำ (ไม่บันทึกจริง — stub `save()`) กดปุ่ม "💧 คุณภาพน้ำ" แสดงตารางค่าที่ inject ไว้ถูกต้องครบทุกคอลัมน์ ✓ ปุ่ม "💦 จัดการน้ำ" เดิมยังทำงานปกติไม่กระทบ ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

### 162. แก้บั๊ก: บ่อที่ลงกุ้งวันนี้หายจากตาราง "สถานะบ่อทั้งหมด" — เปลี่ยนให้วันลงกุ้ง = อายุ 1 วัน (ไม่ใช่ 0)

พี่แจ้งว่าบ่อ A.26 ที่ลงกุ้งวันนี้ไม่ขึ้นในตาราง "สถานะบ่อทั้งหมด" หน้าภาพรวม — ตรวจแล้วพบว่า `pondStatusOv(p)` คำนวณ `age=dDiff(p.startDate,today())` ซึ่งวันลงกุ้งวันเดียวกับวันนี้จะได้ `age=0` แล้วเงื่อนไข `if(age<=0)return'prep'` ตัดสินว่าบ่อยังอยู่สถานะ "เตรียมบ่อ" ตารางเลยกรองไม่แสดง (ตารางโชว์เฉพาะบ่อที่ `cls!=='prep'`) พี่ยืนยันให้แก้โดยนับวันลงกุ้งเป็น "อายุ 1 วัน" แทน

**แก้จุดเดียว แล้วกระจายผลถูกต้องทั่วแอป**: ปรับสูตรใน `pondCurAge(p)` (บรรทัด ~4077 — ฟังก์ชันกลางที่ตั้งไว้ตั้งแต่ entry #155 สำหรับ "อายุกุ้งปัจจุบันของบ่อ") จาก `dDiff(p.startDate,today())` เป็น `dDiff(p.startDate,today())+1` — ไม่แตะ `dDiff()` เอง (ฟังก์ชันวันที่ทั่วไป ใช้กับเรื่องอื่นที่ไม่ใช่อายุบ่อด้วย เช่น ระยะเวลารอบเลี้ยง/DOC range/อายุบันทึกย้อนหลัง ซึ่งความหมายต่างกัน ไม่ควรบวก 1 ตาม)

**จุดที่เปลี่ยนให้เรียก `pondCurAge(p)` แทนการคำนวณ `dDiff(p.startDate,today())` ซ้ำๆ เอง** (เพื่อให้ "อายุปัจจุบัน" ที่โชว์บนหน้าจอตรงกันทุกที่ ไม่เพี้ยนคนละสูตรแบบที่เคยเป็นปัญหามาก่อน — entry #155):
- `pondStatusOv(p)` — ตัวจำแนกสถานะ prep/ok/warn/danger (บ่อลงกุ้งวันนี้ตอนนี้ได้ age=1 → ไม่เข้าเงื่อนไข prep แล้ว แก้บั๊กหลักตรงนี้)
- `renderOv()` — ตัวเรียงบ่อ+ป้อนข้อมูลเข้าตาราง "สถานะบ่อทั้งหมด" (ทำให้คอลัมน์ "อายุ X วัน" ของตารางแสดงถูกด้วย)
- `renderPondDetail()` — ป้าย "⏱ อายุ X วัน" หน้ารายละเอียดบ่อ
- หน้าทดลองเวชภัณฑ์ (sec-trial) — ป้าย "DOC" ในตัวเลือกบ่อ (ความหมายเดียวกับอายุ ควรนับแบบเดียวกัน)
- หน้ารอบเลี้ยง (sec-crops) — ชิป "เลี้ยงมา X วัน"
- หน้า CEO Dashboard — การ์ดบ่อ "อายุ X วัน" และ sheet รายละเอียดบ่อ
- หน้า ABW Growth — คอลัมน์อายุปัจจุบันในตาราง

**จุดที่แก้แยก เพราะมีเงื่อนไขพิเศษที่ต้องคงไว้**: หน้า "จัดการบ่อ" (`renderManage`) มีบ่อที่ยัง**ไม่ถึง**วันลงกุ้ง (`startDate` เป็นอนาคต) แสดงเป็น "เตรียมบ่อ (เหลือ N วัน)" นับถอยหลัง — ค่านี้ต้องคงสูตรเดิม (`dDiff` ดิบ ไม่บวก 1) ไว้ เพราะเป็นคนละความหมาย (นับถอยหลังไปข้างหน้า ไม่ใช่อายุนับจากอดีต) แก้แค่กรณีที่ลงกุ้งแล้ว (`age>=0`) ให้ `+1` ตอนแสดงผลเท่านั้น

**ไม่แตะ**: จุดคำนวณอายุที่เป็น business-logic threshold ภายใน (ไม่ใช่ข้อความ "อายุ X วัน" ที่โชว์ตรงๆ ให้พี่เห็น) เช่น เกณฑ์ "ทันเป้า/ล่าช้า" ของหน้ารอบเลี้ยง (`renderCrops` — เกณฑ์ age>0/age>=90), การเทียบ ABW กับมาตรฐานตามอายุ (`ceoStatus` — ใช้ `last.age` ที่บันทึกไว้เป็นหลัก), การเปรียบเทียบรอบก่อน (`renderCropBenchmark`), การคำนวณ week bucket ของกราฟ, ระยะเวลารอบเลี้ยงที่ปิดแล้ว (`closeDate-startDate`), อายุที่บันทึกไว้ในแต่ละ record ย้อนหลัง (`r.age`/`rec.age` ตอนกรอกข้อมูล) — เพราะเป็นค่าที่ถูก tune ไว้กับ convention เดิม หรือเป็น snapshot ประวัติที่แก้ย้อนหลังไม่ได้ (ข้อมูลเก่าที่บันทึกไปแล้วจะไม่ขยับตาม)

ทดสอบผ่าน local server (จำลองบ่อลงกุ้งวันนี้จากข้อมูลทดสอบ ไม่บันทึกจริง — stub `save()`): `pondCurAge(p)`=1 และ `pondStatusOv(p)`='ok' ✓ บ่อขึ้นในตาราง "สถานะบ่อทั้งหมด" พร้อม "อายุ 1 วัน" ถูกต้อง ✓ หน้า "จัดการบ่อ" แสดง "1 วัน" ถูกต้องเช่นกัน ✓ ทดสอบเคสบ่อที่ยังไม่ถึงวันลงกุ้ง (startDate อนาคต) ยังคงแสดง "เตรียมบ่อ (เหลือ N วัน)" ถูกต้องไม่กระทบ ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

### 161. หน้ารายละเอียดบ่อ: ลบการ์ด "เวชพันฑ์ผสมกิน" (รายบ่อ) + "อาหารรายวัน 14 วัน" + "การเติบโต–น้ำหนักเฉลี่ย" เพราะซ้ำซ้อน

พี่รีวิวหน้ารายละเอียดบ่อแล้วขอลบ 3 การ์ดที่ข้อมูลซ้ำกับที่อื่นแล้ว: (1) "เวชพันฑ์ผสมกิน" รายบ่อ — ซ้ำกับ Protocol เวชภัณฑ์ล่าสุดที่หน้าภาพรวม (entry #158-160) แล้ว (2) กราฟ "อาหารการกินรายวัน 14 วันล่าสุด" — ซ้ำกับ "สมุดประวัติการให้อาหาร" ที่อยู่ในหน้าเดียวกันด้านบน (3) กราฟ "การเติบโต — น้ำหนักเฉลี่ย (รายสัปดาห์)" — ไม่จำเป็น ข้อมูลซ้ำซ้อนกับกราฟ ABW ที่อื่น

**แก้**: `renderPondDetail(id)` ลบ 3 `pdCard(...)` บล็อก (`medproto`/`chfeedday`/`chgrowth`) และเลิกเรียก `renderPdMedProtocol(p)` — กราฟ `chfeedweek` (⚖️ อาหาร vs การเติบโต รายสัปดาห์) ไม่ได้ถูกขอให้ลบ เก็บไว้เหมือนเดิม

**ลบทิ้งทั้งระบบ** (dead code หลังตัดจุดเรียกใช้ทั้งหมดแล้ว ไม่เหลือใครเรียก): ระบบตารางเวชภัณฑ์แบบหมุนเวียนรายบ่อทั้งชุด — `MED_PROTOCOL_CATS`, `medProtocolAdvance`, `sweepMedProtocols` (ไม่มีจุดเรียกอยู่แล้วด้วย), `medProtocolStatus`, `medProtocolAvailItems`, `medBadge`, `pdProtoStartNew/Edit/AddRoundQuick/CancelEdit/AddRound/RemoveRound/AddMed/SearchMed/RemoveMed/ToggleLoopDraft/Save/EndEarly/StopAll/LoopBackFirst/ToggleLoop`, `pdProtoFormHtml`, `renderPdMedProtocol`, `mprotEsc`, ตัวแปร `_pdProtoEditing`/`_pdProtoDraft`, field `p.medProtocol` (ไม่มีจุดอ่าน/เขียนเหลือในโค้ดแล้ว — เช็คด้วย grep ทั่วไฟล์), และ CSS `.mprot-sched/.mprot-round/.mprot-dot/.mprot-box/.mprot-top/.mprot-lbl/.mprot-meds/.mprot-meta/.mprot-chip-x/.mprot-round-edit` (ใช้เฉพาะ UI ที่ลบไปแล้วเท่านั้น — คนละชุดกับ `#ov-mprot-feed/water/upd/save-btn` ของ Protocol ภาพรวมที่ไม่ใช้ class เหล่านี้)

`renderPdCharts(p)` ตัดส่วนวาดกราฟ `pd-c-feedday` และ `pd-c-growth` ออก เหลือ `pd-c-water`/`pd-c-feedweek` ตามเดิม (ตัวแปร `buckets`/`wkLbls` ยังใช้ร่วมกับ feedweek เหมือนเดิม)

ทดสอบผ่าน local server: เปิดหน้ารายละเอียดบ่อ B1 ยืนยัน 3 การ์ดหายไปจริง การ์ดที่เหลือ (สมุดประวัติการให้อาหาร, เวชภัณฑ์ที่ใช้ล่าสุด, กราฟอาหาร vs การเติบโตรายสัปดาห์ ฯลฯ) ยังอยู่ครบ ✓ กราฟ `feedWeek`/`water` ยัง render ปกติ ยืนยันผ่าน `_pdCharts` object ไม่มี key `feedDay`/`growth` เหลือค้าง ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

### 160. "Protocol เวชภัณฑ์ล่าสุด": จำกัดให้แก้ไข/บันทึกได้เฉพาะ role admin

พี่ขอให้ช่องบันทึก Protocol เวชภัณฑ์ล่าสุด (entry #158-159) แก้ไข/บันทึกได้เฉพาะ role admin เท่านั้น (role อื่น เช่น staff/ceo ดูได้อย่างเดียว)

**แก้**:
- `renderOvMedProtocol()`: เพิ่ม `canEdit=APP_ROLE==='admin'` ตั้ง `readOnly` ให้ทั้ง 2 textarea ตาม role, เปลี่ยนพื้นหลังเป็น `var(--sf2)` (เทาอ่อน เหมือนช่อง readonly อื่นๆ ในแอป) เมื่อไม่ใช่ admin, ซ่อนปุ่ม "💾 บันทึก" (เพิ่ม `id="ov-mprot-save-btn"` ให้ปุ่มเพื่ออ้างอิง) เมื่อไม่ใช่ admin
- `ovProtoSave()`: เพิ่ม guard `if(APP_ROLE!=='admin')return;` ที่บรรทัดแรก (defense-in-depth เหมือนแพทเทิร์น `initPalm()` — กันเผื่อมีการเรียกฟังก์ชันตรงๆ ผ่าน console ข้าม UI ที่ปิดไว้)

ทดสอบผ่าน local server: role admin → แก้ไขได้/ปุ่มโชว์ปกติ ✓ role staff → `readOnly=true`, พื้นหลังเทา, ปุ่มบันทึกซ่อน, เรียก `ovProtoSave()` ตรงๆ ผ่าน console ก็ไม่บันทึก (ยืนยัน `D.medProtocolOverview` ไม่เปลี่ยน) ✓ role ceo → ผลเหมือน staff (อ่านอย่างเดียว) ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

### 159. "Protocol เวชภัณฑ์ล่าสุด": ตัด card-sub ยาวๆ ออก + ย้ายลงใต้ "สถานะบ่อทั้งหมด" + ลดขนาดกรอบให้เล็กลง

พี่ขอ 3 อย่างต่อจาก entry #158: (1) ลบข้อความอธิบายยาวๆ ใต้หัวการ์ดทิ้ง ไม่ต้องขึ้นโชว์ (2) เลื่อนการ์ดลงไปอยู่ใต้ "สถานะบ่อทั้งหมด" แทนที่จะอยู่บนสุด (3) ลดขนาดกรอบให้เล็กลง เพราะในหัวพี่มองว่ามันแค่ข้อมูล update ล่าสุด/การบันทึก ไม่จำเป็นต้องเด่นเท่าตอนนี้

**แก้**: HTML ในหน้าภาพรวม —
- ลบ `<div class="card-sub">...</div>` ที่อยู่ใต้หัวข้อการ์ดออก (การ์ดเหลือแค่หัวข้อ + 2 ช่อง note + ปุ่มบันทึก)
- ย้ายบล็อก `ov-medproto-card` จากตำแหน่งก่อน `ovd-card` (ตารางสถานะบ่อ) มาไว้ต่อจาก `ovd-card` แทน — ลำดับใหม่: KPI bar → สถานะบ่อทั้งหมด → Protocol เวชภัณฑ์ล่าสุด → ปฏิทินจับกุ้ง ...
- ลดขนาด: `textarea` ทั้ง 2 ช่องจาก `rows="7"`/`rows="6"` เหลือ `rows="3"` ทั้งคู่ ลดฟอนต์เป็น `var(--fs11)` (เดิม inherit ปกติ ~13-15px) ลด gap/margin รอบๆ (จุดสี, ระยะห่างหัวข้อ, padding เส้นคั่นด้านล่าง) ให้กรอบโดยรวมเตี้ยลงชัดเจน — ไม่แตะ `renderOvMedProtocol()`/`ovProtoSave()` เพราะ logic เดิมใช้ได้ปกติ แค่ปรับ UI เท่านั้น

ทดสอบผ่าน local server: ยืนยันลำดับการ์ดใหม่ถูกต้อง (สถานะบ่อทั้งหมด ขึ้นก่อน Protocol) ✓ ไม่มี card-sub โชว์แล้ว ✓ บันทึก note ทดสอบ (ไม่บันทึกจริง — stub `save()`) ยังทำงานถูกต้อง อัปเดตเวลาแสดงผลตรง ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

### 158. "Protocol เวชภัณฑ์ล่าสุด" เปลี่ยนจากโน้ตรายบ่อ → โน้ตเดียวของทั้งฟาร์ม ย้ายขึ้นบนสุดของหน้าภาพรวม

พี่ถ่ายรูปกระดานไวท์บอร์ดที่ฟาร์มจดโปรแกรมเวชภัณฑ์ไว้ (โปรแกรมผสมอาหาร + โปรแกรมลงน้ำ) บอกว่าอยากเอาข้อมูลนี้มาเก็บในแอปเป็น "protocol ล่าสุด" อยู่บนสุดของหน้าภาพรวม ไม่ต้องแยกลงรายบ่อ เพราะหน้างานเปลี่ยนโปรแกรมอยู่ตลอด แอปตามไม่ทันวันต่อวัน ให้ดูภาพรวมที่เดียวดีที่สุด — สานต่อจาก entry #157 ที่เพิ่งเปลี่ยนจากตารางรอบอัตโนมัติเป็นโน้ตรายบ่อ 8 กล่อง คราวนี้ยุบรวมเป็นโน้ตเดียว ทำ mockup ให้ดูก่อน (Artifact) พี่ยืนยัน "ok" แล้ว implement

**แก้**:
- HTML: ย้ายการ์ด `ov-medproto-card` จากตำแหน่งเดิม (ท้ายๆ ก่อน `ov-main-grid`) ขึ้นมาเป็นการ์ดแรกสุดของหน้าภาพรวม (ต่อจาก `ov-kpi-bar` ก่อน `ovd-card` ตารางสถานะบ่อ) เปลี่ยนเนื้อหาในการ์ดจากลิสต์รายบ่อเป็น 2 ช่อง `textarea` คงที่: 🍚 โปรแกรมผสมอาหาร (`ov-mprot-feed`) และ 💧 โปรแกรมลงน้ำ (`ov-mprot-water`) พร้อมบรรทัดอัปเดตล่าสุด + ปุ่มบันทึกเดียวคุมทั้ง 2 ช่อง ลบ `display:none` เริ่มต้นออก (โชว์เสมอ เหมือน `ovd-card`)
- JS: เพิ่ม field ใหม่ระดับฟาร์ม `D.medProtocolOverview={feedNote,waterNote,updatedAt,updatedBy}` (lazy-init ตอนบันทึก ตามแพทเทิร์นเดียวกับ `D.clerkCash`/`D.palmPlots`) ลบ `ovProtoSaveNote(pid)` เดิม (ผูกรายบ่อ) แทนที่ด้วย `ovProtoSave()` ไม่รับ pid บันทึกทั้ง 2 ช่องพร้อมกัน แก้ `renderOvMedProtocol()` ให้ sync ค่าจาก `D.medProtocolOverview` เข้า textarea ทั้งสอง (เช็ค `document.activeElement` กันเขียนทับตอนพี่กำลังพิมพ์อยู่) — ไม่แตะระบบตารางรอบอัตโนมัติในหน้ารายละเอียดบ่อ (`p.medProtocol.rounds`/`renderPdMedProtocol`/`pdProto*`) เหมือน entry #157

ทดสอบผ่าน local server: การ์ดใหม่ขึ้นเป็นอันดับแรกของหน้าภาพรวมจริง ✓ พิมพ์ + กดบันทึก (ไม่บันทึกจริง — stub `save()`) ยืนยัน `D.medProtocolOverview` เก็บค่าถูก ✓ ล้างค่าแล้ว re-render ดึงค่าที่บันทึกไว้กลับมาแสดงถูกต้อง (พิสูจน์ persist ผ่าน state จริงไม่ใช่แค่ DOM) ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

### 157. หน้าภาพรวม การ์ด "เวชภัณฑ์ปัจจุบัน — ทุกบ่อ": ตัดตารางวันต่อวันออก เหลือแค่ช่อง Note + วันอัปเดตล่าสุด

พี่ขอให้การ์ด "Protocol เวชภัณฑ์ปัจจุบัน — ทุกบ่อ" หน้าภาพรวม เปลี่ยนจากที่ดึงตารางรอบอัตโนมัติมาโชว์ (รอบที่ N · วันที่ X/Y) เป็นแค่ช่องบันทึกอิสระว่าปัจจุบันฟาร์มใช้เวชภัณฑ์อะไรบ้าง เพราะหน้างานจริงมีการปรับเปลี่ยนตลอด การแจ้งวันต่อวันตามตารางที่ตั้งไว้ล่วงหน้าไม่ตรงกับการใช้งานจริง — อยากได้แค่ Note พร้อมบันทึกวันที่อัปเดตล่าสุด

**แก้**: `renderOvMedProtocol()` (หน้าภาพรวม) ตัดส่วน `headBadge`/`medsHtml` ที่ดึงจาก `medProtocolStatus(p)` (สถานะรอบ/นับวันในรอบ) ออกทั้งหมด เหลือแค่ `textarea` บันทึกหมายเหตุ + บรรทัดอัปเดตล่าสุด (`p.medProtocol.note`/`noteUpdatedAt`/`noteUpdatedBy` — ฟิลด์เดิมที่มีอยู่แล้ว ไม่ต้องเปลี่ยน schema) และปุ่มบันทึก (`ovProtoSaveNote` เดิม ไม่แตะ) เปลี่ยนเงื่อนไขแสดงบ่อจาก "เฉพาะบ่อที่เคยตั้งตารางหรือมี note" เป็น **ทุกบ่อที่กำลังเลี้ยงอยู่** (`sortPondsByAge(wqfeedActivePonds())`) ให้ตรงกับชื่อการ์ด "ทุกบ่อ" จริงๆ — ใครก็เพิ่ม note ได้ทันทีไม่ต้องมีตารางเก่ามาก่อน ปรับข้อความ card-sub ให้สื่อความหมายใหม่ด้วย

**ไม่แตะ**: ระบบตารางรอบอัตโนมัติ (`p.medProtocol.rounds`/`active`/`loop`/`curIdx`/`medProtocolAdvance`/`sweepMedProtocols`) และหน้าแก้ไขตารางในรายละเอียดบ่อ (`renderPdMedProtocol`/`pdProto*`) ยังอยู่ครบเหมือนเดิม เพราะพี่ระบุเจาะจงแค่การ์ดหน้าภาพรวม ยังไม่ได้ขอให้ลบฟีเจอร์ตารางทั้งระบบ

ทดสอบผ่าน local server: ยืนยันการ์ดแสดงครบทุกบ่อ active (8 บ่อ) แบบ note-only ไม่มีข้อมูลรอบ/วันปนอยู่ ✓ บันทึก note ทดสอบที่บ่อ A.14 (ไม่บันทึกจริง — stub `save()`) แสดงเวลาอัปเดตถูกต้อง เฉพาะบ่อนั้น บ่ออื่นไม่กระทบ ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

### 156. การ์ดภาพรวมบ่อ (kpiHero): เพิ่ม "อาหารวันนี้" และ "กก./แสนตัว" ขึ้นมาโชว์คู่กับ ไซส์/น้ำหนักรวม/อาหารรวม/FCR ประเมิน

พี่ขอให้การ์ด KPI ในหน้าภาพรวมของแต่ละบ่อ (popup ที่กดเข้าไปดูรายบ่อ) ย้ายข้อมูล "อาหารล่าสุด" ขึ้นมาโชว์อยู่ในกรอบเดียวกับไซส์/น้ำหนักรวม/อาหารรวม/FCR ประเมิน ก่อน แล้วขอเพิ่มอีก 2 ค่าคือ "อาหารวันนี้" กับ "กก./แสนตัว" เข้าไปด้วย เพราะเป็นข้อมูลที่ต้องดูทันที ไม่อยากต้องกดเข้าไปหาในส่วนอื่นของ popup

**แก้**: `renderPondDetail(id)` ส่วน `kpiHero` (การ์ด grid 2 คอลัมน์) — การ์ดที่เพิ่มรอบแรกเปลี่ยนชื่อจาก "อาหารล่าสุด" เป็น "อาหารวันนี้" ให้ตรงกับชื่อที่ใช้เรียกจุดเดียวกันในส่วน `feedHtml` ของหน้าเดียวกันอยู่แล้ว (ค่า/ที่มาเหมือนเดิมทุกอย่าง — ดึงจาก `latFeed`, กำกับวันที่ "(วันนี้)" หรือวันที่จริงผ่าน `fD()`) และเพิ่มการ์ดใหม่ "กก./แสนตัว" ต่อจากกัน ดึงจากตัวแปร `per100k` (`feedPer100k(latFeed.totalFeed,pRelease(p))`) ที่มีอยู่แล้วในฟังก์ชัน ตอนนี้ grid มี 6 การ์ดพอดี (ไซส์/น้ำหนักรวม/อาหารวันนี้/กก.แสนตัว/อาหารรวม/FCRประเมิน) เลยเอา `grid-column:1/-1` ของการ์ด FCR ออก ให้เรียงเป็น grid 2 คอลัมน์ 3 แถวเต็มพอดีไม่มีช่องว่าง

ทดสอบผ่าน local server: inject ข้อมูลอาหารจำลองชั่วคราว (ไม่บันทึกจริง — stub `save()`) ยืนยัน "อาหารวันนี้" = 45.5 กก. (วันนี้), "กก./แสนตัว" = 9.2 (คำนวณถูกจาก 45.5÷4.92) ✓ สถานะไม่มีข้อมูล (`—`) ก็ถูกต้อง ✓ สกรีนช็อตยืนยัน layout 2×3 เรียบร้อยไม่มีช่องว่าง ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

### 155. ขยายการเรียงบ่อตามอายุกุ้ง (entry #154) ไปทุกหน้าที่มีข้อมูลบ่อเรียงลงมา

พี่ขอให้ทุกหน้าที่มี "ข้อมูลบ่อเรียงลงมาด้านล่าง" เรียงตามอายุกุ้งมาก→น้อยเหมือนตารางภาพรวม (entry #154) ไม่ใช่แค่หน้าภาพรวม — ยกตัวอย่างหน้าบันทึกอาหารทุกบ่อ กับบันทึกคุณภาพน้ำ

**เพิ่มฟังก์ชันกลาง** ใกล้ `wqfeedActivePonds()`: `pondCurAge(p)` (อายุกุ้งปัจจุบันเป็นวัน จาก `dDiff(p.startDate,today())`) และ `sortPondsByAge(list)` (เรียงมาก→น้อยด้วยฟังก์ชันนี้) ใช้จุดเดียวกันทุกที่กันสูตรคำนวณอายุเพี้ยนไปคนละแบบ

**หน้าที่แก้ (ใช้ `sortPondsByAge()` แทนการเรียงชื่อ/สถานะ/ไม่เรียงเลย):**
- 💧 คุณภาพน้ำ/รายวัน — แถบชิปเลือกบ่อ (`renderEpChips`) และตัวหา "บ่อถัดไปที่ยังไม่กรอก" (`epNextUnfilled`) เดิมเรียงชื่อ ก.-ฮ.
- 🍖 อาหาร & FCR — ตารางกรอกอาหารทุกบ่อ (`renderFeedGrid`) เดิมเรียงชื่อ
- 🌡️ น้ำ-อาหาร (วิเคราะห์) — dropdown+รายการแจ้งเตือนความผิดปกติ (`initWqFeed`/`renderWqFeedAlerts`) เดิมไม่เรียง
- ⚠️ ความเสี่ยง — การ์ดประเมินความเสี่ยงรายบ่อ (`renderRisk`) เดิมไม่เรียง (หมายเหตุ: วิดเจ็ต "บ่อเสี่ยงสูงสุด" ที่หน้าภาพรวมยังคงเรียงตามคะแนนเสี่ยงเหมือนเดิม เพราะจุดประสงค์ต่างกัน — อันนั้นคือ "จัดอันดับความเร่งด่วน" ไม่ใช่ตารางข้อมูลทั่วไป)
- 📈 ABW Growth — ชิปเลือกบ่อ/กราฟ/ตาราง (`initAbw`→`renderAbwKPI/Chart/Table`) เดิมไม่เรียง (สีในกราฟ/ชิป/ตารางยังตรงกันเพราะเรียงจุดเดียวก่อนแจกให้ทั้ง 3 ฟังก์ชัน)
- 🔄 เปรียบเทียบ — ตาราง heatmap คุณภาพน้ำ (`renderWQHeatmap`) เดิมเรียงตามความรุนแรงสถานะ (วิกฤต→เฝ้าระวัง→ปกติ) เปลี่ยนเป็นเรียงอายุ (สีป้ายสถานะ/ตัวกรองยังทำงานเหมือนเดิม แค่ไม่คุมลำดับแถวแล้ว)
- 👷 ค่าแรง — แถบชิปยืนยันบ่อ active วันนี้ (`renderLaborActiveSection`) เดิมไม่เรียง
- ⚡ ค่าไฟฟ้า — แถบชิปยืนยันบ่อ active วันนี้ (`renderElecActiveSection`) เดิมไม่เรียง
- 🏠 ภาพรวม — วิดเจ็ตตารางเวชภัณฑ์ (`renderOvMedProtocol`) เดิมไม่เรียง
- 🌴 CEO Dashboard — การ์ดบ่อทั้งหมด (`renderCeo`) เดิมไม่เรียง

**ไม่แก้โดยตั้งใจ**: หน้า/วิดเจ็ตที่การเรียงมีจุดประสงค์เฉพาะอื่นที่ไม่ใช่แค่ "แสดงรายการบ่อ" เช่น การ์ด "ปฏิทินจับกุ้ง & พาเชียล" ที่เรียงตาม yield/ระยะเวลาที่เหลือ (ช่วยตัดสินใจว่าจะจับบ่อไหนก่อน), วิดเจ็ต "บ่อเสี่ยงสูงสุด" ที่เรียงตามคะแนนเสี่ยง, และ dropdown/checkbox เลือกบ่อทั่วไป (`sortedPonds()`) ที่ยังเรียงตามชื่อไว้เพื่อหาบ่อง่ายเวลาเลือกจากลิสต์ยาวๆ — เปลี่ยนพวกนี้จะทำให้ใช้งานยากขึ้นไม่ใช่ง่ายขึ้น

ทดสอบผ่าน local server: ตรวจทุกหน้าที่แก้ด้วย `getComputedStyle`/อ่าน DOM จริง ยืนยันลำดับ A.14(135วัน)→A.15(127)→A.22(127)→A.21(126)→A.23(126)→A.1(82)→A.2(82)→A.12(82) ตรงกันทุกหน้า (อาหาร, คุณภาพน้ำ, ความเสี่ยง, เปรียบเทียบ, CEO) ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

### 154. ตาราง "สถานะบ่อทั้งหมด" หน้าภาพรวม: เรียงตามอายุกุ้งมาก→น้อย แทนเรียงตามสถานะ

พี่ขอให้ตารางสถานะบ่อทั้งหมด (หน้าภาพรวม) เรียงจากอายุกุ้งมากไปน้อย ไม่ต้องเรียงตามสถานะบ่อ (เดิมเรียง 🔴ต้องดู → ⚠️เฝ้าระวัง → ✅ปกติ ก่อน แล้วค่อยเรียงอายุในกลุ่มเดียวกัน)

**แก้**: `_ovdSort()` เดิมมี `rank={danger:0,warn:1,ok:2}` คุมลำดับหลัก แล้วค่อย fallback `b.age-a.age` ตัดส่วน rank ออก เหลือแค่เรียงตามอายุมาก→น้อยอย่างเดียว — ไม่กระทบส่วนอื่น (การยุบกลุ่ม "ปกติครบถ้วน", ตัวกรอง "ต้องดูก่อน"/"ยังไม่กรอกวันนี้", สีป้ายสถานะ) เพราะ field `cls` (danger/warn/ok) ยังคงอยู่ครบ ใช้แค่ไม่เอามาคุมลำดับการเรียงแล้ว

ทดสอบผ่าน local server: อ่านลำดับจริงจากตาราง — บ่ออายุ 135→127→127→126→126→82→82→82 วัน เรียงถูกต้องจากมากไปน้อยไม่ขึ้นกับสถานะ ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

### 153. ปรับข้อความ empty state ทั่วแอปให้บอกทางออกชัดเจน (UX copy review)

พี่ให้รีวิว UX copy ของ empty state ทั่วแอป พบว่ามีข้อความ "ยังไม่มีข้อมูล" กว่า 60 จุด ส่วนใหญ่บอกแค่สถานะเฉยๆ ไม่บอกว่าต้องทำอะไรต่อ ต่างจากบางจุด (เช่น "ยังไม่มีแปลงปาล์ม กด 'เพิ่มแปลงใหม่' เพื่อเริ่มต้น") ที่มีอยู่แล้วและบอกทางออกชัดเจน — ใช้จุดหลังเป็นต้นแบบ พี่ยืนยันให้แก้เลย

**หลักการที่ใช้**: รูปแบบ "สถานะ + ทางออก" คั่นด้วย `—` (ตามแพทเทิร์นเดิมที่มีอยู่แล้วในโค้ดบางจุด) อ้างอิงชื่อปุ่ม/แท็บ/ฟอร์มด้วย `"เครื่องหมายคำพูด"` ตรงกับชื่อจริงในหน้าจอเป๊ะ (เช็คโค้ด HTML จริงทุกจุดก่อนเขียน ไม่เดา) **ไม่ใช่ทุกจุดที่แก้** — จุดที่เป็น derived/analysis (เช่น P&L, ความเสี่ยง, สรุปค่าแรง/ค่าไฟที่มีคำอธิบายเงื่อนไขอยู่แล้ว) หรือเป็นมุมมองอ่านอย่างเดียวของ CEO ปล่อยไว้ตามเดิมถ้าใส่ CTA แล้วจะผิดหรือทำให้เข้าใจผิดว่ากดอะไรได้จากตรงนั้น

**แก้ทั้งหมด 39 จุด** ครอบคลุม: หน้ารายละเอียดบ่อ (คุณภาพน้ำ/อาหาร/สุ่มตัวอย่าง/จัดการน้ำ, กราฟไม่พอข้อมูล), หน้าค่าแรง (พนักงานประจำ/รายวัน/เบิกเงินล่วงหน้า), หน้าค่าไฟฟ้า (ประวัติบิล), หน้าสต็อก (รายการพัสดุ — แยกข้อความระหว่าง "ว่างจริง" กับ "ตัวกรองไม่เจอ", รับเข้าส่วนกลาง), หน้าเสมียน (รายรับ-รายจ่าย), หน้าตะวันปาล์ม (รายรับ-รายจ่าย/ประวัติใส่ปุ๋ย/กำหนดใส่ปุ๋ยรอบถัดไป), หน้ากองทุน (ประวัติซื้อ/ขาย/NAV), หน้าสุ่มตัวอย่าง/จัดการน้ำ/กุ้งตาย/ผลแล็บ (รายบ่อ), หน้าทดลองเวชภัณฑ์ (บันทึก journal), หน้าอาหาร (กริดไม่มีบ่อ), หน้าความเสี่ยง (ไม่มีบ่อให้ประเมิน), หน้า P&L (KPI/ตาราง/กราฟต้นทุน), หน้ารอบเลี้ยง (ประวัติ), หน้าจับกุ้ง (รายการพาเชียล), CEO dashboard (ไม่มีบ่อ)

ทดสอบผ่าน local server: `node --check` ผ่านทั้ง 2 script block ✓ เปิดทดสอบจริงหลายหน้า (P&L, ค่าแรง, ตะวันปาล์ม/กองทุน, สต็อก, ความเสี่ยง) ยืนยันข้อความใหม่ขึ้นถูกต้องตรงเงื่อนไข ไม่มี console error ✓ — sync ไฟล์แล้ว

### 152. แก้ปัญหา Accessibility 3 จุดจากการตรวจสอบ WCAG 2.1 AA (label ผูก input / chip กดคีย์บอร์ดไม่ได้ / สีจางคอนทราสต์ต่ำ)

พี่ขอให้ตรวจสอบแอปตามมาตรฐาน accessibility (WCAG 2.1 AA) แล้วขอให้แก้ 3 จุดที่กระทบผู้ใช้เยอะที่สุดแต่ความเสี่ยงต่ำ: (1) label ฟอร์มไม่ผูกกับ input (2) ปุ่มกรองแบบ chip กดด้วยคีย์บอร์ดไม่ได้ (3) สีตัวหนังสือจางคอนทราสต์ไม่พอ

**1) ผูก `<label for="...">` เข้ากับ input ทั้งแอป** — ตรวจพบว่า `<label>` ทั้ง 231 จุดในทุกฟอร์มของแอปไม่มี `for=` เชื่อมกับ id ของ input เลยสักจุด (screen reader จะอ่าน placeholder แทนชื่อฟิลด์จริง) เขียนสคริปต์ Node จับคู่ label→input/select/textarea ที่อยู่ติดกันแล้วเติม `for="id"` ให้อัตโนมัติ — **เจอบั๊กจากสคริปต์เอง 1 จุดระหว่างทาง**: regex non-greedy ข้าม `</label>` ของ label นึงไปจับคู่กับ input ของอีกฟิลด์ที่อยู่ไกลออกไป (label "สถานะการทดลอง" ถูกผูกผิดไปกับ input "ชื่อเวชภัณฑ์" ที่อยู่คนละฟิลด์) — ตรวจพบด้วยสคริปต์ verify แยกต่างหากที่เช็คความติดกันจริงๆ (ไม่ใช่แค่ค้นหาไปข้างหน้า) แก้กลับให้ถูกแล้ว ผลสุดท้าย 223/231 label ผูกด้วย `for=` ตรงถูกต้อง (ยืนยันด้วยสคริปต์ verify ว่า id ตรงกับ input ที่อยู่ติดกันจริง 100%) ที่เหลือ 8 จุด: 6 จุดเป็น label ที่ wrap checkbox ไว้ข้างในอยู่แล้ว (implicit association ตาม HTML spec ใช้ได้โดยไม่ต้องมี for) และ 2 จุดเป็น label หัวข้อกลุ่ม (ไม่ได้ผูกกับ input เดียว เช่น "เลือกกราฟที่ต้องการแสดงใน PDF" ที่คลุมหลาย checkbox) ปล่อยไว้ตามเดิม — ไม่ใช่บั๊ก แค่เป็นรูปแบบ group label ที่ต้องใช้ `<fieldset>/<legend>` ถ้าจะแก้ให้สมบูรณ์ (ขอบเขตนอกเหนือจาก 3 ข้อที่พี่ขอรอบนี้) พบ 3 จุดที่ label มีอยู่แล้วแต่ input ไม่มี id (เติม id ใหม่ให้แล้วผูกคู่กัน: `bc-survival-${p.id}`, `bc-feedrate-${p.id}` ในการ์ดเปรียบเทียบ biomass, `mprot-days-${idx}` ในตารางแผนยา)

**2) ปุ่มกรองแบบ chip (`<span onclick>`/`<div onclick>`) กดด้วยคีย์บอร์ดไม่ได้** — ก่อนหน้านี้ chip กรอง (เช่น `.trial-chip` ในหน้าทดลองเวชภัณฑ์, ตัวเลือกประเภทเงินสด ฯลฯ) ไม่มี `tabindex`/`role`/ตัวรับ Enter-Space เลย ผู้ใช้คีย์บอร์ดอย่างเดียวกดไม่ได้ — **ไม่แปลง tag เป็น `<button>` โดยตรง** (เสี่ยงกระทบ CSS selector ที่ผูกกับ span/div เดิมในหลายจุด) แต่เขียนฟังก์ชันกลาง `initA11yKeyboardChips()` แทน: สแกนหา `span[onclick],div[onclick],tr[onclick]` ทั้งหมด เติม `tabindex="0"` + `role="button"` + `keydown` handler รองรับ Enter/Space ให้อัตโนมัติ ทำงานทั้งกับของที่มีตั้งแต่โหลดหน้าแรก และของที่ render เพิ่มทีหลังแบบไดนามิก (ใช้ `MutationObserver` เฝ้าดู DOM เพราะหลายหน้า render chip ผ่าน `innerHTML` ตอน user โต้ตอบ ไม่ได้มีตั้งแต่แรก) — แก้จุดเดียว ครอบคลุมทุกหน้าที่มี pattern นี้โดยไม่ต้องไล่แก้ทีละ render function

**3) `--tx3` (สีตัวหนังสือจาง) คอนทราสต์ต่ำเกินไป** — ค่าเดิม `#9AA1AB` ให้คอนทราสต์แค่ 2.6:1 บนพื้นขาว (ต้องการ 4.5:1 ตาม WCAG AA) ใช้กับข้อความจริงเยอะมาก (ป้าย KPI, หัวตาราง, แท็บที่ยังไม่เลือก, hint ต่างๆ) เปลี่ยนเป็น `#5D6874` — คำนวณคอนทราสต์แล้วผ่าน 4.5:1 ทั้งบนพื้นขาว (`--sf`, 5.68:1) และพื้นเทาอ่อน (`--sf2`, 5.10:1) ที่ใช้จริงในแอป **หมายเหตุ**: ค่าใหม่ใกล้เคียงกับ `--tx2` (สีข้อความรอง) มากเพราะข้อจำกัดทางคณิตศาสตร์ — พื้นหลังที่ใช้จริงเกือบขาวสนิท ทำให้ "ตัวหนังสือจางกว่าแต่ยังผ่าน AA" แทบไม่มีช่องว่างเหลือให้ทำสีจางกว่านี้ได้จริง จึงเลือกค่าที่ผ่านมาตรฐานแน่นอนแทนที่จะประนีประนอมกับคอนทราสต์

ทดสอบผ่าน local server: **label** — เขียนสคริปต์ verify ยืนยัน 219/220 คู่ที่สคริปต์แรกผูกถูกต้อง 100%, เจอ 1 คู่ผิด แก้แล้วเหลือ 0 ผิด, ทดสอบจริงในเบราว์เซอร์ `input.labels` คืนค่าชื่อ label ถูกต้อง (เช่นช่องอีเมลได้ "อีเมล" ไม่ใช่ placeholder อีกต่อไป) ✓ **chip keyboard** — จำลองกด Enter จริงด้วย `KeyboardEvent` บน chip ที่มี `tabindex=0`/`role=button` ยืนยันว่า focus ได้และ Enter สั่งงาน onclick จริง (class เปลี่ยนถูกต้อง) ✓ ทดสอบ chip ที่ render แบบไดนามิกทีหลัง (เช่น chip กรองแปลงในแท็บประวัติตัดปาล์ม) ก็ได้ tabindex อัตโนมัติเหมือนกัน (ยืนยัน MutationObserver ทำงาน) ✓ **สี** — `getComputedStyle` ยืนยันค่าตัวแปรเปลี่ยนเป็น `#5D6874` ถูกต้อง ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error ตลอดการทดสอบ — sync ไฟล์แล้ว

### 151. เพิ่มเครื่องมือ "ลบประวัติรอบเลี้ยงเก่ากว่าปี 2568 ทิ้งถาวร" ในหน้าจัดการบ่อ (Admin)

พี่ขอลบข้อมูลรอบเลี้ยงย้อนหลังตั้งแต่ปี 2567 ลงไปทั้งหมด เหลือแค่ปี 2568–2569 เพราะเป็นข้อมูลอัปเดตล่าสุดที่ใช้งานจริง รวมถึง P&L รายบ่อ/รายรอบของปี 2567 ย้อนหลังด้วย แต่ **ไม่ให้แตะข้อมูลค่าไฟฟ้า**

**เหตุผลที่ไม่ลบให้ตรงๆ ทันที**: ตรวจสอบพบว่า P&L รายบ่อ/รายรอบ (`renderPL*`) ไม่มี store แยกต่างหาก — อ่านข้อมูลจาก `p.cropHistory[]` โดยตรงทุกจุด ดังนั้นลบ `cropHistory` เก่าจุดเดียวก็ครบทั้งประวัติรอบเลี้ยงและ P&L เก่าไปพร้อมกันโดยอัตโนมัติ ไม่ต้องหา store แยกมาลบซ้ำ — ส่วนค่าไฟฟ้าเก็บคนละ array กันคนละหน้าที่อยู่แล้ว ไม่ไปยุ่งเลยตามที่พี่สั่ง

**สำคัญ**: ระบบนี้ (Claude Code ที่รันทดสอบ) ต่อเข้าฐานข้อมูล Firebase จริงของฟาร์มไม่ได้โดยตรง (ทดสอบด้วย `firebase.database().ref('tawan_app').once('value')` ตรงๆ ได้ `permission_denied` — ต้อง login จริงถึงจะอ่าน/เขียนได้) จึง**ไม่สามารถลบข้อมูลให้อัตโนมัติจากฝั่งนี้ได้** สร้างเป็นปุ่มเครื่องมือในแอปแทน ให้พี่ (ที่ login จริง) กดสั่งลบเองบนเว็บจริง เหมือนเครื่องมือ maintenance อื่นๆ ในหน้านี้ (seed ประวัติ, แปลงชื่อ B→A ฯลฯ)

**สิ่งที่ทำ**: เพิ่มบล็อกใหม่ในหน้า "⚙️ จัดการบ่อ" (Admin เท่านั้น) มี 2 ปุ่ม:
1. **👁️ ดูตัวอย่างก่อนลบ** — เรียก `previewCropHistoryCleanup()` นับจำนวนรอบที่จะถูกลบ/เหลือ (ใช้ `histRecYear(c)` ตัวเดิมที่ใช้กับ dropdown PDF ปิดบ่อ entry #148 เพื่อให้นิยาม "ปี" ตรงกันทั้งแอป) แสดงผลผ่าน `confirm()`+`console.log()` ไม่ลบจริง ให้เช็คตัวเลขก่อนได้
2. **🗑️ ลบรอบเลี้ยงเก่ากว่าปี 2568 ทันที** — `cleanupOldCropHistory()`: แสดง `confirm()` บอกจำนวนที่จะลบ+จำนวนที่จะเหลือชัดเจนก่อนกดยืนยันจริง เมื่อยืนยันแล้ว **ดาวน์โหลดไฟล์สำรอง JSON ของข้อมูลทั้งหมดให้อัตโนมัติก่อนลบทุกครั้ง** (เรียก `exportJSON()` ที่มีอยู่แล้วในโค้ดแต่ไม่เคยถูกผูกปุ่มใช้งานมาก่อน) จากนั้นลูปทุกบ่อ กรอง `p.cropHistory` เอาเฉพาะปี ≥2568 ไว้ ที่เหลือ tombstone เข้า `D.deletedIds` (รูปแบบเดียวกับ `deleteCropRecord()`/`fixYear2569Round1()` ที่มีอยู่แล้ว กันข้อมูลกลับมาจาก merge) แล้ว `saveForce()` (เขียนทับ ไม่ merge กับ remote เหมือนจุดลบข้อมูลอื่นๆ ในหน้านี้)

ทดสอบผ่าน local server (stub `save()`/`saveForce()`/`exportJSON()` เหมือนทุกครั้งที่ทดสอบฟีเจอร์ลบข้อมูล กันไม่ให้กระทบ Firebase จริง): จำลอง 8 บ่อ × 5 ปี (2564,2566,2567,2568,2569) = 40 รอบ → preview นับได้ถูกต้อง (ลบ 24 เหลือ 16) ✓ กดลบจริงแล้วเหลือทุกบ่อแค่ปี 2568+2569 (2 รอบ/บ่อ) ตรงเป๊ะ ✓ `D.deletedIds` มี tombstone ครบ 24 รายการ ✓ `exportJSON()` (backup) ถูกเรียกก่อนลบเสมอ ✓ กดซ้ำรอบสองไม่มีอะไรให้ลบ ขึ้นข้อความแจ้งถูกต้อง (idempotent) ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

**ขั้นตอนที่พี่ต้องทำต่อ**: เข้าเว็บจริงด้วยบัญชี Admin → เมนู "⚙️ จัดการบ่อ" → เลื่อนลงไปที่ "🔴 ลบประวัติรอบเลี้ยงเก่ากว่าปี 2568 ทิ้งถาวร" → กด "👁️ ดูตัวอย่างก่อนลบ" เช็คจำนวนก่อน (เปิด console/F12 ดูรายละเอียดได้) → พอใจแล้วค่อยกด "🗑️ ลบรอบเลี้ยงเก่ากว่าปี 2568 ทันที" (จะดาวน์โหลดไฟล์สำรองให้อัตโนมัติก่อนลบเสมอ — เก็บไฟล์นี้ไว้เผื่อจำเป็นต้องกู้คืน)

### 150. 🔴 แก้บั๊ก "ประวัติตัดปาล์ม": ราคา/กก. ต้องดึงจากที่กรอกจริง ไม่ใช่คำนวณจาก amount/weight

พี่ทักหลังใช้ entry #149 ว่าคอลัมน์ "ราคา/กก." ในตาราง "ประวัติตัดปาล์มรายครั้ง" **ไม่ต้องคำนวณ** ให้ดึงจากข้อมูลที่กรอกไว้ตรงๆ แทน เพราะมีการจ้างทีมตัดหลายเจ้า แต่ละเจ้าคิดค่าจ้างไม่เท่ากัน — จำนวนเงิน (`amount`) ที่กรอกในแต่ละรอบคือ **รายรับสุทธิหลังหักค่าจ้างทีมตัดแล้ว** ไม่ใช่ยอดขายดิบ ส่วนราคาที่ต้องการดูคือ **ราคาขาย ณ วันนั้นจริงๆ**

**บั๊ก**: `renderPalmHarvestChart()`/`renderPalmHarvestTable()` เดิมคำนวณราคา/กก. จาก `amount ÷ weightKg` ทั้งที่ `amount` มีค่าค่าจ้างทีมตัด (แปรผันตามเจ้าที่จ้าง) หักปนอยู่แล้ว ทำให้ราคาที่โชว์ผิดเพี้ยนจากราคาขายจริง — เช่น ขาย 6.00 บาท/กก. เท่ากัน 2 รอบ แต่จ้างทีมตัดคนละเจ้า (ค่าจ้างต่างกัน) จะโชว์ราคาไม่เท่ากันทั้งที่ราคาขายจริงเท่ากัน

**แก้**: ฟอร์มบันทึกรายรับเดิม (`savePalmMoney()`) มีช่อง "ราคา/กก." (`#pm-price`) อยู่แล้วและบันทึกเก็บเป็น `rec.pricePerKg` ทุกครั้งที่กรอก (ไม่ใช่ field ใหม่) — เปลี่ยนทั้งกราฟและตารางมาอ่าน `r.pricePerKg` ตรงๆ แทนการหาร:
- **กราฟรายเดือน**: ถัวเฉลี่ยแบบถ่วงน้ำหนักด้วย `pricePerKg × weightKg` แล้วหารด้วยน้ำหนักรวม (แทน `amount ÷ weightKg` เดิม) — record ที่ไม่มี `pricePerKg` (กรอกไว้ไม่ครบ) จะไม่ถูกนับในค่าเฉลี่ย กันดึงค่าเฉลี่ยเพี้ยน
- **ตาราง**: คอลัมน์ "ราคา/กก." โชว์ `r.pricePerKg` ตรงๆ ถ้าไม่มีค่า (ไม่ได้กรอกไว้ตอนบันทึก) โชว้ "-" แทนที่จะคำนวณผิดๆ
- เปลี่ยนหัวคอลัมน์ท้ายตารางจาก "ยอดขาย" เป็น "รายรับสุทธิ" ให้ตรงความหมายจริงตามที่พี่อธิบาย (กันสับสนว่าเป็นยอดขายดิบ)
- ปรับข้อความอธิบายใต้หัวการ์ดกราฟให้บอกชัดว่าใช้ราคาที่กรอกไว้ตรงๆ ไม่ใช่คำนวณจากยอดเงิน

ทดสอบผ่าน local server (stub `save()` เหมือนทุกครั้ง): จำลอง 2 รอบราคาขาย 6.00 บาท/กก. เท่ากัน แต่ค่าจ้างทีมตัดต่างกัน (รายรับสุทธิ 10,500 vs 11,800 จากน้ำหนักเท่ากัน 2,000 กก.) → ราคาเฉลี่ยเดือน มี.ค. โชว์ 6.00 ถูกต้อง (ไม่ใช่ค่าเฉลี่ยของ 5.25/5.90 แบบเดิม) ✓ ตารางโชว์ราคา/กก. ตรงกับที่กรอกไว้ทุกแถว ✓ ทดสอบ record ที่ไม่มี `pricePerKg` (ไม่ได้กรอกไว้) → ตารางโชว์ "-" ถูกต้อง และไม่กระทบค่าเฉลี่ยเดือนอื่น ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error — sync ไฟล์แล้ว

### 149. หน้าตะวันปาล์ม: แยกตารางรายรับ/รายจ่าย + คอลัมน์ราคา/หน่วยปุ๋ย + แท็บใหม่ "ประวัติตัดปาล์ม" (ทำ mockup ก่อน)

พี่ขอ 3 อย่างในหน้าตะวันปาล์มพร้อมกัน: (1) ตาราง "ประวัติรายรับ-รายจ่าย" อยากให้แยกรายรับ/รายจ่ายกันดูเพื่อง่ายต่อการดูข้อมูล (2) ตาราง "ประวัติการใส่ปุ๋ย" อยากเพิ่มราคา/กระสอบของแต่ละรอบ เพื่อดูว่าราคาปุ๋ยขึ้นลงยังไง (3) อยากเพิ่มประวัติการตัดปาล์ม เพื่อดูว่าเดือนไหนราคาดีที่สุด — ทำ mockup ครอบคลุมทั้ง 3 ข้อ พี่อนุมัติด้วยคำว่า "ทำได้"

**ส่วนที่ 1 — แยกตารางรายรับ/รายจ่าย**: เดิม `renderPalmMoney()` โชว์ทุก record ปนกันตารางเดียว มีคอลัมน์ "ประเภท" บอกว่าเป็นรายรับหรือรายจ่าย เพิ่มปุ่มสลับ 📥 รายรับ / 📤 รายจ่าย (state ใหม่ `_palmMoneyViewType` แยกจาก `_palmMoneyMode` เดิมที่คุมฟอร์มกรอกข้อมูล — คนละหน้าที่กัน) กดแล้ว `renderPalmMoney()` กรอง `list.filter(r=>r.type===_palmMoneyViewType)` ก่อนแสดง ตัดคอลัมน์ "ประเภท" ออกเพราะซ้ำซ้อนกับปุ่มสลับที่บอกอยู่แล้ว ข้อความตอนไม่มีข้อมูลก็แยกเฉพาะประเภทที่กำลังดู ("ยังไม่มีประวัติรายรับ"/"ยังไม่มีประวัติรายจ่าย")

**ส่วนที่ 2 — คอลัมน์ราคา/หน่วยปุ๋ย + ลูกศรเทรนด์**: `renderPalmFertLog()` เพิ่มคอลัมน์ "ราคา/หน่วย" คำนวณจาก `cost ÷ (kg / หน่วยที่ใช้จริงของ record นั้น)` (แปลงกลับเป็นราคาต่อกระสอบ/กก. ตาม `unitLabel` เดิมของแต่ละ record ไม่ fix หน่วยเดียวเพราะบันทึกแต่ละรอบอาจใช้หน่วยไม่เหมือนกัน) เทียบราคากับ record **เก่าที่สุดที่ใกล้ที่สุด** ของปุ๋ย `type` เดียวกัน (`list.slice(i+1).find(x=>x.type===r.type)`) ขึ้นลูกศร ▲ สีแดง (ราคาขึ้น) หรือ ▼ สีเขียว (ราคาลง) ต่อท้ายราคา — record แรกสุดของแต่ละชนิดปุ๋ย (ไม่มีของเก่ากว่าให้เทียบ) ไม่มีลูกศร

**ส่วนที่ 3 — แท็บใหม่ "ประวัติตัดปาล์ม"**: แทนที่จะสร้างฟอร์มกรอกข้อมูลใหม่ซ้ำซ้อน สังเกตว่า record รายรับ "ขายปาล์ม" ใน `D.palmMoney` (ฟอร์มเดิมในแท็บรายรับ-รายจ่าย) มี field `weightKg` อยู่แล้ว ครบพอคำนวณราคา/กก. ได้เลย จึงทำเป็น**มุมมองอ่านอย่างเดียว**ของข้อมูลเดิม ไม่เพิ่มจุดกรอกข้อมูลใหม่ (แก้ไข/ลบยังทำที่แท็บรายรับ-รายจ่ายเหมือนเดิม): เพิ่มปุ่มเมนูย่อย "🌾 ประวัติตัดปาล์ม" + pane ใหม่ `palmp-harvest` มี (ก) กราฟแท่ง Chart.js ราคาเฉลี่ย/กก. แยกตามเดือนปฏิทิน **รวมทุกปีที่มีข้อมูล** ถัวเฉลี่ยแบบถ่วงน้ำหนัก (ยอดขายรวมของเดือนนั้น ÷ น้ำหนักรวม ไม่ใช่ค่าเฉลี่ยของราคาต่อครั้งเฉยๆ) พร้อม highlight เดือนที่ราคาเฉลี่ยดีที่สุดเป็นแท่งสีเขียวแยกจากแท่งอื่น + ข้อความสรุปเดือนที่ดีที่สุด (ข) ตารางประวัติรายครั้งเรียงล่าสุดก่อน — ฟังก์ชันใหม่: `palmHarvestList()` (กรอง `type==='income'` และมี `weightKg>0`), `renderPalmHarvestFilterChips()`/`setPalmHarvestFilterPlot()` (กรองตามแปลง), `renderPalmHarvestChart()`, `renderPalmHarvestTable()` — ลงทะเบียนใน `palmTab()` router เคส `'harvest'`

ทดสอบผ่าน local server (stub `save()` เหมือนทุกครั้งที่ทดสอบฟีเจอร์ในหน้านี้ กันไม่ให้กระทบ Firebase จริง): ส่วนที่ 1 — สลับปุ่มรายรับ/รายจ่ายแล้วตารางกรองถูกต้อง ข้อความว่างตรงประเภท ✓ ส่วนที่ 2 — ใส่ปุ๋ยชนิดเดียวกัน 2 รอบราคาต่างกัน (700→800 บาท/กระสอบ) ขึ้นลูกศร ▲ ถูกต้อง รอบแรกสุดไม่มีลูกศร ✓ ส่วนที่ 3 — ใส่ข้อมูลรายรับทดสอบ 3 รายการ (มี.ค. 2 รายการ, ก.ค. 1 รายการ) ยืนยันราคาเฉลี่ยถ่วงน้ำหนักคำนวณถูกต้อง (มี.ค. = 6.25 บ./กก. จาก 25,000฿/4,000กก., ก.ค. = 4.00 บ./กก.) กราฟ highlight เดือน มี.ค. เป็นเดือนราคาดีที่สุดถูกต้อง ตารางเรียงวันที่ล่าสุดก่อนถูกต้อง ✓ `node --check` ผ่านทั้ง 2 script block ✓ ไม่มี console error ทุกจุด — sync ไฟล์แล้ว

### 148. หน้า PDF ปิดบ่อ: dropdown "เลือกรอบที่ปิด" จำกัดโชว์แค่ปี ≥ 2568

พี่บอกว่า dropdown เลือกรอบที่ปิด (ประวัติ) ในหน้า PDF ปิดบ่อมีข้อมูลย้อนหลังมากเกินไป ทั้งที่ข้อมูลจริงอัปเดตล่าสุดถึงแค่ปี 2568 เท่านั้น — ถ้าในอนาคตอัปเดตข้อมูลย้อนหลังเพิ่มจะแจ้งให้ขยับเกณฑ์นี้

**แก้**: เพิ่มค่าคงที่ `EX_HIST_MIN_YEAR_BE=2568` แล้วกรอง `p.cropHistory` ก่อนสร้าง dropdown ใน `onExPondChange()` — ใช้ `histRecYear(c)` ตัวเดิมที่มีอยู่แล้ว (ยึดปีตาม `closeDate` เป็นหลัก เหมือนที่หน้า P&L/ประวัติใช้อยู่) ไม่สร้างการคำนวณปีคู่ขนาน **สำคัญ**: `<option value="${i}">` ยังคงเป็น index ดั้งเดิมใน `p.cropHistory` เหมือนเดิมทุกจุด (แค่ไม่ list ตัวเลือกที่เก่ากว่าเกณฑ์) เพราะ `getCloseSummary(p,idx)`/`genPDF()`/ฟังก์ชันอื่นๆ อ้างอิง index นี้ตรงๆ ถ้า re-index ใหม่จะทำให้เลือกรอบผิดไปเลย — ใช้เทคนิค map เก็บ `{c,i}` คู่กันแล้ว filter แทน

ถ้าทุกรอบของบ่อนั้นเก่ากว่าเกณฑ์ (edge case) จะซ่อน dropdown เหมือนไม่มีประวัติ แต่ขึ้นข้อความแยกให้รู้ว่า "มีประวัติปิดบ่อเฉพาะก่อนปี 2568 (ยังไม่แสดง)" ไม่ใช่ข้อความ "ยังไม่มีประวัติปิดบ่อ" เฉยๆ กันสับสนว่าข้อมูลหายไปจริงๆ

ทดสอบผ่าน local server (stub `save()` เหมือนทุกครั้ง): ใส่ประวัติทดสอบ 5 รอบ ปี 2565–2569 → dropdown โชว์แค่ 2 รอบหลัง (2568, 2569) ค่า `value` ยังตรงกับ index เดิมในอาร์เรย์ (3, 4) ไม่ใช่ index ใหม่ (0, 1) ✓ auto-select รอบล่าสุดที่ยังแสดงอยู่ถูกต้อง ✓ ทดสอบ edge case บ่อที่มีประวัติทั้งหมดเก่ากว่าเกณฑ์ → ซ่อน dropdown + ขึ้นข้อความอธิบายถูกต้อง ✓ ไม่มี console error — sync ไฟล์แล้ว

### 147. แก้ GitHub Actions "Validate HTML before Pages publish" ที่ fail ซ้ำ 3 ครั้ง — เป็น false positive จากคอมเมนต์ของเราเอง

พี่ให้เช็ค email ล่าสุดจาก GitHub พบว่า workflow "Validate HTML before Pages publish" (`.github/workflows/deploy.yml`) fail ติดกัน 3 ครั้ง (commit 3564ea8, f89f21d, ceb5970) — เช็คแล้วพบว่า**ไม่ใช่ปัญหาจริงกับ HTML** และ**ไม่กระทบการเผยแพร่เว็บ** (GitHub Pages publish อัตโนมัติแยกจาก workflow ตัวนี้ อยู่แล้ว เว็บจริงอัปเดตปกติทุกครั้ง) แต่ควรแก้เพื่อไม่ให้ขึ้น fail ปลอมทุกครั้งที่ commit

**สาเหตุ**: script ใน workflow เช็คแค่นับจำนวนตัวอักษร `<script` เทียบกับ `</script>` แบบ `grep -o '<script' | wc -l` (ไม่ได้ parse HTML จริง) — คอมเมนต์ที่เขียนไว้ตอนแก้ entry #144 ("...ถูกโหลดด้วย `<script src>` ทุกครั้งที่เปิดแอป") มีข้อความ "`<script src>`" อยู่ในนั้นพอดี ทำให้ grep นับเป็น `<script` เพิ่มอีก 1 (เป็น 9) ทั้งที่แท็กจริงในไฟล์มีแค่ 8 คู่ครบถ้วนสมบูรณ์ — ตรวจสอบยืนยันด้วย `grep -n '<script'` ทั้งไฟล์ เจอบรรทัด comment (13192) เป็นตัวก่อปัญหาตัวเดียว

**แก้**: เปลี่ยนคำในคอมเมนต์จาก "`<script src>`" เป็น "แท็ก script src" (ไม่มีเครื่องหมาย `<>` แล้ว) ทั้งใน index.html และ TawanFarm_App.html — ความหมายเดิมทุกประการ แค่เลี่ยงข้อความที่ grep ตีความผิด

ทดสอบ: รัน check เดียวกับที่ workflow ใช้ ยืนยัน `<script` open=8 `</script>` close=8 ตรงกันแล้วทั้ง 2 ไฟล์ ✓ `node --check` ผ่านทั้ง 2 script block (ยืนยันว่าแก้แค่คำในคอมเมนต์ ไม่กระทบโค้ดจริง) ✓ ไฟล์ยัง byte-identical กันทั้งคู่ — sync แล้ว

### 146. ประวัติน้ำ/อาหารบนมือถือ: เปลี่ยนตารางเลื่อนเป็นการ์ดแยกทีละวัน (ทำ mockup ก่อน)

พี่บอกว่าตาราง "ประวัติน้ำย้อนหลัง" และ "สมุดประวัติการให้อาหาร" บนมือถือดูยาก — วันที่ทับกับปุ่มลบข้อมูล ตรวจแล้วพบว่าทั้งสองตารางมีคอลัมน์เยอะ (ค่าน้ำ 8 ค่า/อาหารหลายคอลัมน์) พอบีบให้พอดีจอมือถือ ปุ่ม ✏️/🗑️ ในคอลัมน์แรกไปติดกับวันที่คอลัมน์ถัดมา แถมต้องเลื่อนซ้าย-ขวาดูค่าที่เหลือด้วย ทำ mockup เทียบก่อน-หลังให้ดู พี่อนุมัติแล้วเริ่มทำจริง

**สิ่งที่ทำ**: `renderPdWqHistory()`/`renderPdFeedHistory()` เดิม render แค่ตาราง เปลี่ยนเป็น render **ทั้งตาราง (เดิม) + การ์ดแยกทีละวัน (ใหม่)** ควบคู่กันในโค้ดเดียว แล้วใช้ CSS media query คุมว่าจอไหนโชว์อะไร — `.pd-hist-tblwrap` (ตารางเดิม) ซ่อนที่ ≤600px, `.pd-hist-cards` (การ์ดใหม่) โชว์เฉพาะ ≤600px — **จอคอมไม่เปลี่ยนอะไรเลย** เห็นตารางเดิมทุกประการ

โครงการ์ด: หัวการ์ดแยกวันที่/อายุ (ซ้าย) ออกจากปุ่ม ✏️/🗑️ (ขวาสุด) ด้วย flex `justify-content:space-between` — ไม่มีทางทับกันแบบตารางเดิมเพราะไม่ใช่ cell ติดกันแล้ว ค่าน้ำ 8 ค่าจัดเป็นกริด 2 คอลัมน์ (`.pd-rec-grid`) แทนเรียงแถวเดียวยาว ไม่ต้องเลื่อนจอ ใช้ `st()`/`sc()` ตัวเดิมจำแนกสีค่าผิดปกติ (warn/danger) เหมือนตารางเดิมทุกจุด ไม่สร้างการคำนวณคู่ขนาน บันทึกล่าสุด (record แรกหลัง reverse) ไฮไลต์พื้นเหลืองอ่อนเหมือนคลาส `hi` เดิม หมายเหตุแยกท้ายการ์ดอ่านได้เต็มไม่ตัดคำ

ทดสอบผ่าน local server ที่ viewport 375px (มือถือ) และ 1280px (จอคอม) — stub `save()` เหมือนทุกครั้งที่ทดสอบฟีเจอร์ที่มีปุ่มลบ/แก้ไข กันไม่ให้ทดสอบไปกระทบ Firebase จริง: ที่ 375px การ์ดแสดงถูกต้องครบ (14 การ์ดน้ำ, 2 การ์ดอาหาร) ปุ่ม ✏️/🗑️ แยกจากวันที่ชัดเจนไม่ทับกัน ✓ ที่ 1280px ยืนยันด้วย `getComputedStyle` ว่าตารางเดิมโชว์ (`display≠none`) และการ์ดใหม่ซ่อนอยู่ (`display:none`) ถูกต้อง ✓ ไม่มี console error — sync ไฟล์แล้ว

### 145. ปฏิทินจับกุ้ง & พาเชียล: ลูกศร ◀ ▶ ปรับเกณฑ์ กก./ไร่ "ลองดู" (ทำ mockup ก่อน)

พี่อยากรู้ว่าถ้าไม่เอาพาเชียลที่เกณฑ์เดิม 4,000 กก./ไร่ (4 ตัน) จะได้ไซส์กุ้งเท่าไรและอีกกี่วัน — เดิมเกณฑ์นี้ล็อกไว้ในโค้ด (`PARTIAL_YIELD_TARGET=4000`) ไม่มีทางปรับจากหน้าจอได้เลย ทำ mockup ให้ลองกดลูกศรดูก่อน พี่ยืนยันว่าอยากให้เป็น **"ตัวลองดูเฉยๆ"** ไม่ต้องบันทึกถาวร ปิดแอปแล้วกลับเป็น 4,000 เสมอ

**สิ่งที่ทำ**: เพิ่มตัวแปร `_phTarget` (ค่าเริ่มต้น = `PARTIAL_YIELD_TARGET` เดิม) เก็บใน memory (JS variable) เท่านั้น **ไม่บันทึกลง `D`/Firebase** — ปิดแอปหรือรีเฟรชแล้วกลับเป็น 4,000 เสมอตามที่พี่ต้องการเป๊ะ ส่วน `partialHarvestInfo()` เปลี่ยนจากอ่าน `PARTIAL_YIELD_TARGET` ตรงๆ มาอ่าน `_phTarget` แทน (จุดเดียวที่เรียกฟังก์ชันนี้อยู่ในไฟล์คือ `renderOvPartialHarvest()`) เพิ่มกล่องลูกศร ◀ ▶ ที่หน้าภาพรวม เลื่อนทีละ 250 กก./ไร่ ในช่วง 2,000–6,000 (ปุ่มจะ disable เองที่ขอบ) พร้อมปุ่ม "คืนค่า 4,000" ที่โผล่มาเฉพาะตอนค่าไม่ตรง 4,000 (กดแล้วหายไปเพราะกลับมาเท่าเดิมแล้ว) หน้าตา/สไตล์ตรงกับ mockup ที่พี่อนุมัติทุกจุด (`.tgt-box`/`.tgt-btn`/`.tgt-num` ฯลฯ)

ทดสอบผ่าน local server (stub `save()` เหมือนทุกครั้งที่ทดสอบฟีเจอร์นี้ กันไม่ให้ค่าทดสอบไปเขียนทับ Firebase จริง): ใส่ข้อมูลบ่อทดสอบ (samples 2 ครั้งให้มี PDG คำนวณได้) กดลูกศร ▶ จาก 4,000→4,250 ยืนยันว่าเวลาคาดการณ์ยืดออก (51→58 วัน) และไซส์เป้าหมายใหญ่ขึ้น (41.7→44.3 ก.) ตรงตามสูตรจริง ✓ ปุ่ม "คืนค่า 4,000" กดแล้วกลับ 4,000 และตัวปุ่มหายไปถูกต้อง ✓ กดลูกศรจนสุดขอบ 2,000/6,000 ปุ่มขึ้น disabled ถูกต้อง ✓ ไม่มี console error — sync ไฟล์แล้ว

### 144. 🔴 เลิกโหลดไฟล์ seed 545KB ทุกครั้งที่เปิดแอป + พบ seedCropHistory.js เป็นไฟล์เสีย

พี่ถามว่า "ข้อมูลรอบเลี้ยงย้อนหลังเยอะเกินไปไหม มีส่วนทำให้แอปหนักไหม" — วัดจริงแล้วตอบได้ว่า **ข้อมูลในฐานข้อมูลไม่ใช่ปัญหาเลย** แต่ระหว่างตรวจเจอของจริงที่หนักกว่ามาก

**ผลวัดฐานข้อมูลจริง (production)**: ทั้งฐานข้อมูล **70.6 KB** เท่านั้น — `records` (ค่าน้ำรายวัน) 69.2 KB คิดเป็น 98%, ส่วน **`cropHistory` = 0 KB / 0 รายการ** ยังไม่มีข้อมูลสักรายการ เป็นไปไม่ได้ที่จะทำให้แอปหนัก · 70KB เท่ารูปถ่ายรูปเดียว เก็บต่ออีกหลายปีก็ยังสบาย

**ของจริงที่หนัก: ไฟล์ seed 2 ตัวถูกโหลดด้วย `<script src>` ทุกครั้งที่เปิดแอป รวม 545 KB (uncompressed)** ทั้งที่ใช้แค่ตอน admin กดปุ่ม seed ครั้งเดียวในชีวิต — gzip แล้วดาวน์โหลดแค่ 42KB (ข้อมูลซ้ำเยอะ บีบได้ดี) แต่เบราว์เซอร์ต้อง **แกะ+สร้าง object จาก 545KB เต็มๆ ทุกครั้ง** ซึ่งเป็นงาน CPU บน main thread ก่อนแอปจะเริ่มทำงานได้ (มือถือรุ่นกลาง-ล่างช้ากว่าคอมหลายเท่า)

**🔴 บั๊กที่เจอระหว่างทาง — `seedCropHistory.js` เป็นไฟล์ไม่สมบูรณ์ ไม่ทำงานมาตลอด**: ไฟล์ถูกตัดกลางคัน บรรทัดสุดท้ายจบค้างกลางคำว่า `la` (กำลังจะเขียน `labResults`) → `node --check` fail ด้วย `SyntaxError: Unexpected end of input` → JS parse ไม่ผ่านทั้งไฟล์ → **ทั้ง `HISTORICAL_CROPS` และฟังก์ชัน `seedHistoricalCrops()` ไม่มีอยู่จริงในแอป** (ยืนยันบน production ว่า `typeof` = `undefined` ทั้งคู่) แปลว่าเสียแรงดาวน์โหลด+แกะ 231KB ทิ้งเปล่าทุกครั้ง **และปุ่ม "📥 seed ประวัติการเลี้ยงย้อนหลัง" กดแล้วเงียบมาตลอด** — ไม่มีอะไรเกิดขึ้นและไม่มีข้อความบอกด้วย (ReferenceError เงียบๆ ใน onclick)

**แก้ 3 จุด**:
1. เอา `<script src="seedCropHistory.js">` และ `<script src="seedHarvestHistory.js">` ออกจากท้ายไฟล์ ไม่โหลดตอนเปิดแอปอีกต่อไป
2. เพิ่ม `lazyLoadScript()` + `runSeedHarvestHistory(btn)` — โหลด `seedHarvestHistory.js` เฉพาะตอนกดปุ่ม (โชว์ "⏳ กำลังโหลดข้อมูล..." ระหว่างรอ แล้วค่อยเรียกฟังก์ชันเดิม) ปุ่มกลับสภาพเดิมทั้งกรณีสำเร็จและ error
3. `runSeedHistoricalCrops(btn)` — พยายาม lazy load เหมือนกัน แต่ถ้ายังไม่เจอฟังก์ชัน (เพราะไฟล์เสีย) จะ **แจ้งข้อความชัดเจนว่าไฟล์ไม่สมบูรณ์ ต้องซ่อมก่อน** แทนกดแล้วเงียบแบบเดิม

**ไม่ลบไฟล์ `seedCropHistory.js` ทิ้ง** — ตัวข้อมูล `HISTORICAL_CROPS` ~10,000 บรรทัดยังอยู่ครบ ขาดแค่ท้ายฟังก์ชันไม่กี่บรรทัด ถ้าวันหน้าอยากใช้ประวัติชุดนี้จริงสามารถซ่อมได้ (เก็บไว้ในรีโปเฉยๆ ไม่ถูกโหลด = ไม่มีต้นทุน)

ทดสอบผ่าน local server: หลังแก้ **seed script โหลดตอนเปิดแอป = 0 ไฟล์** (เดิม 2 ไฟล์ 545KB) ✓ `domContentLoadedMs` = 91ms ✓ แอป render ปกติครบ 8 บ่อ ✓ ทดสอบ lazy load จริง — `seedHarvestHistory.js` โหลดใน 25ms แล้วได้ `HISTORICAL_HARVESTLOG` 636 รายการ + ฟังก์ชันครบ (ทดสอบเฉพาะการโหลด ไม่เรียกฟังก์ชัน seed จริงเพื่อกันไม่ให้เขียนข้อมูลลง Firebase) ✓ ทดสอบปุ่มไฟล์เสีย — ขึ้นข้อความอธิบายถูกต้องและปุ่มกลับสภาพเดิม ✓ `node --check` ผ่าน ไม่มี console error — sync ไฟล์แล้ว

### 143. เมนูซ้าย (จอคอม): แยกเมนูหลัก/เมนูย่อยให้ชัดด้วยสี+ขนาด (ทำ mockup ก่อน)

พี่บอกว่ากดเมนูหลักแล้วเมนูย่อยเด้งขึ้นมา สีและขนาดใกล้กันจนแยกไม่ออก ทำงานสับสน — ตรวจ CSS จริงพบว่าต่างกันแค่ 1px (เมนูหลัก 13px `#c4d8ca` / เมนูย่อย 12px `#a9c3b0`) ทำ mockup เทียบก่อน-หลังให้ดู พี่อนุมัติแล้วเริ่มทำจริง

**สิ่งที่ปรับ**: เมนูหลัก (`.sn-top`/`.sn-grp-btn`) ใหญ่ขึ้น 13→14.5px ตัวหนา 600 สีขาวนวลแทนเทาเขียวจาง · เมนูย่อย (`.sn-link`) จางลงอีก (`#a9c3b0`→`#93b3a0`) คงขนาด 12px ไว้ · กลุ่มที่กางอยู่มีพื้นเข้ม+ขีดเขียวซ้าย (`box-shadow:inset`) · เส้นแนวตั้งบางๆ ใต้เมนูย่อยบอกว่าเป็น "ลูก" ของหัวข้อ · เมนูย่อยที่เลือกอยู่มีแถบเขียวหน้าแทนเปลี่ยนแค่สีตัวอักษร

**บั๊กที่เจอระหว่างทำ (ไม่ใช่ของใหม่ แต่โผล่ตอนแก้จุดนี้พอดี)**: ปุ่ม `.sn-top`/`.sn-link` มีคลาส `.tab`/`.tab-item` ติดมาด้วย (ใช้ให้ `goTabByName()` ค้นหาปุ่มข้ามหน้าได้) ซึ่ง `.tab{font-size:12px...}`/`.tab-item{font-size:13px...}` ประกาศไว้คนละจุดในไฟล์ CSS ด้วย specificity เท่ากัน (0,1,0) ทำให้ rule ไหนอยู่หลังกว่าชนะแบบเงียบๆ — ทดสอบจริงพบว่าสไตล์ใหม่ที่เพิ่งเขียนโดนทับหมด (เมนูหลักได้ 12px แทน 14.5px, เมนูย่อยได้ 13px/หนา แทน 12px/บาง) แก้โดย scope ทุก selector ด้วย `.side-nav` (0,2,0) ให้ชนะแน่นอนไม่ว่า `.tab`/`.tab-item` จะประกาศก่อนหรือหลัง — พบเพราะทดสอบด้วย `getComputedStyle()` เทียบค่าที่ตั้งใจเขียนจริงๆ ไม่ใช่แค่ดูว่า CSS ผ่าน syntax check

ทดสอบผ่าน local server จอกว้าง 1280px (เมนูซ้ายแสดงเฉพาะ ≥601px): เปิดกลุ่ม "รายวัน" แล้ววัด `getComputedStyle()` ของปุ่มเมนูหลัก/เมนูย่อยจริง ยืนยันได้ค่าตรงตามที่ตั้งใจทุกจุด (14.5px/700/สว่าง สำหรับเมนูหลัก, 12px/400/จาง สำหรับเมนูย่อย, กล่องเงาขีดเขียวตอนกางกลุ่ม) ✓ ไม่มี console error — sync ไฟล์แล้ว หน้าตาตรงกับ mockup ที่พี่อนุมัติทุกประการ

### 142. การ์ด "ต้นทุนสะสม" หน้ารายละเอียดบ่อ: เพิ่มรายละเอียดอาหาร/ลูกกุ้ง/ค่าแรง/ค่าไฟ (ทำ mockup ก่อน)

พี่ขอเพิ่มรายละเอียดในการ์ดต้นทุนสะสม 4 หมวด: อาหารกุ้ง (กระสอบ+กก.), ลูกกุ้ง (กี่ตัว ตัวละกี่บาท), ค่าแรง, ค่าไฟ — ทำ mockup ให้ดูก่อนโดยจำลองการ์ดจริงเป๊ะ (ใช้ CSS variable/class ชุดเดียวกับแอปจริงทั้งหมด ไม่ได้ออกแบบใหม่) พี่ดูแล้วโอเคให้เริ่มทำจริงได้เลย

**หลักการ**: ทั้ง 4 หมวดนี้เดิมกดแล้วไม่มีอะไรเกิดขึ้น (ไม่มี dropdown รายละเอียดเหมือนหมวดสต็อกอื่นๆ) เพราะไม่ได้เก็บเป็น "รายการ" แบบสต็อก จึงไม่ใช้ระบบ itemized list เดิม แต่เพิ่มบรรทัดข้อความสรุปแทน — **ไม่สร้างคำนวณคู่ขนาน** ทุกตัวเลขดึงจากฟังก์ชัน/ตัวแปรที่มีอยู่แล้วในหน้านี้:
- **อาหารกุ้ง** = รวม `p.feedLog[].totalFeed` (กก.) ทั้งหมด ÷ 25 = กระสอบ (ใช้ตัวหาร 25 ตัวเดียวกับที่ `pondFeedCost()` ใช้คำนวณราคาอยู่แล้ว กันเลขไม่ตรงกัน)
- **ลูกกุ้ง** = `pRelease(p)` (ตัวเลขเดียวกับที่การ์ดข้อมูลบ่อด้านบนใช้แสดง "X ตัว") หาร `p.costFry` ได้ราคา/ตัว
- **ค่าแรง/ค่าไฟ** = ใช้ `age` (อายุบ่อ, ตัวแปรเดียวกับที่ badge สถานะบ่อใช้) หารต้นทุนที่คำนวณไว้แล้ว (`pdLaborCost`/`pdElecCost`) ได้ค่าเฉลี่ย/วัน — ไม่ใช่ค่าคงที่ เพราะสูตรจริงคิดต่อวันแล้วหารด้วยจำนวนบ่อ active ที่เปลี่ยนได้ทุกวัน ตัวเลขเฉลี่ยนี้จึงเป็นค่า "สรุปย้อนหลัง" ไม่ใช่เรตปัจจุบัน

ขยาย `pdCostCatRow()` ให้รับ `detailHtml` เสริม (แทนที่ itemized list เมื่อมีการส่งมา) ใช้ `pdToggleCatItems(key)` ตัวเดิมที่มีอยู่แล้ว ไม่ต้องเขียนกลไกเปิด/ปิดใหม่

ทดสอบผ่าน local server (stub `save()`, ตั้ง `APP_ROLE='admin'` ชั่วคราวเพราะการ์ดนี้โชว์เฉพาะ admin): ใส่ feedLog ทดสอบ 350 กก. → โชว่ "รวม 350 กก. ≈ 14 กระสอบ" ถูกต้อง ✓ ลูกกุ้ง 660,000 ตัว + costFry 45,000 → โชว่ "660,000 ตัว × ฿0.07/ตัว" ถูกต้อง ✓ ค่าแรง/ค่าไฟ โชว่ "เลี้ยงมาแล้ว 127 วัน · เฉลี่ย ฿.../วัน" ถูกต้อง (ทดสอบสูตรหารแยกกับเลขสมมติยืนยันด้วย) ✓ กด/พับขยายแต่ละแถวทำงานถูกต้อง ✓ ไม่มี console error — sync ไฟล์แล้ว

### 141. เพิ่มหมวด "ปูน" ในช่องค้นหาของเวชพันฑ์ผสมกิน

พี่แจ้งว่ามีการใช้ปูนบางตัวผสมอาหารด้วย (ไม่ใช่แค่ยา/จุลินทรีย์) แต่ช่องค้นหาเดิมกรองแสดงเฉพาะ 4 หมวด (`chemOral`,`chemWater`,`probioOral`,`probioWater`) ทำให้หาสินค้าหมวดปูนไม่เจอ — เพิ่ม `'lime'` เข้าไปใน `MED_PROTOCOL_CATS` (entry #140) จุดเดียว เพราะ `medProtocolAvailItems()` อ่านหมวดจาก array นี้ตัวเดียวใช้ทั้งช่องค้นหาและ badge สี (สีปูนใช้ `STOCK_CAT_CSS.lime`/label `STOCK_CAT_LABEL.lime` ที่มีอยู่แล้วในหน้าสต็อก ไม่ต้องเพิ่มสีใหม่)

ทดสอบผ่าน local server (stub `save()` เหมือนทุกครั้ง): เพิ่มสินค้าทดสอบหมวดปูน พิมพ์ค้นหาในช่องเพิ่มรายการ ปรากฏในผลลัพธ์ถูกต้องพร้อม label "ปูน — ..." ✓ ไม่มี console error — sync ไฟล์แล้ว

### 140. ปรับตารางเวชพันฑ์ผสมกิน 3 จุด: เปลี่ยนชื่อการ์ด + ช่องพิมพ์ค้นหาแทน dropdown + วันอัปเดตล่าสุด

พี่ขอแก้ 3 อย่างในฟีเจอร์ตารางการลงเวชภัณฑ์ (entry #138) หลังใช้งานจริง:

**1) เปลี่ยนชื่อการ์ด** จาก "🔁 ตารางการลงเวชภัณฑ์" เป็น "🔁 เวชพันฑ์ผสมกิน" ตามที่พี่เรียกฟีเจอร์นี้จริงๆ ในหน้างาน — แก้แค่ข้อความ title ใน `pdCard('medproto',...)` ไม่กระทบ logic ใดๆ

**2) ช่องเพิ่มรายการเปลี่ยนจาก dropdown เป็นพิมพ์ค้นหา** — เดิมเป็น `<select>` ต้องกดเปิดแล้วเลื่อนหาทีละบรรทัด ถ้ารายการยาในสต็อกเยอะจะหายาก เปลี่ยนเป็น `<input list="...">` ผูกกับ `<datalist>` (ฟีเจอร์ autocomplete มาตรฐานของเบราว์เซอร์ พิมพ์ไม่กี่ตัวอักษรก็กรองแสดงตัวเลือกให้ทันที ไม่ต้องเขียน JS filter เอง) จับ event `change` แล้วจับคู่ข้อความที่พิมพ์/เลือกกลับไปหา item ตัวจริงด้วยฟังก์ชันใหม่ `pdProtoSearchMed()` ก่อนเรียก `pdProtoAddMed()` เดิม — เพิ่ม `mprotEsc()` (escape `&`/`"`/`<`) เพราะรอบนี้ต้องใส่ชื่อสินค้าไว้ใน attribute `value="..."` ของ `<option>` ซึ่งต่างจาก dropdown เดิมที่ใส่เป็น text content ธรรมดา (ถ้าชื่อสินค้ามีเครื่องหมาย `"` จะทำให้ HTML พังถ้าไม่ escape) ทดสอบแล้วว่าชื่อสินค้าที่มี `"` อยู่ในชื่อยังทำงานถูกต้อง

**3) เพิ่มวันที่อัปเดตล่าสุด** — พี่อยากรู้ว่าตารางนี้เปลี่ยนข้อมูลล่าสุดเมื่อไร (คนละอันกับ "หมายเหตุ" ในหน้าภาพรวมที่มีวันอัปเดตอยู่แล้ว — อันนี้คือของ "ตาราง" เอง) เพิ่ม `updatedAt`/`updatedBy` เข้าไปใน `p.medProtocol` stamp ทุกครั้งที่กด "บันทึกตาราง" (`pdProtoSave`) ใช้รูปแบบวันที่เดียวกับที่ `ovProtoSaveNote` ใช้อยู่แล้ว (`toLocaleString('th-TH',...)` + `curUserLabel()`) ไม่สร้างรูปแบบใหม่ซ้ำซ้อน แสดงผลต่อจากบรรทัด "เหตุผลที่สั่งใช้" ในการ์ด

ทดสอบผ่าน local server (stub `save()` ชั่วคราวกันไม่ให้ไปแตะ Firebase จริงเหมือนทุกครั้งที่ทดสอบฟีเจอร์นี้): ชื่อการ์ดเปลี่ยนถูกต้อง ✓ ช่องพิมพ์ค้นหาโชว์ตัวเลือกจาก stock ที่ยังไม่ถูกเลือกในรอบนั้น ✓ พิมพ์/เลือกแล้วเพิ่มเข้ารอบได้ถูกต้อง ✓ ชื่อสินค้าที่มีเครื่องหมายคำพูดไม่ทำให้ HTML พัง ✓ บันทึกตารางแล้วโชว์ "🕒 อัปเดตล่าสุด: ..." ถูกต้อง ✓ ไม่มี console error — sync ไฟล์แล้ว

### 139. แก้บั๊กเรื่องมือถือค้าง/โหลดช้าตอนเปิดหน้ารายละเอียดบ่อ — เปลี่ยนเกจวงกลม 4 อันจาก Chart.js เป็น CSS ล้วน

พี่แจ้งว่ามือถือ "เริ่มมีการค้าง แหละโหลดช้าบางหน้า" โดยเฉพาะหน้า "ภาพรวม" กับ "รายละเอียดบ่อ" ตอนสลับหน้าในแอป — ตรวจสอบโดยวัดเวลาจริงด้วย `performance.now()` ครอบแต่ละฟังก์ชัน render ก่อนสรุปว่าอะไรคือสาเหตุ (ไม่เดาเฉยๆ)

**สิ่งที่ตัดออกจากการเป็นสาเหตุ**: ข้อมูลจริงในฐานข้อมูลยังเล็กมาก (~71KB, 8 บ่อ, สูงสุด 74 records/บ่อ) ไม่ใช่ปัญหาข้อมูลบวม และ Chart.js ทุกจุด (55 จุดทั้งไฟล์) มีการ `.destroy()` ก่อนสร้างใหม่ถูกต้องแล้ว ไม่ใช่ memory leak · `renderOv()` เองก็เร็วมาก (~3ms) ไม่ใช่คอขวด

**สาเหตุจริงที่วัดได้**: `openPondDetail()` ทั้งฟังก์ชันใช้เวลา ~91ms บนคอมทดสอบ (มือถือรุ่นกลาง-ล่างจะช้ากว่านี้หลายเท่า) — แยกตามฟังก์ชันย่อยพบว่า `renderPdGauges()` (เกจวงกลมครึ่งวง pH/DO/แอมโมเนีย-ไนไตรท์/อุณหภูมิ 4 อัน) ใช้เวลาถึง **59ms หรือ 65% ของเวลาทั้งหมด** เพราะสร้าง Chart.js doughnut instance เต็มรูปแบบ 4 ตัวทุกครั้งที่เปิดหน้าบ่อ ทั้งที่แต่ละอันแสดงผลแค่ "ครึ่งวงกลมสี + ตัวเลข" เท่านั้น รวมกับ `renderPdCharts()` (กราฟเส้น/แท่งจริงอีก 4 อัน) อีก 24ms กลายเป็น 8 Chart.js instance ต่อการเปิดบ่อ 1 ครั้ง

**แก้ไข**: เขียน `renderPdGauges()` ใหม่ทั้งหมดให้เลิกพึ่ง Chart.js เปลี่ยนเป็น CSS ล้วน — ใช้ `conic-gradient` วาดส่วนโค้งสี (`.pd-gauge-arc-box::before`) แล้ว "เจาะรู" ตรงกลางด้วยวงกลมทึบสีพื้นการ์ด (`.pd-gauge-arc-box::after` สีเดียวกับพื้นหลังการ์ด) ทับให้ดูเป็นวงแหวนบาง — ตอนแรกลองใช้ CSS `mask`/`-webkit-mask` กับ `radial-gradient` เพื่อเจาะรู (เทคนิคมาตรฐานทั่วไป) แต่ทดสอบจริงพบว่า **`mask` ไม่ render เลยในเบราว์เซอร์ที่ทดสอบ** (สงสัยว่าเป็นปัญหา compat กับ WebView บางรุ่น) จึงเปลี่ยนมาใช้วิธี "วงกลมทึบทับ" แทนซึ่งทำงานได้ 100% ทุกเบราว์เซอร์เพราะไม่ต้องพึ่ง CSS masking เลย ลบ `pdBustGauges()`/`_pdGaugeCharts`/Chart.js call ออกทั้งหมดในฟังก์ชันนี้ (ไม่มีใครเรียกใช้ที่อื่นแล้ว)

ทดสอบวัดผลจริงก่อน-หลังด้วย `performance.now()` ครอบ `openPondDetail()`: **renderPdGauges จาก 59ms เหลือ 0ms** (ลบ Chart.js instantiation overhead ออกหมด) ทำให้เวลาเปิดหน้าบ่อทั้งหมดลดจาก ~91ms เหลือ ~18-19ms (**ลดลง ~80%**) — มือถือรุ่นกลาง-ล่างที่ช้ากว่านี้หลายเท่าจะรู้สึกถึงความต่างชัดเจนกว่าตัวเลขบนคอมอีก ตรวจสอบภาพหน้าจอยืนยันว่าเกจแสดงผลเหมือนเดิมทุกประการ (สีตามเกณฑ์ ตัวเลข ข้อความสถานะ) เพียงแต่ไม่มี Chart.js instance อยู่เบื้องหลังแล้ว · `node --check` ผ่านทั้ง 2 script block — sync ไฟล์แล้ว

### 138. สมุดบันทึกการใช้เวชภัณฑ์แบบตารางหมุนเวียนหลายรอบ + กระดาน Protocol ในหน้าภาพรวม

พี่เจอปัญหา 2 อย่าง: (1) สั่งเวชภัณฑ์ไปแล้วลืมว่าตอนนี้บ่อไหนใช้ตัวไหนอยู่ (2) สั่งทดลองใช้ยา 5 วันแล้วลืมสั่งต่อ พนักงานเลยใช้ตัวทดลองต่อไปเรื่อยๆ เพราะไม่มีคำสั่งใหม่มายกเลิก — ขอ mockup ก่อนแล้วค่อยทำจริง (ทำ mockup 3 รอบ: v1 คิดเป็น "ออเดอร์ตายตัวต่อตัวยา" พี่แก้เป็น v2 "ตารางหมุนเวียนหลายรอบ" เช่น ยา a,b,c ใช้ 3 วัน → สลับเป็น c,d,e 3 วัน → วนกลับ แล้วเพิ่ม v3: ปุ่มจบรอบก่อนกำหนด + แก้ไขรอบล่วงหน้าได้โดยไม่กระทบรอบที่ใช้อยู่ + กระดาน Protocol หน้าภาพรวม)

**โครงสร้างข้อมูล** เก็บที่ `p.medProtocol = {active, rounds:[{meds:[{id,name,category}],days}], loop, curIdx, curStart, reason, note, noteUpdatedAt, noteUpdatedBy}` — `curIdx` ชี้รอบที่กำลังใช้อยู่, ถ้าไม่วนซ้ำและหมดรอบแล้วจะเป็น sentinel `curIdx>=rounds.length` (= "จบตารางแล้ว ไม่มีคำสั่งต่อ")

**"self-healing tick"** — แอปนี้ไม่มี cron ฝั่งเซิร์ฟเวอร์ จึงให้ `medProtocolAdvance(p)` เดินตารางอัตโนมัติทุกครั้งที่ render หน้าภาพรวม/หน้ารายละเอียดบ่อ ใช้ `while` loop เทียบ `curStart` กับวันนี้ทีละรอบ (กันเคสไม่ได้เปิดแอปหลายวันติด ไม่ใช่กระโดดตรงไปวันนี้แต่ไล่กินรอบทีละรอบให้ถูกต้อง)

**แก้รอบล่วงหน้าไม่กระทบรอบที่ใช้งานจริง** (พี่ตอบ "ต้องการ") — `pdProtoSave()` จะรีเซ็ต `curIdx=0,curStart=วันนี้` เฉพาะตารางใหม่ทั้งหมดเท่านั้น ถ้าเป็นการแก้ตารางที่ active อยู่แล้วจะคง `curIdx`/`curStart` เดิมไว้ (แค่ clamp กันรอบที่ใช้อยู่ถูกลบไปพอดี) ส่วนกรณีตารางจบไปแล้วแล้วเพิ่มรอบใหม่ (`pdProtoAddRoundQuick`) จะชี้ `curIdx` ไปที่รอบใหม่ที่เพิ่มพร้อมรีเซ็ต `curStart=วันนี้`

**ปุ่มจบรอบก่อนกำหนด** (พี่ตอบ "อยาก") — `pdProtoEndEarly()` บังคับ `curStart` ให้ elapsed ≥ จำนวนวันของรอบปัจจุบันแล้วเรียก `medProtocolAdvance()` ต่อทันที

**หน้ารายละเอียดบ่อ** เพิ่มการ์ด "🔁 ตารางการลงเวชภัณฑ์" — timeline เส้นแนวตั้งจุดวงกลม done(✓เขียว)/active(▶ม่วง+progress bar)/pending(เลขจาง เส้นประ) แต่ละรอบโชว์ badge สีตามหมวดของยา (ใช้ `STOCK_CAT_CSS` ชุดเดียวกับหน้าสต็อก ไม่สร้างสีใหม่) ปุ่มวนซ้ำ (`.trial-chip` เดิม) และปุ่มหยุดทั้งหมด (สไตล์ inline `--db`/`--d` แบบเดียวกับปุ่มอันตรายจุดอื่นในแอป ไม่มี `.btn-del` ให้ใช้) ถ้าจบตารางแล้วจะขึ้นแบนเนอร์เตือนสีแดงพร้อม 3 ปุ่มกู้สถานการณ์: วนกลับรอบ 1 / เพิ่มรอบใหม่ / หยุดทั้งหมด

**หน้าภาพรวม** เพิ่มการ์ด "💊 Protocol เวชภัณฑ์ปัจจุบัน — ทุกบ่อ" (ซ่อนถ้าไม่มีบ่อไหนมีตารางเลย) แสดงยาที่ใช้อยู่รอบปัจจุบันของทุกบ่อ **+ ช่องหมายเหตุอิสระแยกจากตาราง** (`note`) พร้อมวันเวลา+ชื่อผู้บันทึกล่าสุด — ตามที่พี่อธิบายว่า "ตารางคือแผนที่วางไว้ล่วงหน้า ส่วนหมายเหตุคือสิ่งที่เกิดขึ้นจริงวันนี้" กันสับสนเวลาสั่งงานปากเปล่าแล้วลืมพิมพ์เข้าตารางอย่างเป็นทางการ

ทดสอบ: `node --check` ผ่านทั้ง 2 script block ✓ ทดสอบผ่าน local server โดย stub ฟังก์ชัน `save()` ชั่วคราวในเบราว์เซอร์ (ไม่แตะ Firebase จริงเพราะฐานข้อมูลนี้ sync ข้อมูลจริงของฟาร์ม) ครบทุกเคส: สร้างตารางใหม่ 2 รอบ+วนซ้ำ → เดินหน้าอัตโนมัติ (progress bar/badge/meta วันที่ถูกต้อง) → จบรอบก่อนกำหนดข้ามไปรอบถัดไป → วนกลับรอบ 1 เมื่อครบรอบสุดท้าย → ปิดวนซ้ำแล้วจบตารางขึ้นสถานะ "จบตารางแล้ว" พร้อม 3 ปุ่มกู้สถานการณ์ครบ → วนกลับ/เพิ่มรอบใหม่ทำงานถูกต้อง → แก้ไขรอบล่วงหน้าขณะรอบปัจจุบันกำลังทำงานอยู่ไม่กระทบ `curIdx`/`curStart` เดิม → กระดาน Protocol หน้าภาพรวมโชว์ผลถูกต้อง + บันทึกหมายเหตุพร้อมประทับเวลาสำเร็จ ✓ ไม่มี console error — sync ไฟล์แล้ว

### 137. ปรับการแสดงผลบนมือถือ: ขยายฟอนต์เล็กทั้งชุด + แก้บั๊กเกจล้นจอ

พี่บอกว่าบนมือถือ "ไม่เล็กหรือใหญ่เกินไป บางข้อมูลเหมือนจะโดนบีบจนอ่านยาก" — วัดจริงบนจอ 375px เจอ 2 เรื่อง

**1) ฟอนต์เล็กเกินไป (สาเหตุหลักของ "อ่านยาก")** — ไล่นับทั้งไฟล์เจอ `font-size` ต่ำกว่า 12px อยู่ **278 จุด** (9px×14, 9.5px×8, 10px×53, 10.5px×18, 11px×138, 11.5px×47) กระจายทั้งใน CSS class (114 rule) และ inline style ในเทมเพลต JS ปัญหาคือภาษาไทยมีสระบน-ล่าง+วรรณยุกต์ซ้อนกัน ที่ 10-11px บนจอโทรศัพท์อ่านยากกว่าภาษาอังกฤษขนาดเดียวกันมาก

แก้ทีละจุดไม่ไหว (และจะไปเปลี่ยนขนาดบนจอคอมด้วย) จึง**แปลงเป็นตัวแปร CSS ทั้ง 278 จุด** (`font-size:10px` → `font-size:var(--fs10)` ฯลฯ) ประกาศค่าเริ่มต้นที่ `:root` ให้**เท่าเดิมเป๊ะทุกตัว** แล้วเขียนทับเฉพาะใน `@media(max-width:600px)` ให้ขยับขึ้น ~1-2px (9/9.5→10.5, 10/10.5→11.5, 11→12, 11.5→12.5) — จอคอมได้ค่าเดิมทุกประการ มือถืออ่านง่ายขึ้นโดยเลย์เอาต์ไม่แตก · ปรับ `.tbl` บนมือถือจาก 12px→12.5px และ padding 8px→9px ด้วย

⚠️ **จุดที่ต้องกันไว้**: PDF ปิดบ่อสร้างจาก div กว้าง 800px คงที่แล้วถ่ายเป็นภาพด้วย html2canvas — ตัวมันอยู่ในหน้าเดียวกัน ถ้ากด export บนมือถือ media query จะไปขยายฟอนต์ใน PDF ด้วย ทำให้ PDF หน้าตาไม่เหมือนกันระหว่างมือถือกับคอม จึงใส่ class `pdf-export-root` ที่ div นั้นแล้วรีเซ็ตตัวแปรกลับเป็นค่าจอใหญ่ (ทดสอบยืนยันแล้วว่าในกล่อง PDF ได้ 9/10/11/11.5px เหมือนเดิมแม้ viewport = 375px)

**2) เกจในหน้ารายละเอียดบ่อล้นจอ (บั๊กเดิม ไม่ได้เกิดจากข้อ 1)** — `.pd-gauges` ใช้ `grid-template-columns:repeat(2,1fr)` ซึ่ง `1fr` = `minmax(auto,1fr)` และ `auto` จะไม่ยอมหดต่ำกว่าขนาดเนื้อหา · canvas ของ Chart.js กว้าง 300px โดยดีฟอลต์ → ดันคอลัมน์บานเป็น 314px×2 = **650px ล้นจอ 375px** แก้เป็น `minmax(0,1fr)` ให้คอลัมน์หดได้ canvas ย่อตามช่องจริง (ยืนยันว่าเป็นบั๊กเดิมโดยรันไฟล์ก่อนแก้เทียบ — ได้ 650px เท่ากัน)

ทดสอบ: จอคอม 1280px ตัวแปรทุกตัวได้ค่าเดิมเป๊ะ (9/9.5/10/10.5/11/11.5px) ✓ มือถือ 375px ครบ **30 หน้า** (รวม CEO + หน้ารายละเอียดบ่อกางทุกการ์ด) ไม่มีหน้าไหนล้นแนวนอน ✓ เกจจาก 314px → 171px พอดีช่อง ✓ จอ 360px (Android ทั่วไป) ไม่ล้นทุกหน้า ✓ `node --check` ผ่าน ไม่มี console error — sync ไฟล์แล้ว

**หมายเหตุ**: จอ 320px (iPhone SE รุ่นแรก) ยังมี 3 หน้าล้นเล็กน้อย ~20px (สุ่มตัวอย่าง/รอบเลี้ยง/P&L) — ตรวจแล้วเป็นของเดิมก่อนแก้ (sample 342px, pl 340px เท่ากันทั้งก่อนและหลัง) ไม่ใช่ผลจากงานนี้ ยังไม่ได้แก้เพราะอยู่นอกขอบเขต ถ้าพี่ใช้เครื่องจอเล็กขนาดนั้นบอกได้จะตามแก้ให้

### 136. แก้สีหมวด "จุลินทรีย์กิน" กับ "จุลินทรีย์ลงน้ำ" ที่คล้ายกันเกินไป

พี่บอกว่าสี 2 หมวดนี้ในการ์ดเวชภัณฑ์ (entry #135) คล้ายกันเกินไปแยกไม่ออก — ตรวจแล้วพบว่าเดิมทั้งคู่เป็นโทนม่วง/violet เกือบเหมือนกัน (`probioOral` #7E22CE vs `probioWater` #5B21B6 ต่างกันแค่ความเข้ม)

**เปลี่ยนเป็นคนละโทนสีชัดเจน**: `probioOral` (จุลินทรีย์กิน) → indigo `#4338CA` / `probioWater` (จุลินทรีย์ลงน้ำ) → fuchsia `#A21CAF` — เลือกจากการไล่เช็คสีที่ใช้อยู่แล้วทั้งแอปในโครงสร้างเดียวกัน (`CEO_CAT_DOT` ที่มีทั้ง feed/chem/chemOral/chemWater/lime/equip/fry/labor/elec/shared ปรากฏเป็นจุดสีเรียงกันในการ์ดต้นทุนสะสม) กันเลือกสีไปชนกับหมวดอื่นที่มีอยู่แล้วซ้ำอีก (ตอนแรกลองทีล/ชมพูแล้วพบว่าชนกับ `labor`/`seed` พอดี เลยเปลี่ยนมาใช้คู่ indigo/fuchsia แทน)

**แก้ 3 จุดให้สีตรงกันหมด** (เดิมมี `STOCK_CAT_CSS` badge, `PD_MED_CAT_OPTS` ปุ่มเลือกหมวดในการ์ดเวชภัณฑ์, และ `CEO_CAT_DOT` จุดสีในการ์ดต้นทุนสะสม — ใช้ hex คนละชุดกันมาตั้งแต่แรกอยู่แล้ว เปลี่ยนให้ตรงกันทั้ง 3 จุดในคราวเดียว) ผลคือ badge/ปุ่ม/จุดสีของ 2 หมวดนี้จะเป็นสีเดียวกันเป๊ะไม่ว่าจะไปโผล่ที่หน้าไหนของแอป

ทดสอบผ่าน local server: badge หมวด `probioOral` render เป็น indigo `rgb(67,56,202)`, หมวด `probioWater` เป็น fuchsia `rgb(162,28,175)` ต่างกันชัดเจน ✓ ปุ่มเลือกหมวดตอนกดเลือก ("on") ได้สีตรงกับ badge เป๊ะ ✓ ไม่มี console error — sync ไฟล์แล้ว

### 135. การ์ดเวชภัณฑ์ในหน้ารายละเอียดบ่อ: สีตามหมวดชัดเจน + ปุ่มกดเลือกหมวดแทน dropdown

พี่ขอ 2 อย่างสำหรับการ์ด "💊 เวชภัณฑ์ที่ใช้ล่าสุด" (entry #129/#130): (1) จำแนกสีของหมวดให้ชัดเจนขึ้น (2) เปลี่ยนจาก dropdown เป็นปุ่มกดเลือกหมวด

**สี**: เดิมทุกแถวในตารางใช้ badge สีเดียว (`b-info` ฟ้าหมดทุกหมวด) แก้เป็นใช้ `STOCK_CAT_CSS[category]` — คลาสสีเดียวกับที่หน้าสต็อกใช้อยู่แล้ว (`.stock-cat-chemOral` ฟ้าเข้ม, `.stock-cat-chemWater` ฟ้าอ่อน/cyan, `.stock-cat-probioOral` ม่วง, `.stock-cat-probioWater` ม่วงเข้ม) ทำให้มองแวบเดียวแยกหมวดออกจากกันได้ ไม่ต้องสร้างชุดสีใหม่

**ปุ่มเลือกหมวด**: เอา `<select id="pd-med-cat">` ออก แทนด้วยแถบปุ่ม `.trial-chip` เหมือนปุ่มเลือกช่วงวันที่มีอยู่แล้ว — ฟังก์ชันใหม่ `pdMedCatChips(cat)` render ปุ่ม "ทุกหมวด" + 4 หมวด โดยปุ่มที่กำลังเลือกอยู่ (`.on`) จะได้สีพื้นตรงกับหมวดนั้นทันที (inline style ทับ `.trial-chip.on` ปกติ เพราะแต่ละหมวดมีสีต่างกัน ใช้ CSS class เดียวไม่ได้) ใช้ hex สีเดียวกับตัวหนังสือใน `STOCK_CAT_CSS` (`#1D4ED8`/`#0369A1`/`#7E22CE`/`#5B21B6`) — กดปุ่มแล้วรู้ทันทีว่ากำลังกรองหมวดไหนอยู่ ไม่ต้องเปิด dropdown มาดู

`setPdMedCat(v)` เหลือแค่เซ็ต state แล้วเรียก render — ให้ `renderPdMedHistory()` regenerate ปุ่มทั้งแถวจาก state ทุกครั้งเหมือนกับปุ่มช่วงวันที่มีอยู่แล้ว (`pdMedPeriodChips`) ไม่ต้องเขียน logic toggle class ซ้ำ

ทดสอบผ่าน local server: ปุ่มหมวดขึ้นครบ 5 ปุ่มพร้อม emoji เดิม ✓ badge ในตารางเปลี่ยนสีตามหมวดจริง (`stock-cat-chemOral`/`stock-cat-chemWater`) ✓ กดปุ่ม "ยาลงน้ำ" กรองเหลือเฉพาะแถวที่ตรงหมวด ✓ ปุ่มที่เลือกอยู่มีสีพื้น `#0369A1` ตรงกับหมวด chemWater ✓ ไม่มี console error — sync ไฟล์แล้ว

### 134. ลบหน้า "🧪 ประวัติสารเคมี & วัสดุ" — ซ้ำซ้อนกับหน้าสต็อก

พี่ถามว่าหน้า "ประวัติสารเคมี & วัสดุ" (`sec-chem`) ซ้ำซ้อนกับหน้าอื่นไหม — ตรวจสอบแล้วพบว่า `renderChemHistory()` ดึงข้อมูลจากแหล่งเดียวกันเป๊ะกับหน้าสต็อก → แท็บ "ประวัติ" → "ประวัติเบิกออก" (`renderStockOutLog()`): ทั้งคู่ query `D.inventoryLog` ที่ `type==='out' && pondId` มีตัวกรองชุดเดียวกัน (บ่อ/หมวด/เดือน) ต่างกันแค่หน้าสต็อกมีคอลัมน์ "ผู้บันทึก" + ปุ่มลบเพิ่มมา ส่วนหน้าสารเคมีเป็นเวอร์ชัน "ดูอย่างเดียว" ที่ทำอะไรได้น้อยกว่า — ไม่มีเหตุผลต้องมี 2 เมนูแยกกันดูข้อมูลชุดเดียวกัน พี่ยืนยันให้ลบทิ้ง

**ลบออกทั้งหมด**: ปุ่มเมนู 3 จุด (sidebar `sn-link`, top-tab dropdown `tab-chem`, mobile sheet `data-t="chem"`), section `<div id="sec-chem">` ทั้งก้อน, ฟังก์ชัน `initChem()`/`renderChemHistory()`/`clearChemFilter()`/`onChPond()` (ตัวหลังเป็น dead code อยู่แล้วไม่เคยถูกเรียกจริง), และ `if(t==='chem')initChem();` ใน router `goTab()`

**สิ่งที่ไม่แตะ (คนละ namespace แต่ใช้คำว่า "chem" เหมือนกัน)**: `toggleChSec('chem')`/`ch-sec-chem`/`renderChChemTable()` ในหน้าประวัติรอบเลี้ยงที่ปิดแล้ว (แสดง `c.chemLog` ของรอบนั้นๆ คนละข้อมูลกับ `D.inventoryLog`) และเงื่อนไข `cat==='chem'` ใน `PD_MED_CATS`/`getStockCostGroupsForPond()` (หมวดสต็อกเก่าที่ยังใช้จัดกลุ่มต้นทุนเวชภัณฑ์อยู่)

ทดสอบผ่าน local server: `sec-chem`/`tab-chem`/ปุ่มเมนูทั้ง 3 จุดหายไปจริง ✓ ฟังก์ชัน `toggleChSec`/`renderChChemTable` (คนละฟีเจอร์) ยังทำงานได้ปกติ ✓ หน้าสต็อก/จัดการน้ำเปิดได้ปกติไม่มีอะไรพัง ✓ ไม่มี console error — sync ไฟล์แล้ว

### 133. ปฏิทินพาเชียล: เพิ่มไซส์กุ้งที่คาดว่าจะได้ ณ วันที่คาดการณ์

พี่ขอให้การ์ด "ปฏิทินจับกุ้ง & พาเชียล" (entry #124) แสดง "การประมาณการไซส์ ที่จะถึง ณ วันนั้นๆ" ด้วย — เดิมบอกแค่ว่าอีกกี่วันจะถึงเกณฑ์ 4,000 กก./ไร่ แต่ไม่บอกว่าตอนนั้นกุ้งจะไซส์เท่าไหร่ ซึ่งเป็นตัวเลขที่ต้องรู้เพื่อตัดสินใจว่าจะจับตอนนั้นคุ้มไหม (ไซส์ยิ่งใหญ่ราคายิ่งดี)

**ตัวเลขที่ต้องการมีอยู่ในสูตรเดิมอยู่แล้ว** — `targetAbw` (ABW ที่ทำให้ yield แตะ 4,000) ถูกคำนวณไว้เพื่อหาจำนวนวันอยู่แล้ว แค่ไม่เคย return ออกมา จึงเพิ่ม `targetAbw`/`abwNow`/`abw` เข้าไปใน return ของ `partialHarvestInfo()` แล้วแสดงผล 3 จุด: (1) การ์ดบ่อที่ถึงเกณฑ์แล้ว → เพิ่ม "ไซส์ตอนนี้" (2) การ์ดบ่อที่ยังไม่ถึง → บรรทัดใหม่ "ไซส์คาด ณ วันนั้น X · ตอนนี้ ~Y" (3) ตารางปฏิทิน → คอลัมน์ใหม่ "ไซส์คาด ณ วันนั้น" (แถว "ถึงไซส์ 22 ก." ใส่ 22 ตรงๆ ตามนิยาม)

ฟังก์ชันใหม่ `fmtShrimpSize(g)` แสดงเป็น `X.X ก. (N ตัว/กก.)` — โชว์ทั้ง 2 หน่วยเพราะชาวบ่อคุยกันด้วย "ตัว/กก." เป็นหลัก แต่ ABW ในระบบเก็บเป็นกรัม

**แก้บั๊กที่เจอระหว่างทาง**: เดิมคำนวณจำนวนวันจาก `(targetAbw − lat.abw) / pdg` โดยใช้ ABW ของ**วันที่สุ่ม** แต่เอาไปบวกกับ**วันนี้** ทำให้วันที่คาดการณ์เลื่อนออกไปเกินจริงเท่ากับอายุของผลสุ่ม (สุ่มมาแล้ว 10 วัน = คลาด 10 วัน) แก้เป็นประเมิน ABW ณ วันนี้ก่อน (`abwNow = lat.abw + pdg × วันที่ผ่านมาตั้งแต่สุ่ม`) แล้วค่อยหารด้วย PDG — วันที่คาดการณ์แม่นขึ้น และไซส์ที่โชว่ว่า "ตอนนี้" ก็ตรงกับความจริงมากขึ้นด้วย

ทดสอบด้วยข้อมูลจำลอง 3 บ่อ: บ่อถึงเกณฑ์แล้วโชว์ไซส์ปัจจุบัน 18.2 ก. (55 ตัว/กก.) ✓ บ่อยังไม่ถึงโชว์ทั้งไซส์คาด ณ วันนั้นและไซส์ตอนนี้ ✓ ตรวจเลขด้วยมือแล้วตรง (targetAbw = 13.0 × 24,000/5,330 = 58.5 ก.) ✓ บ่อสุ่มครั้งเดียวไม่แสดงบรรทัดไซส์คาด (คำนวณไม่ได้ ไม่ขึ้น NaN) ✓ ตารางปฏิทินมีคอลัมน์ไซส์ครบทุกแถว ✓ ไม่มี console error — sync ไฟล์แล้ว

### 132. หน้ารายละเอียดบ่อ: ทำทุกการ์ดให้พับเก็บได้ (ปิดหมดตอนเปิดหน้า)

พี่สั่งว่า "ต้องการปุ่ม dropdown ของทุกหัวข้อที่มี เพื่อไม่ให้หน้านั้นรกและเลื่อนลงเยอะเกินไป หากต้องการดูข้อมูลอะไร ผู้ใช้กดปุ่ม dropdown ของข้อมูลนั้นเอง" — ต่อยอดจาก entry #128 ที่ทำ dropdown ให้เฉพาะการ์ดต้นทุน

**ฟังก์ชันกลางใหม่ 2 ตัว** (วางข้าง `pdToggleCostDetail`):
- `pdCard(key,title,bodyHtml,extraCls)` — สร้างการ์ดพับได้: หัวข้อเป็นแถบกดได้ (`.pd-col-head`) + ลูกศร `▾` ในวงกลม, เนื้อหาอยู่ใน `.pd-col-body` ที่ซ่อนไว้
- `pdToggleCard(key)` — สลับกาง/ยุบ + จำสถานะใน `_pdOpenCards` (Set)

**แปลงการ์ดทั้งหมด 15 ใบ** ให้ใช้ `pdCard()`: ต้นทุนสะสม, ความเสี่ยง, คุณภาพน้ำล่าสุด, ประวัติค่าน้ำ, อาหารล่าสุด, สมุดประวัติอาหาร, เวชภัณฑ์, ไซส์ล่าสุด, เปรียบเทียบ Biomass, เทียบรอบก่อน, การจัดการน้ำ และกราฟ 4 ใบ — **ปิดทั้งหมดตอนเปิดหน้า** ส่วนที่ยังโชว่ตลอดคือหัวบ่อ/อายุ/แจ้งเตือน/เกจวัด/KPI hero (ไม่มีหัวข้อ เป็นสรุปที่ต้องเห็นทันที)

**3 เรื่องที่ต้องแก้พ่วง เพราะการพับเก็บทำให้พัง:**
1. **กราฟ Chart.js ขนาดเป็น 0** — canvas ที่ถูกสร้างตอนอยู่ใน `display:none` จะได้ขนาด 0 พอกางออกมาจะเป็นกราฟเปล่า แก้โดยสั่ง `chart.resize()` ทุกตัวใน `_pdCharts` ตอนกางการ์ด (ทดสอบแล้วได้ขนาดจริง 434×200)
2. **หัวข้อซ้ำ 2 ชั้น** — `renderBioCompare()`/`renderCropBenchmark()` เขียน `card-t` ของตัวเองลง container (4 จุด) พอมี wrapper ให้หัวข้อแล้วจะซ้ำ → ลบออก ส่วนหัวข้อ dynamic ของ benchmark ("บ่อ X · DOC = N วัน") ย้ายไปเป็น `card-sub` ในเนื้อหาแทน
3. **fallback ของกราฟ** — 3 จุดที่เขียน `parentElement.innerHTML='<div class="card-t">...ยังไม่มีข้อมูลพอ'` ตอนไม่มีข้อมูล ก็จะยัดหัวข้อซ้ำเข้าไปใน body → ตัดเหลือแค่ข้อความ

**จำสถานะกาง/ยุบข้าม re-render** — `_pdOpenCards` เป็น Set ระดับโมดูล ไม่ใช่ state ใน DOM เพราะหน้านี้ถูก re-render ทั้งก้อนบ่อย (กดแก้/ลบในตารางประวัติ, เปลี่ยนตัวกรองเวชภัณฑ์) ถ้าไม่จำไว้การ์ดที่เพิ่งกางจะยุบกลับเองทุกครั้ง

ทดสอบผ่าน local server: การ์ด 15 ใบ ปิดหมดตอนโหลด ✓ กดกาง/ยุบทำงานถูก ลูกศรหมุน ✓ กราฟได้ขนาดจริงหลังกาง ✓ เนื้อหาที่เขียนโดยฟังก์ชันแยก (ตารางประวัติน้ำ/อาหาร/เวชภัณฑ์, fc-grid, bio, benchmark) มีข้อมูลครบแม้การ์ดปิดอยู่ ✓ re-render แล้วการ์ดที่กางไว้ยังกางอยู่ การ์ดอื่นยังปิด ✓ บ่อสถานะ "เตรียมบ่อ" ไม่มีการ์ดความเสี่ยง (13 ใบ) ✓ ไม่มีหัวข้อซ้ำ ✓ ไม่มี console error — sync ไฟล์แล้ว

### 131. 🔴 ต้นทุนสต็อก/เวชภัณฑ์: เริ่มนับตั้งแต่ช่วงเตรียมบ่อ (ย้อน 60 วันก่อนลงกุ้ง) ไม่ใช่วันลงกุ้ง

พี่แจ้งว่า "ต้นทุนสะสมของแต่ละบ่อ ไม่จำเป็นต้องเริ่มนับสต็อกจากวันที่ลงกุ้ง เพราะมีการเตรียมบ่อก่อนหน้า ในการเตรียมบ่อมีการเบิกสินค้าล่วงหน้า เช่นบ่อ A.1 ลงกุ้งวันที่ 23/5/69 แต่มีการเบิกของไปใช้ตั้งแต่ 21/4/69"

**บั๊กจริง**: ทุกจุดที่คิดต้นทุนสต็อกใช้ `p.startDate` (วันลงกุ้ง) เป็นวันเริ่มช่วง ทำให้ของที่เบิกไปเตรียมบ่อ — โซดาไฟ/คอปเปอร์ซัลเฟต/ไดโครวอส/คลอรีน ซึ่งตาม `PREP_STEPS` เบิกล่วงหน้าได้ถึง 60 วัน — **ตกหล่นจากต้นทุนทั้งหมด** ต้นทุนบ่อจึงต่ำกว่าจริงมาตลอด

**ฟังก์ชันกลางใหม่ `pondCostStartDate(p)`** (วางไว้ข้าง `getStockItemsForPond` กับ building block ต้นทุนตัวอื่น):
- คืนวันที่ย้อนหลัง `POND_PREP_LOOKBACK_DAYS = 60` วันจาก `p.startDate` (ตรงกับขั้น "ตากบ่อ" ที่เป็นขั้นแรกสุดใน `PREP_STEPS` = -60 วัน)
- **กันนับซ้อนรอบก่อน**: ถ้าบ่อมี `cropHistory` ที่ปิดไปแล้วก่อนวันลงกุ้งรอบนี้ จะไม่ย้อนเลยวันปิดรอบก่อน (ใช้ `closeDate + 1` เป็นพื้น) ไม่งั้นต้นทุนปลายรอบเก่าจะถูกดึงมานับซ้ำในรอบใหม่

**แก้ 8 จุดที่เรียกใช้** ให้ใช้ฟังก์ชันกลางแทน `p.startDate`: `pondRunningCost()`, การ์ดต้นทุนหน้ารายละเอียดบ่อ (stock + shared), `openCropModal()` auto-fill (stockGroups + sharedCost), `ceoTotalCost()`, และ CEO sheet (stock + shared)

**ขอบเขต — พี่เลือกเองว่าเอาแค่สต็อก/เวชภัณฑ์**: ค่าแรง/ค่าไฟยังนับจาก `p.startDate` เหมือนเดิม เพราะสูตรหารต่อบ่อ (`activePondCountOnDate`) นับเฉพาะบ่อที่ลงกุ้งแล้ว ถ้าขยายช่วงย้อนหลังด้วยจะทำให้ส่วนแบ่งของบ่ออื่นที่กำลังเลี้ยงอยู่ลดลงตามไปด้วย (กระทบตัวเลขบ่ออื่นโดยไม่ตั้งใจ)

เพิ่มบรรทัดอธิบายในการ์ดต้นทุน (ในส่วน dropdown รายละเอียด) บอกว่าสต็อกนับตั้งแต่วันไหน และค่าแรง/ค่าไฟนับจากวันลงกุ้ง เพื่อให้เห็นชัดว่าทำไมตัวเลขเปลี่ยน

ทดสอบด้วยเคสจริงของพี่ (ลงกุ้ง 23/5/69, เบิกโซดาไฟ 21/4/69): วันเริ่มนับ = 24 มี.ค. 69 ✓ ต้นทุนรวม 6,200 (แบบเดิมได้แค่ 1,200 — ตกหล่นของเตรียมบ่อ 5,000) ✓ ตัวกันซ้อนรอบก่อน: ใส่ `cropHistory` ปิด 1/5/69 → วันเริ่มนับขยับเป็น 2/5/69 ไม่ย้อนไปถึง 21/4 ที่เป็นของรอบเก่า ✓ การ์ดหน้าบ่อแสดงบรรทัดอธิบายถูกต้อง ไม่มี console error — sync ไฟล์แล้ว

**หมายเหตุ**: รอบที่ปิดไปแล้วไม่กระทบ (ตัวเลขถูก freeze ไว้ใน `cropHistory` ตอนปิดรอบ) — มีผลกับรอบที่กำลังเลี้ยงอยู่และรอบที่จะปิดหลังจากนี้เท่านั้น

### 130. การ์ดเวชภัณฑ์ในหน้ารายละเอียดบ่อ: เพิ่มตัวกรองหมวด + เลือกช่วงวันได้ (เดิม entry #129)

พี่ต้องการให้การ์ด "เวชภัณฑ์ที่ใช้ล่าสุด" ที่เพิ่งทำ (entry #129, fix ที่ 7 วันตายตัว) มี "ปุ่ม dropdown เพื่อแยกหมวดหมวดเวชพันณ์ และเลือกวันที่ได้ คล้ายกับหน้าของสต็อก"

**เพิ่มตัวกรอง 2 ชุด** ในการ์ดเดิม:
1. **ช่วงวัน** — ใช้ chip แบบเดียวกับประวัติค่าน้ำ/อาหาร (`pdHistPeriodChips` pattern) แต่สร้างฟังก์ชันแยก `pdMedPeriodChips()` เพราะมีตัวเลือกต่างกัน (7/14/30 วัน/ทั้งหมด — ของเดิมมีแค่ 14/30/ทั้งหมด) ตัวแปร state ใหม่ `_pdMedPeriod` (default 7)
2. **หมวดเวชภัณฑ์** — `<select id="pd-med-cat">` ตัวเลือกตรงกับหน้าสต็อก (ยากิน/ยาลงน้ำ/จุลินทรีย์กิน/จุลินทรีย์ลงน้ำ) state ใหม่ `_pdMedCat`

**แก้ `renderPdMedHistory(p)`**: คำนวณวันเริ่มต้นจาก `_pdMedPeriod` (ถ้า `'all'` ใช้ `p.startDate`) แล้วกรองต่อด้วย `_pdMedCat` ถ้าเลือกไว้ (ยังใช้ `getStockItemsForPond()` ตัวเดิม ไม่สร้าง query คู่ขนาน) — sync ค่า `<select>` กับ `_pdMedCat` ทุกครั้งที่ render เพราะการ์ดนี้ถูกสร้างใหม่ทั้งก้อนทุกครั้งที่เปิดหน้าบ่อ (ไม่งั้น dropdown จะรีเซ็ตกลับ "ทุกหมวด" ทั้งที่ state ยังจำค่าเดิมอยู่ ทำให้ตัวกรองที่แสดงกับข้อมูลที่กรองจริงไม่ตรงกัน)

ทดสอบผ่าน local server: default 7 วันโชว์ถูกต้อง ✓ สลับเป็น 30 วันเห็นรายการเพิ่ม ✓ กรองหมวด chemWater เหลือเฉพาะรายการที่ตรง ✓ กด "ทั้งหมด" ดึงย้อนถึงวันเริ่มบ่อ ✓ เปิดหน้าบ่อซ้ำ (จำลองสลับบ่อไปกลับ) ตัวกรองทั้ง 2 ยัง sync ตรงกับ state เดิม ✓ ไม่มี console error — sync ไฟล์แล้ว

### 129. หน้ารายละเอียดบ่อ: การ์ดเวชภัณฑ์ที่ใช้ล่าสุด 7 วัน

พี่ต้องการดู "เวชพันฑ์ที่ใช้ลงบ่อนั้นๆย้อนหลัง 7 วัน โดยไม่ต้องไปดูที่หน้าข้อมูลประวัติ สต็อก" ในหน้ารายละเอียดบ่อ

**ฟังก์ชันใหม่ `renderPdMedHistory(p)`** — เรียก `getStockItemsForPond(p.id, from, today())` (ฟังก์ชันเดิมที่ใช้คิดต้นทุนสะสมของบ่ออยู่แล้ว ไม่สร้าง query คู่ขนาน) โดย `from` = วันนี้ย้อนหลัง 7 วัน แล้วกรองเฉพาะหมวดเวชภัณฑ์ผ่าน const `PD_MED_CATS=['chemOral','chemWater','probioOral','probioWater','chem']` (ยากิน/ยาลงน้ำ/จุลินทรีย์กิน/จุลินทรีย์ลงน้ำ/หมวด "ยา/สารเคมี (เดิม)") — ไม่รวมอาหาร/ปูน/อุปกรณ์นอก/ลูกกุ้ง

**การ์ดใหม่** "💊 เวชภัณฑ์ที่ใช้ล่าสุด (7 วัน)" วางไว้ก่อนการ์ด "📏 ไซส์ล่าสุด" แสดงตาราง วันที่/รายการ/หมวด/จำนวน — เป็น read-only (ไม่มีปุ่มแก้ไข/ลบเหมือนประวัติค่าน้ำ/อาหาร เพราะแค่ต้องการดูเฉยๆ ไม่ได้ขอแก้ในหน้านี้) แสดงให้ทุก role เห็นเหมือนกัน (ไม่ใช่ข้อมูลการเงิน ไม่ต้อง gate ด้วย `APP_ROLE==='admin'` เหมือนการ์ดต้นทุนสะสม)

ทดสอบผ่าน local server: seed ข้อมูล 4 รายการ (1 รายการเข้าเงื่อนไขถูกต้อง, 1 รายการเกิน 7 วัน, 1 รายการหมวดอาหาร, 1 รายการของบ่ออื่น) → แสดงเฉพาะรายการที่ตรงเงื่อนไขเท่านั้น ✓ role staff เห็นการ์ดนี้ปกติ ✓ กรณีไม่มีข้อมูลขึ้นข้อความ "ยังไม่มีการใช้เวชภัณฑ์ใน 7 วันที่ผ่านมา" ✓ ไม่มี console error — sync ไฟล์แล้ว

### 128. หน้ารายละเอียดบ่อ: การ์ด "ต้นทุนสะสม" ทำเป็น dropdown ย่อรายละเอียดแยกหมวด

พี่บอกว่าการ์ดต้นทุนสะสมในหน้ารายละเอียดบ่อ "ข้อมูลที่โชว์มีเยอะเกินไป เลื่อนหน้าจอเยอะเกิน" — ให้เหลือแค่ 2 บรรทัดสรุปโชว์ตลอด (รวมต้นทุนสะสมของบ่อนี้ + ต้นทุน/กก. ประมาณ) ส่วนรายละเอียดแยกหมวด (อาหาร/ยา/ปูน/อุปกรณ์นอก/ลูกกุ้ง/ค่าแรง/ค่าไฟ พร้อมรายการย่อยแต่ละหมวด) ให้ซ่อนไว้หลังปุ่ม dropdown

**แก้ `renderPondDetail()`** ส่วนสร้าง `costCardHtml`: เพิ่มแถบ "ดูรายละเอียดแยกหมวด ▾" คั่นระหว่าง 2 บรรทัดสรุปกับส่วนรายละเอียด แล้วห่อคำอธิบายวิธีคิด + `pdCatRowsHtml` (breakdown ทุกหมวด) ไว้ใน `<div class="ceo-cat-items" id="pd-cost-detail">` ที่ซ่อนโดย default (ใช้ CSS class `.ceo-cat-items`/`.show` ที่มีอยู่แล้วจาก entry ก่อนหน้า — ไม่สร้างสไตล์ใหม่)

**ฟังก์ชันใหม่ `pdToggleCostDetail()`** — toggle class `show`/`open` บน `#pd-cost-detail`/`#pd-cost-detail-caret` เหมือนรูปแบบเดียวกับ `pdToggleCatItems()` ที่ใช้ toggle รายการย่อยในแต่ละหมวดอยู่แล้ว (ปุ่มย่อยรายหมวดข้างในยังทำงานเป็นอิสระเหมือนเดิม กดดูรายละเอียดหมวดใหญ่แล้วค่อยกดดูรายการย่อยแต่ละหมวดได้อีกที)

ทดสอบผ่าน local server: เปิดหน้ารายละเอียดบ่อ (admin) → เห็นแค่ 2 บรรทัดสรุปตอนแรก ✓ กดปุ่ม "ดูรายละเอียดแยกหมวด" → breakdown โผล่มา caret หมุน ✓ กดซ้ำ → ยุบกลับเหมือนเดิม ✓ ไม่มี console error — sync ไฟล์แล้ว

### 127. ปุ่มล้างรายการเก่า "อุปกรณ์นอก" ที่เคยผูกกับกระเป๋าเงินสดเสมียน

พี่สั่งว่า "ลบข้อมูลแชร์ทุกบ่อที่มาจากรายรับรายจ่างของเสมียนออกให้หมด เพราะ ไม่ต้องการนำมารวมกับ อุปกรณ์นอก" — ต่อยอดจาก entry #125/#126 ที่ตัดการผูกออกแล้วสำหรับรายการ**ใหม่** แต่รายการ**เก่า** (บันทึกไว้ก่อนหน้านั้น ตอนที่ `saveClerkCash()` ยังสร้าง `D.inventoryLog` ให้อัตโนมัติ) ยังค้างอยู่ในต้นทุน "อุปกรณ์นอก" — ตอนนั้นเลือกปล่อยไว้เพื่อไม่ให้ตัวเลขรอบที่ปิดไปแล้วเปลี่ยน แต่พี่ยืนยันชัดเจนแล้วว่าต้องการให้ลบออกให้หมด

**ฟังก์ชันใหม่ `cleanupClerkCashSharedLinks()`** (admin-only) — หาทุกรายการใน `D.clerkCash` ที่ยังมี `linkedLogId` ค้างอยู่ ลบ record ที่ผูกไว้ใน `D.inventoryLog` ออก (เข้า `D.deletedIds` เพื่อ sync ลบข้าม device ให้ถูกต้อง) แล้วลบ field `linkedLogId` ออกจาก `D.clerkCash` entry — **ตัว `D.clerkCash` entry เองไม่ถูกลบ** ประวัติเงินสดเสมียนยังอยู่ครบ แค่ตัดการผูกกับอุปกรณ์นอกออก มี `confirm()` ก่อนทำและแจ้งจำนวนที่ล้างสำเร็จหลังทำ

**ปุ่ม** `🧹 ล้างรายการเก่าที่เคยผูกกับ "อุปกรณ์นอก" (N รายการ)` วางไว้ในการ์ดบนสุดของแท็บ "รายรับ-รายจ่ายเงินสด (เสมียน)" — ซ่อนอัตโนมัติเมื่อไม่มีรายการเหลือให้ล้างแล้ว (นับจาก `linkedLogId` ที่ยังค้างใน `renderClerkCash()`)

ทดสอบ: จำลองรายการเก่า 2 รายการที่ผูกกับ inventoryLog + รายการ inventoryLog อื่นที่ไม่เกี่ยวข้อง (หมวดอาหาร) → กดปุ่ม → ลบเฉพาะ 2 รายการที่ผูกจริง เหลือรายการอื่นไว้ครบ ✓, `D.clerkCash` ยังมี 3 รายการเท่าเดิม (ไม่ลบประวัติ) ✓, `linkedLogId` หายไปจากทุก entry ✓, `D.deletedIds` มี id ที่ลบแล้วครบ ✓, ปุ่มซ่อนอัตโนมัติหลังล้างหมด ✓, กดซ้ำตอนไม่มีอะไรให้ล้างขึ้น alert แจ้งเฉยๆ ไม่พัง ✓ ไม่มี console error — sync ไฟล์แล้ว

**หมายเหตุสำคัญ**: การล้างจริงต้องให้พี่กดปุ่มเองในแอปที่ deploy จริง (login เป็น admin) เพราะเป็นการแก้ไขข้อมูลจริงใน Firebase — ผมแก้ได้แค่โค้ด/ทดสอบกับข้อมูลจำลองในเครื่องเท่านั้น ไม่มีสิทธิ์เข้าถึงฐานข้อมูลจริงของฟาร์ม

### 126. กระเป๋าเงินสดเสมียน: staff เห็นข้อมูลทั้งหมด ไม่กรองเฉพาะรายการของตัวเอง

พี่สั่งว่า "รายรับ รายจ่ายเสมียน staff สามารถดูข้อมูลได้ เพราะ staff เป็นผู้กรอกข้อมูล และถือเงินตรงส่วนนี้" — ตรวจสอบพบว่า `renderClerkCash()` เดิมมี logic กรองข้อมูลสำหรับ role staff (`isStaffScoped`) ที่แสดงเฉพาะรายการที่ `c.by===curUserLabel()` ตรงกับผู้ใช้ที่ login อยู่เท่านั้น ทำให้ staff เห็นข้อมูลไม่ครบ (โดยเฉพาะถ้าคนอื่นเป็นคนกรอกแทนหรือชื่อ `by` ไม่ตรงเป๊ะ) ทั้งที่ staff คือคนถือเงินสดก้อนนี้จริงๆ ควรเห็นภาพรวมทั้งหมด ไม่ใช่แค่ที่ตัวเองบันทึก

**แก้**: ลบเงื่อนไข `isStaffScoped` ออกทั้งหมดใน `renderClerkCash()` — ตอนนี้ทุก role เห็นรายการ ยอดรวม และยอดคงเหลือชุดเดียวกันหมด (เหมือนเดิมกับ `D.palmMoney`/หน้าอื่นๆ ที่ staff เข้าถึงได้) ลบ element `#cash-staffnote` ("🔒 มุมมองพนักงานเห็นเฉพาะรายการที่คุณบันทึกเอง...") ที่ไม่ได้ใช้แล้วออกจาก HTML/JS ด้วย

ถือเป็นส่วนขยายของ entry #125 (ตัดการผูกกระเป๋าเงินสดเสมียนออกจากข้อมูลอื่น) — ตอนนี้หน้านี้เป็นบันทึกเงินสดแบบเปิดให้ทุก role ที่เข้าถึงได้ดูครบเหมือนกันหมด ไม่มีการกรองแยกตามผู้ใช้อีกต่อไป

ทดสอบ: seed ข้อมูล 3 รายการ (คนละ `by`) → login staff → เห็นครบทั้ง 3 รายการ, KPI ตรงกับที่ admin เห็น, ป้าย "มุมมองพนักงาน" หายไปแล้ว, ไม่มี console error — sync ไฟล์แล้ว

### 125. กระเป๋าเงินสดเสมียน: ตัดการผูกกับข้อมูลส่วนอื่นออกทั้งหมด (เก็บเป็นตัวเลขลอยๆ)

พี่สั่งว่า "ไม่ต้องนำเงินไปผูกกับข้อมูลอะไร เป็นตัวเลขลอยๆ เพื่อเก็บข้อมูลช่วงนี้ก่อน เพราะเงินที่เบิกจากส่วนกลาง ทางบัญชีจะทำข้อมูลเป็นข้อมูลอุปกรณ์นอก ใส่ข้อมูลทีหลัง" — เหตุผลคือ **ถ้าแอปผูกให้อัตโนมัติ แล้วบัญชีลงข้อมูลอุปกรณ์นอกซ้ำอีกที จะกลายเป็นนับซ้ำ 2 รอบ** (พี่บอกว่าถ้าการทำงานลงตัวแล้วอาจเปลี่ยนในอนาคต)

**ให้อยู่ที่เดิม** (แท็บย่อย "รายรับ-รายจ่ายเงินสด (เสมียน)" ในหน้าสต็อก) ไม่ย้ายออกมาเป็นหน้าใหม่ — ตัดเฉพาะการผูกออก 2 จุด:
1. `saveClerkCash()` — ลบบล็อกที่สร้าง `D.inventoryLog` (type `shared`, category `equip`) อัตโนมัติตอนบันทึกรายจ่าย ตอนนี้รายจ่ายเสมียน**ไม่เข้าต้นทุนบ่อ/P&L อีกต่อไป**
2. `renderCeo()` — ลบการ์ด KPI "รายได้อื่น (ขายกุ้งสุ่ม/เสีย)" และตัวแปร `otherIncome` ออก

ยังคงเก็บ subtype (`transfer` / `sale`) ไว้เหมือนเดิม เพราะยังมีประโยชน์ในการดูที่มาของเงินภายในหน้ากระเป๋าเอง (KPI ในหน้านั้นเปลี่ยนข้อความจาก "ส่งเข้า CEO แล้ว" → "ไม่ผูกกับข้อมูลส่วนอื่น")

**ข้อมูลเก่า**: รายการที่บันทึกไว้ตอนที่ยังผูกอยู่ ยังมี `linkedLogId` ติดมา — **ปล่อยไว้เหมือนเดิม ไม่ลบย้อนหลัง** (ไม่ให้ตัวเลขต้นทุนของรอบที่ปิดไปแล้วเปลี่ยน) แต่ยังเก็บ cascade ใน `deleteClerkCash()` ไว้ เพื่อว่าถ้าลบรายการเก่าพวกนั้น ตัวที่ผูกไว้ใน `inventoryLog` จะถูกลบตามไปด้วย ไม่เหลือค้างเป็นต้นทุนลอย

ทดสอบ: บันทึกรายจ่าย → `D.inventoryLog` ยังเป็น 0 และไม่มี `linkedLogId` ✓ · บันทึกรายรับ subtype `sale` ได้ปกติ ไม่สร้าง log ✓ · หน้า CEO ไม่มีคำว่า "รายได้อื่น" แล้ว ✓ · ลบรายการเก่าที่มี `linkedLogId` ยัง cascade ลบ inventoryLog ให้ถูกต้อง ✓ — sync ไฟล์แล้ว

### 124. หน้าภาพรวม: ปฏิทินจับกุ้ง & เตือนพาเชียลที่ 4,000 กก./ไร่

พี่กำหนดเกณฑ์เองว่า "ต้องการให้มีการเตือนครั้งแรกคือ yield/ไร่ เท่ากับ 4000 ขึ้นไป ถึงเริ่มการพาเชียล" — ใช้ข้อมูลที่มีอยู่แล้วทั้งหมด ไม่ต้องกรอกอะไรเพิ่ม

**ฟังก์ชันใหม่ `partialHarvestInfo(p)`** — คำนวณ yield/ไร่ ปัจจุบัน = `getLatestBiomass(p) / p.size` แล้วเทียบกับค่าคงที่ `PARTIAL_YIELD_TARGET = 4000`:
- ถึงเกณฑ์แล้ว → `{ready:true}` แสดงการ์ดแดง "🚨 พาเชียลได้แล้ว"
- ยังไม่ถึง + มีผลสุ่ม ≥ 2 ครั้ง → คาดการณ์วันด้วย PDG: **Biomass โตตามสัดส่วน ABW** (สมมติอัตรารอดคงที่) จึงหา ABW เป้าหมายจาก `targetAbw = abw × (4000 × ไร่) / biomass` แล้วแปลงเป็นจำนวนวันด้วย `(targetAbw − abw) / pdg`
- สุ่มครั้งเดียว/ไม่มี PDG → `{noProj:true}` แสดง "รอผลสุ่มครั้งที่ 2" (คาดการณ์ไม่ได้ แต่ยังโชว์ yield ปัจจุบัน)
- ไม่ระบุขนาดบ่อ (ไร่) หรือยังไม่มีผลสุ่มเลย → คืน `null` ข้ามบ่อนั้นไป

**`renderOvPartialHarvest()`** — การ์ดใหม่ `#ov-partial-card` วางถัดจากตาราง "สถานะบ่อทั้งหมด" (เต็มความกว้าง เพราะมีตารางปฏิทิน) มี 3 ส่วน: (1) บ่อที่ถึงเกณฑ์แล้ว เรียงตาม yield มาก→น้อย (2) บ่ออื่นๆ พร้อม progress bar เทียบ 4,000 เรียงตามความใกล้ (บ่อที่คาดการณ์ไม่ได้ไว้ท้ายสุด) (3) ปฏิทิน 60 วันข้างหน้า รวมทั้งวันพาเชียลและวันถึงไซส์เป้าหมาย (จาก `projHarvest()` ที่มีอยู่แล้วแต่ไม่เคยถูกใช้แสดงผลที่ไหนเลย) เรียงตามวันที่ · กรองบ่อด้วย `wqfeedActivePonds()` (ตัดบ่อ prep/ว่าง/ปิดออก) · ถ้าไม่มีบ่อไหนคิดได้เลย การ์ดทั้งใบซ่อน

ทดสอบด้วยข้อมูลจำลอง 5 บ่อครบทุกเคส: บ่อถึงเกณฑ์ (4,853 กก./ไร่ → การ์ดแดง ✓), บ่อใกล้/ไกล (เรียงตามวันถูกต้อง ✓), บ่อสุ่มครั้งเดียว (ขึ้น "รอผลสุ่มครั้งที่ 2" ✓), บ่อไม่ระบุไร่ (ถูกข้าม ✓), ไม่มีบ่อเลย (การ์ดซ่อน ✓), ปฏิทินรวม 2 ประเภทเหตุการณ์เรียงตามวันที่ถูกต้อง ✓ ไม่มี console error — sync ไฟล์แล้ว

**หมายเหตุ — ข้อที่ไม่ได้ทำ**: เดิมเสนอ "ราคาตลาด → กำไรลอยตัว" ด้วย แต่ค้นหาแหล่งข้อมูลแล้วพบว่าไม่มีเว็บไทยเจ้าไหนเปิด API/CORS ให้ดึงอัตโนมัติ (ตลาดไท, kasetprice, กรมการค้าภายใน, กรมประมง เป็น HTML/PDF ล้วน) และราคาตลาดกลางก็ไม่ใช่ราคาหน้าบ่อที่แพรับซื้อจริง — พี่ตัดสินใจไม่ทำ เพราะ "ข้อมูลราคาไม่มี update ทุกวัน"

### 123. หน้าภาพรวม: ตัดโดนัทสถานะบ่อ (ซ้ำซ้อน) + ย่อการ์ดวันพระ/วันโกนเหลือบรรทัดเดียว

จากการวิเคราะห์หน้าภาพรวมรอบก่อน (entry #122) มี 2 ข้อเสนอ "ลด" ที่ยังไม่ได้ทำ — พี่ยืนยันให้ทำทั้งคู่:

**1. ตัดการ์ด "🍩 สถานะบ่อทั้งฟาร์ม" (โดนัท) ออกทั้งหมด** — ข้อมูลซ้ำซ้อนกับ 3 จุดที่มีอยู่แล้ว: KPI การ์ดที่ 1 ("6/27 บ่อ · เตรียมบ่อ 1 · ว่าง 20"), `ovd-sum-strip` (ต้องดู/เฝ้าระวัง/ยังไม่กรอก/ปกติ), แถมกินพื้นที่มากสุดในหน้าแต่ให้ข้อมูลน้อยสุด — ลบ HTML การ์ด `#ov-status-donut`, ฟังก์ชัน `renderOvDonut()` และ `_ovDonutChart` ทั้งหมด, ลบ CSS ที่ไม่ได้ใช้แล้ว

**2. ย่อการ์ด "🌙 วันพระ/วันโกน" เหลือบรรทัดเดียว** — เดิมมี 4 บล็อค (ข้างขึ้นข้างแรม + alert + แถบ 10 วัน + คำอธิบาย) แต่สิ่งที่ actionable จริงมีแค่ "วันโกนถัดไปอีกกี่วัน" → เขียน `renderMoonWanphra()` ใหม่ทั้งหมด ให้เหลือ `<div id="wp-oneline">` บรรทัดเดียว:
  - ถ้าวันนี้เป็นวันโกน → "🧂🧂 วันนี้เป็นวันโกน — อย่าลืมลงแร่ธาตุรวม..."
  - ถ้าพรุ่งนี้เป็นวันโกน → "🧂 พรุ่งนี้เป็นวันโกน — เตรียมแร่ธาตุรวมไว้ล่วงหน้า..."
  - ปกติ → "🧂 วันโกนถัดไป: อีก N วัน (DD/MM)" (ค้นหาล่วงหน้าไม่เกิน 60 วันจากตาราง `WANPHRA_TABLE`)
  ลบ HTML `#moon-strip`/`#wp-alert`/`#wp-strip`, ลบ CSS `.moon-strip`/`.moon-ico`/`.moon-txt`/`.wp-strip`/`.wp-day*` และ const `WD_TH` ที่ไม่ได้ใช้แล้ว (หมายเหตุ: `moonInfo()` ยังใช้อยู่ในตรรกะ `pondRiskFeed()` ไม่ได้แตะ)

ทดสอบผ่าน local server: `node --check` ผ่านทั้ง 2 script block, เรียก `renderOv()` ตรง console แล้วเช็ค DOM ยืนยัน `#ov-status-donut`/`#moon-strip`/`#wp-strip`/`#wp-alert` ถูกลบออกจริง ไม่เหลือ element เก่า, `#wp-oneline` แสดงผลถูกต้อง ("🧂 วันโกนถัดไป: อีก 6 วัน (5/8)" ตรงกับตารางวันพระปี 2569), ไม่มี console error — sync ไฟล์แล้ว

### 122. หน้าภาพรวม: เพิ่มการ์ด "📦 สต็อกใกล้หมด"

พี่ให้วิเคราะห์หน้าภาพรวม (role admin เจ้าของฟาร์ม/ผู้บริหารคนเดียว) ว่าขาดอะไร ควรเพิ่ม/ลดอะไร — วิเคราะห์แล้วเสนอ 4 ข้อ (ราคาตลาด/กำไรลอยตัว, ปฏิทินจับกุ้งคาดการณ์, แถบสรุปการเงินรวมศูนย์, เตือนสต็อกใกล้หมด) พี่เลือกทำข้อ 4 ก่อน — ทำ mockup แล้วพี่ยืนยัน "ok เริ่มทำได้"

**สิ่งที่ทำ**: เพิ่มการ์ด "📦 สต็อกใกล้หมด" ในแถบข้าง (`.ov-side`) ของหน้าภาพรวม ใช้เงื่อนไขเดียวกับ `renderStockWarn()` ที่มีอยู่แล้วในหน้าสต็อกทุกประการ (`i.minQty>0 && i.qty<=i.minQty`) ไม่มีเงื่อนไข/ข้อมูลใหม่ แค่ดึงมาแสดงซ้ำที่หน้าภาพรวมด้วย — แต่ละรายการโชว่ชื่อ/เหลือเท่าไหร่/ขั้นต่ำเท่าไหร่ พื้นแดงเตือน มีลิงก์ "ดูทั้งหมด" กดแล้วพาไปหน้าสต็อกทันที (`document.getElementById('tab-stock').click()`) — **ถ้าไม่มีของใกล้หมด การ์ดทั้งใบจะไม่แสดงเลย** (`display:none`) กันหน้าภาพรวมรกเมื่อของครบปกติ

ฟังก์ชันใหม่: `renderOvStockWarn()` เรียกจาก `renderOv()` ทุกครั้งที่เปิดหน้าภาพรวม

ทดสอบผ่าน local server: ไม่มี `D.inventoryItems` เลย → การ์ดซ่อนถูกต้อง, เพิ่มสินค้าทดสอบ 3 รายการ (2 รายการต่ำกว่าขั้นต่ำ + 1 รายการปกติ) → การ์ดโชว่แค่ 2 รายการที่ต่ำกว่าขั้นต่ำถูกต้อง (รายการปกติไม่ติดมาด้วย), กดลิงก์ "ดูทั้งหมด" พาไปหน้าสต็อกถูกต้อง ไม่มี console error — sync ไฟล์แล้ว

### 121. หน้าภาพรวม: ซ่อนบ่อ "เตรียมบ่อ" ที่ยังไม่มีข้อมูลเตรียมจริง (ไม่มีวันลงกุ้ง/ยังไม่ได้เริ่มเตรียม) ออกจากการ์ดโชว์

พี่แจ้งว่าข้อมูลในหน้าภาพรวมเยอะเกินไป — บ่อสถานะ "เตรียมบ่อ" (age<=0 หรือยังไม่ลงกุ้ง) บางบ่อยังไม่มีข้อมูลอะไรเลย (ไม่เคยกดเริ่มเตรียม ไม่มี prepChecklist) แต่ก็ยังโผล่เป็นการ์ดว่างๆ ในหน้าภาพรวม ไม่ต้องการให้โชว์

**สาเหตุ**: `renderOv()` เดิมนับทุกบ่อที่ `pondStatusOv(p)==='prep'` เป็น `prepM` หมด ไม่ว่าจะมี `p.prepChecklist` จริงหรือไม่ — ต่างจากหน้า "เตรียมบ่อ" เอง (`initPrep()`) ที่กรองเฉพาะบ่อที่มี `prepChecklist` และมีข้อมูลจริง (`stockDate` หรือ `steps` อย่างน้อย 1 รายการ) เท่านั้นถึงจะ auto-แสดง — สองหน้าใช้เกณฑ์ไม่ตรงกัน

**แก้แล้ว**: ปรับ `renderOv()` ให้ `prepM` ใช้เกณฑ์เดียวกับ `initPrep()` — กรองเฉพาะบ่อที่มี `prepChecklist` และมีข้อมูลจริง (`stockDate||Object.keys(steps).length>0`) เท่านั้นถึงจะแสดงเป็นการ์ด "เตรียมบ่อ" บ่อที่สถานะ prep แต่ยังไม่แตะอะไรเลย (`idleNotPrepped`) จะถูกนับรวมเข้า "ว่าง/พักบ่อ" แทน (ทั้ง KPI bar, การ์ดรายการ, และโดนัทสถานะบ่อ) — ตัวเลขยังรวมกันครบ ไม่หายไปไหน แค่ย้ายหมวดให้ตรงความเป็นจริงมากขึ้น (บ่อที่ยังไม่ได้เริ่มทำอะไรเลย = ว่าง ไม่ใช่ "กำลังเตรียม")

ทดสอบผ่าน local server: สร้างสถานการณ์บ่อ 2 ใบสถานะ prep — ใบหนึ่งไม่มี prepChecklist เลย อีกใบมี steps+stockDate จริง → หน้าภาพรวมโชว่การ์ด "เตรียมบ่อ" แค่ใบที่มีข้อมูลจริง (1 การ์ด) ใบที่ไม่มีข้อมูลถูกนับเข้า "ว่าง/พักบ่อ" แทน ตัวเลข KPI bar/โดนัทตรงกับจำนวนการ์ดที่แสดงจริง ไม่มี console error — sync ไฟล์แล้ว

### 120. ปรับหน้าบ่อ: ย้ายวันพระ/วันโกนไปหน้าภาพรวม + ลบ "สิ่งที่ต้องทำวันนี้" + เพิ่มสมุดประวัติค่าน้ำ/อาหารในหน้าบ่อ

พี่แจ้ง 3 อย่าง: (1) ย้ายวันพระ/วันโกนออกจากหน้าบ่อ (ซ้ำกันทุกบ่อ) ไปไว้หน้าภาพรวมแทน (2) ลบ "สิ่งที่ต้องทำวันนี้" ในหน้าบ่อทิ้งทั้งหมด ไม่ได้ใช้ (3) เพิ่มประวัติค่าน้ำ/อาหารย้อนหลังในหน้าบ่อ ไม่ต้องสลับหน้าไปหน้า "จัดการน้ำ"/"อาหาร & FCR" — ทำ mockup แล้วพี่ยืนยัน

**1. ย้ายวันพระ/วันโกน**: `renderMoonWanphra()` (populate `#moon-strip`/`#wp-strip`/`#wp-alert`) เดิมถูกเรียกจาก `renderFarmCard(p)` ทุกครั้งที่เปิดหน้าบ่อ (ข้อมูลไม่ผูกกับ `p` เลย เป็นแค่ปฏิทินวันนี้ของฟาร์ม) — ย้าย HTML การ์ด "🌙 วันพระ/วันโกน" ไปไว้ใน `.ov-side` ของหน้าภาพรวม และย้ายจุดเรียก `renderMoonWanphra()` ไปที่ `renderOv()` แทน เรียกครั้งเดียวตอนโหลดภาพรวม ไม่ต้องซ้ำทุกบ่อ

**2. ลบ "สิ่งที่ต้องทำวันนี้"**: ลบ HTML (`fc-act-progress`/`fc-act-bar`/`fc-act-list`) และ JS ที่เกี่ยวข้องทั้งหมดออกจาก `renderFarmCard()` — รวมถึงลบ `FC_ACTIONS` const (คำแนะนำ 12 รายการต่อหมวดความเสี่ยง), `_fcDoneSet`, `fcToggleAction()` ที่กลายเป็นโค้ดตายไปด้วย (ไม่ใช้ที่ไหนอีกแล้ว) — คงเหลือแค่ `fc-grid` (การ์ดความเสี่ยง 5 หมวด) กับ `fcSetFilter()` (ไฮไลท์หมวดที่เลือก) ไว้เหมือนเดิม

**3. เพิ่มสมุดประวัติในหน้าบ่อ**: เพิ่ม 2 การ์ดใหม่ต่อจากการ์ด "ล่าสุด" เดิม — "📖 ประวัติค่าน้ำย้อนหลัง" กับ "📖 สมุดประวัติการให้อาหาร" — ดึงข้อมูลชุดเดียวกับหน้า "จัดการน้ำ"/"อาหาร & FCR" มาแสดง (ไม่สร้างข้อมูลซ้ำ) มีชิปเลือกช่วงเวลา 14/30 วัน/ทั้งหมด (ใช้ `.trial-chip` ที่มีอยู่แล้ว) แก้ไข/ลบได้จากตรงนี้เลย:
- ฟังก์ชันใหม่: `renderPdWqHistory(p)`/`renderPdFeedHistory(p)`, `setPdWqPeriod()`/`setPdFeedPeriod()`, `pdEditWQ()`/`pdDeleteWQ()` (ห่อ `editWQ`/`deleteWQ` เดิม + คลิกไปแท็บ "รายวัน" ให้ก่อนถ้าจำเป็น), `pdEditFeed()`/`pdDeleteFeedEntry()` (เขียนใหม่ตามรูปแบบการลบใน `saveFeedGrid()` เดิม เรียก `reverseFeedStockDeduction()` ก่อนลบเพื่อคืนสต็อกที่เคยตัดไว้ให้ถูกต้อง)
- เพิ่มตัวแปร global `_pdCurPondId` (set ใน `renderPondDetail()`) ให้ปุ่มเปลี่ยนช่วงเวลารู้ว่ากำลังดูบ่อไหนอยู่ (เดิมตั้งใจใช้ `_fcCurPond` แต่พบว่าตัวแปรนั้นไม่ได้ set เมื่อบ่ออยู่สถานะ "เตรียมบ่อ" เลยเปลี่ยนมาใช้ตัวแปรเฉพาะที่ set ทุกครั้งแทน)

ทดสอบผ่าน local server: หน้าภาพรวมโชว่การ์ดวันพระ/วันโกนถูกต้อง (พระจันทร์เต็มวันนี้ตามสูตร), หน้าบ่อไม่มี "สิ่งที่ต้องทำวันนี้"/moon-strip ซ้ำแล้ว, การ์ดความเสี่ยง 5 หมวดยังทำงานปกติ, เพิ่มข้อมูลทดสอบแล้วตารางประวัติค่าน้ำ/อาหารแสดงถูกต้องครบทุกคอลัมน์, สลับช่วงเวลา 14/30/ทั้งหมดทำงานได้ไม่มี error, ลบรายการอาหารทดสอบสำเร็จ (คืนสต็อกถูกต้อง) ตารางอัปเดตทันที ไม่มี console error — sync ไฟล์แล้ว

### 119. แยกประเภทรายรับเสมียนเป็น "เบิกจากส่วนกลาง" vs "ขายกุ้งสุ่ม/กุ้งเสีย" — ส่งรายได้จริงเข้า CEO อัตโนมัติ ไม่ทับซ้อนกับรายได้ปิดรอบ

พี่ให้วิเคราะห์ว่ารายรับของเสมียน (มีทั้งเบิกจากส่วนกลาง และขายกุ้งสุ่ม/กุ้งเสีย) ควรนำไปคำนวณยังไงไม่ให้ทับซ้อนกัน — วิเคราะห์พบว่าตอนนี้ทั้งสองแบบถูกเก็บรวมเป็น `type:'in'` ก้อนเดียว ทำให้ "ขายกุ้งสุ่ม/เสีย" (รายได้จริงของฟาร์ม) หายไปจากภาพรวม CEO ทั้งหมด ในขณะที่ "เบิกจากส่วนกลาง" (แค่ย้ายเงินภายใน) ถูกนับถูกต้องอยู่แล้ว — เสนอ 2 ทางเลือก พี่เลือกทางเลือก A (แยกเป็น "รายได้อื่น" ระดับฟาร์ม ไม่ผูกกับรายได้ปิดรอบรายบ่อ) ทำ mockup แล้วพี่ยืนยันเพิ่มเติมว่าเงินขายกุ้งสุ่ม/เสียก็เข้ากระเป๋าเสมียนใช้จ่ายในฟาร์มได้เหมือนกัน (เบิกจากส่วนกลางเป็นแค่ตัวเติมเมื่อเงินสดไม่พอ) — ตรงกับดีไซน์ที่ mockup ไว้แล้ว

**Data model**: เพิ่ม field ใหม่บนเรคคอร์ด `D.clerkCash[]` เดิม (ไม่สร้าง array ใหม่ — เบากว่า pattern ผูกลิงก์ข้าม array ที่เคยใช้):
- `subtype:'transfer'|'sale'` (เฉพาะ `type==='in'`, ไม่มี field นี้ = ของเก่า/ค่าเริ่มต้น ถือเป็น `'transfer'` เพื่อ backward-compat)
- `pondId?`, `weightKg?` (เฉพาะ subtype `'sale'`, ไม่บังคับกรอก)

**กลไก**: ทั้ง 2 subtype ยังคงบวกเข้า "ยอดคงเหลือกระเป๋าเสมียน" เหมือนเดิมทุกอย่าง (ไม่เปลี่ยนพฤติกรรมเดิม) — ต่างกันแค่ subtype `'sale'` จะถูกกรองนับแยกเป็น "รายได้อื่นสะสม" เพิ่มเติม ไม่แตะ `harvestResult.revenue`/ช่อง "รายได้รวม" ตอนปิดรอบเลยแม้แต่จุดเดียว จึงไม่มีทางนับซ้อนกับรายได้ปิดรอบรายบ่อ

**UI ที่เพิ่ม**:
- ฟอร์มบันทึกรายรับ (`#skp-cash`): เพิ่มชิปเลือกประเภทย่อย "🏦 เบิกจากส่วนกลาง" (default) / "🦐 ขายกุ้งสุ่ม/กุ้งเสีย" (โชว์เฉพาะโหมด "รายรับ") — เลือก "ขายกุ้งสุ่ม/เสีย" แล้วโชว์ช่องเสริม "บ่อ" (ไม่บังคับ) + "น้ำหนัก (กก.)" (ไม่บังคับ)
- KPI การ์ดที่ 4 ของหน้าเสมียน เปลี่ยนจาก "จำนวนรายการ" เป็น "รายได้อื่นสะสม (ขายกุ้งสุ่ม/เสีย)"
- ตารางประวัติ: ป้ายแยก 3 สี — เขียว "🏦 เบิกจากส่วนกลาง", ฟ้า "🦐 ขายกุ้งสุ่ม/เสีย" (`.b-info`), แดง "📤 รายจ่าย"
- **แดชบอร์ด CEO** (`renderCeo()`): เพิ่มการ์ด KPI ใหม่ "รายได้อื่น (ขายกุ้งสุ่ม/เสีย)" คำนวณจาก `D.clerkCash` ที่ `type==='in'&&subtype==='sale'` ทั้งหมด (ไม่กรองตามเดือน แสดงยอดสะสมทั้งชีวิต) พร้อมข้อความกำกับ "ไม่รวมในรายได้ปิดรอบ"

ทดสอบผ่าน local server: บันทึกรายรับ 2 แบบ (เบิกส่วนกลาง 1,200 + ขายกุ้งสุ่ม 640 พร้อมน้ำหนัก 8.5 กก.) → ยอดคงเหลือรวม 1,840 ถูกต้อง, KPI "รายได้อื่นสะสม" โชว์ 640 ถูกต้อง, ตารางแสดงป้ายแยกสีถูกต้อง, สลับไปดูแดชบอร์ด CEO เห็นการ์ด "รายได้อื่น (ขายกุ้งสุ่ม/เสีย) ฿640" ถูกต้อง, ทดสอบ entry เก่าที่ไม่มี `subtype` field → แสดงเป็น "เบิกจากส่วนกลาง" ถูกต้อง (backward-compatible) ไม่มี console error — sync ไฟล์แล้ว

### 118. เพิ่มแท็บ "💳 เงินเบิกล่วงหน้า" ในหน้าค่าแรง — ติดตามเงินที่บริษัทสำรองจ่าย (visa/work permit) และการหักคืนรายสัปดาห์

พี่ถามว่าเวลาพนักงานเบิกเงินก้อนล่วงหน้า (เช่น ค่า visa/work permit) แล้วทยอยหักคืนจากค่าแรงรายสัปดาห์ ควรกรอกยอดไหนในช่อง "ยอดจ่ายจริง" ของหน้าพนักงานรายวัน — ตอบว่า**ควรกรอกยอดเต็มก่อนหักเสมอ** เพราะช่องนั้นใช้คำนวณต้นทุนค่าแรง/กก. ที่ต้องสะท้อนต้นทุนแรงงานจริง ไม่ใช่กระแสเงินสดที่จ่ายออกจริง (เงินที่หักคืนไม่ใช่รายจ่ายที่หายไป แต่เป็นการชำระหนี้คืน) — พี่ขอให้เพิ่มฟีเจอร์แยกไว้ติดตามยอดเบิก/หักคืนนี้ต่างหาก ไม่ให้ไปปนกับข้อมูลค่าแรง

**Data model ใหม่**:
- `D.staffAdvances[]` — รายการเบิกแต่ละครั้ง: `{id,name,date,amount,reason,weeklyAmount,by}`
- `D.staffAdvanceRepay[]` — รายการหักคืนแต่ละงวด: `{id,advanceId,date,amount,by}`

**หลักการแยกอิสระ**: หน้านี้เป็นบัญชีลูกหนี้ล้วนๆ **ไม่แตะ `D.staffList`/`dw-amount`(ยอดจ่ายจริงพนักงานรายวัน)/`getLaborCostForPond`/ต้นทุนค่าแรงใดๆ เลยแม้แต่จุดเดียว** — มีแค่กล่องข้อความเตือนในหน้าย้ำหลักการนี้ไว้ให้เห็นชัดเจน

**หน้า `#lp-adv`** (แท็บย่อยที่ 4 ในหน้า "ค่าแรง"): KPI 4 ใบ (พนักงานที่มีหนี้ค้าง/ยอดเบิกคงค้างรวม/คืนแล้วสะสมเดือนนี้/เบิกใหม่สะสมเดือนนี้), ฟอร์มบันทึกเบิกใหม่ (ชื่อ/วันที่/จำนวน/เหตุผล/ยอดหักคืนต่อสัปดาห์ที่ตกลงไว้), ฟอร์มบันทึกหักคืน (เลือกพนักงานจาก dropdown ที่กรองเฉพาะคนมีหนี้ค้าง + โชว์ยอดค้างในตัวเลือก, พรีวิวยอดคงเหลือหลังหักแบบสด), รายการการ์ดต่อพนักงาน (แถบความคืบหน้า, เบิกไป/คืนแล้ว/คงเหลือ, ประมาณเหลืออีกกี่สัปดาห์, ปุ่มดูประวัติหักคืนแบบขยาย/ย่อในการ์ด, badge เปลี่ยนเป็น "✅ หักคืนครบแล้ว" อัตโนมัติเมื่อคงเหลือ=0)

เพิ่มการ validate ห้ามหักคืนเกินยอดที่ค้างอยู่จริง (`saveStaffAdvanceRepay()` เช็คก่อนบันทึก), ลบรายการเบิกจะ cascade ลบประวัติหักคืนทั้งหมดของรายการนั้นไปด้วย (`deleteStaffAdvance()`)

ทดสอบผ่าน local server: เบิก 5,000 บาท หักคืนงวดแรก 500 → เหลือ 4,500 ถูกต้อง (เหลืออีก ~9 สัปดาห์), ทดสอบหักคืนเกินยอดค้าง → บล็อกพร้อม error message ถูกต้อง, หักคืนงวดที่ 2 ให้ครบ 5,000 → badge เปลี่ยนเป็น "หักคืนครบแล้ว" อัตโนมัติ ดรอปดาวน์เลือกพนักงานไม่มีชื่อนี้แล้ว (ไม่มีหนี้ค้าง), ขยายดูประวัติหักคืนแสดงครบทั้ง 2 งวด, ลบรายการเบิก cascade ลบประวัติหักคืนสำเร็จ ไม่มี console error — sync ไฟล์แล้ว

### 117. 🔴 แก้บั๊ก: CEO ออกจากระบบไม่ได้ (มือถือ) — เพิ่มปุ่ม "ออกจากระบบ" ในแดชบอร์ด CEO โดยตรง

พี่แจ้งว่าหน้า CEO ไม่มีทางออกจากระบบ (logout) ได้

**สาเหตุ**: เจอ 2 ชั้นที่ซ้อนกันจนปิดทางออกทั้งหมดสำหรับ CEO บนมือถือ:
1. `role==='ceo'` จะซ่อน sidebar/top-tabs/bottom-nav ทั้งหมด (`body.role-ceo .side-nav,.tabs-wrap,#bnav{display:none!important}`) แสดงแค่ `#sec-ceo` เต็มจอเท่านั้น (ตามที่บันทึกไว้ตั้งแต่ข้อ 108)
2. ปุ่ม "ออกจากระบบ" หลักอยู่ใน header (`.hdr-logout-btn`) แต่มี CSS `@media(max-width:768px){.hdr-logout-btn{display:none!important}}` ซ่อนไว้บนมือถือ แล้วสลับไปโชว์ปุ่มสำรอง `.mobile-logout-block` แทน — แต่ปุ่มสำรองนี้ฝังอยู่ในหน้า "จัดการบ่อ" (`sec-manage`) ซึ่งเป็นหน้าปกติที่ต้องผ่าน `goTab()`/เมนูถึงจะเข้าได้ — CEO เข้าไม่ถึงเมนูเลยจากข้อ 1 จึงไม่มีทางเห็นปุ่มนี้เลยบนมือถือ

**แก้แล้ว**: เพิ่มปุ่ม "🚪 ออกจากระบบ" (พื้นแดง) ไว้ใน `.ceo-hdr` ของหน้า CEO โดยตรง (คู่กับปุ่ม "🌴 ตะวันปาล์ม" ที่มีอยู่แล้ว) เรียก `doLogout()` ตัวเดียวกับปุ่มอื่นๆ ในระบบ ไม่ต้องเขียนฟังก์ชันใหม่

ทดสอบผ่าน local server ที่ viewport มือถือ (375×812) ในบทบาท CEO: ปุ่มแสดงผลถูกต้อง กดแล้วเรียก `confirm()` ก่อน `doLogout()` ตามพฤติกรรมเดิม ไม่มี console error — sync ไฟล์แล้ว

### 116. กองทุน: เพิ่มบันทึกการขาย (ถอนเงิน) — เก็บข้อมูลแยกอิสระ ไม่ผูกกับการเงินส่วนอื่นเลยทั้ง 2 กองทุน

ต่อจากข้อ 115 — พี่แจ้งว่ามีการขายกองทุนบางส่วนเพื่อนำเงินมาใช้เป็นค่าใช้จ่ายของสวนปาล์ม ขอเพิ่มปุ่ม/ช่องบันทึกการขาย

**Data model ใหม่**: `D.fundSells[]` — `{id,fund:'palm'|'shrimp',date,units,nav,amount,note,by}` (amount คำนวณจาก `units×nav`)

**กติกาการเชื่อมโยง**: **ไม่ผูกกับข้อมูลการเงินส่วนอื่นเลย ทั้ง 2 กองทุน** — ตอนแรกทำให้ K-USXNDQ-A สร้างรายรับในหน้ารายรับ-รายจ่ายปาล์มอัตโนมัติ แต่พี่ยืนยันว่าให้เก็บแยกไว้ดูเฉยๆ เหมือน K WORLDX จึงถอดกลไกการผูกออกทั้งหมด (ลบ `linkedFundSellId`/`linkedMoneyId` ของสายกองทุน + guard ใน `deletePalmMoney()` ที่เคยเพิ่มไว้) — หมายเหตุ: การผูก `linkedMoneyId` ระหว่างปุ๋ย↔รายจ่ายปาล์ม (ข้อ 111) **ยังคงอยู่ตามเดิม** ไม่เกี่ยวกับการเปลี่ยนครั้งนี้

**สูตรคำนวณ KPI ใหม่** (เปลี่ยนจากเดิมที่ยังไม่รองรับการขาย): ใช้หลัก "total return" นับรวมทั้งเงินที่ขายออกไปแล้วและมูลค่าคงเหลือ ไม่ต้องคำนวณต้นทุนเฉลี่ยต่อ lot ให้ซับซ้อน — `หน่วยที่ถืออยู่ตอนนี้ = หน่วยซื้อสะสม - หน่วยขายสะสม`, `เงินลงทุนสุทธิ = เงินซื้อสะสม - เงินขายออกสะสม`, `มูลค่าปัจจุบัน = หน่วยที่ถืออยู่ × NAVล่าสุด`, `กำไร/ขาดทุนสะสม = (มูลค่าปัจจุบัน + เงินขายออกสะสมทั้งหมด) - เงินซื้อสะสมทั้งหมด` — สูตรนี้ถูกต้องเสมอไม่ว่าจะขายไปกี่ครั้งเพราะเป็นการเทียบกระแสเงินสดเข้า-ออกรวมกับมูลค่าคงเหลือ

เพิ่มการ validate ห้ามขายเกินจำนวนหน่วยที่ถืออยู่จริง (`saveFundSell()` เช็คก่อนบันทึก), อัปเดต `fundUnitsAsOf()`/`fundInvestedAsOf()`→`fundNetInvestedAsOf()` ให้หักลบยอดขายด้วยเวลาคำนวณกราฟย้อนหลัง, เพิ่มตาราง "ประวัติการขาย" คู่กับตารางซื้อ/NAV เดิม

ทดสอบผ่าน local server: ซื้อปาล์ม 45,000@10.4522 → ขาย 2,000 หน่วย@10.72 (=฿21,440) → KPI คำนวณถูกต้องทุกตัว (หน่วยเหลือ 2,305.31, เงินลงทุนสุทธิ 23,560, กำไรสะสม +1,698/+3.77%), ยืนยัน `D.palmMoney` ยังว่าง 0 รายการ (ไม่มีการสร้างรายรับอัตโนมัติแล้ว) และ record การขายไม่มี field `linkedMoneyId`, ทดสอบขายเกินจำนวนที่ถือ → บล็อกพร้อม error message ถูกต้อง, ลบรายการขายได้ปกติไม่กระทบข้อมูลอื่น — sync ไฟล์แล้ว

### 115. เพิ่มแท็บ "📈 กองทุน" ในตะวันปาล์ม — ติดตาม 2 กองทุน (K-USXNDQ-A จากขายปาล์ม, K WORLDX จากขายลูกกุ้ง) แยกอิสระจากกัน

พี่ต้องการบันทึกเงินที่นำไปซื้อกองทุน K-USXNDQ-A (จากรายได้ขายปาล์ม) ถามต่อว่าจะแสดง/อัปเดตกำไรขาดทุนยังไงได้บ้าง — อธิบายว่าแอปเป็น client-side ล้วนไม่มี backend ดึง NAV อัตโนมัติจากเว็บกสิกรไม่ได้ (ติด CORS) แนะนำให้กรอก NAV ด้วยมือเป็นระยะแทน คล้ายหน้า "ราคาวันนี้"/"ค่าไฟฟ้า" — ทำ mockup ให้ดูก่อน พี่ยืนยันแล้วขอเพิ่มกองทุนที่ 2 คือ K WORLDX ซึ่งเงินมาจากการขาย "ใบลูกกุ้ง" (post-larvae) ของฝั่งตะวันฟาร์ม แต่ย้ำว่า **ห้ามนำไปคำนวณรวมกับต้นทุนตะวันฟาร์มเด็ดขาด แค่เก็บข้อมูลแยกไว้ดูเฉยๆ**

**Data model ใหม่** (top-level field ใหม่ lazy-init เหมือนแบบเดิมของโมดูลปาล์ม ไม่ลงทะเบียนใน `normalizeD()`/`LOG_KEYS`):
- `D.fundBuys[]` — รายการซื้อกองทุนแต่ละครั้ง: `{id,fund:'palm'|'shrimp',date,amount,nav,units,by}` (`units` คำนวณจาก `amount/nav` ตอนบันทึก)
- `D.fundNav[]` — บันทึก NAV ที่กรอกเป็นระยะ: `{id,fund:'palm'|'shrimp',date,nav,by}`

ทั้งสองกองทุนใช้ field เดียวกันแต่กรองด้วย `fund` เพื่อแยกข้อมูลเป็นอิสระ 100% — ไม่มีจุดไหนใน `savePalmMoney()`/`saveClerkCash()`/`D.inventoryLog`/ต้นทุนฝั่งกุ้งหรือปาล์มถูกแตะเลยแม้แต่จุดเดียว (คนละ concept กับ pattern การผูกลิงก์ที่เคยใช้ในปุ๋ย/เสมียน — นี่คือบัญชีแยกเดี่ยวๆ ไม่ผูกกับอะไรทั้งสิ้น)

**หน้า `#palmp-fund`** (แท็บย่อยที่ 5 ใน `#sec-palm`): ปุ่มสลับกองทุน (`.fundbtn.on-palm` สีเขียว / `.fundbtn.on-shr` สีน้ำเงิน — เพิ่ม CSS var `--shr/--shrl/--shrd` ใหม่), กล่องข้อความอธิบายเปลี่ยนตามกองทุนที่เลือก (`FUND_META.palm/.shrimp` เก็บ note/สี/คำอธิบายไว้), KPI 4 ใบ (หน่วยสะสม/เงินลงทุนสะสม/มูลค่าปัจจุบัน/กำไร-ขาดทุน), กราฟเส้น Chart.js (มูลค่าพอร์ต vs เงินลงทุนสะสม ณ แต่ละวันที่มีการบันทึก NAV — คำนวณจริงจาก `fundUnitsAsOf()`/`fundInvestedAsOf()` ไม่ใช่ข้อมูลสุ่มแบบ mockup), ฟอร์มบันทึกซื้อ (คำนวณหน่วยที่ได้ = เงิน÷NAV แบบ preview สด), ฟอร์มบันทึก NAV, ตารางประวัติทั้งคู่พร้อมปุ่มลบ

ทดสอบผ่าน local server: บันทึกซื้อกองทุนปาล์ม 2 ครั้ง (45,000@10.4522 + 38,000@9.8760) + NAV ล่าสุด 10.9563 → หน่วยสะสม/เงินลงทุน/มูลค่า/กำไรคำนวณถูกต้องทั้งหมด, สลับไปกองทุนกุ้ง (K WORLDX) → เริ่มจาก 0 ไม่ปนกับข้อมูลปาล์มเลย, บันทึกซื้อ+NAV ฝั่งกุ้งแล้วสลับกลับไปปาล์ม → ข้อมูลปาล์มยังคงเดิมไม่ถูกกระทบ (ยืนยัน isolation ทำงานถูกต้อง), ลบรายการทดสอบทั้งคู่สำเร็จ, staff role มองไม่เห็นเมนูเหมือนเดิม (สืบทอด gate จากหน้าตะวันปาล์ม) — sync ไฟล์แล้ว

### 114. ตะวันปาล์ม: เพิ่มผลผลิตต่อไร่ในการ์ดแปลง + กราฟไลน์แนวโน้มผลผลิต 12 เดือนในหน้าภาพรวม

ต่อจากข้อ 113 — พี่เลือกทำ 2 ข้อจากที่แนะนำไว้:

1. **ผลผลิตต่อไร่ (กก./ไร่/ปี)** — เพิ่มบรรทัด "ผลผลิตปีนี้ (สะสม)" ในการ์ดแปลง คำนวณจากผลรวม `weightKg` ของรายการรายรับ (`D.palmMoney` type==='income') ที่มีบันทึกน้ำหนักในปีปัจจุบันของแปลงนั้น ÷ พื้นที่ (ไร่) — เป็นคนละมิติจาก "ปุ๋ยปีนี้ (สะสม)" ที่มีอยู่แล้ว (นั่นคือปุ๋ยที่ใส่ต่อต้น ไม่ใช่ผลผลิตที่เก็บเกี่ยวได้)
2. **กราฟไลน์แนวโน้มผลผลิต (12 เดือนล่าสุด)** — เพิ่มการ์ดใหม่ในแท็บ "ภาพรวม" ด้านบนสุด ใช้ Chart.js (line chart, ธีมสีเขียวปาล์ม #2F7D4F ตาม CSS var `--palm`) พร้อมแถบตัวกรองแปลง (ทุกแปลง/รายแปลง คล้ายรูปแบบ chip เดิม) แกน Y ใช้ `fmtKM()` ย่อหน่วยให้อ่านง่าย ข้อมูลรวมน้ำหนัก (กก.) จากรายรับที่มี `weightKg` แยกตามเดือนของ 12 เดือนที่ผ่านมา

ฟังก์ชันใหม่: `renderPalmYieldFilterChips()`, `setPalmYieldFilterPlot(id,btn)`, `renderPalmYieldChart()` (มี destroy-before-recreate pattern ป้องกัน error "Canvas is already in use" ตามแบบ `renderAbwChart()`/`_pdCharts.*` ที่มีอยู่แล้วในแอป) — เรียกทั้งคู่ตอนเปิดแท็บ "ภาพรวม" ใน `palmTab()`

ทดสอบผ่าน local server: สร้างแปลงพื้นที่ 10 ไร่ + บันทึกรายรับน้ำหนัก 1,500 กก. → การ์ดแปลงโชว่ "ผลผลิตปีนี้ (สะสม) 150.0 กก./ไร่" ถูกต้อง (1500÷10), กราฟไลน์ในหน้าภาพรวมโชว่ 12 เดือนถูกต้อง เดือนปัจจุบันมีค่า 1,500 กก. ตรงกับที่บันทึกไว้ ตัวกรองแปลงทำงานถูกต้อง ไม่มี console error — sync ไฟล์แล้ว

### 113. ตะวันปาล์ม: เพิ่มรายรับ/กำไรสุทธิสะสมในการ์ดแปลง + ตัวเลือกช่วงเวลา (เดือนนี้/ปีนี้/ทั้งหมด) ในหน้ารายรับ-รายจ่าย

ต่อจากข้อ 112 — พี่ขอให้ทำ 2 ข้อเสนอแนะที่แนะนำไว้ท้ายข้อ 112 ต่อด้วย:

1. **การ์ดแปลง** — เดิมมีแค่ "รายจ่ายรวม (ตั้งแต่เริ่ม)" เพิ่ม "รายรับรวม (ตั้งแต่เริ่ม)" และ "กำไรสุทธิสะสม (ตั้งแต่เริ่ม)" (สีเขียวถ้าเป็นบวก แดงถ้าติดลบ) ให้เห็นภาพรวมกำไร-ขาดทุนสะสมทั้งอายุแปลงในที่เดียว
2. **หน้า "รายรับ-รายจ่าย"** — เพิ่มแถบเลือกช่วงเวลา 3 ปุ่ม "เดือนนี้ / ปีนี้ / ทั้งหมด" (`#pm-period-chips`, ตัวแปร `_palmMoneyPeriod` ค่าเริ่มต้น `'year'` ให้ตรงกับพฤติกรรมเดิม) เหนือการ์ด KPI แทนที่จะ fix เป็น "ตั้งแต่ต้นปี" ตายตัว — ตารางประวัติด้านล่างยังคงโชว์ทุกรายการเหมือนเดิม (ไม่ถูกกรองตามช่วงเวลานี้ กรองแค่ตามแปลงเท่านั้น) มีแค่การ์ดสรุป KPI 3 ใบที่เปลี่ยนตามช่วงที่เลือก

ทดสอบผ่าน local server: จำลองรายจ่าย 2 รายการ(ปี 2567+ปีปัจจุบัน)รวม 5,500 กับรายรับ 14,880 → การ์ดแปลงโชว่รายรับ 14,880/รายจ่าย 5,500/กำไรสุทธิ 9,380 ถูกต้อง, สลับปุ่ม "เดือนนี้"/"ปีนี้"/"ทั้งหมด" แล้ว KPI เปลี่ยนค่าตามจริง (ทั้งหมด=5,500 ตรงกับรายจ่ายสะสมทุกปีในการ์ดแปลง, ปีนี้/เดือนนี้=2,500 ตัดรายการปี 2567 ออกถูกต้อง) — sync ไฟล์แล้ว

### 112. ปรับปรุงการ์ด/หน้าตะวันปาล์ม — เพิ่มรายจ่ายรวมสะสม, สถานะสีเขียว active, เปลี่ยนสรุปเงินเป็นตั้งแต่ต้นปี

ต่อจากข้อ 111 — พี่ขอเพิ่มข้อมูลในหน้าตะวันปาล์ม 3 จุด:

1. **การ์ดแปลง** — เพิ่มบรรทัด "รายจ่ายรวม (ตั้งแต่เริ่ม)" คำนวณจาก `D.palmMoney` ที่ type==='expense' ของแปลงนั้น รวมทุกปีไม่จำกัดช่วงเวลา (ต่างจาก "ปุ๋ยปีนี้" ที่นับเฉพาะปีปัจจุบัน)
2. **ป้ายสถานะ "ให้ผลผลิตแล้ว"** — เปลี่ยนจากป้ายพื้นอ่อน (`b-ok`) เป็นป้ายพื้นเขียวทึบตัวหนังสือขาว (`.badge.b-active-grn`) พร้อมจุดกระพริบ (`.live-dot` ใช้ `@keyframes pulse` เดิมที่มีอยู่แล้วสำหรับ sync indicator) ให้ดู active มากขึ้นตามที่พี่ขอ
3. **สรุปรายรับ-รายจ่าย** — เปลี่ยนตัวกรองจาก "เดือนนี้" เป็น "ตั้งแต่ต้นปี" (นับจากต้นปีปฏิทินปัจจุบันถึงวันนี้) ทั้งในแท็บ "รายรับ-รายจ่าย" (การ์ด KPI 3 ใบ) และแท็บ "ภาพรวม" (การ์ดรายได้/กำไรสุทธิ) ให้สอดคล้องกัน — เปลี่ยน field filter จาก `today().slice(0,7)` (ปี-เดือน) เป็น `today().slice(0,4)` (ปีอย่างเดียว) ในทั้ง `renderPalmOverview()` และ `renderPalmMoney()`

ทดสอบผ่าน local server: จำลองรายจ่าย 2 รายการ (ปี 2567 กับปี 2569) รวม 5,500 บาท → การ์ดแปลงโชว์ "รายจ่ายรวม (ตั้งแต่เริ่ม) ฿5,500" ถูกต้อง (รวมข้ามปี), การ์ด KPI "รายจ่ายสะสม (ตั้งแต่ต้นปี)" โชว์แค่ ฿2,500 (เฉพาะปี 2569 ปัจจุบัน) ถูกต้อง, ป้ายสถานะเช็คด้วย `getComputedStyle` ยืนยันพื้นหลังเขียวทึบ ตัวหนังสือขาว มี `.live-dot` จริง — sync ไฟล์แล้ว

### 111. สร้างโมดูล "ตะวันปาล์ม" — dashboard สวนปาล์มใหม่ทั้งชุด (admin/CEO เท่านั้น)

พี่ต้องการหน้าต่างข้อมูลอีกชุดชื่อ "ตะวันปาล์ม" สำหรับเก็บข้อมูลสวนปาล์ม (แปลง, รายรับ-รายจ่าย, ประวัติใส่ปุ๋ย) อยู่ในแอปตะวันเดิม เห็นเฉพาะ role admin และ CEO เท่านั้น — ทำ mockup ให้ดูก่อน (ผ่าน Artifact) แล้วพี่ยืนยัน "ok เริ่มทำได้"

**ข้อกำหนดสำคัญที่พี่ย้ำ**: ข้อมูลการเงินของตะวันปาล์ม (รายรับ-รายจ่าย) **ต้องแยกอิสระจากตะวันฟาร์ม (กุ้ง) โดยเด็ดขาด ห้ามนำไปรวม/คำนวณกับข้อมูลการเงินฝั่งกุ้งเลย** เพราะรายได้จากขายปาล์มนำไปลงทุนกองทุนต่อ ไม่ได้เข้ากองกลางฟาร์มกุ้ง

**Data model ใหม่** (top-level field ใหม่ทั้งหมด lazy-init ด้วย `||[]` ตรงจุดใช้งาน ไม่ลงทะเบียนใน `normalizeD()`/`LOG_KEYS` — ตามแบบ `D.medTrials` เดิม เพื่อไม่กระทบ schema เก่า):
- `D.palmPlots[]` — แปลงปาล์ม: `{id,name,variety,area,trees,plantYear,status:'producing'|'notyet'}`
- `D.palmMoney[]` — รายรับ-รายจ่ายของสวนปาล์มเอง (ไม่แตะ `D.inventoryLog`/ต้นทุนฝั่งกุ้งเลย): `{id,type:'income'|'expense',plotId,date,amount,note,by,weightKg?,pricePerKg?,linkedFertId?}`
- `D.palmFertilizer[]` — ประวัติใส่ปุ๋ยต่อแปลง (มีช่อง "ยี่ห้อปุ๋ย" ตามที่พี่ขอเพิ่ม): `{id,plotId,date,type,brand,qtyRaw,unitLabel,kg,cost,next,note,by,linkedMoneyId?}`

**หน้าใหม่ `#sec-palm`** มี 4 แท็บย่อย (ใช้ pattern `.stock-sub-btn`/`.stock-pane` เดิมจากหน้าสต็อก): ภาพรวม (KPI + เตือนกำหนดใส่ปุ๋ยรอบถัดไป), แปลงปาล์ม (การ์ดแปลงเพิ่ม/แก้/ลบ), รายรับ-รายจ่าย (บันทึกรายรับจากขายปาล์ม/รายจ่าย + กรองตามแปลง + KPI รายเดือน), ประวัติใส่ปุ๋ย (บันทึกพร้อมแปลงหน่วยอัตโนมัติ กระสอบ 50/25 กก. หรือกิโลกรัม → รวมเป็น กก., ผูกค่าใช้จ่ายเข้า `D.palmMoney` อัตโนมัติถ้ากรอกไว้)

**การคำนวณ กก./ต้น/ปี** (พี่ขอเพิ่ม): สรุปตารางต่อแปลง คำนวณจากผลรวม `kg` ของ `D.palmFertilizer` ที่ตรงปีที่เลือก หารด้วยจำนวนต้นในแปลงนั้น — แสดงทั้งในตาราง "สรุปปริมาณปุ๋ยที่ใส่ต่อต้น" และในการ์ดแปลง (ปีปัจจุบัน)

**กลไกเข้าถึงสำหรับ CEO** (สำคัญทางสถาปัตยกรรม): พบว่า `role==='ceo'` จะซ่อน sidebar/top-tabs/bottom-nav ทั้งหมดผ่าน `body.role-ceo` และบังคับโชว์แค่ `#sec-ceo` เท่านั้น (`showCeoOnly()`) — CEO เข้าไม่ถึงหน้าใหม่ผ่านระบบ `goTab()`/เมนูปกติเลย จึงเพิ่มปุ่ม "🌴 ตะวันปาล์ม" ฝังไว้ใน header ของ `#sec-ceo` โดยตรง (`openPalmFromCeo()` — ข้าม `goTab()` สลับ class `.sec` เอง) พร้อมปุ่ม "← กลับแดชบอร์ด CEO" ในหน้าปาล์ม (โชว่เฉพาะ `APP_ROLE==='ceo'`) ที่เรียก `showCeoOnly()` กลับไป

**Nav สำหรับ admin**: เพิ่มเมนู "🌴 ตะวันปาล์ม" (class `admin-only`) ใน 3 จุด — sidebar (กลุ่มใหม่ "สวนปาล์ม"), desktop dropdown (`#grp-settings`), mobile sheet — และ router `goTab()` เพิ่ม case `'palm'`

**บั๊กที่เจอระหว่างพัฒนา**: คอมเมนต์หัวบล็อก JS ใหม่เขียน `/* ... ═══ */` ตอนแรกลืมปิด `*/` ทำให้ comment กลืนโค้ดทั้งหมดที่ตามมาจนกว่าจะเจอ `*/` บังเอิญตัวถัดไปในไฟล์ (ของ `editChem()`) พังทั้งไฟล์ — ตรวจพบจาก `node --check` แล้วแก้เป็นคอมเมนต์บรรทัดเดียวสั้นๆ ปิดถูกต้อง

**ทดสอบผ่าน local server ครบ**: syntax check ผ่าน `node --check` ทุก `<script>` block, สวมบทบาท **admin** — เพิ่มแปลง/บันทึกรายรับ(คำนวณยอดจากน้ำหนัก×ราคาอัตโนมัติ)/บันทึกรายจ่ายใส่ปุ๋ย(แปลงหน่วยกระสอบ→กก.ถูกต้อง, สร้างรายจ่ายผูกอัตโนมัติใน `D.palmMoney`)/กก.ต้นปีคำนวณถูก(600กก.÷264ต้น=2.27)/ลบรายการที่ผูกกันถูกบล็อกจากฝั่งเงิน+ต้องลบจากฝั่งปุ๋ยแทน+cascade ลบทั้งคู่สำเร็จ, สวมบทบาท **CEO** — ปุ่มจากแดชบอร์ด CEO เปิดหน้าปาล์มได้ ปุ่มกลับคืนสู่ CEO ได้ (sidebar/tabs ยังคงถูกซ่อนตามปกติ), สวมบทบาท **staff** — เมนูปาล์มไม่โชว่ทั้ง 3 จุด (sidebar/dropdown/mobile sheet) และ `initPalm()` เงียบไม่ทำอะไรแม้เรียกตรงๆ ผ่าน console — sync `index.html`↔`TawanFarm_App.html` แล้ว

### 110. ตัวเลขย่อหลักพัน (K) — เพิ่มเกณฑ์ >1 ล้านให้ขึ้น M แทน (กันโชว์ "1000K")

พี่แจ้งว่าตัวเลขที่ย่อเป็นหน่วย K (เช่น มูลค่ารวม) ถ้าเกิน 1 ล้านให้เปลี่ยนไปโชว่เป็น M แทน ไม่ใช่ปล่อยให้ขึ้น "1000K"

ไล่หาทั้งไฟล์เจอ 2 จุดที่ย่อเป็น K แบบไม่มีเกณฑ์ล้าน: การ์ด "มูลค่าสต็อกรวม" ในหน้าสต็อก (`renderStockKPI`) และแกน Y ของกราฟแนวโน้มค่าแรงรายเดือน (หน้าค่าแรง แท็บ "ประวัติ/สรุป")

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`) — เพิ่มฟังก์ชันกลาง `fmtKM(v)` (วางไว้ข้าง `fD()`): `<1,000` → โชว่ตัวเต็ม, `≥1,000` → หาร 1,000 ปัดเป็นจำนวนเต็ม +'K', `≥1,000,000` → หาร 1,000,000 ทศนิยม 1 ตำแหน่ง (ตัด `.0` ทิ้งถ้าลงตัวพอดี) +'M' — แทนที่โค้ดคำนวณ K แบบ inline เดิมทั้ง 2 จุดด้วยการเรียก `fmtKM()` ตัวเดียวกัน กันแก้ไม่ครบถ้วนถ้ามีจุดอื่นในอนาคตที่ต้องย่อเลขแบบนี้ก็เรียกใช้ฟังก์ชันเดียวกันได้เลย

ทดสอบผ่าน local server: `fmtKM()` ทดสอบค่าตรงขอบเขตครบ (500→"500", 1,500→"2K", 999,000→"999K", 1,000,000→"1M", 1,234,567→"1.2M", 2,500,000→"2.5M", 0→"0") ถูกต้องทุกกรณี, จำลองมูลค่าสต็อกรวม 1,500,000 บาท → การ์ด "มูลค่าสต็อกรวม" ขึ้น "1.5M" ถูกต้อง (ก่อนแก้จะขึ้น "1500K") ไม่มี console error, sync สองไฟล์แล้ว

ต่อจากข้อ 108 — พี่แจ้งว่า "เสมียน" login เป็น role staff ให้เห็นตัวเลขแค่ยอดรายรับ-รายจ่ายที่ตัวเองเป็นคนจด (ไม่ใช่ของทุกคนในกองกลาง)

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`) — `renderClerkCash()`: เมื่อ `APP_ROLE==='staff'` กรอง `D.clerkCash` เหลือเฉพาะรายการที่ `by===curUserLabel()` **ก่อน**คำนวณอะไรทั้งหมด (ไม่ใช่แค่ซ่อนแถวคนอื่นทีหลัง) เพราะ "ยอดคงเหลือสะสม" ต่อแถวเป็นตัวเลขคำนวณต่อเนื่องจากรายการก่อนหน้า ถ้าซ่อนแถวแต่ยังคำนวณจากข้อมูลทุกคนอยู่เบื้องหลัง ตัวเลขที่ staff เห็นจะแอบรั่วรวมยอดของคนอื่นเข้ามาด้วยทางอ้อม — เปลี่ยนป้าย KPI "ยอดคงเหลือปัจจุบัน" เป็น "ยอดสุทธิที่คุณบันทึก" และหัวคอลัมน์ตาราง "คงเหลือสะสม" เป็น "สุทธิสะสม (เฉพาะคุณ)" ให้ตรงความหมายใหม่ พร้อมแบนเนอร์แจ้ง "🔒 มุมมองพนักงานเห็นเฉพาะรายการที่คุณบันทึกเอง — ยอดคงเหลือรวมทั้งกองทุนดูได้จาก Admin" (รูปแบบเดียวกับแบนเนอร์ staff ในแท็บ "ยอดขายลูกค้าภายนอก") — role admin/CEO ยังเห็นข้อมูลครบทุกคนเหมือนเดิมไม่เปลี่ยนแปลง

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 2 ผู้บันทึก (somchai 2 รายการ, malee 2 รายการ) จำลอง `curUserLabel()` เป็น "somchai": สลับ role staff → เห็นแค่ 2 รายการของ somchai, ยอดสุทธิคำนวณถูกต้อง (5,000−200=4,800) ไม่เห็นรายการ/ยอดของ malee เลย, แบนเนอร์ขึ้นถูกต้อง — สลับกลับ role admin → เห็นครบ 4 รายการ ยอดรวมถูกต้อง (8,000−700=7,300) แบนเนอร์หายไป ไม่มี console error, sync สองไฟล์แล้ว

### 108. หน้าสต็อก — เพิ่มแท็บ "รายรับ-รายจ่าย (เสมียน)" ผูกรายจ่ายเข้ากับต้นทุน "อุปกรณ์นอก" อัตโนมัติ

พี่ต้องการ card บันทึกรายรับ-รายจ่ายเงินสดสำหรับตำแหน่ง "เสมียน" ที่เบิกเงินจากส่วนกลางไว้ซื้อของทำอาหารให้คนงาน/ค่าใช้จ่ายเบ็ดเตล็ดในฟาร์ม โดยขอให้นำ "รายจ่าย" ไปคิดรวมกับต้นทุน "อุปกรณ์นอก" (ข้อ 102) — คุยรายละเอียดก่อนทำ 3 คำถาม พี่ยืนยัน: (1) ต้องการทั้งรายรับ+รายจ่ายพร้อมยอดคงเหลือสะสม ไม่ใช่แค่บันทึกรายจ่ายเฉยๆ (2) เฉพาะ "รายจ่าย" เท่านั้นที่นำไปหารแบ่งทุกบ่อ ส่วน "รายรับ" (เงินเบิกจากส่วนกลางมาเติม) เป็นแค่การโอนเงิน ไม่ใช่ต้นทุนจริง ไม่หารบ่อ (3) วางเป็นแท็บใหม่ในหน้า "สต็อก" (อยู่ใกล้กับโหมด "แชร์ทุกบ่อ" เดิมที่ผูกกันอยู่)

**สถาปัตยกรรม:** ข้อมูลเก็บเป็น `D.clerkCash[]` array ใหม่ `{id,type:'in'|'out',date,amount,note,by,linkedLogId?}` — **รายการ "รายจ่าย" (type='out') เท่านั้น** ที่ auto-สร้าง entry คู่กันใน `D.inventoryLog` (`type:'shared'`, `category:'equip'`, `itemId:null`, `unitCost:<จำนวนเงิน>`, `qty:1`, `unit:'รายการ'`) แล้วเก็บ id ที่สร้างไว้ที่ `entry.linkedLogId` — เลือกวิธีนี้แทนการเขียนสูตรคำนวณคู่ขนานใหม่ เพราะทำให้รายจ่ายของเสมียนไหลเข้า **ทุกจุดที่ใช้ `getSharedCostForPond`/`getSharedItemsInRange` อยู่แล้วโดยอัตโนมัติ** (การ์ดต้นทุนสะสมหน้ารายละเอียดบ่อ, ชีท CEO, auto-fill ช่องอุปกรณ์นอกตอนปิดรอบ, แท็บ "ประวัติแชร์ทุกบ่อ" ข้อ 106) โดยไม่ต้องแก้โค้ดจุดเหล่านั้นเลยสักบรรทัด — "รายรับ" (type='in') ไม่แตะ `D.inventoryLog` เลยตามที่ตกลงกัน

**หน้าตา:** แท็บใหม่ "💰 รายรับ-รายจ่าย (เสมียน)" ในหน้าสต็อก (`stockTab('cash',...)`) มี 3 การ์ด: (1) KPI ยอดคงเหลือปัจจุบัน/รายรับสะสม/รายจ่ายสะสม/จำนวนรายการ (2) ฟอร์มบันทึกสลับโหมดรายรับ/รายจ่าย (`setCashMode`, รีใช้สไตล์ปุ่ม `.stock-mode-btn.on-in/.on-out` เดิม) (3) ตารางประวัติเรียงใหม่สุดก่อน พร้อมคอลัมน์ "คงเหลือสะสม" ต่อแถว (คำนวณ running balance จากลำดับเวลาจริง**ก่อน**กรองเดือน กันยอดเพี้ยนถ้ากรองดูแค่บางเดือน) — ลบรายการ "รายจ่าย" จะลบรายการที่ผูกไว้ใน `D.inventoryLog` พร้อมกันเสมอ (มี confirm แจ้งเตือนก่อน) ทั้งคู่ tombstone เข้า `D.deletedIds` ถูกต้อง

**ยังไม่ได้ทำ (ตามที่พี่ยืนยันว่าไม่จำเป็นตอนนี้):** ไม่แนบรูปใบเสร็จ — ไม่ได้ถามเพิ่มเพราะพี่เคยตัดสินใจไม่เอาฟีเจอร์แนบรูปมาแล้วในข้อ 105/107 (ข้อจำกัดเรื่องค่าใช้จ่าย Firebase Storage เดียวกัน)

**ข้อควรทราบ:** ไม่ได้ซ่อนตัวเลขในแท็บนี้จาก role staff เหมือนโหมด "แชร์ทุกบ่อ" (ข้อ 103) เพราะฟีเจอร์นี้สร้างมาให้ "เสมียน" ใช้บันทึก/ดูยอดคงเหลือของตัวเองโดยตรง (ถ้าเสมียน login เป็น role staff แล้วมองไม่เห็นยอดเงินตัวเองจะใช้งานไม่ได้เลย) — ถ้าต้องการซ่อนจาก staff บางกลุ่มในอนาคตแจ้งเพิ่มได้

ทดสอบผ่าน local server: บันทึกรายรับ 5,000 (เบิกเงินเติม) + รายจ่าย 350 (ซื้อกับข้าว) → ยอดคงเหลือ/สะสมคำนวณถูกต้อง (16,000 รับ − 700 จ่าย = 15,300 คงเหลือ นับรวมข้อมูลทดสอบเก่าที่ค้างจากรอบก่อน), ข้อความแจ้งบันทึกสำเร็จแสดงถูกต้อง (🔴 เจอบั๊กที่แก้ทันที: `clearClerkCashForm()` เผลอเรียก `showStockMsg('cash','')` ล้างข้อความสำเร็จที่เพิ่งตั้งไปทันที ลบบรรทัดนั้นออกเพราะ concern คนละเรื่องกัน ควรแค่ล้างช่องกรอก ไม่ควรไปยุ่งกับข้อความแจ้งผล), รายจ่ายที่บันทึกไปโผล่ในแท็บ "ประวัติแชร์ทุกบ่อ" ถูกต้อง และ `getSharedCostForPond()` นับรวมเข้าไปในต้นทุนของบ่อจริงถูกต้อง, ลบรายการรายจ่ายผ่านปุ่มจริง → รายการที่ผูกใน `D.inventoryLog` ถูกลบตามไปด้วยพร้อม tombstone ครบทั้งคู่, ตัวกรองเดือนทำงานถูกต้อง ไม่มี console error, sync สองไฟล์แล้ว

พี่แจ้งว่าช่อง "วัตถุประสงค์"/"ผลสรุปเบื้องต้น" ในการ์ด "รายละเอียดการทดลอง (เฉพาะแล็บ)" เล็กเกินไป อ่านข้อมูลลำบาก — เสนอ 3 ไอเดียเพิ่มเติมสำหรับหน้านี้ พี่เลือกทำข้อ 1 (สถานะการทดลอง) กับข้อ 3 (ค้นหา/กรองรายการ) ข้าม ข้อ 2 (แนบรูปภาพ — พี่ถามเรื่องค่าใช้จ่าย Firebase Storage ก่อน แล้วตัดสินใจไม่ทำ ตรงกับที่เคยตัดฟีเจอร์แนบรูปออกไปแล้วในข้อ 105)

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):
1. **ขยายช่องกรอก** — `textarea#trial-objective`/`#trial-conclusion` จาก `rows="2"` เป็น `rows="6"` พร้อม `resize:vertical` (ลากขยายเพิ่มเองได้) และเพิ่ม `line-height:1.6` ให้อ่านง่ายขึ้น
2. **สถานะการทดลอง** — เพิ่ม `_trialDraft.status` (`'ongoing'|'done'|'paused'`, ค่าเริ่มต้น `'ongoing'`) พร้อมช่องเลือกแบบชิป 🟢 กำลังทดลอง / ✅ เสร็จสิ้น / ⏸️ ระงับ ในการ์ด "ชื่อการทดลอง + ช่วงเวลา" (`setTrialStatus()`/`renderTrialStatusButtons()`) บันทึกไปกับ `saveTrial()` — รายการเก่าที่ไม่มี field นี้ (บันทึกไว้ก่อนข้อนี้) fallback เป็น `'ongoing'` อัตโนมัติ ไม่ต้องไล่แก้ข้อมูลเก่า
3. **ค้นหา/กรองรายการในดรอปดาวน์ "การทดลองที่บันทึกไว้"** — เพิ่มช่องค้นหาชื่อ (`onTrialSearchInput()`) และชิปกรองสถานะ (ทั้งหมด/กำลังทดลอง/เสร็จสิ้น/ระงับ, `setTrialListFilterStatus()`) กรองรายการก่อนขึ้น `<select id="trial-select">` ผ่านฟังก์ชันกลาง `populateTrialSelect()` (แยกออกมาจาก `initTrialTab()` เดิมเพื่อเรียกซ้ำได้จากทั้ง filter/search และตอน init) — แต่ละตัวเลือกในดรอปดาวน์ขึ้นไอคอนสถานะนำหน้าชื่อด้วย (`TRIAL_STATUS_ICON`) ให้กวาดตาเห็นสถานะโดยไม่ต้องกดเข้าไปดู
4. เพิ่ม CSS `.trial-chip`/`.trial-chip.on` (ปุ่มชิปพื้นทึบสีน้ำตาลส้มตอนเลือก) ใช้ร่วมกันทั้งชิปตั้งสถานะและชิปกรองรายการ (แยกด้วย class ย่อย `.trial-status-chip`/`.trial-filter-chip` กันไม่ให้ toggle ผิดกลุ่มกัน)

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 3 การทดลอง (สถานะต่างกัน + 1 รายการเก่าไม่มี status field): ดรอปดาวน์ขึ้นไอคอนสถานะถูกต้องครบทุกรายการรวมรายการเก่าที่ fallback เป็น "🟢 กำลังทดลอง" อัตโนมัติ, ค้นหาชื่อกรองเหลือเฉพาะรายการที่ตรงถูกต้อง, กดชิปกรองสถานะ "ระงับ" เหลือเฉพาะรายการที่ระงับถูกต้อง กดชิป "ทั้งหมด" คืนค่าครบ, กดชิปสถานะในการ์ด "ชื่อการทดลอง" เปลี่ยน `_trialDraft.status` และ toggle class ถูกต้อง, กด "บันทึกการทดลอง" แล้วสถานะใหม่ถูกบันทึกและดรอปดาวน์อัปเดตไอคอนทันที ไม่มี console error, sync สองไฟล์แล้ว

### 106. หน้าสต็อก — เพิ่มแท็บ "ประวัติแชร์ทุกบ่อ" (อุปกรณ์นอก) ที่ขาดอยู่ + แก้บั๊ก deleteStockLog ตัดสต็อกซ้ำ

พี่แจ้งว่าโหมดเบิก "แชร์ทุกบ่อ" (ข้อ 102) ไม่มีหน้าดูประวัติย้อนหลังเลย — ตรวจโค้ดพบว่าจริง: แท็บ "ประวัติ" เดิมมีแค่ 2 โหมด "เบิกออก รายบ่อ" (`renderStockOutLog`, กรอง `type==='out'&&l.pondId`) กับ "รับเข้าส่วนกลาง" (`renderStockInLog`, กรอง `type==='in'`) — ไม่มีโหมดไหนกรอง `type==='shared'` เลย (และ entry ของโหมดนี้ก็มี `pondId:''` เสมออยู่แล้วตามดีไซน์ข้อ 102 ทำให้ไม่มีทางหลุดเข้าไปโชว์ในแท็บ "เบิกออก รายบ่อ" ได้ด้วย) รายการที่เคยบันทึกไปแล้วจึงไม่มีที่ไหนในแอปให้ดูย้อนหลังเลยนอกจากเปิด DevTools

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):
- เพิ่มปุ่มโหมดที่ 3 **"🔧 แชร์ทุกบ่อ"** ในแท็บ "ประวัติ" คู่กับ 2 โหมดเดิม (`setStockLogMode('shared')`)
- ฟังก์ชันใหม่ `renderStockShareLog()` — กรอง `D.inventoryLog` ที่ `type==='shared'` มีตัวกรองหมวด/เดือนเหมือนแท็บ "เบิกออก รายบ่อ" (กรองจาก `l.category` ที่ snapshot ไว้ในรายการเองตรงๆ ไม่ต้อง join กับ `D.inventoryItems` เพราะสินค้าที่พิมพ์เองไม่มี `itemId` ให้ join) — คอลัมน์ราคา/หน่วย, มูลค่ารวม, จำนวนบ่อที่แชร์ (`activePondCountOnDate`), ส่วนแบ่ง/บ่อ **ซ่อนจาก role staff** เหมือนตะกร้าตอนบันทึก (ข้อ 103) มีการ์ด KPI สรุปจำนวนรายการ+มูลค่ารวมตามตัวกรองที่เลือก (admin เท่านั้น)
- 🔴 **เจอบั๊กเพิ่มระหว่างทำ**: `deleteStockLog()` เช็คแค่ `log.type==='out'||log.type==='sale'` ถึงจะ "บวกคืน" สต็อกตอนลบ ส่วนที่เหลือ (รวม `type==='shared'`) ตกไปอยู่ branch "หักออก" ซึ่งถูกต้องสำหรับ `type==='in'` เท่านั้น — สินค้าจากแคตตาล็อกในโหมดแชร์ทุกบ่อ (มี `itemId`) ถูกตัดสต็อกตอนบันทึกเหมือนโหมด "เบิกออก" ทุกประการ แต่ตอนลบกลับไปหักซ้ำอีกที (แทนที่จะบวกคืน) ทำให้ยอดคงเหลือผิดเพี้ยนไปสองเท่าของรายการที่ลบทุกครั้ง — แก้โดยเพิ่ม `log.type==='shared'` เข้ากลุ่ม "บวกคืน" ด้วย (สินค้าที่พิมพ์เองไม่มี `itemId` อยู่แล้วจึงไม่มีผลกับจุดนี้)

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง (สินค้าจากแคตตาล็อก 1 รายการ + สินค้าพิมพ์เอง 1 รายการ ปนกับข้อมูล seed เดิม): แท็บใหม่แสดงครบ 4 รายการ, ตัวกรองหมวด/เดือนทำงานถูกต้อง, สลับ role staff/admin ซ่อน/โชว่คอลัมน์ราคาและการ์ด KPI ถูกต้อง, กดลบรายการสินค้าจากแคตตาล็อกผ่านปุ่มจริง → ยอดคงเหลือบวกกลับถูกต้อง (20→23 ตรงตาม qty ที่ลบ 3, ก่อนแก้จะกลายเป็น 17) พร้อม tombstone เข้า `D.deletedIds` ถูกต้อง ไม่มี console error, sync สองไฟล์แล้ว

### 105. ย้าย "ทดลองเวชภัณฑ์" ออกมาเป็นหน้าแยก (อันดับ 2 ต่อจาก "ผลแล็บ") + เพิ่มโหมดทดลองในห้องแล็บ (ไม่ผูกกับบ่อ) + บันทึกประจำการทดลอง

พี่ขอให้ย้าย "ทดลองเวชภัณฑ์" (ข้อ 74-75, 79, 95 — เดิมเป็นหนึ่งในปุ่มหมวดของหน้า "เปรียบเทียบ") ออกมาเป็นหน้า/เมนูของตัวเอง วางไว้อันดับ 2 ต่อจาก "ผลแล็บ" (ก่อน "เปรียบเทียบ") จากนั้นขอเพิ่มอีก 2 อย่าง: (1) ช่องบันทึกประจำการทดลองเป็นข้อความ เก็บเป็นประวัติ (2) รองรับการทดลองที่ไม่ได้ทำกับบ่อเลี้ยงจริง เป็นการทดลองภายในห้องแล็บอย่างเดียว — คุยออกแบบผ่าน mockup ก่อน ([mockup_ทดลองเวชภัณฑ์_หน้าแยก+บันทึกรูปภาพ.html](../Mockup และ Prototype/mockup_ทดลองเวชภัณฑ์_หน้าแยก+บันทึกรูปภาพ.html)) รอบแรกออกแบบพร้อมปุ่มแนบรูปภาพ พี่ขอให้ตัดออกทีหลัง ("ไม่ต้องมีการแนบรูปภาพ") เหลือบันทึกเป็นข้อความอย่างเดียว แก้ mockup แล้ว implement เข้าแอปจริงตามนั้น

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):

1. **ย้ายหน้า** — ดึง markup ทั้งหมดของ `#cmp-trial` (เดิมอยู่ใน `sec-compare`) ออกมาเป็น `<div id="sec-trial" class="sec">` แยกต่างหาก วางไว้ระหว่าง `sec-lab` กับ `sec-compare` — ลงทะเบียนเมนูครบ 3 จุดตามแพทเทิร์นเดิมของแอป (sidebar `sn-link`, desktop dropdown `tab-item id="tab-trial"`, มือถือ `mob-sheet-item data-t="trial"`) ทั้งหมดอยู่อันดับ 2 ในกลุ่ม "วิเคราะห์" ต่อจาก "ผลแล็บ" ตรงตามที่ขอ พร้อมเพิ่ม `goTab()` router clause `if(t==='trial')initTrialTab();`
2. **ตัด logic ของ "ทดลองเวชภัณฑ์" ออกจากหน้า "เปรียบเทียบ" ทั้งหมด** — ลบปุ่มหมวด `cmp-cat-trial` ออกจากแถบปุ่ม (เหลือ 5 หมวดเดิม: คุณภาพน้ำ/อาหาร/ไซส์/แล็บ/น้ำ), ลบทุกเงื่อนไข `isTrial`/`cmp-trial` ใน `setCmpCat()`/`initCompare()` ที่เคย short-circuit ไปเรียก `initTrialTab()` แทน `renderCompare()` (ฟังก์ชัน/ตัวแปรอื่นของ trial ทั้งหมด — `_trialDraft`, `TRIAL_METRICS`, `trial*()` — ไม่ได้ย้าย/แก้ชื่อ ยังเรียกใช้ได้เหมือนเดิมทุกจุด เพราะ id ของ element ที่มันอ้างถึงยังเหมือนเดิม แค่ย้าย parent section)
3. **เพิ่มประเภทการทดลอง "บ่อเลี้ยง / ห้องแล็บ"** — การ์ดใหม่ "ประเภทการทดลอง" (ปุ่ม toggle `tb-pond`/`tb-lab`, ฟังก์ชันใหม่ `setTrialType(t)`) เก็บลง `_trialDraft.type` (`'pond'|'lab'`, ค่าเริ่มต้น `'pond'` เพื่อไม่กระทบการทดลองเก่าที่มีอยู่แล้วทั้งหมด — โหลดกลับมาด้วย `t.type||'pond'` fallback) — เลือก "ห้องแล็บ" แล้วซ่อนทั้ง section ที่เกี่ยวกับบ่อ (`#trial-pond-section` ครอบ: เลือกบ่อ/ตัวชี้วัด/กราฟการเติบโต/มินิเทียบ/ตารางรายละเอียด) เปลี่ยนมาโชว์การ์ดใหม่ "รายละเอียดการทดลอง (เฉพาะแล็บ)" แทน (`#trial-lab-only-card` — ช่อง "วัตถุประสงค์/สิ่งที่ต้องการทดสอบ" กับ "ผลสรุปเบื้องต้น" เก็บลง `_trialDraft.objective`/`conclusion`) — `saveTrial()` ปรับ validation ให้โหมดแล็บไม่บังคับเลือก 2-4 บ่อเหมือนโหมดบ่อ (บันทึก `ponds:{}` เสมอสำหรับโหมดแล็บ)
4. **เพิ่ม "บันทึกประจำการทดลอง"** — การ์ดใหม่ท้ายหน้า (ก่อนปุ่มบันทึก/ลบ) ใช้ได้ทั้ง 2 ประเภท: ปุ่ม "+ เพิ่มบันทึกใหม่" เปิดฟอร์มย่อย (วันที่ + ข้อความ) กด "บันทึก" เพิ่มเข้า `_trialDraft.journal[]` (`{id,date,note}`) แสดงเรียงใหม่สุดก่อน ลบทีละรายการได้ (`toggleTrialJournalAdd()`/`addTrialJournalEntry()`/`deleteTrialJournalEntry()`/`renderTrialJournal()`) — **ไม่มีการแนบรูปภาพ** ตามที่พี่ตัดออกจาก mockup รอบสุดท้าย — บันทึกจริงลง Firebase พร้อมกับข้อมูลการทดลองทั้งหมดตอนกด "บันทึกการทดลอง" (ไม่ใช่บันทึกทันทีที่เพิ่มรายการ — ตามแพทเทิร์นเดียวกับที่เลือกบ่อ/ตัวชี้วัดที่มีอยู่แล้ว คือแก้ไขแบบ draft ก่อน แล้วกดบันทึกทีเดียว)
5. **CSS ใหม่** — เพิ่มตัวแปรสี `--lab`/`--labl`/`--labd` (โทนเขียวอมฟ้า แยกจาก `--p` สีหลักของแอป ใช้กำกับป้าย "แล็บ") และคลาส `.type-row`/`.type-btn`/`.type-desc`/`.lab-tag`/`.jr-*` (บันทึกประจำการทดลอง)

**ขอบเขตที่ตั้งใจไม่แตะ:** ฟิลด์/ฟังก์ชันคำนวณตัวชี้วัดทั้งหมดของโหมดบ่อ (ADG/FCR/กุ้งตาย/ผลแล็บ/ค่าน้ำในช่วงทดลอง, กราฟไฮไลต์ช่วง — ข้อ 95) ไม่ได้แก้ logic ใดๆ เลย มีแค่ย้ายตำแหน่ง section — `D.medTrials[]` ยังเป็น lazy-init เหมือนเดิม ไม่ได้เพิ่มใน `normalizeD`/`LOG_KEYS` (ฟิลด์ใหม่ `type`/`objective`/`conclusion`/`journal` ก็เช่นกัน อ่านด้วย fallback `||` ทุกจุด ไม่มีความเสี่ยง data-migration กับข้อมูลเก่า)

Mockup ที่ใช้ยืนยันดีไซน์: [mockup_ทดลองเวชภัณฑ์_หน้าแยก+บันทึกรูปภาพ.html](../Mockup และ Prototype/mockup_ทดลองเวชภัณฑ์_หน้าแยก+บันทึกรูปภาพ.html) (ชื่อไฟล์ยังมีคำว่า "บันทึกรูปภาพ" ค้างจากรอบแรก แต่เนื้อหาถูกแก้ตัดส่วนรูปภาพออกแล้วตามที่พี่ยืนยันรอบสุดท้าย)

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง (ไม่ได้ล็อกอิน Firebase จริง ปลอดภัย 100%): เมนู "🧪 ทดลองเวชภัณฑ์" ขึ้นถูกตำแหน่งอันดับ 2 ต่อจาก "ผลแล็บ" ครบทั้ง 3 จุด (sidebar/desktop dropdown/มือถือ) คลิกจริงนำไปหน้า `sec-trial` ถูกต้อง, ค่าเริ่มต้นโหมด "บ่อเลี้ยง" ถูกต้อง สลับเป็น "ห้องแล็บ" ซ่อน section บ่อ+โชว่การ์ดวัตถุประสงค์/ผลสรุปถูกต้อง (และสลับกลับถูกต้อง), บันทึกการทดลองโหมดแล็บ (ไม่เลือกบ่อเลย) สำเร็จพร้อม `objective`/`conclusion`/`journal` ครบถูกต้อง, โหลดกลับมาด้วย `loadTrial()` ค่าตรงกันทุกฟิลด์รวม UI toggle, ทดสอบ validation โหมดบ่อ (ยังบังคับ 2-4 บ่อเหมือนเดิม) เลือกครบ 2 บ่อแล้วบันทึกสำเร็จ, ลบการทดลองทำงานถูกต้อง (tombstone เข้า `D.deletedIds`), สลับไปหน้า "เปรียบเทียบ" ยืนยันเหลือ 5 หมวดถูกต้องไม่มีร่องรอยของทดลองเวชภัณฑ์หลงเหลือ ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว

### 104. หน้าสต็อก — กดเลือกสินค้าแล้วแป้นตัวเลขบนมือถือไม่เด้งขึ้น

พี่แจ้งว่าหน้า "บันทึกรับ-เบิก" (ทุกโหมด: รับเข้า/เบิกออก/แชร์ทุกบ่อ/ขายลูกค้าภายนอก — ใช้ฟังก์ชันเดียวกัน) พิมพ์ค้นหาสินค้าแล้วกดเลือกจากรายการ อยากให้เด้งไปโฟกัสช่องจำนวนทันทีพร้อมเปิดแป้นตัวเลขบนมือถือให้เลย เพื่อพิมพ์เลขต่อได้ไว — ตรวจโค้ดพบว่า `addStockToCart()` มีการ `.focus()` ช่องจำนวนอยู่แล้วจริง แต่ครอบด้วย `setTimeout(...,50)` ซึ่งเป็นสาเหตุที่แป้นตัวเลขไม่เด้งขึ้นบนมือถือ (เบราว์เซอร์มือถือส่วนใหญ่ต้องการให้ `.focus()` อยู่ใน call stack เดียวกับ event ที่ผู้ใช้กด (เช่น click เลือกสินค้า) แบบ synchronous เท่านั้นถึงจะยอมเปิดคีย์บอร์ดเสมือนให้ — พอห่อด้วย `setTimeout` แม้แค่ 50ms ก็ทำให้ "user activation" ที่เชื่อมกับ click เดิมหลุดไปแล้ว โฟกัสอาจติดแต่คีย์บอร์ดจะไม่เด้ง)

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`) — เอา `setTimeout` ออก เรียก `.focus()`/`.select()` ทันทีหลัง `renderStockCart()` แบบ synchronous อยู่ใน call stack เดียวกับ click ที่กดเลือกสินค้าจากรายการค้นหา

ทดสอบผ่าน local server ด้วยการคลิกจริงผ่านเมาส์ (ไม่ใช่เรียกฟังก์ชันตรงหรือ `.click()` จากสคริปต์ ซึ่งพบว่าไม่นับเป็น user gesture ที่แท้จริงในเบราว์เซอร์ทดสอบ ให้ผลไม่ตรงกับการใช้งานจริง): พิมพ์ค้นหา "ฟูมิค" → คลิกเลือกจากรายการ → ช่องจำนวนขึ้นกรอบโฟกัสสีน้ำเงินทันที (ยืนยันด้วยภาพหน้าจอ) → พิมพ์ "5" ทับค่าเดิม "1" ได้ทันทีโดยไม่ต้องคลิกช่องเอง ถูกต้องตามที่ต้องการ ไม่มี console error, sync สองไฟล์แล้ว (ฟังก์ชันนี้ใช้ร่วมกันทั้ง 4 โหมดของหน้าบันทึกรับ-เบิก แก้จุดเดียวได้ผลครบทุกโหมด)

### 103. 🔴 หน้าสต็อก — โหมด "แชร์ทุกบ่อ" เผลอโชว์ราคาทุนให้ role staff เห็น

พี่แจ้งว่าตอนเพิ่มรายการสินค้าในโหมด "แชร์ทุกบ่อ" (ข้อ 102) role staff เห็นราคาทุนของสินค้าด้วย — ตรวจแล้วพบว่าเป็นจุดที่หลุดจริง: ตอนทำโหมดนี้ใหม่เพิ่มป้าย `฿{ราคาทุน}/{หน่วย}` ต่อท้ายแต่ละแถวในตะกร้าให้เห็นราคาก่อนกดบันทึก แต่ลืมเช็ค role เหมือนจุดอื่นๆ ในหน้าสต็อกที่ทำถูกไว้แล้ว (เช่น ตาราง "รายการพัสดุ" ที่ซ่อนคอลัมน์ราคาจาก staff, โหมด "เบิกออกไปบ่อ" ที่ไม่โชว่ราคาให้ใครเห็นเลยตั้งแต่ต้น — ดูข้อ 96)

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`) — `renderStockCart()`: เพิ่มเงื่อนไข `APP_ROLE==='admin'` ควบคู่กับ `isShared` ก่อนแสดงป้ายราคา — staff จะไม่เห็นราคาทุนในตะกร้าโหมดนี้อีกต่อไป (แถวอื่นๆ เช่น ชื่อสินค้า/จำนวน/หมวด ยังเห็นได้ปกติ เพราะไม่ใช่ข้อมูลการเงิน) admin ยังเห็นราคาเหมือนเดิมเพื่อตรวจสอบยอดก่อนกดบันทึก — ไม่กระทบการบันทึกจริง เพราะ `c.unitCost` ยังถูกเก็บในตะกร้าและใช้คำนวณตอน `saveStockTxBatch()` เหมือนเดิมทุกประการ แค่ซ่อนการแสดงผลเท่านั้น

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง (ไม่ได้ล็อกอิน Firebase จริง ปลอดภัย 100%): เพิ่มสินค้าลงตะกร้าโหมด "แชร์ทุกบ่อ" แล้วสลับ `applyRole('staff')` → ป้ายราคาหายไปจากตะกร้าถูกต้อง (แถวอื่นยังอยู่ครบ), สลับกลับ `applyRole('admin')` → ป้ายราคากลับมาถูกต้อง ไม่มี console error, sync สองไฟล์แล้ว

### 102. หน้าสต็อก — เพิ่มโหมดเบิก "แชร์ทุกบ่อ" (อุปกรณ์นอก) + พิมพ์สินค้าใหม่เองได้ถ้าไม่มีในสต็อก

พี่ต้องการหมวด "อุปกรณ์นอก" ที่นำค่าใช้จ่ายไปแชร์เฉลี่ยให้ทุกบ่อที่มีกุ้งอยู่ตอนนั้นๆ อัตโนมัติ — คุยรายละเอียดแล้วพี่ปรับ 2 จุด: (1) ไม่ใช่หมวดสินค้าใหม่แยกต่างหาก แต่เป็น**โหมดการเบิกที่ 4** ที่ดึงสินค้าได้จากสต็อกทั้งหมดข้ามทุกหมวด เพราะของบางอย่าง (เช่น อะมิโน, โพแทสเซียม) ถูกจัดหมวดเดิมไว้แล้วแต่บางครั้งซื้อมาใช้ร่วมทั้งฟาร์ม ระบุลงบ่อใดบ่อหนึ่งไม่ได้ (2) สินค้าบางอย่างซื้อจากข้างนอกครั้งเดียว ไม่เคยมีอยู่ในแคตตาล็อกสต็อกเลย ต้องพิมพ์ชื่อ/หมวด/ราคาเองได้ในหน้าเดียวกัน — ทำ mockup ยืนยัน 3 รอบก่อน ([mockup_อุปกรณ์นอก_แชร์ทุกบ่อ.html](../Mockup และ Prototype/mockup_อุปกรณ์นอก_แชร์ทุกบ่อ.html)) พี่คอนเฟิมแล้วให้ implement เข้าแอปจริง

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):

1. **หน้าสต็อก → บันทึกรับเข้า/เบิกออก** เพิ่มโหมดที่ 4 **"🔧 แชร์ทุกบ่อ"** (`setStockMode('shared')`) — ไม่มีช่องเลือกบ่อ (เหมือนโหมดขาย) มีแบนเนอร์อธิบายวิธีคิด ช่องค้นหาสินค้าเดิม (`filterStockSearch`) ค้นได้ทุกหมวดอยู่แล้วโดยไม่ต้องแก้อะไร (ไม่เคยกรองตามหมวดตั้งแต่แรก) — เพิ่มลิงก์ **"➕ สินค้านี้ไม่มีในสต็อก — พิมพ์ชื่อ/ราคาเองแทนการค้นหา"** เปิดฟอร์มย่อยกรอกชื่อ/หมวด/จำนวน/หน่วย/ราคาต่อหน่วยเอง (`addManualItemToCart()`) ไม่ผูกกับ `D.inventoryItems` เลย (ไม่มีสต็อกให้ตัด ไม่เช็คยอดคงเหลือ)
2. **ตะกร้าเบิก** (`_stockCart`) เพิ่ม `cartId` เป็นตัวระบุแถวที่ไม่ซ้ำเสมอ (สินค้าจากแคตตาล็อกใช้ `itemId` เดิม, สินค้าที่พิมพ์เองไม่มี `itemId` เลยต้องมีตัวระบุแยก) — แก้ `addStockToCart`/`removeFromCart`/`updateCartQty`/`updateCartPrice`/`renderStockCart` ให้ใช้ `cartId` แทน `itemId` ตรงๆ ทุกจุด (ของเดิมยังทำงานเหมือนเดิมทุกประการเพราะ `cartId=itemId` สำหรับสินค้าจากแคตตาล็อก) — ตะกร้าโหมด "แชร์ทุกบ่อ" โชว์จุดสีตามหมวดจริงของสินค้า (`.ceo-cat-dot`, ใช้ `CEO_CAT_DOT` เดิม) + ป้าย "(ของใหม่)" กำกับสินค้าที่พิมพ์เอง
3. **`saveStockTxBatch()`** เพิ่ม branch `mode==='shared'` — สินค้าจากแคตตาล็อกตัดสต็อกตามปกติเหมือนโหมด "เบิกออก" ส่วนสินค้าที่พิมพ์เอง (`itemId` ว่าง) ไม่มีสต็อกให้ตัด — บันทึกลง `D.inventoryLog` เป็น `type:'shared'`, `pondId:''` เสมอ พร้อม **snapshot `category`/`unitCost` ลงในรายการเองเสมอ** (ต่างจากโหมดอื่นที่ join ราคา/หมวดกับ `D.inventoryItems` ตอนอ่านทีหลัง เพราะสินค้าที่พิมพ์เองไม่มี `itemId` ให้ join ย้อนหลังได้)
4. **ฟังก์ชันคำนวณส่วนแบ่งใหม่** `getSharedItemsInRange(fromDate,toDate)` / `getSharedCostForPond(pondId,fromDate,toDate)` — สูตรเดียวกับค่าไฟ/ค่าแรง (`activePondCountOnDate`) แต่คิดเป็นรายรายการแทนรายวัน (เพราะเป็นเหตุการณ์ไม่ต่อเนื่องแบบบิลรายเดือน): แต่ละรายการ ÷ จำนวนบ่อที่ active ณ วันที่บันทึกรายการนั้น แล้วรวมเฉพาะรายการที่อยู่ในช่วงวันที่เลี้ยงของบ่อนั้น
5. **การ์ด "ต้นทุนสะสม"** ทั้งหน้ารายละเอียดบ่อของ admin (ข้อ 98/99) และชีท CEO — เพิ่มแถว **"อุปกรณ์นอก (แชร์)"** รวมเข้ายอดรวมต้นทุนสะสม กดขยายดูรายละเอียดได้ (ชื่อสินค้า/วันที่/จำนวนบ่อที่แชร์/ส่วนแบ่งของบ่อนี้)
6. **ฟอร์มปิดรอบ** — ช่อง "อุปกรณ์นอก" (`cm-cost-equip-out`) เปลี่ยนจากกรอกเองล้วนๆ เป็น**ดึงจาก `getSharedCostForPond` อัตโนมัติ** (สูตรเดียวกับค่าแรง/ค่าไฟที่ auto-fill อยู่แล้ว) ยังแก้ไขเองได้ — **ไม่แตะ** `editCropModal`/`ecm-cost-equip-out` (แก้ไขรอบที่ปิดแล้ว) ตามหลักการเดิมที่ยืนยันไว้กับค่าแรง (ข้อ 72): ไม่อยากให้ตัวเลขรอบเก่าขยับเองถ้าข้อมูลปัจจุบันเปลี่ยนไป

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง (ไม่ได้ล็อกอิน Firebase จริง — เพิ่งพบว่า `permission_denied` ทำให้ `save()` ปลอดภัย 100% ไม่มีทางเขียนถึง production ได้เลยตอนไม่ได้ล็อกอิน จึงทดสอบ `save()` จริงได้เต็มที่ในรอบนี้): เพิ่มสินค้าจากแคตตาล็อก (หมวดยากิน, 3 ขวด×900฿) + สินค้าพิมพ์เอง (หมวดยาลงน้ำ, 3 ขวด×250฿) ลงตะกร้าโหมด "แชร์ทุกบ่อ" พร้อมกัน → บันทึกสำเร็จ ตัดสต็อกสินค้าจากแคตตาล็อกถูกต้อง (20→17) สินค้าพิมพ์เองไม่แตะสต็อกเลย ทั้งคู่ snapshot หมวด/ราคาถูกต้อง, `getSharedCostForPond` คำนวณตรงกับมือ (3,450÷8 บ่อ=431.25) และให้ค่าเท่ากันทุกบ่อที่ active, การ์ดต้นทุนสะสมทั้งหน้า admin และ CEO โชว่แถว/ยอดรวม/drill-down ตรงกัน 100%, ฟอร์มปิดรอบ auto-fill 431 ถูกต้อง, ทดสอบ validation (ไม่เลือกหมวด/ไม่กรอกราคา) บล็อกถูกต้อง, `removeFromCart` ด้วย `cartId` ทำงานถูกต้องทั้ง 2 ประเภท ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว

### 101. หน้าภาพรวม — การ์ดบ่อ "เตรียมบ่อ" กดแล้วเด้งไปหน้า "เตรียมบ่อ" แทนหน้ารายละเอียดบ่อ

พี่แจ้งว่าการ์ดบ่อสถานะ "เตรียมบ่อ" ในหน้าภาพรวม กดเข้าไปแล้วพาไปหน้ารายละเอียดบ่อ (`sec-ponddetail`) ซึ่งไม่มีประโยชน์เพราะบ่อยังไม่มีกุ้ง — อยากให้กดแล้วไปหน้า "เตรียมบ่อ" (`sec-prep`) ตรงๆ เพื่อให้ทำขั้นตอนต่อไปง่ายขึ้น

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):
- `renderPrepPondCard(p)` เปลี่ยน `onclick` จาก `openPondDetail(id)` เป็น `jumpPrep(id)` (ฟังก์ชันใหม่ ตามแพทเทิร์นเดียวกับ `jumpFeed()`/`jumpEntry()` ที่มีอยู่แล้ว)
- `jumpPrep(pid)` สลับไปแท็บ "เตรียมบ่อ" แล้วเปิดการ์ดของบ่อนั้นอัตโนมัติ (`_prepOpen.add(pid)`) พร้อมเลื่อนหน้าไปหาให้เลย (`scrollIntoView`)
- **เจอเคสที่ต้องแก้เพิ่ม:** บ่อสถานะ "เตรียมบ่อ" บางบ่อ (แค่ตั้ง `startDate` ล่วงหน้าไว้) ยังไม่เคยเริ่มเช็คลิสต์เลย (`p.prepChecklist` เป็น null) ซึ่งหน้า "เตรียมบ่อ" จริงจะไม่แสดงบ่อเหล่านี้เลย (`initPrep()` auto-populate เฉพาะบ่อที่มีข้อมูลเช็คลิสต์แล้วเท่านั้น) ถ้าเด้งไปตรงๆ จะไม่เจอการ์ดปลายทาง — `jumpPrep()` จึงเริ่มเช็คลิสต์ให้อัตโนมัติก่อน (โค้ดเดียวกับ `addPrepPond()`) เฉพาะกรณีที่บ่อนั้นยังไม่อยู่ใน `_prepPonds` เท่านั้น ไม่งั้นข้ามไปเลยไม่เรียก `save()` ซ้ำ
- เพิ่ม `id="prep-card-${pid}"` ให้การ์ดในหน้า "เตรียมบ่อ" (`renderPrepCard()`) เพื่อใช้เป็นเป้าหมาย scroll

ทดสอบผ่าน local server ด้วยข้อมูลจริง — เนื่องจากบางกรณีต้องเรียก `save()` จริง (เริ่มเช็คลิสต์ให้บ่อที่ยังไม่มี) จึง stub ฟังก์ชัน `save()` ชั่วคราวระหว่างทดสอบเพื่อไม่ให้เขียนข้อมูลทดสอบลง Firebase จริง (ยืนยันว่า mock ทำงานแทนของจริงสำเร็จก่อนทดสอบ): กดการ์ดบ่อ A21 (เตรียมบ่อ, ยังไม่มีเช็คลิสต์มาก่อน) จากหน้าภาพรวมด้วย `.click()` จริง → เด้งไปหน้า "เตรียมบ่อ" ถูกต้อง, การ์ดของบ่อนั้นถูกสร้างขึ้นมาใหม่+เปิดอัตโนมัติ+เลื่อนไปหาถูกต้อง (`save()` ถูกเรียก 1 ครั้งตามคาด), ทดสอบกดซ้ำครั้งที่ 2 (บ่อนี้อยู่ใน `_prepPonds` แล้ว) → ไม่เรียก `save()` ซ้ำ (0 ครั้ง) ถูกต้อง ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว

### 100. 🔴 แก้บั๊ก: กราฟผสม (แท่ง+เส้น) หลายจุดในแอป เส้นถูกแท่งบังจนมองไม่เห็น

พี่แจ้งว่ากราฟที่มีทั้งแท่งและเส้นในข้อมูลเดียวกัน เส้นกราฟมองไม่เห็นเพราะถูกแท่งบัง — ไล่หาทุกจุดในแอปที่ใช้ Chart.js สร้างกราฟผสม (bar+line ในชาร์ตเดียวกัน) เจอ 5 จุด แบ่งเป็น 2 กลุ่ม:

**ต้นตอบั๊ก:** Chart.js วาด dataset ตามค่า `order` (ค่าเริ่มต้น 0 ทุก dataset) เรียงจากน้อยไปมาก แต่ถ้า `order` เท่ากัน จะเรียงตาม index ในอาร์เรย์ — จุดสำคัญคือ **Chart.js วาดจริงจากลำดับ "มาก→น้อย" (ย้อนกลับ)** ทำให้ dataset ที่มี `order`/index **น้อยที่สุด** ถูกวาด**ทีหลังสุด**และอยู่**บนสุด** — โค้ดเดิมส่วนใหญ่เขียนแท่งไว้ก่อน (index 0) แล้วเส้นไว้ทีหลัง (index 1) โดยไม่ตั้งค่า `order` เลย ผลคือแท่ง (index น้อยกว่า) ถูกวาดทีหลังและทับเส้นสนิท — ตรงกันข้ามกับที่คนเขียนโค้ดตั้งใจ (คิดว่าเรียงแบบเดิมเส้นจะอยู่บนเพราะเขียนทีหลัง)

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`) — เพิ่ม `order:2` ให้ dataset ที่เป็นแท่ง และ `order:1` ให้ dataset ที่เป็นเส้น (ค่าน้อยกว่า = วาดทีหลัง = อยู่บนสุด) ใน 3 จุดที่ยังไม่เคยตั้งค่า order เลย:
1. หน้ารายละเอียดบ่อ — กราฟ "🍤 อาหาร vs การเติบโต รายสัปดาห์" (แท่งอาหาร/สัปดาห์ + เส้น ADG)
2. หน้าค่าไฟฟ้า — กราฟ "แนวโน้มค่าไฟ" (แท่งบิลรวมทั้งฟาร์ม + เส้นอัตรา/บ่อ/วัน, ข้อ 78)
3. PDF ปิดบ่อ — กราฟ "เทียบกับรอบก่อนหน้าของบ่อนี้" ในโหมด "ทุกบ่อ" (แท่ง FCR + เส้นอัตรารอด)

**จุดที่ตรวจแล้วถูกต้องอยู่แล้ว (ไม่ต้องแก้)** — มี 2 จุดที่ตั้งค่า order ไว้แล้วตั้งแต่ต้น และถูกทิศทางพอดี (เส้น order ต่ำกว่าแท่ง): กราฟ "เทียบกับรอบก่อนหน้าของบ่อนี้" ในหน้า PDF ปิดบ่อ (`renderExChart`, แท่ง FCR/Yield order 2/3 + เส้นอัตรารอด order 1) และกราฟ "กำไร-ต้นทุน เทียบรอบก่อนหน้า" ใน PDF โหมดบ่อเดียว (แท่งรายได้/ต้นทุน order 2 + เส้นกำไรสุทธิ order 1)

ทดสอบผ่าน local server ด้วยข้อมูลจริง (read-only, ไม่ `save()`): กราฟ "อาหาร vs การเติบโต" หน้ารายละเอียดบ่อ — เส้น ADG ขึ้นเหนือแท่งชัดเจนถูกต้อง (ก่อนแก้เส้นหายไปข้างหลังแท่งสนิท), กราฟ "แนวโน้มค่าไฟ" — เส้นอัตรา/บ่อ/วันขึ้นเหนือแท่งบิลรวมถูกต้อง, กราฟ PDF 'compare' — ทดสอบแยกด้วย canvas จำลองข้อมูลเดียวกัน (ไม่กระทบข้อมูลจริง เพราะต้องมีรอบปิดแล้ว ≥2 รอบถึงจะเรียกได้จริงในแอป) ยืนยันเส้นสีเขียวขึ้นเหนือแท่งสีน้ำเงินถูกต้องด้วยค่า order เดียวกัน ไม่มี console error, sync สองไฟล์แล้ว

### 99. เพิ่มประมาณต้นทุนบาท/กก. ของแต่ละบ่อในการ์ด "ต้นทุนสะสม" (หน้ารายละเอียดบ่อ + ชีทรายละเอียดบ่อของ CEO)

พี่ถามว่าประมาณต้นทุนบาท/กิโลของแต่ละบ่อ ณ วันนั้นได้ไหม ถามชัดก่อนทำว่าหมายถึงต้นทุนสะสมทั้งหมด÷น้ำหนักตอนนี้ หรือต้นทุนเฉพาะวันนั้น÷น้ำหนักที่โตขึ้นวันนั้น — พี่ยืนยัน**แบบแรก** (ต้นทุนสะสมทั้งหมด ÷ Biomass ประเมินตอนนี้) จากนั้นพี่ขอเพิ่มว่าต้องการให้ role **CEO** เห็นข้อมูลนี้ด้วย ไม่ใช่แค่ admin

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`) — เพิ่มแถว "ต้นทุน/กก. ประมาณ (ณ วันนี้)" สูตรเดียวกัน (`ต้นทุนสะสม ÷ Biomass ประเมิน`) ใน 2 จุด:
1. **หน้ารายละเอียดบ่อ** (`renderPondDetail()`, admin เท่านั้น, การ์ด "ต้นทุนสะสม" จากข้อ 98) — คำนวณจาก `pdCostTotal ÷ biomass` (ตัวแปร `biomass` เดียวกับที่ใช้ใน kpiHero — `getLatestBiomass(p)` ไม่คำนวณ Biomass ซ้ำ)
2. **ชีทรายละเอียดบ่อของ CEO** (`ceoOpenSheet()`, บล็อก "ต้นทุนสะสม") — คำนวณจาก `totalCost ÷ bio` (ตัวแปร `bio` เดียวกับที่ใช้แสดง "สรุปบ่อ" ด้านบนในชีทเดียวกัน) วางต่อท้ายแถว "รวมต้นทุนสะสม"

ทั้ง 2 จุดถ้าบ่อยังไม่มีข้อมูลสุ่มตัวอย่างเลย (Biomass เป็น null) โชว่ "-" แทนเดาค่า — เป็น**ค่าประมาณระหว่างเลี้ยง ไม่ใช่ต้นทุน/กก. สุดท้ายตอนปิดรอบ** (ตัวหลังคำนวณจากน้ำหนักจับจริงตอนปิดรอบ คนละค่ากันโดยตั้งใจ ดูข้อ 27/29) — role staff ไม่เห็นทั้ง 2 จุดนี้เลย (หน้ารายละเอียดบ่อกันด้วย `APP_ROLE==='admin'` เดิม, CEO เข้าถึงชีทของตัวเองอยู่แล้วผ่าน `showCeoOnly()`)

ทดสอบผ่าน local server ด้วยข้อมูลจริง (read-only, ไม่ `save()`): บ่อ A.1 ต้นทุนสะสม ฿1,170,756 ÷ Biomass ประเมิน 11,392 กก. = ฿102.8/กก. ตรงกันทั้ง 2 จุด (หน้ารายละเอียดบ่อของ admin และชีท CEO) ตรงกับคำนวณมือ, บ่อที่ยังไม่มีข้อมูลสุ่มตัวอย่างขึ้น "-" พร้อมข้อความอธิบายถูกต้อง ไม่มี console error, sync สองไฟล์แล้ว

### 98. role admin เห็นต้นทุนสะสมรายบ่อในหน้ารายละเอียดบ่อ + เปลี่ยน KPI card "บ่อต้องดู" ในหน้าภาพรวมเป็นต้นทุนสะสมรวมฟาร์ม (เฉพาะ admin)

พี่ขอ 2 อย่าง: (1) role admin อยากเห็นต้นทุนแต่ละบ่อในหน้ารายละเอียดบ่อ เหมือนกับที่ role CEO เห็นอยู่แล้ว (2) เปลี่ยนการ์ด KPI "บ่อที่ต้องดู" ในแถบบนสุดของหน้า "ภาพรวม" เป็น "ต้นทุนสะสมรวมของทั้งฟาร์มตอนนี้" — ขอ mockup ก่อนตามที่พี่สั่ง

**จุดที่ต้องถามก่อนทำ:** หน้า "ภาพรวม" (`sec-overview`) เป็นหน้าที่ staff เห็นได้ด้วย (ไม่ได้อยู่ใน `FINANCE_TABS`/`FINANCE_SECS` ที่ซ่อนจาก staff) ถ้าเปลี่ยนการ์ด KPI เป็นต้นทุนรวมฟาร์มตรงๆ จะขัดกับกฎที่เพิ่งยืนยันไปในข้อ 96 (staff ห้ามเห็นข้อมูลการเงิน) — ถามพี่แล้วยืนยันว่า **"ซ่อนจาก staff เห็นเฉพาะ admin/CEO"** staff ยังเห็นการ์ด "⚠️ บ่อต้องดู" แบบเดิมทุกประการ

ทำ mockup ให้ดูก่อน ([mockup_ต้นทุนบ่อ_admin+KPIรวมฟาร์ม.html](../Mockup และ Prototype/mockup_ต้นทุนบ่อ_admin+KPIรวมฟาร์ม.html)) พี่ขอให้ย้ายการ์ดต้นทุนในหน้ารายละเอียดบ่อขึ้นไปอยู่บนสุด แก้ mockup แล้วพี่ยืนยันให้ implement เข้าแอปจริง

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):

1. **หน้า "รายละเอียดบ่อ" (`renderPondDetail()`)** — เพิ่มการ์ดใหม่ "💰 ต้นทุนสะสม" (มีป้าย "ADMIN เท่านั้น") วางไว้**บนสุด**ของเนื้อหา (ต่อจากชิปอายุบ่อ ก่อนแบนเนอร์แจ้งเตือน/Farm Card) แสดงเฉพาะ `APP_ROLE==='admin'` และไม่ใช่บ่อที่กำลังเตรียม (`cls!=='prep'`) — คำนวณด้วย **building block ชุดเดียวกับที่หน้า CEO ใช้อยู่แล้ว** (`getStockItemsForPond`, `pondFeedCost`, `p.costFry`, `getLaborCostForPond`, `getElecCostForPeriod`) ไม่สร้างสูตรคำนวณคู่ขนานใหม่ — แยกตามหมวดเหมือนหน้า CEO เป๊ะ (อาหาร/ยากิน/ยาลงน้ำ/จุลินทรีย์กิน/จุลินทรีย์ลงน้ำ/ปูน/อุปกรณ์/ลูกกุ้ง/ค่าแรง/ค่าไฟ) หมวดที่มีรายการสินค้าจริงกดแตะขยายดูรายละเอียดได้ (`pdToggleCatItems()`) รีใช้ CSS class ของหน้า CEO ตรงๆ (`.ceo-item-row`/`.ceo-cat-row`/`.ceo-cat-items` ฯลฯ) ไม่สร้าง CSS ซ้ำ
2. **หน้า "ภาพรวม" (`renderOv()`, `ov-kpi-bar`)** — การ์ด KPI ใบที่ 4 เปลี่ยนเป็น role-conditional: **admin** เห็น "💰 ต้นทุนสะสมรวมทั้งฟาร์ม" (ผลรวม `ceoTotalCost(p)` ของทุกบ่อที่กำลังเลี้ยงอยู่ — ฟังก์ชันเดียวกับหน้า CEO) **staff** ยังเห็น "⚠️ บ่อต้องดู" (จำนวนบ่อ danger/warn) เหมือนเดิมทุกประการ ไม่มีการเปลี่ยนแปลงใดๆ สำหรับ staff — ข้อมูลจำนวนบ่อต้องดูของ staff ยังดูซ้ำได้จากตาราง "สถานะบ่อทั้งหมด" ด้านล่างอยู่แล้ว ไม่ได้หายไปไหน (CEO ไม่ได้เข้าหน้านี้อยู่แล้วเพราะมีแดชบอร์ดของตัวเองแยกต่างหาก `showCeoOnly()`)

ทดสอบผ่าน local server ด้วยข้อมูลจริงที่มีอยู่ (26 บ่อ, ไม่มี `save()`/mutate ข้อมูลใดๆ เพราะ browser tab เชื่อมกับ Firebase บัญชีจริงอยู่ — ใช้แค่ read-only function calls): `applyRole('admin')`→`renderOv()` การ์ด KPI ใบที่ 4 ขึ้น "💰 ต้นทุนสะสมรวมทั้งฟาร์ม" ถูกต้อง (3.29 ล้าน฿ จาก 4 บ่อที่กำลังเลี้ยง), สลับ `applyRole('staff')` การ์ดกลับเป็น "⚠️ บ่อต้องดู" เหมือนเดิมไม่มีร่องรอยของการ์ดต้นทุน, เปิดหน้ารายละเอียดบ่อ (`openPondDetail('B1')`) ด้วย role admin การ์ดต้นทุนขึ้นบนสุดถูกต้อง (฿1,170,756) ตรงกับ `ceoTotalCost(p)` เป๊ะ 100%, กดแถวหมวด "ยาลงน้ำ" ขยาย/ยุบ list สินค้าได้จริงผ่าน `.click()` event, สลับ role staff การ์ดหายไปสมบูรณ์, ทดสอบบ่อที่กำลังเตรียม (`A21`) การ์ดไม่แสดงถูกต้อง ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว

### 97. นำข้อมูลจากอินโฟกราฟิกแร่ธาตุ/ไนไตรท์ที่พี่ส่งมาเช็ค (24/7/69) มาปรับใช้ — เกณฑ์ Mg/Ca ตามความเค็ม + สัญญาณไนไตรท์เสี่ยงเพิ่มที่ความเค็มต่ำ + เตือน Bottom DO + เตือนกุ้งช็อกตอนถ่ายน้ำ

พี่ส่งอินโฟกราฟิก 6 ภาพเรื่องแร่ธาตุ/ไนไตรท์/ความเค็ม/อุณหภูมิมาให้เช็คว่าขัดแย้งกับข้อมูลเดิม (PDF "ไนไตรท์สะสม" ของ ดร.พุทธ) ไหม และมีอะไรเอามาปรับใช้กับตะวันฟาร์มได้บ้าง — ตรวจแล้วไม่ขัดแย้งกัน (อินโฟกราฟิกใหม่อธิบายกลไกละเอียดกว่าของ PDF เดิมที่พูดไว้กว้างๆ) แต่เจอว่า**เกณฑ์ Mg/Ca ในแอปเป็นค่าคงที่ตัวเดียว ไม่ปรับตามความเค็ม ทั้งที่ตารางแร่ธาตุจริงบอกว่าความต้องการต่างกันได้หลายเท่าตัวระหว่างความเค็มต่ำ-สูง** — พี่เลือกให้ทำ 4 ใน 5 ข้อที่เสนอ (ข้าม Potassium เพราะฟาร์มไม่มีเครื่องมือวัด)

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):

1. **เกณฑ์ Mg/Ca ปรับตามความเค็มที่วัดได้จริงของบ่อนั้น** — เพิ่มตาราง `MINERAL_BY_SALINITY` (38 แถว ความเค็ม 3–40 ppt พร้อมช่วง Mg/Ca ตามอินโฟกราฟิก) + ฟังก์ชัน `mineralRangeAtSalinity(sal)` (interpolate ระหว่างแถวที่ใกล้ที่สุด) — `st(key,v,rec)` เพิ่มพารามิเตอร์ `rec` (record/ค่าที่กำลังกรอกอยู่ทั้งชุด) ถ้ามี `rec.sal` และ key เป็น `mg_`/`ca` จะคำนวณช่วงยอมรับจากความเค็มแทนค่าคงที่ของ `PARAMS` (ไม่มีข้อมูลความเค็ม fallback ไปใช้เกณฑ์เดิมทุกประการ ไม่กระทบพารามิเตอร์อื่น) — อัปเดตทุกจุดที่เรียก `st()` ให้ส่ง record เข้าไปด้วย (ตารางประวัติคุณภาพน้ำ, heatmap เปรียบเทียบ, ตาราง "น้ำ-อาหาร") ยกเว้นลูกศรเทียบค่าก่อน-หลัง (`wqArrow`) ที่ยังใช้เกณฑ์เดิมตามเดิม (เป็นแค่ตัวชี้ทิศทาง ไม่ใช่ตัวตัดสินสถานะหลัก)
   - หน้า "บันทึกคุณภาพน้ำ" (`onFI`) กรอก Mg/Ca ตอนนี้จะดูค่าความเค็มที่กรอกในฟอร์มเดียวกันแบบสด (ไม่ต้องกดบันทึกก่อน) ป้าย "ช่วงเกณฑ์ปกติ" เปลี่ยนเป็น "ตามความเค็มที่กรอก" พร้อมช่วงตัวเลขจริง — ถ้ากรอกความเค็มทีหลัง Mg/Ca ที่กรอกไว้แล้วจะรีเฟรชสีให้เองอัตโนมัติ ไม่ต้องพิมพ์ซ้ำ
   - **Hardness ไม่ได้ปรับ** เพราะตารางต้นฉบับให้มาเป็นค่ากลางค่าเดียวต่อความเค็ม ไม่ใช่ช่วง lo-hi ชัดเจนแบบ Mg/Ca — ไม่อยากเดาช่วงยอมรับเอง คงเกณฑ์คงที่เดิมไว้ก่อน
2. **สัญญาณไนไตรท์เสี่ยงเพิ่มที่ความเค็มต่ำ** — เพิ่มใน `pondRiskDisease()` (Farm Card): ถ้าความเค็ม<5 ppt และไนไตรท์≥0.2 mg/L → เฝ้าระวัง, ≥0.5 mg/L → อันตราย (อ้างอิงเกณฑ์ปลอดภัยที่ 1 ppt จากอินโฟกราฟิก) — เป็นสัญญาณเสริมเพิ่มเข้าไปเท่านั้น **ไม่ได้แก้เกณฑ์ NO2 พื้นฐานของ `classifyOv()`** (ยืนยันไว้แล้ว 16 มิ.ย. 69 ใช้กับทั้งแอป ไม่แตะ)
3. **เตือน Bottom DO** — ใน `pondRiskDisease()` เดียวกัน: ถ้าไนไตรท์อยู่ในระดับเฝ้าระวัง/อันตราย (ตามเกณฑ์ `classifyOv` เดิม) แต่ DO ผิวน้ำ (เช้าหรือบ่าย) ยังปกติ → ขึ้นข้อความ "ไนไตรท์สูงแต่ DO ผิวน้ำปกติ — อาจเป็นสัญญาณว่าพื้นบ่อขาดออกซิเจน (Bottom DO)" (อ้างอิง PDF เดิม + อินโฟกราฟิกใหม่ที่อธิบายว่าไนตริฟิเคชันขั้น 2 ต้องใช้ออกซิเจนที่พื้นบ่อ)
4. **เตือนกุ้งช็อกตอนถ่ายน้ำ** — หน้า "จัดการน้ำ" เลือก "เปลี่ยนถ่ายน้ำ" ขึ้นข้อความเตือนถาวร "ถ่ายน้ำปริมาณมากรวดเดียว เสี่ยงกุ้งช็อกถ้าน้ำใหม่อุณหภูมิ/ความเค็มต่างจากบ่อเดิมเกิน 5°C" — เป็นคำเตือนทั่วไป**ไม่ใช่การคำนวณเทียบจริง** เพราะแอปไม่มีข้อมูลอุณหภูมิ/ความเค็มของน้ำที่สูบเข้ามาใหม่ (ไม่ได้วัดตอนสูบเข้า และช่อง "ปริมาณ" ก็ถูกตัดไปแล้วตั้งแต่ข้อ 64) — เลือกการดำเนินการอื่น (ระบายน้ำ/บำบัดน้ำ) ข้อความจะซ่อนอัตโนมัติ

**ข้าม (ตามที่พี่ยืนยัน): เพิ่มฟิลด์ Potassium (K+)** — ฟาร์มไม่มีเครื่องมือวัด เพิ่มช่องกรอกไปก็ไม่มีใครกรอกได้จริง

ทดสอบผ่าน local server: `mineralRangeAtSalinity()` ตรงกับตารางต้นฉบับทุกจุดยึด (3/15/40 ppt) และ interpolate ค่ากลางถูกต้อง (15.5 ppt), `st('mg_',v,rec)` ให้ผลต่างกันถูกต้องระหว่างมี/ไม่มีข้อมูลความเค็ม (ค่าเดียวกัน 650 mg/L: ok ที่ 15ppt แต่ warn ถ้าไม่รู้ความเค็ม), `pondRiskDisease()` ทดสอบ 4 เคส (ความเค็มต่ำ+ไนไตรท์ปานกลาง→warn, ความเค็มต่ำมาก+ไนไตรท์สูง→danger, ไนไตรท์สูง+DOผิวน้ำปกติ→เตือน Bottom DO, ค่าปกติทั้งหมด→ok) ถูกต้องครบ, ฟอร์มบันทึกคุณภาพน้ำพิมพ์ Mg ก่อนความเค็มแล้วพิมพ์ความเค็มทีหลัง ป้าย/สีรีเฟรชอัตโนมัติถูกต้อง, สลับ "การดำเนินการ" ในหน้าจัดการน้ำ ข้อความเตือนโชว์/ซ่อนถูกต้อง, เดินผ่านหน้าหลักทั้งหมด (ภาพรวม/น้ำ-อาหาร/เปรียบเทียบ/รอบเลี้ยง/ความเสี่ยง) ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว

### 96. 🔴 role staff ยังเปิด "ปิดรอบ" เห็น/แก้ข้อมูลการเงินได้ + ดาวน์โหลด PDF การเงินได้ (หลุดจากรายการหน้าที่ซ่อนไว้)

พี่ขอให้เช็คอีกทีว่า role staff เห็นจำนวนเงิน/กำไรขาดทุน/ต้นทุน และแก้ไขข้อมูลสำคัญไม่ได้จริงไหม — ไล่ตรวจทั้งแอปแล้วพบว่ากลไกหลัก (`FINANCE_TABS`/`FINANCE_SECS` ใน `applyRole()`) ซ่อนทั้งหน้า "จับกุ้ง/สถิติจับกุ้ง/PDF ปิดบ่อ/ค่าแรง/ค่าไฟฟ้า/P&L/จัดการบ่อ" ถูกต้องอยู่แล้ว และหน้าสต็อก/รายงานยอดขายก็ซ่อนราคาต้นทุน-กำไรจาก staff ถูกต้อง (เช็คแล้วว่าเป็นการซ่อนระดับ logic ไม่ใช่แค่ CSS — `renderSalesReport()` ไม่ render ค่าเหล่านี้ลง HTML เลยถ้า `APP_ROLE==='staff'`)

**แต่เจอ 2 จุดหลุดจริง** — ทั้งคู่อยู่ในหน้า **"รอบเลี้ยง" (`sec-crops`)** ซึ่งไม่ได้อยู่ใน `FINANCE_TABS`/`FINANCE_SECS` (ตั้งใจให้ staff ดูตารางสรุปรอบเลี้ยง/ประวัติได้ปกติ) แต่ 2 องค์ประกอบข้างในกลับไม่มีการเช็ค role เลย:

1. **ปุ่ม "ปิดรอบ"** (เปิด `cropModal`) — ไม่มีการเช็ค `APP_ROLE` เลยแม้แต่นิดเดียว (จุดเดียวในตารางที่ลืมใส่ ทั้งที่ปุ่ม "แก้ไข"/"ลบ" ข้างๆ ในตาราง "ประวัติรอบเลี้ยง" มีการเช็ค `APP_ROLE==='admin'` ถูกต้องอยู่แล้ว) — พอกดเข้าไป staff จะเห็น**และแก้ไขได้ทั้งหมด**: รายได้รวม, ต้นทุน 9 หมวด (อาหาร/ลูกกุ้ง/เวชภัณฑ์/ปูน/ค่าแรง/ค่าไฟ/น้ำมัน/อุปกรณ์/ค่าจับกุ้ง), ส่วนแบ่งกำไร (คนเลี้ยง/ผู้จัดการ), และ**กำไรสุทธิ** — แล้วกด "ยืนยันปิดรอบ" ปิดรอบเลี้ยงจริงได้เลย (การกระทำที่ย้อนกลับไม่ได้ ล้างข้อมูลรายวันเริ่มรอบใหม่ทันที)
2. **การ์ด "🔍 ดูข้อมูลย้อนหลังบ่อที่ปิดแล้ว"** — ช่อง "ยอดขายสุทธิ (บาท)" แสดงให้ทุก role เห็น และปุ่ม "📄 ดาวน์โหลด PDF สรุปรอบนี้" (`exportChPdf`) เรียก `genPDF()` ตัวเดียวกับหน้า "PDF ปิดบ่อ" ที่ถูกซ่อนไว้แล้ว — เท่ากับ staff เลี่ยงหน้าที่ถูกซ่อนแล้วดาวน์โหลด PDF ที่มีต้นทุนแยกหมวด+กำไรสุทธิ+Margin ครบผ่านทางอ้อมได้

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):
- ปุ่ม "ปิดรอบ" ในตาราง "สรุปรอบเลี้ยงปีนี้" เพิ่มเงื่อนไข `APP_ROLE==='admin'` เข้าไปคู่กับเงื่อนไขเดิม (`isCurYear&&hasStarted`) — staff จะไม่เห็นปุ่มนี้เลย (ปิดรอบเป็นสิทธิ์ Admin เท่านั้น เหมือนปุ่ม "แก้ไข"/"ลบ" ข้างๆ ที่ทำถูกอยู่แล้ว)
- การ์ด "ดูข้อมูลย้อนหลังบ่อที่ปิดแล้ว" (`renderClosedDetail()`) — การ์ด "ยอดขายสุทธิ" และปุ่ม "ดาวน์โหลด PDF สรุปรอบนี้" แสดงเฉพาะ `APP_ROLE==='admin'` เท่านั้น (การ์ด ผลผลิต/อัตรารอด/FCR/ระยะเลี้ยง และกราฟการเติบโต/อาหาร/ข้อมูลดิบ ที่ไม่ใช่ข้อมูลการเงิน ยังเห็นได้ปกติทุก role เหมือนเดิม)

**ตรวจแล้วไม่พบปัญหาเพิ่ม** (จุดที่เช็คแล้วปลอดภัย): หน้า "สต็อก" — ตาราง "รายการพัสดุ" ซ่อนคอลัมน์ราคา/มูลค่าถูกต้องสำหรับ staff, "เบิกออกไปบ่อ" ไม่แสดงราคาให้ใครเห็นเลยไม่ว่า role ไหน, ปุ่ม "📄 PDF" ในตารางประวัติรอบเลี้ยง (`goPdfForCrop`) แม้ staff กดได้แต่พาไปหน้า `sec-export` ที่ถูกซ่อนด้วย `style.display:none` (inline style ชนะกฎ `.sec.on{display:block}` ในสไตล์ชีทเสมอเพราะไม่มี `!important` — ตรวจสอบ cascade แล้วยืนยันไม่มีทางหลุด)

**ข้อจำกัดที่ต้องบอกพี่ (ไม่ใช่บั๊กใหม่ เป็นสถาปัตยกรรมเดิมของแอปนี้ตั้งแต่ต้น — ดูข้อ 7):** การซ่อนทั้งหมดนี้เป็นการซ่อนระดับ UI/client-side เท่านั้น ถ้า staff เปิด DevTools แล้วเรียกฟังก์ชัน (เช่น `openCropModal('B1')`) ตรงๆ ผ่าน console ก็ยังเข้าถึงข้อมูล/ฟังก์ชันได้อยู่ดี เพราะข้อมูลทั้งหมดโหลดมาที่ฝั่ง client แล้วตั้งแต่ต้น การป้องกันที่แท้จริงต้องทำที่ Firebase Security Rules ไม่ใช่โค้ดหน้าเว็บ — เป็นข้อจำกัดที่มีมาแต่ต้นของสถาปัตยกรรมแอปนี้ (single-file client app) ไม่ใช่สิ่งที่แก้ได้ในรอบนี้

ทดสอบผ่าน local server ด้วย `applyRole('staff')` จำลอง: ปุ่ม "ปิดรอบ" หายไปจากตาราง (`hasCloseBtn:false`), การ์ด "ยอดขายสุทธิ" และปุ่มดาวน์โหลด PDF หายไปจากการ์ดดูข้อมูลย้อนหลัง (`hasNetSalesLabel:false`, `hasPdfBtn:false`) — สลับกลับ `applyRole('admin')` ยืนยันทั้ง 3 อย่างกลับมาแสดงถูกต้องเหมือนเดิม (ไม่มี regression), เดินเมนู staff ผ่านทุกหน้าหลัก (ภาพรวม/รายละเอียดบ่อ/อาหาร/สต็อก/เปรียบเทียบ/เตรียมบ่อ) ไม่มี console error, ยืนยัน `FINANCE_SECS` ทั้ง 7 หน้ายัง `display:none` ถูกต้องสำหรับ staff ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว

### 95. หน้า "ทดลองเวชภัณฑ์" — เพิ่มช่วงเวลาที่ทดลอง กรองทุกตัวชี้วัดให้เหลือเฉพาะข้อมูลในช่วงนั้น

พี่แจ้งว่าการ์ดเปรียบเทียบเวชภัณฑ์ข้ามบ่อ (ข้อ 74-75, 79) ต้องการช่องกรอก "ช่วงเวลาที่ทำการทดลอง" แล้วดึงข้อมูลการเลี้ยงของช่วงนั้นมาเปรียบเทียบ — ตรวจโค้ดเดิมพบว่าทุกตัวชี้วัด (FCR/กุ้งตาย/ผลแล็บ/ค่าน้ำ) ดึงจาก **"ล่าสุด" หรือ "สะสมทั้งรอบ"** ทั้งหมด ไม่เคยกรองตามช่วงเวลาที่ทดลองจริงเลย — ถ้าทดลองแค่บางช่วงกลางรอบ ตัวเลขจะปนข้อมูลก่อน/หลังช่วงทดลองไปด้วย ไม่ใช่ผลของการทดลองล้วนๆ ทำ mockup ให้ดูก่อน ([mockup_ทดลองเวชภัณฑ์_ช่วงเวลา.html](../Mockup และ Prototype/mockup_ทดลองเวชภัณฑ์_ช่วงเวลา.html)) พี่ยืนยันแล้วให้ implement เข้าแอปจริงตามนั้น

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):
- เพิ่มช่อง **"วันที่เริ่ม–สิ้นสุดทดลอง"** ในการ์ดตั้งชื่อการทดลอง พร้อมปุ่ม "ยังดำเนินอยู่ (ตั้งเป็นวันนี้)" และป้ายบอกจำนวนวัน — `_trialDraft`/`D.medTrials[]` เพิ่มฟิลด์ `startDate`/`endDate` (บันทึก/โหลดคู่กับชื่อทดลองปกติ)
- ฟังก์ชันใหม่ `trialResolvedRange()`/`trialClamp()`/`trialPondRange(p)` — คำนวณช่วงวันที่ "ที่มีข้อมูลจริง" ของแต่ละบ่อ (ตัดขอบอัตโนมัติถ้าทดลองเริ่มก่อนบ่อนั้นลงกุ้ง หรือเกินวันนี้ พร้อมป้ายเตือน) และแปลงเป็น DOC ของบ่อนั้นให้ (แต่ละบ่อลงกุ้งคนละวัน ช่วงปฏิทินเดียวกันจึงตรงกับอายุกุ้งคนละช่วง)
- ตัวชี้วัดทั้งหมดเปลี่ยนมาคำนวณเฉพาะในช่วง: `trialAdgInRange`/`trialFcrInRange` (feed ในช่วง ÷ biomass ที่เพิ่มขึ้นในช่วง จากตัวอย่างสุ่มใกล้ขอบช่วงที่สุด)/`trialMortalityPctInRange`/`trialWaterAvgInRange` — ทุกตัวกรองด้วย helper กลาง `trialInRange()`
- ผลแล็บ (`trialLabInRange`) — ถ้ามีผลตรวจอยู่ในช่วงพอดีใช้ค่านั้น ถ้าไม่มีจะ fallback ไปใช้ค่าล่าสุด**ก่อน**ช่วงพร้อมข้อความเตือนชัดเจน (การ์ดสรุปด้านบนตารางระบุชื่อบ่อที่ใช้ fallback, ตารางรายละเอียดติดดอกจัน `*` กำกับ) — ไม่ใช่กล่องดำ
- กราฟการเติบโตเปลี่ยนจากแกน DOC เป็น**แกนวันที่ปฏิทิน** (custom Chart.js plugin วาดแถบสีอ่อนไฮไลต์ช่วงทดลองบนกราฟ ไม่ต้องพึ่ง library เสริม) พร้อมชิป "ADG ในช่วงทดลอง" ต่อบ่อใต้กราฟ
- ตารางรายละเอียดเพิ่มคอลัมน์ "ช่วงที่มีข้อมูลจริง" (พร้อมข้อความ "ตัดขอบตามข้อมูลจริง" ถ้าทดลองเกินขอบเขตข้อมูลบ่อนั้น) และ "DOC ในช่วง"

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 3 บ่อ (ลงกุ้งคนละวัน จำลองอาหาร/สุ่มตัวอย่าง/กุ้งตาย/ค่าน้ำ/ผลแล็บรายวันด้วยสูตร feeding rate เดียวกับข้อ 94): ตั้งช่วงทดลอง 20 มิ.ย.–4 ก.ค. → DOC ที่คำนวณได้ต่อบ่อตรงกับที่ mockup ทำนายไว้เป๊ะ (17–31/19–33/23–37), กราฟกะบานสีไฮไลต์ถูกตำแหน่ง, FCR ในช่วงคำนวณได้ค่าสมเหตุสมผล (1.09-1.19), ทดสอบ fallback ผลแล็บ (เลื่อนช่วงให้ไม่มีผลตรวจในนั้น) ขึ้นข้อความเตือนถูกต้องครบทุกบ่อ, ทดสอบ clamp (ตั้งวันเริ่มก่อนบ่อลงกุ้ง) ขึ้นป้ายเตือน "ก่อนวันลงกุ้งบ่อนี้" ถูกต้อง, ปุ่ม "ยังดำเนินอยู่" ทำงานถูกต้อง, บันทึก/โหลดการทดลองกลับมาได้ค่าวันที่ครบถูกต้อง (round-trip), validate บังคับกรอกวันที่เริ่มก่อนบันทึกทำงานถูกต้อง, สลับไปหมวดเปรียบเทียบอื่น (คุณภาพน้ำ) ยังทำงานปกติไม่มี regression ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว

### 94. การ์ด "เปรียบเทียบ Biomass 2 วิธี" — เปลี่ยน Feeding Rate จากตารางกะคร่าวๆ เป็นสูตรจริงจากเอกสาร ดร.พุทธ

พี่ขอให้เช็คว่าค่า Feeding Rate ที่แอปใช้อยู่ (ในการ์ด "เปรียบเทียบ Biomass 2 วิธี" ข้อ 55) เป็นค่ากลางที่ถูกต้องไหม เทียบกับเอกสาร/แหล่งข้อมูล — ของเดิม `bcFeedRateSuggestion(abw)` เป็นตาราง 5 ช่วงกะคร่าวๆ ที่ระบุไว้ในโค้ดเองแล้วว่า "ยังไม่ใช่ตารางจากเอกสารฉบับเต็ม รอข้อมูลเพิ่ม" (ข้อ 55)

**ไล่เช็คเอกสาร ดร.พุทธ ส่องแสงจินดา ที่มีอยู่ในเครื่อง** (`AI กับการเลี้ยงกุ้ง/Precision feeding ในการเลี้ยงกุ้งขาว.pdf`) พบสูตรคำนวณ Feeding Rate ตัวจริงอยู่ในหน้า 16 — เป็นสมการถดถอย (regression) จากข้อมูลภาคสนาม ขึ้นกับ **ทั้งอายุกุ้งและน้ำหนักกุ้งพร้อมกัน** (ไม่ใช่แค่น้ำหนักอย่างเดียวแบบตารางเดิม):
```
Feeding rate (%) = e^(4.055 − 0.799×ln(อายุกุ้ง วัน) + 0.129×ln(น้ำหนักกุ้ง กรัม))
```
ตรวจสอบสูตรกับตัวอย่างคำนวณในเอกสารแล้วตรง (DOC 60 วัน, ABW 15.4 กรัม → 3.09% ตรงกับที่เอกสารโชว์เป๊ะ)

**เทียบตารางเดิมกับสูตรจริง** (ใช้เส้นโค้งอายุ→ABW มาตรฐานที่แอปมีอยู่แล้ว `getAbwTarget`) พบว่าช่วง DOC 42 วันขึ้นไป (ABW ≥ ~10 กรัม) ตารางเดิมใกล้เคียงสูตรจริงพอสมควร แต่ช่วง **DOC 7–35 วัน (กุ้งเล็ก) คลาดเคลื่อนชัดเจน** โดยเฉพาะ DOC 28–35 ที่สูตรจริงบอกว่าควรลดอัตราลงมาที่ ~4.3–5% แล้ว แต่ตารางเดิมยังแนะนำสูงถึง 5–8% (เสี่ยงแนะนำให้อาหารเกินในช่วงนี้)

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`) — แทนที่ `bcFeedRateSuggestion(abw)`/`bcFeedRateMid(abw)` (คืนค่าเป็นช่วง+ค่ากลาง) ด้วย `bcFeedRateFormula(age,abw)` (คำนวณค่าเดียวตรงจากสูตรจริง) — `renderBioCompare()` ดึง `age` จาก `lat.age` ของการสุ่มตัวอย่างล่าสุด (fallback คำนวณจาก `p.startDate` ถ้าไม่มี) ส่งเข้าสูตรแทนใช้แค่ ABW อย่างเดียว — ถ้าไม่มีข้อมูลอายุเลย (เช่น รอบเก่าที่ไม่เคยกรอก) fallback เป็นค่าประมาณ 2.5% พร้อมข้อความแจ้งเตือนให้กรอกอายุกุ้งตอนสุ่มตัวอย่างเพื่อความแม่นยำ — ข้อความอธิบายใต้ช่องกรอกเปลี่ยนจาก "ช่วงอ้างอิงทั่วไป...ยังไม่ใช่ตารางจากเอกสารฉบับเต็ม" เป็นระบุที่มาสูตรชัดเจน + ค่าที่คำนวณได้จริงจากอายุ/น้ำหนักของบ่อนั้น — กลไกแก้ไขเอง/กดค่าแนะนำ (`_bcFeedRateOverride`) ไม่เปลี่ยน ยังทำงานเหมือนเดิม

**ข้อจำกัดที่ต้องบอกพี่:** สูตรนี้เป็น regression จากข้อมูลภาคสนามที่ ดร.พุทธ เก็บมา เอกสารไม่ได้ระบุว่า fit จากช่วงอายุ/สายพันธุ์/ความหนาแน่นแบบไหนบ้าง จึงยังเป็น "ค่าประมาณที่ดีที่สุดเท่าที่มีเอกสารรองรับ" ไม่ใช่ค่าสัมบูรณ์ — ยังปรับแก้เองได้เสมอตามคำแนะนำจริงของฟาร์ม ยังไม่ได้ไล่เช็ค `Pond dynamics.pdf`/`shrimp health management.pdf` อีก 2 ไฟล์ว่ามีตาราง Feeding Rate เสริมอีกไหม (พี่ยืนยันว่ายังไม่จำเป็นตอนนี้)

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง: บ่อที่มี age=60/abw=15.4 → คำนวณได้ 3.1% ตรงกับเอกสารเป๊ะ, บ่อที่ไม่มีอายุกุ้ง (age=0, ไม่มี startDate) → fallback 2.5% + ข้อความเตือนแสดงถูกต้อง, แก้ไขค่าเองแล้วกดปุ่ม "↺ ค่าแนะนำ" กลับไป auto ถูกต้อง (ยิง `.click()`/`change` event จริง) ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว

### 93. ล้างข้อมูล/ฟังก์ชันตายค้างที่ทำให้ sync ช้า+โค้ดรก + แก้ฟังก์ชันต้นทุนพัสดุซ้ำซ้อน + หน้าความเสี่ยงยิงโหลดพยากรณ์ซ้ำ

พี่แจ้งมา 3 จุดให้ตรวจ:

**1. ข้อมูลตายค้างในระบบ (ทำให้ sync ช้า + โค้ดรก)** — ไล่หาจุดที่ยังถูก init/merge/sync ทุกครั้งทั้งที่ไม่มี UI หน้าไหนอ่าน/เขียนแล้วจริง เจอ:
- **ระดับบนสุด**: `D.harvestLog` (retired ตั้งแต่ข้อ 24 — เปลี่ยนไปใช้ `p.partialHarvests`/`p.cropHistory` แทน), `D.sales`/`D.marketPrices` (retired ตั้งแต่ลบหน้า "ราคาวันนี้"), `D.laborCosts` (retired ตั้งแต่ข้อ 65/72 — เปลี่ยนไปใช้ `D.dailyLaborWeeks`) — ทั้ง 4 ตัวยังถูก `mergeArr`/`mergeByDate` ทุกครั้งที่ sync กับ Firebase (เพิ่มขนาด payload + เวลาประมวลผลทุกรอบ sync โดยไม่มีใครใช้ผลลัพธ์เลย)
- **ระดับต่อบ่อ**: `p.plankton` — พบ comment เก่า `// plankton section removed` ยืนยันว่าหน้าโรงเพาะแพลงก์ตอนถูกถอดออกไปนานแล้ว แต่ตัวข้อมูล `plankton:[]` ยังถูก init/assign id/clear/archive/merge อยู่ใน **ทุกบ่อ ทุกครั้ง** ที่ปิดรอบ/เปิดบ่อใหม่/sync/นำเข้า JSON — หนักกว่าตัวบนสุดเพราะคูณด้วยจำนวนบ่อ

**แก้แล้ว** — ลบทั้งสองชุดออกจาก `normalizeD`/`loadData`/`mergeRemote`/`LOG_KEYS`/ทุก object literal ที่สร้างบ่อใหม่/archive รอบ/ล้างรอบ (ไม่ลบข้อมูลเก่าที่อาจค้างอยู่ใน Firebase จริง — แค่แอปเลิกอ่าน/เขียน/sync มันแล้ว ถ้ามีของเก่าอยู่ก็นิ่งเฉยๆ ไม่กระทบอะไร) พร้อมลบ `mergeByDate()` (ฟังก์ชันที่สร้างไว้ใช้กับ `marketPrices` อย่างเดียว ไม่มีที่เรียกอีกแล้ว)

**2. ฟังก์ชันคำนวณ "ต้นทุนพัสดุต่อบ่อ" มี 2 ตัวทำงานซ้ำ** — `getStockItemsForPond()` (คืน list รายการเบิกสต็อกไม่รวมอาหาร) กับ `getStockCostBreakdownForPond()` (คืนยอดรวมแยกหมวด) ทั้งคู่กรอง `D.inventoryLog` ด้วยเงื่อนไขเดียวกันเป๊ะ (type='out' + pondId + ช่วงวันที่ + ไม่รวมหมวดอาหาร) แต่เขียนแยกกันคนละที่ — เสี่ยงแก้เงื่อนไขจุดเดียวแล้วอีกจุดไม่ตรงตามโดยไม่รู้ตัว

**แก้แล้ว** — `getStockCostBreakdownForPond()` เปลี่ยนมาต่อยอดจาก `getStockItemsForPond()` แทนการกรอง `D.inventoryLog` เอง (เหลือแหล่งกรองข้อมูลจุดเดียว) และ `ceoTotalCost()` เปลี่ยนจากดึง list มา reduce เองเป็นเรียก `getStockCostBreakdownForPond(...).total` ตรงๆ (ไม่ต้องมี list ที่ไม่ได้ใช้)

**3. หน้า "ความเสี่ยง" ยิงโหลดพยากรณ์อากาศใหม่ทุกครั้งที่เปิด** — router `goTab()` เรียก `fetchWx()` ตรงๆ ทุกครั้งที่สลับมาแท็บนี้ ทั้งที่มี cache `_wxDaily`/`_wxToday` อยู่แล้ว (จุดอื่นในแอปที่ใช้ cache เดียวกัน เช่น การ์ดสภาพอากาศหน้าภาพรวม เช็คก่อนว่ามีแคชหรือยังทุกจุด มีแค่จุดนี้ที่ลืมเช็ค) ทำให้ยิง request ไป Open-Meteo API ซ้ำโดยไม่จำเป็นทุกครั้งที่เข้าหน้านี้

**แก้แล้ว** — เปลี่ยนเป็นเช็คก่อนว่ามี `_wxDaily` แล้วหรือยัง ถ้ามีเรียก `renderRisk(_wxDaily)` ใช้ข้อมูลเดิมทันที ไม่ยิง fetch ซ้ำ ถ้ายังไม่เคยโหลดเลย (ครั้งแรกที่เปิดแอป) ค่อย `fetchWx()` เหมือนเดิม — ปุ่ม "🔄 อัปเดต" ที่หัวหน้ายังเรียก `fetchWx()` ตรงๆ เหมือนเดิม กดแล้วดึงข้อมูลใหม่ได้ทุกเมื่อตามต้องการ

ทดสอบผ่าน local server: (1) `normalizeD`/`mergeRemote` รันกับข้อมูลจำลองที่มี field เก่า (`harvestLog`/`sales`/`marketPrices`/`laborCosts`) ปนมาจาก remote ไม่ error, สร้างบ่อทดสอบยืนยัน `plankton` ไม่ถูกสร้างอีก, หน้า "จัดการบ่อ"/"ภาพรวม"/P&L โหลดผ่านปกติไม่มี error (ทดสอบเส้นทางที่ยังอ้างอิง `p.records.length` ฯลฯ ในเช็ค orphan/counts ที่เพิ่งตัด `plankton` ออก); (2) จำลองบ่อเบิกสต็อก 2 หมวด (ยากิน 500, ปูน 200) → `getStockCostBreakdownForPond`/`getStockCostGroupsForPond`/`getStockItemsForPond`/`ceoTotalCost`/`pondRunningCost` ให้ยอดตรงกันทุกจุด (700 บาท) ไม่มี console error; (3) ยืนยันด้วยการ monkey-patch นับจำนวนครั้งที่ `fetchWx()` ถูกเรียก — เข้าแท็บ "ความเสี่ยง" ซ้ำ (มีแคชอยู่แล้วจากตอนเปิดแอป) เรียก 0 ครั้ง แต่ตารางยังแสดงผลถูกต้องครบ (ใช้ `_wxDaily` แคช), ปุ่ม "🔄 อัปเดต" ยังผูก `fetchWx()` ตรงเหมือนเดิม ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว

### 92. หน้า "เตรียมบ่อ" — เตือนขั้นตอนที่เลยกำหนด + ยุบการ์ดให้สเกลได้เมื่อเตรียมพร้อมกันหลายบ่อ

ต่อจากข้อ 91 ที่ทำหน้า "ภาพรวม" ให้สเกลได้ พี่ถามว่าหน้า "เตรียมบ่อ" ปรับอะไรได้อีก — เสนอ 2 อย่าง พี่ตอบ "ทำทั้งข้อ 1และ 2":

**1. เตือนขั้นตอนที่เลยกำหนด** — ฟังก์ชันใหม่ `prepOverdueSteps(p)` (ใช้ร่วมกันทั้งการ์ดเต็มหน้า "เตรียมบ่อ" และการ์ดย่อในหน้า "ภาพรวม" กันเขียนเกณฑ์ซ้ำ 2 ที่) คืนรายการขั้นตอนที่ยังไม่ติ๊กว่าทำ **และ** วันเป้าหมาย (`addDays(stockDate, daysBeforeStock)`) ผ่านไปแล้ว — การ์ดเตรียมบ่อขึ้นป้าย "⚠️ เลยกำหนด N ขั้นตอน" ที่หัวการ์ด แถวขั้นตอนที่เลยกำหนดในตารางไฮไลต์แดง+โชว่ "⚠ เลยกำหนด" ใต้วันเป้าหมาย, การ์ดย่อในหน้าภาพรวม (`renderPrepPondCard`) ก็ขึ้นป้ายเดียวกันด้วย

**2. ยุบการ์ดให้สเกลได้** — เดิม `renderPrep()` วางการ์ดเต็ม (ตาราง 10 ขั้นตอน) ของทุกบ่อต่อกันยาวๆ ถ้าเตรียมหลายบ่อพร้อมกันจะเลื่อนดูช้าเหมือนปัญหาหน้าภาพรวมเดิม (ข้อ 91) — แก้ด้วยแพทเทิร์นเดียวกัน: ค่าเริ่มต้นย่อเหลือแค่แถบหัว (ชื่อบ่อ/รอบ/%/badge เลยกำหนด/วันลงกุ้ง/ปุ่มล้างแผน) ตารางขั้นตอนเต็มไม่ render ลง DOM จนกว่าจะกดขยาย (`togglePrepOpen(pid)`, module state `_prepOpen`) — บ่อที่มีขั้นตอนเลยกำหนดจะถูกขยายให้อัตโนมัติแค่ครั้งแรกที่เจอ (`_prepSeenDefault` กันเปิดซ้ำถ้าผู้ใช้กดปิดเอง) ส่วนบ่อที่ตามแผนปกติเริ่มต้นยุบไว้

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 2 บ่อ (1 บ่อมีขั้นตอนเลยกำหนดจริง 2 ขั้นตอน, 1 บ่อตามแผนปกติ): บ่อเลยกำหนดขึ้นป้าย+ขยายอัตโนมัติถูกต้องทั้งหน้าเตรียมบ่อและการ์ดย่อภาพรวม, บ่อตามแผนยุบเป็นค่าเริ่มต้นไม่มีป้ายเตือน, กดแถบหัวยิง `.click()` จริงยุบ/ขยายสลับถูกต้อง, ติ๊กเช็คบ็อกซ์ในตารางที่ขยายอยู่ยังบันทึกถูกต้องและการ์ดคงสถานะขยายไว้หลัง re-render, ทดสอบจอมือถือ (375px) เลย์เอาต์ป้ายเตือนไม่ล้นถูกต้อง ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว

### 91. รีดีไซน์หน้า "ภาพรวม" — รวมสถานะบ่อ/เช็คลิสต์/ค่าผิดปกติที่ซ้ำซ้อนกัน 4-5 กล่อง เป็นตารางเดียว + สเกลได้เมื่อมีหลายสิบบ่อ

พี่ขอให้ไล่ตรวจทั้งแอปหาบั๊ก+จุดข้อมูลซ้ำซ้อน ตรวจแล้วพบว่าจุดใหญ่สุดที่กระทบการใช้งานจริงอยู่ที่หน้า "ภาพรวม" — สถานะบ่อ/เช็คลิสต์รายวัน/ค่าผิดปกติ ชุดข้อมูลเดียวกันกระจายอยู่ 4-5 ที่พร้อมกัน:
- ตาราง "สถานะบ่อทั้งหมด" (`renderOvDashboard`, ข้อ 49)
- การ์ดบ่อแยกตามสถานะ (`pondCard`/`ov-sections`)
- "✅ เช็คลิสต์วันนี้" (`renderChecklist`) — คำนวณ hasWQ/hasFeed/hasStock ซ้ำกับตารางเป๊ะ
- "🔔 การแจ้งเตือน" (`allAlerts`/`getAlerts`) — ค่าเดียวกับคอลัมน์ "เหตุผล" ในตาราง แค่แตกเป็นรายการละเอียดกว่า

ทำ mockup ให้ดูก่อน 2 รอบ ([mockup_ภาพรวม_รวมข้อมูลซ้ำซ้อน.html](../Mockup และ Prototype/mockup_ภาพรวม_รวมข้อมูลซ้ำซ้อน.html)) — รอบแรกรวมเป็นการ์ดใหญ่ต่อบ่อ พี่ทักท้วงว่าถ้าเลี้ยงพร้อมกันหลายสิบบ่อในอนาคต การ์ดใหญ่จะเลื่อนดูช้า จึงปรับรอบสองเป็นตารางทึบ (dense) + ยุบกลุ่มบ่อปกติเป็นค่าเริ่มต้น พี่คอนเฟิมแล้วให้ implement เข้าแอปจริง

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):

1. **`allReasonsOv(p,last,age)`** (ฟังก์ชันใหม่ แยกจาก `reasonOv()` เดิม) — คืนค่าผิดปกติ**ทั้งหมด**ของบ่อ (ไม่ใช่แค่ตัวแย่สุด) เรียงแย่สุดก่อน ครอบคลุมเกณฑ์เดียวกับ `reasonOv()` เป๊ะ (น้ำ+แล็บ+อายุ) — `reasonOv()` เดิมเปลี่ยนเป็น wrapper บางๆ เรียก `allReasonsOv()[0]` แทน (ทุกจุดที่เคยเรียก `reasonOv()` ยังทำงานเหมือนเดิมทุกประการ ไม่กระทบ)
2. **ตาราง "สถานะบ่อทั้งหมด" เพิ่ม 3 คอลัมน์** (ไซส์/Biomass/อาหารวันนี้ — ย้ายมาจากการ์ดบ่อเดิม) และ**ปุ่ม "+N อื่นๆ"** ท้ายคอลัมน์เหตุผล ขยายดูค่าผิดปกติที่เหลือได้ (ใช้ `allReasonsOv` แทนที่ `allAlerts`/`getAlerts` เดิมที่ครอบคลุมแค่น้ำ ไม่รวมแล็บ/อายุ — ครบกว่าของเดิมด้วยซ้ำ)
3. **ยุบกลุ่ม "ปกติครบถ้วน" (สถานะปกติ + กรอกน้ำ/อาหาร/สต็อกครบ) เป็นแถวเดียวโดยอัตโนมัติ** เมื่อดูฟิลเตอร์ "ทั้งหมด" — กดแถวขยายดูทั้งหมดได้ (`_ovdShowAllOk`/`toggleOvdShowAllOk()`) ระยะเลื่อนจึงขึ้นกับจำนวนบ่อที่ต้องดูจริง ไม่ใช่จำนวนบ่อทั้งหมด — ทดสอบกับฟาร์มจำลอง 24 บ่อ (20 บ่อปกติ) เหลือแค่ 4-5 แถวให้เลื่อนผ่าน
4. **ลบส่วนที่ซ้ำซ้อนออกทั้งหมด**: การ์ดบ่อกำลังเลี้ยง (`ov-sections` เหลือแค่เตรียมบ่อ+ว่าง), การ์ด "✅ เช็คลิสต์วันนี้" (`renderChecklist`, ลบ CSS `.cl-*` ที่ใช้เฉพาะจุดนี้ทั้งหมด), การ์ด "🔔 การแจ้งเตือน" (`allAlerts`/`getAlerts`/`paramBadness`, ลบเป็น dead code เพราะไม่มีที่ไหนเรียกอีก) — กระดิ่ง 🔔 และปุ่มลัด "แจ้งเตือน" หน้าแรก (`bnavAlerts`/`homeQuickAlerts`) เปลี่ยนจาก scroll ไปการ์ดที่ลบไปแล้ว เป็นกรองตาราง "ต้องดูก่อน" แล้ว scroll ไปตารางแทน — ตรงประเด็นกว่าเดิม
5. **"ว่าง/พักบ่อ"** เปลี่ยนจากการ์ดเปล่าซ้ำกัน N ใบ (ไม่มีข้อมูลต่างกันเลย) เป็นบรรทัดสรุปเดียว "N บ่อ ยังไม่มีรอบเลี้ยง"
6. **`pondCard()` เปลี่ยนชื่อเป็น `renderPrepPondCard(p)`** — ตัดกิ่งโค้ดที่ตายแล้ว (เดิมรองรับทั้งบ่อกำลังเลี้ยง/เตรียมบ่อ ตอนนี้ถูกเรียกใช้แค่กรณีเตรียมบ่อจุดเดียวหลังข้อ 4) พร้อมลบ CSS ที่ใช้เฉพาะกิ่งที่ตัดออก (`.pc-ov-row/-lbl/-val/-div/-alert/-empty`) และ CSS `.cl`/`.cl-dot` เก่าที่เป็น dead code มาก่อนหน้านี้แล้ว (ไม่มี HTML อ้างถึงเลย)

ทดสอบผ่าน local server ด้วยข้อมูลจำลองครบทุกกรณี: 3 บ่อ danger (1 ตัวมี multi-alert 4 ค่าผิดปกติ — nh4/DO/pH/phSwing พร้อมกัน ปุ่ม "+3 อื่นๆ" กดจริงขยายถูกต้องครบ, ยืนยัน `event.stopPropagation()` กันไม่ให้คลิกปุ่มแล้วหลุดไปหน้ารายละเอียดบ่อโดยไม่ตั้งใจ), เพิ่มบ่อปกติครบถ้วน 5-6 บ่อทดสอบยุบกลุ่ม/ขยายกลุ่มด้วยคลิกจริงถูกต้อง (ทดสอบขอบเขตกรณีมีแค่ 1 บ่อปกติก็ยุบเป็นแถวเดียวถูกต้อง), มือถือ (resize 375px) ตาราง desktop ซ่อนสลับเป็นการ์ดที่มีฟีเจอร์เดียวกันครบ, กระดิ่ง `bnavAlerts()` กรอง "ต้องดูก่อน" ถูกต้อง, การ์ดเตรียมบ่อ/บรรทัดว่างยังแสดงถูกต้องไม่กระทบ, ยืนยัน `pondCard`/`renderChecklist`/`allAlerts`/`getAlerts` ถูกลบสมบูรณ์ (`typeof`=`undefined`) ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว

### 90. 🔴 หน้า "สถิติจับกุ้ง" — ข้อมูล "แพ/ห้องเย็น" ไม่ตรงความจริง เปลี่ยนมาดึงจากข้อมูลจริงในแอปแทน Google Sheets

พี่แจ้งว่าตาราง "แพ/ห้องเย็น" ในหน้า "สถิติจับกุ้ง" ไม่ตรงกับความจริง — ตรวจสอบพบว่าข้อมูลนี้มาจาก 1 ใน 2 แหล่งซึ่งทั้งคู่แยกขาดจากข้อมูลจับกุ้งจริงที่บันทึกในแอปเลย:
1. **Google Sheets ภายนอก** ผ่าน `HarvestSync_AppsScript.gs` — ตรวจโค้ดพบว่าสคริปต์นี้อ่านข้อมูลแพได้จากชีทชื่อ `ยอดรายแพ` เป๊ะๆ เท่านั้น (ต่างจากยอดรายเดือนที่อ่านจากทุกชีท `จับกุ้งปีXX`) พี่แจ้งว่าจริงๆ ข้อมูลแพ/ห้องเย็นอยู่ในชีท `จับกุ้งปี69` เอง ที่ช่วง K81:M98 (คนละโครงสร้างกับที่สคริปต์คาดไว้เลย) ทำให้ `data.buyers` จาก Google Sheets ว่างเปล่าเสมอ
2. **ตัวเลข hardcode ฝังในโค้ด** (`HV_BUYERS`, ปี 65-69) — เป็นข้อมูลอ้างอิงที่พิมพ์ไว้ครั้งเดียวตอนสร้างฟีเจอร์นี้ ไม่เคยอัปเดตตามการจับจริงเลย พอ Google Sheets sync ไม่ได้ข้อมูลแพ (ตามข้อ 1) ระบบเลย fallback มาโชว่ตัวเลขนิ่งชุดนี้แทนตลอด โดยที่ข้อความสถานะยังขึ้น "✅ ซิงค์สำเร็จ" ทำให้ดูเหมือนไม่มีปัญหา

พี่ยืนยันว่า**ไม่ต้องแก้ Google Sheets/Apps Script แล้ว ให้เปลี่ยนมาใช้ข้อมูลจริงจากในแอปแทน**

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):
- ลบค่าคงที่ `HV_BUYERS` (hardcode) และโค้ดรับ `data.buyers` จาก Google Sheets fetch ทิ้งทั้งหมด (ไม่ใช้แล้ว — ส่วนยอดรายเดือน/รายปีจาก Google Sheets ยังคงทำงานเหมือนเดิม ไม่ได้แตะ)
- เพิ่มฟังก์ชันใหม่ `hsBuyersFromApp(shortYr)` — รวมยอด "แพ/ห้องเย็น" จากข้อมูลจับกุ้งจริงที่มีอยู่แล้วในระบบทั้งหมด: พาเชียลของรอบที่กำลังเลี้ยงอยู่ (`p.partialHarvests[]`), พาเชียลของรอบที่ปิดแล้ว (`p.cropHistory[].partialHarvests[]`), และยอดปิดรอบสุดท้าย (`p.cropHistory[].harvestResult` — ใช้ `hr.revenue` ถ้ามี ไม่งั้นคำนวณจาก `totalWeight×price`) กรองตามปีจากวันที่ของแต่ละรายการเอง (ไม่ใช่ปีของรอบเลี้ยง) แล้วรวมยอดเงิน+น้ำหนักตามชื่อแพ เรียงจากยอดเงินมากไปน้อย
- `hsRenderYear()` เปลี่ยนจากอ่าน `HV_BUYERS[yr]` เป็นเรียก `hsBuyersFromApp(yr)` สดทุกครั้งที่ render — ข้อมูลจะอัปเดตอัตโนมัติทันทีที่มีการบันทึกพาเชียล/ปิดรอบใหม่ผ่านหน้า "จับกุ้ง" ไม่ต้องพึ่งการซิงค์จากภายนอกอีกต่อไป

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 3 บ่อ (บ่อกำลังเลี้ยงมีพาเชียล 2 ครั้งให้แพเดียวกัน, บ่อปิดรอบแล้วมีทั้งพาเชียล+ปิดรอบให้อีกแพหนึ่งในปีเดียวกัน, บ่อปิดรอบปีอื่นให้แพที่สาม): เรียกฟังก์ชันตรงยืนยันยอดรวมต่อแพถูกต้อง 100% (SIFCO 402,000฿/2,800กก. = พาเชียล 112,000 + ปิดรอบ 290,000 ตรงตามคำนวณมือ), กรองปีถูกต้อง (ปีอื่นไม่ปนกัน), เรียกผ่าน UI จริง (`goTabByName`→`hsSetYr`) ตารางแสดงผลถูกต้องครบพร้อมราคา/กก. คำนวณถูก, ปีที่ไม่มีข้อมูลขึ้นข้อความแจ้งปกติไม่พัง, ยืนยัน `HV_BUYERS` ถูกลบออกจากโค้ดสมบูรณ์ (`typeof`=`undefined`) ไม่มี console error, sync สองไฟล์แล้ว

### 89. 🔴 หน้า "รอบเลี้ยง" + "เตรียมบ่อ" — แก้ 2 บั๊กที่ทำให้การทำงานรู้สึกไม่เป็น logic

พี่รู้สึกว่าการ์ด "รอบเลี้ยง"/"เตรียมบ่อ" ทำงานไม่ค่อยเป็น logic ขอให้ตรวจสอบ — ไล่โค้ดทั้ง 2 หน้าแล้วเจอบัคจริง 2 จุด ยืนยันด้วยข้อมูลจำลองในเบราว์เซอร์ครบทั้งคู่ก่อนแก้:

**1. หน้า "รอบเลี้ยง" — บ่อที่เลี้ยง 3 รอบขึ้นไปในปีเดียว รอบล่าสุด+รอบที่กำลังเลี้ยงหายจากตาราง**

ตาราง "สรุปรอบเลี้ยงปีนี้" ออกแบบมาให้โชว์ได้แค่ 2 คอลัมน์รอบ/บ่อ/ปีตายตัว (สอดคล้องหัวข้อ "เป้าหมาย: 2 รอบ/ปี/บ่อ") แต่ตัวระบบคำนวณเลขรอบจริง (`computeNextCrop`) รองรับกี่รอบก็ได้ต่อปีมาตั้งแต่ก่อนหน้านี้แล้ว (ฟาร์มจริงบางปี 1 รอบ บางปี 3 รอบ) — เดิม `showCurAt=totalClosed==0?1:totalClosed==1?2:null` แปลว่าถ้าบ่อไหนปิดไปแล้ว **≥2 รอบในปีนั้น** แล้วกำลังเลี้ยงรอบถัดไปอยู่ (รอบที่ 3 ขึ้นไป) ทั้งรอบที่ปิดล่าสุดและรอบที่กำลังเลี้ยงอยู่ตอนนี้จะ**ไม่ถูกแสดงที่ไหนในแถวนั้นเลย** (ปุ่ม "ปิดรอบ" ยังกดได้ปกติเพราะเช็คแยกออกจากส่วนแสดงผล แต่มองด้วยตาจะไม่รู้เลยว่าบ่อนี้กำลังเลี้ยงรอบอะไรอยู่)

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`) — เปลี่ยนตรรกะเลือกช่องแสดงผล: **รอบปัจจุบัน (กำลังเลี้ยง/เตรียมบ่อ) ต้องโชว่เสมอถ้ามี** ไม่ว่าจะเคยปิดไปแล้วกี่รอบก่อนหน้า ส่วนอีกช่องที่เหลือโชว่ **รอบปิด "ล่าสุด"** (ไม่ใช่รอบเก่าสุดแบบเดิม) — ประวัติรอบเก่ากว่าที่ไม่พอที่โชว่ในตารางสรุปนี้ยังดูครบทุกรอบได้ที่ตาราง "ประวัติรอบเลี้ยง" ด้านล่างซึ่งไม่จำกัดจำนวนอยู่แล้ว (ไม่มีข้อมูลหายจริง แค่ตารางสรุปด้านบนแสดงได้จำกัด 2 ช่อง) พร้อมลบตัวแปร `c1slot`/`c2slot` ที่เป็น dead code (คำนวณไว้แต่ไม่เคยถูกใช้จริง)

**2. หน้า "เตรียมบ่อ" — บ่อที่ลงกุ้งจริงแล้วยังค้างโผล่ในรายการเตรียมบ่อ**

`initPrep()` เช็คว่าบ่อไหน "ลงกุ้งจริงแล้วให้ซ่อนออกจากรายการเตรียมบ่อ" โดยเทียบ `p.startDate` (วันลงจริง) ต้อง**ตรงเป๊ะ**กับ `stockDate` ที่วางแผนไว้ในเช็คลิสต์ — ถ้าวันลงจริงคลาดจากแผนแม้แค่วันเดียว (ปกติมากในการทำฟาร์มจริง) เงื่อนไขจะไม่ตรง บ่อนั้นก็ยังค้างอยู่ในหน้า "เตรียมบ่อ" ทั้งที่ลงกุ้งเลี้ยงจริงไปแล้ว (ปรากฏซ้ำทั้งในกลุ่ม "กำลังเลี้ยง" และ "เตรียมบ่อ" พร้อมกัน)

**แก้แล้ว** — เปลี่ยนเงื่อนไขจากเทียบวันที่ตรงเป๊ะ เป็นใช้ `pondStatusOv(p)!=='prep'` มาตรฐานเดียวกับที่หน้าอื่นทั้งแอปใช้กำหนดว่า "บ่อนี้กำลังเลี้ยงจริงหรือยัง" (`wqfeedActivePonds()` ก็ใช้ตัวเดียวกันนี้) ไม่ว่าวันที่ลงจริงจะตรงแผนเป๊ะหรือไม่ก็ตาม — ยังคงพฤติกรรมเดิมที่บ่อซึ่ง `p.startDate` เป็นวันในอนาคต (วางแผนล่วงหน้าแต่ยังไม่ถึงวันลงจริง) ยังคงอยู่ในรายการเตรียมบ่อตามปกติ (ไม่ได้ทำให้หายไปก่อนเวลา)

ทดสอบผ่าน local server ด้วยข้อมูลจำลองครบทุกกรณี: (ก) บ่อปิดไปแล้ว 3 รอบ+กำลังเลี้ยงรอบ 4 → ตารางแสดงรอบ 3 (ปิดล่าสุด) คู่กับรอบ 4 (กำลังเลี้ยง 52 วัน) ถูกต้องทั้งคู่ (ก่อนแก้ทั้งสองรอบไม่โชว่เลย), (ข) regression 3 เคสปกติ (0 ปิด+กำลังเลี้ยงรอบ1, 1 ปิด+กำลังเลี้ยงรอบ2, 2 ปิดไม่มีรอบปัจจุบัน) ยังแสดงผลถูกต้องเหมือนเดิมทุกจุดไม่มีการถดถอย, (ค) เตรียมบ่อ: บ่อวางแผนลง 1 มิ.ย. แต่ลงจริง 5 มิ.ย. (คลาด 4 วัน, `pondStatusOv`='ok') → ไม่โผล่ในรายการเตรียมบ่อแล้ว ส่วนบ่อที่ยัง`startDate`เป็นอนาคตจริง (`pondStatusOv`='prep') ยังโผล่ตามปกติ ไม่มี console error, sync สองไฟล์แล้ว

### 88. แก้ค่า "อาหาร/แสนตัว" ในชีทรายละเอียดบ่อ (CEO) ให้ตรงกับการ์ดบ่อหน้าแรก

พี่แจ้งว่าข้อ 87 ที่เพิ่ง implement "อาหาร/แสนตัว" ในบล็อก "สรุปบ่อ" ของชีทรายละเอียดบ่อ ตัวเลขไม่ตรงกับที่โชว่อยู่แล้วในการ์ดบ่อหน้ารายการ (หน้าแรกของ CEO dashboard) — ตรวจพบว่าเข้าใจสูตรผิดจริง: ของเดิมในชีทใช้ `feedPer100k(cumFeed, pRelease(p))` (อาหาร**สะสมทั้งรอบ** ÷ ยอดปล่อยจริง) แต่การ์ดหน้ารายการใช้ `ceoFeedPer100k(p)` ที่คำนวณจาก `ceoFeedDisplay(p).val` (อาหาร**วันนี้/ล่าสุด** เท่านั้น ไม่ใช่สะสม) — คนละความหมายกันเลย จึงเห็นตัวเลขไม่ตรงกัน

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`) — `ceoOpenSheet()`: เปลี่ยนไปเรียกใช้ `ceoFeedPer100k(p)` ตัวเดียวกับที่การ์ดหน้ารายการใช้อยู่แล้วตรงๆ (ไม่สร้างสูตรคำนวณคู่ขนานใหม่) ให้ค่าตรงกัน 100% ทั้ง 2 จุด

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง (บ่อมีบันทึกอาหารวันนี้ 88 กก., ยอดปล่อยจริง 420,000 ตัว): ค่าที่การ์ดหน้ารายการ (`ceoFeedPer100k`) กับค่าที่โชว่ในชีทรายละเอียดตรงกันเป๊ะทั้งคู่ (21.0 กก.) ไม่มี console error, sync สองไฟล์แล้ว

### 87. หน้า CEO — แตะแต่ละหมวดต้นทุนสะสมดู list สินค้าได้ + เพิ่ม "อาหาร/แสนตัว" ในสรุปบ่อ

ต่อจากข้อ 86 พี่ขอเพิ่ม 2 จุด:

**1. แตะหมวดต้นทุนแต่ละหมวด (ในบล็อก "ต้นทุนสะสม") ให้ขยายดู list สินค้า/รายละเอียดในหมวดนั้นได้** — จากข้อ 86 ที่เพิ่งย่อการแสดงผลจากไล่ทีละสินค้าเป็นสรุปยอดรวมต่อหมวด (แก้บั๊กเลื่อนไม่ได้) พี่ต้องการดูรายละเอียดเจาะลึกได้เมื่อต้องการ — เพิ่ม `ceoToggleCatItems(key)` ให้แถวหมวดที่มีรายการสินค้าจริง (ยากิน/ยาลงน้ำ/จุลินทรีย์กิน/จุลินทรีย์ลงน้ำ/ปูน/อุปกรณ์ และ "อื่นๆ (เดิม)") กดแตะแล้วขยาย/ยุบ list แสดงชื่อสินค้า+จำนวน+ยอดรวมต่อสินค้านั้น (มีไอคอน ▾ กำกับว่ากดขยายได้ หมุน 180° ตอนเปิด) — หมวดที่เป็นค่าคำนวณล้วนไม่มีรายชื่อสินค้าจริง (อาหารกุ้ง/ลูกกุ้ง/ค่าแรง/ค่าไฟ) ไม่มีไอคอนขยาย ไม่ตอบสนองการแตะ (ไม่มีอะไรให้ดูเพิ่ม)

**2. เพิ่ม "อาหาร/แสนตัว" ในบล็อก "สรุปบ่อ"** — ใช้ฟังก์ชัน `feedPer100k(cumFeed, pRelease(p))` เดิมที่มีอยู่แล้ว (ใช้ในการ์ด "เทียบกับรอบก่อน" ข้อ 57 อยู่แล้ว) เพิ่มเป็นช่องที่ 5 ต่อจาก Biomass/ไซส์/FCR ประเมิน/Yield/ไร่

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง (บ่อเบิกยากิน 2 ชนิด + ปูน 1 ชนิด, อาหารสะสม 120 กก., ยอดปล่อยจริง 420,000 ตัว): `อาหาร/แสนตัว` แสดง "29 กก." ตรงกับคำนวณด้วยมือ (120÷420,000×100,000=28.57≈29), แถวหมวด "ยากิน"/"ปูน" มีไอคอนขยายถูกต้อง ส่วนหมวดอื่นไม่มี, ยิง `.click()` จริงที่แถว "ยากิน" → list ขยายแสดง "ยากินA 5 ซอง · ฿500" และ "ยากินB 3 ซอง · ฿450" ถูกต้องตรงกับข้อมูลจำลอง, กดซ้ำยุบกลับถูกต้อง ไม่มี console error, sync สองไฟล์แล้ว

### 86. 🔴 หน้า CEO (มือถือ) — เลื่อนดูข้อมูลค่าไฟไม่ได้ + จัดหมวดต้นทุนสะสมใหม่ตามหมวดสต็อกจริง

พี่แจ้งว่า role CEO บนมือถือ เข้าไปดูรายละเอียดบ่อแล้วเลื่อนลงไปดูข้อมูล "ค่าไฟ" ในบล็อก "ต้นทุนสะสม" ไม่ได้ พร้อมขอให้แยกหมวดต้นทุนให้ชัดเจน: อาหารกุ้ง, ยากิน, ยาลงน้ำ, จุลินทรีย์กิน, จุลินทรีย์ลงน้ำ, ปูน, อุปกรณ์, ค่าแรง, ค่าไฟ

**ต้นตอบัคเลื่อนไม่ได้:** `.ceo-acc-panel` (accordion "ต้นทุนสะสม" ใน `ceoOpenSheet()`) ตั้ง `max-height:900px` คู่กับ `overflow:hidden` ไว้ตอนเปิด (บรรทัด ~860) — ของเดิมไล่แสดงรายการเบิกสต็อกทีละสินค้า (`itemsGrouped` ต่อ 1 ชื่อสินค้า/หมวด/หน่วย) แล้วต่อท้ายด้วยอาหาร/ลูกกุ้ง/ค่าแรง/**ค่าไฟฟ้า**/รวม — ถ้าบ่อไหนเบิกพัสดุหลายรายการต่างชื่อกัน (เรื่องปกติของบ่อที่เลี้ยงมานาน) ความสูงเนื้อหาจริงจะเกิน 900px แถวท้ายๆ (ค่าแรง/ค่าไฟ/ยอดรวม) จะถูก `overflow:hidden` ตัดทิ้งไปเลย ไม่ใช่แค่ซ่อนแล้วเลื่อนดูได้ — ตรงกับอาการที่พี่เจอเป๊ะ (ไม่ใช่บั๊กจอ iOS/scroll ปกติ)

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):
1. เพิ่ม `max-height` ของ `.ceo-acc-panel` ตอนเปิดจาก `900px` เป็น `3000px` — เผื่อพอสำหรับเนื้อหาจริงทุกกรณี ไม่ตัดอีก
2. **เขียนใหม่การแสดงผล "ต้นทุนสะสม"** (`ceoOpenSheet()`) จากเดิมไล่แสดงทีละชื่อสินค้า (ยาวไม่จำกัดตามจำนวนสินค้าที่เคยเบิก) เปลี่ยนเป็น**สรุปรวมตามหมวดคงที่ 9 หมวด**ตามที่พี่ขอ: อาหารกุ้ง → ยากิน → ยาลงน้ำ → จุลินทรีย์กิน → จุลินทรีย์ลงน้ำ → ปูน → อุปกรณ์ → ลูกกุ้ง → ค่าแรง → ค่าไฟ (จัดกลุ่มด้วย `STOCK_CATEGORIES`/`STOCK_CAT_LABEL` เดิมที่ใช้อยู่แล้วในหน้าสต็อก ไม่สร้างชุดหมวดใหม่ซ้ำ) พร้อมจุดสี (`CEO_CAT_DOT`) กำกับแต่ละหมวดให้กวาดตาง่าย — หมวดเก่าที่เลิกใช้แล้ว (`chem`/`seed` ที่ยังมีข้อมูลติดอยู่ในสต็อกเก่า) รวมเป็นแถว "อื่นๆ (เดิม)" ท้ายสุด ไม่หายไปจากยอดรวม
3. ผลพลอยได้: จำนวนแถวคงที่ (9-10 แถว) ไม่ว่าบ่อนั้นจะเบิกสต็อกกี่รายการก็ตาม — แก้ปัญหาความยาวไม่จำกัดที่เป็นต้นเหตุของบั๊กข้อ 1 ไปด้วยในตัว

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง: จำลองบ่อที่เบิกพัสดุ 12 รายการกระจาย 6 หมวด (เพื่อ stress-test ความยาวแบบที่เคยพัง) เปิดชีท CEO → บล็อก "ต้นทุนสะสม" แสดงครบ 10 แถวตามหมวดที่ขอ ยอดรวมแต่ละหมวดตรงกับที่คำนวณด้วยมือ (เช่น ยากิน 3,050 = สินค้า 2 ชนิดในหมวดเดียวกันรวมกันถูกต้อง) เนื้อหาสูง 441px อยู่ในขอบเขต max-height ใหม่ 3000px ไม่ถูกตัด (ก่อนแก้กรณีนี้เกือบครึ่งของ 900px แต่กรณีบ่อที่เบิกมากกว่านี้จะโดนตัดจริง), เลื่อน `ceo-sheet-body` ลงสุดแล้วแถว "รวมต้นทุนสะสม" (แถวสุดท้าย ต่อจากค่าไฟ) อยู่ในมุมมองที่เลื่อนถึงได้จริง ไม่มี console error, sync สองไฟล์แล้ว

## รีดีไซน์: รายงาน PDF ปิดบ่อ (sec-export)

พี่บ่นว่ากราฟ/ข้อมูลในรายงาน PDF ปิดบ่อ (ที่ปริ้นให้ผู้บริหารดูตัดสินใจ) ดูสับสน ไม่เมคเซนส์ ตรวจแล้วเจอ 3 ปัญหา แก้ครบทุกข้อในรอบเดียว (ข้อ 1 กับ 4 เดิมเสนอแยกกัน แต่เป็นโค้ดจุดเดียวกันเลยแก้พร้อมกัน):
1. **ไม่มีข้อมูลการเงินเลยในรายงาน** — ทั้งที่ตอนปิดรอบเก็บต้นทุน (ลูกกุ้ง/อาหาร/เคมี/แรงงาน/ไฟฟ้า) + กำไรสุทธิไว้ครบใน `harvestResult` แต่ PDF ไม่เคยดึงมาแสดง — เพิ่มฟังก์ชัน `closeSummaryHtml()` ที่จัดกลุ่มข้อมูลเป็น 3 หมวด (🦐 ผลผลิต / 💰 การเงิน / ⏱ การซื้อขาย-เวลา) ใช้ร่วมกันทั้ง preview บนจอและ PDF จริง มีรายได้, ต้นทุนแยกรายการ, รวมต้นทุน, กำไรสุทธิ (สีเขียว/แดงตามบวก/ลบ), กำไร%, ต้นทุนต่อกก.
2. **กราฟยัด 3 แกนไว้กราฟเดียวอ่านยาก** (อาหารสะสม+การกิน+ABW คนละสเกล) — แยกเป็น 2 กราฟเดี่ยวๆ อ่านง่าย: กราฟการเติบโต (ABW) และกราฟอาหารสะสม
3. **กราฟเทียบรอบก่อนหน้าที่มีแค่บนจอ preview ไม่เคยอยู่ใน PDF จริง** — ใส่กราฟนี้ (FCR + อัตรารอด เทียบทุกรอบที่ปิดแล้วของบ่อนั้น) เข้าไปใน PDF ด้วยแล้ว (โชว์เมื่อบ่อนั้นมีรอบปิดแล้ว ≥2 รอบ)
4. `getCloseSummary()` เพิ่ม field การเงิน (costFry/costFeed/costChem/costLabor/costElec/costOther/hasCostData/totalCost/revenue/netProfit/marginPct/costPerKg) ที่ไม่เคยมีมาก่อน
- ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 2 รอบเลี้ยงที่ปิดแล้ว (มีต้นทุน/กำไร/กราฟครบ): preview แสดงข้อมูลการเงินถูกต้อง, genPDF() รันจบไม่มี error, มีกราฟครบ 3 กราฟ (โต/อาหาร/เทียบรอบ) ไม่กระทบข้อมูลจริง

**สถานะ:** ตรวจครบทุกหน้าแล้ว (29/29) เจอบัคจริง 2 จุด แก้ไปแล้วในโค้ด **ยังไม่ได้ deploy** — รอ push ผ่าน `push_*.bat`
- 🔴 #4 ค่าแรงรั่วข้ามรอบเลี้ยง กระทบ Break-even + Risk Exposure Scorecard
- 🔴 #6 ปุ่ม "แก้ไข" หน้าจัดการน้ำสร้างรายการซ้ำแทนอัปเดต

## UX audit — กรอก/แสดงข้อมูลซ้ำซ้อน (แจ้งจากพี่ว่าแอปรู้สึกซับซ้อน)

รายการที่ตรวจเจอ:
1. **[แก้แล้ว]** ปิดรอบเลี้ยง (`openCropModal`) เดิมเคลียร์ต้นทุนลูกกุ้ง (`cm-cost-fry`) ว่างทุกครั้งทั้งที่มีอยู่แล้วใน `p.costFry`, และตั้งต้นทุนอาหาร (`cm-cost-feed`) เป็น 0 เสมอทั้งที่ label เขียนว่า "คำนวณจาก feedLog" (มี `pondFeedCost()` อยู่แล้วแต่ไม่เคยถูกเรียกใช้ตรงนี้) — แก้ให้ดึงค่าจริงมา prefill ให้ทั้งคู่ (ลูกกุ้งยังแก้ไขได้, อาหาร readonly ตามดีไซน์เดิม)
2. **[แก้แล้ว]** ป้าย "FCR" ในแอปมี 2 ความหมายปนกัน — FCR ประเมินระหว่างเลี้ยง (จาก biomass ประเมิน) vs FCR สุดท้ายตอนปิดรอบ (จากน้ำหนักจับจริง) แสดงเป็น "FCR" เฉยๆ เหมือนกันหมดหลายหน้า ทำให้ตัวเลขไม่ตรงกันข้ามหน้าดูเหมือนบัค — ตามที่พี่ขอ **เอา FCR ออกจากทุกหน้ายกเว้นหน้ารายละเอียดบ่อ** (ตัดคอลัมน์ FCR ออกจากหน้าอาหาร + หน้าจัดการบ่อ, ตัดแถวที่ซ้ำกับการ์ด KPI ในหน้ารายละเอียดบ่อเองออก) เหลือแสดงจุดเดียวทั้งแอป ส่วน "FCR สุดท้าย" ตอนปิดรอบ/แก้ไขปิดรอบ (`cm-fcr`/`ecm-fcr`) เป็นคนละค่ากัน (จากน้ำหนักจับจริง) ไม่ได้แตะ
3. **[ปิดประเด็น — ไม่แก้]** เมนู "รายวัน" 6 หน้าย่อย — ทำม็อกอัพ 2 แบบให้ดู (รวมหน้าเดียวมี sub-tab / เช็คลิสต์กดลิงก์ตรง) พี่เทียบกับของเดิมแล้วเห็นว่าของเดิมทำงานสะดวกอยู่แล้ว เพราะน้ำ/อาหารมักไม่ได้กรอกพร้อมกันในเวลาเดียว — ตัดสินใจไม่แก้โครงสร้างเมนูส่วนนี้
4. **[ต่ำ ไม่ต้องแก้]** ชื่อโรงเพาะฟักกรอกได้ทั้งตอนเพิ่ม/แก้บ่อ (`mdl-hatchery`) และตอนปิดรอบ (`ecm-hatchery`) — เป็น snapshot คนละช่วงเวลากันโดยเจตนา ไม่ใช่บัค
5. **[แก้แล้ว — ลบทิ้งทั้งหมด]** ไล่ตรวจหน้าที่เหลือทั้งหมดแล้ว ส่วนใหญ่ไม่พบปัญหาซ้ำซ้อน ยกเว้นจุดนี้: หน้า "ราคาวันนี้" มีฟอร์ม "บันทึกการขายจริง" (`D.sales`) ซ้อนทับกับหน้า "จับกุ้ง" (`D.harvestLog`) เกือบทุกฟิลด์ แต่ไม่เชื่อมข้อมูลกันเลย — พี่แจ้งว่า **ไม่ได้ใช้งานหน้า "ราคาวันนี้" เลย เพราะหาราคามา update ไม่ได้** เลยตัดสินใจ **ลบหน้า "ราคาวันนี้" ทิ้งทั้งหมด**
   - หน้า "Break-even" ใช้ `priceForSize()` จากราคาวันนี้เป็นแกนหลักในการคำนวณ "มูลค่ากุ้งในบ่อ" — ถ้าลบราคาทิ้งหน้านี้จะพังไปด้วย พี่ยืนยันให้ **ลบ Break-even ทิ้งด้วย**
   - หน้า "Risk Exposure Scorecard" ใช้ผลจาก Break-even (`pondBreakeven`) คิด 40% ของคะแนนความเสี่ยงเงินลงทุน — พี่ยืนยันให้ **ลบทิ้งด้วยเช่นกัน** (ไม่ได้ใช้งานหน้านี้)
   - **สรุป: ลบ 3 หน้าออกจากแอปทั้งหมด** — ราคาวันนี้ (`sec-price`), Break-even (`sec-breakeven`), Risk Exposure Scorecard (`sec-riskfin`) พร้อมฟังก์ชัน/เมนู/CSS ที่เกี่ยวข้องทั้งหมด (`priceForSize`, `pondBreakeven`, `riskExposureForPond`, `initPrice/Breakeven/RiskFin`, `renderPrice*`, `saveSale/Price`, `PRICE_SIZES`, `.pr-chart` ฯลฯ) — ฟังก์ชันที่ใช้ร่วมกับหน้าอื่น (`pondFeedCost`, `getLaborCostForPond`, `getElecCostForPeriod`, `wqfeedActivePonds`, `calcRisk` ฯลฯ) เก็บไว้ครบ ไม่กระทบ P&L/ปิดรอบ/หน้าอื่น
   - `D.marketPrices`/`D.sales` เหลือเป็น field เปล่าที่ไม่มีใครอ่าน/เขียนแล้วในโค้ด (ข้อมูลเก่าถ้ามีจะยังอยู่ใน database เฉยๆ ไม่ได้ลบทิ้ง)
   - ทดสอบผ่าน local server แล้ว: ทุกหน้าที่เหลือโหลดได้ปกติ ไม่มี error, ปิดรอบเลี้ยงยังทำงานถูกต้อง (auto-fill ต้นทุนอาหารยังใช้ได้)

แอปจัดการฟาร์มกุ้ง (single-file app: `TawanFarm_App.html` / `index.html`, ~8300 บรรทัด, Firebase backend, deploy ผ่าน GitHub Pages)

## รีดีไซน์: สมุดให้อาหารรายวัน (sec-feed)

หน้า "บันทึกอาหาร" เดิมกรอกได้ทีละบ่อ (เลือกบ่อ→กรอก→บันทึก→สลับบ่อ ซ้ำ 8 รอบ/วัน) เปลี่ยนเป็นตารางกรอกทุกบ่อพร้อมกันในหน้าเดียว:
- ช่อง "อาหารวันนี้" auto-prefill จากค่าล่าสุดที่เคยกรอก (ของวันนั้นถ้ามี ไม่งั้นของวันก่อนหน้าล่าสุด)
- มี date picker ด้านบน — เปลี่ยนวันที่ = โหลด/แก้ไขข้อมูลวันนั้นของทุกบ่อ (ใช้แทนทั้งฟอร์ม "แก้ไข" และตารางประวัติเดิม)
- ปุ่ม "บันทึกทั้งหมด" ครั้งเดียว บันทึก/แก้ไข/ลบ (เว้นว่างช่อง = ลบ) ทุกบ่อพร้อมกัน ตัดสต็อกอาหารอัตโนมัติเหมือนเดิม
- ฟังก์ชันเดิมที่ลบไป: `editFeed`, `saveFeed`, `onFdPond`, `clearFeed`, `populateFeedStockItems`, `updateFeedStockCalc`, `updateFeedRate`, `renderFeedMetrics`, `renderFeedChart`, `renderFeedTable`, `deleteFeed` — แทนที่ด้วย `renderFeedGrid`, `feedGridRow`, `fdGridCalcRate`, `saveFeedGridAll`
- กราฟ FCR/แนวโน้มอาหารรายบ่อแบบละเอียด ย้ายไปดูที่หน้า "รายละเอียดบ่อ" (มีอยู่แล้ว) แทน
- ทดสอบผ่าน local server (`python -m http.server`) แล้ว: บันทึก/แก้ไข/ลบ + ตัดสต็อก/reverse สต็อก ทำงานถูกต้องหมด — การเขียนไป Firebase จริงถูกบล็อกด้วย permission_denied เพราะไม่ได้ login (ยืนยันว่าไม่กระทบข้อมูลจริงระหว่างทดสอบ)

## รีดีไซน์: บันทึกคุณภาพน้ำแบบชิปบาร์ (sec-entry)

หน้า "บันทึกคุณภาพน้ำ" มี 14 พารามิเตอร์/บ่อ — **ไม่ทำเป็นตารางรวมทุกบ่อแบบหน้าอาหาร** เพราะกว้างเกินไปและค่าน้ำเป็นค่าที่ต้องวัดจริง (auto-prefill แบบหน้าอาหารจะอันตราย เสี่ยงบันทึกค่าเก่าทับโดยไม่ได้วัดจริง) แทนที่ dropdown เลือกบ่อด้วยแถบชิปทุกบ่อ + auto-advance หลังบันทึก:
- ก่อนแก้ ทำม็อกอัพโต้ตอบได้ให้ดูก่อน ([mockup_บันทึกคุณภาพน้ำ_ชิปบาร์.html](Tawan Farm/Mockup และ Prototype/mockup_บันทึกคุณภาพน้ำ_ชิปบาร์.html)) ผู้ใช้ยืนยันดีไซน์แล้วค่อยทำจริง
- แถบชิปบนสุดโชว์ทุกบ่อ + สถานะกรอกแล้ว/ยังไม่กรอกของวันที่เลือก (เขียว=กรอกแล้ว, เทา=ยังไม่กรอก, ส้ม=กำลังกรอกอยู่) พร้อม progress bar
- ฟอร์มยังเป็นค่าว่างเสมอเวลาสลับบ่อ — **ไม่ auto-prefill ค่าน้ำ** (คงพฤติกรรมเดิมที่ตั้งใจไว้)
- คลิกชิปบ่อที่กรอกแล้ว → โชว์ฟอร์มว่าง + คำเตือนว่ามีข้อมูลอยู่แล้ว (ไม่ prefill อัตโนมัติ) — การแก้ไขจริงยังต้องกด ✏️ ในตารางประวัติเหมือนเดิม (คงพฤติกรรม safety เดิมไว้ตั้งใจ)
- กด "บันทึก" แล้วเด้งไปบ่อถัดไปที่ยังไม่กรอกอัตโนมัติ ครบทุกบ่อขึ้นข้อความ 🎉
- เปลี่ยนวันที่ (date picker) = ดูสถานะ/กรอกของวันนั้นทุกบ่อ — ใช้แทนการเปิดฟอร์มแก้ไขแยก
- เพิ่มฟังก์ชันใหม่: `renderEpChips`, `updateEpHeader`, `selectEPond`, `epNextUnfilled`, `clearWQFields` (แยกจาก `clearWQ` เพราะของเดิม reset วันที่กลับเป็นวันนี้ทุกครั้งที่สลับบ่อ ซึ่งจะพังตอนแก้ข้อมูลย้อนหลังหลายบ่อ)
- ตารางประวัติของแต่ละบ่อยังอยู่เหมือนเดิม (ไม่ได้ตัดออกเหมือนหน้าอาหาร เพราะ 14 ค่าดูเป็นตารางยังมีประโยชน์)
- ทดสอบผ่าน local server แล้ว: สลับบ่อ/บันทึก/auto-advance/เปลี่ยนวันที่/แก้ไขย้อนหลัง/progress bar ทำงานถูกต้องหมด ไม่กระทบข้อมูลจริง (permission_denied เหมือนครั้งก่อน)

## แก้บัคหน้าโรงเพาะฟัก (sec-hatchery) + เครื่องมือรวมชื่อ

**สาเหตุ:** เจอจากพี่รายงานว่าเว็บจริงหน้า "โรงเพาะฟัก" เห็นชื่อซ้ำ (เช่น "cp ชุมพร1" กับ "ซีพี" แยกกันเป็นคนละแถว)

### 🔴 บัคจริงที่เจอเพิ่ม (แก้แล้ว): `c.closed` ไม่เคยถูกตั้งค่าโดย `confirmCloseCrop()`
`hatcheryStats()` (index.html) และ `hsCropStats()` (หน้าสถิติจับกุ้ง) กรองข้อมูลด้วย `c.closed` แต่ตอนปิดรอบเลี้ยงจริงในแอปไม่เคยตั้งค่า field นี้เลย (ตรวจสอบแล้วว่า field นี้ถูกตั้งเฉพาะใน DEFAULT seed data กับฟังก์ชัน patch ข้อมูลเฉพาะกิจ 1 จุด) — แปลว่ารอบเลี้ยงที่ปิดผ่านแอปตามปกติในอนาคตจะไม่ถูกนับในสองหน้านี้เลย (ต่างจากหน้า P&L ที่เช็คจาก `harvestResult`/`closeDate` แทน เลยไม่มีปัญหานี้) แก้โดย:
- ลบเงื่อนไข `!c.closed` ออกจากทั้ง `hatcheryStats()` และ `hsCropStats()` (การมี record อยู่ใน `cropHistory` ก็หมายถึงปิดแล้วอยู่แล้วโดยธรรมชาติ)
- เพิ่ม `closed:true` ใน `confirmCloseCrop()` เผื่อมีจุดอื่นในอนาคตอ้างอิง field นี้

### เครื่องมือใหม่: 🔧 รวมชื่อโรงเพาะฟักที่สะกดต่างกัน (Admin เท่านั้น)
เพิ่มการ์ดใหม่ท้ายหน้า "โรงเพาะฟัก" (แสดงเฉพาะ `APP_ROLE==='admin'`) — สแกนชื่อโรงเพาะฟักทั้งหมดที่ใช้อยู่จริง (จาก `p.hatchery` ปัจจุบันของทุกบ่อ + `cropHistory[].hatchery` ทุกรอบ) แสดงพร้อมจำนวนรายการที่ใช้ชื่อนั้น ให้ติ๊กเลือกอย่างน้อย 2 ชื่อที่หมายถึงโรงเพาะฟักเดียวกัน กรอกชื่อที่ต้องการใช้จริง กด "รวมชื่อที่เลือก" จะแก้ทุกบ่อ/ทุกรอบที่ใช้ชื่อเหล่านั้นให้เป็นชื่อเดียวกันทันที แล้ว refresh กราฟ/ตารางสรุปให้เอง — ฟังก์ชันใหม่: `hatcheryNameUsage`, `renderHatcheryMergeTool`, `toggleHatcheryMergeSelByIdx`, `mergeHatcheryNames`

**หมายเหตุสำคัญ:** ผมไม่มีบัญชี login เข้าแอปจริง (initFirebase() ทำงานเฉพาะหลัง login เท่านั้น — index.html:7722) เลยมองไม่เห็นข้อมูลจริง 373 รอบที่พี่มี ต้อง**ใช้เครื่องมือนี้เองตอน login แล้ว**ถึงจะรวมชื่อในข้อมูลจริงได้ — ทดสอบ logic ผ่าน local server ด้วยข้อมูลจำลองแล้วว่าทำงานถูกต้อง (merge/คำนวณสถิติใหม่/ซ่อนจาก staff) ไม่กระทบข้อมูลจริง

กำลังไล่ตรวจสอบทุกหน้าหาบัคทีละหน้า เช็คลิสต์นี้อัปเดตระหว่างทำงาน — ดูรายละเอียดแต่ละหน้าด้านล่าง

## Checklist (แยกตามหน้า)

- [x] ภาพรวม (`sec-overview`) — ตรวจแล้ว: "-" ไม่ใช่บัคโค้ด แต่เป็นเพราะไม่มีข้อมูลสุ่มตัวอย่าง (ดูรายละเอียดด้านล่าง)
- [x] รายละเอียดบ่อ (`sec-ponddetail`) — เจอ dead code เล็กน้อย (ดูรายละเอียด)
- [x] สภาพอากาศและน้ำขึ้นน้ำลง (`sec-tide`) — ไม่พบบัค, โค้ด async loading + harmonic tide model ถูกต้อง
- [x] บันทึกคุณภาพน้ำ/รายวัน (`sec-entry`) — ไม่พบบัค, โค้ดบันทึก/แก้ไข/ลบทำงานถูกต้อง มี comment อธิบาย edge case ที่แก้ไปแล้วครบ
- [x] เฝ้าระวังโรค (`sec-disease`) — ยืนยันเป็น dead code จริง (ดูรายละเอียด)
- [x] อาหาร & FCR (`sec-feed`) — ไม่พบบัค, ตรวจ logic ตัดสต็อก/reverse ตอนแก้ไขรายการแล้วถูกต้อง
- [x] สุ่มตัวอย่าง (`sec-sample`) — ไม่พบบัค, `saveSample()` update-in-place ด้วย date matching ถูกต้อง
- [x] จัดการน้ำ (`sec-watlog`) — 🔴 เจอบัคจริง แก้แล้ว (ดู #6): ปุ่ม "แก้ไข" สร้างรายการซ้ำแทนที่จะอัปเดต
- [x] สารเคมี (`sec-chem`) — ไม่ใช่ฟอร์มบันทึกแล้ว (ถูกเปลี่ยนเป็นหน้าดูประวัติเบิกจากสต็อกอย่างเดียว) ไม่พบบัค
- [x] ผลแล็บ (`sec-lab`) — ไม่พบบัค
- [x] เปรียบเทียบ (`sec-compare`) — ไม่พบบัค
- [x] ความเสี่ยง (`sec-risk`) — ไม่พบบัค, `calcWeatherRisk()`/`calcRisk()` logic ถูกต้อง
- [x] น้ำ-อาหาร (`sec-wqfeed`) — ไม่พบบัค, threshold การเตือน (DO<5, temp ลด≥1.5°C, อาหารลด≥10%) ตรงตาม commit ที่ตั้งใจไว้
- [x] โรงเพาะฟัก (`sec-hatchery`) — ไม่พบบัค, `hatcheryStats()` คำนวณค่าเฉลี่ยถูกต้อง
- [x] Break-even (`sec-breakeven`) — 🔴 กระทบจากบัค #4 (ค่าแรงข้ามรอบ) แก้แล้ว, ส่วนอื่น (`pondBreakeven`, interpolation ราคา) ถูกต้อง
- [x] Risk Exposure Scorecard (`sec-riskfin`) — 🔴 กระทบจากบัค #4 (ค่าแรงข้ามรอบ) แก้แล้ว, `calcRisk()`/`riskExposureForPond()` logic ถูกต้อง
- [x] รอบเลี้ยง (`sec-crops`) — ไม่พบบัค, `confirmCloseCrop()`/archive logic ถูกต้อง (จุดที่เคยพังคือ #4 ซึ่งแก้แล้ว)
- [x] เตรียมบ่อ (`sec-prep`) — ไม่พบบัค
- [x] จับกุ้ง (`sec-harvest`) — ไม่พบบัค (ไม่มีฟังก์ชัน "แก้ไข" รายการ มีแต่ลบ จึงไม่เจอบัคแบบเดียวกับหน้าจัดการน้ำ)
- [x] สถิติจับกุ้ง (`sec-harveststat`) — ไม่พบบัคชัดเจน, หน้านี้ดึงข้อมูลจาก Google Sheets URL แยกต่างหาก (ผูกกับ HarvestSync_AppsScript.gs) ไม่ได้ใช้ D.harvestLog โดยตรง
- [x] PDF ปิดบ่อ (`sec-export`) — ไม่พบบัค
- [x] จัดการบ่อ (`sec-manage`) — ไม่พบบัค, `savePond()`/`delPond()` ถูกต้อง
- [x] จัดการผู้ใช้ (`sec-users`) — ไม่พบบัค, แต่มีข้อสังเกต (ดู #7)
- [x] ราคาวันนี้ (`sec-price`) — ไม่พบบัค, interpolation ราคาตามไซส์ถูกต้อง
- [x] ค่าแรง (`sec-labor`) — 🔴 เจอบัคการเงินจริง แก้แล้ว (ดูรายละเอียด #4)
- [x] ค่าไฟฟ้า (`sec-elec`) — ไม่พบบัคร้ายแรง, เจอความไม่สอดคล้องเล็กน้อย (ดู #5)
- [x] สต็อก (`sec-stock`) — ไม่พบบัค, การตัด/คืนสต็อกอัตโนมัติจากอาหารถูกต้อง
- [x] ABW Growth (`sec-abw`) — ไม่พบบัค, `getAbwTarget()` interpolation ถูกต้อง
- [x] P&L สรุป (`sec-pl`) — ไม่พบบัค, ใช้ข้อมูล archived `harvestResult` ต่อรอบที่ปิดแล้ว ไม่โดนกระทบจากบัค #4 (เพราะกรอกค่าแรงเองตอนปิดรอบ ไม่ได้ใช้ `pondLaborCost` แบบ live)

## บัคที่เจอ / แก้แล้ว

### 1. ภาพรวม — Biomass/ไซส์ ขึ้น "-" (ไม่ใช่บัคโค้ด, เป็น data gap)
- ตรวจโค้ด `pondCard()` / `renderOv()` / `getLatestBiomass()` (index.html:4217-4292, 3287-3291) แล้ว logic ถูกต้อง
- ไซส์และ Biomass คำนวณจาก `p.samples` (การสุ่มตัวอย่างล่าสุด) เท่านั้น — ถ้าบ่อไม่เคยถูกบันทึกการ "สุ่มตัวอย่าง" เลย ค่าจะเป็น "-" ตามดีไซน์
- บ่อ A.14/A.15/A.22/A.21/A.23 ที่เห็นในหน้าเว็บจริงอายุ 96-105 วันแต่ยังไม่มีข้อมูลสุ่มเลย แปลว่าทีมงานยังไม่ได้กรอกข้อมูลหน้า "สุ่มตัวอย่าง" ให้บ่อเหล่านี้
- **สาเหตุเชิงโปรดักต์ (ไม่ใช่โค้ดพัง):** เช็คลิสต์ประจำวันในหน้าภาพรวม (`ov-checklist`) มีแค่ "ค่าน้ำ / อาหาร / สต็อกรายวัน" — ไม่มีการเตือนให้สุ่มตัวอย่างเป็นระยะ (ปกติควรสุ่มทุก 7-14 วัน) ทำให้บ่อถูกลืมได้ง่าย
- **ข้อเสนอ (ยังไม่ได้แก้ เพราะเป็นการเพิ่มฟีเจอร์ ไม่ใช่ bug fix):** เพิ่ม badge เตือนในการ์ดบ่อ เช่น "⚠ ไม่ได้สุ่มมา 14+ วัน" เมื่อ `today() - lastSample.date > 14`

### 2. รายละเอียดบ่อ — dead code `backToOv()` (index.html:3943-3947) ✅ ลบแล้ว
- ฟังก์ชันนี้อ้างอิง `document.getElementById('sec-ov')` แต่ id จริงของหน้าภาพรวมคือ `sec-overview` — ถ้าถูกเรียกจะพัง (null.classList error)
- ตรวจแล้วไม่มีปุ่มไหนเรียกฟังก์ชันนี้เลยในทั้งไฟล์ (ปุ่ม "‹ ภาพรวม" จริงใช้ `document.getElementById('tab-overview').click()` แทน ซึ่งทำงานถูกต้อง) — ไม่กระทบผู้ใช้งานจริง เป็นโค้ดขยะ **ลบออกแล้วทั้ง 2 ไฟล์**

### 4. 🔴 ค่าแรงรั่วข้ามรอบเลี้ยง กระทบ Break-even + Risk Exposure (แก้แล้ว)
**ไฟล์:** `index.html` และ `TawanFarm_App.html` บรรทัด ~3032 (ฟังก์ชัน `pondLaborCost`)

**บัคคืออะไร:** `pondLaborCost(p)` เดิมรวมค่าแรงจาก `D.laborCosts` "ทุกรายการที่เคยบันทึกให้บ่อนี้" โดยไม่กรองช่วงวันที่เลย:
```js
// เดิม (ผิด)
function pondLaborCost(p){
  return Math.round((D.laborCosts||[]).filter(r=>r.pondId===p.id).reduce((s,r)=>s+(r.amount||0),0));
}
```
แต่ตอน "ปิดรอบเลี้ยง" (`confirmCloseCrop()` บรรทัด ~7595) ระบบจะ archive แล้ว **เคลียร์** `p.records/feedLog/samples/waterLog/chemLog/labResults` ให้บ่อพร้อมเริ่มรอบใหม่ — แต่ **ไม่ได้แตะ `D.laborCosts` เลย** เพราะมันเป็น array กลางที่ผูกกับ `pondId` เฉยๆ ไม่ผูกกับรอบเลี้ยง

**ผลกระทบจริง:** บ่อไหนที่เลี้ยงมาแล้ว ≥2 รอบ (เช่น บ่อ A.1 ปิดรอบ 1 ไปแล้วกำลังเลี้ยงรอบ 2) ตัวเลข "ต้นทุนค่าแรง" ในหน้า **Break-even** (`sec-breakeven`) และ **Risk Exposure Scorecard** (`sec-riskfin`) จะบวกค่าแรงของรอบเก่าที่ปิดไปแล้วเข้ามาด้วย ทำให้:
- ต้นทุนสะสมของรอบปัจจุบันสูงเกินจริง
- สถานะ "คุ้มทุนแล้ว/ยังไม่คุ้มทุน" ผิด (เอียงไปทาง "ยังไม่คุ้มทุน" มากกว่าความจริง)
- % ความเสี่ยงเงินลงทุนในหน้า Risk Exposure สูงเกินจริง

ที่จริงระบบมีฟังก์ชัน `getLaborCostForPond(pondId, fromDate, toDate)` (บรรทัด ~4700) ที่กรองตามช่วงวันที่ถูกต้องอยู่แล้ว และหน้า "ค่าแรง" เองก็ไม่ได้ใช้ `pondLaborCost` แบบผิดนี้ — จุดที่พังคือ `pondBreakeven()` เรียก `pondLaborCost(p)` แบบไม่กรองวันที่เท่านั้น

**วิธีแก้:** เปลี่ยนให้ `pondLaborCost` เรียกใช้ `getLaborCostForPond` แบบกรองตาม `p.startDate` ถึงวันนี้ (ตรงกับ pattern ที่ `getElecCostForPeriodByShare(p.startDate,today(),share)` ใช้อยู่แล้ว):
```js
// ใหม่ (แก้แล้ว)
function pondLaborCost(p){
  return Math.round(getLaborCostForPond(p.id,p.startDate,today()));
}
```
แก้ไปแล้วทั้ง `index.html` และ `TawanFarm_App.html` — **ยังไม่ได้ deploy** (ต้อง push ผ่าน `push_*.bat` ตามขั้นตอนปกติของโปรเจกต์)

### 5. ค่าไฟฟ้าใช้วิธีหารสัดส่วนไม่ตรงกัน 2 จุด (minor, ไม่ใช่บัคชัดเจน — เป็นทางเลือกออกแบบ)
- `getElecCostForPeriodByShare()` (index.html:2973) หารตาม **สัดส่วน biomass** ของบ่อ — ใช้ในหน้า Break-even/Risk Exposure (real-time)
- `getElecCostForPeriod()` (index.html:4856) หารเท่าๆ กันตาม **จำนวนบ่อทั้งหมด** (`D.ponds.length||27`) — ใช้ตอน auto-fill ค่าไฟในฟอร์ม "ปิดรอบเลี้ยง" (`openCropModal` บรรทัด 7483)
- ผลคือค่าไฟที่ประเมินให้บ่อเดียวกันจะไม่ตรงกันระหว่างตอนเลี้ยงอยู่ (หน้า Break-even) กับตอนปิดรอบ (ฟอร์มปิดบ่อ) เพราะวิธีหารคนละแบบ — ไม่ถึงกับ "พัง" แต่ตัวเลขไม่ perfectly consistent ถ้าต้องการความแม่นยำควรเลือกใช้วิธีเดียวกันทั้งสองจุด (แนะนำวิธี share-based เพราะแม่นกว่า proportional ตามจำนวนบ่อเฉยๆ)

### 6. 🔴 ปุ่ม "แก้ไข" ในหน้าจัดการน้ำสร้างรายการซ้ำแทนอัปเดต (แก้แล้ว)
**ไฟล์:** `index.html` และ `TawanFarm_App.html` — markup หน้า `sec-watlog` + ฟังก์ชัน `editWaterLog()`/`saveWaterLog()`/`clearWaterLog()`

**บัคคืออะไร:** ปุ่ม ✏️ ในตารางประวัติจัดการน้ำเรียก `editWaterLog(pid, idx)` ซึ่งดึงค่าฟิลด์เดิมมาใส่ในฟอร์มให้แก้ไข แต่ไม่ได้เก็บว่า "กำลังแก้ไขรายการไหนอยู่" เลย พอกด "บันทึก" ฟังก์ชัน `saveWaterLog()` เดิมจะสร้าง record ใหม่ (id ใหม่จาก `Date.now()`) แล้ว push เข้า array เสมอ — ไม่เคยอัปเดตของเดิม ผลคือ **กด "แก้ไข" แล้ว "บันทึก" จะได้ข้อมูลซ้ำ 2 รายการ แทนที่จะแก้ไขของเดิม** (ต่างจากฟอร์มอื่นอย่าง WQ/อาหาร/สุ่มตัวอย่าง ที่ผูก record ด้วย pond+วันที่ ซึ่งใช้ทับกันได้ แต่หน้าน้ำอนุญาตหลายรายการต่อวันโดยตั้งใจ จึงต้องมีตัวระบุ record แยกต่างหาก)

**วิธีแก้:** เพิ่ม hidden input `wl-edit-id` เก็บ id ของ record ที่กำลังแก้, `editWaterLog()` ตั้งค่านี้เมื่อกดแก้ไข, `saveWaterLog()` เช็คว่ามีค่านี้หรือไม่ — ถ้ามีให้ใช้ id เดิมและอัปเดตตำแหน่งเดิมใน array แทนการ push ใหม่ และเคลียร์ค่านี้ทั้งหลังบันทึกสำเร็จและตอนกด "ล้างฟอร์ม" — แก้ไปแล้วทั้ง 2 ไฟล์ **ยังไม่ได้ deploy**

### 7. การซ่อนข้อมูลการเงินจาก staff เป็นแค่ระดับ UI (ข้อสังเกต ไม่ใช่บัค ไม่ได้แก้)
`applyRole()` (index.html:2076) ซ่อนแท็บ/หน้าการเงิน (`FINANCE_TABS`/`FINANCE_SECS`: จับกุ้ง, สถิติจับกุ้ง, PDF ปิดบ่อ, ค่าแรง, ค่าไฟฟ้า, P&L, จัดการบ่อ, Break-even, Risk Exposure) ด้วย `style.display='none'` เมื่อ role ไม่ใช่ admin — เป็นการซ่อนแค่ฝั่ง UI/browser เท่านั้น ถ้า staff เปิด DevTools เรียกฟังก์ชันตรงๆ (เช่น `switchTab('pl')`) ก็ยังเห็นข้อมูลได้ เพราะข้อมูลทั้งหมดโหลดมาที่ฝั่ง client อยู่แล้ว การป้องกันจริงต้องทำที่ Firebase Security Rules (`firebase_rules.json`) ไม่ใช่ที่ UI — เป็นข้อจำกัดที่มีมาแต่ต้นของสถาปัตยกรรมแอปนี้ (client-side single file) ไม่ใช่สิ่งที่เพิ่งพัง แจ้งไว้เผื่อพี่อยากจะเสริมความปลอดภัยเพิ่มในอนาคต

### 3. เฝ้าระวังโรค — dead code ยืนยันแล้ว (sec-disease) ✅ ลบแล้ว
- ไม่มีปุ่ม `tab-disease` หรือทางเข้าใดๆ ในเมนูเรียก `switchTab('disease')` เลย — ยืนยันตรงกับ commit `9704a93` ที่บอกว่าถอดเมนูนี้ออกแล้ว
- **ลบออกแล้วทั้งหมด**: markup `sec-disease`, ฟังก์ชัน `DISEASES`/`DW_SEV`/`dwState`/`dwReset`/`dwSevClass`/`dwToggleSym`/`dwSetSev`/`renderDiseaseWatch`/`dwSevLabel`/`renderDwSummary`/`initDisease`/`onDsPond`/`onDsDate`/`loadDsForDate`/`saveDs`/`clearDs`, entry `'ds-pond':''` ใน `populateAll()`, router `if(t==='disease')initDisease();`, และการอ้างอิง `rec.diseaseWatch`/`renderDwSummary()` ใน `saveWQ()`/`deleteWQ()` — ลบออกจากทั้ง `index.html` และ `TawanFarm_App.html`, ทดสอบแล้วว่า WQ save/delete ยังทำงานปกติไม่มี error

### 8. สต็อกพัสดุ — 3 ช่องโหว่ logic (แก้แล้ว)
**ไฟล์:** `index.html` และ `TawanFarm_App.html` — markup หน้า `sec-stock` + ฟังก์ชัน `saveStockTxBatch()`/`deleteStockLog()`/`stockTab()`

**8.1 เบิกออกไม่บังคับเลือกบ่อปลายทาง** — เดิม `saveStockTxBatch()` ตอนโหมด "เบิกออก ไปบ่อ" ไม่เช็คว่าเลือกบ่อหรือยัง ถ้าลืมเลือกแล้วบันทึก สต็อกจะถูกตัดจริงแต่ `pondId` ว่างเปล่า ทำให้รายการหายไปจากหน้า "ประวัติ" (กรองด้วย pondId) และไม่ถูกคิดต้นทุนบ่อไหนเลยตอนปิดบ่อ (`getInventoryCostForPond` กรอง pondId ตรง) — **แก้แล้ว**: เพิ่มเช็ค `if(mode==='out'&&!pondId)` แจ้งเตือนและ block การบันทึก พร้อมใส่ `*` ที่ label "บ่อปลายทาง"

**8.2 ประวัติ "รับเข้า" ดูไม่ได้เลยในแอป** — เดิมหน้า "ประวัติ" โชว์แค่ฝั่งเบิกออกรายบ่อ ส่วนรายการรับเข้าส่วนกลาง (`type==='in'`) ไม่มีที่แสดงเลย มีแค่ตัวเลขนับจำนวนครั้งบน KPI card — **แก้แล้ว**: เพิ่ม toggle ในแท็บ "ประวัติ" แยก "📤 เบิกออก รายบ่อ" / "📥 รับเข้าส่วนกลาง" ฟังก์ชันใหม่ `setStockLogMode()`/`renderStockInLog()` แสดงตารางรับเข้าทั้งหมดเรียงตามวันที่ ลบได้เหมือนกัน

**8.3 ลบรายการประวัติผิด → สต็อกไม่ย้อนกลับ** — เดิม `deleteStockLog()` ลบ log อย่างเดียว ไม่แก้ `item.qty` ให้ (มีแค่ข้อความเตือนว่า "ยอดสต็อกจะไม่ถูกย้อนกลับ") ต้องไปแก้จำนวนคงเหลือเองอีกขั้น เสี่ยงลืม — **แก้แล้ว**: `deleteStockLog()` คำนวณย้อนยอดให้อัตโนมัติ (`out` ที่เคยตัดออก → บวกคืน, `in` ที่เคยรับเข้า → หักออก) ทดสอบแล้วว่าตัวเลขกลับมาถูกต้อง (119→99 ตอนลบ in, 99→100 ตอนลบ out)

ทดสอบทั้ง 3 จุดผ่าน browser + mock data แล้ว ไม่มี console error

### 9. สต็อกพัสดุ — ปรับหมวดหมู่ใหม่ ตัด "ลูกกุ้ง" ออก (แก้แล้ว)
**ไฟล์:** `index.html` และ `TawanFarm_App.html` — `STOCK_CATEGORIES`/`STOCK_CAT_LABEL`/`STOCK_CAT_CSS`/`STOCK_CATALOG` + dropdown 3 จุด (`sk-cat`, `sk-filter-cat`, `ch-hist-cat`)

เปลี่ยนจากเดิม 4 หมวด (ลูกกุ้ง/อาหารกุ้ง/ยา-สารเคมี/อุปกรณ์-วัสดุ) เป็น 7 หมวดใหม่ตามที่แจ้ง: **อาหารกุ้ง, ยากิน, ยาลงน้ำ, จุลินทรีย์กิน, จุลินทรีย์ลงน้ำ, ปูน, อุปกรณ์** — ตัด **ลูกกุ้ง** ออกทั้งหมด เพราะบันทึกซ้ำกับข้อมูลตอนลงกุ้งในหน้าอื่นอยู่แล้ว

- ลบตัวเลือก "ลูกกุ้ง" ออกจาก dropdown ทั้ง 3 จุด และลบ 13 รายการลูกกุ้งออกจาก `STOCK_CATALOG` (แคตตาล็อกราคาไว้กด "นำเข้าสินค้า")
- หมวด "ยา/สารเคมี" เดิม (~90 รายการใน `STOCK_CATALOG`) ถูก**แยกอัตโนมัติ**เป็นยากิน/ยาลงน้ำ/จุลินทรีย์กิน/จุลินทรีย์ลงน้ำ/ปูน โดยดูจากชื่อ/หมายเหตุสินค้า (เช่น มีคำว่า "ปูน" → หมวดปูน, มีคำว่า "จุลินทรีย์/ไบโอ/Bac/Probiotic" → หมวดจุลินทรีย์) — **⚠️ เป็นการแยกแบบประมาณการ ไม่ได้ตรวจสอบกับพี่ทีละรายการ แนะนำให้เข้าไปเช็ค/แก้หมวดของแต่ละสินค้าอีกครั้งในหน้า "รายการพัสดุ" (ปุ่ม ✏️) ให้ตรงกับการใช้งานจริง**
- รายการพัสดุที่เคย**บันทึกไว้จริงในฐานข้อมูล** (ไม่ใช่แค่ในแคตตาล็อก) ที่มีหมวด "ลูกกุ้ง"/"ยา-สารเคมี" แบบเดิม **จะไม่หายหรือพัง** — ยังคงแสดงผลได้ปกติ (label ต่อท้ายด้วย "(เดิม)") เพราะเก็บ mapping หมวดเก่าไว้ใน `STOCK_CAT_LABEL`/`STOCK_CAT_CSS` เพื่อ backward-compat แต่จะเลือกหมวดเดิมพวกนี้ไม่ได้แล้วตอนเพิ่ม/แก้ไขรายการใหม่ — พี่สามารถกดแก้ไขรายการเก่าให้เป็นหมวดใหม่เองได้ทีละรายการเมื่อสะดวก
- ทดสอบผ่าน browser: นำเข้าแคตตาล็อก 105 รายการ ครบทั้ง 7 หมวด ไม่มีหมวด seed หลงเหลือ ไม่มี console error

### 10. ปิดรอบ (openCropModal) — ดึงต้นทุนสารเคมีจากสต็อกจริงอัตโนมัติ (เพิ่มใหม่)
**ไฟล์:** `index.html` และ `TawanFarm_App.html` — `getStockCostBreakdownForPond()` (ฟังก์ชันใหม่) + `openCropModal()` + markup `cropModal` (ช่อง "ต้นทุนสารเคมี")

ตอนปิดรอบ (ปุ่ม "ปิดรอบ" ที่ยังเลี้ยงอยู่ ไม่ใช่แก้ไขรอบที่ปิดแล้ว) ช่อง "ต้นทุนสารเคมี" ตอนนี้**ดึงข้อมูลจริงจากสต็อก**ให้อัตโนมัติแทนที่จะพิมพ์เอง — รวมยอดจากรายการที่เบิกออกจริง (`D.inventoryLog`, `type==='out'`) ของบ่อนั้นในช่วงวันที่เลี้ยง (ไม่รวมหมวด "อาหารกุ้ง" เพราะคิดแยกจาก `pondFeedCost` อยู่แล้ว) พร้อมโชว์บรรทัดสรุปแยกตามหมวด (เช่น "ยาลงน้ำ 2,500฿ · ปูน 1,620฿") ให้เห็นที่มาแบบโปร่งใส ไม่ใช่กล่องดำ — ตัวเลขรวมยังแก้ไขเองได้ถ้าจำเป็น เหมือนกับต้นทุนลูกกุ้ง/อาหารที่ดึงอัตโนมัติอยู่ก่อนแล้ว

**ขอบเขตที่ตั้งใจ:** แก้เฉพาะการ "ปิดรอบปัจจุบัน" (`openCropModal`/`confirmCloseCrop`) เท่านั้น — **ไม่แตะ** โมดัล "แก้ไขรอบที่ปิดแล้ว" (`editCropModal`/`ecm-cost-chem`) และ**ไม่แตะ**ข้อมูลประวัติเก่า `chemLog`/`DEFAULT_CATS` เลยตามที่พี่ระบุไว้ (ข้อมูลรอบที่ปิดไปแล้วให้อยู่เหมือนเดิม)

ทดสอบผ่าน browser + mock data: เบิกพัสดุ 2 หมวด (ยาลงน้ำ 5×500=2,500, ปูน 10×162=1,620) ให้บ่อ A.1 แล้วเปิดโมดัลปิดรอบ ยืนยันว่าช่องต้นทุนสารเคมีขึ้น 4,120 ตรงกับยอดจริง และบรรทัดแยกหมวดถูกต้อง, บ่อที่ไม่มีประวัติเบิกจะเว้นว่างและซ่อนบรรทัดสรุปตามปกติ ไม่มี console error

### 11. หน้าอาหาร (feed grid) — ตัดช่อง "ยี่ห้อ/เบอร์" ออก + ตัวเลือกสต็อกโชว์เฉพาะที่ยังมีของ (แก้แล้ว)
**ไฟล์:** `index.html` และ `TawanFarm_App.html` — `feedGridRow()`/`renderFeedGrid()`/`saveFeedGridAll()`

- ลบช่องกรอก "ยี่ห้อ/เบอร์" (`fdg-*-brand`) ออกจากตารางบันทึกอาหารรายวันทุกบ่อ เพราะข้อมูลสินค้าที่เลือกในช่อง "สต็อกที่ตัด" บอกยี่ห้อ/เบอร์ครบอยู่แล้ว ไม่ต้องพิมพ์ซ้ำ — แต่ยังเก็บฟิลด์ `feedBrand` ในข้อมูลไว้เหมือนเดิม (สำคัญกับตาราง "ประวัติอาหาร" ตอนดูรอบที่ปิดแล้ว) โดยตอนบันทึกจะ**ดึงชื่อสินค้าที่เลือกในช่องสต็อกมาใส่ให้อัตโนมัติแทนการพิมพ์เอง**
- ช่อง "สต็อกที่ตัด" (dropdown เลือกอาหารกุ้งเพื่อตัดสต็อก) ตอนนี้**กรองเฉพาะสินค้าที่ยังมีคงเหลือ (qty>0)** สินค้าที่สต็อกหมดแล้วจะไม่โชว์ในตัวเลือกอีก — ยกเว้นสินค้าที่ถูกเลือกไว้แล้วจากบันทึกเดิม (กันกรณีสต็อกหมดหลังบันทึกไปแล้ว จะยังเห็นค่าที่เคยเลือกอยู่ ไม่หายไปเงียบๆ)
- เพิ่มคอลัมน์ "ผู้จำหน่าย" ต่อจาก "สต็อกที่ตัด" — auto-fill จากฟิลด์ "ผู้จำหน่าย/หมายเหตุ" (`item.note`) ของสินค้าที่เลือกในหน้ารายการพัสดุ อัปเดตอัตโนมัติทันทีที่เปลี่ยนตัวเลือกสต็อก (`fdGridUpdateSupplier()`) เป็นข้อมูลอ่านอย่างเดียว ไม่ใช่ช่องกรอก

ทดสอบผ่าน browser + mock data: ลบคอลัมน์ยี่ห้อ/เบอร์ออกจากตารางแล้ว, สินค้าหมดสต็อก (qty=0) ไม่โชว์ใน dropdown, สินค้ามีของ (qty=50) โชว์ปกติ, บันทึกแล้ว `feedBrand` ถูกเซ็ตอัตโนมัติเป็นชื่อสินค้าที่เลือก, กรณีสินค้าที่เคยเลือกไว้สต็อกหมดทีหลัง (qty เป็น 0) ยังคงโชว์เป็นตัวเลือกที่ถูกซีเล็คอยู่ ไม่มี console error

### 12. เพิ่ม role "CEO" — หน้าภาพรวมผู้บริหาร (เพิ่มใหม่)
**ไฟล์:** `index.html` และ `TawanFarm_App.html` — markup `sec-ceo` (ใน `.main`, ก่อน `sec-overview`), ฟังก์ชันชุด `ceo*`/`renderCeo()`/`showCeoOnly()`/`getStockItemsForPond()`, CSS block "CEO DASHBOARD", แก้ `applyRole()`/`renderUsers()`/`initFirebase()`

ผ่านการรีวิว mockup กับพี่หลายรอบก่อนเริ่มเขียนโค้ดจริง (ดูสรุปดีไซน์ท้ายหัวข้อนี้)

**พฤติกรรม role=ceo:**
- ซ่อนเมนูทั้งหมด (side-nav, แถบแท็บบน, bottom-nav) ด้วย `body.role-ceo` — เห็นแค่หน้า `sec-ceo` เต็มจอ ไม่มีเมนูกรอกข้อมูลรายวันให้เห็นเลย ตรงตามที่ขอ ("ไม่จำเป็นต้องมีปุ่มกดเยอะ")
- `applyRole('ceo')` เรียก `showCeoOnly()` บังคับซ่อนทุก `.sec` แล้วโชว์แค่ `sec-ceo` ทันที ไม่ผ่านกลไก `goTab()` ปกติ (กัน CEO เผลอกดไปหน้าอื่นเพราะไม่มีปุ่มเมนูให้กดอยู่แล้ว)
- ถ้า admin เปลี่ยน role ของ user ที่กำลังใช้งานอยู่ออกจาก "ceo" แบบ real-time (ผ่านหน้าจัดการผู้ใช้) จะพากลับไปหน้าภาพรวมปกติอัตโนมัติแทนที่จะค้างหน้า CEO ที่ตอนนี้ไม่มีสิทธิ์ดูแล้ว (`ceoLeaveIfActive()`)

**หน้า CEO Dashboard มีอะไรบ้าง:**
- KPI 5 ใบ เห็นครบในหน้าเดียว ไม่ต้องเลื่อน: ต้นทุนสะสมรวมทั้งฟาร์ม (hero), อาหารรวมวันนี้, Biomass รวม, บ่อที่กำลังเลี้ยง, บ่อควรติดตาม — **ไม่มีรายได้/กำไรประเมิน** เพราะไม่มีข้อมูลราคาตลาดอ้างอิงจริง (หน้า "ราคาวันนี้" ถูกลบไปแล้วก่อนหน้านี้)
- การ์ดบ่อทั้งหมด (จาก `wqfeedActivePonds()`): sparkline การเติบโตจาก ABW ของ `p.samples`, Biomass/ไซส์/อาหารวันนี้/อาหารต่อแสนตัว (normalize ด้วย `p.stock`), ต้นทุนสะสม, ป้ายสถานะ "ปกติ/ควรติดตาม" (`ceoStatus()` เทียบ ABW ล่าสุดกับ `getAbwTarget(age)` — ต่ำกว่ามาตรฐาน 15%+ ถือว่าควรติดตาม)
- แตะการ์ด → bottom sheet (`ceoOpenSheet()`) โชว์: ข้อมูลบ่อ (วันลงกุ้ง, แหล่งลูกกุ้ง, น้ำหนักรวมสะสม, อาหารรวมสะสมจาก `getCumFeed()`, จัดการน้ำล่าสุดจาก `p.waterLog`), ค่าน้ำเช้าล่าสุดจาก `p.records`, การเติบโต (Biomass/ไซส์/FCR จาก `getFCR()` + กราฟ), และ accordion 3 หัวข้อแบบแตะขยาย (ไม่เปลี่ยนหน้า): **ต้นทุนสะสม**, **ค่าน้ำย้อนหลัง 7 วัน**, **อาหารย้อนหลัง 7 วัน**

**ต้นทุนสะสม (จุดสำคัญ) — ดึงจากสต็อกจริง ไม่ใช่กรอกมือ:**
- ฟังก์ชันใหม่ `getStockItemsForPond(pondId,startDate,endDate)` คืนรายการเบิกสต็อกจริง (`D.inventoryLog`, `type==='out'`, pondId ตรง, ช่วงวันที่เลี้ยง) **ไม่รวมหมวดอาหาร** — itemized ทีละรายการ (ชื่อ/หมวด/วันที่/จำนวน×ราคา) พร้อมจุดสีตามหมวด (`CEO_CAT_DOT`)
- เหตุผลที่ไม่รวมอาหารในรายการ itemized: ต้นทุนอาหารคำนวณจาก `pondFeedCost(p)` อยู่แล้ว (ฟังก์ชันเดิม ใช้ราคาเฉลี่ยสำรองถ้าวันนั้นไม่ได้ผูกกับสต็อกจริง) — ถ้า itemize อาหารซ้ำจะทำให้ต้นทุนอาหารถูกนับซ้ำ/ไม่สอดคล้องกับตัวเลขที่ใช้อยู่แล้วในหน้าปิดรอบ (`openCropModal`)
- ต้นทุนสะสมรวม = `p.costFry` (กรอกตอนลงกุ้ง) + `pondFeedCost(p)` (อาหาร) + ผลรวม stock items (ไม่รวมอาหาร) + `getLaborCostForPond()` (ค่าแรง) + `getElecCostForPeriod()` (ค่าไฟ) — ใช้ฟังก์ชันเดิมที่มีอยู่แล้วทั้งหมด ไม่สร้างวิธีคำนวณคู่ขนานใหม่ที่อาจไม่ตรงกับตัวเลขในหน้าปิดรอบ

**สิทธิ์ในหน้า "จัดการผู้ใช้":** เพิ่มตัวเลือก "CEO (ผู้บริหาร)" ใน dropdown เปลี่ยน role และฟอร์มเพิ่มผู้ใช้ใหม่ — ไม่ต้องแก้ Firebase Console เอง แค่เข้าหน้านี้เลือก role ได้เลยเหมือนเดิม (ตอบคำถามพี่ก่อนเริ่มสร้าง — ดูรายละเอียดในบทสนทนา)

ทดสอบผ่าน browser + mock data ครบ: `applyRole('ceo')` ซ่อนเมนูทั้งหมดถูกต้อง, KPI/การ์ดบ่อ render ตรงข้อมูลจริงที่ seed ไว้ (Biomass, ต้นทุนรวม, itemized stock cost ตรงตัวเลข), accordion เปิด/ปิดถูกต้อง, สลับ role ออกจาก ceo ระหว่างใช้งานพากลับหน้าภาพรวมถูกต้อง, dropdown "จัดการผู้ใช้"/"เพิ่มผู้ใช้ใหม่" มีตัวเลือก CEO ครบ ไม่มี console error

**ประวัติดีไซน์ (ก่อนเขียนโค้ดจริง):** รีวิว mockup 4 รอบผ่าน Artifact — (1) เวอร์ชันเดสก์ท็อป มีรายได้/กำไรประเมิน → พี่ขอปรับเป็นมือถือเป็นหลัก (2) ตัดรายได้ออกเพราะไม่มีราคาตลาดจริง เพิ่มอาหารรวมต่อวัน (3) ปรับ KPI ให้เห็นครบไม่ต้องเลื่อน ใช้สี/ธีมเดียวกับแอปจริงเป๊ะ เพิ่มอาหารต่อแสนตัว (4) เพิ่มบล็อกข้อมูลบ่อ (วันลงกุ้ง/แหล่งลูกกุ้ง/ค่าน้ำเช้า/น้ำหนัก-อาหารสะสม/จัดการน้ำล่าสุด) — ไฟล์ม็อกอัพอยู่ที่ `Mockup และ Prototype/mockup_ceo_dashboard.html` (ใช้ข้อมูลจำลอง ไม่ใช่ของจริง เก็บไว้อ้างอิงดีไซน์เท่านั้น)

### 79. ทดลองเวชภัณฑ์ — เพิ่มตัวชี้วัด EHP-ตัวอ่อน (%) และเนื้อเยื่อตับ-ลำไส้ (ลำไส้อักเสบ%/เม็ดไขมันรวม%)

พี่ขอเพิ่มตัวชี้วัดในแท็บ "ทดลองเวชภัณฑ์" (ข้อ 74-75): EHP/Microsporidian (ตัวอ่อน EHP %) และเนื้อเยื่อตับ-ลำไส้ (ลำไส้อักเสบ%, เม็ดไขมันรวม%) — ทำ mockup ยืนยันก่อน (อัปเดตไฟล์เดิม `mockup_ทดลองเวชภัณฑ์.html` เป็น v3) พี่ยืนยันแล้วให้ implement

**จุดดี:** ทั้ง 3 ค่าที่ขอมีฟิลด์เก็บอยู่แล้วในหน้า "ผลแล็บ" (`ehpLarva`, `gutInflam`, `lipidDroplet`) ไม่ต้องเพิ่มจุดกรอกข้อมูลใหม่ที่ไหนเลย แค่ดึงมาโชว์ในหน้าทดลองเวชภัณฑ์

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):
- `TRIAL_METRICS` เพิ่ม 2 รายการ: `ehp` ("EHP — ตัวอ่อน (%)") และ `gutLipid` ("เนื้อเยื่อตับ-ลำไส้") — ค่าเริ่มต้นติ๊กไว้ทั้งคู่ (ทั้งใน `_trialDraft` เริ่มต้นและ `newTrial()`)
- `renderTrialResults()` เพิ่ม 2 เงื่อนไข: `m.ehp` ดึง `labResults` ล่าสุดของแต่ละบ่อ field `ehpLarva` ทำเป็นการ์ดแท่งเทียบรายบ่อ 1 การ์ด; `m.gutLipid` ดึง `gutInflam`+`lipidDroplet` ทำเป็น**2 การ์ดแยก** (เม็ดไขมันรวมมีหมายเหตุ "ยิ่งสูงยิ่งดี" กำกับไว้ เพราะทิศทางตรงข้ามกับตัวชี้วัดอื่นในหน้านี้)
- ตาราง "รายละเอียดรายบ่อ" เพิ่ม 3 คอลัมน์: EHP ตัวอ่อน, ลำไส้อักเสบ, ไขมันรวม

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 2 บ่อ (ค่าผลแล็บต่างกันชัดเจน) ผ่านการคลิก/พิมพ์จริง: การ์ดแท่งเทียบทั้ง 3 ตัวชี้วัดใหม่คำนวณตรงกับข้อมูลจำลอง (EHP 3%/18%, ลำไส้อักเสบ 6%/30%, ไขมันรวม 82%/48%) ตรงกันทั้งการ์ดและตาราง ไม่มี console error, sync สองไฟล์แล้ว

### 78. ปรับใหญ่หน้า "ค่าไฟฟ้า" ทั้งหมด — ให้ทำงานคล้ายหน้า "ค่าแรง"

พี่ขอให้ปรับหน้า "ค่าไฟฟ้า" ทำงานคล้ายหน้า "ค่าแรง" (ข้อมูลสอดคล้องกัน กวาดสายตาเห็นง่าย) พร้อมให้เสนอตัดส่วนที่ไม่จำเป็นและแนะนำสิ่งที่ควรเพิ่ม — ทำ mockup ยืนยันก่อน (`Mockup และ Prototype/mockup_ค่าไฟฟ้า_ปรับหน้าใหม่.html`) พี่ยืนยันแล้วให้ implement เข้าแอปจริง

**ไฟล์:** `index.html`/`TawanFarm_App.html` — เขียนหน้า `sec-elec` ใหม่ทั้งหมด

**โครงสร้างใหม่ — แยก 2 แท็บย่อย** (`.elec-sub-btn`/`.elec-pane` + `elecTab()` คลาสใหม่แยกจาก `.labor-sub-btn`/`.labor-pane` โดยตั้งใจ — ทดสอบยืนยันแล้วว่าถ้าใช้ชื่อคลาสเดียวกันจะชนกัน เพราะ `laborTab()`/ฟังก์ชันคล้ายกันใช้ `querySelectorAll` แบบ global ไม่ scope เฉพาะหน้า สลับแท็บย่อยหน้าหนึ่งจะไปรีเซ็ตสถานะแท็บย่อยอีกหน้าโดยไม่ตั้งใจ):
- **📝 บันทึกบิล** — ฟอร์มกรอกหม้อ 1/2/3 เดิมทั้งหมด ไม่มีการเปลี่ยนแปลง + ตารางประวัติบิล (จากข้อ 77) ย้ายมาอยู่ในแท็บนี้
- **📋 ประวัติ/สรุป** — เนื้อหาใหม่ทั้งหมด

**เพิ่ม (เดิมไม่มี):**
- KPI การ์ดใหม่ 3 ใบ: "บ่อที่กำลังเลี้ยงตอนนี้", "ค่าไฟ/บ่อ/วัน (ตอนนี้)", และที่สำคัญที่สุด **"ค่าไฟ/กก. เฉลี่ย (รอบที่ปิด)"** — ฟังก์ชันใหม่ `elecDataStartDate()` (หาวันแรกที่มีบิลไฟจริงจาก `D.electricityBills` แบบเดียวกับ `laborDataStartDate()`) กรองเฉพาะรอบที่ปิดตั้งแต่มีข้อมูลบิลจริง
- แถบตรวจสอบ "บ่อที่กำลังเลี้ยงอยู่วันนี้" (`renderElecActiveSection`) — ใช้ `wqfeedActivePonds()` เดียวกับหน้าอื่น ให้เช็คตาเปล่าว่าตัวหารตรงหน้างานไหม
- กราฟแนวโน้มใหม่ (`renderElecTrendChart`, Chart.js mixed bar+line) — แท่ง=บิลรวมทั้งฟาร์ม, เส้น=อัตรา/บ่อ/วัน (2 แกน Y) แทนกราฟแท่งเดี่ยวเดิมที่ตอบไม่ได้ว่าบิลสูงขึ้นเพราะบ่อเพิ่มหรือค่าไฟแพงขึ้นจริง
- ตาราง **"ค่าไฟ/กก. เทียบรอบที่ปิดแล้ว"** (`renderElecCropSummary`) — โครงสร้าง/badge ปกติ-สูงกว่าเฉลี่ย-สูงผิดปกติ เหมือน `renderLaborCropSummary` เป๊ะ

**ตัดออก (ตามที่เสนอและพี่ยืนยัน):**
- กราฟ "แนวโน้มค่าไฟรายปี" (`renderElecYearlyTrend`, `el-yearly-trend`) — เทียบยอดบิลดิบข้ามปีโดยไม่ปรับตามจำนวนบ่อที่เปลี่ยนทุกปี ไม่มีประโยชน์ ลบพร้อม CSS `.yr-*` ที่ใช้เฉพาะจุดนี้ และค่าคงที่ `ELEC_COSTPERKG_REF` (ข้อมูลอ้างอิงภายนอกที่เคยผูกไว้กับกราฟนี้)
- KPI "จำนวนบิล" และ "เทียบเดือนก่อน" — ไม่ช่วยตัดสินใจอะไร/ซ้ำกับกราฟแนวโน้มใหม่ที่เห็นชัดกว่า
- กราฟแท่งเดี่ยว "ค่าไฟ 12 เดือนย้อนหลัง" (`renderElecChart`, `el-chart`) — แทนที่ด้วย `renderElecTrendChart` ที่ให้ข้อมูลมากกว่า

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 7 เดือน + 4 บ่อ (1 บ่อมีรอบปิดแล้ว) ผ่านการคลิกจริงทั้งหมด: KPI 4 การ์ดคำนวณถูกต้องตรงกัน, แถบบ่อ active แสดงถูกต้อง (4 บ่อ), กราฟ Chart.js สร้าง 2 dataset ถูกต้อง, ตาราง ค่าไฟ/กก. ตรงกับ KPI การ์ด (16.36 ตรงกันทั้ง 2 จุด), บันทึก/ลบบิลผ่านปุ่มจริงรีเฟรชทุกส่วนใหม่ถูกต้อง, ทดสอบยืนยันว่าสลับแท็บย่อยหน้าค่าไฟฟ้าไม่กระทบสถานะแท็บย่อยหน้าค่าแรง (และกลับกัน) ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว

### 77. หน้า "ค่าไฟฟ้า" — เพิ่มคอลัมน์ "อัตรา/วัน", "บ่อที่กำลังเลี้ยง", "อัตรา/บ่อ/วัน" ในตารางประวัติบิล

ต่อจากข้อ 76 — พี่แจ้งว่า "card ข้อมูลค่าไฟ" ไม่มีอะไรเปลี่ยนแปลง ตรวจสอบแล้วพบว่าไม่ใช่บั๊ก: ข้อ 76 แก้แค่**ตัวเลขต้นทุนที่ใช้คำนวณ** (auto-fill ตอนปิดรอบ/CEO/ต้นทุนสะสม) แต่**ไม่เคยเพิ่มคอลัมน์แสดงผลตามที่ mockup ทำไว้เข้าหน้า "ค่าไฟฟ้า" จริง** — พี่ที่ดู mockup ไว้คาดหวังเห็นคอลัมน์ อัตรา/วัน, บ่อที่กำลังเลี้ยง, อัตรา/บ่อ/วัน ในตาราง "ประวัติบิลค่าไฟ" ของหน้าจริงด้วย จึงเพิ่มให้ตรงกับ mockup

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`) — `renderElecHistory()` เพิ่ม 3 คอลัมน์ต่อแถวบิล:
- **อัตรา/วัน (฿)** = `totalBaht ÷ จำนวนวันในเดือนนั้น`
- **บ่อที่กำลังเลี้ยง** = `activePondCountOnDate()` (ฟังก์ชันเดิมที่ใช้หารค่าแรง/ค่าไฟอยู่แล้ว) นับ ณ วันสุดท้ายของเดือนนั้น — ถ้าเป็นเดือนปัจจุบันที่ยังไม่จบเดือน (วันสุดท้ายยังมาไม่ถึง) นับ ณ วันนี้แทน
- **อัตรา/บ่อ/วัน (฿)** = อัตรา/วัน ÷ จำนวนบ่อ

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 3 เดือน + 3 บ่อทยอยเริ่มเลี้ยงคนละเดือน (ม.ค.=1 บ่อ, ก.พ.=2 บ่อ, มี.ค.=3 บ่อ): ตารางแสดงจำนวนบ่อถูกต้องตามช่วงเวลาจริงของแต่ละเดือน, อัตรา/บ่อ/วันคำนวณตรง (2,206→1,271→995 ตามลำดับ ลดลงเพราะมีบ่อมาช่วยหารมากขึ้น) ไม่มี console error, sync สองไฟล์แล้ว

### 76. ต้นทุนค่าไฟรายบ่อ — เปลี่ยนจากอัตราคงที่ 2,500 บ./วัน เป็นบิลไฟจริง÷บ่อที่เลี้ยงอยู่จริงรายวัน (สูตรเดียวกับค่าแรง)

พี่ถามว่าสูตรค่าแรง (ข้อ 65-72) เอาไปปรับใช้กับค่าไฟได้ไหม — ตรวจโค้ดพบว่า `getElecCostForPeriod()` เดิมใช้อัตราคงที่ `2,500 บาท/วัน × จำนวนวันเลี้ยง` ไม่สนใจว่าเลี้ยงกี่บ่อพร้อมกันเลย ทั้งที่แอปมีหน้า "ค่าไฟฟ้า" ให้กรอกบิลจริงรายเดือนอยู่แล้ว (`D.electricityBills`) แต่ไม่เคยถูกใช้คำนวณต้นทุนต่อบ่อ (มีคอมเมนต์เดิมบอกไว้ว่า "เผื่อหาความเชื่อมโยงในอนาคต") — ทำ mockup ให้ดูก่อน (`Mockup และ Prototype/mockup_ค่าไฟฟ้า_หารตามบ่อที่เลี้ยงจริง.html`, เพิ่มคอลัมน์ "บ่อที่กำลังเลี้ยง"/"อัตรา/บ่อ/วัน" ตามที่พี่ขอเพิ่ม) พี่ยืนยันแล้วให้ implement เข้าแอปจริง

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`) — คงชื่อ/พารามิเตอร์ `getElecCostForPeriod(fromDate,toDate)` เดิมไว้ทุกจุดที่เรียกอยู่แล้ว (`pondRunningCost`, auto-fill ตอน "ปิดรอบ" `cm-cost-elec`, `ceoTotalCost`, P&L) ได้ผลลัพธ์ใหม่อัตโนมัติไม่ต้องแก้จุดเรียก:
- ฟังก์ชันใหม่ `dailyElecRate(dateStr)` — หาบิลไฟจริงของเดือนที่ครอบวันที่กำหนดจาก `D.electricityBills` แปลงเป็นอัตรา/วัน (`totalBaht ÷ จำนวนวันในเดือนนั้น`) ถ้าเดือนนั้นยังไม่มีบิลจริง (เช่นเดือนปัจจุบันที่บิลยังไม่ออก) ใช้บิลเดือนล่าสุดที่มีจริงยืดไปเป็นค่าประมาณการ — แพทเทิร์นเดียวกับ `weekOfDate` ของค่าแรงเป๊ะ
- `getElecCostForPeriod` วนลูปรายวันเหมือน `getLaborCostForPond` รวม `dailyElecRate(ds)` หารด้วย `activePondCountOnDate(ds)` (ฟังก์ชันเดิมที่ใช้หารค่าแรงอยู่แล้ว) แล้วบวกสะสม
- ลบค่าคงที่ `ELEC_RATE_PER_DAY=2500` ที่ไม่ใช้แล้วออก

ทดสอบผ่าน local server ด้วยข้อมูลจำลองบิลไฟ ม.ค.-ก.ค. 2569 ตรงตาม mockup: กรณี 1 บ่อ (ไม่มีการหาร) ผลลัพธ์ตรงกับคำนวณด้วยมือเป๊ะ (diff=0, เร็ว 3.3ms), กรณี 6 บ่อจำลองสถานการณ์เดียวกับ mockup — กดปุ่ม "ปิดรอบ" จริงจากตารางรอบเลี้ยง ช่อง "ต้นทุนค่าไฟฟ้า" auto-fill ตรงกับที่คำนวณเป๊ะ, หน้า "ค่าไฟฟ้า" (กรอกบิลจริง/ดูประวัติ) ยังทำงานปกติไม่มี regression ไม่มี console error, sync สองไฟล์แล้ว

### 75. ทดลองเวชภัณฑ์ — เปลี่ยนจากปุ่ม "ใช้/ไม่ใช้" เป็นติดป้ายชื่อสินค้าต่อบ่อ (รองรับเทียบ A vs B vs C ในการทดลองเดียว)

พี่ถามว่าถ้าอยากเทียบสินค้า A กับสินค้า B โดยตรง (ไม่ใช่แค่ "ใช้ vs ไม่ใช้" สินค้าเดียว) จะทำยังไง — ของเดิม (ข้อ 74) ออกแบบมาแค่ 2 กลุ่มตายตัว ยังไม่รองรับ ปรับตามที่พี่เลือก:

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):
- แต่ละบ่อที่เลือกในรายการ "เลือกบ่อที่ต้องการเปรียบเทียบ" เปลี่ยนจากปุ่ม "ใช้/ไม่ใช้" (`setTrialGroup`, ค่าคงที่ 2 แบบ) เป็น**ช่องพิมพ์ชื่อสินค้าอิสระต่อบ่อ** (`setTrialLabel`) — พิมพ์ชื่อเดียวกันในหลายบ่อ = จัดกลุ่มเดียวกันอัตโนมัติ, พิมพ์คนละชื่อ = แยกเป็นคนละกลุ่ม รองรับเทียบได้มากกว่า 2 สินค้าพร้อมกันในการทดลองเดียว (เว้นว่าง = ไม่ระบุ/ไม่ใช้อะไร) มี datalist แนะนำชื่อที่เคยพิมพ์ไว้แล้วในบ่ออื่นของการทดลองเดียวกัน กันพิมพ์ชื่อเดียวกันไม่ตรงกัน (เช่น "สินค้า A" vs "สินค้าA")
- สีในกราฟ/แท่งเทียบ/ตาราง กำหนดจาก**ชื่อป้ายที่แตกต่างกัน** (`TRIAL_LABEL_COLORS`, ไล่สีตามลำดับชื่อที่พบก่อน-หลัง) แทนสีคงที่ 2 กลุ่มเดิม — บ่อที่ไม่ระบุชื่อ (เว้นว่าง) ใช้สีเทากลางเป็นกลุ่ม "ไม่ระบุ" เสมอ
- ตัดเส้นประ/เส้นทึบในกราฟการเติบโตออก (เพราะไม่ใช่ binary 2 กลุ่มแล้ว) แยกด้วยสีอย่างเดียว, ตัดคลาส CSS `.grp-toggle`/`.grp-btn`/`.grp-tag.use`/`.grp-tag.nouse` ที่ไม่ใช้แล้วออกทั้งหมด (ระหว่างทำพบว่า `.grp-btn` ที่ตั้งไว้เดิมชื่อชนกับคลาสเดิมของปุ่มเมนูกลุ่มในไซด์บาร์/มือถือ `.tab-grp>.grp-btn` — แม้จะไม่กระทบเพราะ specificity ของกฎเดิมสูงกว่า แต่ถอดออกเพื่อไม่ให้เสี่ยงชนกันในอนาคต)
- แก้คำอธิบายในการ์ดเลือกบ่อให้ตรงกับพฤติกรรมใหม่ (อธิบายวิธีเทียบ A vs B ตรงๆ ในข้อความ)

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 4 บ่อ (2 บ่อพิมพ์ "สินค้า A" เหมือนกัน, 1 บ่อพิมพ์ "สินค้า B", 1 บ่อเว้นว่าง) ผ่านการพิมพ์จริงในช่องกรอก (`input` event ไม่ใช่เรียกฟังก์ชันตรง): กราฟแยกสีถูกต้อง 3 กลุ่ม (สินค้า A สีน้ำเงิน, สินค้า B สีส้ม, ไม่ระบุ สีเทา), legend/ตารางแสดงชื่อป้ายตรงกับที่พิมพ์, บันทึก/โหลดการทดลองกลับมาได้ป้ายครบถูกต้อง ไม่มี console error, sync สองไฟล์แล้ว

### 74. เพิ่มแท็บใหม่ "🧪 ทดลองเวชภัณฑ์" ในหน้า "เปรียบเทียบ"

พี่ต้องการหน้าเทียบผลการทดลองใช้เวชภัณฑ์ระหว่างบ่อ — ทำ mockup ให้ดูก่อน 2 รอบ (รอบแรกออกแบบเป็น "กลุ่มทดลอง vs กลุ่มควบคุม" auto-detect จากประวัติเบิกสต็อกทั้งฟาร์ม แต่พี่แก้โจทย์ 3 ข้อ: (1) ชื่อเวชภัณฑ์อาจไม่ได้เบิกผ่านสต็อกทุกครั้ง ต้องกรอกเองได้ (2) เทียบแค่ 2-4 บ่อที่เลือกเอง จากรอบที่กำลังเลี้ยงอยู่ตอนนี้เท่านั้น ไม่เอารอบเก่ามาเทียบ (3) เพิ่มค่าน้ำเป็นตัวชี้วัด — mockup ที่ยืนยัน: `Mockup และ Prototype/mockup_ทดลองเวชภัณฑ์.html`) แล้ว implement เข้าแอปจริง

**ไฟล์:** `index.html`/`TawanFarm_App.html` — เพิ่มปุ่มหมวด `cmp-cat-trial` ในหน้า "เปรียบเทียบ" (`sec-compare`) ต่อจากหมวดเดิม (คุณภาพน้ำ/อาหาร/ไซส์/แล็บ/น้ำ), เนื้อหาใหม่ `#cmp-trial`

**กลไกหลัก:**
- **ชื่อการทดลอง** — ช่องพิมพ์อิสระ (`trial-name`) พร้อม datalist แนะนำจากชื่อสินค้าที่เคยเบิกให้บ่อจริง (`D.inventoryLog` type='out') แต่ไม่บังคับเลือกจากรายการ
- **เลือกบ่อ 2-4 บ่อ** — ใช้ `wqfeedActivePonds()` เดียวกับหน้าอื่น (กรองเฉพาะบ่อที่กำลังเลี้ยงอยู่จริง ไม่ดึงรอบปิดแล้วมาปน) แสดง DOC ต่อบ่อ กดเลือกแล้วมีปุ่ม "ใช้/ไม่ใช้" ต่อบ่อให้ระบุเองเป็นรายบ่อ (เพราะสินค้าทดลองอาจไม่มีประวัติเบิกให้ auto-detect ได้)
- **ตัวชี้วัด 5 อย่าง เลือกได้หลายอย่าง**: การเติบโต (ABW/ADG จาก `p.samples`), FCR ประเมิน (ใช้ `getFCR()` เดิม), กุ้งตายสะสม% (จาก `p.mortalityLog` เดิม ÷ `pRelease()`), ผลแล็บ TCBS-ตับ (จาก `p.labResults` ล่าสุด `hepatoGreen`), ค่าน้ำ (เลือกพารามิเตอร์ได้จาก `PARAMS` เดิม เฉลี่ยจาก `p.records`)
- **กราฟการเติบโต** — Chart.js line chart แกน x = อายุกุ้ง (DOC จาก `sample.age`) ไม่ใช่วันปฏิทิน เพื่อเทียบบ่อที่ลงกุ้งคนละวันได้อย่างยุติธรรม เส้นทึบ=ใช้ (โทนน้ำเงิน) เส้นประ=ไม่ใช้ (โทนเทา)
- **ตัวชี้วัดอื่น** แสดงเป็นแท่งเทียบรายบ่อ (ไม่ใช่ค่าเฉลี่ยกลุ่มก้อนใหญ่ เพราะเทียบแค่ 2-4 บ่อที่ระบุชื่อเอง)
- **บันทึกการทดลองได้** (`D.medTrials[]` — ฟิลด์ใหม่ระดับบนสุด ตามแพทเทิร์นเดียวกับ `D.dailyLaborWeeks` คือ lazy-init ด้วย `||[]` ทุกจุดที่อ่าน ไม่ต้องแก้ `normalizeD`) เลือกดูการทดลองเก่า/สร้างใหม่/ลบได้จาก dropdown ด้านบน
- คำเตือนความรับผิดชอบ: ข้อมูลนี้เทียบจากบ่อจริงที่กำลังเลี้ยงอยู่ ไม่ใช่การทดลองแบบควบคุมทางวิทยาศาสตร์ ปัจจัยอื่นอาจปนอยู่ ใช้เป็นข้อมูลอ้างอิงเท่านั้น

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 4 บ่อที่กำลังเลี้ยงอยู่ (DOC 45-52 วัน, 2 บ่อใช้เวชภัณฑ์/2 บ่อไม่ใช้) ผ่านการคลิกจริงทั้งหมด (ไม่เรียกฟังก์ชันตรง): เลือกบ่อจาก chip picker ครบ 4 บ่อ, สลับกลุ่มใช้/ไม่ใช้ต่อบ่อถูกต้อง, กราฟ Chart.js สร้าง 4 เส้นถูกต้อง, ตัวชี้วัดอื่น (FCR/กุ้งตาย%/TCBS-ตับ/ค่าน้ำ) คำนวณตรงกับข้อมูลจำลอง, สลับพารามิเตอร์น้ำเปลี่ยนผลถูกต้อง, ปิดติ๊ก "การเติบโต" ซ่อนการ์ดกราฟถูกต้อง, บันทึก/โหลด/ลบการทดลองผ่าน dropdown จริงทำงานถูกต้องครบ, การ์ดเตือน &lt;2 บ่อขึ้นถูกต้อง, จำกัดสูงสุด 4 บ่อทำงานถูกต้อง (alert แจ้งเตือน), สลับไปมาระหว่างหมวดเดิม (คุณภาพน้ำ/อาหาร/ไซส์/แล็บ/น้ำ) ยังทำงานปกติไม่มี regression ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว

### 73. 🔴 แก้บั๊ก: หน้าค่าแรงยังโชว่ข้อความเตือนเก่าว่า "ไม่เชื่อมกับปิดรอบอัตโนมัติ" ทั้งที่เชื่อมแล้ว

พี่ถามว่าทำไมยังขึ้นข้อความเตือนว่ายังไม่เชื่อมต่อกัน ทั้งที่เพิ่งเชื่อมไปในข้อ 72 — สาเหตุ: ตอนทำข้อ 72 ผมแก้แค่ logic การ auto-fill (`openCropModal`) แต่ลืมแก้แบนเนอร์คำเตือนคงที่ (`#lb-link-note`) ที่ติดอยู่หัวหน้า "ค่าแรง" มาตั้งแต่ข้อ 45 (ตอนนั้นเขียนไว้ถูกต้องว่า "ไม่เชื่อมกับปิดรอบอัตโนมัติ ต้องกรอกเอง") — พอเชื่อมจริงในข้อ 72 แล้ว ข้อความนี้กลายเป็นเท็จทันทีแต่ไม่มีใครไปแก้

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`): เปลี่ยนแบนเนอร์เป็น "✅ เชื่อมกับ 'ปิดรอบ' อัตโนมัติแล้ว — ตอนกดปุ่ม 'ปิดรอบ' ช่องต้นทุนค่าแรงจะดึงยอดจากหน้านี้มาให้อัตโนมัติ ยังแก้ไขตัวเลขเองได้ถ้าจำเป็น" พร้อมเปลี่ยนสีจากเหลือง (เตือน) เป็นเขียว (ok) ให้ตรงกับความหมายใหม่

ทดสอบผ่าน local server: เปิดแท็บ "ค่าแรง" ข้อความในแบนเนอร์ตรงกับที่แก้ไขถูกต้อง ไม่มี console error, sync สองไฟล์แล้ว — **บทเรียน:** เวลาที่การแก้ไขทำให้ข้อความ/คำเตือนที่เคยเขียนไว้ถูกต้องกลายเป็นเท็จ ต้องไล่หาข้อความอธิบายที่เกี่ยวข้องด้วยทุกครั้ง ไม่ใช่แค่แก้ logic แล้วจบ

### 72. เชื่อมต้นทุนค่าแรงเข้ากับฟอร์ม "ปิดรอบ" อัตโนมัติ (เดิมกรอกเองมาตั้งแต่ข้อ 29)

พี่ยืนยันว่าเทียบตัวเลขที่คำนวณจากสูตรใหม่ (ข้อ 65-71) กับยอดที่เคยกรอกเองไว้ในฟอร์มปิดรอบแล้ว **ไม่ต่างกัน** — ให้เชื่อมเข้ากับฟอร์ม "ปิดรอบ" อัตโนมัติได้เลย (ย้อนกลับการตัดสินใจเดิมในข้อ 29 ที่ให้กรอกเองเพราะตอนนั้นยังไม่มีระบบคำนวณที่แม่นพอ — ตอนนี้มีแล้วและพี่ตรวจสอบยืนยันความถูกต้องแล้ว)

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`) — เฉพาะฟอร์ม **"ปิดรอบ"** (`openCropModal`/`cm-cost-labor`, ปุ่ม "ปิดรอบ" ที่บ่อยังเลี้ยงอยู่) เท่านั้น:
- `cm-cost-labor` เปลี่ยนจากเว้นว่างให้กรอกเอง เป็น auto-fill จาก `getLaborCostForPond(pid,p.startDate,today())` — สูตรเดียวกับที่ใช้ทั่วทั้งแอป (เงินเดือนประจำ+พนักงานรายวัน หารตามจำนวนบ่อ active จริงรายวัน) ยังแก้ไขตัวเลขเองได้ถ้าจำเป็น (ไม่ใช่ readonly)
- แก้ label ในฟอร์มจาก "(กรอกเอง)" เป็น "(ดึงจากข้อมูลเงินเดือน+ค่าแรงรายสัปดาห์ แก้ไขได้)" ให้ตรงกับพฤติกรรมใหม่

**ขอบเขตที่ตั้งใจไม่แตะ:** ฟอร์ม **"แก้ไขข้อมูลปิดบ่อ"** (`editCropModal`/`ecm-cost-labor` — ใช้แก้รอบที่ปิดไปแล้วในอดีต) ยังคงกรอกเองเหมือนเดิม ไม่ auto-fill เพราะการแก้ไขรอบเก่าไม่ควรคำนวณใหม่จากข้อมูลรายสัปดาห์ปัจจุบัน (ข้อมูลรายสัปดาห์อาจถูกแก้ไข/เพิ่มเติมภายหลังได้ ถ้า auto-fill รอบเก่าด้วยจะทำให้ตัวเลขปิดรอบเก่าขยับได้โดยไม่ตั้งใจทุกครั้งที่เปิดฟอร์มแก้ไข)

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง (บ่อ A.1 ลงกุ้ง 1 ม.ค. 69, เงินเดือนประจำ 22,000, ค่าแรงรายสัปดาห์ 56,000/สัปดาห์ต่อเนื่องถึงวันนี้) กดปุ่ม "ปิดรอบ" จริงจากตารางรอบเลี้ยง (ไม่เรียกฟังก์ชันตรง) → `cm-cost-labor` auto-fill 1,770,827 บาท ตรงกับ `getLaborCostForPond()` เป๊ะ, ยังแก้ไขค่าในช่องเองได้ปกติ ไม่มี console error, sync สองไฟล์แล้ว

### 71. เพิ่ม 2 คอลัมน์ในหน้าค่าแรง: "จำนวนวันที่เลี้ยง" (ค่าแรง/กก.) และ "บ่อที่กำลังเลี้ยง" (ประวัติจ่ายรายสัปดาห์)

พี่ขอเพิ่มข้อมูล 2 จุด:

1. **ตาราง "ค่าแรง/กก. เทียบรอบที่ปิดแล้ว"** (`renderLaborCropSummary`) — เพิ่มคอลัมน์ "จำนวนวันที่เลี้ยง" ต่อแถว คำนวณจาก `dDiff(c.startDate,c.closeDate)+1` (นับรวมวันลงกุ้งด้วย ใช้ `dDiff()` ตัวเดียวกับที่หน้าอื่นในแอปใช้อยู่แล้ว)
2. **ตาราง "ประวัติจ่ายค่าแรงรายสัปดาห์"** (`renderDailyWeekTable`) — เพิ่มคอลัมน์ "บ่อที่กำลังเลี้ยง" ต่อแถว ใช้ `activePondCountOnDate(r.weekEnd)` (ฟังก์ชันเดิมที่ใช้หารต้นทุนค่าแรงต่อบ่ออยู่แล้ว) นับ ณ วันตัดรอบ (วันสุดท้าย) ของสัปดาห์นั้น — ช่วยให้พี่เห็นตาตาไปพร้อมกันว่ายอดที่จ่ายสัปดาห์นั้นถูกหารด้วยกี่บ่อ

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 2 บ่อ (บ่อหนึ่งปิดรอบไปแล้ว บ่อหนึ่งเริ่มเลี้ยงใหม่ทีหลัง) ยืนยันว่า "จำนวนวันที่เลี้ยง" คำนวณตรง (100 วัน จากช่วง 1 ธ.ค. 68 – 10 มี.ค. 69) และ "บ่อที่กำลังเลี้ยง" นับถูกต้องแยกตามช่วงเวลาจริงของแต่ละสัปดาห์ (สัปดาห์ที่บ่อ 2 ยังไม่เริ่มเลี้ยงนับแค่ 1 บ่อ, สัปดาห์ที่บ่อ 1 ปิดไปแล้วแต่บ่อ 2 เริ่มแล้วก็นับแค่ 1 บ่อเช่นกัน คนละบ่อกัน) ไม่มี console error, sync สองไฟล์แล้ว

### 70. 🔴 แก้บั๊กร้ายแรง: "ค่าแรง/กก. เฉลี่ย" อ่านผิดฟิลด์น้ำหนักจับ ทำให้รอบที่ปิดจริงหายไปเกือบหมด (6 รอบ แทนที่จะเป็น 27)

พี่แจ้งว่า "รอบที่ปิดแล้ว (ใช้คำนวณ)" ขึ้น 6 รอบ ทั้งที่หน้า "ประวัติรอบเลี้ยง (ปิดแล้ว)" ปี 2569 มี 27 รอบ — ตรวจโค้ดพบว่าเป็นบั๊กเดิมที่มีมาก่อนหน้านี้แล้ว (ไม่ใช่เพิ่งเกิดจากการแก้ข้อ 69) ที่เพิ่งถูกเปิดโปงเพราะข้อ 69 เพิ่มการกรองตามวันที่เข้ามาด้วย

**บั๊กคืออะไร:** `renderLaborKPI()` และ `renderLaborCropSummary()` อ่านน้ำหนักจับจาก `c.totalWeight` ตรงๆ — แต่ `confirmCloseCrop()` (ฟังก์ชันที่ปิดรอบจริงในแอป) ไม่เคยเก็บน้ำหนักไว้ที่ฟิลด์นี้เลย เก็บไว้ที่ `c.harvestResult.totalWeight` แทน (เหมือนกับที่ P&L/PDF ปิดบ่อ/สถิติจับกุ้งใช้กันอยู่แล้วถูกต้อง — ดู `(c.harvestResult||{}).totalWeight` บรรทัด 4151) ผลคือ**ทุกรอบที่ปิดจริงผ่านแอปไม่เคยถูกนับในค่าเฉลี่ยนี้เลยตั้งแต่ต้น** เหลือแค่ 6 รอบที่ขึ้นเพราะเป็นข้อมูล seed/ทดสอบเก่าที่บังเอิญมีทั้ง `totalWeight` และ `harvestResult.totalWeight` ครบ 2 ฟิลด์

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`): เปลี่ยนทั้ง 2 ฟังก์ชันให้อ่าน `c.totalWeight||(c.harvestResult||{}).totalWeight` (รองรับทั้งรอบจริงที่ปิดผ่านแอปและข้อมูล seed เก่าที่ยังมีฟิลด์แบบเดิม) — ตรวจสอบจุดอื่นทั้งไฟล์ที่ใช้ `.totalWeight` แล้วยืนยันว่าจุดอื่นทั้งหมด (P&L, PDF, สถิติจับกุ้ง) อ่านถูกฟิลด์อยู่แล้ว บั๊กนี้จำกัดอยู่แค่ 2 ฟังก์ชันในหน้าค่าแรงที่เพิ่งสร้างขึ้นจากข้อ 65

ทดสอบผ่าน local server จำลอง 27 บ่อ ปิดรอบกระจายทุกเดือน ม.ค.-ก.ค. 2569 ด้วยโครงสร้างข้อมูลแบบเดียวกับที่ `confirmCloseCrop()` สร้างจริง (มีแค่ `harvestResult.totalWeight` ไม่มี `totalWeight` ชั้นบนสุด) — ทั้งการ์ด KPI และตารางในแท็บ "ประวัติ/สรุป" นับได้ครบ 27 รอบตรงกับหน้า "ประวัติรอบเลี้ยง" ถูกต้อง (ก่อนแก้จะเหลือ 0 รอบเพราะไม่มีฟิลด์ `totalWeight` เลย) ไม่มี console error, sync สองไฟล์แล้ว

### 69. ค่าแรง/กก. เฉลี่ย — นับเฉพาะรอบที่ปิดตั้งแต่มีข้อมูล "ประวัติจ่ายค่าแรงรายสัปดาห์" จริงเป็นต้นไป

พี่ยืนยันว่าข้อมูล "ประวัติจ่ายค่าแรงรายสัปดาห์" ที่กรอกไว้เป็นข้อมูลจริงย้อนหลังถึง ม.ค. 2569 และขอให้ค่าเฉลี่ย "ค่าแรง/กก." นับเฉพาะรอบที่ปิดในช่วงที่มีข้อมูลจริงนี้เท่านั้น — รอบเก่ากว่านั้น (ก่อนมีข้อมูลรายสัปดาห์จริง) ไม่ต้องโชว์ จนกว่าจะกรอกข้อมูลย้อนหลังเพิ่ม

**สาเหตุที่ต้องกรอง:** `getLaborCostForPond()` คำนวณส่วนพนักงานรายวันจาก `weekOfDate()` ซึ่งคืนค่า 0 สำหรับวันที่ที่ไม่มีข้อมูลสัปดาห์ใดครอบคลุมถึง (ก่อนสัปดาห์แรกที่เคยบันทึกไว้) — รอบที่ปิดไปแล้วก่อนมีข้อมูลรายสัปดาห์จริงจึงได้ค่าแรง/กก. ต่ำเกินจริง (นับแค่เงินเดือนประจำ ไม่มีค่าแรงรายวันเลย) ก่อนแก้พบว่าค่าเฉลี่ยที่โชว์ (25.89 บาท/กก. จาก 141 รอบ) ถูกรอบเก่าเหล่านี้ฉุดค่าลงมาก เพราะส่วนใหญ่ปิดก่อนมีข้อมูลรายสัปดาห์จริง

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):
- เพิ่มฟังก์ชัน `laborDataStartDate()` — หาวันแรกสุดที่มีข้อมูลจริงจาก `D.dailyLaborWeeks` (ถอยหลัง 6 วันจาก `weekEnd` ของสัปดาห์แรกสุดที่บันทึกไว้ = วันเริ่มของสัปดาห์แรกที่มีข้อมูลจริง) — เป็นค่า**dynamic** ไม่ hardcode ปี 2569 ตรงๆ ถ้าพี่กรอกข้อมูลย้อนหลังเพิ่มในอนาคต (เช่นถึง พ.ย. 2568) ตัวกรองนี้จะขยับตามอัตโนมัติโดยไม่ต้องแก้โค้ดอีก
- กรอง `closed` crops ทั้งใน `renderLaborKPI()` (การ์ด KPI บนสุด) และ `renderLaborCropSummary()` (ตารางในแท็บ "ประวัติ/สรุป") ให้เหลือเฉพาะรอบที่ `closeDate>=laborDataStartDate()`
- เพิ่มข้อความบอกช่วงที่ใช้คำนวณไว้ใต้ตัวเลขทั้ง 2 จุด (เช่น "1 รอบ · ตั้งแต่ปิดรอบ 1 ม.ค. 69 เป็นต้นไป") ให้เห็นชัดว่าตัวเลขไม่ใช่กล่องดำ ตรงตามหลักการเดียวกับที่ทำไว้ในหน้า CEO (ข้อ 61)

ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 3 รอบ (ปิด 2567, ปิด ต.ค.-ธ.ค. 2568 ก่อนมีข้อมูลรายสัปดาห์, ปิด พ.ค. 2569 หลังมีข้อมูลรายสัปดาห์) + ข้อมูลรายสัปดาห์เริ่ม ม.ค. 2569: `laborDataStartDate()` คืนค่า 2026-01-01 ถูกต้อง, ทั้ง 2 การ์ดกรองเหลือแค่รอบที่ 3 (1 รอบ) ตัวเลขค่าแรง/กก. ตรงกันทั้ง 2 จุด (144.59 บาท/กก.) รอบเก่า 2 รอบถูกตัดออกถูกต้อง ไม่มี console error, sync สองไฟล์แล้ว

### 68. 🔴 แก้บั๊กร้ายแรง: กรอกค่าแรงรายวันแล้ว "ไม่มีอะไรเกิดขึ้น" ทั้งแท็บ "พนักงานรายวัน" และ "ประวัติ/สรุป"

พี่แจ้งว่ากรอกข้อมูลแล้วไม่มีอะไรเกิดขึ้นทั้ง 2 แท็บ — สาเหตุคือระหว่างพัฒนาฟีเจอร์นี้ในเซสชันนี้ ผมเปลี่ยนชื่อฟิลด์ที่ใช้ระบุสัปดาห์ไป **3 รอบ** (`weekStart`→`payDate`→`weekEnd`, ดูข้อ 65-67) แต่ละรอบ deploy จริงขึ้น production ไปแล้ว — ถ้าพี่เคยกรอกข้อมูลทดสอบ/จริงไว้ระหว่างช่วงที่ schema เก่ายังใช้งานอยู่ รายการนั้นจะไม่มีฟิลด์ `weekEnd` (ชื่อฟิลด์ปัจจุบัน) ติดค้างอยู่ใน Firebase

**กลไกบั๊ก:** `renderDailyWeekTable()` เรียง `list.sort((a,b)=>b.weekEnd.localeCompare(a.weekEnd))` — ถ้ามีรายการไหนไม่มี `weekEnd` (undefined) โค้ดจะ throw `TypeError` ทันที และเพราะ `saveDailyWeek()` เรียก `renderDailyWeekTable()` **ก่อน** `renderLaborKPI()`/`renderLaborActiveSection()`/`renderLaborMonthChart()`/`renderLaborCropSummary()` — พอ throw กลางทาง โค้ดที่เหลือทั้งหมดหยุดทำงานทันที ตารางประวัติไม่อัปเดต และแท็บ "ประวัติ/สรุป" (ที่พึ่งฟังก์ชันพวกนี้ทั้งหมด) ก็ไม่ถูกเรียกเลย — ตรงกับอาการที่พี่เจอเป๊ะ (ข้อความ "บันทึกเรียบร้อย" ยังขึ้นได้เพราะ set ไว้ก่อนบรรทัดที่ throw แต่ทุกอย่างหลังจากนั้นไม่ทำงาน)

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):
- เพิ่มฟังก์ชัน `normalizeDailyLaborWeeks()` เรียกอัตโนมัติทุกครั้งที่เข้าแท็บ "ค่าแรง" (ใน `initLabor()` ก่อน `renderDailyWeekTable()`) — รายการที่มี `payDate` (สคีมารุ่นข้อ 66) แปลงเป็น `weekEnd` ให้อัตโนมัติ (`weekEnd = payDate - 1 วัน`, ถูกต้องตามสูตร) แล้ว `save()` เก็บกลับ ไม่ต้องกรอกซ้ำ
- รายการที่มีแค่ `weekStart` (สคีมารุ่นแรกสุด ข้อ 65 — ทิศทางตัดรอบผิด ไม่มีทางแปลงเป็น `weekEnd` ได้อย่างแม่นยำเพราะความหมายเดิมผิดตั้งแต่ต้น) จะถูก**คัดออกอัตโนมัติ**ไม่ให้ค้างอยู่ในระบบอีก (กันไม่ให้กลับมาพังซ้ำ) — ถ้าพี่เคยกรอกข้อมูลจริงไว้ช่วงนั้นจะต้องกรอกใหม่ผ่านฟอร์มปัจจุบัน (ตรวจแล้วช่วงที่ schema นี้ใช้งานสั้นมาก แค่ระหว่างข้อ 65 เท่านั้น)
- เพิ่ม guard `.filter(r=>r.weekEnd)` ใน `renderDailyWeekTable()` เป็นเซฟตี้เน็ตอีกชั้น เผื่อมีข้อมูลผิดรูปแบบหลุดเข้ามาในอนาคตจากสาเหตุอื่น จะไม่ทำให้ทั้งหน้าเงียบพังอีก (แค่ไม่แสดงแถวนั้น)

ทดสอบผ่าน local server จำลองสถานการณ์จริง: ใส่รายการเก่าทั้ง 2 สคีมาปนกัน (`weekStart` เก่าสุด + `payDate` รุ่นก่อนหน้า) เปิดแท็บ "ค่าแรง" → `payDate` แปลงเป็น `weekEnd` ถูกต้อง (16/07→15/07), `weekStart` ถูกคัดออกอัตโนมัติ ไม่มี error, จากนั้นกรอกสัปดาห์ใหม่ผ่านปุ่มจริง (`.click()`) → บันทึกสำเร็จ ไม่ throw, ตารางอัปเดตครบ 2 แถว, สลับไปแท็บ "ประวัติ/สรุป" → แถบตรวจสอบบ่อ + KPI ค่าแรง/วัน render ข้อมูลถูกต้องครบ (ก่อนแก้จะว่างเปล่าเพราะ error บล็อกไว้) ไม่มี console error, sync สองไฟล์แล้ว

### 67. ปรับกลับ: ช่องกรอกสัปดาห์ค่าแรงรายวัน — ใช้วันพุธ (วันตัดรอบ) เป็นตัวระบุ ไม่ใช่วันพฤหัสบดี (วันจ่ายจริง)

ต่อจากข้อ 66 — พี่แจ้งว่าจะกรอกข้อมูลด้วย **วันที่ 15 (วันพุธที่ตัดรอบ)** ไม่ใช่วันที่ 16 (วันพฤหัสที่จ่ายจริง) ตามที่ผมเพิ่งแก้ไปในข้อ 66 (รอบต่อไปจะกรอกวันที่ 22) — ปรับกลับให้ตรงกับพฤติกรรมการกรอกจริงของพี่

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`): เปลี่ยนช่องกรอกกลับเป็น **"วันตัดรอบ (ต้องเป็นวันพุธ)"** validate บังคับวันพุธเหมือนเดิม — แต่**ความหมายเบื้องหลังยังคงเป็นตัวแก้จากข้อ 66** (ไม่ได้ย้อนกลับไปใช้ตรรกะเดิมที่ผิดของข้อ 65): ตอนนี้วันที่ที่กรอก (พุธ) ถูกตีความถูกต้องว่าเป็น**วันสุดท้าย/วันตัดรอบของสัปดาห์** ช่วงที่คุมคือ 7 วันย้อนหลัง `[วันที่กรอก-6, วันที่กรอก]` (พฤหัส→พุธ) ไม่ใช่นับเดินหน้าแบบข้อ 65 ที่ผิด
- เปลี่ยนชื่อฟิลด์ข้อมูลจาก `payDate` เป็น `weekEnd` (ความหมาย: วันสุดท้ายของสัปดาห์ค่าแรง = วันพุธที่ตัดรอบ)
- `mostRecentThursdayOf` → เปลี่ยนกลับเป็น `mostRecentWednesdayOf` (ยังคงหาวันที่ใกล้ที่สุดทั้งเดินหน้า/ถอยหลังเหมือนข้อ 66 ไม่ใช่ถอยหลังอย่างเดียวแบบข้อ 65)
- `weekOfDate(dateStr)` เปลี่ยน periodStart เป็น `weekEnd-6` (แทน `payDate-7`) และ periodEnd เป็น `weekEnd` ตรงๆ (แทน `payDate-1`)
- ตารางประวัติเปลี่ยนหัวคอลัมน์เป็น "วันตัดรอบ (พุธ)" สถานะ 🔒 เปลี่ยนข้อความเป็น "ตัดรอบแล้ว" (จากเดิม "จ่ายจริงแล้ว")

ทดสอบผ่าน local server ด้วยสถานการณ์จริงที่พี่ระบุ: กรอกวันที่ 15/07/2569 (พุธ) ยอด 61,500 → ระบบคุมช่วง 9–15/07 ถูกต้อง ขึ้นสถานะ 🔒 ตัดรอบแล้ว (วันนี้ 22/07 ผ่านไปแล้ว), กรอกรอบถัดไปวันที่ 22/07/2569 (พุธ) ยอด 60,000 → `weekOfDate('2026-07-20')` จับคู่กับสัปดาห์ 22/07 ถูกต้อง (คุมช่วง 16–22/07), `weekOfDate('2026-07-15')` ยังคงจับคู่กับสัปดาห์แรกถูกต้องไม่ทับกัน, `getLaborCostForPond` คำนวณ 61,500 บาทตรงเป๊ะสำหรับช่วง 9–15/07 (1 บ่อ) ไม่มี console error, sync สองไฟล์แล้ว

### 66. 🔴 แก้บั๊ก: สัปดาห์ค่าแรงรายวันเข้าใจทิศทางตัดรอบผิด (วันพุธ=วันเริ่ม → ที่ถูกคือวันพุธ=วันตัดรอบ/จบสัปดาห์, จ่ายจริงวันพฤหัสบดี)

พี่ถามยืนยันความเข้าใจ ("เงินรายสัปดาห์จ่ายทุกวันพฤหัสบดี ตัดรอบจบทุกวันพุธ ยอดของสัปดาห์ที่ 15/07/2026 คือยอดที่จ่ายจริงแล้ว ไม่ใช่ยอดประมาณการ") — ตรวจสอบแล้วพบว่าตอนทำข้อ 65 ผมเข้าใจทิศทาง **"ตัดรอบ"** ผิด: เดิมออกแบบให้ผู้ใช้กรอก "สัปดาห์เริ่ม (ต้องเป็นวันพุธ)" แล้วนับไปข้างหน้า 7 วัน (พุธ→อังคารถัดไป) ทั้งที่ "ตัดรอบทุกวันพุธ" ที่พี่บอกไว้จริงๆ แปลว่า**วันพุธคือวันสุดท้าย/วันปิดของสัปดาห์ ไม่ใช่วันแรก** — สัปดาห์การทำงานจริงคือ **พฤหัสบดี→พุธ (7 วัน)** ปิดรอบวันพุธ แล้ว**จ่ายจริงวันถัดไปคือพฤหัสบดี**

**ผลกระทบที่ตรวจพบ:** ทดสอบด้วยวันที่จริงที่พี่ยกตัวอย่าง (15/07/2569) พบว่า 15/07/2569 **เป็นวันพุธจริง** (วันตัดรอบ) — ถ้ากรอกลงช่องเดิมเป็น "สัปดาห์เริ่ม" ระบบเดิมจะตีความว่าคุมช่วง 15/07–21/07 (เดินหน้า) ซึ่ง**ผิดทิศทาง** ที่ถูกต้องคือยอดที่ตัดรอบวันพุธ 15/07 ต้องคุมช่วง **09/07–15/07 (พฤหัส→พุธ ย้อนหลัง)** และจ่ายจริงวันที่ **16/07/2569 (พฤหัสบดี)**

**แก้แล้ว** (`index.html`/`TawanFarm_App.html`):
- เปลี่ยนช่องกรอกจาก "สัปดาห์เริ่ม (ต้องเป็นวันพุธ)" เป็น **"วันจ่ายจริง (ต้องเป็นวันพฤหัสบดี)"** — validation เปลี่ยนจากบังคับวันพุธเป็นบังคับวันพฤหัสบดี, ฟิลด์ข้อมูลเปลี่ยนชื่อจาก `weekStart` เป็น `payDate`
- `mostRecentWednesdayOf` → เปลี่ยนเป็น `mostRecentThursdayOf` — **ไม่ได้แค่เปลี่ยนชื่อ** แก้ logic เป็นหาวันพฤหัสบดี**ที่ใกล้ที่สุด**ทั้งเดินหน้า/ถอยหลัง (เดิมเดินถอยหลังอย่างเดียว) เพราะถ้าพี่พิมพ์วันพุธผิดเข้ามา (คิดว่าเป็นวันตัดรอบ) ควรแนะนำวันพฤหัสถัดไป (จ่ายจริงของสัปดาห์นั้น) ไม่ใช่วันพฤหัสของสัปดาห์ก่อนหน้า
- `weekOfDate(dateStr)` คำนวณใหม่: หาสัปดาห์ที่ **ช่วงคุม `[payDate-7, payDate-1]`** ครอบวันที่ต้องการ (แทนที่จะนับเดินหน้าจาก weekStart แบบเดิม) ถ้าวันที่ต้องการยังไม่ถึงสัปดาห์ที่มีคนกรอก (เช่น สัปดาห์ปัจจุบันที่ยังไม่ถึงวันจ่าย) จะใช้ยอดของสัปดาห์ล่าสุดที่มีอยู่ยืดไปข้างหน้าเป็นค่าประมาณการชั่วคราว (พฤติกรรมเดิมที่ตั้งใจไว้ ไม่เปลี่ยน)
- ตารางประวัติเพิ่มคอลัมน์ "ช่วงที่คุม" โชว์ช่วงวันที่จริงที่ยอดนั้นครอบคลุม (เช่น "9 ก.ค. 69 – 15 ก.ค. 69") ให้เห็นชัดว่ายอดนี้คุมสัปดาห์ไหน ไม่ต้องคำนวณเอง — สถานะ 🔒/📅 เปลี่ยนจากเทียบ `weekEnd<today` เป็นเทียบ `payDate<=today` ตรงไปตรงมา (ถึงวันจ่ายแล้ว = จ่ายจริงแล้ว)

**ยืนยันคำตอบให้พี่:** ถ้ากรอกยอดของสัปดาห์ที่ตัดรอบ 15/07/2569 ในแอปตอนนี้ ต้องกรอกวันที่ **16/07/2569 (พฤหัสบดี)** เป็น "วันจ่ายจริง" (ไม่ใช่ 15/07) ระบบจะคุมช่วง 09/07–15/07 ให้อัตโนมัติ และเมื่อถึง/เลยวันที่ 16/07 ไปแล้ว (วันนี้ 22/07/2569) จะขึ้นสถานะ 🔒 "จ่ายจริงแล้ว" ทันที ตรงกับที่พี่เข้าใจไว้ทุกประการ — ที่ต้องแก้คือ**วันที่ที่กรอกในช่อง** (พฤหัส ไม่ใช่พุธ) เพราะระบบเดิมออกแบบทิศทางผิดตั้งแต่ข้อ 65

ทดสอบผ่าน local server ด้วยข้อมูลจำลองตรงตามสถานการณ์จริงที่พี่ยกตัวอย่าง (payDate 16/07/2569 ยอด 61,500 บาท 32 คน): `weekOfDate` คืนค่าถูกต้องสำหรับทุกวันในช่วง 09/07–15/07 (รวมขอบทั้งสองด้าน), วันก่อนช่วง (08/07) คืน null ถูกต้อง, ตารางประวัติ render คอลัมน์ช่วงที่คุม + สถานะ 🔒 ถูกต้องเป๊ะ, ฟังก์ชันแนะนำวันพฤหัสใกล้ที่สุดทดสอบทั้งกรณีพิมพ์วันพุธ (แนะนำพฤหัสถัดไปถูกต้อง) และวันอาทิตย์ (แนะนำพฤหัสก่อนหน้าถูกต้อง), `saveDailyWeek()`→`getLaborCostForPond()` round-trip เต็มรูปแบบผ่านปุ่มจริงคำนวณได้ 61,500 บาทตรงเป๊ะสำหรับช่วง 7 วันที่คุม (1 บ่อ) ไม่มี console error, sync สองไฟล์แล้ว

### 65. ปรับใหญ่หน้า "ค่าแรง" ทั้งหมด — คำนวณตามจำนวนบ่อที่เลี้ยงจริงรายวัน + พนักงานรายวันบันทึกรายสัปดาห์

พี่คุยเรื่องสูตรค่าแรงกันหลายรอบตั้งแต่ข้อ 59 (mockup แบบหารตามบ่อที่เลี้ยงจริง) จนถึงตัดสินใจ implement เข้าแอปจริงทั้งหมด สรุปสิ่งที่แก้:

**1. ฟังก์ชันคำนวณกลางใหม่ (คงชื่อ `getLaborCostForPond(pondId,fromDate,toDate)` เดิมไว้ ทุกจุดที่เรียกอยู่แล้วได้ผลลัพธ์ใหม่อัตโนมัติไม่ต้องแก้จุดเรียก — `pondRunningCost`, `ceoTotalCost`, `ceoOpenSheet`, `getCloseSummary` ฯลฯ):**
- `allCropWindows()`/`activePondCountOnDate(dateStr)` — หาว่าบ่อไหน "กำลังเลี้ยงอยู่จริง" ในวันใดๆ ทั้งปัจจุบัน (บ่อที่ `p.startDate` มีค่า นับเป็นช่วงเปิดไม่มีวันสิ้นสุด) และย้อนหลัง (ทุกรอบใน `p.cropHistory` ที่มี `startDate`+`closeDate`) — สำคัญเพราะการคำนวณค่าแรงของรอบที่ปิดไปแล้วต้องรู้ว่าตอนนั้นมีกี่บ่อเลี้ยงพร้อมกันจริง ไม่ใช่แค่จำนวนบ่อวันนี้
- `dailyFixedStaffRate(dateStr)` — เงินเดือนพนักงานประจำที่มีผล ณ วันนั้น (ใช้ pattern effective-dated เดิมของ `D.staff`) × 12 ÷ 365 (แม่นกว่า ÷30 เดิม)
- `weekOfDate(dateStr)`/`dailyWeeklyWorkerRate(dateStr)` — หาสัปดาห์ค่าแรงรายวันล่าสุดที่มีผล ณ วันนั้นจาก `D.dailyLaborWeeks` (ฟิลด์ใหม่) ÷ 7
- `getLaborCostForPond` ใหม่วนลูปรายวันในช่วงที่กำหนด รวม (เงินเดือนประจำ+รายวัน) หารด้วยจำนวนบ่อ active วันนั้นๆ แล้วบวกสะสม — ทดสอบแล้วเร็ว (<3ms แม้ช่วง 20+ วัน) ไม่กระทบ performance

**2. ลบแท็บ "ค่าแรงรายบ่อ" (ไม่ได้ใช้งานจริง) แทนที่ด้วย "👷 พนักงานรายวัน (รายสัปดาห์)":**
- ฟอร์มบันทึกยอดจ่ายจริงต่อสัปดาห์ (สัปดาห์เริ่มวันพุธ ตัดรอบทุกวันพุธตามที่พี่ยืนยัน) มี validation เตือนถ้าเลือกวันที่ไม่ใช่วันพุธ พร้อมแนะนำวันพุธที่ใกล้ที่สุด
- ตารางประวัติโชว่สถานะ 🔒 ปิดสัปดาห์แล้ว (ผ่านมาแล้วจริง) / 📅 ประมาณการ (ยังไม่ถึง) แก้ไข/ลบได้
- ลบฟังก์ชันเดิม `saveLaborCost/clearLaborCost/deleteLaborCost/renderLaborTable` และ dropdown ที่ผูกกับ `D.laborCosts` ทั้งหมด (คงตัวแปร `D.laborCosts` เก่าไว้เฉยๆ ไม่ลบข้อมูลเดิมถ้ามี แค่ไม่มี UI ให้กรอก/แสดงอีกต่อไป)

**3. ออกแบบใหม่แท็บ "ประวัติ/สรุป" (เดิมโชว่ข้อมูลที่ผูกกับ `D.laborCosts` ที่ไม่มีประโยชน์) เป็น 4 ส่วนตามที่พี่เลือก:**
- แถบตรวจสอบบ่อที่ระบบนับว่า "กำลังเลี้ยงอยู่" วันนี้ (ใช้ `wqfeedActivePonds()` เดียวกับหน้าอื่น) + ค่าแรง/บ่อ/วันปัจจุบัน — ใช้เช็คตาเปล่าว่าตรงความจริงไหม
- กราฟแท่งซ้อนแนวโน้มค่าแรงรวมรายเดือน 8 เดือนล่าสุด แยกสีเงินเดือนประจำ(คงที่)/รายวัน(ผันแปร)
- ตารางค่าแรง/กก. เทียบรอบที่ปิดแล้ว (ใช้สูตรใหม่) พร้อม badge ปกติ/สูงกว่าเฉลี่ย/สูงผิดปกติ เทียบกับค่าเฉลี่ยทั้งฟาร์ม

**4. อัปเดต KPI การ์ดบนสุดของหน้า (`lb-kpi`)** ให้อ่านข้อมูลจากแหล่งใหม่ (สัปดาห์ล่าสุดแทน `D.laborCosts`) เช่นกัน

**ข้อจำกัดที่ต้องบอกพี่:** ตอนนี้แอปยังไม่มีบัญชี login จริงให้ผมกรอกยอดจ่ายจริงของ 32 คนรายสัปดาห์ย้อนหลัง พี่ต้องเข้าไปกรอกเองผ่านหน้า "พนักงานรายวัน (รายสัปดาห์)" ทุกวันพุธที่จ่ายจริง ระบบจะเริ่มคำนวณแม่นขึ้นเรื่อยๆ ตามจำนวนสัปดาห์ที่กรอกสะสม

- Mockup ที่ใช้ยืนยันตลอดทาง: `mockup_ค่าแรงประจำ_หารตามบ่อที่เลี้ยงจริง.html`, `mockup_ค่าแรง_แบบAแบบB.html`, `mockup_ค่าแรงประจำ_นำเสนอจริง.html`, `mockup_ค่าแรงรายวัน_รายอาทิตย์.html`, `mockup_ค่าแรง_ประวัติสรุปใหม่.html`
- ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 6 บ่อ (2 บ่อมีประวัติปิดรอบ) + 25 สัปดาห์ค่าแรงรายวัน: บันทึก/แก้ไข/ลบสัปดาห์ทำงานถูกต้อง, validation วันพุธทำงานถูกต้อง, `activePondCountOnDate` นับถูกต้องทั้งปัจจุบันและย้อนหลัง (ยืนยันด้วยกรณีทดสอบเฉพาะที่มีบ่อน้อย/มากต่างกันในแต่ละช่วงเวลา), แถบตรวจสอบ/กราฟ/ตารางค่าแรงต่อกก. render ถูกต้องครบ, `pondRunningCost`/`ceoTotalCost`/`ceoOpenSheet` ยังทำงานปกติไม่มี error ไม่มี regression

### 64. ตัดช่อง "ปริมาณ (เซนติเมตร)" ออกจากฟอร์มบันทึกการจัดการน้ำ

พี่แจ้งว่าช่อง "ปริมาณ (เซนติเมตร)" ในการ์ดบันทึกการเปลี่ยน/เติมน้ำ (`sec-watlog`) ไม่ได้ใช้งานจริง เพราะพนักงานกะปริมาณด้วยสายตา ไม่ได้วัดจริง — ลบช่อง `wl-vol` ออกจากฟอร์ม พร้อมเก็บกวาดจุดที่ผูกกับ element นี้:
- `saveWaterLog()` เซ็ต `vol:null` ตรงๆ แทนอ่านค่าจาก input, ตัดข้อความ "X ซม." ออกจากข้อความแจ้งบันทึกสำเร็จ/อัปเดต (จะโชว่ค่าว่างตลอดไปถ้าไม่ตัด)
- `editWaterLog()`/`clearWaterLog()` ตัดการอ้างอิง `wl-vol` ออก (ไม่งั้น error null เวลากดแก้ไข/ล้างฟอร์ม)
- จุดอื่นที่แสดงผล `vol` (ตารางประวัติ, การ์ดรายละเอียดบ่อ, ชีท CEO, กราฟเทียบปริมาณน้ำ) **ไม่ได้แตะ** เพราะมี null-guard อยู่แล้ว (โชว่ "-" หรือซ่อนบรรทัดถ้าไม่มีค่า) ข้อมูลเก่าที่เคยกรอกไว้ (ถ้ามี) จะยังอยู่ครบ แค่ไม่มีการกรอกใหม่อีกต่อไป
- ทดสอบผ่าน local server: บันทึก/แก้ไข/ล้างฟอร์มทำงานปกติไม่มี error, ข้อความแจ้งผลไม่มี "- ซม." ค้างอยู่แล้ว, `vol` ที่บันทึกเป็น `null` ตามที่ตั้งใจ

### 63. 🔴 แก้บั๊ก: หน้า CEO เนื้อหาไม่กึ่งกลางจอบน iPad

พี่แจ้งว่าเปิดหน้า CEO บน iPad/iPhone แล้วข้อมูลไม่เข้ากลางจอ — ตรวจสอบพบสาเหตุ: กฎ CSS ที่จองพื้นที่ sidebar (`.side-nav` กว้าง 230px, บรรทัด ~72-77) เซ็ต `.main{margin-left:230px!important;...}` ไว้ `!important` ทุกจอ ≥601px (รวม iPad ทั้งแนวตั้ง 768px และแนวนอน 1024px) — กฎนี้ตั้งใจไว้สำหรับ role admin/staff ที่มี sidebar แสดงจริง แต่หน้า CEO ซ่อน sidebar ทั้งหมด (`body.role-ceo .side-nav{display:none!important}`) พื้นที่ 230px ที่จองไว้เลยกลายเป็นช่องว่างลอยๆ ทางซ้าย ดันเนื้อหาเบี้ยวไปทางขวา ไม่กึ่งกลางจอ — ปัญหานี้เกิดเฉพาะจอกว้าง ≥601px (iPad) เท่านั้น เพราะกฎนี้ไม่ทำงานที่จอมือถือ (<601px) จึงไม่เจอปัญหาบนมือถือ
- แก้ด้วยการเพิ่ม override เฉพาะ `body.role-ceo` (specificity สูงกว่า `.main` เปล่าๆ ชนะกฎเดิมได้แม้เป็น `!important` ทั้งคู่) รีเซ็ต `margin-left/margin-right:auto` + `width:100%` ที่ breakpoint 601px/700px/1000px ทั้ง 3 จุด ให้กลับมาใช้ `max-width` ตามที่ตั้งใจไว้เดิม (640/900/1180px ตามลำดับ) แทนการยึด margin-left คงที่
- ไม่กระทบ role admin/staff — ยืนยันแล้วว่ายังได้ `margin-left:230px` ตามเดิมเป๊ะ
- ทดสอบผ่าน local server ที่ iPad portrait (768px) และ landscape (1024px): `.main` element กว้างเต็มจอสมมาตรทั้ง 2 ฝั่ง (ช่องว่างซ้าย=ขวา=0px, `centered:true`) เทียบกับก่อนแก้ที่เบี้ยวไปทางขวา 230px ไม่มี console error

### 62. หน้า CEO — สลับลำดับ "สรุปบ่อ" ขึ้นมาก่อน "ค่าน้ำล่าสุด" ในชีทรายละเอียดบ่อ

พี่ขอสลับลำดับบล็อกในชีท: เดิม ข้อมูลบ่อ → ค่าน้ำล่าสุด → สรุปบ่อ → accordion ต้นทุน/ประวัติ เปลี่ยนเป็น ข้อมูลบ่อ → **สรุปบ่อ** → ค่าน้ำล่าสุด → accordion — สลับแค่ตำแหน่ง markup ใน `#ceo-sheet-body` ไม่กระทบ id/ฟังก์ชันที่ผูกข้อมูลเข้าแต่ละบล็อก (`ceoOpenSheet()` เติมข้อมูลเหมือนเดิมทุกอย่าง)
- ทดสอบผ่าน local server: ลำดับ block title ที่ render จริงออกมาเป็น `["ข้อมูลบ่อ","สรุปบ่อ","ค่าน้ำล่าสุด"]` ตรงตามที่ต้องการ ไม่มี console error

### 61. หน้า CEO — ต้นทุนสะสม สรุปรวมต่อสินค้า (ตัดประวัติรายวันที่ค้างจากข้อ 60)

ตอนทำข้อ 60 ผมสร้าง mockup ไว้ทั้ง 2 ส่วน (การ์ด/ชีท + ต้นทุนสะสม) แต่ตอน implement เข้าแอปจริงทำแค่ส่วนการ์ด/ชีท ส่วนต้นทุนสะสมยังเป็นโค้ดเดิม (โชว่รายการเบิกแยกตามวันที่) ค้างไว้โดยไม่ได้ตั้งใจ พี่ถามแล้วตรวจพบว่ายังไม่ได้แก้จริง — แก้ให้ตรงกับ mockup ที่ยืนยันไว้แล้ว:
- ก่อนแสดงผล `itemsHtml` ใน `ceoOpenSheet()` เพิ่ม step group `stockItems` ตาม key (ชื่อ+หมวด+หน่วย) รวม `qty`/`total` ต่อสินค้าก่อนวนแสดง
- แต่ละแถวโชว่แค่ "หมวด · รวม X หน่วย" ไม่โชว่วันที่/รายการแยกแต่ละครั้งอีกต่อไป — ยอดรวมบาทต่อสินค้ายังถูกต้องเหมือนเดิม (sum ของทุก transaction)
- ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 2 รายการเบิกสินค้าเดียวกันคนละวัน (5+5 กก.) → รวมเป็น 1 แถว "รวม 10 กก." ยอด 1,200 ฿ ถูกต้อง ไม่มี console error

### 60. หน้า CEO — ขยายฟอนต์ + ปรับ "สรุปบ่อ" ในชีทรายละเอียดบ่อ

พี่ยืนยัน mockup แล้วว่าการ์ดบ่อหน้าแรก (list) เหมือนเดิมถูกแล้ว ไม่ต้องแก้ แต่ให้ปรับที่ **ชีทรายละเอียดบ่อ** (เปิดตอนกดเข้าไปดูบ่อแต่ละบ่อในหน้า CEO) แทน — ปรับ 2 รอบตามฟีดแบ็ก:
- **หัวชีท** (`.ceo-sheet-meta` — บรรทัด "อายุ X วัน · ABW Y ก." ใต้ชื่อบ่อ): ขยายจาก 11.5px → 15px + ตัวหนาขึ้น ตามที่พี่แจ้งว่ามองไม่เห็น
- **บล็อก "สรุปบ่อ"** (เดิมชื่อ "การเติบโต", `.ceo-stat-grid`): เปลี่ยนจาก 3 คอลัมน์ตัวเล็ก (Biomass/ไซส์/FCR) เป็น 4 ช่อง 2 คอลัมน์ตัวใหญ่ขึ้น (label 10px→12.5px, ค่า 14px→19px) — คงฟิลด์เดิม **Biomass / ไซส์ / FCR ประเมิน** ไว้ตามที่พี่ยืนยัน พร้อมเพิ่ม **Yield/ไร่** (= Biomass ปัจจุบัน ÷ ขนาดบ่อ) เป็นฟิลด์ที่ 4 (ลองสลับเป็นอายุ/น้ำหนักกุ้งไปก่อนรอบแรกเพราะข้อมูลนั้นซ้ำกับหัวชีทอยู่แล้ว พี่ขอให้กลับมาใช้ Biomass/FCR เดิมแทน)
- ต้นทุนสะสม (สรุปรวมต่อสินค้า ไม่โชว่ประวัติรายวัน) ตามที่คุยกันไว้ก่อนหน้า พี่ยืนยันว่าถูกต้องแล้ว ไม่ต้องแก้เพิ่ม
- Mockup ที่ใช้ยืนยันแนวทางเริ่มต้น: `Mockup และ Prototype/mockup_CEO_การ์ดบ่อใหญ่ขึ้น_ต้นทุนสรุปรวม.html`
- ทดสอบผ่าน local server ด้วยข้อมูลจำลอง (บ่อ A.1 อายุ 62 วัน, ABW 20.8g, ไซส์ 48 ตัว/กก., Biomass 8,954 กก./ขนาด 4 ไร่ → Yield 2,239 กก./ไร่): ค่าที่โชว่ในชีทตรงกับที่คำนวณด้วยมือ, ฟอนต์ใหญ่ขึ้นจริงตามที่ตั้ง (หัวชีท 15px, สรุปบ่อ 19px/12.5px), ไม่มี console error

### 59. ปรับเกณฑ์เลือก "บ่อพี่น้อง" ในการ์ดเทียบกับรอบก่อน — เพิ่มความหนาแน่น + แหล่งลูกกุ้ง

เดิม `bmFindSibling()` (ข้อ 57) เลือกบ่อพี่น้องแค่จากขนาดไร่เท่ากัน + อายุ (DOC) ใกล้เคียงที่สุด พี่ขอเพิ่มความหนาแน่นปล่อย (ตัว/ตรม.) และแหล่งลูกกุ้งเข้าไปด้วย เพราะกระทบอัตราการโต/รอดโดยตรง — เปลี่ยนจากเลือกตามอายุใกล้สุดอย่างเดียว เป็นระบบให้คะแนนถ่วงน้ำหนัก 3 ปัจจัย (ขนาดไร่ยังเป็นเงื่อนไขบังคับต้องเท่ากันเหมือนเดิม ไม่ได้เอาเข้าคะแนน):
- ผลต่างอายุ (วัน) ÷ 5
- ผลต่างความหนาแน่น (`dens()` ที่มีอยู่แล้ว) ÷ 10
- แหล่งลูกกุ้งไม่ตรงกัน +3 (เทียบเท่าอายุห่างกัน 15 วัน — ให้น้ำหนักมากสุดเพราะกระทบผลลัพธ์การเลี้ยงมากกว่าอายุ/ความหนาแน่น)
- เลือกบ่อคะแนนรวมต่ำสุด (ยิ่งใกล้เคียงยิ่งดี)
- อัปเดต label ในการ์ดให้โชว์เหตุผลที่เลือกด้วย เช่น "บ่อ A.7 (กำลังเลี้ยง — หนาแน่น 78 ตัว/ตรม., แหล่งลูกกุ้งเดียวกัน)" — พี่จะเห็นว่าทำไมระบบเลือกบ่อนี้มาเทียบ ไม่ใช่กล่องดำ
- ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 3 บ่อพี่น้องที่แข่งกัน: ยืนยันว่าบ่อที่แหล่งลูกกุ้งตรงกัน (แม้อายุห่างกว่านิดหน่อย) ถูกเลือกเหนือบ่อที่อายุใกล้กว่าแต่แหล่งลูกกุ้งต่างกัน ตรงตามน้ำหนักที่ตั้งไว้ ไม่มี console error

### 58. 🔴 แก้บั๊ก: หน้า "ความเสี่ยง" โชว์บ่อที่จับไปแล้ว + พลาดค่า ALK/DO=0

พี่แจ้งว่าหน้า "ประเมินความเสี่ยง" มีบ่อที่จับไปแล้วขึ้นโชว์ข้อมูล — ตรวจสอบเจอ 2 บั๊กจริงในหน้านี้:
1. **`renderRisk()` วนลูป `D.ponds` ตรงๆ ไม่กรองบ่อที่ปิดรอบแล้ว/ยังไม่ลงกุ้งออก** — ทุกหน้าอื่นในแอปที่ต้องเลือก "บ่อที่กำลังเลี้ยงอยู่" ใช้ helper กลาง `wqfeedActivePonds()` (กรอง `pondStatusOv()!=='prep'` ออก) อยู่แล้ว แต่หน้าความเสี่ยงลืมใช้ตัวนี้ ทำให้บ่อที่ปิดรอบไปแล้ว (`startDate`/`records` ถูกล้างเป็นค่าว่าง → `pondStatusOv` คืน `'prep'`) ยังถูกประเมินความเสี่ยงและโชว์การ์ดอยู่ — แก้โดยเปลี่ยนมาใช้ `wqfeedActivePonds()` เหมือนหน้าอื่น พร้อมเพิ่ม empty state ("ยังไม่มีบ่อที่กำลังเลี้ยงอยู่ให้ประเมินความเสี่ยง") กรณีไม่มีบ่อที่กำลังเลี้ยงเหลือเลย — บั๊กนี้กระทบ "บ่อเสี่ยงสูงสุด" ในหน้าภาพรวมด้วย (`renderOvRiskList` อ่านจาก cache เดียวกัน) แก้จุดเดียวได้ผลทั้ง 2 หน้า
2. **`calcRisk()` เช็ค ALK/DO ต่ำด้วย truthy shortcut (`last.alk&&...`, `last.doM&&...`) แทนเช็ค null/ค่าว่างแบบชัดเจน** — ถ้าค่าที่กรอกเป็น `0` พอดี (ALK=0 หรือ DO=0 ซึ่งเป็นกรณีอันตรายที่สุด) เงื่อนไข `0&&...` จะเป็น false เสมอ ทำให้ระบบไม่เตือนความเสี่ยงเลยทั้งที่ควรเตือนหนักสุด — แก้เป็น `last.alk!=null&&last.alk!==''&&last.alk<120` (แพทเทิร์นเดียวกับที่ nh4/no2 ใช้อยู่แล้วในฟังก์ชันเดียวกัน ซึ่งไม่มีบั๊กนี้)
- ทดสอบผ่าน local server ด้วยข้อมูลจำลอง: บ่อที่ปิดรอบแล้ว (stock=0, startDate ว่าง) ไม่ขึ้นในการ์ดความเสี่ยงอีกต่อไป, บ่อที่มี ALK=0/DO=0 ขึ้นเตือน "ALK ต่ำ (0)"/"DO เช้าต่ำ (0)" ถูกต้อง, กรณีไม่มีบ่อที่กำลังเลี้ยงเลยขึ้น empty state ไม่พังหน้า ไม่มี console error

### 57. แจ้งเตือนอากาศ/ข้างขึ้นข้างแรม/วันพระ-วันโกน + การ์ดเทียบกับรอบก่อน (mockup ยืนยันก่อนทำจริงตามที่พี่ขอ)

ต่อจากข้อ 51 (Farm Card) พี่เสนอเพิ่ม 2 เรื่อง แล้วขอให้ทำ mockup ก่อน จากนั้นเพิ่มโจทย์อีก 2 ข้อกลางทาง (วันพระ/วันโกนสำหรับลงแร่ธาตุรวม + แหล่งลูกกุ้ง/อาหารกก.แสนตัวในการ์ดเทียบรอบก่อน) ก่อนอนุมัติให้ implement:

1. **ข้างขึ้นข้างแรม + วันพระ/วันโกน ผสานเข้า Farm Card** (หน้ารายละเอียดบ่อ):
   - `moonPhase()`/`moonInfo()` คำนวณจากสูตร synodic month มาตรฐาน (ไม่ต้องเรียก API เพิ่ม) — โชว์ว่าใกล้จันทร์เต็ม/ดับไหม พร้อมคำอธิบายว่ากินอาหารลดลงช่วงนี้เป็นปกติ
   - `WANPHRA_TABLE` เก็บวันพระจริงของปี 2569 (ตรวจสอบจาก thaipbs.or.th/now/content/3498 ก่อนใช้ ไม่ได้เดาสูตรเอง) — `wanphraInfo()` คำนวณ "ก่อนวันโกน" (2 วันก่อนวันพระ) และ "วันโกน" (1 วันก่อนวันพระ) จากตารางนี้ เพราะพี่แจ้งว่าทุกก่อนวันโกน+วันโกนฟาร์มจะลงแร่ธาตุรวมในบ่อ — มีแถบเลื่อน 10 วันข้างหน้า + แบนเนอร์เตือนถ้าวันนี้/พรุ่งนี้ตรงกับวันที่ต้องลงแร่ธาตุ
   - ⚠️ **ข้อจำกัดสำคัญที่ต้องบอกพี่**: ตารางวันพระใช้ได้แค่ปี 2569 (2026) เท่านั้น เพราะปฏิทินจันทรคติไทยมีปีอธิกมาส/อธิกวารทำให้เดือนไม่สม่ำเสมอ คำนวณเองด้วยสูตรง่ายๆ จะคลาดเคลื่อนได้ — ปีต่อไปต้องมาอัปเดตตาราง `WANPHRA_TABLE` เพิ่ม (หาข้อมูลได้จาก Thai PBS/myhora.com ทุกต้นปี)
   - ผสานสัญญาณเข้ากับ Farm Card เดิม 2 จุด: `pondRiskDO()` เพิ่มเงื่อนไขเตือนถ้าพยากรณ์อากาศพรุ่งนี้ (จาก cache `_wxDaily` ที่ `fetchWx()` โหลดไว้แล้วตอนเปิดแอป ไม่ยิง fetch ซ้ำ) มีฝนหนัก+ลมกระโชก — เพิ่มเป็นเหตุผลเตือน DO ตกล่วงหน้า; `pondRiskFeed()` เติมข้อความอธิบายในเหตุผลเดิมถ้าใกล้ช่วงจันทร์เต็ม/ดับ ว่าอาหารลดอาจเป็นเรื่องปกติ ไม่ใช่สัญญาณโรคเสมอไป (ไม่เปลี่ยนระดับ badge เอง แค่เติมบริบทให้คนอ่าน)

2. **การ์ด "เทียบกับรอบก่อน"** (หน้ารายละเอียดบ่อ ใต้การ์ดเปรียบเทียบ Biomass): ตอบคำถาม "รอบนี้โตช้ากว่ารอบที่แล้วไหม" โดยเทียบที่ **DOC เดียวกัน** ไม่ใช่เทียบวันที่ปัจจุบัน — มี 2 โหมดสลับได้ (ปุ่มไหนมีข้อมูลจริงถึงจะโชว์):
   - **รอบก่อนของบ่อนี้** — ดึงจาก `p.cropHistory` รอบล่าสุดที่ปิดแล้วและมี `samples` (ไม่งั้นไม่มีจุดเทียบ)
   - **บ่อพี่น้องขนาดเดียวกัน** — หาบ่ออื่นที่กำลังเลี้ยงอยู่ (`startDate` มีค่า) ขนาด (`p.size`) เท่ากัน เลือกตัวที่อายุใกล้เคียงกับบ่อปัจจุบันที่สุด (`bmFindSibling()`)
   - เทียบ 3 KPI: ABW ที่ DOC เดียวกัน, FCR สะสม (สูตรเดียวกับ `getFCR()` แค่คำนวณ ณ อายุที่ต้องการผ่าน `bmBuildSeries().fcrAtAge()`), อัตรารอด — พร้อม badge ▲▼ บอกดีขึ้น/แย่กว่า
   - เพิ่มตามที่พี่ขอเพิ่ม: แถวเทียบ "ลูกกุ้งจากโรงเพาะ" และ "อาหาร ณ DOC เดียวกัน (กก./แสนตัว)" ใช้ `feedPer100k()` ที่มีอยู่แล้ว
   - กราฟ SVG inline (ไม่พึ่ง Chart.js เพิ่ม) พล็อต ABW ตามอายุจริงจากข้อมูลสุ่มตัวอย่างที่มี (ไม่ใช่เส้นสมูทสมมติแบบ mockup) เส้นทึบ=รอบนี้ เส้นประ=ที่เทียบ
   - ถ้าไม่มีทั้งรอบก่อนและบ่อพี่น้องให้เทียบ โชว่ข้อความแจ้งเฉยๆ ไม่พังหน้า
- Mockup ที่ใช้ยืนยันดีไซน์ทั้ง 2 รอบ (รอบแรก + หลังเพิ่มโจทย์): `Mockup และ Prototype/mockup_แจ้งเตือนอากาศ-ข้างขึ้นข้างแรม_เทียบรอบก่อน.html`
- ทดสอบผ่าน local server ด้วยข้อมูลจำลอง (บ่อ A.1 DOC 45 เทียบรอบก่อน DOC เดียวกัน + เทียบบ่อพี่น้อง A.2): ABW/FCR/อัตรารอดคำนวณตรงกับที่มือคำนวณเช็คไว้, สลับแท็บถูกต้อง, hatchery/feed100k โชว่ถูกต้อง, กราฟ SVG render ไม่พัง, วันพระ/วันโกน ตรงกับปฏิทินจริงที่ตรวจสอบแล้ว (20/7=ก่อนวันโกน, 21/7=วันโกน, 22/7=วันพระ) — ระหว่างทดสอบเจอบั๊ก timezone (`toISOString()` เทียบวันที่ผิดไป 1 วัน) แก้เป็นใช้ local date component แทน ไม่มี console error

### 56. 🔴 แก้บั๊ก: บ่อที่ปิดรอบไปแล้วยังมี feedLog/samples/labResults ค้าง (A.21/A.22/A.23) + เพิ่มปุ่มล้างข้อมูลค้าง

พี่แจ้งว่าบ่อ A.21/A.22/A.23 ในหน้า "จัดการบ่อ" ขึ้นสถานะ "ยังไม่ลงกุ้ง" (เคยเลี้ยงและปิดรอบไปแล้ว ตอนนี้ไม่มีกุ้ง) แต่คอลัมน์จำนวนรายการอาหาร/สุ่มตัวอย่าง/ผลแล็บ ยังโชว์ตัวเลขค้างอยู่ (94/9/3 รายการ) ทั้งที่ตอนปิดรอบโค้ดตั้งใจเคลียร์ทั้งคู่พร้อมกัน (`p.startDate=''` และ `p.feedLog=[]` ฯลฯ ในบล็อกเดียวกันของ `closeCropModal`)

**สาเหตุที่เป็นไปได้:** ระบบ sync แบบ union-merge กับ remote/เครื่องอื่น — ถ้า tombstone (`D.deletedIds`) ไม่ครอบคลุมรายการเก่าที่ไม่มี `id` (หรือรายการที่ sync มาจากเครื่อง/session ที่ยังไม่เห็นการปิดรอบ) ข้อมูลรอบเก่าจะถูกดันกลับเข้า array สดของรอบใหม่ได้ — มี comment เตือนเรื่องนี้ไว้ในโค้ดอยู่แล้วตรงจุดปิดรอบ แต่ไม่มีทางล้างข้อมูลที่หลุดมาแล้วโดยไม่กระทบรอบปัจจุบัน (ถ้ามี)

**แก้:**
- เพิ่มปุ่ม 🧹 ในหน้า "จัดการบ่อ" โชว์เฉพาะบ่อที่เข้าเงื่อนไข "ข้อมูลค้าง" จริงๆ (`orphaned` = `startDate` ว่าง + `stock` เป็น 0 + มี log อย่างน้อย 1 ประเภทไม่ว่าง) ป้องกันไม่ให้กดโดนบ่อที่กำลังเลี้ยงอยู่
- ฟังก์ชัน `cleanupOrphanData(id)` มี guard 2 ชั้น: (1) เช็คซ้ำว่า `startDate`/`stock` ว่างจริงก่อนล้าง ไม่งั้น alert เตือนแล้วยกเลิก (2) confirm dialog โชว์จำนวนรายการแต่ละประเภทก่อนล้างจริง — ใช้ path เดียวกับตอนปิดรอบ (tombstone ทุก `id` ที่เจอลง `D.deletedIds` ก่อนล้าง array) เพื่อกัน sync ดันกลับมาอีกรอบ
- ข้อมูลที่เคยปิดรอบสำเร็จจะยังอยู่ใน `p.cropHistory` ครบ (ปุ่มนี้ล้างแค่ log ที่ค้างใน array สดของรอบปัจจุบัน ไม่แตะ `cropHistory`)
- ทดสอบผ่าน local server ด้วยข้อมูลจำลอง (บ่อ orphan มี feedLog 94/samples 9/labResults 3 = รวม 106 รายการ): ปุ่ม 🧹 ขึ้นถูกต้อง, กดแล้วล้าง array เหลือ 0 ครบ, `D.deletedIds` เพิ่มขึ้น 106 รายการพอดี, ปุ่มหายไปหลังล้างเพราะไม่เข้าเงื่อนไข orphaned แล้ว, ทดสอบ guard กับบ่อที่มี `stock`/`startDate` จริง → ระบบปฏิเสธไม่ล้างให้ถูกต้อง ไม่มี console error

### 55. เพิ่ม "ยอดแถม" แยกจากยอดสั่งซื้อ + การ์ดเปรียบเทียบ Biomass 2 วิธี (แบบฟาร์ม vs แบบเอกสาร)

พี่แจ้งว่าตัวเลขที่ระบบใช้คำนวณอยู่คือยอดสั่งซื้อลูกกุ้งเท่านั้น ไม่รวมยอดแถมจากโรงเพาะฟัก ทำให้ยอดปล่อยจริงในระบบต่ำกว่าความจริง — เพิ่มฟีเจอร์ 2 ส่วน (ผ่านการทำ mockup ยืนยันดีไซน์ก่อนตามที่พี่ขอ):

1. **เพิ่มช่อง "ยอดแถม" แยกจาก "ยอดสั่งซื้อ"** ในฟอร์มจัดการบ่อ (`#pondModal`) — ฟิลด์ใหม่ `p.stockBonus` เก็บแยกจาก `p.stock` เดิม เพื่อความโปร่งใส ตรวจสอบย้อนหลังได้ว่าตัวไหนคือยอดสั่ง ตัวไหนคือของแถม มีช่อง "ยอดปล่อยจริง" (readonly) โชว์ผลรวมอัตโนมัติ (`syncMdlTotal()`)
2. เพิ่ม helper กลาง `pRelease(p)=(p.stock||0)+(p.stockBonus||0)` แล้วแทนที่ทุกจุดคำนวณที่เคยใช้ `p.stock` ตรงๆ ด้วยฟังก์ชันนี้ — ครอบคลุม `dens()`, `getLatestBiomass()`, `fcPeriodFCR()` (FCR ช่วง), feed per 100k ทุกจุด (การ์ดรายละเอียดบ่อ/กราฟสุ่มตัวอย่าง/กราฟเปรียบเทียบ), % กุ้งตายสะสม, เครื่องคิดเลข biomass ในหน้าสุ่มตัวอย่าง, อัตรารอดตอนปิดรอบ (`calcCropEcon`), feed per 100k ในการ์ด CEO dashboard — จุดที่ยังคงอ้างอิง `p.stock` เดิมไว้ตรงๆ คือจุดที่เป็น field กรอกจริง (ฟอร์ม/schema) ไม่ใช่จุดคำนวณ
3. ตอนปิดรอบเลี้ยง (`openCropModal`) เก็บ `stockBonus` ลง snapshot `cropHistory` ด้วย (ไม่ใช่แค่ `stock`) และล้างค่าเป็น 0 พร้อมกับ `p.stock` ตอนเริ่มรอบใหม่ — `getCloseSummary()` รวมยอดสั่ง+แถมให้อัตโนมัติก่อนส่งไปแสดงในรายงาน PDF/preview ปิดบ่อ
4. **การ์ดใหม่ "เปรียบเทียบ Biomass 2 วิธี"** ในหน้ารายละเอียดบ่อ (ใต้การ์ดไซส์ล่าสุด) — เทียบ **แบบฟาร์ม** (ABW × ยอดปล่อยจริง × อัตรารอดที่กรอกเอง ÷ 1000) กับ **แบบเอกสาร** (ไขว้ตรวจจากอาหารที่ให้จริงวันนี้ ÷ Feeding Rate% ที่ควรเป็นตามไซส์กุ้ง) — Feeding Rate auto-fill เป็นค่ากลางของช่วงอ้างอิง (เช่น ABW 15g → auto 3.5%) แต่แก้เองได้ พร้อมปุ่ม "↺ ค่าแนะนำ" กลับไปใช้ auto — ถ้าตัวเลข 2 แบบต่างกัน ≥25% ขึ้นแจ้งเตือนแดงให้ตรวจสอบด่วน, ≥10% เตือนเหลือง, ต่ำกว่านั้นแจ้งเขียวว่าตัวเลขน่าเชื่อถือ
   - ช่วงอ้างอิง Feeding Rate% เป็นค่าประมาณทั่วไป **ไม่ใช่ตารางฉบับเต็มจากเอกสาร ดร.พุทธ** (สไลด์ต้นฉบับเป็นภาพ อ่านตารางละเอียดไม่ครบ) — พี่แจ้งว่าจะส่งเอกสารตารางจริงมาเพิ่มภายหลัง ตอนนี้ใช้ค่าประมาณคร่าวๆ ไปพลางก่อน (ระบุไว้ในข้อความเตือนใต้ช่องกรอกด้วย)
   - ค่าที่แก้ไข Feeding Rate เป็น state ชั่วคราวในหน้า (`_bcFeedRateOverride`) ไม่บันทึกลง Firebase — เป็นเครื่องมือช่วยตัดสินใจ ไม่ใช่ข้อมูลถาวร
- Mockup ที่ใช้ยืนยันดีไซน์ก่อนทำจริง: `Mockup และ Prototype/mockup_เปรียบเทียบ_biomass_2วิธี.html`
- ทดสอบผ่าน local server ด้วยข้อมูลจำลอง (บ่อทดสอบ 500,000+25,000 ตัว, ABW 15.4g, อาหาร 36 กก./วัน, อัตรารอด 80%): `pRelease()` คำนวณถูกต้อง (525,000), หัวการ์ดบ่อโชว์ "525,000 ตัว (สั่ง 500,000+แถม 25,000)", การ์ดเปรียบเทียบ Biomass render ตรงกับผลจาก mockup เป๊ะ (แบบฟาร์ม 6,468 กก. vs แบบเอกสาร 1,286 กก. → เตือนแดง 80.1%), ฟอร์มแก้ไขบ่อ prefill/บันทึก `stockBonus` ถูกต้อง, ปุ่มแก้ไข/รีเซ็ต Feeding Rate ทำงานถูกต้องผ่าน DOM event จริง ไม่มี console error — ยังไม่ได้กรอกยอดแถมจริงของ 4 บ่อที่ใช้งานอยู่ (ไม่มีบัญชี login เข้าแอปจริง พี่ต้องกรอกเองผ่านหน้า "จัดการบ่อ")

### 54. 🔴 แก้บั๊ก: เมนู "รายวัน" ในไซด์บาร์ไม่ขยายให้เห็นเมนูย่อย (การ์ด "กุ้งตาย" ไม่ขึ้นโชว์)
พี่แจ้งว่า card กุ้งตายไม่มีขึ้นโชว์หลังจากทำข้อ 53 เสร็จ — ตรวจสอบพบ 2 ปัญหาซ้อนกันในเมนูไซด์บาร์ (`.sn-sub`):
- **ปัญหาที่ 1 (ชัดเจน):** `.sn-sub.open{max-height:320px}` เป็นค่าคงที่ที่ตั้งไว้ตอนกลุ่ม "รายวัน" มี 7 เมนูย่อย พอเพิ่ม "กุ้งตาย" เป็นอันที่ 8 เนื้อหาจริงสูง 352px เกิน 320px ทำให้เมนูย่อยตัวสุดท้าย (ที่เพิ่งเพิ่มใหม่) ถูกครอบด้วย `overflow:hidden` มองไม่เห็น — แก้โดยเพิ่มเป็น `max-height:600px` ให้มีที่ว่างเผื่ออนาคต
- **ปัญหาที่ 2 (ซ่อนอยู่ ตรวจเจอระหว่างทดสอบ):** แม้จะแก้ max-height แล้ว เมนูทั้งกลุ่มก็ยังไม่ขยายเลย (`clientHeight` วัดได้ 0px ทั้งที่ class `.open` ถูกเพิ่มถูกต้องและ CSS rule ก็ match) — เป็นบั๊กเรนเดอร์ที่การ toggle class หลายตัวใน `snToggleGrp()` (remove ของเก่าทั้งหมดแล้ว add ของใหม่ในจังหวะเดียวกันแบบ synchronous) ทำให้ browser ไม่ยอม commit การเปลี่ยนแปลง `max-height` แบบมี transition ให้ถูกต้อง ค้างอยู่ที่ 0 ทั้งตอนเปิดและตอนปิด — วิธียืนยันบั๊ก: สลับ `display:none`→`''` แล้ว `void el.offsetHeight` บังคับให้ browser คำนวณ style ใหม่ทั้งหมด แก้ปัญหาได้ทันที
- แก้โดยเพิ่มฟังก์ชันช่วย `snForceReflow(el)` (สลับ display บังคับ style recalc) เรียกใช้ทั้งใน `snToggleGrp()` (ตอนเปิด/ปิดกลุ่มเมนู) และใน `goTab()` (ตอนคลิกลิงก์เมนูย่อยแล้ว sync เมนูซ้ายให้เปิดกลุ่มที่ active ค้างไว้)
- ทดสอบผ่าน local server แล้ว: เปิด/ปิด/เปิดซ้ำเมนู "รายวัน" ได้ผลลัพธ์ถูกต้องทุกครั้ง (`clientHeight` สลับ 0↔352 ตรงกับ state), คลิกเมนู "กุ้งตาย" นำไปหน้าที่ถูกต้องและเมนูซ้ายซิงค์สถานะเปิด/active ถูกต้อง ไม่มี console error — **หมายเหตุ:** บั๊กนี้อาจเป็นปัญหาเดิมที่แฝงอยู่ในโค้ดมาก่อนหน้านี้แล้ว (ไม่ได้เกิดจากการเพิ่มเมนูใหม่โดยตรง) เพิ่งมาเจอเพราะพี่ทดสอบเมนูที่เพิ่งเพิ่มใหม่พอดี

### 53. หน้าใหม่ "กุ้งตายรายวัน" + เชื่อมเข้า Farm Card Disease Risk (implement จาก mockup ที่พี่คอนเฟิม)
ต่อจากข้อ 51 — พี่ยืนยันให้ทำหน้าบันทึกจำนวนกุ้งตายรายวัน (จากเอกสารวิชาการ ดร.พุทธ: 1 ใน 5 ข้อมูลหลักที่ควรบันทึกทุกวัน) เป็นหน้าใหม่แยกต่างหากในกลุ่มเมนู "รายวัน" ทำม็อกอัพให้ดูก่อน ([mockup_บันทึกกุ้งตายรายวัน.html](../Mockup และ Prototype/mockup_บันทึกกุ้งตายรายวัน.html)) พี่คอนเฟิมแล้ว implement เข้าแอปจริง:
- หน้าใหม่ `sec-mortality` (🦐 กุ้งตาย) ต่อท้ายกลุ่ม "รายวัน" — ลงทะเบียนครบ 3 จุดตามรูปแบบเดิมของแอป (sidebar `sn-link`, dropdown บนเดสก์ท็อป `tab-item`, มือถือ `mob-sheet-item`)
- ข้อมูลใหม่ `p.mortalityLog[]` — {id, date, age, count, cause, notes, by} หนึ่งรายการต่อบ่อต่อวัน (update-in-place ด้วย id/date match แบบเดียวกับ `saveLab`/`saveSample`) — สาเหตุให้เลือก: ไม่ทราบ/DO ต่ำ/โรค-เชื้อ/สภาพอากาศ/ศัตรูธรรมชาติ/อื่นๆ
- **เตือนอัตโนมัติเมื่อตายผิดปกติ** — ถ้าจำนวนตายวันนี้ ≥ ค่าเฉลี่ย 7 วันย้อนหลัง × 2 (และ ≥3 ตัว) ขึ้นกล่องเตือนสีแดงทันทีตอนพิมพ์ (`onMtCountInput()`)
- การ์ดสรุป: ตายวันนี้ / สะสมรอบนี้ / % จากจำนวนปล่อย (`p.stock`) ใช้ `.ovd-sum-chip` เดิมที่มีอยู่แล้ว (ไม่สร้างคลาสใหม่ซ้ำ) + กราฟแท่งเล็ก 7 วันล่าสุด (คลาสใหม่ `.mt-bars` เพราะยังไม่มีของเดิมให้ใช้ซ้ำ)
- **เชื่อมเข้า Farm Card** — เพิ่มสัญญาณใน `pondRiskDisease()`: ถ้าตายวันนี้ ≥ ค่าเฉลี่ย 7 วัน×2 (เกณฑ์เดียวกับหน้ากรอก) ยกเป็น `danger` ทันที พร้อมเพิ่ม action แนะนำ "ตรวจสาเหตุกุ้งตายผิดปกติทันที" ในหมวด Disease Risk
- ทดสอบผ่าน local server แล้ว: ลงทะเบียนเมนูครบ 3 จุด, บันทึก/แก้ไข/ลบทำงานถูกต้อง (update-in-place ตามวันที่), เตือนผิดปกติขึ้นถูกต้องเมื่อตายเกิน 2 เท่าของค่าเฉลี่ย, การ์ดสรุป/กราฟ/ตารางอัปเดตถูกต้องหลังบันทึก, Farm Card หน้ารายละเอียดบ่อขึ้น Disease Risk เป็น "ต้องดู!" ถูกต้องเมื่อมีข้อมูลกุ้งตายผิดปกติของวันนั้น ไม่มี console error

### 52. รวม CSS badge สถานะ + เชื่อมเกณฑ์หน้าผลแล็บเข้ากับ classifyOv() (2 ข้อที่เลื่อนไว้จากข้อ 50 — แก้แล้ว)
พี่ให้ทำ 2 ข้อที่เลื่อนไว้ต่อได้เลย (ไม่ต้องรอ mockup เพราะเป็นงาน refactor เบื้องหลัง ไม่กระทบหน้าตา):
- **[แก้แล้ว] รวม CSS badge สถานะ 5 ชุดที่เขียนสีซ้ำกัน** (`.b-*`, `.pcb-*`, `.ovd-st-badge.*`, `.ai.*`, `.fc-act-src.*`) — เดิมแต่ละชุดเขียน `background:var(--ob);color:var(--ok)` (และคู่ warn/danger) แยกกันเอง 5 จุด เสี่ยงแก้ที่เดียวไม่ครบ รวมเป็น selector เดียวกัน (เช่น `.b-ok,.pcb-ok,.ovd-st-badge.ok,.ai.ok{...}`) และใช้ `border-left:3px solid currentColor` แทนการเขียนตัวแปรสีซ้ำรอบสามใน `.ai.*` — พบว่า `.b-warn` เดิมใช้ `--w-tx` ส่วนอีก 4 ชุดใช้ `--w` ตรงๆ (ค่าเท่ากันพอดี #B45309 ทั้งคู่ แค่คนละชื่อตัวแปร) รวมเป็น `var(--w)` เดียว ไม่กระทบสี — **ไม่แตะ** `.fc-item-badge.*` เพราะตั้งใจให้เป็นพื้นทึบ+ตัวอักษรขาว (ดีไซน์ต่างจากกลุ่มอื่นโดยตั้งใจ ตามที่ยืนยันไว้ตอนทำ Farm Card)
- **[แก้แล้ว] หน้าผลแล็บ (`updateLabStatus()`) เดิมคำนวณเกณฑ์ warn/danger แยกเองในฟังก์ชัน ไม่ผ่าน `classifyOv()`** — เพิ่ม case ที่ขาดใน `classifyOv()`: `hepatoGreen`/`hepatoYellow` (ใช้เกณฑ์เดียวกับ `tcbsGreen`/`tcbsYellow`), `tvcHepato` (เกณฑ์เดียวกับ `tvc`), `gutInflam`, `lipidDroplet` แล้วแก้ `updateLabStatus()` ให้เรียก `classifyOv()` แทนคำนวณเอง — ระหว่างแก้พบจุดที่เกณฑ์เดิมเพี้ยนกันเล็กน้อย: TVC เดิมในหน้าผลแล็บใช้ `tvc>1e5` (มากกว่าอย่างเดียว) แต่ `classifyOv` ใช้ `tvc>=1e5` (มากกว่าหรือเท่ากับ) — ตอนนี้ตรงกันหมดแล้วผ่าน `classifyOv` เดียว (ผลกระทบจริงมีแค่ค่า TVC ที่เท่ากับ 100,000 พอดีเป๊ะ ซึ่งแทบไม่เกิดในทางปฏิบัติ)
- ทดสอบผ่าน local server แล้ว: badge ทุกกลุ่ม (`.pcb-*` ในการ์ดบ่อ, `.ovd-st-badge` ในตารางสถานะบ่อทั้งหมด, `.fc-item-badge` ใน Farm Card) แสดงสีถูกต้องตรงกับก่อนแก้ 100%, หน้าผลแล็บกรอกค่าทดสอบ (TCBS/ตับ/TVC ตับ/ไขมัน) ได้ผลลัพธ์ตรงตามเกณฑ์เดิมทุกจุด ไม่มี console error

### 51. Farm Card ในหน้ารายละเอียดบ่อ — ความเสี่ยง 5 หมวด + สิ่งที่ต้องทำวันนี้ (สูตรจากเอกสารวิชาการ ดร.พุทธ)
พี่ให้เอกสาร PDF 5 ไฟล์ของ ดร.พุทธ ส่องแสงจินดา (ไนไตรท์สะสม, Pond dynamics, Precision feeding, Shrimp biomass estimation, Shrimp health management — รวม 167 หน้า) ให้อ่านทำความเข้าใจแล้วนำมาปรับใช้ — อ่านครบทุกหน้าด้วยการ render PDF เป็นภาพ (poppler ไม่มีในเครื่อง เลยติดตั้ง pymupdf/Pillow แทน ประกอบภาพเป็น grid 6 หน้า/ภาพเพื่อลดจำนวนรอบอ่าน) สรุปแล้วพี่ให้กลับไปทำต่องานที่ค้างไว้ (Farm Card mockup ข้อ 1 Today's Action + ข้อ 3 ความเสี่ยง 5 หมวด) โดยเอาสูตร/เกณฑ์จากเอกสารมาแทน placeholder logic เดิม:
- **หลักการสำคัญที่สุดจากเอกสาร (ย้ำซ้ำในทุกไฟล์): "แนวโน้มสำคัญกว่าค่าที่วัดครั้งเดียว"** — เพิ่มฟังก์ชัน trend ใหม่ที่ใช้ประวัติที่มีอยู่แล้วในแอป ไม่ต้องมีเซนเซอร์เพิ่ม: `fcDoTrend()` (DO ลดต่อเนื่อง), `fcFeedTrend()` (อาหารกินลดต่อเนื่อง — เอกสารระบุว่านี่คือ "สัญญาณเตือนโรคที่ดีที่สุดอย่างหนึ่ง"), `fcRisingTrend()` (NH3/NO2 มีแนวโน้มขึ้น), `fcPeriodFCR()` (FCR เฉพาะช่วงล่าสุด เทียบกับ FCR สะสม เผยประสิทธิภาพอาหารที่ค่าสะสมอาจกลบไว้)
- **DO Risk**: ใช้ `classifyOv('doM'/'doA')` เดิม + เพิ่มสัญญาณ trend ลดต่อเนื่องเป็น warn แม้ค่าล่าสุดยังไม่ผิดเกณฑ์
- **Pond Stability**: `phSwing` เดิม + เพิ่ม **DO swing** ใหม่ (|doM-doA|, เลียนแบบ phSwing — เกณฑ์ประมาณจากเอกสารที่ระบุว่า pH เสถียร (สวิง≤0.4) สำคัญกว่า pH สูง) + alkalinity/salinity
- **Disease Risk** (จุดใหม่ที่สำคัญสุด): รวมผลแล็บเดิม (TCBS/TVC/EHP/ตับอ่อน) + **สัญญาณเตือนล่วงหน้าจากเอกสาร** — ถ้าอาหารกินลดลง+DO ลดลงพร้อมกัน = danger ทันที (สูตรนี้ย้ำซ้ำ 3 รอบในเอกสารต่างไฟล์กันว่าเป็นชุดสัญญาณเตือนโรคที่แม่นที่สุด) หรือ NH3/NO2 มีแนวโน้มขึ้น = warn
- **Feed Risk**: อาหารกินลดลงต่อเนื่อง + FCR ช่วงล่าสุดสูงกว่า FCR สะสมชัดเจน (>30%)
- **Economic Risk**: FCR สะสม >1.6 warn / >2.0 danger (อิงตัวอย่างตัวเลขจริงจากเอกสาร Precision feeding ที่เทียบ FCR 1.6 vs 1.2)
- แต่ละหมวดคลิกกรองรายการ "สิ่งที่ต้องทำวันนี้" ได้ (เหมือน mockup) ข้อความคำแนะนำอ้างอิงจากเอกสารโดยตรง (เช่น "เปิดเครื่องตีน้ำเพิ่มทันที + ลดอาหาร 20–50%" สำหรับ DO วิกฤต, "ตรวจยอกินอาหารและของเสียในยอ" สำหรับ Feed Risk)
- ติ๊กเสร็จได้ (เก็บสถานะในหน่วยความจำระหว่าง session ไม่บันทึกลง Firebase — เป็นรายการแนะนำรายวัน ไม่ใช่ประวัติถาวร)
- **ตั้งใจไม่แตะ**: เกณฑ์ตี 3–5 (DO ต่ำสุดตามธรรมชาติ) และ Bottom DO (พื้นบ่อต่ำกว่าผิวน้ำมาก) — ทำในซอฟต์แวร์ไม่ได้เพราะไม่มีเซนเซอร์ real-time ตามที่คุยกันไว้ก่อนหน้า, ไม่แก้ threshold เดิมใน `classifyOv()`/`pondStatusOv()` (คงไว้ตามเดิมกันกระทบวงกว้าง เกณฑ์ DO เดิม <3=danger/<4=warn ตรงกับเอกสารอยู่แล้ว), ไม่ทำ "จำนวนกุ้งตายรายวัน" ที่เอกสารแนะนำ (เป็น field ข้อมูลใหม่ กระทบ schema กว้างกว่า รอพี่ยืนยันแยกอีกที)
- อยู่ในหน้ารายละเอียดบ่อ (`sec-ponddetail`) ใต้กล่องแจ้งเตือนเดิม แสดงเฉพาะบ่อที่กำลังเลี้ยงอยู่ (ไม่โชว์ตอนเตรียมบ่อ)
- ทดสอบผ่าน local server ด้วยข้อมูลจำลอง: จำลอง DO+อาหารลดลงต่อเนื่อง 4 วันพร้อมกัน → Disease Risk ขึ้น danger ด้วยข้อความที่ถูกต้อง, จำลอง NO2 ขึ้นอย่างเดียว → Disease Risk ขึ้น warn ถูกหมวด, กดกรองตามหมวด/ติ๊กเช็คลิสต์/แถบความคืบหน้าทำงานถูกต้อง, จอมือถือแสดงผล 2 คอลัมน์อ่านง่าย ไม่มี console error

### 50. รวมสี/ไอคอนสถานะ ok/warn/danger ให้สม่ำเสมอทั้งแอป (audit + แก้แล้ว)
ต่อจากข้อ 49 — พี่ขอทำอีกข้อที่เคยเสนอไว้: สีสถานะให้สม่ำเสมอทั้งแอป ตรวจสอบก่อนพบว่าแอปมีระบบ token สี (`--ok`/`--w`/`--d` + `--ob`/`--wb`/`--db`) อยู่แล้ว แต่ใช้ไม่สม่ำเสมอ 2 แบบ พี่เลือกให้แก้เฉพาะ 2 ข้อนี้ก่อน (เลื่อนข้อ CSS-badge-ซ้ำซ้อน 4 ชุด กับข้อ lab-page คำนวณเกณฑ์แยกจาก `classifyOv()` ไปทำทีหลัง เพราะเสี่ยงกระทบกว้างกว่า):
- **[แก้แล้ว] อีโมจิสถานะไม่ตรงกันข้ามหน้า** — รวมเป็นชุดเดียวทั้งแอป: ✅ (ปกติ) / ⚠️ (เฝ้าระวัง) / 🔴 (อันตราย) แก้ที่: หัวข้อแจ้งเตือนบนสุด (`hdr-alert`), การ์ดรายละเอียดบ่อ, รายการแจ้งเตือนหน้าภาพรวม, การ์ด "สถานะบ่อทั้งหมด" ที่เพิ่งทำในข้อ 49, แบนเนอร์ความเสี่ยงระดับฟาร์ม + สรุปรวมฟาร์ม + การ์ดรายบ่อในหน้าความเสี่ยง (เดิมใช้ 🔴/🟡/⚪ ปนกับ 🔴/⚠️ คนละจุด), สรุปสถานะรวมในหน้าผลแล็บ — **ไม่แตะ** 🟢/🟡 ที่ใช้เป็น "ชื่อสีเชื้อ TCBS" (เช่น "🟢 เชื้อเขียว", "🟡 เชื้อเหลือง") เพราะเป็นศัพท์ทางแล็บจริง (สีโคโลนีบนอาหารเลี้ยงเชื้อ) ไม่ใช่ไอคอนความรุนแรง คนละความหมายกับ ok/warn/danger แม้จะหน้าตาคล้ายกัน — ตรวจแยกทุกจุดก่อนแก้เพื่อไม่ให้ปนกัน
- **[แก้แล้ว] สีเขียนตรงๆ (`#DC2626` ฯลฯ) แทนที่จะใช้ `var(--d)`** — แก้ประมาณ 15 จุด: `.stock-warn`/`.stock-qty-low`/`.wq-danger`/`.wq-arrow-*-ok/-bad` (สต็อก/คุณภาพน้ำ), badge "ใกล้หมด" ในหน้าสต็อก, สี P&L กำไร/ขาดทุน 2 จุด, แถบเตือนบ่อยังไม่กรอกอาหารในหน้าอาหาร (border เดิม `#f0d9a8` เปลี่ยนเป็น `var(--w)` ให้ตรงกับรูปแบบกล่องเตือนที่อื่นในแอป), แถบสีคะแนนความเสี่ยงรายวันในหน้าความเสี่ยง (`#e53e3e`/`#dd6b20`/`#2f855a` → `var(--d)`/`var(--w)`/`var(--ok)`) — ทุกจุดเป็นสีเดียวกันเป๊ะ (hex ตรงกับตัวแปรอยู่แล้ว) ยกเว้นจุดเดียวที่เปลี่ยนโทนจริง (แถบเตือนอาหาร + แถบคะแนนความเสี่ยง) ซึ่งเปลี่ยนให้ตรงกับ pattern ที่ใช้ทั่วแอปอยู่แล้ว ไม่ใช่คิดสีใหม่
- **[ตรวจแล้ว ไม่แก้ — มีเหตุผล]** สี "ต้องดู" บนการ์ด KPI ภาพรวม (`#ff8a70`) จงใจใช้สีส้มอ่อนแทน `var(--d)` เพราะการ์ดนั้นพื้นหลังเข้ม (`--sn-bg2` สีเขียวเข้มธีมไซด์บาร์) `var(--d)` ปกติอาจ contrast ไม่พอบนพื้นเข้ม — เป็นการปรับสีเพื่อ contrast ที่ตั้งใจ ไม่ใช่บัค ไม่แตะ
- **[ตรวจแล้ว ไม่แก้]** สี PARAMS ในหน้าคุณภาพน้ำ (`color:'#e53e3e'` ของ Temp เช้า ฯลฯ) เป็นสีพารามิเตอร์แต่ละตัวสำหรับกราฟ (pH=น้ำเงิน, Temp=แดง, DO=ฟ้า ฯลฯ) คนละแนวคิดกับสีสถานะ ok/warn/danger ไม่ใช่จุดที่ต้องรวม
- ทดสอบผ่าน local server แล้ว: หน้าภาพรวม/ความเสี่ยง/ผลแล็บ แสดงไอคอน/สีใหม่ถูกต้อง, ข้อความ "🟢 เชื้อเขียว" ในผลแล็บยังอยู่ครบไม่ได้ถูกแก้ผิด ไม่มี console error

### 49. หน้าภาพรวม — เพิ่มการ์ด "สถานะบ่อทั้งหมด" รวมสถานะ/เช็คลิสต์/ค่าผิดปกติในตารางเดียว (implement จาก mockup ที่พี่คอนเฟิม)
ต่อจากข้อ 48 — พี่ถามต่อว่ามีอะไรแนะนำอีกให้แอปน่าใช้ขึ้น เสนอ 2 ทาง (สีสถานะสม่ำเสมอทั้งแอป / แดชบอร์ดสรุปภาพรวมสำหรับ admin-CEO) พี่เลือกทำแดชบอร์ดก่อน ทำม็อกอัพให้ดู ([mockup_dashboard_สถานะบ่อทั้งหมด.html](../Mockup และ Prototype/mockup_dashboard_สถานะบ่อทั้งหมด.html)) พี่คอนเฟิมให้เพิ่มเป็นการ์ดใหม่ในหน้าภาพรวม (ไม่ตัดของเดิมออก):
- การ์ดใหม่ "📊 สถานะบ่อทั้งหมด" อยู่ใต้แถบ KPI บนสุดของหน้าภาพรวม — **รวม**ข้อมูลที่ก่อนหน้านี้กระจายอยู่หลายจุด (การ์ดบ่อสีสถานะในกริด, เช็คลิสต์ ✓/– รายวัน, รายการแจ้งเตือนด้านล่าง, บ่อเสี่ยงสูงสุดข้างๆ) มาไว้ในตารางเดียว เรียงบ่อที่ต้องดูก่อน (`danger`→`warn`→`ok`) ไว้บนสุดอัตโนมัติ — ใช้ `pondStatusOv()`/`reasonOv()` เดิมที่มีอยู่แล้ว ไม่สร้าง logic คำนวณสถานะซ้ำ
- แถบสรุป 4 ตัวเลขบนสุด (ต้องดูด่วน/เฝ้าระวัง/ยังไม่กรอกวันนี้/ปกติครบถ้วน) + ชิปกรองด่วน (ทั้งหมด/ต้องดูก่อน/ยังไม่กรอกวันนี้) กดกรองเหลือเฉพาะบ่อที่มีปัญหาได้
- แต่ละแถวโชว์ ✓/– ว่ากรอกน้ำ/อาหาร/สต็อกของวันนี้แล้วหรือยัง (logic เดียวกับ `renderChecklist()`) + เหตุผล/ค่าผิดปกติแบบสั้นในคอลัมน์เดียว คลิกแถวไปหน้ารายละเอียดบ่อได้เลย (`openPondDetail()`)
- Responsive: จอกว้างเป็นตาราง (`.ovd-table`), มือถือ (`max-width:680px`) เปลี่ยนเป็นการ์ดไล่ลง (`.ovd-cards`)
- ฟังก์ชันใหม่: `renderOvDashboard(active)` (เรียกจาก `renderOv()` ท้ายสุด ใช้ `active` ที่คำนวณไว้แล้วไม่คำนวณซ้ำ), `_ovdRows`, `_ovdFiltered`, `_ovdSort`, `setOvdFilter`, `_ovdChk`
- 🔴 เจอบัคระหว่างทดสอบ: ตอนแรกตารางไม่ซ่อนบนมือถือทั้งที่มี media query `.ovd-table{display:none}` แล้ว — สาเหตุคือ selector ของแอปเดิม `.card table{display:block}` (class+element = specificity สูงกว่า `.ovd-table` เฉยๆ) ทับอยู่ แก้โดยเพิ่ม specificity เป็น `.card .ovd-table{display:none}` ในมือถือแทน
- ทดสอบผ่าน local server ด้วยข้อมูลจำลอง 8 บ่อ: เรียงลำดับตามสถานะถูกต้อง, ชิปกรองทำงานถูกต้อง (นับจำนวนตรง), คลิกแถวไปหน้ารายละเอียดบ่อสำเร็จ, จอกว้างแสดงตาราง/มือถือแสดงการ์ดถูกต้องหลังแก้บัค specificity ไม่มี console error

### 48. Micro-feedback ตอนบันทึกสำเร็จ — เพิ่มความน่าดึงดูดในการใช้งาน (implement จาก mockup ที่พี่คอนเฟิม)
พี่ถามว่าทำยังไงให้แอปน่าดึงดูดขึ้นสำหรับ staff/admin — เสนอว่าฟีดแบ็กเชิงฟังก์ชันตอนบันทึกสำเร็จสำคัญกว่าลูกเล่นภาพหวือหวา ทำม็อกอัพ 4 แบบให้ดูก่อน ([mockup_micro_feedback_บันทึกสำเร็จ.html](../Mockup และ Prototype/mockup_micro_feedback_บันทึกสำเร็จ.html)) พี่คอนเฟิมแล้วค่อย implement เข้าแอปจริงทั้ง 4 แบบ:
- **ปุ่ม → เครื่องหมายถูก** (`flashSaveBtn()`) — ปุ่มเปลี่ยนเป็นสีเขียว + ✓ "บันทึกแล้ว" ชั่วครู่แล้วกลับปกติ ใช้กับฟอร์มบันทึกเดี่ยวๆ: ผลแล็บ (`saveLab`), สุ่มตัวอย่าง (`saveSample`), ค่าแรง (`saveLaborCost`), ค่าไฟฟ้า (`saveElecBill`)
- **กล่องข้อความ — เลื่อนเข้า + ไอคอนเด้ง + จางหายนุ่มนวล** — อัปเกรด `showMsg()`, `showElecMsg()`, `showStockMsg()` ทั้ง 3 ฟังก์ชันให้มีทรานซิชันแทนโผล่/หายแบบตัดวูบ (คลาส `.msg-box`/`.msg` ร่วมกัน) มีผลอัตโนมัติกับทุกหน้าที่ใช้ 3 ฟังก์ชันนี้อยู่แล้ว (ผลแล็บ, สุ่มตัวอย่าง, ค่าแรง, ค่าไฟ, สต็อก, คุณภาพน้ำ, จัดการบ่อ, ฯลฯ) — ระหว่างทางพบว่า `.msg` (ใช้โดยค่าไฟ/สต็อก) ไม่เคยมี CSS จริงมาก่อน (กล่องไม่มีสี ไม่มีกรอบ) เลยเพิ่มสไตล์ให้ครบไปด้วยในคราวเดียว
- **Toast มุมจอ** (`showToast()`) — เหมาะกับหน้าที่กรอกต่อเนื่องเร็วๆ ไม่บังฟอร์มเดิม ใช้กับบันทึกคุณภาพน้ำ (`saveWQ`) และสต็อก (`saveStockTxBatch`) — บนมือถือ (`max-width:600px`) เปลี่ยนเป็นแถบเต็มความกว้างเลื่อนขึ้นจากขอบล่างแทนมุมขวา เผื่อ safe-area
- **แถวตารางกะพริบเขียว** (`flashRow()`) — ใช้กับหน้าอาหาร (`saveFeedGridAll`) ทุกแถว `<tr data-pond>` ที่เพิ่งบันทึกจะไฮไลต์เขียวเรียงลำดับทีละแถว (stagger 80ms)
- ทดสอบผ่าน local server ด้วยข้อมูลจำลองทุกฟังก์ชันที่แก้ (saveLab/saveSample/saveLaborCost/saveElecBill/saveWQ/saveStockTxBatch/saveFeedGridAll) ยืนยันว่าปุ่ม/กล่องข้อความ/toast/แถวทำงานถูกต้องครบ ไม่มี console error

### 47. หน้าผลแล็บ — รีดีไซน์ใหม่เป็น 2 แท็บ น้ำ/ตับ-ตัวกุ้ง (implement จาก mockup ที่พี่คอนเฟิม)
ต่อจากข้อ 45 ที่ตรวจเจอว่าการ์ด "บันทึกผลแล็บ" มีช่องกรอกอัดรวมกันเยอะขึ้นเรื่อยๆ (~11 ช่อง) จนเริ่มแน่น — พี่ขอให้ออกแบบหน้าใหม่ให้ทำงานง่ายขึ้นแต่ข้อมูลยังครบทั้งน้ำและตับ ทำม็อกอัพให้ดูก่อน ([mockup_ผลแล็บ_ใหม่_น้ำ-ตับ.html](../Mockup และ Prototype/mockup_ผลแล็บ_ใหม่_น้ำ-ตับ.html)) พี่คอนเฟิมแล้วค่อย implement:
- แบ่งฟอร์มเป็น 2 แท็บสลับได้ (🌊 ตรวจจากน้ำ / 🦐 ตรวจจากตับ-ตัวกุ้ง) แต่ละแท็บมีป้ายนับจำนวนช่องที่กรอกแล้ว (เช่น "2/3") อัปเดตสดตามที่พิมพ์ — สลับแท็บไม่ล้างค่าที่กรอกไว้ในแท็บอื่น กด "บันทึกทั้งหมด" ครั้งเดียวบันทึกข้อมูลทั้ง 2 แท็บพร้อมกันไม่ว่าจะอยู่แท็บไหนตอนกด
- ช่อง "ลักษณะตับอ่อน" (`lb-hepato`) เปลี่ยนดีฟอลต์จาก `normal` (สมมติว่าปกติถ้าไม่ได้กรอก) เป็นว่างเปล่า/`null` (ไม่ฟันธงว่าปกติถ้ายังไม่มีใครตรวจด้วยสายตาจริง) — ตารางประวัติจึงต้องรองรับกรณีนี้ด้วย เพิ่ม badge สีกลาง (`.b-na`) แสดง "-" แทนที่จะขึ้นเป็นแดง/อันตรายทั้งที่ยังไม่มีข้อมูล
- ฟังก์ชันใหม่: `setLabSrc(src,btn)` สลับแท็บ, `updateLabSrcCount(src)` นับช่องที่กรอกแล้วในแท็บนั้น, `onLabInput(src)` เรียกทั้งสถานะรวมเดิม + นับช่องใหม่ทุกครั้งที่พิมพ์ — แก้ `clearLab/initLab/editLab/saveLab/renderLabTable` ให้รองรับดีไซน์ใหม่และดีฟอลต์ `hepato` แบบใหม่
- ทดสอบผ่าน local server ด้วยข้อมูลจำลอง: สลับแท็บค่าที่กรอกไว้ยังอยู่ครบ, ป้ายนับอัปเดตถูกต้อง, บันทึกได้ทั้ง 2 แท็บพร้อมกันไม่ว่าอยู่แท็บไหน, แก้ไขรายการเก่า (`editLab`) prefill ทั้ง 2 แท็บและป้ายนับถูกต้อง, ล้างฟอร์ม (`clearLab`) รีเซ็ตครบรวมถึงป้ายนับกลับเป็น 0, ตารางประวัติแสดง badge "-" สีกลางถูกต้องเมื่อยังไม่ได้ตรวจตับด้วยสายตา และ badge สีตามจริงเมื่อมีการกรอก ไม่มี console error

### 46. สต็อก — เพิ่ม autocomplete ชื่อลูกค้าภายนอก กันพิมพ์ชื่อเดียวกันไม่ตรงกัน (แก้แล้ว)
ต่อจากข้อ 45 ที่ตรวจเจอว่าช่องชื่อลูกค้า/ร้าน (`sk-tx-cust`) เป็นช่องพิมพ์อิสระล้วนๆ เสี่ยงพิมพ์ชื่อเดียวกันไม่ตรงกัน (เช่น "ร้านฟาร์มสุข" vs "ฟาร์มสุข") ทำให้รายงานแยกลูกค้าผิดกลุ่ม — พี่เห็นด้วยกับข้อเสนอให้ทำ autocomplete แทนการเปิดหน้า "จัดการลูกค้า" แยกใหม่ (ไม่อยากเพิ่มตารางข้อมูล/หน้าจัดการเพิ่มโดยไม่จำเป็น):
- ใช้ `<datalist>` ของ HTML ผูกกับช่อง `sk-tx-cust` (attribute `list="sk-cust-list"` + `autocomplete="off"` กันเบราว์เซอร์เอา autofill ของตัวเองมาแทรก) — พิมพ์ชื่อใหม่ที่ไม่เคยมีในลิสต์ก็ยังทำได้ปกติ ไม่ใช่ dropdown บังคับเลือก
- ฟังก์ชันใหม่ `populateCustList()` ดึงชื่อลูกค้าที่ไม่ซ้ำจาก `D.inventoryLog` (เฉพาะ `type==='sale'`) มาเรียงตามตัวอักษรไทยใส่ใน datalist — เรียกตอนเข้าโหมด "ขายลูกค้าภายนอก" (`setStockMode('sale')`) และเรียกซ้ำทันทีหลังบันทึกขายสำเร็จ เพื่อให้ชื่อที่เพิ่งพิมพ์ใหม่ใช้ autocomplete ได้ในรายการถัดไปเลยโดยไม่ต้องรีเฟรชหน้า
- ไม่เพิ่ม schema/ตารางใหม่ใดๆ ใช้ข้อมูลที่มีอยู่แล้วล้วนๆ
- ทดสอบผ่าน local server แล้ว: เข้าโหมดขายลูกค้าภายนอกด้วยข้อมูลจำลอง 2 รายการ ลิสต์ขึ้นชื่อครบ 2 ชื่อเรียงตามตัวอักษรถูกต้อง ไม่มี console error

### 45. UX audit รอบ 3 — ข้อความ "ค่าแรง" หลอกตา + เช็คฟีเจอร์ใหม่ช่วงหลังว่าซ้ำซ้อนไหม (แก้แล้ว + ตรวจแล้วไม่พบ)
พี่ขอให้ตรวจอีกรอบว่ามี card ไหนกรอกข้อมูลซ้ำซ้อนกับ card อื่นบ้าง เจอ 1 จุดจริง (ข้อความหลอกตา) + ตรวจลึกฟีเจอร์ใหม่ 4 อย่างที่เพิ่มช่วงหลัง (ขายลูกค้าภายนอก, แพที่จับ, ผู้บันทึกสต็อก, TVC/แบคทีเรียตับ) ไม่พบจุดซ้ำซ้อนเพิ่ม

**[แก้แล้ว] หน้า "ค่าแรง" — ข้อความเชื่อมอัตโนมัติไม่ตรงกับความจริง:** `index.html`/`TawanFarm_App.html` — เดิมมีข้อความเขียนว่า "🔗 เชื่อมกับ 'ปิดรอบ' — ค่าแรงรายบ่อจะถูกดึงไปรวมอัตโนมัติ" แต่โค้ดจริง (`openCropModal()`) เคลียร์ช่อง `cm-cost-labor` ว่างทุกครั้ง เป็นช่องกรอกเองล้วนๆ (ตามที่พี่ยืนยันไว้ก่อนหน้านี้ในเซสชันนี้ว่าต้องการให้กรอกเองเพราะต้องเทียบกับตัวเลขจริงนอกแอป) — ข้อความเดิมไม่เคยถูกอัปเดตให้ตรงกับการตัดสินใจนั้น ทำให้ผู้ใช้เข้าใจผิดว่าไม่ต้องกรอกซ้ำ แก้เป็นข้อความเตือน (สีเหลือง) บอกตรงๆ ว่าต้องกรอกเองตอนปิดรอบ ทดสอบผ่าน browser แล้ว ไม่มี console error

**[ตรวจแล้ว ไม่พบซ้ำซ้อน] ฟีเจอร์ใหม่ 4 อย่างช่วงหลัง:**
- **แพที่จับ** (`hv-buyer` ตอนพาเชียล → `cm-buyer` ตอนปิดรอบ) — ไม่ใช่การพิมพ์ซ้ำ เพราะ `openCropModal()` ดึงชื่อแพจากพาเชียลล่าสุดของรอบมา prefill ให้อัตโนมัติอยู่แล้ว (`lastPartial?.buyer`) แก้ไขได้ถ้าจับที่แพอื่น
- **ผู้บันทึกสต็อก** (`curUserLabel()`) — ดึงจากบัญชี Firebase Auth ที่ล็อกอินอยู่โดยอัตโนมัติ ไม่มีช่องให้กรอกเลย จึงไม่มีทางซ้ำซ้อนกับที่อื่น
- **TVC/แบคทีเรียในตับ** — เช็คแล้วเป็นค่าวัดคนละตัวกับที่มีอยู่แล้วทั้งหมด (TCBS น้ำ/ตับ = จำนวนเชื้อจากตัวอย่างน้ำ/เนื้อเยื่อตับ, TVC น้ำ/ตับ = ค่าวัดคนละมาตรฐาน, "ลักษณะตับอ่อน" = การประเมินด้วยสายตา) ไม่ทับซ้อนกัน แค่ถูกอัดรวมอยู่ในการ์ด "บันทึกผลแล็บ" การ์ดเดียวจนมีช่องกรอกเยอะขึ้นเรื่อยๆ (ตอนนี้ ~11 ช่อง) — ไม่ใช่บั๊ก แต่เป็นจุดที่การ์ดเริ่มแน่นขึ้น ถ้าอยากแบ่งเป็นหมวดย่อย (เช่น accordion) ในอนาคตแจ้งได้
- **ขายลูกค้าภายนอก** — ไม่มีหน้า "จัดการลูกค้า" แยกต่างหาก ชื่อลูกค้า/ร้าน (`sk-tx-cust`) เป็นช่องพิมพ์อิสระไม่มี autocomplete จากชื่อที่เคยกรอกไว้ก่อนหน้า — ไม่ใช่ข้อมูลซ้ำซ้อนกับ card ไหน (ไม่มีชื่อลูกค้าที่อื่นในระบบให้ซ้ำ) แต่เป็นความเสี่ยงพิมพ์ชื่อเดียวกันไม่ตรงกัน (เช่น "ร้านฟาร์มสุข" vs "ฟาร์มสุข") ทำให้รายงานแยกลูกค้าผิดกลุ่มได้ — เป็นข้อเสนอแนะเสริม ไม่ใช่บั๊ก ยังไม่ได้แก้

### 44. หน้า P&L — ลบคอลัมน์/ข้อมูล "พัสดุ" ทิ้งทั้งหมด (แก้แล้ว)
ต่อจากข้อ 43 — พี่บอกว่าในเมื่อ `costStock` ไม่ได้เอามารวมกับต้นทุนแล้ว ให้ลบชุดข้อมูลนี้ทิ้งไปเลย (แทนที่จะเก็บไว้เป็นคอลัมน์อ้างอิงเฉยๆ)

**ไฟล์:** `index.html`/`TawanFarm_App.html` — `renderPL()`, `renderPLTable()`

- ลบฟังก์ชัน `getStockCostForPond()` ทิ้งทั้งหมด (ไม่มีจุดอื่นเรียกใช้แล้วหลังข้อ 43)
- `renderPL()` เลิกคำนวณ/แนบ `costStock` เข้าแต่ละแถวข้อมูล
- ตัดคอลัมน์ "พัสดุ(฿)" (พร้อม tooltip ℹ️ ที่เพิ่งใส่ไว้ในข้อ 43) ออกจากตาราง P&L รายบ่อทั้งหมด — ตารางเหลือ 12 คอลัมน์ (จากเดิม 13)

ทดสอบผ่าน browser: `typeof getStockCostForPond === 'undefined'` ยืนยันลบฟังก์ชันแล้วจริง, หัวตาราง/แถวข้อมูลไม่มีคอลัมน์พัสดุเหลืออยู่ (นับได้ 12 cell ตรงกับหัวตาราง), ต้นทุนรวมในหน้า P&L ยังถูกต้องเหมือนเดิม (218,100 ตามตัวอย่างเดียวกับข้อ 43) ไม่มี console error

### 43. 🔴 หน้า P&L — แก้บัคนับต้นทุนพัสดุซ้ำ (แก้แล้ว)
บัคที่แจ้งไว้ตั้งแต่ตอนทำแท็บขายลูกค้าภายนอก (ข้อ 32) — `costStock` (ยอดเบิกสต็อกดิบทั้งหมดของบ่อ จาก `getStockCostForPond`) ถูกบวกเข้า "ต้นทุนรวม" ซ้ำกับ `costMed+costLime+costEquip` ที่กรอก/ดึงไว้แล้วตอนปิดรอบ ทั้งที่เป็นข้อมูลชุดเดียวกัน — ทำ mockup เทียบก่อน-หลังให้ดูก่อน ([mockup_แก้บัค_PL_นับซ้ำ.html](../Mockup และ Prototype/mockup_แก้บัค_PL_นับซ้ำ.html)) พี่คอนเฟิมแล้วค่อย implement จริง

**ไฟล์:** `index.html`/`TawanFarm_App.html` — `renderPLKPI()`, `renderPLTable()`, `renderPLCostChart()`

- ตัด `+(r.costStock||0)` ออกจากสูตรรวม "ต้นทุนรวม" ทั้ง 3 จุด: KPI card, คอลัมน์ "ต้นทุน(฿)" ในตารางรายบ่อ, ผลรวม/สัดส่วนในกราฟโดนัทต้นทุน
- คอลัมน์ "พัสดุ(฿)" ในตารางรายบ่อยังโชว์ `r.costStock` ไว้เหมือนเดิม (เผื่ออยากดูอ้างอิง) แต่เพิ่มไอคอน ℹ️ พร้อม tooltip อธิบายว่าเป็นข้อมูลชุดเดียวกับเวชภัณฑ์/ปูน/อุปกรณ์ ไม่ได้บวกซ้ำเข้าต้นทุนแล้ว
- กราฟโดนัท: เอาหมวด "พัสดุ(ระบบเดิม)" (ที่จริงคือ `costStock` ไม่ใช่ของเก่าอย่างที่ label บอก) ออกจากรายการทั้งหมด เพราะพอไม่รวมในผลรวมแล้วโชว่เป็น slice ก็ไม่มีความหมาย

**ผลกระทบ:** กระทบเฉพาะหน้า P&L (KPI/ตาราง/กราฟ) เท่านั้น — กำไรสุทธิที่โชว์ในหน้านี้ของทุกรอบที่ปิดไปแล้วจะขยับขึ้นเล็กน้อย (เท่ากับส่วนที่เคยนับซ้ำ) ไม่กระทบข้อมูลที่กรอกไว้ในฟอร์มปิดรอบ, PDF สรุปปิดบ่อ, หรือประวัติรอบเลี้ยง เพราะจุดเหล่านั้นไม่เคยใช้ `costStock` มาบวกอยู่แล้ว

ทดสอบผ่าน browser + mock data (จำลองตามตัวอย่างใน mockup เป๊ะ): เบิกพัสดุรวม 4,900 บาท ให้บ่อ, `costMed+costLime+costEquip` ในฟอร์มปิดรอบ = 4,900 เท่ากัน (คนละแหล่งข้อมูลเดียวกัน) → ต้นทุนรวมในหน้า P&L หลังแก้ = 218,100 ตรงกับผลรวม 8 รายการต้นทุนตรงพอดี (ไม่บวกซ้ำ 4,900 อีก) ทั้ง KPI card, ตารางรายบ่อ, และกราฟโดนัทตรงกันหมด ไม่มี console error

### 42. หน้าผลแล็บ — แยก TVC น้ำ/ตับ เหมือน TCBS (เพิ่มใหม่)
พี่แจ้งว่า TVC (Total Viable Count) มีทั้งผลตรวจจากน้ำและจากตับ เดิมมีช่อง TVC เดียวปนกันอยู่ — ทำตามแพทเทิร์นเดียวกับ TCBS/เชื้อเขียว-เหลือง ที่เพิ่งแยกน้ำ/ตับไปก่อนหน้านี้ (ข้อ 36)

**ไฟล์:** `index.html`/`TawanFarm_App.html` — เปลี่ยนป้ายชื่อ `lb-tvc` เป็น "TVC — น้ำ" เพิ่มช่องใหม่ `lb-tvc-hep` ("TVC — ตับ"), แก้ `saveLab()`/`clearLab()`/`editLab()`/`updateLabStatus()`/`renderLabTable()` + เพิ่มตัวเลือกในหน้าเปรียบเทียบ (`cmp-lab-param`/`labLbls`)

- เก็บค่าตับลงฟิลด์ใหม่ `tvcHepato` ใน `p.labResults[]` แยกจาก `tvc` (น้ำ) เดิม ใช้เกณฑ์สีเดียวกัน (ปกติ &lt;10^5)
- ตารางประวัติผลแล็บเพิ่มคอลัมน์ "TVC (ตับ)" แยกจาก "TVC (น้ำ)"
- หน้าเปรียบเทียบเพิ่มตัวเลือก "TVC - ตับ (CFU/g)" แยกจาก "TVC - น้ำ (CFU/mL)" เดิม

ทดสอบผ่าน browser: กรอก TVC น้ำ 2×10^5 + TVC ตับ 5×10^7 บันทึกแล้วค่าถูกต้องครบทั้งคู่, แบนเนอร์สถานะแยกข้อความ "TVC น้ำ"/"TVC ตับ" ถูกต้อง, ตารางประวัติโชว์ทั้งสองคอลัมน์ถูกต้อง, แก้ไขรายการ prefill ค่ากลับมาถูกต้อง, หน้าเปรียบเทียบเลือก "TVC - ตับ" แล้วกราฟขึ้นค่าตรงกับข้อมูลจริงของทั้ง 2 บ่อ ไม่มี console error

### 41. สต็อก — เก็บชื่อผู้บันทึกทุกรายการ (รับเข้า/เบิกออก/ขาย) เอาไว้ re-check ย้อนหลัง (เพิ่มใหม่)
พี่ขอให้เก็บ log ว่าใครเป็นคนบันทึกรายการสต็อกแต่ละรายการ เผื่อกรอกผิดพลาดจะได้ตามหาคนแก้ได้

**ไฟล์:** `index.html`/`TawanFarm_App.html` — ฟังก์ชันใหม่ `curUserLabel()`, แก้ `saveStockTxBatch()`, ตัวตัดสต็อกอัตโนมัติจากบันทึกอาหาร, `renderStockInLog()`/`renderStockOutLog()`/`renderSalesReport()`/`exportSaleCSV()`

- `curUserLabel()` ดึงชื่อผู้ใช้ที่ล็อกอินอยู่จาก Firebase Auth (`displayName` ถ้ามี ไม่งั้นใช้ส่วนหน้า `@` ของอีเมล) — ใช้ข้อมูลบัญชีที่มีอยู่แล้วในระบบ ไม่ต้องเพิ่มช่องกรอกชื่อเอง
- ทุกรายการที่เขียนลง `D.inventoryLog` (รับเข้า/เบิกออกไปบ่อ/ขายลูกค้าภายนอก ผ่านฟอร์มสต็อก และรายการตัดสต็อกอัตโนมัติจากการบันทึกอาหาร) จะแปะฟิลด์ `by` เก็บชื่อผู้บันทึกไว้ตอนสร้างรายการ
- เพิ่มคอลัมน์ "ผู้บันทึก" ในตารางประวัติทั้ง 3 จุด: ประวัติรับเข้าส่วนกลาง, ประวัติเบิกออกรายบ่อ, รายงานยอดขายลูกค้าภายนอก (รวมถึง Export CSV ของรายงานขายด้วย)
- **ขอบเขตที่ตั้งใจ:** เก็บเฉพาะ "ใครสร้างรายการ" ตามที่ขอ ไม่ได้ทำ log การแก้ไข/ลบเพิ่มเติม (ถ้าต้องการรู้ว่าใครลบ/แก้ด้วยแจ้งเพิ่มได้) — เป็นการบันทึกระดับ client เหมือนจุดอื่นในแอป ไม่ใช่ audit log ที่ป้องกันการแก้ไขย้อนหลังได้ (ดูข้อ 7 เรื่องข้อจำกัดการซ่อนข้อมูล/สิทธิ์ระดับ UI)

ทดสอบผ่าน browser (จำลอง `firebase.auth().currentUser`): บันทึกรับเข้า/เบิกออก/ขาย ครบทั้ง 3 โหมด ชื่อผู้บันทึกถูกเก็บและแสดงถูกต้องในตารางประวัติทั้ง 3 จุด ไม่มี console error

### 40. สต็อก — ช่องราคาขายลูกค้าภายนอก เลิกดึงราคาแนะนำอัตโนมัติ ต้องกรอกเองทุกครั้ง (แก้แล้ว)
พี่ขอให้ช่องราคาขาย (โหมด "ขายลูกค้าภายนอก") เป็นช่องเปล่าไม่ต้องมีราคาโชว์ขึ้นมาอัตโนมัติ เพราะราคาปรับเปลี่ยนบ่อย เดิม `addStockToCart()` ดึง `item.sellPrice||item.unitPrice` มา prefill ให้ในตะกร้าเสมอ

**ไฟล์:** `index.html`/`TawanFarm_App.html` — `addStockToCart()`, `updateCartPrice()`, `renderStockCart()`, `saveStockTxBatch()` + ลบช่อง "ราคาขายแนะนำ" (`sk-sell`) ออกจากฟอร์มรายการพัสดุทั้งหมด

- ช่องราคาในตะกร้าตอนขายเริ่มต้นว่างเปล่าเสมอ (ไม่มี prefill/placeholder เป็นตัวเลขแล้ว) ต้องกรอกเองทุกครั้ง
- เพิ่มการเช็คก่อนบันทึก: ถ้าเป็นโหมดขายแล้วไม่ได้กรอกราคา จะแจ้งเตือน "กรุณากรอกราคาขาย ... ก่อนบันทึก" กันบันทึกราคา 0 บาทโดยไม่ตั้งใจ
- **ลบฟิลด์ "ราคาขายแนะนำ" (`sk-sell`/`item.sellPrice`) ออกทั้งหมด** เพราะเป็นที่มาเดียวของราคาที่ดึงมา prefill — ตอนนี้ไม่มีใครใช้ค่านี้แล้วจึงลบทิ้งแทนที่จะปล่อยเป็นช่องกรอกที่ไม่มีผลอะไร (ข้อมูลเก่าที่เคยมี `sellPrice` ติดอยู่ใน `D.inventoryItems` จะยังอยู่ในฐานข้อมูลเฉยๆ ไม่ได้ลบทิ้ง แค่แอปไม่อ่าน/ไม่เขียนฟิลด์นี้อีกต่อไป)

ทดสอบผ่าน browser: เพิ่มสินค้าเข้าตะกร้าโหมดขาย ช่องราคาว่างเปล่าจริง (ไม่ใช่ "0"), กดบันทึกโดยไม่กรอกราคาถูกบล็อกพร้อมข้อความแจ้งเตือนถูกต้อง, กรอกราคาแล้วบันทึกสำเร็จเก็บค่าถูกต้อง, ฟอร์มรายการพัสดุไม่มีช่อง "ราคาขายแนะนำ" หลงเหลือแล้ว ไม่มี console error

### 39. เพิ่มไอคอนแอป + manifest.json — ติดตั้งเป็นไอคอนหน้าจอโฮมได้จริง (เพิ่มใหม่)
พี่ถามว่าทำเป็นไอคอนแอปในโทรศัพย์ได้ไหม พร้อมส่งโลโก้จริง (สองตัวกุ้งวงกลมพีช + "TAWAN FARM") มาให้ใช้

**สถานะเดิม:** มี `apple-mobile-web-app-capable` อยู่แล้วแต่ไอคอนเป็น SVG แบบ data-URI ซึ่ง iOS ส่วนใหญ่ไม่ยอมโชว์เป็นไอคอน (ขึ้นเป็น screenshot แทน) และไม่มี `manifest.json` เลย ทำให้ Android/Chrome ไม่มีปุ่ม "ติดตั้งแอป" ให้กด ต้องเข้าเมนูเอง และเปิดมาก็ยังมีแถบ URL ค้าง ไม่เต็มจอ

**ไฟล์:** `repo/icons/` (ใหม่: `icon-192.png`, `icon-512.png`, `apple-touch-icon.png` 180×180, `favicon-32.png`), `repo/manifest.json` (ใหม่), แก้ `<head>` ของ `index.html`/`TawanFarm_App.html`

- ย่อโลโก้จริงที่พี่ส่งมา (`รูปภาพอื่นๆ/E6446836-...png`, ต้นฉบับ 500×500) เป็น PNG 4 ขนาดด้วย System.Drawing (PowerShell) แบบ high-quality bicubic ไม่ใช้ AI สร้างใหม่ — ใช้รูปจริงตามที่ขอ
- เพิ่ม `<link rel="manifest">` + เปลี่ยน `apple-touch-icon`/`icon` ให้ชี้ไฟล์ PNG จริงแทน SVG data-URI เดิม
- `manifest.json`: `display:"standalone"` (ซ่อนแถบ URL เวลาเปิดจากไอคอนหน้าจอโฮม), ชื่อแอป "ตะวันฟาร์ม", สีธีมตรงกับแอป (`#F6F7F9`)

ทดสอบผ่าน browser + local server: `manifest.json` โหลดผ่าน fetch ได้ค่าครบถูกต้อง, ลิงก์ icon/apple-touch-icon resolve ได้ทั้งหมด (200), ไม่มี console error — **ยังไม่ได้ทดสอบ "เพิ่มไปหน้าจอโฮม" จริงบนมือถือ** (ต้องรอ deploy ผ่าน GitHub Pages ก่อนถึงจะลองกดจากมือถือได้จริง)

### 38. หน้าภาพรวม — การ์ดสภาพอากาศ เพิ่มข้อมูลน้ำขึ้นน้ำลงของวันนั้น (เพิ่มใหม่)
พี่ขอให้การ์ด "สภาพอากาศวันนี้" ในหน้าภาพรวม (`ov-weather-card`) โชว์น้ำขึ้นน้ำลงของวันนั้นด้วย — ต่อเนื่องจากข้อ 37 ที่เพิ่งเชื่อมตารางน้ำขึ้นน้ำลงจริงเข้าแอป

**ไฟล์:** `index.html`/`TawanFarm_App.html` — ฟังก์ชันใหม่ `_tTodaySummary()` + แก้ `renderOvWeatherCard()`

- `_tTodaySummary()` คืนเวลา/ระดับน้ำขึ้นสูงสุด+ลงต่ำสุดของวันนี้ — ใช้ `TIDE_TABLE` ตรงๆ ถ้าวันนี้อยู่ในช่วงข้อมูลจริง (มิ.ย.–ธ.ค. 2569) ไม่งั้น fallback ไปหา extrema จากโมเดล Harmonic เฉพาะวันนี้เหมือนกลไกในหน้า "น้ำขึ้นน้ำลง" (ข้อ 37)
- เพิ่มบรรทัด "🌊 ขึ้น HH:MM น. (X.X ม.) · ลง HH:MM น. (X.X ม.)" ต่อท้ายการ์ดสภาพอากาศ — แสดงได้ทันทีแม้สภาพอากาศ (`_wxToday`, ดึงจาก API ภายนอก) ยังโหลดไม่เสร็จ เพราะข้อมูลน้ำขึ้นน้ำลงคำนวณในเครื่องไม่ต้องรอ network

ทดสอบผ่าน browser: การ์ดโชว์ "ขึ้น 01:00 น. (2.4 ม.) · ลง 11:00 น. (0.6 ม.)" ตรงกับ `TIDE_TABLE['2026-07-17']` เป๊ะ, ตอนสภาพอากาศยังโหลดไม่เสร็จ (`_wxToday=null`) บรรทัดน้ำขึ้นน้ำลงยังโชว์ปกติ ไม่มี console error

### 37. 🔴 น้ำขึ้นน้ำลง — ค่าที่โชว์ไม่ตรงกับตารางจริงที่พี่ให้ไว้ (แก้แล้ว)
พี่แจ้งว่าข้อมูลน้ำขึ้นน้ำลงในเว็บไม่ตรงกับข้อมูลที่ป้อนให้ — ตรวจแล้วพบว่าหน้า `sec-tide` เดิมคำนวณทุกอย่างจากโมเดล **Harmonic constituent แบบประมาณการ** (`_tAt`/`_tExtrema`, คอมเมนต์เดิมก็ระบุเองว่า "ประมาณ ±30–60 นาที และ ±0.15ม.") ไม่เคยอ่านข้อมูลจริงที่พี่ให้ไว้เลย — เจอไฟล์ตารางน้ำขึ้นน้ำลงจริง 2 ไฟล์ที่ `Mockup และ Prototype/../ตารางน้ำขึ้นน้ำลง/` (`tides_2026-06.json`, `tides_jul-dec_2026.json` รวม 214 วัน มิ.ย.–ธ.ค. 2569 วันละ 1 ขึ้น+1 ลง) ที่ไม่เคยถูกเชื่อมเข้าแอปเลย

**ไฟล์:** `index.html`/`TawanFarm_App.html` — `TIDE_TABLE` (ข้อมูลจริงฝังเป็น const), ฟังก์ชันใหม่ `_tDateKey`/`_tTimeToMs`/`_tAt2`/`_tExtremaMixed`, แก้ `renderTide()`

- ฝังข้อมูลจริงทั้ง 2 ไฟล์ (214 วัน) ลงเป็น `TIDE_TABLE` ในโค้ดโดยตรง
- `_tExtremaMixed(s0,s1)` แทนที่ `_tExtrema()` เดิมในจุดที่ใช้แสดงรายการขึ้น/ลงรายวัน — วันไหนอยู่ในช่วงตารางจริงใช้ค่า `hiT/hiV/loT/loV` ตรงๆ (ไม่ผ่านการคำนวณเลย ตรงเป๊ะกับที่ให้มา) วันไหนอยู่นอกช่วง (ก่อน 1 มิ.ย. 69 หรือหลัง 31 ธ.ค. 69) ค่อย fallback ไปสแกนหา extrema จากโมเดล Harmonic เหมือนเดิม
- `_tAt2(ms)` แทนที่ `_tAt()` เดิมสำหรับ "ระดับน้ำตอนนี้" และกราฟคลื่นรายวัน — ตารางจริงมีแค่ 1 ขึ้น+1 ลง/วัน (ไม่ใช่เส้นโค้งต่อเนื่อง) จึงต้อง interpolate ระหว่างจุดขึ้น/ลงจริงของวันก่อน/วันนี้/วันถัดไป (สูงสุด 6 จุด) ด้วยสูตร cosine ease ให้เส้นโค้งลื่นและวิ่งผ่านค่าจริงพอดีทุกจุด นอกช่วงตารางจริง fallback ไป `_tAt()` เดิม
- แก้ข้อความแจ้งท้ายการ์ดให้ตรงกับความจริง: ถ้า 7 วันข้างหน้าอยู่ในช่วงตารางจริงครบ โชว์ "✅ ข้อมูลจริงจากตารางน้ำขึ้นน้ำลง" แทนคำเตือน "ค่าโดยประมาณ" เดิม (ถ้าคาบเกี่ยวช่วงนอกตาราง โชว่าข้อความผสมบอกตามจริง)

ทดสอบผ่าน browser: รายการขึ้น/ลง 7 วันข้างหน้าตรงกับ `TIDE_TABLE` เป๊ะทุกวัน (เวลา+ระดับน้ำ), ระดับน้ำ ณ เวลาที่ตรงกับจุดขึ้น/ลงจริงในตารางให้ค่าตรงเป๊ะ (ทดสอบ 17 ก.ค. 69: ขึ้น 01:00=2.40ม., ลง 11:00=0.60ม. ตรงกับตารางทุกตัว), วันที่นอกช่วงตาราง (ทดสอบ มี.ค. 2570) ยัง fallback ไปใช้ค่า Harmonic เดิมถูกต้อง ไม่มี console error

### 36. หน้าผลแล็บ — เพิ่มช่องบันทึกแบคทีเรียในตับ (Hepatopancreas) แยกจาก TCBS ในน้ำ (เพิ่มใหม่)
พี่ขอเพิ่มช่องบันทึก "แบคทีเรียในตับ" ในการ์ด "บันทึกผลแล็บ" — ก่อนหน้านี้มีแค่ TCBS ในน้ำ (เชื้อเขียว/เหลือง) ยังไม่มีช่องสำหรับผลตรวจแบคทีเรียจากเนื้อเยื่อตับ/ตับอ่อนโดยเฉพาะ ยืนยันกับพี่แล้วว่าต้องการรูปแบบเดียวกับ TCBS ในน้ำ (แยกเขียว/เหลือง 2 ช่อง)

**ไฟล์:** `index.html`/`TawanFarm_App.html` — markup การ์ด "บันทึกผลแล็บ" (`lb-hep-g`, `lb-hep-y`) + `saveLab()`/`clearLab()`/`editLab()`/`updateLabStatus()`/`renderLabTable()`

- เพิ่มช่อง "🦠 จดบันทึกแบคทีเรียในตับ (Hepatopancreas)" ต่อจากบล็อก TCBS ในน้ำเดิม — 2 ช่อง เชื้อเขียว/เหลือง หน่วย CFU/g ใช้เกณฑ์สีเดียวกับ TCBS ในน้ำ (เขียว: ปลอดภัย&lt;100 / เฝ้าระวัง 100–1,000 / อันตราย&gt;1,000, เหลือง: ปลอดภัย&lt;1,000 / เฝ้าระวัง&gt;1,000)
- เก็บลงฟิลด์ใหม่ `hepatoGreen`/`hepatoYellow` ใน `p.labResults[]` (คนละฟิลด์กับ `hepato` เดิมที่เป็น dropdown ลักษณะตับแบบมอง — ไม่ปนกัน)
- แสดงในแบนเนอร์สถานะ (`updateLabStatus`) และตารางประวัติผลแล็บ (2 คอลัมน์ใหม่ "🟢/🟡 (ตับ)" แยกจากคอลัมน์น้ำเดิม) พร้อมโหลดกลับมาแก้ไขได้ปกติผ่านปุ่ม ✏️
- **เชื่อมกับหน้า "เปรียบเทียบ" (`sec-compare` แท็บ 🔬 แล็บ) แล้ว** — พี่ถามและขอเพิ่มด้วย: เพิ่ม 2 ตัวเลือกใหม่ "เชื้อเขียว/เหลือง - ตับ" ใน dropdown `cmp-lab-param` (เดิมมีแค่เชื้อเขียว/เหลือง - น้ำ, TVC) label เดิมก็เปลี่ยนต่อท้ายเป็น "- น้ำ" ให้แยกชัดจากของใหม่ — `renderCmpLab()` เดิม generic อยู่แล้ว (`r[metric]`) เลยแค่เพิ่ม option + label ใน `labLbls` ก็ใช้งานได้ทันทีไม่ต้องแก้ logic เพิ่ม
- **ขอบเขตที่ตั้งใจ (ยังไม่ทำ):** ไม่ได้ผูกเข้ากับระบบให้คะแนนความเสี่ยง (`calcRisk`/`tcbsTotal`) เพราะพี่ยังไม่ได้ขอ — ถ้าต้องการให้ค่านี้มีผลกับคะแนนความเสี่ยงด้วยแจ้งเพิ่มได้

ทดสอบผ่าน browser: เลือก 2 บ่อในหน้าเปรียบเทียบ สลับ dropdown เป็น "เชื้อเขียว - ตับ" กราฟขึ้นค่าตรงกับข้อมูลจริงที่บันทึกไว้ (1,500 vs 80) ถูกต้อง ไม่มี console error

ทดสอบผ่าน browser: กรอกค่าเขียว 1,500/เหลือง 300 → แบนเนอร์เตือน "อันตราย AHPND" ถูกต้อง, บันทึกแล้วเก็บ `hepatoGreen:1500,hepatoYellow:300` ถูกต้อง, ตารางประวัติโชว์ค่าถูกต้อง, กด ✏️ แก้ไข prefill ค่ากลับมาถูกต้อง, กด "ล้าง" เคลียร์ช่องใหม่ด้วย ไม่มี console error

### 35. PDF ปิดบ่อ — เพิ่มติ๊กเลือกกราฟที่ต้องการแสดง (เพิ่มใหม่)
พี่ขอให้มีช่องติ๊กเลือกว่าจะโชว์กราฟตัวไหนบ้างใน PDF สรุปปิดบ่อ จากข้อมูลที่มีบันทึกไว้จริง

**ไฟล์:** `index.html`/`TawanFarm_App.html` — markup `#ex-chart-toggles` ในหน้า `sec-export`, ฟังก์ชันใหม่ `renderExChartToggles()`, แก้ `onExPondChange()`/`renderExPreview()`/`genPDF()`

กราฟที่มีให้เลือกต่างกันตามโหมดที่เลือก (ตามที่ `genPDF()` ออกแบบไว้เดิม):
- **เลือกบ่อเดียว** (สรุปหน้าเดียว): 💰 กำไร-ต้นทุนเทียบรอบก่อนหน้า, 📊 FCR/อัตรารอดเทียบรอบก่อนหน้า — โชว์ก็ต่อเมื่อบ่อนั้นมีรอบปิดแล้ว ≥1 รอบ
- **เลือกทุกบ่อ** (สรุปละเอียดหลายหน้า): 📈 การเติบโต (ABW), 🍚 อาหารสะสมตลอดรอบ, 📊 FCR/อัตรารอดเทียบรอบก่อนหน้า — แต่ละอันโชว์ก็ต่อเมื่อมีอย่างน้อย 1 บ่อที่มีข้อมูลจริงสำหรับกราฟนั้น (สุ่มตัวอย่าง/feedLog/ปิดรอบ ≥2 รอบ ตามลำดับ)

`renderExChartToggles()` ซ่อนแถวติ๊กที่ไม่มีข้อมูลให้ใช้อัตโนมัติ (ไม่โชว์ตัวเลือกที่กดไปแล้วไม่มีผลอะไร) ค่าเริ่มต้นติ๊กไว้ครบทุกอันที่มีข้อมูล สลับบ่อ/โหมดใหม่จะรีเซ็ตกลับเป็นค่าเริ่มต้น (ไม่จำการติ๊กข้ามบ่อ) — `genPDF()` เช็คสถานะติ๊กผ่าน `chartWant(kind)` ก่อนจะ push chart job และวาดกล่อง canvas ในทุกจุดที่เคยสร้างกราฟแบบไม่มีเงื่อนไขมาก่อน ถ้าไม่ติ๊กเลยสักอันในโหมดบ่อเดียว ส่วน "กราฟสำหรับผู้บริหาร" จะหายไปทั้งบล็อก (grid ปรับเป็น 1 คอลัมน์อัตโนมัติถ้าติ๊กแค่อันเดียว)

ทดสอบผ่าน browser + mock data: สลับโหมดบ่อเดียว/ทุกบ่อ โชว์ชุดติ๊กถูกต้องตามที่มีข้อมูลจริง, ปลดติ๊ก "FCR เทียบรอบก่อนหน้า" แล้วสร้าง PDF (สปายที่ `Chart` constructor) ยืนยันว่าสร้างเฉพาะกราฟกำไร-ต้นทุนจริง ไม่มีกราฟ FCR ปนมา, สลับไปโหมดทุกบ่อปลดติ๊ก "การเติบโต" ยืนยันไม่สร้างกราฟ ABW แต่กราฟเปรียบเทียบยังสร้างตามปกติ ไม่มี console error

### 34. 🔴 "แพที่จับ" ในหน้าสรุป/PDF ปิดบ่อ ไม่มีที่มาจริง โชว์ค่างว่างเสมอ (แก้แล้ว)
พี่ถามว่าฟอร์ม "แก้ไขข้อมูลปิดบ่อ" ไม่มีช่องกรอกแพที่จับกุ้งเลย แล้วหน้าสรุป PDF ที่มีแถว "ปิดบ่อกับแพ" ดึงข้อมูลมาจากไหน — ตรวจแล้วพบว่า**ไม่มีที่มาจริงเลย เป็นค่าว่างเสมอ**

**สาเหตุ:** หน้า "จับกุ้ง" มีช่อง "แพที่ทำการจับ" (`hv-buyer`) แต่โผล่**เฉพาะโหมดพาเชียลเท่านั้น** พอเลือก "ปิดรอบ" จะซ่อนฟอร์มนี้ทันทีไปเปิด `cropModal`/`editCropModal` แทน ซึ่ง**ไม่มีช่องแพเลยทั้งคู่** — และ `getCloseSummary()` (index.html:6735) เดิม hardcode `buyer:''` ในแถว "ปิดบ่อ" ตรงๆ เลย ทำให้แถว "ปิดบ่อกับแพ" ในหน้าสรุป/PDF (index.html:6675, 6808) โชว์ "-" เสมอสำหรับทุกรอบที่ปิดผ่านแอปตามปกติ

**วิธีแก้:** เพิ่มช่อง "แพที่จับ / ผู้ซื้อ" ในฟอร์มปิดรอบทั้งสอง (`cm-buyer` ในฟอร์มปิดรอบใหม่, `ecm-buyer` ในฟอร์มแก้ไขรอบที่ปิดแล้ว) เก็บลง `harvestResult.buyer` แล้วแก้ `getCloseSummary()` ให้ `closeRows` อ่านจาก `harvestResult.buyer` แทนการ hardcode ค่าว่าง — ฟอร์มปิดรอบใหม่ (`openCropModal`) ยัง**ดึงชื่อแพจากพาเชียลล่าสุดของรอบนั้นมา prefill ให้อัตโนมัติ** (เผื่อจับที่แพเดิม) แก้ไขได้ถ้าไม่ตรง ตามแนวทางเดียวกับที่ต้นทุนลูกกุ้ง/อาหารดึงอัตโนมัติอยู่แล้ว

ทดสอบผ่าน browser: ปิดรอบผ่านปุ่มจริงในหน้า "รอบเลี้ยง" กรอกแพ "แพสมชายซีฟู้ด" → บันทึกลง `harvestResult.buyer` ถูกต้อง, `getCloseSummary()` อ่านค่าคืนมาถูกต้อง, เปิดฟอร์มแก้ไขรอบที่ปิดแล้ว prefill ชื่อแพเดิมถูกต้อง แก้เป็นชื่อใหม่บันทึกซ้ำได้ถูกต้อง (round-trip ครบ) ไม่มี console error — **ข้อมูลรอบเก่าที่ปิดไปแล้วก่อนแก้ครั้งนี้จะยังโชว์ "-" เหมือนเดิม เพราะไม่เคยมีการเก็บชื่อแพไว้จริงๆ ตั้งแต่ต้น ต้องเข้าไปกรอกย้อนหลังผ่านฟอร์ม "แก้ไขข้อมูลปิดบ่อ" เอาเองทีละรอบถ้าต้องการ**

### 33. บ่อที่ถูกลบ — ประวัติเบิกสต็อกโชว์ id ดิบแทนชื่อ (แก้ต้นตอ) + รีดีไซน์หน้า "ประวัติ เบิกออกพัสดุ" เป็นตารางเดียวกรองได้ (แก้แล้ว)
พี่รายงานว่าตารางประวัติหน้า "สารเคมี" โชว์ชื่อบ่อแปลกๆ แบบ `P1781065709969` — ตรวจแล้วนั่นคือ pondId ภายใน ไม่ใช่ชื่อบ่อ

**ต้นตอ:** `delPond()` (index.html, หน้า "จัดการบ่อ") ลบบ่อออกจาก `D.ponds` โดยไม่เก็บชื่อไว้เลย ส่วนประวัติเบิกสต็อกเก่า (`D.inventoryLog`) ที่เคยผูกกับ `pondId` นั้นยังอยู่ (ไม่ได้ลบตาม เพราะเป็นข้อมูลบัญชี/ต้นทุนที่ต้องเก็บไว้) — ทุกจุดที่ join ชื่อบ่อจาก pondId (เช่น `renderChemHistory()` เดิมใช้ `pondMap[r.pondId]||r.pondId`) เลยหาไม่เจอแล้ว fallback ไปโชว์ id ดิบแทน

**แก้ต้นตอ:** 
- `delPond()` เก็บชื่อบ่อไว้ใน `D.deletedPondNames={id: name}` ก่อนลบออกจาก `D.ponds`
- เพิ่มฟังก์ชันกลาง `pondNameById(pondId)` เดียว — เช็คบ่อที่ยังอยู่ก่อน ถ้าไม่เจอเช็ค `D.deletedPondNames` (ต่อท้าย "(ลบแล้ว)" ให้รู้ทันที) ถ้ายังไม่เจอค่อย fallback เป็น id ดิบ — แก้ `renderChemHistory()` ให้ใช้ฟังก์ชันนี้แทน `pondMap` เดิม

**รีดีไซน์หน้า "ประวัติ" → "เบิกออก รายบ่อ" ในหน้าสต็อก:** พี่แจ้งว่าอยากได้แบบดูง่าย/อัปเดตง่ายกว่าเดิม ไม่ต้องกดเข้าไปทีละบ่อ — ของเดิม (`renderStockPondGrid`/`showStockPondHist`/`backToStockPondGrid`) เป็นกริดการ์ดบ่อ (เห็นเฉพาะบ่อที่ยังเลี้ยงอยู่ด้วย — บ่อปิด/ลบแล้วมองไม่เห็นประวัติเลย) ต้องกดเข้าไปทีละบ่อถึงเห็นรายการ **ลบฟังก์ชันเหล่านี้ทิ้งทั้งหมด** แทนที่ด้วยตารางเดียว (`renderStockOutLog()`) รวมทุกบ่อ (รวมบ่อลบแล้ว) พร้อมตัวกรอง 3 ช่อง: เลือกบ่อ (dropdown สร้างจากบ่อที่มีประวัติจริงเท่านั้น ใช้ `pondNameById` โชว์ชื่อ), หมวดพัสดุ, เดือน — กดปุ่ม 🗑️ ลบได้ในแถวทันทีเหมือนเดิม (ย้อนสต็อกคืนอัตโนมัติ + รีเฟรชตัวกรอง)

ทดสอบผ่าน browser + mock data: บ่อที่ถูกลบ (`delPond()`) โชว์ "ชื่อบ่อเดิม (ลบแล้ว)" ถูกต้องในทั้ง dropdown และตาราง (ไม่ใช่ id ดิบ), กรองตามบ่อ/หมวด/เดือนได้ถูกต้อง, ลบแถวแล้วสต็อกย้อนคืนถูกต้อง + dropdown ตัดตัวเลือกบ่อที่ไม่มีประวัติเหลือออกอัตโนมัติ, สลับโหมด เบิกออก/รับเข้า ยังทำงานปกติ ไม่กระทบแท็บขายลูกค้าภายนอก (ข้อ 32) ไม่มี console error

### 32. สต็อกพัสดุ — เพิ่มโหมด "ขายลูกค้าภายนอก" + รายงานยอดขายแบ่งรายเดือน (เพิ่มใหม่)
พี่แจ้งว่าฟาร์มเป็นเอเยนต์ขายอาหารกุ้ง/เวชภัณฑ์ให้ลูกค้านอกด้วย ต้องการตัดสต็อกแยกจากของที่ฟาร์มใช้เอง (ไม่ปนต้นทุนบ่อ) พร้อมจัดการบัญชีสะดวกขึ้น — ทำ mockup ให้ดูก่อน ([mockup_ขายลูกค้าภายนอก_สต็อก.html](../Mockup และ Prototype/mockup_ขายลูกค้าภายนอก_สต็อก.html)) พี่คอนเฟิมแล้วค่อย implement จริง

**ไฟล์:** `index.html`/`TawanFarm_App.html` — markup `sec-stock` (ปุ่มโหมดที่ 3 ในฟอร์ม `skp-tx`, แถวชื่อลูกค้า, ช่อง "ราคาขายแนะนำ" ในฟอร์มรายการพัสดุ, pane ใหม่ `skp-sale`) + ฟังก์ชัน `setStockMode`/`addStockToCart`/`renderStockCart`/`saveStockTxBatch`/`deleteStockLog`/`saveStockItem`/`editStockItem`/`clearStockItem` + ฟังก์ชันใหม่ `updateCartPrice`/`saleLogs`/`initSaleMonthPills`/`selectSaleMonth`/`renderSalesReport`/`exportSaleCSV`

**สถาปัตยกรรม:** ใช้ `D.inventoryLog` ก้อนเดียวกับเดิม (ของจริงตัดสต็อกจากที่เดียวกัน) เพิ่ม `type:'sale'` แยกจาก `'in'`/`'out'` เดิม — รายการขายไม่มี `pondId` เลย (ต่างจากโหมด "เบิกออกไปบ่อ" ที่บังคับเลือกบ่อ) ทำให้ฟังก์ชันคิดต้นทุนบ่อทุกตัวที่มีอยู่แล้ว (`getStockCostForPond`/`getStockCostBreakdownForPond`/`getStockCostGroupsForPond`) กรองด้วย `pondId` อยู่แล้วโดยธรรมชาติไม่ต้องแก้อะไรเพิ่ม — รายการขายจะไม่ถูกนับเป็นต้นทุนบ่อไหนเลยโดยอัตโนมัติ และหน้า "ประวัติ" (เบิกออกรายบ่อ) ก็ไม่โชว์รายการขายปนด้วยเหตุผลเดียวกัน (กรองด้วย pondId เหมือนกัน)

- เพิ่มช่อง "ราคาขายแนะนำ (บาท/หน่วย)" (`sellPrice`) ในฟอร์มรายการพัสดุ แยกจาก "ราคาต้นทุน" (`unitPrice`) เดิม
- โหมดขาย: กรอกชื่อลูกค้า/ร้าน (บังคับ) + เพิ่มสินค้าเข้าตะกร้าเหมือนโหมดเดิม แต่มีช่องแก้ราคาขายต่อรายการ (prefill จาก `sellPrice`, ต่อรองราคาได้) — บันทึกแล้วตัดสต็อกจริงเหมือนโหมด "เบิกออก" พร้อมเก็บ snapshot `unitCost` (ราคาต้นทุน ณ วันที่ขาย กันเพี้ยนถ้าแก้ราคาต้นทุนสินค้าทีหลัง)
- แท็บย่อยใหม่ "🛒 ยอดขายลูกค้าภายนอก" ในหน้าสต็อก: ปุ่มแบ่งรายเดือน (แสดงเฉพาะเดือนที่มีข้อมูลจริง), KPI ยอดขายรวม/ต้นทุนรวม/กำไรรวม, ตารางรายการ, ปุ่ม Export CSV (BOM UTF-8 ส่งบัญชีได้ตรง)
- **ซ่อนต้นทุน/กำไรจาก staff** (ตามที่ขอ — staff เห็นแค่ยอดขาย): ใช้ `APP_ROLE!=='staff'` คุมทั้ง KPI การ์ดต้นทุน/กำไร และคอลัมน์ต้นทุน/กำไรในตาราง (admin/ceo เห็นครบ) พร้อมข้อความแจ้งเตือนใน mode staff ว่าทำไมไม่เห็นตัวเลข — เป็นการซ่อนระดับ UI เหมือนจุดอื่นๆ ในแอป (ดูข้อ 7)
- `deleteStockLog()` เดิมเช็คแค่ `type==='out'` ตอนย้อนสต็อกคืน แก้ให้รวม `type==='sale'` ด้วย (ไม่งั้นลบรายการขายแล้วสต็อกจะถูกหักซ้ำแทนที่จะบวกคืน)

ทดสอบผ่าน browser + mock data ครบ: เพิ่มขาย 5 หน่วย ราคาขาย 190/หน่วย ต้นทุน 150/หน่วย → สต็อกตัดถูกต้อง (20→15), รายงานคำนวณยอดขาย/ต้นทุน/กำไรถูกต้อง (950/750/+200), เบิกไปบ่อจริงคู่ขนาน → `getStockCostForPond` นับเฉพาะรายการที่มี pondId ไม่ปนรายการขาย, หน้า "ประวัติ" รายบ่อไม่โชว์รายการขายปน, สลับ role staff↔admin ซ่อน/โชว์ต้นทุน-กำไรถูกต้อง, แก้ไขรายการพัสดุผ่านฟอร์มจริงบันทึก `sellPrice` ถูกต้อง ไม่มี console error

### 31. % ผู้จัดการ — ตัดสูตร % ออก เหลือช่องกรอกเองล้วนๆ (ยังไม่ผูกสูตร)
พี่แจ้งว่า "%ผู้จัดการ" ต้องดึงข้อมูลจากไฟล์อื่นนอกแอป ตอนนี้ยังไม่ต้องผูกสูตรอะไรเลย — ทำแบบเดียวกับที่ทำให้ "%คนเลี้ยง" ในข้อ 30 (รวมช่อง "% ผู้จัดการ" + "จำนวนเงินผู้จัดการ" (readonly, เดิมคำนวณจาก % × กำไรก่อนหักส่วนแบ่ง) เหลือช่องเดียวชื่อ "ส่วนแบ่งผู้จัดการ (บาท)" กรอกยอดเงินตรงๆ เอง ไม่มีการคำนวณ/placeholder แนะนำใดๆ (ต่างจาก %คนเลี้ยงที่ยังมีตัวเลขแนะนำเป็นเงา — อันนี้ไม่มีสูตรให้แนะนำเลยตามที่พี่บอก)

**ผลต่อสูตรกำไรสุทธิ:** เดิม `กำไรสุทธิ = กำไรก่อนหักส่วนแบ่ง − ส่วนแบ่งผู้จัดการ(auto)` ตอนนี้ทั้งคนเลี้ยงและผู้จัดการเป็นยอดเงินกรอกเองล้วนเหมือนต้นทุนรายการอื่นๆ แล้ว เลยง่ายลงเป็น `กำไรสุทธิ = รายได้ − ต้นทุนตรงทั้งหมด (รวมส่วนแบ่งทั้งสองเป็นรายการต้นทุนปกติ)` — ตัด concept "กำไรก่อนหักส่วนแบ่ง" ออกทั้งฟอร์มปิดรอบและฟอร์มแก้ไข

**โครงสร้างข้อมูล:** ตัดฟิลด์ `pctManager` ออกจากการเก็บข้อมูลใหม่ (รอบเก่าที่เคยมีอยู่ยังอ่านแสดงผลได้) เหลือ `amtManager` เก็บยอดยืนยันตรงๆ เหมือน `amtCaretaker` — พบและแก้บัคแบบเดียวกับข้อ 30 ไปพร้อมกัน: `closeSummaryHtml` เดิมเช็คเงื่อนไขโชว์แถวจาก `pctManager!=null` ทำให้รอบใหม่ (ไม่มี pctManager แล้ว) จะไม่โชว์แถวส่วนแบ่งผู้จัดการเลย — แก้เป็นเช็คจาก `amtManager!=null` แทน (จุดเดียวกับที่เคยแก้ให้คนเลี้ยงไปแล้ว)

ทดสอบผ่าน browser จริง: ฟอร์มปิดรอบเหลือช่องเดียว "ส่วนแบ่งผู้จัดการ (บาท)" ไม่มี placeholder แนะนำ (ตามตั้งใจ) → กรอกยอดตรงๆ คำนวณกำไรสุทธิถูกต้องตรงสูตรมือ 100% (ทดสอบ: รายได้ 520,000 − ต้นทุนรวมทุกรายการรวมคนเลี้ยง 11,000+ผู้จัดการ 7,000 = กำไร 222,000 ตรงเป๊ะ) → ปิดรอบจริงบันทึก `amtManager` ถูกต้อง ไม่มี `pctManager` ติดไปด้วย → `closeSummaryHtml` โชว์แถวส่วนแบ่งผู้จัดการถูกต้องแม้ไม่มี % (7,000 บาท ไม่มีต่อท้าย %) → เปิดฟอร์มแก้ไขรอบที่ปิดแล้ว แก้ยอดจาก 7,000→9,000 กำไรลดลง 2,000 ตรงตามจริง บันทึกถูก — ไม่มี console error, sync สองไฟล์แล้ว **ยังไม่ push**

### 30. ส่วนแบ่งคนเลี้ยง + ค่าจับกุ้ง — เปลี่ยนจากกรอกเองล้วนๆ เป็นคำนวณแนะนำจากไซส์/น้ำหนักจับ
พี่ให้เงื่อนไขคำนวณจริงมา 2 จุด (จากเดิมที่เป็นช่องกรอกเองล้วนๆ ในข้อ 29) — คุยชัดก่อนทำ 3 คำถาม (ตัดช่อง "%คนเลี้ยง" เดิมแล้วเก็บเป็นช่องเดียวชื่อ "%คนเลี้ยง" แต่ค่าในช่องเป็น "บาท" ไม่ใช่ % / ช่วงไซส์ 91-99 ที่ไม่ระบุไว้ให้นับเท่าช่วง 90-70 / ช่วง 24-20 กับ 19 ลงไป อัตราเท่ากันจริง 3 บาท/กก.)

**สูตร:**
- `caretakerRateBySize(size)`: ไซส์ ≥100 ตัว/กก.=ไม่ได้ (0), 70-99=1 บ./กก., 50-69=1.5, 30-49=2, 25-29=2.5, ≤24=3 — คูณกับน้ำหนักที่จับได้ (กก.) เป็นยอดแนะนำ
- `catchCostByWeight(weightKg)`: 1.5 บาท/กก. ปกติ, ถ้าน้ำหนักจับไม่ถึง 4,000 กก. (4 ตัน) คิดเหมา 6,000 บาทแทน

**UX "เงาตัวเลข" ตามที่ขอ:** ช่อง "% คนเลี้ยง (บาท)" กับ "ค่าจับกุ้ง (บาท)" ไม่เติมค่าอัตโนมัติให้ — คำนวณแล้วโชว์เป็น **placeholder** (เช่น "แนะนำ 9,000 (2 บ./กก.)") ในช่องที่ยังว่างเปล่า ต้อง**พิมพ์ยืนยันเอง** (จะปัดเศษเป็นเลขกลมๆ ได้ตามใจ) ค่าที่ใช้คำนวณจริงคือค่าที่พิมพ์เท่านั้น ไม่ใช่ตัวเลขแนะนำ

**โครงสร้างข้อมูล:** ตัดฟิลด์ `pctCaretaker`/ช่อง "จำนวนเงินคนเลี้ยง" แยกออกทิ้ง เหลือฟิลด์เดียว `amtCaretaker` (เก็บยอดเงินที่ยืนยันแล้วตรงๆ) — รอบเก่าที่เคยมี `pctCaretaker`+`amtCaretaker` คู่กันยังอ่านแสดงผลได้ปกติ (`closeSummaryHtml` เช็คเงื่อนไขใหม่จาก `amtCaretaker!=null` แทน `pctCaretaker!=null` ไม่งั้นรอบใหม่ที่ไม่มี pctCaretaker จะไม่โชว์แถวนี้เลย) — "% ผู้จัดการ" ไม่แตะ ยังเป็น % กรอกเอง × กำไรก่อนหักส่วนแบ่งเหมือนเดิม

**🔴 บัคที่เจอระหว่างทดสอบและแก้ทันที (เกิดจากงานนี้เอง ไม่ใช่ของเดิม):** ตอนย้ายยอดเงินคนเลี้ยงจาก "% × กำไร" มาเป็น "ยอดเงินตรง" ลืมเอาค่านี้ไปรวมใน `totalCostDirect` ก่อนคำนวณกำไรก่อนหักส่วนแบ่ง — ทำให้ฐานคำนวณ % ผู้จัดการสูงเกินจริงเท่ากับยอดคนเลี้ยง (เช่น คนเลี้ยง 9,000 × %ผู้จัดการ 3% = ผู้จัดการเพี้ยนไป 270 บาท) เจอจากการเทียบเลขคำนวณมือกับที่แอปคำนวณจริงไม่ตรงกัน แก้แล้วโดยรวม `cm-pct-caretaker`/`ecm-pct-caretaker` เข้า `totalCostDirect` ก่อน (edit-form เดิมทำถูกอยู่แล้ว บัคอยู่ที่ close-form เท่านั้น)

ทดสอบผ่าน browser จริง: unit-test สูตร `caretakerRateBySize`/`catchCostByWeight` ครบทุกช่วงไซส์+น้ำหนัก (17+5 เคส ผ่านหมด รวมขอบเขตที่ยืนยันเป็นพิเศษ 91-99 และ 24-20/19ลงไป) → เปิดฟอร์มปิดรอบ placeholder ขึ้นถูกต้อง (ไซส์ 40→2บ./กก.×4,500กก.=9,000, น้ำหนัก 4,500กก.≥4ตัน→1.5×4,500=6,750) → ช่องยังว่างจริงไม่ใช่ค่าเติม → พิมพ์ยืนยันเลขกลมๆ เองแล้วคำนวณกำไร/ส่วนแบ่งผู้จัดการตรงตามสูตรมือ 100% (แก้บัคข้างต้นแล้วตรง) → ปิดรอบจริง บันทึกถูก → getCloseSummary/closeSummaryHtml/PDF/P&L อ่านค่าตรงกันหมด → เปิดฟอร์มแก้ไขรอบที่ปิดแล้วค่าขึ้นครบถูกต้อง — ไม่มี console error, sync สองไฟล์แล้ว **ยังไม่ push**

### 29. ตัวเลขปิดบ่อ — แยกต้นทุนเป็น 12 หมวดละเอียด + ส่วนแบ่งกำไรคนเลี้ยง/ผู้จัดการ
พี่ต้องการแยกต้นทุนตอนปิดบ่อจากเดิม 6 หมวด (ลูกกุ้ง/อาหาร/สารเคมี/ค่าแรง/ค่าไฟ/อื่นๆ) เป็น 12 หมวดละเอียด — คุยชัดเจน 3 จุดก่อนทำ (ค่าจับกุ้งกรอกเองเหมือนข้ออื่น, %ส่วนแบ่งเป็น%ของกำไรสุทธิและหักเป็นต้นทุน, ค่าแรงเปลี่ยนจากดึงอัตโนมัติเป็นกรอกเองทั้งหมด — ค่าไฟฟ้ายังคงสูตร 2,500 บ./วันเหมือนเดิม)

**หมวดใหม่ (เรียงตามที่พี่ระบุ):**
1. ต้นทุนอาหารกุ้ง — auto (`pondFeedCost`)
2. ต้นทุนลูกกุ้ง — auto จาก `p.costFry` แก้ไขได้
3. ต้นทุนเวชภัณฑ์ — auto ใหม่ รวมยากิน+ยาลงน้ำ+จุลินทรีย์กิน+จุลินทรีย์ลงน้ำ (และหมวด "เดิม" ยา/สารเคมี ถ้ามี) จากสต็อกจริง ผ่านฟังก์ชันใหม่ `getStockCostGroupsForPond()`
4. ต้นทุนปูน — auto ใหม่ จากสต็อกหมวด lime
5. ต้นทุนค่าแรง — **เปลี่ยนเป็นกรอกเองทั้งหมด** (เดิมดึงอัตโนมัติจาก `getLaborCostForPond`) ตามที่พี่ยืนยัน
6. ต้นทุนค่าไฟฟ้า — auto เหมือนเดิม (2,500 บ./วัน)
7. ต้นทุนน้ำมัน — กรอกเอง (ใหม่)
8. อุปกรณ์ — auto ใหม่ จากสต็อกหมวด equip
9. อุปกรณ์นอก — กรอกเอง (ใหม่)
10. ค่าจับกุ้ง — กรอกเอง (ใหม่)
11-12. % คนเลี้ยง / % ผู้จัดการ — กรอกเอง คำนวณเป็นจำนวนเงินอัตโนมัติ (readonly), **สูตร**: `กำไรก่อนหักส่วนแบ่ง = รายได้ − ต้นทุนตรงทั้ง 10 หมวด` แล้ว `จำนวนเงินแต่ละส่วน = กำไรก่อนหักส่วนแบ่ง × %` สุดท้าย `กำไรสุทธิ = กำไรก่อนหักส่วนแบ่ง − ส่วนแบ่งคนเลี้ยง − ส่วนแบ่งผู้จัดการ` (หลีกเลี่ยงการคำนวณวนซ้ำ — ใช้ "กำไรก่อนหักส่วนแบ่ง" เป็นฐานคูณ % ไม่ใช่กำไรสุทธิสุดท้าย)

**ความเข้ากันได้กับข้อมูลเก่า:** ไม่ลบ `costChem`/`costOther` (หมวดเดิม) ออกจากระบบ — `getCloseSummary()` ยังอ่านรวมเข้า `totalCost` ได้ครบ, แสดงเป็นแถว "(เดิม)" ใน `closeSummaryHtml()`/PDF/ตารางแก้ไขเฉพาะตอนที่รอบนั้นมีค่าอยู่จริง (รอบใหม่ที่ใช้หมวดละเอียดจะไม่เห็นแถวนี้เลย) — ฟอร์ม "แก้ไขข้อมูลปิดบ่อ" (`editCropModal`) ยังคงช่อง "สารเคมี(เดิม)/อื่นๆ(เดิม)" ไว้ท้ายฟอร์มเผื่อต้องแก้รอบเก่าที่เคยกรอกแบบเดิม

**จุดที่แก้ครบทุกจุดที่ใช้ต้นทุนปิดบ่อ:** ฟอร์มปิดรอบ (`cropModal`)+`calcCropEcon()`+`confirmCloseCrop()`, ฟอร์มแก้ไข/เพิ่มข้อมูลเก่า (`editCropModal`)+`openEditCropModal()`+`saveEditCrop()`, `getCloseSummary()`, `closeSummaryHtml()` (พรีวิวหน้าจอ), PDF ปิดบ่อหน้าเดียว (`pdfCostBreakdownHtml()` + กราฟ "กำไร-ต้นทุนเทียบรอบก่อนหน้า" ใน `genPDF()`), หน้า P&L ทั้งหมด (`renderPLKPI`/`renderPLTable`/`renderPLCostChart`/`exportPLCSV`)

**ขอบเขตที่ตั้งใจไม่แตะ:** `pondRunningCost()` (ชิปต้นทุนสะสม realtime หน้าจับกุ้ง/รอบเลี้ยงตอนบ่อยังเปิดอยู่) และ `ceoTotalCost()` (CEO dashboard) ยังใช้สูตรเดิม (สต็อกรวมก้อนเดียว + ค่าแรง auto จาก `D.laborCosts`) เพราะเป็นค่าประมาณช่วงที่บ่อยังไม่ปิด ไม่ใช่ตัวเลขสรุปปิดบ่อที่พี่ขอแยกหมวดรอบนี้

**🔴 บัคเดิมที่เจอระหว่างทดสอบ (มีอยู่ก่อนแล้ว ไม่ได้เกิดจากงานนี้ — ยังไม่ได้แก้ รอพี่ตัดสินใจ):** หน้า P&L (`renderPLKPI`/`renderPLTable`) บวก `costStock` (จาก `getStockCostForPond`, สรุปยอดเบิกพัสดุทั้งหมดของบ่อ) เพิ่มเข้าไปเป็นบรรทัดแยก "พัสดุ(฿)" **ทั้งที่ข้อมูลเดียวกันนี้ถูกนับไปแล้วใน costMed/costLime/costEquip (เดิมคือ costChem)** — ทำให้ต้นทุนรวมในหน้า P&L สูงเกินจริง (นับซ้ำ) เจอจากการทดสอบ: บ่อทดสอบมีสต็อกเบิกจริง 4,900 บาท ต้นทุนตรงรวม (ไม่รวม costStock) คำนวณถูกต้อง 317,508 แต่ตาราง P&L โชว์ 322,408 (317,508+4,900 ซ้ำ) — ปัญหานี้มีมาตั้งแต่ก่อนแยกหมวด (costChem เดิมก็ดึงจากแหล่งเดียวกับ costStock) เป็นปัญหาเชิงโครงสร้างของหน้า P&L ที่ควรตัด costStock ออก หรือเปลี่ยนไปใช้แทนที่ costMed/costLime/costEquip แทนการบวกซ้อน — **ไม่ได้แก้ในรอบนี้เพราะกระทบตัวเลข P&L ย้อนหลังทุกรอบที่เคยปิดไปแล้ว ต้องคุยกับพี่ก่อน**

ทดสอบผ่าน browser จริงครบ end-to-end: เตรียมข้อมูลสต็อกจำลอง 6 หมวด (ยากิน/ยาลงน้ำ/จุลินทรีย์กิน/จุลินทรีย์ลงน้ำ/ปูน/อุปกรณ์) → `getStockCostGroupsForPond` รวมกลุ่มถูกต้อง (เวชภัณฑ์ 2,900=1,000+500+800+600, ปูน 1,000, อุปกรณ์ 1,000) → เปิดฟอร์มปิดรอบ auto-fill ถูกทุกช่อง (ค่าแรง/น้ำมัน/อุปกรณ์นอก/ค่าจับกุ้งว่างตามที่ตั้งใจ) → กรอกครบ+ตั้ง %5/%3 → `calcCropEcon()` คำนวณตรงตามสูตรมือ (กำไรก่อนหักส่วนแบ่ง 220,100 → ส่วนแบ่ง 11,005/6,603 → กำไรสุทธิ 202,492) → กดยืนยันปิดรอบจริง บันทึกลง `harvestResult` ครบทุก field → `getCloseSummary`/`closeSummaryHtml`/PDF/P&L อ่านค่าตรงกันหมด → เปิดฟอร์มแก้ไขรอบที่ปิดแล้ว ค่าที่กรอกไว้ขึ้นครบ แก้ค่าแรงแล้ว recalc ส่วนแบ่ง+กำไรสุทธิถูกต้อง บันทึกแล้วข้อมูลอัปเดตจริง — ไม่มี console error ตลอดการทดสอบ, sync สองไฟล์แล้ว **ยังไม่ push**

### 28. เมนู "เพิ่มเติม" บนมือถือ — เปลี่ยนเป็น bottom sheet + สลับ เตือน↔สต็อก (implement จาก mockup ที่พี่คอนเฟิม)
พี่แจ้งว่าปุ่ม "☰ เพิ่มเติม" บนมือถือใช้งานลำบาก — เดิม toggle ให้ `.tabs-wrap` (คอมโพเนนต์ที่ออกแบบไว้สำหรับ dropdown บนแถบเมนูแนวนอนจอกว้าง — ตรวจพบว่าจริงๆ ถูกบังคับซ่อนตลอดทั้งบนจอกว้างและมือถือปกติ โผล่มาแค่ตอนกด "เพิ่มเติม" เท่านั้น) เด้งลงมาทับจากใต้หัวเรื่อง ต้องกด "หมวด" ก่อนถึงเห็น "รายการย่อย" (2 ชั้น) พื้นที่แคบกดพลาดง่าย — ทำ mockup ก่อน (`Mockup และ Prototype/mockup_เมนูมือถือ_ใหม่.html`) พี่คอนเฟิมดีไซน์ใหม่ + ขอเพิ่ม: สลับปุ่มแถบล่าง "🔔 เตือน" → "📦 สต็อก" (ใช้งานทุกวัน) ย้าย "เตือน" ไปอยู่ในเมนูแทน

**ดีไซน์ใหม่:** bottom sheet เต็มจอเลื่อนขึ้นจากด้านล่าง (`#mobSheet`+`#mobSheetScrim`) แทนที่การ toggle `.tabs-wrap.mob-open` เดิม (ไม่ได้ลบ `.tabs-wrap`/`toggleGrp()` ทิ้ง — ปุ่ม `id="tab-xxx"` ข้างในยังจำเป็นเป็น hidden proxy ที่ `bnavGo()`/`mobMenuGo()` เรียก `.click()` เพื่อ navigate):
- ช่องค้นหากรองเมนูแบบ live (`filterMobMenu()`), ทุกหมวดกางให้เห็นครั้งเดียวไม่ต้องกดซ้อน, ปุ่มใหญ่ขึ้น (min-height 44px), ไฮไลต์หน้าปัจจุบันตอนเปิด (`highlightMobSheetCurrent()`), ปิดได้ 3 ทาง (แตะพื้นหลังมืด/ปุ่ม ✕/เลือกเมนู)
- แถบล่าง: **📦 สต็อก แทน 🔔 เตือน** (`bnavGo('stock',this)`) — "🔔 การแจ้งเตือน" ย้ายไปอยู่บรรทัดบนสุดของ bottom sheet พร้อม badge, ปุ่ม "เพิ่มเติม" เองก็ขึ้น badge แดงด้วย (ย้าย `id="bnav-alert-badge"` มาไว้ในปุ่มนี้แทน ไม่ต้องแก้ `updateAlertBadge()`) กระดิ่ง `.mhd-bell` บนหน้าภาพรวมยังกดได้เหมือนเดิม (เปลี่ยนจากพึ่งพา `#bnav-alerts` ที่ถูกลบไปเป็นเรียก `bnavAlerts(null)` ตรงๆ — แก้ `bnavAlerts()` ให้ `btn` เป็น optional)

**บัคที่เจอระหว่างทำและแก้แล้ว (สำคัญ — เจอจากการทดสอบ ไม่ใช่แค่เขียนโค้ดแล้วจบ):**
1. **CSS comment ไม่ปิด `*/`** — เขียนคอมเมนต์อธิบาย admin-only แบบหลายบรรทัดแล้วลืมปิด ทำให้ browser กลืนกฎ CSS ที่ตามมาทั้งหมดเป็นคอมเมนต์ยาว 1,546 ตัวอักษร รวมกฎ admin-only ใหม่ 3 กฎ **และกฎเดิมที่ไม่เกี่ยวข้องเลยอีกหลายตัว** (`.cat-tag`, `.kpi-card`, `.ov-kpi-bar` ฯลฯ) — เจอจากการ scan `document.styleSheets` จริงในเบราว์เซอร์เทียบจำนวน rule ก่อน/หลังแก้ (542 → 558) ไม่ใช่แค่ syntax-check ด้วย Node (Node เช็ค JS เท่านั้น ไม่เช็ค CSS)
2. **`!important` บนกฎ admin-only ชนกับ inline style ของช่องค้นหา** — ปุ่ม admin-only ใช้ `!important` กันปัญหา specificity เดิม แต่ทำให้ `filterMobMenu()` ที่ตั้ง `el.style.display='none'` ธรรมดาตอนค้นหาไม่ตรง ไม่มีผลกับปุ่ม admin-only (inline style ปกติแพ้ `!important` ของสไตล์ชีทเสมอ) — แก้โดยเปลี่ยนไปใช้ `el.style.setProperty('display','none','important')` (inline `!important` ชนะ stylesheet `!important` ได้)

**ทดสอบ:** เครื่องมือ browser คลิกพิกัด (`computer` tool) ตอบสนองไม่คงที่ในเซสชันนี้ช่วงหนึ่ง (ทดสอบแล้วพบว่าเกิดกับปุ่มเดิมที่ไม่ได้แก้ด้วย เช่น "สต็อก" — สรุปว่าเป็นปัญหาของเครื่องมือทดสอบ ไม่ใช่ตัวแอป ยืนยันด้วย `elementFromPoint` ว่า hit-testing ถูกจุดจริง) จึงสลับไปทดสอบด้วยการยิง `.click()` จริงบน element ที่ render แล้ว (ยังคงเป็น genuine DOM click event ตามมาตรฐานที่ตั้งไว้ ต่างจากการเรียกฟังก์ชันตรงๆ) ครบทุกเส้นทาง: เปิด/ปิด sheet, ค้นหากรอง+ล้างค้นหา คืนสถานะครบ, กดรายการ admin-only แสดง/ซ่อนถูกต้องตามสิทธิ์ (สลับ role staff/admin สด), กดรายการนำทางถูกหน้า+sheet ปิดอัตโนมัติ, ปุ่ม "สต็อก" แถบล่างทำงาน, กระดิ่งเตือน+badge ทำงานครบ 3 จุด (bnav-more/sheet/mhd-bell), ปิดด้วย scrim/ปุ่ม ✕ ได้ทั้งคู่, ที่จอกว้าง (≥601px) sheet ถูกบังคับซ่อนแม้ตั้ง class 'show' เอง (media query กันไว้) — ไม่มี console error, sync สองไฟล์แล้ว **ยังไม่ push**

### 27. PDF ปิดบ่อ — ย่อเหลือหน้าเดียว + กราฟผู้บริหาร + ต้นทุนต่อกิโล (implement จาก mockup ที่พี่คอนเฟิม)
ตามที่พี่ขอ: PDF ปิดบ่อสับสน/ยาวเกิน ต้องการให้เหลือหน้าเดียว + กราฟที่จำเป็นสำหรับผู้บริหาร 2 กราฟ — ทำ mockup ก่อน (`Mockup และ Prototype/mockup_pdf_ปิดบ่อ_หน้าเดียว.html`) พี่เลือกกราฟชุด "กำไร-ต้นทุนเทียบรอบก่อนหน้า + FCR/อัตรารอด" (ของเดิม) แล้วขอเพิ่มให้ตารางต้นทุนต่อกิโลโชว์ **ยอดรวม (บาท) ก่อน แล้วต่อด้วยต่อกิโลในวงเล็บ** ต่อท้าย เช่น "อาหาร 82,000 บาท (20.0 บ./กก. · 34%)"

**ขอบเขต:** หน้าเดียวใช้เฉพาะตอนเลือก "บ่อเดียว" (`ponds.length===1` ใน `genPDF()`) — โหมด "ทุกบ่อ (สรุปรวม)" ยังคงพฤติกรรมเดิมทุกอย่าง (หลายหน้า/หลายบ่อ) เพราะใส่หลายบ่อในหน้าเดียวไม่ไหวจริง — โค้ดเดิมทั้งหมดยังอยู่ครบไม่ได้ลบทิ้ง แค่แตกสาขา if/else ใน `ponds.forEach()`

**เนื้อหาหน้าเดียว (บ่อเดียว):**
- แถบหัวบ่อ + รอบ/วันปิด/อายุ
- **4 KPI card**: ผลผลิตรวม / รายได้รวม / กำไรสุทธิ / Margin (`pdfKpiHtml()`)
- **ข้อมูลผลผลิต+การซื้อขาย 3 คอลัมน์กระชับ** (`pdfProdTradeGridHtml()`) — 9 ฟิลด์เดิม (หัวเชื้อ/จำนวนปล่อย/ไซส์/อัตรารอด/FCR/ADG/Yield/แพ/ราคา) แต่จัดเป็นกริดแทนสไตล์ตารางเดิมที่กินที่เยอะ
- **2 กราฟผู้บริหาร**: กราฟใหม่ "กำไร-ต้นทุน เทียบรอบก่อนหน้า" (`kind:'profit'` — รายได้/ต้นทุน แท่ง + กำไรสุทธิเส้น ของทุกรอบที่ปิดแล้วของบ่อนั้น) วางคู่กับกราฟเดิม "FCR/อัตรารอด เทียบรอบก่อนหน้า" (`kind:'compare'`, เกณฑ์แสดงลดจาก ≥2 รอบเหลือ ≥1 รอบ เพราะขนาดเล็กลงเห็นค่าปัจจุบันได้แม้ไม่มีรอบเทียบ)
- **ต้นทุนแยกตามหมวด — ยอดรวม & ต่อกิโล** (`pdfCostBreakdownHtml()`) — แท่ง progress bar 6 หมวด (อาหาร/ลูกกุ้ง/ค่าแรง/เคมี/ไฟ/อื่นๆ) โชว์ยอดบาทตัวหนา + ต่อกิโล+% ในวงเล็บ ต่อด้วยแถวรวมด้านล่าง ซ่อนทั้งบล็อกถ้ายังไม่มีข้อมูลต้นทุน (`s.hasCostData`)
- หมายเหตุท้ายหน้า

**ตัดออกจากโหมดหน้าเดียว** (ยังอยู่ในโหมด "ทุกบ่อ" เหมือนเดิม): กราฟโต ABW, กราฟอาหารสะสมตลอดรอบ, ตารางรายละเอียดการจับ/พาเชียลแต่ละครั้ง (7 คอลัมน์) — เป็นข้อมูลปฏิบัติการ ไม่ใช่สิ่งที่ผู้บริหารต้องดูตอนสรุปปิดบ่อ (พาเชียลยังดูละเอียดได้ที่หน้าประวัติรอบเลี้ยงในแอปตามข้อ 26)

ทดสอบ: unit-test ฟังก์ชันจริงที่ extract จาก index.html ด้วย Node (ข้อมูลจำลองตรงกับ mockup เป๊ะ) — `getCloseSummary` คำนวณ totalCost/netProfit/marginPct/costPerKg ถูกต้องครบ, `pdfKpiHtml`/`pdfProdTradeGridHtml`/`pdfCostBreakdownHtml` render ค่าถูกต้องตรงกับ mockup (241,900 บาท / 59.0 บาท/กก.), เคสไม่มีข้อมูลต้นทุนคืนค่าว่างถูกต้อง (ซ่อนบล็อกไม่พัง) — จากนั้นทดสอบเพิ่มผ่าน browser จริง (เครื่องมือกลับมาใช้ได้แล้ว): เรียก `genPDF()` จริงกับข้อมูลจำลอง 2 รอบเลี้ยง (ดัก `document.body.appendChild` เพื่อสกัด wrap ก่อนถูกลบ):
- **โหมดเลือกบ่อเดียว**: จบโดยไม่มี error/alert, สร้าง canvas ตรง 2 อัน (`pdf-profit-0`,`pdf-fcr-0` — ไม่ใช่ 3 เหมือนเดิม), มี KPI row/ตารางต้นทุนต่อกิโล/หัวข้อ "กราฟสำหรับผู้บริหาร" ครบ, ไม่มีหัวกราฟเก่า ("การเติบโตของกุ้ง"/"อาหารสะสมตลอดรอบ") และไม่มีตารางรายละเอียดการจับเก่าหลงเหลือ, บรรทัดรวมต้นทุนตรงกับ mockup เป๊ะ ("รวมต้นทุน 241,900 บาท (59.0 บาท/กก.)")
- **โหมด "ทุกบ่อ"**: ยืนยันว่ายังใช้โค้ดเดิมทั้งหมด ไม่ถูกแตะ (ไม่มี KPI-card style ใหม่, ยังมีตารางแบบเก่า, มีมากกว่า 2 canvas ต่อบ่อเหมือนเดิม)
- ไม่มี console error ทั้งสองโหมด, sync `index.html`↔`TawanFarm_App.html` แล้ว — **ยังไม่ได้ push**

### 26. เชื่อม 4 สายข้อมูล ทำงานง่ายขึ้น (implement จาก mockup ที่พี่คอนเฟิม)
ทำตาม `Mockup และ Prototype/mockup_4สายข้อมูล_เชื่อมการทำงาน.html` ครบทุกข้อ — helper กลางใหม่ 2 ตัว: `pondRunningCost(p)` (ต้นทุนสะสม realtime สูตรชุดเดียวกับ auto-fill ตอนปิดรอบ) และ `goTabByName(t)` (สลับแท็บด้วยชื่อ ใช้กับปุ่มทางลัดข้ามหน้า)

**สาย 1 จับกุ้ง→ประวัติ→PDF:** แถบสรุปรอบ (`hv-round-strip`) โผล่ตอนเลือกบ่อในหน้าจับกุ้ง (รอบ/อายุ/พาเชียลสะสม + ต้นทุนสะสม admin-only) · ปิดรอบสำเร็จเปลี่ยนจาก alert เป็น modal `closeDoneModal` มีทางไปต่อ 3 ทาง (ดูประวัติ/สร้าง PDF/เริ่มเตรียมบ่อ — ปุ่มเตรียมบ่อ auto-add บ่อเข้ารายการเตรียมให้เลย) · ปุ่ม "📄 PDF" ต่อท้ายทุกแถวประวัติรอบเลี้ยง → `goPdfForCrop(pondId,idx)` เด้งไปหน้า PDF พร้อม preselect บ่อ+รอบ

**สาย 2 อาหาร→สุ่ม→ต้นทุน:** feed grid เพิ่มแบนเนอร์เตือนบ่อที่ยังไม่กรอกวันนั้น + คอลัมน์ "ต้นทุนอาหารสะสม" (admin-only, จาก `pondFeedCost`) · **ตัดคอลัมน์ "ผู้จำหน่าย"** (ซ้ำกับชื่อสินค้าในช่องสต็อกที่ตัด — ลบ `fdGridUpdateSupplier()` ด้วย) · สุ่มตัวอย่างบันทึกแล้วมีกล่องผลทันที (`sp-result` / `renderSampleResult()`): ABW+PDG, เทียบเป้าหมายตามอายุ (`getAbwTarget`) แปลผล ✓/△/✗ ให้เลย, ทางลัดไปหน้ารายละเอียดบ่อ (FCR ยังโชว์ที่เดียวตามนโยบาย)

**สาย 3 ต้นทุน→ปิดรอบ→P&L:** ตารางรอบเลี้ยงโชว์ 💰 ต้นทุนสะสมใต้ปุ่มปิดรอบ (admin-only, hover เห็น breakdown 5 ก้อน) · P&L เพิ่มคอลัมน์ "ดูรายละเอียดรอบ →" (`goCropHistoryFor`) เด้งไปประวัติรอบเลี้ยงปีนั้น · **เลิกใช้ `cropsPerYear` เดารอบ/ปี** — ฟังก์ชันใหม่ `computeNextCrop(p,baseDate)`: ปี=ปีของวันลง(ค.ศ.), รอบ=จำนวนรอบที่ปิดในปีนั้น(histRecYear)+1 — เรียกทั้งตอนปิดรอบ (ค่าตั้งต้น) และตอนลงลูกกุ้งจริงใน `savePond()` (ค่าจริง)

**สาย 4 เตรียมบ่อ→เริ่มรอบใหม่:** เช็คลิสต์ครบ 100% (และบ่อยัง idle) โชว์ปุ่มใหญ่ "🦐 ลงลูกกุ้ง — เริ่มรอบใหม่บ่อนี้" (`startCropFromPrep`) เปิดฟอร์มบ่อ prefill วันลงจากเช็คลิสต์+เคลียร์ค่ารอบเก่า · `savePond()` เพิ่ม logic `wasIdle` (startDate ว่าง→มีค่า = ลงกุ้งรอบใหม่): คำนวณรอบ/ปีใหม่ + ล้างเช็คลิสต์พร้อม tombstone + เอาออกจาก `_prepPonds` — เส้นทางเดียวใช้ได้ทั้งปุ่มใหม่และการแก้บ่อปกติ · การ์ดเตรียมบ่อบนภาพรวมเพิ่มแถบ progress + วันลงเป้าหมาย + "✅ พร้อมลง" · **แทนปุ่ม × (เอาออกจากรายการ) ด้วย "🗑️ ล้างแผน"** เรียก `resetPrepPond` ตัวเดียวกับบนภาพรวม (ลบ `removePrepPond()` ทิ้ง) — `resetPrepPond` อัปเดตให้เอาบ่อออกจาก `_prepPonds`+re-render หน้าเตรียมบ่อด้วย

ทดสอบครบทุกสายผ่าน real DOM (ปิดรอบจริง→dialog→PDF preselect ถูก, ปุ่ม PDF/ดูรายละเอียดรอบเด้งถูกหน้า+ปีถูก, banner/คอลัมน์ต้นทุน/ไม่มีผู้จำหน่าย, กล่องผลสุ่มแปลผลต่ำกว่าเกณฑ์ถูก, ลงลูกกุ้งจากเช็คลิสต์ 100% end-to-end: prefill→save→รอบ 2/2026+เช็คลิสต์ null+tombstone+หลุดจากรายการเตรียม) — ไม่มี console error, sync สองไฟล์แล้ว **ยังไม่ push**

### 25. Audit ลึกรอบ 2 — บัคซ่อนที่มองไม่เห็นจากการใช้เครื่องเดียว (แก้แล้ว 5 จุด)
พี่สั่งไล่ตรวจ logic ทุกหน้าอีกรอบ เน้นบัคที่คนใช้มองไม่เห็น เจอ+แก้ 5 จุด (ยืนยันทุกจุดด้วยเทสต์จำลอง 2 เครื่องใน browser):

1. **[ร้ายแรง] ข้อมูลรอบเก่าฟื้นคืนหลังปิดรอบ (merge resurrect)** — `confirmCloseCrop()` เคลียร์ `p.feedLog=[]` ฯลฯ แต่ `save()` ทำ merge แบบ **union** กับ remote เสมอ (`mergeArr`) → ข้อมูลรอบเก่าจาก Firebase ถูกดันกลับเข้า array สดของรอบใหม่ทันทีที่ save ครั้งแรก (ทั้ง 7 array: feedLog/samples/plankton/waterLog/chemLog/labResults/partialHarvests) ทำให้ FCR/ต้นทุน/กราฟรอบใหม่ปนข้อมูลรอบเก่า — **นี่คือต้นตอใหญ่ของอาการ "ข้อมูลหลายชุด สับสน"** — แก้โดย push id ของทุก log ที่ archive เข้า `D.deletedIds` (tombstone) ตอนปิดรอบ ก่อนเคลียร์ — id มีครบทุก record อยู่แล้ว (normalizeD backfill deterministic id ให้ของเก่า) และ `dropDead` กรองเฉพาะ item ชั้นบนสุด สำเนาที่ archive ไว้ใน `cropHistory[].feedLog` จึงไม่โดนกรอง อยู่ครบ
2. **[ร้ายแรง] เช็คลิสต์เตรียมบ่อฟื้นคืนหลังล้าง** — `resetPrepPond()`/ปิดรอบ ตั้ง `prepChecklist=null` แต่ `mergeChecklist(null, remote)` คืนค่า remote → เช็คลิสต์เดิมกลับมาหลัง sync (ปุ่ม 🗑️ ที่เพิ่งทำในข้อ 21-22 ใช้กับ Firebase จริงไม่ได้ผลถาวร!) — แก้โดย: เช็คลิสต์มี `id` ตั้งแต่สร้าง (`prep_<pid>_<ts>`), ของเก่า normalizeD backfill เป็น `prep_<pondId>` (deterministic เท่ากันทุกเครื่อง), ตอนล้างแผน/ปิดรอบ push id เข้า deletedIds, `mergeRemote` เช็ค tombstone ก่อน merge เช็คลิสต์ (ฝั่งไหนตายแล้วห้ามฟื้น), `mergeChecklist` คง id ไว้ในผลลัพธ์เสมอ
3. **[กลาง] สถิติจับกุ้ง (สรุปจากประวัติเลี้ยง) นับปีผิด** — `hsCropStats` เทียบ `(c.cropYear||0)-2500` ตรงๆ แต่ cropYear ในข้อมูลจริงมีทั้ง พ.ศ. (seed เก่า: 2569) และ ค.ศ. (รอบที่ปิดผ่านแอป: 2026 → 2026-2500=-474) → **รอบที่ปิดผ่านแอปจริงหายจากสรุปทั้งหมด** — แก้เป็น `histRecYear(c)` (ยึดปีจาก closeDate, normalize พ.ศ. ให้อัตโนมัติ)
4. **[กลาง] ตาราง "รายละเอียดการจับแต่ละครั้ง" ใน PDF พัง** (regression จากข้อ 24) — `getCloseSummary().rows` ใหม่ไม่มี `type`/`date` ในแถวปิดบ่อ และไม่มี `type` ในแถวพาเชียล → badge ประเภทเป็น "-" หมด และแถวปิดบ่อเรียงผิดตำแหน่ง — แก้โดยติด `type:'พาเชียล'`/`type:'ปิดบ่อ',date:closeDate` ให้ครบตอนประกอบ rows
5. **[เล็ก] คำเตือนปิดรอบซ้ำเช็คผิดเงื่อนไข** — เดิมเทียบแค่ `cropNum` (รอบ 1 ปีเก่า ชนกับรอบ 1 ปีนี้ = เตือนมั่ว) และเงื่อนไข `closeDate!==closeDate` ทำให้ปิดวันเดียวกันซ้ำ 2 ครั้งไม่เตือนเลย — แก้เป็นเทียบ cropNum + ปี (ผ่าน yearBE ทั้งคู่)

**ข้อสังเกตที่จดไว้ ไม่แก้รอบนี้ (design-level รอเฟส mockup):** (ก) `cropsPerYear` ใน settings ยังถูกใช้คำนวณ rollover รอบ/ปีตอนปิดรอบ ทั้งที่ UI ตั้งค่าถูกถอดออกไปแล้ว (ค่าคงที่ 2 ตลอด) — ควรเปลี่ยนเป็นนับจากรอบจริงในปีนั้นตอนลงลูกกุ้งใหม่ (ข) `item.qty` สต็อกเป็น counter ที่เขียนทับกันข้ามเครื่อง (สองเครื่องเบิกพร้อมกัน = ยอดหาย) ทางแก้จริงคือคำนวณ qty จาก inventoryLog (ค) `parchan` เป็น field ตายใน seed data ไม่มีใครอ่าน (ง) ราคา/ต้นทุนสต็อกย้อนหลังใช้ `unitPrice` ปัจจุบัน ไม่ใช่ราคา ณ วันเบิก

ทดสอบ: จำลอง `mergeRemote` 2 เครื่อง 7 เคส (ปิดรอบแล้ว merge กับ remote เก่า / ล้างแผนแล้ว sync ย้อน / เช็คลิสต์ใหม่หลังล้างต้องรอด) ผ่านครบ, ปิดรอบ end-to-end ผ่านปุ่มจริง (archive ครบ + tombstone ครบ + เคลียร์ครบ + รอบถัดไป 2/2569 ถูก), ปุ่มล้างแผนกดจริงได้ tombstone, hsCropStats นับปีถูกทั้ง พ.ศ./ค.ศ., rows ใน PDF มี type/date ครบ — ไม่มี console error, sync สองไฟล์แล้ว **ยังไม่ push**

### 24. รวมระบบ "จับกุ้ง/ปิดบ่อ/ประวัติ" ให้เหลือชุดข้อมูลเดียว หน้าเดียว (รีดีไซน์ใหญ่ — แก้แล้ว)
พี่แจ้งว่าระบบจับกุ้ง/ปิดบ่อ/เก็บประวัติตอนนี้สับสน มีข้อมูลหลายชุดซ้อนกัน (`D.harvestLog` vs `p.cropHistory` vs การกดยืนยันคนละจุด) ขอ **ไม่ลบข้อมูลเดิม แค่จัดระบบ/หน้าใหม่** ให้เหลือ **หน้าเดียวสำหรับกดยืนยันการจับกุ้งทุกแบบ** และ **หน้าเดียวสำหรับดูประวัติ** (คุยรายละเอียด 3 จุดกับพี่ก่อนลงมือ: ไม่ลบข้อมูล / ใช้ `cropHistory` เป็นแหล่งความจริงเดียว / รวม "พาเชียล" กับ "ปิดรอบ" เป็นฟอร์มเดียว — พี่ยืนยัน "คอนเฟิม" ทั้ง 3 ข้อ)

**โครงสร้างใหม่:**
- **หน้า "จับกุ้ง" (`sec-harvest`) เขียนใหม่ทั้งหมด** เหลือฟอร์มเดียว: เลือกบ่อ → เลือกโหมด (พาเชียล/ปิดรอบ) → ฟิลด์ตามโหมด
  - **โหมด "พาเชียล"** (จับบางส่วน บ่อยังเลี้ยงต่อ) — ฟิลด์เดิมทั้งหมด (วันที่/แพ/ลักษณะ/ไซส์/น้ำหนัก/ราคา/หมายเหตุ) แต่บันทึกลง **`p.partialHarvests[]`** (field ใหม่ผูกกับบ่อนั้นโดยตรง แทน `D.harvestLog` ที่เป็น array แยกลอยๆ) — มีตารางรายการพาเชียลของรอบปัจจุบันโชว์ใต้ฟอร์มพร้อมลบได้
  - **โหมด "ปิดรอบ"** — ไม่มีฟอร์มซ้ำอีกต่อไป แค่ปุ่ม "เปิดฟอร์มปิดรอบเต็มรูปแบบ" เรียก `openCropModal(pondId)` เดิม (preselect บ่อที่เลือกไว้) — ใช้ modal เดิมที่คำนวณต้นทุน/FCR/กำไรครบอยู่แล้ว ไม่สร้างซ้ำ
  - ลบโค้ด/UI ที่ผูกกับ `D.harvestLog` ออกจากหน้านี้ทั้งหมด: `saveHarvest()`, `deleteHarvest()`, `renderHarvest()`, ตาราง "ประวัติการจับกุ้ง", dropdown ปีกับบ่อ (`hv-filter-year`/`hv-filter-pond`), ปุ่มซิงค์ `syncHarvestLogFromCropHistory()` (ข้อ 19 — เลิกใช้เพราะ retire `D.harvestLog` ไปเลย ไม่ต้องซิงค์อีกต่อไป)
  - `D.harvestLog` **ไม่ได้ลบออกจาก database** — โค้ด normalize/merge (`LOG_KEYS` ฯลฯ) ยังคงอยู่เผื่อข้อมูลเก่ามีอยู่ในระบบ แค่ไม่มี UI ไหนอ่าน/เขียนมันแล้ว
- **`confirmCloseCrop()`** (ปิดรอบจริงจากการ์ด "รอบเลี้ยง") เก็บ `p.partialHarvests` เข้าไปใน `p.cropHistory[].partialHarvests` ตอน archive แล้วเคลียร์ `p.partialHarvests=[]` เพื่อเริ่มรอบใหม่
- **หน้า "รอบเลี้ยง" → แท็บ "ประวัติรอบเลี้ยง (ปิดแล้ว)"** เป็นหน้าประวัติหน้าเดียวที่รวมทุกอย่าง — เพิ่มคอลัมน์ "พาเชียล" โชว์ปุ่ม "📦 N" (N = จำนวนรายการพาเชียลของรอบนั้น) กดแล้ว expand แถวย่อยด้านล่างโชว์ตารางรายละเอียดพาเชียลทั้งหมดของรอบนั้น (`togglePartialRow()`)
- `getCloseSummary()` (ใช้โดยหน้า PDF ปิดบ่อ) เปลี่ยนจากอ่าน `D.harvestLog` มาอ่าน `partialHarvests` (ของ `lastCrop` ถ้าเป็นรอบที่ปิดแล้ว หรือของ `p` ถ้ายังเลี้ยงอยู่) รวมกับ `harvestResult` ของรอบปิด — สรุปยอดขาย/ราคาเฉลี่ยยังถูกต้องเหมือนเดิมแค่เปลี่ยนแหล่งข้อมูล

**เพิ่ม field ใหม่ `partialHarvests`** ให้ครบทุกจุดที่มี array ประจำบ่อ (normalize/DEFAULT seed/`LOG_KEYS`/JSON import/`openAddPond`/`newCrop`) ตามแพทเทิร์นเดียวกับ `feedLog`/`waterLog` เดิม

ทดสอบผ่าน local server ด้วย real DOM click/onchange (ไม่เรียกฟังก์ชันตรงๆ): เลือกบ่อ→สลับโหมด พาเชียล/ปิดรอบ ฟิลด์ซ่อน/โชว์ถูกต้อง, กรอกพาเชียลแล้วกดบันทึกจริง (500กก.×130=65,000 ตรง) ขึ้นในตารางถูกต้อง, กดปุ่ม "เปิดฟอร์มปิดรอบ" เปิด modal เดิมพร้อม preselect บ่อถูกต้อง, ทดสอบข้อมูลจำลองที่มี `partialHarvests` ในรอบปิดแล้ว → ปุ่ม "📦" ขึ้นจำนวนถูกต้องและ expand โชว์รายละเอียดได้จริง — ไม่มี console error, sync `index.html`↔`TawanFarm_App.html` แล้ว **ยังไม่ได้ push** รอพี่ยืนยัน

### 23. รอบเลี้ยง — เพิ่มปุ่ม "➕ เพิ่มข้อมูล" สำหรับกรอกรอบเก่าที่ตกหล่น (เพิ่มใหม่)
เพิ่มปุ่ม "➕ เพิ่มข้อมูล" (admin-only) ข้าง dropdown เลือกปีในการ์ด "📚 ประวัติรอบเลี้ยง (ปิดแล้ว)" — ใช้เมื่อบางบ่อมีรอบเลี้ยงเก่าที่ไม่เคยบันทึกไว้ในระบบ (ตกหล่นจากการปิดรอบจริง หรือย้ายมาจากระบบเก่า)

**สถาปัตยกรรม:** ใช้ซ้ำ modal เดิม (`editCropModal`) ที่มีอยู่แล้วสำหรับ "แก้ไขข้อมูลปิดบ่อ" — เพิ่มบล็อก `#ecm-new-fields` (เลือกบ่อ + รอบที่ + ปี พ.ศ.) ที่โชว์เฉพาะตอนกด "เพิ่มข้อมูล" (ซ่อนตอนแก้ไขของเดิม) ฟังก์ชันใหม่ `openAddCropModal()` เคลียร์ฟอร์มว่างเปล่าทั้งหมด + populate dropdown เลือกบ่อ, ส่วน `saveEditCrop()` แยกเป็น 2 branch ด้วย sentinel `idx===-1` (ตั้งค่าใน `openAddCropModal()`):
- **โหมดเพิ่ม** (`idx===-1`): validate ต้องเลือกบ่อ+กรอกรอบที่+ปีครบ, เตือนซ้ำถ้าบ่อนั้นมีรอบที่+ปีเดียวกันอยู่แล้ว (ยังยืนยันเพิ่มซ้ำได้ถ้าต้องการ), สร้าง object รอบใหม่ทั้งชุด (records/feedLog/samples ฯลฯ เป็น array ว่างเพราะไม่มีข้อมูลรายวันของรอบเก่า) push เข้า `p.cropHistory`
- **โหมดแก้ไข** (idx ปกติ): พฤติกรรมเดิมทุกอย่าง ไม่เปลี่ยน

ทดสอบผ่าน browser ครบ: เปิด modal เพิ่มข้อมูล → title/pond-dropdown/new-fields ถูกต้อง, validate 3 จุด (ไม่เลือกบ่อ/ไม่กรอกรอบ/ไม่กรอกปี) เตือนถูกต้อง, บันทึกสำเร็จแล้วเจอใน `p.cropHistory` (cropYear แปลง พ.ศ.→ค.ศ. ถูกต้อง) และโชว์ในตาราง "ประวัติรอบเลี้ยง (ปิดแล้ว)" จริง, ทดสอบ regression โหมดแก้ไขเดิมยังทำงานปกติ (title/new-fields ซ่อนถูกต้อง, บันทึกทับของเดิมได้) ไม่มี console error

### 22. ปุ่มล้างแผนเตรียมบ่อ — แก้จุดสัมผัสเล็กเกินไป กดบนมือถือไม่ติด (แก้แล้ว)
พี่แจ้งว่ากดปุ่ม 🗑️ บนการ์ดเตรียมบ่อ (หน้าภาพรวม) แล้ว "ไม่มีอะไรเกิดขึ้นเลย" — ตรวจสอบด้วยการยิง `.click()` จริงบนปุ่ม (ไม่ใช่เรียกฟังก์ชันตรงๆ ซึ่งไม่เจอบั๊กแบบนี้) พบว่าโค้ดทำงานถูกต้อง 100% แต่**จุดสัมผัสจริงเล็กมากแค่ 17×13px** (เพราะตั้ง `padding:0;line-height:1` ตอนสร้างข้อ 21 ไม่ได้คิดเผื่อขนาดนิ้วสัมผัสจริงบนมือถือ) เล็กกว่ามาตรฐานจุดสัมผัสมือถือ (~44px) มาก ทำให้กดพลาดบ่อยหรือกดไม่ติดเลย

**แก้แล้ว:** เพิ่ม `padding:6px;margin:-6px` (ขยายพื้นที่กดโดยไม่ดันเลย์เอาต์รอบข้าง) + `min-width:28px;min-height:28px;display:inline-flex` จัดไอคอนกึ่งกลาง จุดสัมผัสจริงตอนนี้ ~31×28px

ทดสอบยืนยันด้วย `getBoundingClientRect()` ก่อน/หลังแก้ (17×13 → 31×28) และยิง `.click()` จริงอีกครั้งว่ายังล้าง `prepChecklist` ได้ถูกต้อง ไม่มี console error — **บทเรียนสำคัญ:** ต่อไปนี้เวลาเพิ่มปุ่มเล็กๆ (icon-only) ต้องเช็คขนาดจุดสัมผัสจริงด้วย ไม่ใช่แค่ทดสอบว่าฟังก์ชันทำงานถูกหรือเปล่า

### 21. ภาพรวม: ปุ่ม "เปิดบ่อใหม่" + ล้างแผนเตรียมบ่อ, จัดการบ่อ: ตัดตั้งค่ารอบเลี้ยงต่อปี (เพิ่มใหม่/แก้แล้ว)
- เพิ่มปุ่ม "🦐 เปิดบ่อใหม่" (admin-only) ในแถบ `.home-quick` หน้าภาพรวม เรียก `openAddPond()` เดียวกับปุ่ม "+ เพิ่มบ่อใหม่" ที่มีอยู่แล้วในหน้า "จัดการบ่อ" — สะดวกขึ้นไม่ต้องสลับหน้าไปจัดการบ่อก่อน
- เพิ่มปุ่ม 🗑️ บนการ์ดบ่อสถานะ "เตรียมบ่อ" (หน้าภาพรวม, admin-only) → `resetPrepPond(pid)` ล้าง `p.prepChecklist` กลับเป็น `null` **ไม่ลบบ่อออกจาก `D.ponds`** — ใช้เมื่อเปลี่ยนแผนการเลี้ยง บ่อยังอยู่ในกลุ่ม "เตรียมบ่อ" พร้อมวางแผนใหม่ (ตามที่พี่ยืนยัน — ไม่ใช่การลบบ่อทั้งระบบ)
- ลบการ์ดตั้งค่า "จำนวนรอบเลี้ยงต่อปี" ออกจากหน้า "จัดการบ่อ" ตามที่พี่บอกว่าไม่จำเป็น พร้อมลบฟังก์ชัน `updateCropsPerYear()` และการ populate ค่าใน `populateAll()` ที่กลายเป็น dead code (ค่า `D.settings.cropsPerYear` เดิมยังใช้ในลอจิกขึ้นรอบใหม่ตอนปิดรอบอยู่ แค่ไม่มี UI ให้เปลี่ยนค่าแล้ว ค้างที่ค่าเดิม/default 2)

**บั๊ก 2 จุดที่เจอระหว่างทำ (แก้แล้ว):**
1. ปุ่ม "เปิดบ่อใหม่" ตอนแรกใช้แค่ `class="admin-only"` — แต่ `.home-quick button{display:flex}` (specificity class+type = 0,0,1,1) **สูงกว่า** `.admin-only{display:none}` เฉยๆ (0,0,1,0) ทำให้ปุ่มโชว์ทุก role ไม่ใช่แค่ admin ทั้งที่ยังไม่ทันแก้ role อื่น — แก้โดยเพิ่ม `.home-quick .admin-only{display:none!important}` + คงกฎเดิม `body.role-admin .home-quick .admin-only{display:flex!important}` ไว้คู่กัน (specificity สูงกว่าเลยชนะตอน role=admin)
2. หลังแก้ข้อ 1 พบอีกจุด: `body.role-admin .admin-only{display:revert!important}` (กฎเดิมของทั้งระบบ) รีเซ็ตกลับเป็นค่า default ของ browser (`inline-block` สำหรับ `<button>`) ไม่ใช่ `display:flex` ที่ปุ่มนี้ต้องการ ทำให้ layout ไอคอน+label เพี้ยนตอนโชว์ให้ admin — ต้องระบุทับเฉพาะจุดนี้ด้วย `body.role-admin .home-quick .admin-only{display:flex!important}`

ทดสอบผ่าน browser ครบ: role staff → ปุ่มทั้ง 2 จุด (เปิดบ่อใหม่ + ล้างแผนเตรียมบ่อ) ซ่อนถูกต้อง (`display:none`), role admin → โชว์ถูกต้องด้วย display ที่ถูกต้อง (`flex`/`block` ตามจุด), `resetPrepPond()` ล้าง prepChecklist เป็น null จริง ไม่กระทบ D.ponds, ไม่มี console error

### 20. เพิ่ม dropdown เลือกปี "ประวัติการจับกุ้ง" (เพิ่มใหม่)
เพิ่ม `<select id="hv-filter-year">` ข้าง dropdown เลือกบ่อ — ค่าเริ่มต้นคือปีปัจจุบัน (พ.ศ.) ไม่โชว์ทุกปีรวมกันตามที่พี่ขอ ("เยอะเกินไป") มีตัวเลือก "ทุกปี" ให้เลือกดูรวมได้ถ้าต้องการ ตัวเลือกปีสร้างจากปีที่มีข้อมูลจริงใน `D.harvestLog` (บวกปีปัจจุบันเสมอแม้ยังไม่มีข้อมูล)

**บั๊กที่เจอระหว่างทำ (แก้แล้ว):** รอบแรกวาง logic populate dropdown ไว้ใน `populateAll()` เหมือน `hv-filter-pond` — พอทดสอบพบว่ากดปุ่ม "ซิงค์จากรอบเลี้ยง" (ข้อ 19) แล้ว dropdown ปีไม่อัปเดตทันที เพราะปุ่มนั้นเรียกแค่ `renderHarvest()` ไม่เรียก `populateAll()` — ย้าย logic มาไว้ใน `renderHarvest()` เอง คำนวณรายการปีใหม่ทุกครั้งที่ render แทน

**บั๊กที่สอง (แก้แล้ว):** ตอนคง state ค่าที่เลือกไว้ข้าม render ใช้ `hvYearEl.value=cur||String(curYr)` — แต่ `""` (ค่า "ทุกปี" ที่ผู้ใช้ตั้งใจเลือก) เป็น falsy ทำให้โดนตีความว่า "ยังไม่ได้ตั้งค่า" แล้วดีดกลับไปปีปัจจุบันทุกครั้งที่ render ใหม่ (แก้ไม่ได้จริง) — แก้โดยเช็ค `hvYearEl.options.length===0` (ยังไม่เคย populate) ถึงจะ default เป็นปีปัจจุบัน ไม่งั้นคงค่าที่ผู้ใช้เลือกไว้เป๊ะๆ รวมถึง "ทุกปี" ด้วย

ทดสอบผ่าน browser: dropdown มีตัวเลือกครบตามปีที่มีข้อมูลจริง, ค่าเริ่มต้นกรองเฉพาะปีปัจจุบัน (1 แถว), เลือก "ทุกปี" แล้ว render ซ้ำหลายรอบยังคงโชว่ครบ (2 แถว) ไม่ดีดกลับ ไม่มี console error

### 19. ปุ่มซิงค์ "ประวัติการจับกุ้ง" จาก "ประวัติรอบเลี้ยง (ปิดแล้ว)" (เพิ่มใหม่ — ทดลองเฉพาะปี 2569)
เพิ่มฟังก์ชัน `syncHarvestLogFromCropHistory(targetYearBE)` + ปุ่ม "🔄 ซิงค์จากรอบเลี้ยง (ปี 2569)" (admin-only) ในหัวการ์ด "ประวัติการจับกุ้ง" — วนทุกบ่อ ทุก `p.cropHistory` ที่มี `harvestResult` และ `closeDate` ตรงปี 2569 (เทียบด้วย `histRecYear()` ตัวเดียวกับที่หน้ารอบเลี้ยงใช้กรองปี) แล้วสร้างแถวใหม่ใน `D.harvestLog` type "ปิดบ่อ" ให้ครบ (แก้ต้นตอข้อ 17 ที่ `confirmCloseCrop()` ไม่เคย push เข้า harvestLog เอง)

- กันซ้ำด้วย key `pondId+closeDate` — รันซ้ำกี่ครั้งก็ไม่เพิ่มรายการซ้ำ (ทดสอบแล้ว: รันรอบสองได้ "เพิ่มใหม่ 0 ข้าม 1" ตามคาด)
- รายการที่ซิงค์มาจะมีหมายเหตุต่อท้าย "(ซิงค์จากประวัติรอบเลี้ยง)" ให้แยกออกจากที่พนักงานพิมพ์เองได้ ลบทิ้งเป็นรายตัวได้ปกติถ้าไม่ต้องการ (ปุ่ม 🗑️ เดิม)
- **จำกัดแค่ปี 2569 ตามที่ตกลงว่าจะทดลองก่อน** — ถ้าดูผลลัพธ์แล้วโอเค ค่อยขยายเป็นเลือกปีได้เอง/ซิงค์ทุกปีทีหลัง

ทดสอบผ่าน browser + mock cropHistory: ปิดปี 2569 (closeDate 2026-03-15) ถูกซิงค์เข้ามาน้ำหนัก/ไซส์/ราคาตรงกับ harvestResult, ปิดปี 2568 (closeDate 2025-11-20) ไม่ถูกดึงมา (กรองปีถูกต้อง), รันซ้ำไม่ซ้ำข้อมูล ไม่มี console error

### 18. หน้า "จับกุ้ง" — เอาตัวเลือก "ปิดบ่อ" กลับมา แบบไม่ผูกกับการปิดรอบจริง (แก้แล้ว)
พี่ต้องการเก็บประวัติทั้ง "ปิดบ่อ" และ "พาเชียล" ไว้ในตารางเดียวกัน (ข้อ 16 ตัดออกไปเพราะตอนนั้น "ปิดบ่อ" ผูกกับการเปิด `openCropModal` ให้กรอกซ้ำ) — เพิ่ม `<option value="ปิดบ่อ">` กลับเข้า dropdown "รูปแบบการจับ" แต่**ไม่เอา confirm/`openCropModal` bridge กลับมา** เลือก "ปิดบ่อ" แล้วกด "บันทึก" ตอนนี้แค่เก็บ record ลง `D.harvestLog` เฉยๆ (วันที่/บ่อ/แพ/ไซส์/น้ำหนัก/ราคา/หมายเหตุ — ไม่มีช่องต้นทุนอยู่แล้วในฟอร์มนี้ ไม่ต้องตัดอะไรเพิ่ม) ไม่เปิดหน้าไหนต่อ ไม่ปิดบ่อจริง บ่อยังเลี้ยงต่อได้ปกติ

**การปิดรอบจริง (มีต้นทุน/FCR/archive เข้า `cropHistory`) ยังคงทำได้ทางเดียวคือปุ่ม "ปิดรอบ" ในการ์ด "รอบเลี้ยง" เท่านั้น** ตรงตามที่ตกลงกันไว้ในข้อ 16 — สองระบบนี้แยกกันเด็ดขาดแล้ว: หน้าจับกุ้ง = บันทึกเหตุการณ์จับกุ้ง (ทั้งบางส่วนและทั้งหมด) เป็นประวัติ, รอบเลี้ยง = ปิดรอบเลี้ยงจริงพร้อมคำนวณต้นทุน/กำไร

ทดสอบผ่าน browser: dropdown มี 3 ตัวเลือก (ว่าง/ปิดบ่อ/พาเชียล), เลือก "ปิดบ่อ" บันทึกสำเร็จ ไม่เปิด cropModal ไม่ปิดบ่อจริง (`p.startDate` ยังอยู่) ไม่มี console error

### 17. หน้า "จับกุ้ง" — ลบการ์ด "สรุปการจับกุ้ง" ที่ซ้ำกับหน้าสถิติจับกุ้ง (แก้แล้ว)
ลบการ์ด "📊 สรุปการจับกุ้ง" (มี 2 โหมด: สรุปรวมทุกบ่อจาก `D.harvestLog`, กับสรุปรายบ่อจาก `getCloseSummary()` ที่ซ้ำกับข้อมูลในหน้า "สถิติจับกุ้ง"/PDF ปิดบ่ออยู่แล้ว) — ลบ dropdown `hv-sum-pond`, ฟังก์ชัน `renderPondCloseSummary()` (ใช้ที่เดียว ไม่มีที่อื่นเรียก จึงลบทิ้งได้ทั้งฟังก์ชัน), และ populate-dropdown ที่เกี่ยวข้องออกหมด — เหลือแค่ฟอร์มบันทึก + ตารางประวัติในหน้านี้

**ตรวจเจอเพิ่มเติม (ยังไม่ได้แก้ รอพี่ตัดสินใจ):** "ประวัติการจับกุ้ง" (`D.harvestLog`) ไม่เคยเป็น log อัตโนมัติของการปิดบ่อทุกครั้ง — `confirmCloseCrop()` (ปุ่มปิดรอบจากการ์ดรอบเลี้ยง) เขียนข้อมูลลง `p.cropHistory[].harvestResult` เท่านั้น **ไม่เคย push เข้า `D.harvestLog` เลย** ตารางนี้จะมีแค่รายการที่พนักงานพิมพ์กรอกเองผ่านฟอร์มนี้โดยตรง (ก่อนหน้านี้คือตอนเลือก "ปิดบ่อ" ซึ่งเพิ่งถูกตัดออกในข้อ 16, ต่อไปนี้จะเหลือแต่ "พาเชียล") ทำให้ "ดูเหมือนข้อมูลไม่ครบจากการปิดบ่อทั้งหมด" ตามที่พี่สังเกตเจอ — ไม่ใช่บั๊ก แต่เป็นเพราะตารางนี้ไม่เคยออกแบบมาให้เป็นบันทึกครบทุกการปิดบ่อ ข้อมูลปิดบ่อที่ครบจริงอยู่ใน "รอบเลี้ยง"/"สถิติจับกุ้ง"/PDF ปิดบ่อแทน

### 16. หน้า "จับกุ้ง" — ตัดตัวเลือก "ปิดบ่อ" ที่ซ้ำซ้อนกับรอบเลี้ยง (แก้แล้ว)
เดิม dropdown "รูปแบบการจับ" มี 2 ตัวเลือก: พาเชียล (จับบางส่วน บ่อยังเลี้ยงต่อ — ข้อมูลเฉพาะตัว ไม่ซ้ำที่ไหน) กับ ปิดบ่อ (กรอกน้ำหนัก/ไซส์/ราคา/วันที่ในฟอร์มนี้ แล้ว `saveHarvest()` จะถามซ้ำให้เปิด `openCropModal` จากการ์ดรอบเลี้ยงเพื่อกรอกชุดเดียวกันอีกรอบ) — ถ้ากด "ยกเลิก" ตอนถาม จะเหลือ record type "ปิดบ่อ" ค้างอยู่ทั้งที่บ่อยังไม่ปิดจริง ข้อมูลไม่ตรงสถานะ

**แก้แล้ว:** ลบ `<option value="ปิดบ่อ">` ออกจาก dropdown เหลือแค่ "พาเชียล" และลบโค้ด confirm/`openCropModal` bridge ใน `saveHarvest()` ออก (dead code เพราะเลือก "ปิดบ่อ" จากฟอร์มไม่ได้แล้ว) — ปิดรอบทำได้ทางเดียวคือปุ่ม "ปิดรอบ" ในการ์ด "รอบเลี้ยง" เท่านั้น

ข้อมูลเก่าที่เคยบันทึกด้วย type "ปิดบ่อ" ไม่ได้ลบทิ้ง — โค้ดแสดงผล/รายงานที่อ่านค่านี้ (สรุปการจับกุ้ง, สถิติจับกุ้ง) ยังคงแสดงได้ปกติ แค่กรอกใหม่เลือกไม่ได้แล้ว

ทดสอบผ่าน browser: dropdown เหลือ 2 ตัวเลือก (ว่าง/พาเชียล), บันทึกพาเชียลสำเร็จปกติ ไม่มี console error

### 15. ต้นทุนค่าไฟรายบ่อ — เปลี่ยนสูตรเป็น 2,500 บาท/วัน×วันเลี้ยง (แก้แล้ว)
**ไฟล์:** `getElecCostForPeriod()` (index.html/TawanFarm_App.html) — จุดใช้: หน้าปิดรอบ (`cm-cost-elec`) + CEO dashboard cost breakdown

เดิมคำนวณจากบิลไฟจริง (`D.electricityBills`) หารเฉลี่ยตามจำนวนบ่อ — เพี้ยนเพราะมีเดือน/วันที่ไม่ได้เลี้ยงกุ้งปนอยู่ในค่าเฉลี่ยรายปีด้วย เปลี่ยนเป็นสูตรคร่าวๆ คงที่: **2,500 บาท/วัน × จำนวนวันที่เลี้ยง** (`ELEC_RATE_PER_DAY=2500`, `dDiff(fromDate,toDate)`) ไม่พึ่งบิลจริงอีกต่อไป

- ลบปุ่ม "💡 ประมาณ" (เดิมอิงค่าเฉลี่ย 19.37 บาท/กก., `estimateElecFromBenchmark()`) ออกจากทั้งโมดัลปิดรอบและแก้ไขรอบที่ปิดแล้ว เพราะช่องหลักดึงอัตโนมัติจากสูตรใหม่อยู่แล้วตลอดเวลา ปุ่มสำรองเดิมเลยฟุ่มเฟือย/อาจทำให้ตัวเลขไม่ตรงกันถ้ากดทับ
- **หน้า "ค่าไฟฟ้า" (กรอกบิลจริงรายเดือน, `D.electricityBills`) ไม่แตะเลยตามที่พี่แจ้ง** — ยังกรอก/ดูประวัติได้ปกติ แค่ไม่ถูกใช้คำนวณต้นทุนต่อบ่อจุดนี้แล้ว เผื่อเอาไว้เทียบ/หาความเชื่อมโยงกันในอนาคต

ทดสอบผ่าน browser: บ่ออายุ 106 วัน → 265,000 บาท ตรงสูตรเป๊ะ, auto-fill ในหน้าปิดรอบถูกต้อง, หน้าค่าไฟฟ้ายังทำงานปกติ ไม่มี console error

### 14. CEO Dashboard — responsive สำหรับ iPad (แก้แล้ว)
เดิม `body.role-ceo .main{max-width:640px}` คงที่ไม่ว่าจอกว้างแค่ไหน ทำให้บน iPad การ์ดบ่อเรียงแถวเดียวยาวๆ เหมือนมือถือ เพิ่ม media query 2 breakpoint (≥700px, ≥1000px) ขยาย `.main`, ปรับ `.ceo-kpi-grid` เป็น 4/5 คอลัมน์, `.ceo-pond-list` เป็น grid 2/3 คอลัมน์, และทำ `.ceo-sheet` เป็นกล่อง 560px กึ่งกลางจอ (ไม่เต็มความกว้างแบบ bottom sheet มือถือ) แทนที่จะขยายเต็มจอเหมือนโทรศัพท์

**บั๊กระหว่างทำ:** วาง media query block ไว้ก่อน base rule (`.ceo-kpi-grid{...}`,`.ceo-sheet{...}`) ในไฟล์ ทำให้ base rule ที่มาทีหลังชนะ cascade แม้ specificity เท่ากัน (ไม่มีผลอะไรเลยตอนทดสอบครั้งแรก) — แก้โดยย้าย media query block ไปอยู่ท้ายสุดของ CEO CSS section แทน ทดสอบยืนยันด้วย `getComputedStyle` ที่ viewport 820px (kpi 4 คอลัมน์, pond list 2 คอลัมน์, sheet กว้าง 560px กึ่งกลาง) และ 390px (กลับเป็นค่าเดิมของมือถือครบ) ไม่มี console error

### 13. CEO Dashboard — แก้ 5 จุดหลังใช้งานจริง (แก้แล้ว)
- **อาหารรวมวันนี้** ไม่ว่างเปล่าแล้ว: `ceoFeedDisplay(p)` ถ้าวันนี้ยังไม่กรอก ดึงบันทึกอาหารล่าสุดก่อนหน้ามาแทน ป้าย "(ล่าสุด)"/"* บางบ่อยังไม่กรอก" บอกให้รู้ว่าไม่ใช่ข้อมูลวันนี้จริง
- **ตัดกราฟเส้น (sparkline) ออกทั้งหมด** — ลบ `ceoSpark()`, canvas ในการ์ดและในแผงรายละเอียด เพราะข้อมูลจุดน้อยกราฟไม่มีประโยชน์จริง
- **ค่าน้ำล่าสุด** เปลี่ยนจาก 3 ค่าตายตัว (pH/DO/Temp เช้า) เป็นวนลูป `PARAMS` โชว์ทุกค่าที่มีการวัดจริงในบันทึกล่าสุด พร้อม ▲/▼/▬ เทียบกับบันทึกก่อนหน้า
- **บั๊กแตะเข้า accordion ไม่ได้ (ต้นทุนสะสม/ค่าน้ำย้อนหลัง/อาหารย้อนหลัง)** — root cause: `.ceo-sheet`/`.ceo-scrim` ใช้ `transform:translateY()` + `opacity` แบบ animated แต่ render ผิดตำแหน่งบนอุปกรณ์จริงจนกดไม่ถึงปุ่ม (ยืนยันด้วยการวัด `getBoundingClientRect` เจอ sheet ค้างอยู่นอกจอ) — **แก้โดยเปลี่ยนกลไกเป็น `display:none/flex` toggle ตรงๆ** ตัด transition/animation ออก แลกความลื่นไหลกับความน่าเชื่อถือ ทดสอบด้วยการยิง `.click()` จริงบน accordion header แล้วเปิด/ปิดได้ถูกต้อง
- **จัดลำดับใหม่**: ข้อมูลบ่อ → ค่าน้ำล่าสุด → การเติบโต (ไม่มีกราฟแล้ว) → accordion 3 หัวข้อ

ผ่านการรีวิว mockup กับพี่ก่อนแก้จริง (ไฟล์ `Mockup และ Prototype/mockup_ceo_dashboard.html` อัปเดตเป็น v5) ทดสอบผ่าน browser: ไม่มี canvas เหลือ, ป้ายอาหารเมื่อวานขึ้นถูกต้อง, ค่าน้ำโชว์ 5 พารามิเตอร์พร้อมลูกศรตรงกับข้อมูลจริง, accordion เปิด/ปิดผ่าน real click event ได้ปกติ, ไม่มี console error
