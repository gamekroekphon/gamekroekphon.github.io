# TawanFarm App — บันทึกการตรวจสอบบัค

ประวัติการแก้บัค/ฟีเจอร์ทั้งหมด (entry #1–287) ย้ายไปเก็บที่ [CLAUDE_HISTORY.md](CLAUDE_HISTORY.md) — เปิดอ่านเมื่อต้องอ้างอิงเหตุผล/บริบทของการแก้ไขในอดีต ไม่โหลดเข้า context อัตโนมัติทุกครั้ง

## หมายเหตุโครงสร้างโค้ด

- `index.html` และ `TawanFarm_App.html` เป็นไฟล์เดียวกัน (ต้อง sync ทั้งคู่ทุกครั้งก่อน commit)
- แต่ละหน้าคือ `<div id="sec-xxx" class="sec">` เปลี่ยนหน้าโดย toggle class
- Deploy ด้วย `git commit` + `push` ขึ้น GitHub Pages โดยตรง — สคริปต์ `push_*.bat` และ `deploy.ps1` เก่าถูกลบทิ้งแล้ว (entry #222)
- ขนาดตัวอักษรทั้งแอปเขียนเป็น `calc(<N>px * var(--ui-scale))` — เวลาเพิ่มโค้ดใหม่ห้ามใส่ `font-size:NNpx` โดดๆ ไม่งั้นปุ่ม ก/ก+/ก++ จะไม่ขยายจุดนั้น (entry #225)
- อายุกุ้ง/DOC ใช้ `docAge(startDate,toDate)` เท่านั้น — **วันลงกุ้งคือวันที่ 1** ห้ามเขียน `dDiff(startDate,...)` ตรงๆ (entry #234)
- อายุของ record รายวันใช้ `recAge(p,r)` — ห้ามอ่าน `r.age` ที่เก็บไว้ตรงๆ (ข้อมูลก่อน #234 เป็นฐานเก่า) และห้ามลบอายุกันหาส่วนต่าง ให้คิดจากวันที่แทน (entry #235)
- ปิดบ่อมี 2 ขั้น: `p.harvestDate` = จับกุ้งแล้ว (หยุดแชร์ค่าไฟ/ค่าแรง) · `costEndDate` ในประวัติ = วันกดปิดรอบจริง (จุดตัดของที่เบิกเข้าบ่อ) — ห้ามเอา `closeDate` ไปคิดขอบเขตต้นทุน (entry #236)
- ห้ามใส่ `display:flex` ที่ `<td>` ตรงๆ — ช่องจะหลุดจากระบบตาราง ไม่ยืดตามความสูงแถว เส้นคั่นจะเหลื่อม — ให้ห่อ `<div class="tbl-act">` ข้างใน (entry #241)
- ต้นทุนลูกกุ้งกับค่าขนส่งแยกกันแล้ว (`costFry` / `costTransport`) — รอบที่ปิดก่อน #241 ยังรวมอยู่ใน `costFry` ห้ามไล่แยกย้อนหลัง (entry #241)
- สำเนาข้อมูลใน `repo` ของเครื่องพัฒนา**ไม่ครบ** (ขาด stockBonus/feedLog/samples/inventory) — **ห้ามสรุปเรื่องข้อมูลจริงของฟาร์มจากสำเนานี้** ต้องถามหรือขอภาพหน้าจอ (entry #243)
- ทุกที่ที่โชว์ค่า "ต่อหน่วย" หรือ "ตัวหาร" — ต้องทดสอบด้วยการ**คูณกลับเทียบยอดรวม**เสมอ ปัดทศนิยมน้อยไปทำให้คลาดหลักพัน (entry #243, #245)
- ราคาลูกกุ้ง "ต่อตัว" หารด้วย `p.stock` (ยอดสั่งซื้อ) — ส่วนที่ถามว่า "มีกุ้งกี่ตัว" ใช้ `pRelease()` (ซื้อ+แถม) ตามเดิม (entry #246)
- อาเรย์ใน `D.*` ที่ sync กับ Firebase **ไม่มีการรับประกันลำดับ** (เป็นลำดับที่ merge ไม่ใช่ลำดับวันที่) — ฟังก์ชันที่ return list ไปแสดงผลต้อง `.slice().sort()` ที่ตัวฟังก์ชันเอง (entry #247)
- ต้นทุนที่ "หารกันทุกบ่อ" (ค่าไฟ/ค่าแรง/อุปกรณ์นอก/น้ำมัน) ฝั่งบ่อต้องเริ่มที่ `p.startDate` เท่านั้น ห้ามใช้ `pondCostStartDate()` (ย้อน 60 วัน) — ต้องตรงกับตัวหาร `activePondCountOnDate()` ไม่งั้นแจกเงินเกินมูลค่าจริงของรายการ · `pondCostStartDate()` ใช้ได้เฉพาะของที่ผูก `pondId` ตรงๆ (entry #248)
- ต้นทุนกลุ่มที่หารกันรายวัน สิ้นสุดที่ `p.harvestDate||today()` เสมอ ทั้งยอดรวมและแถวรายหมวด — แถวรายหมวดในการ์ดต้องบวกได้เท่ายอดรวมจาก `pondRunningCost()` (entry #248)
- การจับกุ้งปิดบ่อมี 2 ขั้น: ขั้น 1 `saveCloseHarvest()` หน้าจับกุ้ง (ผลผลิต/รายได้) · ขั้น 2 `cropModal` (ต้นทุน) — **ห้ามเก็บต้นทุนลง `pendingHarvest`** (`CM_COST_FIELDS` คือบัญชีดำ ห้าม restore) ต้องคำนวณสดตอนเปิดฟอร์มเสมอ (entry #249)
- `hv-amount` (หน้าจับกุ้ง) = ยอดครั้งนี้เท่านั้น → เก็บดิบที่ `pendingHarvest.closeAmount` **ห้ามบวกพาเชียลแล้วเก็บ** · `cm-revenue` (ฟอร์มปิดรอบ) = รายได้ทั้งรอบ — **บวกพาเชียลตอนเปิดฟอร์ม** ยอดจะสดเสมอ (entry #261)
- วันเริ่มนับต้นทุนของที่เบิกเข้าบ่อ = **วันกดปิดรอบก่อน + 1** ไม่มีเพดาน — `POND_PREP_LOOKBACK_DAYS=60` ใช้เฉพาะบ่อที่ไม่มีรอบก่อนหน้าในระบบ (entry #250)
- โซนอยู่ที่ `p.zone` — `ZONE_SEED` ใช้ตั้งค่าเริ่มต้นครั้งเดียวเท่านั้น **ห้ามเอาไปตัดสินโซนตอนคำนวณ** · รายชื่อโซนใช้ `allZones()` ดึงจากบ่อจริง (entry #251)
- ส่วนแบ่งคนเลี้ยงคิด **ทีละรอบจับ** (`caretakerShareRows`) ไซส์ของครั้งไหนใช้อัตราของครั้งนั้น — ห้ามเหมา อัตราไซส์ปิดบ่อ × ผลผลิตรวม (entry #251)
- เกณฑ์ FCR **ไต่ตามอายุกุ้ง** (`fcrTargetFor`) — 1.0 ก่อนสุ่มแรก · 1.1 ตอนสุ่มแรก · +0.1 ทุก 2 สัปดาห์ ไม่มีเพดาน — **ห้ามใช้เลขคงที่เช่น 1.5/1.6/2.0** เพราะ FCR สะสมสูงขึ้นตามอายุโดยธรรมชาติ (entry #256)
- เกณฑ์ผลแล็บของฟาร์ม: **เชื้อในตับ เหลือง 10⁴ หรือ เขียว 10² → ผสมยากิน** — `hepatoYellow`/`hepatoGreen` ต้องแยก case จากของในน้ำ และใช้ `>=` ที่จุดเกณฑ์พอดี (entry #260)
- ต้นทุนอาหารคิดจาก `(กก. ÷ 25) × item.unitPrice` **ไม่เกี่ยวกับยอดคงเหลือ** สต็อกติดลบก็คิดถูก — แต่ถ้า**ไม่ผูก `stockItemId`** จะตกไปใช้**ราคาเฉลี่ยทุกเบอร์** คลาดได้ 20% ห้ามกรองดรอปดาวน์เลือกอาหารด้วยยอดคงเหลือ (entry #262)
- คำนวณย้อนหลังจาก `cropHistory` ต้องอ่าน `partialHarvests` **จากตัวรอบที่ archive ไว้ ไม่ใช่จากบ่อ** — อ่านจากบ่อจะได้พาเชียลรอบปัจจุบันมาปนทุกรอบเก่า (entry #255)
- ค่าจับกุ้ง = f(**ผลผลิตรวมทั้งรอบ** = ปิดบ่อ + เกรดพิเศษ + พาเชียล) — เกณฑ์ 4 ตันก็เทียบยอดรวมนี้ · จ่ายทีมชุดจับ ไม่ได้หักจากยอดที่แพโอน (entry #258)
- พาเชียลมี 2 เส้น: `PARTIAL_YIELD_TARGET=4000` = เริ่มหาราคา (เหลือง) · `PARTIAL_YIELD_CEILING=5000` = เพดาน บ่อแน่นแล้ว (แดง) — แถบความคืบหน้าวัดเทียบเพดาน (entry #254)
- ทุกที่ที่หารด้วยอัตราที่มาจากข้อมูลผู้ใช้ (เช่น PDG) ต้องมี**เพดานความสมเหตุสมผลครอบ** — ตัวหารเล็กแล้วผลลัพธ์ระเบิด (entry #254)
- ส่วนแบ่งพนักงานประจำ = รวมเฉพาะคนที่มีอัตราในโซนของบ่อนั้น · ใช้อัตรา **ณ วันปิดบ่อ** (`staffAsOf`) ไม่ใช่อัตราล่าสุด (entry #251)
- พนักงานมี **เงินเดือน 0 ได้** (นักวิชาการ CP — ฟาร์มไม่จ่ายเงินเดือน แต่ได้ส่วนแบ่ง) — ห้ามเช็ค `!salary` เพราะจับ 0 ด้วย · เงินเดือน 0 ต้องมีส่วนแบ่งอย่างน้อย 1 โซน (entry #253)
- `harvestResult.costMed` (และช่องต้นทุนอื่นตอนปิดรอบ) เก็บเป็น **ก้อนเดียวและแก้ด้วยมือได้** — ห้ามเชื่อว่าเป็นผลรวมของรายการเบิก ถ้าต้องแจกแจงให้ re-derive จาก `D.inventoryLog` แล้ว**บอกส่วนต่างเป็นแถว/บรรทัดแยก ห้ามเกลี่ยกลบ** · ยอดรวมต้องคิดจากค่าที่บันทึกไว้เสมอ ไม่ใช่ผลบวกหมวดย่อย (entry #263, #264)
- แจกแจงต้นทุนของรอบที่ปิดแล้ว ใช้ **2 หน้าต่างวันคนละแบบ**: ของที่ผูก `pondId` ใช้ `closedCropCostWindow()` (วันปิดรอบก่อน+1) · ของแชร์ทุกบ่อใช้ `closedCropSharedWindow()` (วันลงกุ้ง) และต้องหาร `activePondCountOnDate()` ด้วย — ใช้ช่วงเดียวกันหมดหรือลืมหาร ตัวเลขจะไม่ตรงกับที่บันทึกตอนปิดรอบ (entry #264)
- `closeSummaryHtml()` ใช้ร่วมกันทั้ง preview บนจอ (`compact:true`) และตัว PDF (`compact:false`) — **ของที่กดได้/โต้ตอบได้ใส่ได้เฉพาะฝั่ง preview** PDF กดไม่ได้ ลูกศร/ปุ่มค้างในไฟล์คือขยะ · รายการสินค้าย่อยไม่ลง PDF โดยตั้งใจ (ยาวเกินไป — พี่สั่ง entry #265)
- ปีของรอบเลี้ยงใช้ `histRecYear(c)` เท่านั้น (คิดจาก `closeDate` คืนเป็น พ.ศ.) — **ห้ามอ่าน `c.cropYear` ตรงๆ เพื่อแบ่ง/เทียบปี** ข้อมูลจริงมีทั้ง พ.ศ. (seed/inject เก่า) และ ค.ศ. (รอบที่ปิดผ่านแอป) ปนกัน ปีเดียวกันจะแตกเป็นสองกลุ่ม (entry #266)
- **ห้าม `innerHTML` ทับการ์ดที่มี `<canvas>` อยู่ข้างใน** — canvas หายถาวร กราฟไม่กลับมาอีกจนกว่าจะรีโหลดหน้า ให้ซ่อน/แสดง element ข้อความแยกแทน (entry #266)
- หน้ารอบเลี้ยงแบ่งปีด้วย `histRecYear()` (ปีที่จับ) **ทุกการ์ด** — การ์ดสรุป/การ์ดประวัติ/ปุ่มสถิติ ต้องได้ชุดรอบเดียวกันเสมอ · รอบที่ยังไม่ปิดโผล่เฉพาะตอนดูปีปัจจุบัน (`selYear===yearBE(curY)`) เพราะยังไม่มีวันจับให้ยึด (entry #267)
- ดรอปดาวน์ `ch-cycle` (การ์ดดูข้อมูลย้อนหลัง) เรียงตามวันจับได้ แต่ **`value` ต้องเป็น index จริงใน `p.cropHistory` เสมอ** — `getCloseSummary(p,idx)` อ้างอิง index นี้ตรงๆ ห้ามส่งลำดับที่เรียงใหม่ไปแทน (entry #267)
- หน้ารอบเลี้ยงมีช่องเลือกปี **ช่องเดียว** (`#cr-year` แถบบนสุด) คุมทั้งการ์ดสรุปและการ์ดประวัติ ผ่าน `inYear(c)` — รองรับ `selYear==='all'` ทุกจุดที่กรอง/นับ/ตั้งป้าย ห้ามเพิ่มช่องปีประจำการ์ดขึ้นมาอีก (entry #268)
- แถวรอบในการ์ดสรุปเป็น grid 5 คอลัมน์ — ถ้าต้องทำให้กดได้ **ห่อด้วย `<div style="display:contents">` เท่านั้น** div ธรรมดาจะทำให้คอลัมน์ของทุกรอบในบ่อเดียวกันไม่ตรงกัน (entry #268)
- ต้นทุนอาหารไม่ได้มาจาก `D.inventoryLog` เหมือนหมวดอื่น — คิดจาก `feedLog` ของรอบ (กก.÷25 × ราคา/กระสอบ) · ทุกจุดที่แจกแจงอาหารต้องเดินสูตรให้ตรงกับ `pondFeedCost()` **ทุกกิ่ง รวมกิ่งที่ผูกสินค้าไม่ได้ → ราคาเฉลี่ยทุกเบอร์** และต้องพิสูจน์ด้วยว่า Σ รายเบอร์ = `pondFeedCost()` (entry #269)
- **ห้ามใช้คำว่า "เบิก" กับอาหาร** — อาหารบันทึกที่หน้า "อาหาร & FCR" ไม่ใช่หน้าสต็อก · `plDetailHTML(items,shown,opt)` มี `opt` ไว้เปลี่ยนคำเรียกให้ตรงกับที่มาของข้อมูล (entry #269)
- สต็อกติดลบได้จาก `applyFeedStockDeduction()` ทางเดียว (หน้าสต็อกตัดด้วย `Math.max(0,…)`) — **ห้ามใส่พื้น 0 ให้มัน** ติดลบคือหลักฐานว่าค้างกรอกรับเข้า ถ้าตัดทิ้งจะไม่มีใครรู้ว่าต้องไปตามกรอก · เตือนของติดลบต้องไม่พึ่ง `minQty` (ฟาร์มไม่เคยตั้ง) ใช้ `negStockItems()` (entry #270)
- คำเตือนตอนกรอกข้อมูลต้อง **ไม่บล็อกการบันทึก** และต้องบอกให้ชัดว่าอะไรผิดอะไรไม่ผิด — สต็อกติดลบ = ต้นทุนถูก/ยอดคงเหลือผิด (แก้ย้อนหลังได้) · ไม่เลือกเบอร์อาหาร = ต้นทุนผิดจริง (entry #262, #270)
- หน้า `กัน` (sec-kan) นำเข้าประวัติรอบเลี้ยงจากชีต "แผนลงลูกกุ้ง" — **ต้องผ่านขั้นตรวจก่อนเขียนเสมอ** · เช็คซ้ำด้วย "บ่อ + วันลงกุ้ง" · แถวซ้ำไม่ติ๊กให้อัตโนมัติ · ชีตไม่มีวันปิดบ่อ จึงเก็บ `closeDate:null` แล้วตั้ง `cropYear` — ทำให้รอบที่นำเข้า**ไม่โผล่ในหน้ากำไร-ขาดทุน** ซึ่งถูกแล้วเพราะชีตไม่มีต้นทุน (entry #271)
- โครงชีตแผนลงลูกกุ้ง: **แถวหัวกลุ่มถือชื่อโรงเพาะฟักของบ่อที่อยู่ใต้มัน** ส่วนแถวบ่อใส่ชื่อบ่อซ้ำในช่อง "กุ้งมาจาก" · เซลล์ยอดนับบางแถวเป็น `(102%) 491,400` ต้องตัดวงเล็บก่อนแปลงเลข · ช่อง "รอด" ในชีตคิดจาก**ฐานยอดสั่ง** ตรงกับ `survivalRateOrder` (entry #271)
- หน้า `กัน` เก็บลง `D.kanDraft` เท่านั้น — **ห้ามเขียน `p.cropHistory` จากหน้านี้** · ถ้าจะทำปุ่มย้ายเข้าแอปหลักวันหลัง ต้อง **merge เติมเฉพาะช่องที่ชีตมี ห้าม replace ทั้ง record** ไม่งั้นต้นทุน/วันปิดบ่อ/feedLog/labResults ของรอบนั้นหายหมด (entry #271→#272)
- เพิ่มคีย์ใหม่ระดับบนใน `D` ต้องไปเติมใน `mergeRemote()` ด้วยเสมอ — มันเริ่มจาก `cln(local)` แล้ว merge เฉพาะคีย์ที่ระบุไว้ ของจากอีกเครื่องจะหายเงียบๆ · และ id ของแต่ละรายการต้องคงที่ข้ามเครื่อง + ลบแล้วต้อง push เข้า `deletedIds` (entry #272)
- ตารางกรอกข้อมูลที่แก้ในช่องได้ (หน้ากัน) — **ห้าม re-render ทั้งตารางตอน onchange** แก้เฉพาะโมเดลแล้ว debounce save ไม่งั้นโฟกัสเด้งออกจากช่องที่กำลังพิมพ์ · วาดใหม่เฉพาะตอนเพิ่ม/ลบแถว (entry #273)
- ช่องกรอกที่ต้องใช้บน iPad ต้องมี `@media (pointer:coarse)` ตั้ง **font-size:16px** ด้วย — ต่ำกว่านั้น Safari จะซูมหน้าจอเองตอนแตะช่อง · ตารางกว้างให้ล็อกคอลัมน์แรกด้วย `position:sticky;left:0` (entry #273)
- หน้า `กัน` = **ตารางเปล่า ไม่ตีความข้อมูลใดๆ** — เก็บตามที่พิมพ์ทุกตัวอักษร · วางจากชีตลงช่องตรงๆ · ห้ามใส่ระบบเดาโครง (แยกบล็อก/จับคู่บ่อ/แปลงวันที่/คำนวณให้) กลับเข้าไปอีก พี่บอกว่าของแบบนั้น "ทำงานยาก ปรับแต่งไม่ได้ ข้อมูลที่ดึงมาสับสน" (entry #271→#274)
- `D.kanDraft` ต้องเป็น **อาเรย์ของช่อง** (id คงที่จากพิกัด) ไม่ใช่ออบเจ็กต์ก้อนเดียว — `mergeArr()` คืน local ทันทีถ้า remote ไม่ใช่อาเรย์ แปลว่าที่กรอกบน iPad จะทับของบนคอมหายทั้งก้อน (entry #274)
- หน้า `กัน` มีหลายชีต — ชีตกับช่องอยู่ใน `D.kanDraft` อาเรย์เดียวกัน แยกด้วย `kanIsSheet()` · ช่องของแต่ละชีตใช้คำนำหน้าคนละแบบผ่าน `kanPfx()` (ชีตแรก `kc_r_c` · ชีตอื่น `kc<n>_r_c`) — **ห้ามเปลี่ยนไปเก็บเป็นออบเจ็กต์ซ้อนชั้น** และ `id` ของชีตแรกต้องเป็น `'kgrid'` ตลอดไป ไม่งั้นของที่กรอกไว้ก่อน #275 หายหมด (entry #275)
- แท็บชีตที่เปิดค้าง (`_kanCur`) เก็บที่ `localStorage` ของแต่ละเครื่อง **ห้าม sync ขึ้น Firebase** — กันบน iPad กับพี่บนคอมต้องเปิดคนละชีตพร้อมกันได้ · ลบชีตต้องดัน id ชีต **และทุกช่องของมัน** เข้า `deletedIds` ไม่งั้นฟื้นกลับมาจากอีกเครื่อง (entry #275)
- ซูมตารางหน้า `กัน` ใช้ตัวแปร `--kz` ตั้งที่ `#kan-grid` — **ห้ามใช้ `transform:scale()`** จะทำให้ `position:sticky` ของหัวแถว/หัวคอลัมน์เพี้ยนและตัวอักษรเบลอ · ทุกมิติในตารางต้องคูณให้ครบ (font · padding · width ของ `.kan-in` · min-width ของ `.kan-rh` · **ขนาดตัวอักษรรายช่อง `x.fs`** · และ `font-size:16px` ในกฎ `pointer:coarse`) และต้องคูณ**ทับ** `--ui-scale` ไม่ใช่แทนที่ (entry #276)
- ระดับซูม (`_kanZoom`) เป็นการตั้งค่าการมองของแต่ละเครื่อง เก็บที่ `localStorage` **ห้าม sync** เหมือนแท็บที่เปิดค้าง · `kanApplyZoom()` ต้องถูกเรียกท้าย `renderKanGrid()` ทุกครั้ง ไม่งั้นซูมหลุดตอนเพิ่มแถว/สลับชีต (entry #276)
- การเลือกในตารางหน้า `กัน` เป็น **อาเรย์ของสี่เหลี่ยม** (`_kanSels`) แก้ผ่าน `kanApplyPick(rect,mod,axis)` ทางเดียว · `kanSelRange()` ต้องตัดช่องที่ทับกันออก (แถวกับคอลัมน์ตัดกันเสมอ) ไม่งั้นทาสีซ้ำและตัวเลขที่โชว์เกินจริง (entry #277)
- **`onfocus` ของ `<input>` ยิงก่อน `onclick` เสมอ** — อะไรที่ต้องอ่านปุ่ม Shift/Ctrl ตอนคลิกช่องในตาราง ต้องดักที่ `onmousedown` ของ `<td>` ไม่ใช่ `onclick` ไม่งั้นช่วงที่เลือกถูกรีเซ็ตไปก่อนแล้ว · และถ้าจะ `preventDefault()` ต้อง `blur()` ช่องที่กำลังพิมพ์ **ก่อน** ไม่งั้น `onchange` ไม่ยิง ค่าที่พิมพ์ค้างหาย (entry #277)
- ปุ่มสลับรูปแบบ (ตัวหนา ฯลฯ) ต้อง **ตัดสินครั้งเดียวจากทั้งช่วงที่เลือก** (`!cells.every(...)`) ห้ามสลับทีละช่อง — ช่องที่ตั้งไว้ก่อนจะกลับทางสวนทางกับที่เห็นบนปุ่ม (entry #277)
- ของที่ผูกกับ**ลำดับ event ของเบราว์เซอร์** ต้องทดสอบด้วยการ**คลิกจริง** ห้ามพิสูจน์ด้วยการเรียกฟังก์ชันตรงๆ — #274 บันทึกว่า Shift ผ่านทั้งที่ไม่เคยทำงานเลย เพราะทดสอบด้วยการเรียก `kanSelect()` เอง (entry #277)
- เหตุการณ์ `copy`/`cut`/`paste` **ไม่ยิงถ้าไม่มีข้อความถูกเลือกอยู่จริง** (พิสูจน์แล้ว: โฟกัส `div[tabindex]` เฉยๆ กด Ctrl+C แล้ว keydown ยิงแต่ copy ไม่ยิง) — ตารางที่เลือกช่องด้วยไฮไลต์ของตัวเองต้องมี **textarea ซ่อน** (`position:fixed;left:-9999px` **ห้าม `display:none`**) ถือโฟกัสไว้ให้เหตุการณ์ `paste` ยิงได้ · **ข้อความในบัฟเฟอร์เติมตอนกด Ctrl+C เท่านั้น** (`kanDoCopy`) ห้ามเติมทุกครั้งที่ช่วงเปลี่ยน — เลือกทั้งตารางแล้วกดลูกศร = สร้างข้อความหลายพันช่องทุกครั้งที่กด (entry #278→#280)
- คีย์ลัดทุกตัวต้องเช็ค **`e.code` (`KeyC`/`KeyX`/`KeyA`)** ไม่ใช่ `e.key` — พอสลับแป้นเป็นภาษาไทย `e.key` จะกลายเป็น `แ` `ป` `ฟ` คีย์ลัดตายหมด (entry #278)
- บนจอสัมผัส (`pointer:coarse`) **ห้ามโฟกัส textarea ซ่อน** — คีย์บอร์ด iPad จะเด้งขึ้นทุกครั้งที่แตะหัวแถว · ผลข้างเคียงคือ iPad ที่ต่อคีย์บอร์ดจะใช้คีย์ลัดไม่ได้ ถ้าจะรองรับต้องเปลี่ยนเงื่อนไขนี้ (entry #278)
- ทุกทางที่ **เขียนข้อมูล** ในหน้ากันต้องมี `APP_ROLE==='admin'` ครบทุกเส้น รวมถึงเส้นที่มาจากคีย์บอร์ด (Enter ต่อแถวอัตโนมัติเคยหลุดให้ staff งอกแถวได้ — entry #278)
- สูตรในหน้า `กัน` = ช่องที่ **ขึ้นต้นด้วย `=` เท่านั้น** เก็บดิบใน `x.v` แล้วคิดตอนแสดงผล · **ห้ามเดาว่าอะไรเป็นสูตรจากหน้าตาข้อมูล** และ **ห้ามใช้ `eval()`** (รันอะไรก็ได้ที่พิมพ์ลงช่อง และบอกไม่ได้ว่าพังตรงไหน) — ใช้ตัวแยกคำ+ไล่ไวยากรณ์ที่เขียนเอง มี `_kanBusy` กันอ้างวนซ้ำ (entry #279)
- **ช่องที่แสดงผลไม่ตรงกับค่าดิบ (ช่องสูตร) ห้ามให้ `onblur` เขียน `el.value` กลับลงโมเดล** — แค่แตะเข้า-ออกโดยไม่แก้อะไร สูตรจะถูกทับด้วยตัวเลขที่มันคำนวณได้เองถาวร · การบันทึกเป็นหน้าที่ของ `onchange` ซึ่งยิงเฉพาะตอนผู้ใช้แก้ข้อความจริง (entry #279)
- เทียบว่า "ของที่วางคือของที่เราคัดลอกเอง" **ด้วยข้อความอย่างเดียวไม่พอ** — ต้องมี `_kanClipStale` ที่ตั้งเมื่อ `window blur` เพราะการไปคัดลอกจากแอปอื่นต้องออกจากหน้าต่างนี้เสมอ ไม่งั้นข้อความที่บังเอิญตรงกันจะได้สูตร/รูปแบบเก่ากลับมาแทนค่าที่วางจริง (entry #279)
- อ่านช่องหน้า `กัน` ผ่าน `kanIdx()` (ดัชนี id→ช่อง + รายชื่อชีตที่เรียงแล้ว) เท่านั้น — สร้างใหม่เองเมื่อ `D.kanDraft` ถูกแทนทั้งก้อนหรือความยาวเปลี่ยน · **ห้ามกลับไปใช้ `find()` ทั้งอาเรย์** และ**ห้ามเรียกฟังก์ชันที่วนทั้งอาเรย์ข้างใน callback ของ `find/filter`** — #275 ทำแบบนั้นจน `kanGet()` กลายเป็น O(N²) อ่านช่องเดียว 97 ms ที่ 1,617 ช่อง เข้าการ์ด/สลับแท็บ/คัดลอกวาง ค้างหมด (entry #280)
- ห่อโค้ดเดิมด้วย `try{...}finally{}` ต้องเช็คว่า `const` ที่ประกาศข้างในไม่ถูกใช้ต่อหลัง `finally` — `renderKanGrid` พังด้วย `g is not defined` เพราะเหตุนี้ (entry #279)
- iPad: `kanHwKeySignal()` ตรวจว่ามีคีย์บอร์ดจริงต่ออยู่จากปุ่มที่คีย์บอร์ดบนจอส่งไม่ได้ (Ctrl/Cmd · ลูกศร · Esc · Tab) แล้วค่อยเปิดบัฟเฟอร์คลิปบอร์ด · **ห้ามเก็บผลลง localStorage** ถอดคีย์บอร์ดแล้วต้องกลับเป็นโหมดจอสัมผัสเอง (entry #279)
- สร้างช่องใหม่ในหน้า `กัน` ต้องผ่าน `kanGetOrMake()` เท่านั้น — มันเติมดัชนีทีละตัว · ถ้า push ลง `D.kanDraft` ตรงๆ ดัชนีจะสร้างใหม่ทั้งก้อน วางทีละพันช่องก็สร้างใหม่พันรอบ · **ห้ามแก้ `D.kanDraft` แบบ `splice`/`pop`** (ความยาวอาจเท่าเดิมแต่ของข้างในเปลี่ยน ดัชนีจะค้างของเก่า) — ลบให้สร้างอาเรย์ใหม่แล้วแทนทั้งก้อนเหมือน `kanClearAll`/`kanDelSheet` (entry #280)
- วางข้อมูลลงหน้า `กัน` **ช่องว่างที่ยังไม่มีอยู่ ไม่ต้องสร้างรายการ** — ชีตจริงว่างราว 40% ถ้าสร้างหมด ข้อมูลบวมเกือบเท่าตัว ทุกการบันทึก/ซิงก์ต้องแบกไปด้วย (entry #280)
- ทดสอบความเร็วต้องใช้ **ข้อมูลขนาดจริง** (ชีตแผนลงลูกกุ้ง ~150×20 หลายชีต) — ที่ 30 ช่องบัค O(N²) มองไม่เห็นเลย เพิ่งระเบิดตอนกันวางชีตจริง (entry #280)
- ลูกศรในตารางหน้า `กัน` มี 2 โหมด (`_kanEditing`): เข้าช่องด้วยคลิก/ลูกศร/Enter = โหมดเลื่อน ลูกศรทุกทิศย้ายช่อง · พิมพ์/ดับเบิลคลิก/F2 = โหมดแก้ ขึ้น/ลงยังย้าย ซ้าย/ขวาเลื่อนเคอร์เซอร์จนชนขอบ · ย้ายช่องต้องผ่าน `kanMoveCell()` (blur ก่อนให้ `onchange` บันทึก) และเลื่อนจอด้วย `kanScrollTo()` ซึ่งเผื่อหัวแถว/หัวคอลัมน์ที่ตรึงไว้ — ห้ามใช้ `scrollIntoView` ตรงๆ ช่องจะไปหลบใต้หัวตาราง (entry #281)
- กฎมือถือ `@media(max-width:768px){.card table{display:block;overflow-x:auto}}` **โดนทุกตารางใน `.card`** — ตารางที่มีกรอบเลื่อนของตัวเองแล้ว (เช่น `.kan-g`) ต้องยกเว้นด้วย `.card table.xxx{display:table;overflow:visible}` ไม่งั้นเกิดกรอบเลื่อนซ้อนสองชั้น หัวที่ตรึงไว้หลุด · **768px = iPad แนวตั้งพอดี** ทดสอบจอแคบต้องลองที่ ≤768 เสมอ (entry #281)
- เครื่องมือทดสอบในเบราว์เซอร์: ชื่อปุ่มต้องเป็น `ArrowRight` ไม่ใช่ `Right` (ส่งผิดชื่อ = ปุ่มไม่ถึงหน้าเลย ดูเหมือนโค้ดพัง) · และปุ่มที่ส่งไม่ทำ default action ของเบราว์เซอร์ (เคอร์เซอร์ในช่องไม่ขยับจริง) — กรณีที่พึ่งตำแหน่งเคอร์เซอร์ต้องตั้ง `setSelectionRange` เองก่อนกด (entry #281)
- **รูปภาพห้ามเก็บใน `D`** — ทั้งแอปเซฟ `D` เป็น JSON ก้อนเดียว (`dbRef.set({json})`) ทุกการกดบันทึกอะไรก็ตามจะส่งรูปทุกใบขึ้นเน็ตซ้ำ · รูปบิลรับของเก็บแยกที่ RTDB path `tawan_bill_photos/<billId>` (`p0..pN` เป็น JPEG dataURL ย่อด้านยาว 1600px) โหลดเมื่อเปิดดูเท่านั้น · path ใหม่ใน RTDB **ต้องเพิ่มใน Firebase Rules ด้วย** (กฎเดียวกับ `tawan_app`: `auth != null && root.child('users').child(auth.uid).exists()`) — เพิ่มแล้ว 17 ก.ย. 2569 (entry #282)
- บิลรับของ (`D.stockBills`): staff ส่ง = `status:'wait'` **สต็อกยังไม่เปลี่ยน** · admin อนุมัติ (`approveBill`) ถึงสร้างรายการ `type:'in'` + เพิ่ม `item.qty` · รายการรับเข้าจากบิลเก็บ `unitCost` (ราคาจากบิล) และ `billId` · **ช่อง "อัปเดตราคาในระบบ" ต้องปิดเป็นค่าเริ่มต้น** เพราะต้นทุนเข้าบ่อคิดจาก `item.unitPrice` ปัจจุบัน แก้ราคา = ต้นทุนที่เคยเบิกไปแล้วเปลี่ยนย้อนหลัง · staff ต้องไม่เห็นราคาในหน้าถ่ายบิล (entry #282)
- ร่างที่กรอกค้างในหน้าต่าง (`_billCap` ของ staff · `_billRev` ของ admin) **ต้องแยกจาก `D`** และตอนกดยืนยันต้องหาบิลจาก `D` ใหม่ด้วย id แล้วเช็กสถานะซ้ำ — ข้อมูลจากอีกเครื่องเข้ามา `D` ถูกแทนทั้งก้อน · อนุมัติจากร่างเก่าที่ค้างอยู่ต้องไม่รับของเข้าสต็อกซ้ำ (entry #282)
- ปุ่มที่ส่งข้อมูลขึ้นเน็ต **ห้าม `await save()`** ก่อนปิดหน้าจอ — สัญญาณอ่อนที่ฟาร์มแล้วปุ่มค้าง "กำลังส่ง…" · `save()` เขียน localStorage ก่อนเสมอแล้วค่อยซิงก์เบื้องหลัง · ส่วนที่ต้องรอจริง (อัปโหลดรูป) ใส่ timeout และกดส่งซ้ำต้องใช้ id เดิม ไม่งั้นรูปที่ค้างส่งกลายเป็นขยะ (entry #282)
- **แอปนี้ไม่มีกฎ `[hidden]{display:none}` ส่วนกลาง** — class ที่ตั้ง `display` เอง (flex/grid) แอตทริบิวต์ `hidden` จะไม่มีผล ต้องเขียน `.xxx[hidden]{display:none!important}` คู่กันเสมอ · `.bill-zoom` เคยคลุมจอดำตลอดเวลาเพราะเหตุนี้ ทดสอบด้วยโค้ดจับไม่ได้ ต้องดูภาพหน้าจอ (entry #282)
- บนจอสัมผัสแอปบังคับปุ่มขั้นต่ำ 44×44px — ปุ่มเล็กบนรูปย่อ ให้คงพื้นที่แตะ 44px แต่ทำส่วนที่มองเห็นเป็น `<span>` เล็กข้างใน (entry #282)
- ทางเข้าบิลรับของ: **staff** มีปุ่ม 📷 ทั้งหน้าภาพรวมและหน้าสต็อก · **admin มีเฉพาะในหน้าสต็อก** (ถ่ายบิล + บิลรอตรวจ + บิลทั้งหมด) ห้ามใส่ปุ่มบิลที่หน้าภาพรวมของ admin (พี่สั่ง) — บิลค้างให้รู้จากป้ายแดงที่เมนูสต็อก · admin ถ่ายบิลเสร็จแล้วเปิดหน้าตรวจของบิลนั้นต่อทันที แต่ยังต้องกดอนุมัติ ห้ามให้เข้าสต็อกอัตโนมัติ (entry #283)
- โปรเจกต์นี้มี **Cloud Functions ใช้งานจริงอยู่** (`functions/index.js`, แผน Blaze เปิดแล้ว, region `asia-southeast1`): `lineWebhook` ตอบคำถามผ่าน LINE · `billNotify` แจ้งเตือนบิลรับของ · **`dailySummary` (สรุปเช้า 10:00) ยกเลิกแล้ว 18 ก.ย. 2569 ตามคำสั่งพี่** — ฟังก์ชันช่วยคำนวณที่สรุปเช้าเคยใช้ยังอยู่ ห้ามลบ เพราะ `lineWebhook` ใช้ร่วม (entry #284)
- **ค่าลับอยู่ใน `functions/.env` เท่านั้น** (LINE token/secret/group + Anthropic key) — ถูก gitignore ไว้ ตรวจแล้วไม่เคยหลุดขึ้น git · ห้าม commit ห้ามพิมพ์ค่าออกมาในแชต (entry #284)
- **`firebase_rules.json` ในรีโปคือกฎที่จะถูก deploy ทับของจริง** — แก้กฎใน Firebase Console เมื่อไหร่ ต้องอัปเดตไฟล์นี้ให้ตรงกันทันที ไม่งั้น `firebase deploy` ครั้งถัดไปจะลบกฎนั้นทิ้ง (เคยเกือบทำ `tawan_bill_photos` หาย — entry #284)
- แจ้งเตือนเข้าไลน์: แอปเขียน `/tawan_events/<id>` = `{type,billId,actorUid,at}` **ห้ามส่งข้อความสำเร็จรูปจากฝั่งแอป** — เซิร์ฟเวอร์ต้องอ่านบิลจริงจาก `tawan_app` แล้วประกอบข้อความเอง (ไม่งั้นใครล็อกอินแอปได้ก็ยิงข้อความอะไรก็ได้เข้ากลุ่มไลน์) · ต้องเช็คสถานะบิลให้ตรงชนิดเหตุการณ์ก่อนส่ง · **ห้ามใส่ราคา/ยอดเงิน** เพราะกลุ่มมี staff อยู่ (entry #284)
- ส่งเหตุการณ์ต้องรอ `save()` สำเร็จก่อน (`billSaveThenEmit`) ไม่งั้นเซิร์ฟเวอร์อ่านบิลไม่เจอ · แต่ห้ามบล็อกหน้าจอ และถ้าเขียนเหตุการณ์ไม่สำเร็จ งานหลักต้องไม่พัง (entry #284)
- **กลุ่มไลน์ที่รับแจ้งเตือนเก็บใน RTDB `/tawan_line_config`** (ไม่ใช่ `LINE_GROUP_ID` ใน `.env` ซึ่งเหลือเป็นค่าสำรอง) · ตั้ง/เปลี่ยนด้วยการพิมพ์ `สวัสดีตะวัน ตั้งกลุ่มแจ้งเตือน` **ในกลุ่มนั้น** ไม่ต้อง deploy · **ห้ามตั้งกลุ่มอัตโนมัติตอนบอทถูกเชิญ** (ใครเชิญบอทเข้ากลุ่มอื่นจะดึงแจ้งเตือนของฟาร์มไปได้) · node นี้ไม่อยู่ใน `firebase_rules.json` แอปฝั่งหน้าเว็บจึงอ่าน/เขียนไม่ได้ (entry #285)
- บอทถูกเตะ/กลุ่มถูกลบ LINE จะตอบ push ด้วย 400 "Failed to send messages" · เช็คแบบอ่านอย่างเดียวได้ด้วย `GET /v2/bot/group/<id>/summary` (404 = บอทไม่อยู่ในกลุ่ม) และโควตาที่ `/v2/bot/message/quota(/consumption)` — ข้อความเข้ากลุ่มนับตามจำนวนสมาชิก (entry #285)
- **หน้าสถิติจับกุ้ง (`sec-harveststat`) คำนวณสดจาก `hsHarvestRows()` เท่านั้น** — ห้ามกลับไปเก็บยอดรายปี/รายเดือนแยก หรือดึงจาก Google Sheets · ปี 60–67 ที่ไม่มีรายการในแอปอยู่ใน `HV_OLD` ใช้กับกราฟอย่างเดียว · การเพิ่ม/เลื่อนวันของรอบในประวัติเปลี่ยนตัวหารค่าไฟ/ค่าแรงของบ่ออื่นในช่วงเดียวกันเสมอ (entry #286)
- บ่อ "รอทำสรุป" (จับปิดบ่อแล้ว ยังไม่กดปิดรอบ) ข้อมูลอยู่ที่ `p.pendingHarvest` ไม่ใช่ `cropHistory` — **ทุกหน้าที่สรุปการจับจาก cropHistory ต้องคิดถึงสถานะนี้ด้วย** หน้าสถิติจับกุ้งดึงมาแสดงพร้อมป้าย "รอปิดรอบ" แล้ว (entry #287)
